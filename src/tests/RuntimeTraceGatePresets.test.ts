import { describe, expect, it } from "vitest";
import {
  createNativeHitTraceArtifact,
  createNativeWhiffTraceArtifact,
  createSyntheticImportedCommonGetHitTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarAnimTypeTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarDownRecoverTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarFallEnvShakeTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarFallMetadataTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarFallTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarGuardKillTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarGuardTimingTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarGuardedTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarHitCountHitIdChainIdTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarIsBoundTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarSnapTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarTypeTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarVelocityTraceArtifact,
  createSyntheticImportedCustomStateGetHitVarYAccelTraceArtifact,
  createSyntheticImportedCustomStateTraceArtifact,
  createSyntheticImportedTargetOwnedCustomStateTraceArtifact,
  createSyntheticImportedDefaultFallAirRecoveryVelocityTraceArtifact,
  createSyntheticImportedDefaultFallGroundRecoveryPriorityTraceArtifact,
  createSyntheticImportedDefaultFallGroundRecoveryTraceArtifact,
  createSyntheticImportedDefaultFallRecoveryInputTraceArtifact,
  createSyntheticImportedDefaultFallRecoveryInputPriorityTraceArtifact,
  createSyntheticImportedDefaultFallRecoveryThresholdTraceArtifact,
  createSyntheticImportedDefaultFallRecoveryTickOrderTraceArtifact,
  createSyntheticImportedDefaultFallRecoveryTooEarlyTraceArtifact,
  createSyntheticImportedDefaultFallRecoveryTraceArtifact,
  createSyntheticImportedDefaultFallOfficialAirRecoveryTraceArtifact,
  createSyntheticImportedDefaultFallOfficialGroundRecoveryTraceArtifact,
  createSyntheticImportedFallTraceArtifact,
  createSyntheticImportedFallDefenceUpTraceArtifact,
  createSyntheticImportedHitFallCanRecoverReadyTraceArtifact,
  createSyntheticImportedHitFallCanRecoverTraceArtifact,
  createSyntheticImportedHitFallFalseTraceArtifact,
  createSyntheticImportedHitFallRecoveryInputPriorityTraceArtifact,
  createSyntheticImportedHitFallRecoverFalseTraceArtifact,
  createSyntheticImportedHitFallRecoverTrueTraceArtifact,
  createSyntheticImportedGetHitVarAnimTypeTraceArtifact,
  createSyntheticImportedGetHitVarDownRecoverTraceArtifact,
  createSyntheticImportedGetHitVarFallDefenceUpTraceArtifact,
  createSyntheticImportedGetHitVarFallCountTraceArtifact,
  createSyntheticImportedGetHitVarFallEnvShakeTraceArtifact,
  createSyntheticImportedGetHitVarFallMetadataTraceArtifact,
  createSyntheticImportedGetHitVarFallRecoverTraceArtifact,
  createSyntheticImportedGetHitVarAirGuardKillTraceArtifact,
  createSyntheticImportedGetHitVarAirGuardHitShakeTimeTraceArtifact,
  createSyntheticImportedGetHitVarCrouchGuardKillTraceArtifact,
  createSyntheticImportedGetHitVarCrouchGuardHitShakeTimeTraceArtifact,
  createSyntheticImportedGetHitVarGuardHitShakeTimeTraceArtifact,
  createSyntheticImportedGetHitVarGuardTimingTraceArtifact,
  createSyntheticImportedGetHitVarGuardedTraceArtifact,
  createSyntheticImportedGetHitVarDamageTraceArtifact,
  createSyntheticImportedGetHitVarKillTraceArtifact,
  createSyntheticImportedGetHitVarGuardKillTraceArtifact,
  createSyntheticImportedGetHitVarHitIdChainIdTraceArtifact,
  createSyntheticImportedGetHitVarHitCountTraceArtifact,
  createSyntheticImportedHitCountPersistTraceArtifact,
  createSyntheticImportedGetHitVarSnapTraceArtifact,
  createSyntheticImportedGetHitVarHitShakeTimeTraceArtifact,
  createSyntheticImportedGetHitVarHitTimeTraceArtifact,
  createSyntheticImportedGetHitVarVelocityTraceArtifact,
  createImportedDefaultFallGroundRecoveryTraceArtifact,
  createImportedDefaultFallRecoveryInputTraceArtifact,
  createImportedDefaultFallRecoveryThresholdTraceArtifact,
  createImportedDefaultFallRecoveryTooEarlyTraceArtifact,
  createImportedDefaultFallRecoveryTraceArtifact,
  createSyntheticImportedDefaultAirFallRecoveryInputTraceArtifact,
  createSyntheticImportedDefaultAirFallRecoveryTooEarlyTraceArtifact,
  createSyntheticImportedDefaultAirGroundImpactTraceArtifact,
  createSyntheticImportedDefaultAirLieDownRecoveryTraceArtifact,
  createSyntheticImportedDefaultAirFallGetHitTraceArtifact,
  createSyntheticImportedDefaultAirGetHitTraceArtifact,
  createSyntheticImportedDefaultCrouchGetHitTraceArtifact,
  createSyntheticImportedDefaultCrouchGetHitProgressionTraceArtifact,
  createImportedDefaultGetHitTraceArtifact,
  createImportedDefaultFallGetHitTraceArtifact,
  createImportedDefaultGetHitProgressionTraceArtifact,
  defaultCrouchGetHitProgressionPhysicsFrames,
  defaultAirFallRecoveryInputActorFrameSequence,
  defaultAirFallRecoveryInputControllerSequence,
  defaultAirFallRecoveryTooEarlyActorFrameSequence,
  defaultAirFallRecoveryTooEarlyControllerSequence,
  defaultFallBounceLieDownControllerSequence,
  defaultFallGetHitActorFrameSequence,
  defaultFallGetHitControllerSequence,
  defaultFallGroundImpactControllerSequence,
  defaultGroundRecoveryControllerSequence,
  defaultHitFallCanRecoverReadyProbeControllerSequence,
  defaultHitFallCanRecoverProbeControllerSequence,
  defaultHitFallRecoveryInputPriorityActorFrameSequence,
  defaultHitFallRecoveryInputPriorityControllerSequence,
  defaultHitFallRecoverFalseProbeControllerSequence,
  defaultHitFallRecoverTrueProbeControllerSequence,
  defaultFallLieDownGetUpActorFrameSequence,
  defaultFallLieDownFastRecoverControllerSequence,
  defaultFallLieDownGetUpControllerSequence,
  defaultFallLieDownNoFastRecoverControllerSequence,
  defaultFallLieDownNoGetUpControllerSequence,
  defaultGetHitProgressionActorFrameSequence,
  defaultGetHitProgressionControllerSequence,
  defaultGetHitProgressionPhysicsFrames,
  createImportedDefaultGuardStateTraceArtifact,
  createImportedGuardTraceArtifact,
  createSyntheticImportedHitstunTraceArtifact,
  createSyntheticImportedP2StateNoGuardIgnoredTraceArtifact,
  officialKfmAirGuardHitControllerSequence,
  officialKfmAirGuardHitPhysicsFrames,
  officialKfmAirGuardLandingWalkReturnActorFrameSequence,
  officialKfmAirRecoveryActorFrameSequence,
  officialKfmAirRecoveryControllerSequence,
  officialKfmAutoGuardEndControllerSequence,
  officialKfmAutoGuardStartControllerSequence,
  officialKfmDefaultCrouchGetHitProgressionControllerSequence,
  officialKfmDefaultCrouchGetHitProgressionPhysicsFrames,
  officialKfmDefaultGetHitProgressionPhysicsFrames,
  officialKfmFallGetHitControllerSequence,
  officialKfmCrouchGuardHitControllerSequence,
  officialKfmCrouchGuardHoldCrouchReturnActorFrameSequence,
  officialKfmCrouchGuardSlideStopControllerSequence,
  officialKfmFallLieDownGetUpControllerSequence,
  officialKfmQcfXActorFrameSequence,
  officialKfmQcfXControllerSequence,
  defaultAirRecoveryLandControllerSequence,
  officialKfmGroundRecoveryActorFrameSequence,
  officialKfmGroundRecoveryControllerSequence,
  officialKfmCrouchGuardHitPhysicsFrames,
  officialKfmStandGuardHitControllerSequence,
  officialKfmStandGuardHoldReturnActorFrameSequence,
  officialKfmStandGuardHoldWalkReturnActorFrameSequence,
  officialKfmStandGuardSlideStopControllerSequence,
  officialKfmStandGuardHitPhysicsFrames,
  defaultAirGuardLandingControllerSequence,
  defaultCrouchGuardSlideStopControllerSequence,
  defaultStandGuardSlideStopControllerSequence,
  syntheticAirGuardCornerPushPhysicsFrames,
  syntheticAirHitCornerPushPhysicsFrames,
  syntheticAirGuardLandingPhysicsFrames,
  syntheticAirGuardLandingWalkReturnActorFrameSequence,
  syntheticAirGuardHitPhysicsFrames,
  syntheticAirGuardVelocityPhysicsFrames,
  syntheticDownHitCornerPushPhysicsFrames,
  syntheticStandGuardCornerPushPhysicsFrames,
  syntheticStandGuardVelocityPhysicsFrames,
  syntheticAutoGuardEndControllerSequence,
  syntheticAutoGuardEndActorFrameSequence,
  syntheticAutoGuardStartControllerSequence,
  syntheticAutoGuardStartActorFrameSequence,
  syntheticCrouchGuardHoldCrouchReturnActorFrameSequence,
  syntheticCrouchGuardHitPhysicsFrames,
  syntheticStandGuardHoldWalkReturnActorFrameSequence,
  syntheticStandGuardHitPhysicsFrames,
  createSyntheticImportedAutoGuardEndTraceArtifact,
  createSyntheticImportedAutoGuardStartTraceArtifact,
  createSyntheticImportedAssertSpecialAirGuardDenyTraceArtifact,
  createSyntheticImportedAssertSpecialControlTraceArtifact,
  createSyntheticImportedAssertSpecialGlobalTelemetryTraceArtifact,
  createSyntheticImportedAssertSpecialHelperExplodShadowTraceArtifact,
  createSyntheticImportedAssertSpecialJuggleTelemetryTraceArtifact,
  createSyntheticImportedAssertSpecialRoundFlowTelemetryTraceArtifact,
  createSyntheticImportedAssertSpecialShadowTelemetryTraceArtifact,
  createSyntheticImportedAssertSpecialCrouchGuardDenyTraceArtifact,
  createSyntheticImportedAssertSpecialGuardDenyTraceArtifact,
  createSyntheticImportedAssertSpecialLifetimeTraceArtifact,
  createSyntheticImportedDefaultLieDownFastRecoveryTraceArtifact,
  createSyntheticImportedDownHitCornerPushTraceArtifact,
  createSyntheticImportedDownHitCornerPushDefaultTraceArtifact,
  createSyntheticImportedAssertSpecialNoFastRecoverFromLieDownTraceArtifact,
  createSyntheticImportedAssertSpecialNoGetUpFromLieDownTraceArtifact,
  createSyntheticImportedAssertSpecialUnguardableTraceArtifact,
  createSyntheticImportedAssertSpecialNoKoTraceArtifact,
  createSyntheticImportedAssertSpecialRoundNotOverTraceArtifact,
  createSyntheticImportedAssertSpecialTimerFreezeTraceArtifact,
  createSyntheticImportedAirGuardLandingTraceArtifact,
  createSyntheticImportedAirHitCornerPushDefaultTraceArtifact,
  createSyntheticImportedGuardCornerPushDefaultTraceArtifact,
  createSyntheticImportedGuardTimingDefaultTraceArtifact,
  createSyntheticImportedGuardVelocityDefaultTraceArtifact,
  createSyntheticImportedAirGuardCornerPushDefaultTraceArtifact,
  createSyntheticImportedAirGuardCornerPushTraceArtifact,
  createSyntheticImportedAirGuardStateTraceArtifact,
  createSyntheticImportedAirGuardVelocityDefaultTraceArtifact,
  createSyntheticImportedAirGuardVelocityTraceArtifact,
  createSyntheticImportedAliveTraceArtifact,
  createSyntheticImportedInGuardDistTraceArtifact,
  createSyntheticImportedInGuardDistFarTraceArtifact,
  createSyntheticImportedGuardDistReversalNoContactTraceArtifact,
  createSyntheticImportedStateExitTraceArtifact,
  createSyntheticImportedCrouchGuardStateTraceArtifact,
  createSyntheticImportedDiagonalCrouchGuardStateTraceArtifact,
  createSyntheticImportedGuardTraceArtifact,
  createSyntheticImportedHitDefCommonSparkTraceArtifact,
  createSyntheticImportedHitDefCommonGuardSparkTraceArtifact,
  createSyntheticImportedHitDefDataGuardSparkTraceArtifact,
  createSyntheticImportedHitDefDataSparkTraceArtifact,
  createSyntheticImportedHitDefFightFxGuardSparkTraceArtifact,
  createSyntheticImportedHitDefFightFxSparkTraceArtifact,
  createSyntheticImportedHitDefHitEffectPackageTraceArtifact,
  createSyntheticImportedHitDefGuardEffectPackageTraceArtifact,
  createSyntheticImportedHitDefHitSoundTraceArtifact,
  createSyntheticImportedHitDefDynamicHitSoundTraceArtifact,
  createSyntheticImportedHitDefDynamicGuardSoundTraceArtifact,
  createSyntheticImportedHitDefHitSparkTraceArtifact,
  createSyntheticImportedHitDefGuardSparkTraceArtifact,
  createSyntheticImportedHitDefGuardSoundTraceArtifact,
  createSyntheticImportedDefaultGuardStateTraceArtifact,
  createSyntheticImportedDefaultGuardHoldWalkReturnTraceArtifact,
  createSyntheticImportedDefaultGuardSlideStopTraceArtifact,
  createSyntheticImportedCrouchGuardHoldCrouchReturnTraceArtifact,
  createSyntheticImportedCrouchGuardSlideStopTraceArtifact,
  createSyntheticImportedExplodBindTraceArtifact,
  createSyntheticImportedExplodRemoveOnGetHitTraceArtifact,
  createSyntheticImportedExplodRemoveOnProjectileGuardTraceArtifact,
  createSyntheticImportedExplodRemoveOnProjectileHitTraceArtifact,
  createSyntheticImportedExplodScaleTraceArtifact,
  createSyntheticImportedExplodIgnoreHitPauseTraceArtifact,
  createSyntheticImportedExplodPauseMoveTimeTraceArtifact,
  createSyntheticImportedExplodSuperMoveTimeTraceArtifact,
  createSyntheticImportedExplodVelocityTraceArtifact,
  createSyntheticImportedHitPauseTimeIgnoreHitPauseTraceArtifact,
  createSyntheticImportedHelperTraceArtifact,
  createSyntheticImportedHelperIsHelperTraceArtifact,
  createSyntheticImportedHelperEnemyNearTraceArtifact,
  createSyntheticImportedHelperParentRootTraceArtifact,
  createSyntheticImportedHelperControllerParamParentRootTraceArtifact,
  createSyntheticImportedHelperDynamicVelAddTraceArtifact,
  createSyntheticImportedHelperDynamicVelMulTraceArtifact,
  createSyntheticImportedHelperDynamicPosSetTraceArtifact,
  createSyntheticImportedHelperDynamicPosAddTraceArtifact,
  createSyntheticImportedHelperExplodTraceArtifact,
  createSyntheticImportedHelperProjectileTraceArtifact,
  createSyntheticImportedHelperRemoveExplodTraceArtifact,
  createSyntheticImportedHelperModifyExplodTraceArtifact,
  createSyntheticImportedHelperModifyProjectileTraceArtifact,
  createSyntheticImportedHelperModifyProjectileOmittedBoundsTraceArtifact,
  createSyntheticImportedHelperModifyProjectileDynamicBoundsTraceArtifact,
  createSyntheticImportedHelperModifyProjectileDynamicParamsTraceArtifact,
  createSyntheticImportedHelperProjHitTraceArtifact,
  createSyntheticImportedHelperProjHitTimeAnyTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideP2StateNoTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideSlotPriorityTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideGuardFlagFilterTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideGuardFlagForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideOneTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroTraceArtifact,
  createSyntheticImportedHelperProjectileTargetTraceArtifact,
  createSyntheticImportedHelperProjectileBareTargetTraceArtifact,
  createSyntheticImportedHelperProjectileTargetControllersTraceArtifact,
  createSyntheticImportedHelperProjectileTargetStateTraceArtifact,
  createSyntheticImportedHelperProjectileDefaultTargetStateTraceArtifact,
  createSyntheticImportedHelperProjectileDefaultTargetControllersTraceArtifact,
  createSyntheticImportedHelperProjectileDefaultTargetTraceArtifact,
  createSyntheticImportedHelperProjGuardTraceArtifact,
  createSyntheticImportedHelperProjectileGuardTerminalTraceArtifact,
  createSyntheticImportedHelperProjectileCancelRemoveFallbackTerminalTraceArtifact,
  createSyntheticImportedHelperProjectileGuardKillTraceArtifact,
  createSyntheticImportedHelperProjectileGuardKoTraceArtifact,
  createSyntheticImportedHelperProjGuardedTimeAnyTraceArtifact,
  createSyntheticImportedHelperProjectileGetHitVarGuardedTraceArtifact,
  createSyntheticImportedHelperProjectileGetHitVarGuardKillTraceArtifact,
  createSyntheticImportedHelperProjectileGetHitVarGuardHitShakeTimeTraceArtifact,
  createSyntheticImportedHelperProjectileGetHitVarAirGuardHitShakeTimeTraceArtifact,
  createSyntheticImportedHelperProjectileGetHitVarHitCountTraceArtifact,
  createSyntheticImportedHelperProjectileGetHitVarHitIdChainIdTraceArtifact,
  createSyntheticImportedHelperProjectileHitCountTraceArtifact,
  createSyntheticImportedHelperProjectileDownHitCornerPushTraceArtifact,
  createSyntheticImportedHelperProjectileDownHitCornerPushDefaultTraceArtifact,
  createSyntheticImportedHelperProjectileGuardCornerPushDefaultTraceArtifact,
  createSyntheticImportedHelperProjectileGuardSlideStopTraceArtifact,
  createSyntheticImportedHelperProjectileGuardTimingDefaultTraceArtifact,
  createSyntheticImportedHelperProjectileGuardVelocityDefaultTraceArtifact,
  createSyntheticImportedHelperProjectileAirHitCornerPushDefaultTraceArtifact,
  createSyntheticImportedHelperProjectileAirGuardCornerPushDefaultTraceArtifact,
  createSyntheticImportedHelperProjectileAirGuardCornerPushTraceArtifact,
  createSyntheticImportedHelperProjectileAirGuardVelocityDefaultTraceArtifact,
  createSyntheticImportedHelperProjectileAirGuardVelocityTraceArtifact,
  createSyntheticImportedHelperProjContactTraceArtifact,
  createSyntheticImportedHelperProjContactTimeAnyTraceArtifact,
  createSyntheticImportedHelperProjContactPersistTraceArtifact,
  createSyntheticImportedHelperProjectileRemoveHitFallbackTerminalTraceArtifact,
  createSyntheticImportedHelperProjectileDefaultBoundsTerminalTraceArtifact,
  createSyntheticImportedHelperProjectileLocalCoordDefaultBoundsTerminalTraceArtifact,
  createSyntheticImportedHelperProjectileHeightBoundTerminalTraceArtifact,
  createSyntheticImportedHelperProjectileEdgeBoundTerminalTraceArtifact,
  createSyntheticImportedHelperProjectileStageBoundTerminalTraceArtifact,
  createSyntheticImportedHelperProjTimeSameIdLastContactTraceArtifact,
  createSyntheticImportedHelperProjTimeSameIdHitThenGuardTraceArtifact,
  createSyntheticImportedHelperProjCancelTimeAnyTraceArtifact,
  createSyntheticImportedHelperProjCancelTimeIdTraceArtifact,
  createSyntheticImportedHelperProjCancelTimeDynamicTraceArtifact,
  createSyntheticImportedHelperHitDefTraceArtifact,
  createSyntheticImportedHelperHitDefPersistTraceArtifact,
  createSyntheticImportedHelperHitCountPersistTraceArtifact,
  createSyntheticImportedHelperMoveHitPersistTraceArtifact,
  createSyntheticImportedHelperMoveGuardedPersistTraceArtifact,
  createSyntheticImportedHelperMoveReversedPersistTraceArtifact,
  createSyntheticImportedHelperTargetTraceArtifact,
  createSyntheticImportedHelperDefaultTargetTraceArtifact,
  createSyntheticImportedHelperBareTargetTraceArtifact,
  createSyntheticImportedHelperTargetControllersTraceArtifact,
  createSyntheticImportedHelperTargetStateTraceArtifact,
  createSyntheticImportedHelperNumExplodTraceArtifact,
  createSyntheticImportedHelperNumHelperTraceArtifact,
  createSyntheticImportedHelperNumProjTraceArtifact,
  createSyntheticImportedHelperBindToParentTraceArtifact,
  createSyntheticImportedHelperBindToRootTraceArtifact,
  createSyntheticImportedHelperScaleTraceArtifact,
  createSyntheticImportedHelperIgnoreHitPauseTraceArtifact,
  createSyntheticImportedHelperPauseMoveTimeTraceArtifact,
  createSyntheticImportedHelperSuperMoveTimeTraceArtifact,
  createSyntheticImportedHelperVelocityTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideDefaultMissOnOverrideForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideDefaultMissOnOverrideGuardFlagFilterTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroSlotPriorityTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroGuardFlagFilterTraceArtifact,
  createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedExplodTraceArtifact,
  createSyntheticImportedHitOverrideTraceArtifact,
  createSyntheticImportedHitOverrideSlotPriorityTraceArtifact,
  createSyntheticImportedHitOverrideGuardFlagFilterTraceArtifact,
  createSyntheticImportedHitOverrideForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedHitOverrideGuardFlagForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedHitOverrideP2StateNoMissTraceArtifact,
  createSyntheticImportedHitOverrideP2GetP1StateZeroMissTraceArtifact,
  createSyntheticImportedHitOverrideMissOnOverrideZeroTraceArtifact,
  createSyntheticImportedHitOverrideMissOnOverrideZeroForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedHitOverrideDefaultMissOnOverrideForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedHitOverrideDefaultMissOnOverrideGuardFlagFilterTraceArtifact,
  createSyntheticImportedHitOverrideMissOnOverrideZeroSlotPriorityTraceArtifact,
  createSyntheticImportedHitOverrideMissOnOverrideZeroGuardFlagFilterTraceArtifact,
  createSyntheticImportedHitOverrideMissOnOverrideOneTraceArtifact,
  createSyntheticImportedProjectileHitOverrideP2StateNoTraceArtifact,
  createSyntheticImportedProjectileHitOverrideSlotPriorityTraceArtifact,
  createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroSlotPriorityTraceArtifact,
  createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroGuardFlagFilterTraceArtifact,
  createSyntheticImportedProjectileHitOverrideGuardFlagFilterTraceArtifact,
  createSyntheticImportedProjectileHitOverrideForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedProjectileHitOverrideGuardFlagForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroTraceArtifact,
  createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedProjectileHitOverrideDefaultMissOnOverrideForceAirGuardKeepStateTraceArtifact,
  createSyntheticImportedProjectileHitOverrideDefaultMissOnOverrideGuardFlagFilterTraceArtifact,
  createSyntheticImportedProjectileHitOverrideMissOnOverrideOneTraceArtifact,
  createSyntheticImportedHelperProjectileReversalTraceArtifact,
  createSyntheticImportedProjectileReversalTraceArtifact,
  createSyntheticImportedCustomStateReversalTraceArtifact,
  createSyntheticImportedGuardReversalTraceArtifact,
  createSyntheticImportedWalkBackGuardReversalTraceArtifact,
  createSyntheticImportedCrouchGuardReversalTraceArtifact,
  createSyntheticImportedAirGuardReversalTraceArtifact,
  createSyntheticImportedHitByAllowTraceArtifact,
  createSyntheticImportedHitByRejectTraceArtifact,
  createSyntheticImportedRejectTraceArtifact,
  createSyntheticImportedReversalTraceArtifact,
  createSyntheticImportedDamageScaleTraceArtifact,
  createSyntheticImportedDynamicDamageScaleTraceArtifact,
  createSyntheticImportedDataDamageScaleTraceArtifact,
  createSyntheticImportedBoundsTraceArtifact,
  createSyntheticImportedScreenBoundCameraTraceArtifact,
  createSyntheticImportedDynamicScreenBoundTraceArtifact,
  createSyntheticImportedGravityTraceArtifact,
  createSyntheticImportedKinematicTraceArtifact,
  createSyntheticImportedDynamicVelAddTraceArtifact,
  createSyntheticImportedDynamicVelMulTraceArtifact,
  createSyntheticImportedDynamicPosSetTraceArtifact,
  createSyntheticImportedDynamicPosAddTraceArtifact,
  createSyntheticImportedControllerParamBottomTraceArtifact,
  createSyntheticImportedControllerParamTargetRedirectTraceArtifact,
  createSyntheticImportedControllerParamRootRedirectTraceArtifact,
  createSyntheticImportedWidthTraceArtifact,
  createSyntheticImportedDynamicWidthTraceArtifact,
  createSyntheticImportedStateTypeSetTraceArtifact,
  createSyntheticImportedDynamicStateTypeSetTraceArtifact,
  createSyntheticImportedDynamicControlTraceArtifact,
  createSyntheticImportedPlayerPushTraceArtifact,
  createSyntheticImportedDynamicPlayerPushTraceArtifact,
  createSyntheticImportedDynamicPosFreezeTraceArtifact,
  createSyntheticImportedTurnTraceArtifact,
  createSyntheticImportedSprPriorityTraceArtifact,
  createSyntheticImportedDynamicSprPriorityTraceArtifact,
  createSyntheticImportedPalFxTraceArtifact,
  createSyntheticImportedDynamicPalFxTraceArtifact,
  createSyntheticImportedTransTraceArtifact,
  createSyntheticImportedDynamicTransTraceArtifact,
  createSyntheticImportedAngleTraceArtifact,
  createSyntheticImportedAngleMulTraceArtifact,
  createSyntheticImportedDynamicAngleMulTraceArtifact,
  createSyntheticImportedDynamicAngleTraceArtifact,
  createSyntheticImportedDynamicEnvColorTraceArtifact,
  createSyntheticImportedEnvColorTraceArtifact,
  createSyntheticImportedEnvColorUnderTraceArtifact,
  createSyntheticImportedEnvShakeTraceArtifact,
  createSyntheticImportedDynamicEnvShakeTraceArtifact,
  createSyntheticImportedRemapPalTraceArtifact,
  createSyntheticImportedDynamicRemapPalTraceArtifact,
  createSyntheticImportedPalFxRemapPalTraceArtifact,
  createSyntheticImportedAfterImageTraceArtifact,
  createSyntheticImportedDynamicAfterImageTraceArtifact,
  createSyntheticImportedDynamicAfterImageTimeTraceArtifact,
  createSyntheticImportedHitDefPriorityTraceArtifact,
  createSyntheticImportedHitDefGuardKoTraceArtifact,
  createSyntheticImportedHitDefGuardKillTraceArtifact,
  createSyntheticImportedHitDefKillTraceArtifact,
  createSyntheticImportedProjectileClashTraceArtifact,
  createSyntheticImportedProjectileContactTraceArtifact,
  createSyntheticImportedProjectileDefaultTargetControllersTraceArtifact,
  createSyntheticImportedProjectileDefaultTargetStateTraceArtifact,
  createSyntheticImportedProjectileMotionTraceArtifact,
  createSyntheticImportedModifyExplodTraceArtifact,
  createSyntheticImportedModifyProjectileDynamicBoundsTraceArtifact,
  createSyntheticImportedModifyProjectileDynamicParamsTraceArtifact,
  createSyntheticImportedModifyProjectileOmittedBoundsTraceArtifact,
  createSyntheticImportedModifyProjectileTraceArtifact,
  createSyntheticImportedProjectileReceivedDamageTraceArtifact,
  createSyntheticImportedProjectileHitTimeAnyTraceArtifact,
  createSyntheticImportedProjectileContactPersistTraceArtifact,
  createSyntheticImportedProjectileContactSuffixTraceArtifact,
  createSyntheticImportedProjectileContactSuffixAnyTraceArtifact,
  createSyntheticImportedProjectileContactMultiIdTraceArtifact,
  createSyntheticImportedProjectileHitMultiIdTraceArtifact,
  createSyntheticImportedProjectileGuardedMultiIdTraceArtifact,
  createSyntheticImportedProjectileHitTimeMultiIdTraceArtifact,
  createSyntheticImportedProjectileContactTimeMultiIdTraceArtifact,
  createSyntheticImportedProjectileGuardedTimeMultiIdTraceArtifact,
  createSyntheticImportedProjectileTimeSameIdLastContactTraceArtifact,
  createSyntheticImportedProjectileTimeSameIdHitThenGuardTraceArtifact,
  createSyntheticImportedProjectileHitSuffixTraceArtifact,
  createSyntheticImportedProjectileHitSuffixAnyTraceArtifact,
  createSyntheticImportedProjectileGuardedSuffixTraceArtifact,
  createSyntheticImportedProjectileGuardedSuffixAnyTraceArtifact,
  createSyntheticImportedProjectileContactTimeTraceArtifact,
  createSyntheticImportedProjectileContactTimeAnyTraceArtifact,
  createSyntheticImportedProjectileGuardedTimeTraceArtifact,
  createSyntheticImportedProjectileGuardedTimeAnyTraceArtifact,
  createSyntheticImportedProjectileTimeTraceArtifact,
  createSyntheticImportedProjectileVelMulTraceArtifact,
  createSyntheticImportedResourceTraceArtifact,
  createSyntheticImportedControlTraceArtifact,
  createSyntheticImportedDynamicLifeAddTraceArtifact,
  createSyntheticImportedDynamicResourceSetTraceArtifact,
  createSyntheticImportedAnimationTraceArtifact,
  createSyntheticImportedChangeAnim2ElemTraceArtifact,
  createSyntheticImportedAnimElemOffsetTraceArtifact,
  createSyntheticImportedAnimElemTraceArtifact,
  createSyntheticImportedAnimElemTimeTraceArtifact,
  createSyntheticImportedAnimTimeTraceArtifact,
  createSyntheticImportedEdgeDistanceTraceArtifact,
  createSyntheticImportedResourceMaxTraceArtifact,
  createSyntheticImportedNoOpTraceArtifact,
  createSyntheticImportedSoundTraceArtifact,
  createSyntheticImportedDynamicSoundPanTraceArtifact,
  createSyntheticImportedDynamicSoundValueTraceArtifact,
  createSyntheticImportedProjectileMultiHitTraceArtifact,
  createSyntheticImportedProjectilePriorityCancelTraceArtifact,
  createSyntheticImportedProjectileCancelRemoveFallbackTerminalTraceArtifact,
  createSyntheticImportedProjectileCancelTimeTraceArtifact,
  createSyntheticImportedProjectileCancelTimeDynamicTraceArtifact,
  createSyntheticImportedProjectileCancelTimeAnyTraceArtifact,
  createSyntheticImportedProjectileCancelTimeVarTraceArtifact,
  createSyntheticImportedProjectileGetHitVarAirGuardHitShakeTimeTraceArtifact,
  createSyntheticImportedProjectileHitCountTraceArtifact,
  createSyntheticImportedProjectileGetHitVarHitCountTraceArtifact,
  createSyntheticImportedProjectileGetHitVarHitIdChainIdTraceArtifact,
  createSyntheticImportedProjectileGetHitVarHitMetadataTraceArtifact,
  createSyntheticImportedProjectileDownHitCornerPushTraceArtifact,
  createSyntheticImportedProjectileDownHitCornerPushDefaultTraceArtifact,
  createSyntheticImportedProjectileGuardCornerPushDefaultTraceArtifact,
  createSyntheticImportedProjectileGuardSlideStopTraceArtifact,
  createSyntheticImportedProjectileGuardTimingDefaultTraceArtifact,
  createSyntheticImportedProjectileGuardVelocityDefaultTraceArtifact,
  createSyntheticImportedProjectileAirHitCornerPushDefaultTraceArtifact,
  createSyntheticImportedProjectileAirGuardCornerPushDefaultTraceArtifact,
  createSyntheticImportedProjectileAirGuardCornerPushTraceArtifact,
  createSyntheticImportedProjectileAirGuardVelocityDefaultTraceArtifact,
  createSyntheticImportedProjectileAirGuardVelocityTraceArtifact,
  createSyntheticImportedHelperProjectileGetHitVarHitMetadataTraceArtifact,
  createSyntheticImportedProjectileGetHitVarGuardKillTraceArtifact,
  createSyntheticImportedProjectileGetHitVarGuardHitShakeTimeTraceArtifact,
  createSyntheticImportedProjectileGetHitVarGuardedTraceArtifact,
  createSyntheticImportedProjectileGuardKillTraceArtifact,
  createSyntheticImportedProjectileGuardKoTraceArtifact,
  createSyntheticImportedProjectileGuardTerminalTraceArtifact,
  createSyntheticImportedProjectileBoundsRemoveTerminalTraceArtifact,
  createSyntheticImportedProjectileDefaultBoundsTerminalTraceArtifact,
  createSyntheticImportedProjectileLocalCoordDefaultBoundsTerminalTraceArtifact,
  createSyntheticImportedProjectileStageBoundTerminalTraceArtifact,
  createSyntheticImportedProjectileEdgeBoundTerminalTraceArtifact,
  createSyntheticImportedProjectileHeightBoundTerminalTraceArtifact,
  createSyntheticImportedProjectileRemoveHitFallbackTerminalTraceArtifact,
  createSyntheticImportedProjectileRemoveTerminalTraceArtifact,
  createSyntheticImportedProjectileGuardTraceArtifact,
  createSyntheticImportedHitDefProjectileTargetMixTraceArtifact,
  createSyntheticImportedProjectileTraceArtifact,
  createSyntheticImportedProjectileTargetControllersTraceArtifact,
  createSyntheticImportedProjectileTargetRedirectTraceArtifact,
  createSyntheticImportedProjectileTargetStateTraceArtifact,
  createSyntheticImportedSuperPauseAnimDisabledTraceArtifact,
  createSyntheticImportedSuperPauseAnimPosTraceArtifact,
  createSyntheticImportedSuperPauseDefaultAnimTraceArtifact,
  createSyntheticImportedSuperPauseDynamicAnimPosTraceArtifact,
  createSyntheticImportedSuperPauseEffectFreezeTraceArtifact,
  createSyntheticImportedSuperPauseDynamicParamsTraceArtifact,
  createSyntheticImportedSuperPausePauseBgTraceArtifact,
  createSyntheticImportedSuperPauseP2DefMulTraceArtifact,
  createSyntheticImportedSuperPauseProjectileFreezeTraceArtifact,
  createSyntheticImportedSuperPauseSoundTraceArtifact,
  createSyntheticImportedSuperPauseTraceArtifact,
  createSyntheticImportedSuperPauseUnhittableTraceArtifact,
  createSyntheticImportedTargetBindPauseTraceArtifact,
  createSyntheticImportedBindToTargetHeadTraceArtifact,
  createSyntheticImportedBindToTargetMidTraceArtifact,
  createSyntheticImportedTargetStateCustomTraceArtifact,
  createSyntheticImportedTargetNoKoTraceArtifact,
  createSyntheticImportedTargetDynamicRedirectTraceArtifact,
  createSyntheticImportedTargetCondBottomTraceArtifact,
  createSyntheticImportedTargetIfElseBottomTraceArtifact,
  createSyntheticImportedTargetRedirectBottomTraceArtifact,
  createSyntheticImportedTargetRedirectTraceArtifact,
  createSyntheticImportedBareTargetRedirectTraceArtifact,
  createSyntheticImportedDefaultTargetRedirectTraceArtifact,
  createSyntheticImportedTargetTraceArtifact,
  createSyntheticImportedHitDefPersistTraceArtifact,
  createSyntheticImportedHitCountTraceArtifact,
  createSyntheticImportedHitAddTraceArtifact,
  createSyntheticImportedVariableTraceArtifact,
  createSyntheticImportedReceivedDamageTraceArtifact,
  createSyntheticImportedRoundKoTraceArtifact,
  createSyntheticImportedRoundTimeOverTraceArtifact,
  createSyntheticImportedRoundTriggerTraceArtifact,
  createSyntheticImportedHitDefAttrTraceArtifact,
  createSyntheticImportedMoveHitCounterTraceArtifact,
  createSyntheticImportedMoveHitPersistTraceArtifact,
  createSyntheticImportedMoveHitResetTraceArtifact,
  createSyntheticImportedMatchContextTraceArtifact,
  createSyntheticImportedMoveContactTraceArtifact,
  createSyntheticImportedNumExplodTraceArtifact,
  createSyntheticImportedEnemyNearTraceArtifact,
  createSyntheticImportedEnemyNearIndexTraceArtifact,
  createSyntheticImportedNumHelperTraceArtifact,
  createSyntheticImportedNumProjTraceArtifact,
  createSyntheticImportedNumTargetTraceArtifact,
  createSyntheticImportedDefaultNumTargetTraceArtifact,
  createSyntheticImportedP2MetricsTraceArtifact,
  createSyntheticImportedTeamSideTraceArtifact,
  createSyntheticImportedP2StateContextTraceArtifact,
  createSyntheticImportedP2DistanceTraceArtifact,
  createSyntheticImportedOwnerMetricsTraceArtifact,
  createSyntheticImportedIdentityTraceArtifact,
  createSyntheticImportedPrevAnimTraceArtifact,
  createSyntheticImportedPrevMoveTypeTraceArtifact,
  createSyntheticImportedPrevStateTypeTraceArtifact,
  createSyntheticImportedPrevStateTraceArtifact,
  createSyntheticImportedRemoveExplodTraceArtifact,
  createSyntheticImportedSelfAnimExistTraceArtifact,
  createSyntheticImportedSelfCommandTraceArtifact,
  createSyntheticImportedSelfStateNoExistTraceArtifact,
  createSyntheticImportedConfigGameSpaceTraceArtifact,
  createSyntheticImportedConstControllerParamTraceArtifact,
  createSyntheticImportedConstCoordinateTraceArtifact,
  createSyntheticImportedGameSpaceTraceArtifact,
  createSyntheticImportedScreenSpaceTraceArtifact,
  createSyntheticImportedGameTimeTraceArtifact,
  createSyntheticImportedStageTimeTraceArtifact,
  createSyntheticImportedStateContextTraceArtifact,
  createImportedBasicMovementTraceArtifact,
  createSyntheticImportedBasicMovementTraceArtifact,
  createSyntheticImportedXTraceArtifact,
  createSyntheticImportedTraceFighter,
  importedBasicMovementScript,
  importedDelayedXScript,
  importedGuardScript,
  importedCommonGetHitScript,
  importedCustomStateScript,
  importedDefaultAirFallGetHitScript,
  importedDefaultAirGetHitScript,
  importedDefaultCrouchGetHitScript,
  importedDefaultGetHitScript,
  importedDefaultFallGetHitScript,
  importedDefaultFallRecoveryInputScript,
  importedDefaultFallOfficialGroundRecoveryScript,
  importedDefaultFallOfficialRecoveryInputScript,
  importedDefaultFallOfficialRecoveryTooEarlyScript,
  importedDefaultFallOfficialRecoveryScript,
  importedDefaultFallRecoveryScript,
  importedDefaultCrouchGuardStateScript,
  importedDefaultDiagonalCrouchGuardStateScript,
  importedDefaultGuardStateScript,
  importedDefaultGetHitProgressionScript,
  importedHitDefGuardKillScript,
  importedHitDefKillScript,
  importedHitDefPriorityScript,
  importedAutoGuardEndScript,
  importedAutoGuardStartScript,
  importedInGuardDistScript,
  importedExplodScript,
  importedRemoveOnGetHitExplodScript,
  importedProjectileGuardRemoveOnGetHitExplodScript,
  importedProjectileRemoveOnGetHitExplodScript,
  importedHelperScript,
  importedProjectileClashScript,
  importedProjectileMotionScript,
  importedProjectileVelMulScript,
  importedProjectileMultiHitScript,
  importedProjectilePriorityCancelScript,
  importedProjectileScript,
  importedProjectileGuardScript,
  importedSuperPauseEffectScript,
  importedSuperPauseProjectileScript,
  importedSuperPauseScript,
  importedTargetBindPauseScript,
  importedXHitstunScript,
  importedXStateExitScript,
  importedTargetScript,
  nativeHitScript,
  nativeWhiffScript,
  officialKfmBasicMovementActorFrameSequence,
  qcfXContactScript,
  qcfXScript,
} from "../mugen/runtime/RuntimeTraceGatePresets";

describe("RuntimeTraceGatePresets", () => {
  it("creates a native golden hit artifact with hit evidence", () => {
    const artifact = createNativeHitTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      schemaVersion: "runtime-trace-artifact/v0",
      generatedAt: "2026-06-25T00:00:00.000Z",
      status: "passed",
      target: {
        id: "native-nova-hit-golden",
        source: "native",
      },
      gates: [
        {
          label: "native-hit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.trace.eventCount).toBeGreaterThanOrEqual(1);
    expect(artifact.trace.combatReasonCount).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("hit");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("hit");
  });

  it("creates a native golden whiff artifact with inferred whiff evidence", () => {
    const artifact = createNativeWhiffTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "native-nova-whiff-golden",
        source: "native",
      },
      gates: [
        {
          label: "native-whiff-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.trace.eventCount).toBe(0);
    expect(artifact.trace.combatReasonCount).toBeGreaterThanOrEqual(1);
    expect(artifact.trace.combatReasons.some((reason) => reason.reason === "whiff" && reason.source === "inferred")).toBe(true);
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("whiff");
  });

  it("creates a synthetic imported golden x artifact with route, controller, command, and hit evidence", () => {
    const artifact = createSyntheticImportedXTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-x-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.evidence.actorSources).toEqual(["demo", "imported"]);
    expect(artifact.gates[0]?.evidence.routedStates).toContain(200);
    expect(artifact.gates[0]?.evidence.executedStates).toContain(200);
    expect(artifact.gates[0]?.evidence.executedStates).toContain(261);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 261]);
    expect(artifact.gates[0]?.evidence.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.activeCommands).toContain("x");
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("hit");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported basic movement artifact with walk, crouch, jump, and idle evidence", () => {
    const artifact = createSyntheticImportedBasicMovementTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-basic-movement-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-basic-movement-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const scriptInputs = (artifact.script ?? []).map((frame) => (frame.p1 ?? []).join("+"));
    expect(scriptInputs).toEqual(expect.arrayContaining(["F", "D", "U"]));
    expect(gate?.evidence.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateNo: 0, animNo: 0, stateType: "S", physics: "S" }),
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateNo: 20, animNo: 20, stateType: "S", physics: "S" }),
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateNo: 10, animNo: 10, stateType: "C", physics: "C" }),
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateNo: 40, animNo: 40, stateType: "A", physics: "A" }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "imported basic movement state order",
        steps: [
          { actorId: "p1", source: "imported", actorKind: "player", stateNo: 20, animNo: 20, minFrames: 4 },
          { actorId: "p1", source: "imported", actorKind: "player", stateNo: 10, animNo: 10, minFrames: 4 },
          { actorId: "p1", source: "imported", actorKind: "player", stateNo: 40, animNo: 40, minFrames: 5 },
        ],
      },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      stateType: "S",
      moveType: "I",
      physics: "S",
    });
  });

  it("creates a reusable imported basic movement artifact for fixture-backed QA", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "imported-basic-movement-fixture",
      displayName: "Imported Basic Movement Fixture",
    });
    const artifact = createImportedBasicMovementTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "imported-basic-movement-fixture-golden",
      targetLabel: "Imported basic movement fixture route",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "imported-basic-movement-fixture-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-basic-movement-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateNo: 20, animNo: 20 }),
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateType: "C", physics: "C" }),
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateType: "A", physics: "A", observedPosYAtMost: -1 }),
      ]),
    );
  });

  it("describes official KFM basic movement actor-frame order separately from synthetic movement", () => {
    expect(officialKfmBasicMovementActorFrameSequence()).toEqual({
      label: "Official KFM basic movement actor-frame order",
      steps: [
        { actorId: "p1", source: "imported", actorKind: "player", stateNo: 20, animNo: 20, stateType: "S", physics: "S", minFrames: 4 },
        { actorId: "p1", source: "imported", actorKind: "player", stateNo: 11, animNo: 11, stateType: "C", physics: "C", minFrames: 4 },
        { actorId: "p1", source: "imported", actorKind: "player", stateNo: 41, animNo: 41, stateType: "A", physics: "A", minFrames: 5 },
      ],
    });
  });

  it("creates a synthetic imported MoveContact artifact with contact branch evidence", () => {
    const artifact = createSyntheticImportedMoveContactTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-movecontact-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(262);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 262]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported MoveHit counter artifact with contact timing evidence", () => {
    const artifact = createSyntheticImportedMoveHitCounterTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-movehit-counter-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(263);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 263]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported MoveHitReset artifact with reset evidence", () => {
    const artifact = createSyntheticImportedMoveHitResetTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-movehitreset-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "MoveHitReset"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef", "contact:movehitreset"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toContain(263);
    expect(evidence?.executedControllers.MoveHitReset).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["contact:movehitreset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({ stateNo: 200, animNo: 200 });
  });

  it("creates a synthetic imported movehitpersist artifact with Move* persistence evidence", () => {
    const artifact = createSyntheticImportedMoveHitPersistTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-movehitpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 344, 345]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 344, 345]));
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({ stateNo: 345, animNo: 345 });
  });

  it("creates a synthetic imported hitdefpersist artifact with active HitDef persistence evidence", () => {
    const artifact = createSyntheticImportedHitDefPersistTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdefpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 346, 347]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 346, 347]));
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({ stateNo: 347, animNo: 347 });
  });

  it("creates a synthetic imported no-op artifact for Null, ForceFeedback, debug clipboard, MakeDust, and DestroySelf controllers", () => {
    const artifact = createSyntheticImportedNoOpTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-noop-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Null",
      "ForceFeedback",
      "DisplayToClipboard",
      "AppendToClipboard",
      "ClearClipboard",
      "MakeDust",
      "DestroySelf",
      "HitDef",
    ]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "noop:null",
      "noop:forcefeedback",
      "noop:displaytoclipboard",
      "noop:appendtoclipboard",
      "noop:clearclipboard",
      "noop:makedust",
      "noop:destroyself",
      "hitdef",
    ]);
    expect(evidence?.executedControllers.Null).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ForceFeedback).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.DisplayToClipboard).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AppendToClipboard).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ClearClipboard).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.MakeDust).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.DestroySelf).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["noop:null"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["noop:forcefeedback"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["noop:displaytoclipboard"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["noop:appendtoclipboard"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["noop:clearclipboard"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["noop:makedust"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["noop:destroyself"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({ stateNo: 200, animNo: 200 });
  });

  it("creates a synthetic imported HitCount artifact with direct hit count evidence", () => {
    const artifact = createSyntheticImportedHitCountTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitcount-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(264);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 264]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported hitcountpersist artifact with state-entry persistence evidence", () => {
    const artifact = createSyntheticImportedHitCountPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitcountpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 342, 343]));
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 343,
      animNo: 343,
    });
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 342, 343]);
  });

  it("creates a synthetic imported HitAdd artifact with bounded hit-count evidence", () => {
    const artifact = createSyntheticImportedHitAddTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitadd-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(265);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 265]);
    expect(evidence?.executedControllers.HitAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["contact:hitadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported variable artifact with typed variable operation evidence", () => {
    const artifact = createSyntheticImportedVariableTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-variable-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 288]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "HitDef",
      "VarSet",
      "VarAdd",
      "VarRandom",
      "VarRangeSet",
    ]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "variable:varset",
      "variable:varadd",
      "variable:varrandom",
      "variable:varrangeset",
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 288]));
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VarAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VarRandom).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VarRangeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varrandom"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varrangeset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({ stateNo: 288, animNo: 288 });
  });

  it("creates a synthetic imported resource artifact with typed resource operation evidence", () => {
    const artifact = createSyntheticImportedResourceTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-resource-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 289]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "HitDef",
      "LifeAdd",
      "LifeSet",
      "PowerAdd",
      "PowerSet",
    ]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "resource:lifeadd",
      "resource:lifeset",
      "resource:poweradd",
      "resource:powerset",
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 289]));
    expect(evidence?.executedControllers.LifeAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.LifeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PowerAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PowerSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:lifeadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:lifeset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:poweradd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:powerset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          stateNo: 289,
          animNo: 289,
          minLife: 750,
          maxLife: 750,
          minPower: 900,
          maxPower: 900,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 289,
        animNo: 289,
        observedLifeAtLeast: 750,
        observedLifeAtMost: 750,
        observedPowerAtLeast: 900,
        observedPowerAtMost: 900,
        minFrames: 1,
      },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 289,
      animNo: 289,
      life: 750,
      power: 900,
    });
  });

  it("creates a synthetic imported control artifact with typed CtrlSet operation evidence", () => {
    const artifact = createSyntheticImportedControlTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-control-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 200,
      animNo: 200,
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(
      expect.arrayContaining([{ type: "CtrlSet", minCount: 2 }]),
    );
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(
      expect.arrayContaining([{ operation: "resource:ctrlset", minCount: 2 }]),
    );
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 200,
        animNo: 200,
        ctrl: true,
      },
    ]);
  });

  it("creates a synthetic imported dynamic control artifact with resolved CtrlSet operation evidence", () => {
    const artifact = createSyntheticImportedDynamicControlTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-control-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 200,
      animNo: 200,
      ctrl: true,
    });
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "CtrlSet", "HitDef"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "resource:ctrlset", "hitdef"]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 200,
        animNo: 200,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported dynamic LifeAdd artifact with resolved resource operation evidence", () => {
    const artifact = createSyntheticImportedDynamicLifeAddTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-lifeadd-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 290]));
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.LifeAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:lifeadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 290,
          animNo: 290,
          minLife: 1,
          maxLife: 1,
        }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 290,
      animNo: 290,
      life: 1,
    });
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef", "LifeAdd"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "resource:lifeadd"]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 290,
        animNo: 290,
        observedLifeAtLeast: 1,
        observedLifeAtMost: 1,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported dynamic LifeSet/Power artifact with resolved resource operation evidence", () => {
    const artifact = createSyntheticImportedDynamicResourceSetTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-resourceset-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 291]));
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.LifeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PowerAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PowerSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:lifeset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:poweradd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:powerset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 291,
          animNo: 291,
          minLife: 750,
          maxLife: 750,
          minPower: 900,
          maxPower: 900,
        }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 291,
      animNo: 291,
      life: 750,
      power: 900,
    });
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef", "LifeSet", "PowerAdd", "PowerSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "variable:varset",
      "hitdef",
      "resource:lifeset",
      "resource:poweradd",
      "resource:powerset",
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 291,
        animNo: 291,
        observedLifeAtLeast: 750,
        observedLifeAtMost: 750,
        observedPowerAtLeast: 900,
        observedPowerAtMost: 900,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported animation artifact with ChangeAnim and ChangeAnim2 evidence", () => {
    const artifact = createSyntheticImportedAnimationTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-animation-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ChangeAnim).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeAnim2).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", animNo: 205 }),
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", animNo: 206 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 200,
      animNo: 206,
    });
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(
      expect.arrayContaining(["ChangeAnim", "ChangeAnim2"]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", animNo: 205, minFrames: 1 },
      { actorId: "p1", source: "imported", actorKind: "player", animNo: 206, minFrames: 1 },
    ]);
  });

  it("creates a synthetic imported ChangeAnim2 elem/elemtime artifact with active-state frame seeding evidence", () => {
    const artifact = createSyntheticImportedChangeAnim2ElemTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-changeanim2-elem-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ChangeAnim2).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateNo: 200, animNo: 207 }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      id: "p1",
      source: "imported",
      stateNo: 200,
      animNo: 207,
      frameIndex: 2,
      animTime: 20,
    });
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(expect.arrayContaining(["ChangeAnim2"]));
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 200, animNo: 207, minFrames: 1 },
    ]);
  });

  it("creates a synthetic imported AnimTime artifact with animation-end route evidence", () => {
    const artifact = createSyntheticImportedAnimTimeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-animtime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 291]));
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 291]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 200, animNo: 200, minFrames: 3 },
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 291, animNo: 291, minFrames: 1 },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 291,
      animNo: 291,
    });
  });

  it("creates a synthetic imported AnimElemTime artifact with animation-element timing route evidence", () => {
    const artifact = createSyntheticImportedAnimElemTimeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-animelemtime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 292]));
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 292]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 200, animNo: 200, minFrames: 3 },
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 292, animNo: 292, minFrames: 1 },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 292,
      animNo: 292,
    });
  });

  it("creates a synthetic imported AnimElem artifact with current element route evidence", () => {
    const artifact = createSyntheticImportedAnimElemTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-animelem-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 299]));
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 299]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 200, animNo: 200, minFrames: 1 },
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 299, animNo: 299, minFrames: 1 },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 299,
      animNo: 299,
    });
  });

  it("creates a synthetic imported AnimElem offset artifact with elapsed element route evidence", () => {
    const artifact = createSyntheticImportedAnimElemOffsetTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-animelem-offset-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 300]));
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 300]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 200, animNo: 200, minFrames: 5 },
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 300, animNo: 300, minFrames: 1 },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 300,
      animNo: 300,
    });
  });

  it("creates a synthetic imported edge-distance artifact with stage-bound trigger evidence", () => {
    const artifact = createSyntheticImportedEdgeDistanceTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-edge-distance-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.routedStates).toContain(293);
    expect(evidence?.executedStates).toContain(293);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([293]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([293]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 293, animNo: 293, minFrames: 1 },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 293,
      animNo: 293,
    });
  });

  it("creates a synthetic imported sound artifact with PlaySnd and StopSnd event evidence", () => {
    const artifact = createSyntheticImportedSoundTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-sound-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "PlaySnd", "SndPan", "StopSnd"]);
    expect(artifact.gates[0]?.requirements.requiredSoundEvents).toEqual([
      { actorId: "p1", type: "PlaySnd", group: 5, index: 0, channel: 2, lowPriority: true, volumeScale: 50, legacyVolume: -8, freqMul: 0.5, loop: true, pan: 32, stateNo: 200 },
      { actorId: "p1", type: "PlaySnd", group: 5, index: 1, channel: 3, absPan: -64, stateNo: 200 },
      { actorId: "p1", type: "SndPan", channel: 2, pan: -48, stateNo: 200 },
      { actorId: "p1", type: "StopSnd", channel: 2, stateNo: 200 },
    ]);
    expect(evidence?.executedControllers.PlaySnd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SndPan).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StopSnd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:sndpan"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:stopsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", type: "PlaySnd", group: 5, index: 0, channel: 2, lowPriority: true, volumeScale: 50, legacyVolume: -8, freqMul: 0.5, loop: true, pan: 32, stateNo: 200 }),
        expect.objectContaining({ actorId: "p1", type: "PlaySnd", group: 5, index: 1, channel: 3, absPan: -64, stateNo: 200 }),
        expect.objectContaining({ actorId: "p1", type: "SndPan", channel: 2, pan: -48, stateNo: 200 }),
        expect.objectContaining({ actorId: "p1", type: "StopSnd", channel: 2, stateNo: 200 }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "PlaySnd", group: 5, index: 0, channel: 2, lowPriority: true, volumeScale: 50, legacyVolume: -8, freqMul: 0.5, loop: true, pan: 32, stateNo: 200 }),
        expect.objectContaining({ type: "PlaySnd", group: 5, index: 1, channel: 3, absPan: -64, stateNo: 200 }),
        expect.objectContaining({ type: "SndPan", channel: 2, pan: -48, stateNo: 200 }),
        expect.objectContaining({ type: "StopSnd", channel: 2, stateNo: 200 }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic sound-pan artifact with typed audio evidence", () => {
    const artifact = createSyntheticImportedDynamicSoundPanTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-sound-dynamic-pan-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef", "PlaySnd", "SndPan", "StopSnd"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "audio:playsnd", "audio:sndpan", "audio:stopsnd"]);
    expect(artifact.gates[0]?.requirements.requiredSoundEvents).toEqual([
      { actorId: "p1", type: "PlaySnd", group: 5, index: 2, channel: 2, pan: -24, stateNo: 200 },
      { actorId: "p1", type: "SndPan", channel: 2, absPan: 64, stateNo: 200 },
      { actorId: "p1", type: "StopSnd", channel: 2, stateNo: 200 },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PlaySnd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SndPan).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StopSnd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:sndpan"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:stopsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", type: "PlaySnd", group: 5, index: 2, channel: 2, pan: -24, stateNo: 200 }),
        expect.objectContaining({ actorId: "p1", type: "SndPan", channel: 2, absPan: 64, stateNo: 200 }),
        expect.objectContaining({ actorId: "p1", type: "StopSnd", channel: 2, stateNo: 200 }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic sound-value artifact with typed audio evidence", () => {
    const artifact = createSyntheticImportedDynamicSoundValueTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-sound-dynamic-value-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef", "PlaySnd"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "audio:playsnd"]);
    expect(artifact.gates[0]?.requirements.requiredSoundEvents).toEqual([
      { actorId: "p1", type: "PlaySnd", group: 5, index: 3, channel: 4, soundPrefix: "kfm", stateNo: 200 },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PlaySnd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", type: "PlaySnd", group: 5, index: 3, channel: 4, soundPrefix: "kfm", stateNo: 200 }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "PlaySnd", group: 5, index: 3, channel: 4, raw: "Fvar(0),var(1)", soundPrefix: "kfm", stateNo: 200 }),
      ]),
    );
  });

  it("creates a synthetic imported ReceivedDamage artifact with defender-local branch evidence", () => {
    const artifact = createSyntheticImportedReceivedDamageTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-receiveddamage-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-receiveddamage-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 280]));
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 5000, 280]);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p2" && actor.stateNo === 280)).toBe(true);
  });

  it("creates a synthetic imported HitDefAttr artifact with attr-filter branch evidence", () => {
    const artifact = createSyntheticImportedHitDefAttrTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdefattr-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(266);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 266]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported PrevStateNo artifact with branch evidence", () => {
    const artifact = createSyntheticImportedPrevStateTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-prevstateno-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 267, 268]));
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 267, 268]);
    expect(
      artifact.trace.frames.some((frame) =>
        frame.delta?.actorChanges.some((actor) => actor.id === "p1" && actor.changes.includes("state 267->268")),
      ),
    ).toBe(true);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 0 && actor.prevStateNo === 268)).toBe(true);
  });

  it("creates a synthetic imported PrevMoveType artifact with branch evidence", () => {
    const artifact = createSyntheticImportedPrevMoveTypeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-prevmovetype-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 269, 270]));
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 269, 270]);
    expect(
      artifact.trace.frames.some((frame) =>
        frame.delta?.actorChanges.some((actor) => actor.id === "p1" && actor.changes.includes("state 269->270")),
      ),
    ).toBe(true);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 0 && actor.prevMoveType === "I")).toBe(true);
  });

  it("creates a synthetic imported PrevAnim artifact with branch evidence", () => {
    const artifact = createSyntheticImportedPrevAnimTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-prevanim-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 275, 276]));
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 275, 276]);
    expect(
      artifact.trace.frames.some((frame) =>
        frame.delta?.actorChanges.some((actor) => actor.id === "p1" && actor.changes.includes("state 275->276")),
      ),
    ).toBe(true);
    expect(
      artifact.trace.frames.some((frame) =>
        frame.delta?.actorChanges.some((actor) => actor.id === "p1" && actor.changes.includes("anim 205->275")),
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported PrevStateType artifact with branch evidence", () => {
    const artifact = createSyntheticImportedPrevStateTypeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-prevstatetype-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 271, 272]));
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 271, 272]);
    expect(
      artifact.trace.frames.some((frame) =>
        frame.delta?.actorChanges.some((actor) => actor.id === "p1" && actor.changes.includes("state 271->272")),
      ),
    ).toBe(true);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 0 && actor.prevStateType === "S")).toBe(true);
  });

  it("creates a synthetic imported EnemyNear artifact with redirect branch evidence", () => {
    const artifact = createSyntheticImportedEnemyNearTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-enemynear-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.routedStates).toContain(269);
    expect(evidence?.executedStates).toContain(269);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([269]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([269]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 269)).toBe(true);
  });

  it("creates a synthetic imported EnemyNear indexed artifact with fail-closed evidence", () => {
    const artifact = createSyntheticImportedEnemyNearIndexTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-enemynear-index-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.routedStates).toContain(284);
    expect(evidence?.routedStates).not.toContain(285);
    expect(evidence?.executedStates).toContain(284);
    expect(evidence?.executedStates).not.toContain(285);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([284]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([284]);
    expect(artifact.gates[0]?.requirements.forbiddenExecutedStates).toEqual([285]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 284)).toBe(true);
  });

  it("creates a synthetic imported P2 metric artifact with trigger evidence", () => {
    const artifact = createSyntheticImportedP2MetricsTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-p2metrics-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.routedStates).toContain(275);
    expect(evidence?.executedStates).toContain(275);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([275]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([275]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 275)).toBe(true);
  });

  it("creates a synthetic imported TeamSide artifact with owner and EnemyNear side evidence", () => {
    const artifact = createSyntheticImportedTeamSideTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-teamside-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(299);
    expect(evidence?.executedStates).toContain(299);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([299]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([299]);
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      expect.objectContaining({ actorId: "p1", stateNo: 299, animNo: 299 }),
    ]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 299 && actor.animNo === 299)).toBe(true);
  });

  it("creates a synthetic imported P2 state-context artifact with opponent metadata evidence", () => {
    const artifact = createSyntheticImportedP2StateContextTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-p2-state-context-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(296);
    expect(evidence?.executedStates).toContain(296);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([296]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([296]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 296)).toBe(true);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p2" && actor.stateType === "S" && actor.moveType === "I")).toBe(
      true,
    );
  });

  it("creates a synthetic imported P2 distance artifact with spacing evidence", () => {
    const artifact = createSyntheticImportedP2DistanceTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-p2-distance-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(297);
    expect(evidence?.executedStates).toContain(297);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([297]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([297]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 297)).toBe(true);
  });

  it("creates a synthetic imported owner metrics artifact with state and position evidence", () => {
    const artifact = createSyntheticImportedOwnerMetricsTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-owner-metrics-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(298);
    expect(evidence?.executedStates).toContain(298);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([298]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([298]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 298)).toBe(true);
  });

  it("creates a synthetic imported identity artifact with name trigger evidence", () => {
    const artifact = createSyntheticImportedIdentityTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-identity-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(276);
    expect(evidence?.executedStates).toContain(276);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([276]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([276]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 276)).toBe(true);
  });

  it("creates a synthetic imported SelfStateNoExist artifact with state-existence branch evidence", () => {
    const artifact = createSyntheticImportedSelfStateNoExistTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-selfstatenoexist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.routedStates).toContain(277);
    expect(evidence?.executedStates).toContain(277);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([277]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([277]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 277)).toBe(true);
  });

  it("creates a synthetic imported SelfAnimExist artifact with action-existence branch evidence", () => {
    const artifact = createSyntheticImportedSelfAnimExistTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-selfanimexist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.routedStates).toContain(290);
    expect(evidence?.executedStates).toContain(290);
    expect(evidence?.executedStates).not.toContain(9999);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([290]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([290]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 290)).toBe(true);
  });

  it("creates a synthetic imported SelfCommand artifact with owner command branch evidence", () => {
    const artifact = createSyntheticImportedSelfCommandTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-selfcommand-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(278);
    expect(evidence?.executedStates).toContain(278);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([278]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([278]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 278)).toBe(true);
  });

  it("creates a synthetic imported StageTime artifact with match-tick branch evidence", () => {
    const artifact = createSyntheticImportedStageTimeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-stagetime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(279);
    expect(evidence?.executedStates).toContain(279);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([279]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([279]);
    expect(artifact.trace.frames.some((frame) => frame.tick >= 3)).toBe(true);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 279)).toBe(true);
  });

  it("creates a synthetic imported GameTime artifact with global tick branch evidence", () => {
    const artifact = createSyntheticImportedGameTimeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gametime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(294);
    expect(evidence?.executedStates).toContain(294);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([294]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([294]);
    expect(artifact.trace.frames.some((frame) => frame.tick >= 4)).toBe(true);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 294)).toBe(true);
  });

  it("creates a synthetic imported GameWidth/GameHeight artifact with localcoord branch evidence", () => {
    const artifact = createSyntheticImportedGameSpaceTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gamespace-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(9301);
    expect(evidence?.executedStates).toContain(9301);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([9301]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([9301]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 9301)).toBe(true);
  });

  it("creates a synthetic imported ScreenWidth/ScreenHeight artifact with zoom-stable branch evidence", () => {
    const artifact = createSyntheticImportedScreenSpaceTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-screenspace-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(9302);
    expect(evidence?.executedStates).toContain(9302);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([9302]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([9302]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 9302)).toBe(true);
  });

  it("creates a synthetic imported config game-space artifact with config override evidence", () => {
    const artifact = createSyntheticImportedConfigGameSpaceTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-config-gamespace-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(9303);
    expect(evidence?.executedStates).toContain(9303);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([9303]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([9303]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 9303)).toBe(true);
  });

  it("creates a synthetic imported Const240p/Const480p/Const720p artifact with localcoord branch evidence", () => {
    const artifact = createSyntheticImportedConstCoordinateTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-const-coordinate-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(9304);
    expect(evidence?.executedStates).toContain(9304);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([9304]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([9304]);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState"]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 9304)).toBe(true);
  });

  it("creates a synthetic imported Const240p/Const480p/Const720p controller-param artifact with VelSet evidence", () => {
    const artifact = createSyntheticImportedConstControllerParamTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-const-controller-param-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(200);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(2);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VelSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["kinematic:velset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 Const controller-param VelSet order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VelSet", name: "Seed Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
          { stateNo: 200, controller: "VelSet", name: "Const Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
        ],
      },
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 200,
        animNo: 200,
        observedVelXAtLeast: 12,
        observedVelYAtMost: -6,
        minFrames: 2,
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          minVel: expect.objectContaining({ y: -6 }),
          maxVel: expect.objectContaining({ x: 12 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported state-context artifact with ctrl and metadata branch evidence", () => {
    const artifact = createSyntheticImportedStateContextTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-state-context-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(295);
    expect(evidence?.executedStates).toContain(295);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([295]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([295]);
    expect(
      artifact.trace.finalActors.some(
        (actor) =>
          actor.id === "p1" &&
          actor.stateNo === 295 &&
          actor.prevStateType === "S" &&
          actor.prevMoveType === "I" &&
          actor.physics === "S",
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported Alive artifact with owner-life branch evidence", () => {
    const artifact = createSyntheticImportedAliveTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-alive-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(280);
    expect(evidence?.executedStates).toContain(280);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([280]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([280]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 280 && actor.life > 0)).toBe(true);
  });

  it("creates a synthetic imported RoundNo/RoundState artifact with single-round branch evidence", () => {
    const artifact = createSyntheticImportedRoundTriggerTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-round-trigger-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(281);
    expect(evidence?.executedStates).toContain(281);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([281]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([281]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 281)).toBe(true);
  });

  it("creates a synthetic imported round KO artifact with RoundSnapshot evidence", () => {
    const artifact = createSyntheticImportedRoundKoTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-round-ko-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.roundFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          state: "ko",
          winner: "Synthetic Imported Round KO",
          message: "Synthetic Imported Round KO wins",
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredRoundFrames).toEqual([
      { state: "ko", winner: "Synthetic Imported Round KO", message: "Synthetic Imported Round KO wins" },
    ]);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")?.life).toBe(0);
  });

  it("creates a synthetic imported round time-over artifact with RoundSnapshot evidence", () => {
    const artifact = createSyntheticImportedRoundTimeOverTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-round-timeover-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-round-timeover-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(expect.arrayContaining(["imported", "demo"]));
    expect(evidence?.roundFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          state: "timeover",
          winner: "Draw",
          message: "Time over - draw",
          minTimer: 0,
          maxTimer: 0,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredRoundFrames).toEqual([
      { state: "timeover", winner: "Draw", message: "Time over - draw", observedTimerAtMost: 0 },
    ]);
  });

  it("creates a synthetic imported RoundsExisted/MatchOver artifact with match-context branch evidence", () => {
    const artifact = createSyntheticImportedMatchContextTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-match-context-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(282);
    expect(evidence?.executedStates).toContain(282);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([282]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([282]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 282)).toBe(true);
  });

  it("creates a synthetic imported LifeMax/PowerMax artifact with resource-cap branch evidence", () => {
    const artifact = createSyntheticImportedResourceMaxTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-resource-max-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.routedStates).toContain(283);
    expect(evidence?.executedStates).toContain(283);
    expect(artifact.gates[0]?.requirements.requiredRoutedStates).toEqual([283]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([283]);
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      expect.objectContaining({ actorId: "p1", stateNo: 283, life: 750 }),
    ]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.stateNo === 283 && actor.life === 750)).toBe(true);
  });

  it("creates a synthetic imported NumTarget artifact with target-memory branch evidence", () => {
    const artifact = createSyntheticImportedNumTargetTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-numtarget-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(263);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 263]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported default NumTarget artifact with target id 0 evidence", () => {
    const artifact = createSyntheticImportedDefaultNumTargetTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-numtarget-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(268);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 268]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 0 })]),
    );
  });

  it("creates a synthetic imported NumHelper artifact with helper-count branch evidence", () => {
    const artifact = createSyntheticImportedNumHelperTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-numhelper-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-numhelper-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(264);
    expect(evidence?.effectKinds).toContain("helper");
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 264]);
  });

  it("creates a synthetic imported NumProj artifact with projectile-count branch evidence", () => {
    const artifact = createSyntheticImportedNumProjTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-numproj-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-numproj-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(273);
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 273]);
  });

  it("creates a synthetic imported Helper IsHelper artifact with helper-local branch evidence", () => {
    const artifact = createSyntheticImportedHelperIsHelperTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-ishelper-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-ishelper-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1201, animNo: 921 })]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      { source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1201, animNo: 921, minFrames: 1 },
    ]);
  });

  it("creates a synthetic imported Helper EnemyNear artifact with helper-local opponent branch evidence", () => {
    const artifact = createSyntheticImportedHelperEnemyNearTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-enemynear-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-enemynear-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1202, animNo: 922 })]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      { source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1202, animNo: 922, minFrames: 1 },
    ]);
  });

  it("creates a synthetic imported Helper Parent/Root artifact with helper-local redirect evidence", () => {
    const artifact = createSyntheticImportedHelperParentRootTraceArtifact({ generatedAt: "2026-07-03T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-parentroot-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-parentroot-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1400, animNo: 940 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      { source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1400, animNo: 940, minFrames: 1 },
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1400, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper controller-param Parent/Root artifact with dynamic velocity evidence", () => {
    const artifact = createSyntheticImportedHelperControllerParamParentRootTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-controller-param-parentroot-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-controller-param-parentroot-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          stateNo: 1401,
          animNo: 941,
          minVel: { x: 5, y: -3 },
          maxVel: { x: 5, y: -3 },
        }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1401,
        animNo: 941,
        observedVelXAtLeast: 5,
        observedVelXAtMost: 5,
        observedVelYAtLeast: -3,
        observedVelYAtMost: -3,
        minFrames: 1,
      },
    ]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Helper", "VelSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "helper", "kinematic:velset"]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1401, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper dynamic VelAdd artifact with typed helper telemetry", () => {
    const artifact = createSyntheticImportedHelperDynamicVelAddTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-dynamic-veladd-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-dynamic-veladd-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:veladd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          stateNo: 1402,
          animNo: 942,
          minVel: { x: 5, y: -3 },
          maxVel: { x: 5, y: -3 },
        }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1402,
        animNo: 942,
        observedVelXAtLeast: 5,
        observedVelXAtMost: 5,
        observedVelYAtLeast: -3,
        observedVelYAtMost: -3,
        minFrames: 1,
      },
    ]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Helper", "VelSet", "VelAdd"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "helper",
      "kinematic:velset",
      "kinematic:veladd",
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1402, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper dynamic VelMul artifact with typed helper telemetry", () => {
    const artifact = createSyntheticImportedHelperDynamicVelMulTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-dynamic-velmul-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-dynamic-velmul-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelMul).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velmul"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          stateNo: 1403,
          animNo: 943,
          minVel: { x: 4, y: -6 },
          maxVel: { x: 4, y: -6 },
        }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1403,
        animNo: 943,
        observedVelXAtLeast: 4,
        observedVelXAtMost: 4,
        observedVelYAtLeast: -6,
        observedVelYAtMost: -6,
        minFrames: 1,
      },
    ]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Helper", "VelSet", "VelMul"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "helper",
      "kinematic:velset",
      "kinematic:velmul",
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1403, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper dynamic PosSet artifact with typed helper telemetry", () => {
    const artifact = createSyntheticImportedHelperDynamicPosSetTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-dynamic-posset-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-dynamic-posset-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:posset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          stateNo: 1404,
          animNo: 944,
          minPos: { x: 16, y: -20 },
          maxPos: { x: 16, y: -20 },
        }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1404,
        animNo: 944,
        observedPosXAtLeast: 16,
        observedPosXAtMost: 16,
        observedPosYAtLeast: -20,
        observedPosYAtMost: -20,
        minFrames: 1,
      },
    ]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Helper", "PosSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "helper", "kinematic:posset"]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1404, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper dynamic PosAdd artifact with typed helper telemetry", () => {
    const artifact = createSyntheticImportedHelperDynamicPosAddTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-dynamic-posadd-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-dynamic-posadd-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          stateNo: 1405,
          animNo: 945,
          minPos: { x: 18, y: -17 },
          maxPos: { x: 18, y: -17 },
        }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1405,
        animNo: 945,
        observedPosXAtLeast: 18,
        observedPosXAtMost: 18,
        observedPosYAtLeast: -17,
        observedPosYAtMost: -17,
        minFrames: 1,
      },
    ]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Helper", "PosSet", "PosAdd"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "helper",
      "kinematic:posset",
      "kinematic:posadd",
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1405, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper Explod artifact with helper-parented visual effect evidence", () => {
    const artifact = createSyntheticImportedHelperExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-explod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-explod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "explod"]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1205, animNo: 925 }),
        expect.objectContaining({ source: "effect", actorKind: "explod", ownerId: "p1", animNo: 939 }),
      ]),
    );
    expect(gate?.requirements.requiredWorldLifecycleEvents).toEqual([
      { type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "spawn", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
      { type: "active", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1205, minAge: 1 },
      { actorId: "p1-explod-0", kind: "explod", ownerId: "p1", effectId: 8800, minAge: 1, minRemoveTime: 24, minSpritePriority: 7 },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-explod-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "explod", id: 8800, spritePriority: 7 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile artifact with helper-parented projectile evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1212, animNo: 932 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 943 }),
      ]),
    );
    expect(gate?.requirements.requiredWorldLifecycleEvents).toEqual([
      { type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
      { type: "active", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1212, minAge: 1 },
      { actorId: "p1-projectile-0", kind: "projectile", ownerId: "p1", effectId: 8850, minAge: 1, minPriority: 2 },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8850, priority: 2 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile remove hit fallback terminal artifact with visible fallback evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileRemoveHitFallbackTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-remove-hit-fallback-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-remove-hit-fallback-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1320, animNo: 1120 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1122, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1320 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8930,
            hasHit: false,
            hitsRemaining: 1,
            hitAnimNo: 1122,
            removalReason: "timeout",
            terminalReason: "timeout",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default bounds terminal artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileDefaultBoundsTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-default-bounds-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-default-bounds-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1324, animNo: 1132 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1134, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    const projectileEffect = evidence?.effectPayloads.find((payload) => payload.actorId === "p1-projectile-0")?.effect;
    expect(projectileEffect).toEqual(
      expect.objectContaining({
        kind: "projectile",
        id: 8934,
        hasHit: false,
        hitsRemaining: 1,
        removalReason: "bounds",
        terminalReason: "bounds",
        terminalDuration: 2,
      }),
    );
    expect(projectileEffect).not.toHaveProperty("edgeBound");
    expect(projectileEffect).not.toHaveProperty("stageBound");
    expect(projectileEffect).not.toHaveProperty("heightBound");
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1324 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8934,
            removalReason: "bounds",
            terminalReason: "bounds",
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1324, minAge: 1 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8934,
        hasHit: false,
        minHitsRemaining: 1,
        removalReason: "bounds",
        terminalReason: "bounds",
        minTerminalAge: 1,
        minTerminalDuration: 2,
      },
    ]);
  });

  it("creates a synthetic imported Helper Projectile localcoord default bounds terminal artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileLocalCoordDefaultBoundsTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-localcoord-default-bounds-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-localcoord-default-bounds-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1325, animNo: 1135 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1137, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8935,
            edgeBound: 80,
            stageBound: 80,
            heightBound: { low: -480, high: 2 },
            removalReason: "bounds",
            terminalReason: "bounds",
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1325, minAge: 1 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8935,
        edgeBound: 80,
        stageBound: 80,
        heightBound: { low: -480, high: 2 },
        hasHit: false,
        minHitsRemaining: 1,
        removalReason: "bounds",
        terminalReason: "bounds",
        minTerminalAge: 1,
        minTerminalDuration: 2,
      },
    ]);
  });

  it("creates a synthetic imported Helper Projectile projheightbound terminal artifact with explicit height-bound evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileHeightBoundTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-heightbound-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-heightbound-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1321, animNo: 1123 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1125, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1321 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8931,
            hasHit: false,
            hitsRemaining: 1,
            heightBound: { low: -120, high: 60 },
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1321, minAge: 1 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8931,
        hasHit: false,
        minHitsRemaining: 1,
        heightBound: { low: -120, high: 60 },
        removalReason: "bounds",
        terminalReason: "bounds",
        minTerminalAge: 1,
        minTerminalDuration: 2,
      },
    ]);
  });

  it("creates a synthetic imported Helper Projectile projedgebound terminal artifact with explicit edge-bound evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileEdgeBoundTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-edgebound-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-edgebound-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1322, animNo: 1126 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1128, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1322 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8932,
            hasHit: false,
            hitsRemaining: 1,
            edgeBound: 24,
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1322, minAge: 1 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8932,
        hasHit: false,
        minHitsRemaining: 1,
        edgeBound: 24,
        removalReason: "bounds",
        terminalReason: "bounds",
        minTerminalAge: 1,
        minTerminalDuration: 2,
      },
    ]);
  });

  it("creates a synthetic imported Helper Projectile projstagebound terminal artifact with explicit stage-bound evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileStageBoundTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-stagebound-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-stagebound-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1323, animNo: 1129 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1131, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1323 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8933,
            hasHit: false,
            hitsRemaining: 1,
            stageBound: 24,
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1323, minAge: 1 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8933,
        hasHit: false,
        minHitsRemaining: 1,
        stageBound: 24,
        removalReason: "bounds",
        terminalReason: "bounds",
        minTerminalAge: 1,
        minTerminalDuration: 2,
      },
    ]);
  });

  it("creates a synthetic imported Helper RemoveExplod artifact with helper-local cleanup evidence", () => {
    const artifact = createSyntheticImportedHelperRemoveExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-removeexplod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-removeexplod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "explod", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "remove", kind: "explod", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1206, animNo: 926 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1207, animNo: 927 }),
        expect.objectContaining({ source: "effect", actorKind: "explod", ownerId: "p1", animNo: 940 }),
      ]),
    );
    expect(gate?.requirements.requiredWorldLifecycleEvents).toEqual([
      { type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "spawn", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
      { type: "active", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
      { type: "remove", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1207, minAge: 2 },
      { actorId: "p1-explod-0", kind: "explod", ownerId: "p1", effectId: 8810, minAge: 1, minRemoveTime: 80, minSpritePriority: 7 },
    ]);
  });

  it("creates a synthetic imported Helper ModifyExplod artifact with helper-local mutation evidence", () => {
    const artifact = createSyntheticImportedHelperModifyExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-modifyexplod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-modifyexplod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "explod", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "explod", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1208, animNo: 928 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1209, animNo: 929 }),
        expect.objectContaining({ source: "effect", actorKind: "explod", ownerId: "p1", animNo: 941 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1209, minAge: 2 },
      {
        actorId: "p1-explod-0",
        kind: "explod",
        ownerId: "p1",
        effectId: 8820,
        minAge: 3,
        minRemoveTime: 90,
        minSpritePriority: 8,
        removeOnGetHit: true,
        ignoreHitPause: true,
        minPauseMoveTime: 3,
        minSuperMoveTime: 4,
        scaleX: 2,
        scaleY: 0.5,
      },
    ]);
  });

  it("creates a synthetic imported Helper ModifyProjectile artifact with helper-local mutation evidence", () => {
    const artifact = createSyntheticImportedHelperModifyProjectileTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-modifyprojectile-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-modifyprojectile-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1214, animNo: 934 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1215, animNo: 935 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 945 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1215, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        effectId: 8852,
        minAge: 3,
        minRemoveTime: 52,
        minSpritePriority: 8,
        minPriority: 4,
        minHitsRemaining: 3,
        maxHitsRemaining: 3,
        hasHit: false,
        scaleX: 2,
        scaleY: 0.5,
        edgeBound: 28,
        stageBound: 24,
        heightBound: { low: -120, high: 60 },
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8852,
            priority: 4,
            hitsRemaining: 3,
            edgeBound: 28,
            stageBound: 24,
            heightBound: { low: -120, high: 60 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ModifyProjectile omitted bounds artifact", () => {
    const artifact = createSyntheticImportedHelperModifyProjectileOmittedBoundsTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-modifyprojectile-omitted-bounds-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-modifyprojectile-omitted-bounds-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1215, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        effectId: 8852,
        minAge: 3,
        minRemoveTime: 52,
        minSpritePriority: 8,
        minPriority: 4,
        minHitsRemaining: 3,
        maxHitsRemaining: 3,
        hasHit: false,
        scaleX: 2,
        scaleY: 0.75,
        edgeBound: 34,
        stageBound: 26,
        heightBound: { low: -132, high: 68 },
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8852,
            priority: 4,
            hitsRemaining: 3,
            scale: { x: 2, y: 0.75 },
            edgeBound: 34,
            stageBound: 26,
            heightBound: { low: -132, high: 68 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper dynamic ModifyProjectile bounds artifact", () => {
    const artifact = createSyntheticImportedHelperModifyProjectileDynamicBoundsTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-modifyprojectile-dynamic-bounds-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-modifyprojectile-dynamic-bounds-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(4);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(4);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1215, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        effectId: 8852,
        minAge: 3,
        minRemoveTime: 52,
        minSpritePriority: 8,
        minPriority: 4,
        minHitsRemaining: 3,
        maxHitsRemaining: 3,
        hasHit: false,
        scaleX: 2,
        scaleY: 0.5,
        edgeBound: 52,
        stageBound: 36,
        heightBound: { low: -144, high: 72 },
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8852,
            edgeBound: 52,
            stageBound: 36,
            heightBound: { low: -144, high: 72 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper dynamic ModifyProjectile params artifact", () => {
    const artifact = createSyntheticImportedHelperModifyProjectileDynamicParamsTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-modifyprojectile-dynamic-params-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-modifyprojectile-dynamic-params-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(15);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(15);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1215, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        effectId: 8852,
        minAge: 3,
        minRemoveTime: 46,
        minSpritePriority: 9,
        minPriority: 5,
        minHitsRemaining: 6,
        maxHitsRemaining: 6,
        hasHit: false,
        scaleX: 3,
        scaleY: 1,
        edgeBound: 28,
        stageBound: 24,
        heightBound: { low: -120, high: 60 },
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8852,
            removeTime: 46,
            spritePriority: 9,
            priority: 5,
            hitsRemaining: 6,
            removeOnHit: false,
            scale: { x: 3, y: 1 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjHit artifact with helper-local projectile contact evidence", () => {
    const artifact = createSyntheticImportedHelperProjHitTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projhit-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projhit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1216, animNo: 936 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1217, animNo: 937 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 946 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1217, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8853,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8853 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8853 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 0, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7002,
            raw: "F7002",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7002,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -68 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "hit",
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7002, offsetX: 18, offsetY: -68 }),
      }),
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8853, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjHitTime any artifact with helper-local any-projectile evidence", () => {
    const artifact = createSyntheticImportedHelperProjHitTimeAnyTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projhittime-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projhittime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1260, animNo: 986 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1261, animNo: 987 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 988 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1261, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8864,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8864 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8864 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 12, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7018,
            raw: "F7018",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7018,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 19, y: -62 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8864, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile HitOverride plus p2stateno artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideP2StateNoTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-p2stateno-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-p2stateno-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([889, 1273, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([889, 1273, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8870 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8870, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8870 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8870 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1272, animNo: 1017 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1019 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1272, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8870, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride slot priority artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideSlotPriorityTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-slot-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-slot-priority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 778]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([779, 889, 1278, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Helper",
      "Projectile",
      { type: "HitOverride", minCount: 2 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "helper",
      "projectile",
      { operation: "hitoverride", minCount: 2 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 778]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([779, 889, 1278, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8873 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8873, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8873 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8873 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1277, animNo: 1026 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1028 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1277, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8873, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 778,
      animNo: 778,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride plus MissOnOverride zero slot priority artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroSlotPriorityTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 778]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([779, 889, 1288, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Helper",
      "Projectile",
      { type: "HitOverride", minCount: 2 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "helper",
      "projectile",
      { operation: "hitoverride", minCount: 2 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 778]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([779, 889, 1288, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8878 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8878, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8878 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8878 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1287, animNo: 1041 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1043 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1287, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8878, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 778,
      animNo: 778,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride plus MissOnOverride zero guardflag filter artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 889, 1291, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Helper",
      "Projectile",
      { type: "HitOverride", minCount: 3 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", { operation: "hitoverride", minCount: 3 }]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8881 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8881, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 889, 1291, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8881 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8881, hasBinding: false }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1289, targetCount: 1 }),
        }),
        expect.objectContaining({
          ownerId: "p1",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8881, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride default MissOnOverride guardflag filter artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideDefaultMissOnOverrideGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 889, 1293, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Helper",
      "Projectile",
      { type: "HitOverride", minCount: 3 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", { operation: "hitoverride", minCount: 3 }]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8882 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8882, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 889, 1293, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8882 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8882, hasBinding: false }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1292, targetCount: 1 }),
        }),
        expect.objectContaining({
          ownerId: "p1",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8882, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride guardflag filter artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 889, 1280, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Helper",
      "Projectile",
      { type: "HitOverride", minCount: 3 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "helper",
      "projectile",
      { operation: "hitoverride", minCount: 3 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 889, 1280, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8874 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8874, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8874 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8874 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1279, animNo: 1029 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1031 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1279, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8874, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 889, 1282, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 889, 1282, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8875 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8875, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8875 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8875 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1281, animNo: 1032 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1034 }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1281, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8875, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride guardflag forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideGuardFlagForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-guardflag-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-guardflag-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 779, 889, 1297, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Helper",
      "Projectile",
      { type: "HitOverride", minCount: 3 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "helper",
      "projectile",
      { operation: "hitoverride", minCount: 3 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 779, 889, 1297, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8884 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8884, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8884 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8884 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1296, animNo: 1053 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1055 }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1296, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8884, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride plus MissOnOverride one artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideOneTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-missonoverride-one-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-missonoverride-one-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([777, 889, 1275, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([777, 889, 1275, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reject");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.targetLinks).toEqual([]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1274, animNo: 1020 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1022 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1274, targetCount: 0 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8871, hitsRemaining: 1, hasHit: false }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "reject" && event.line.includes("missonoverride = 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride plus MissOnOverride zero artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([889, 1276, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([889, 1276, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8872 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8872, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8872 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8872 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1275, animNo: 1023 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1025 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1275, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8872,
            hitsRemaining: 0,
            hasHit: true,
          }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride plus MissOnOverride zero forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideMissOnOverrideZeroForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 889, 1284, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 889, 1284, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8876 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8876, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8876 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8876 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1283, animNo: 1035 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1037 }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1283, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8876,
            hitsRemaining: 0,
            hasHit: true,
          }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile Target artifact with helper-owned target redirect evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileTargetTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-target-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-target-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1224, animNo: 954 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1225, animNo: 955 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 956 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", minLife: 982, maxLife: 982 }),
      ]),
    );
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8860 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8860, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8860 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8860 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 982 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 2, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7008,
            raw: "F7008",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7008,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 13, y: -54 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8860, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile bare Target artifact with helper-owned current-target redirect evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileBareTargetTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-bare-target-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-bare-target-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1241, animNo: 977 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1242, animNo: 978 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 979 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", minLife: 982, maxLife: 982 }),
      ]),
    );
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8863 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8863, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8863 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8863 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 982 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 11, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7017,
            raw: "F7017",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7017,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 21, y: -49 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1242, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8863, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile Target controllers artifact with target side-effect evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileTargetControllersTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-target-controllers-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-target-controllers-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1233, animNo: 965 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1234, animNo: 966 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 967 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", facing: 1, minLife: 958, maxLife: 958 }),
      ]),
    );
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8861 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8861, hasBinding: false, minFrames: 1 },
      {
        ownerId: "p1-helper-0",
        actorId: "p2",
        targetId: 8861,
        hasBinding: true,
        minFrames: 1,
        minAge: 1,
        minBindingRemaining: 1,
        maxBindingRemaining: 3,
        bindingOffsetX: 36,
        bindingOffsetY: -12,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8861 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8861, hasBinding: false }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8861, hasBinding: true }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 958, power: 40 })]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1234, targetCount: 0 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8861, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 7, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7013,
            raw: "F7013",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7013,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 17, y: -51 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile TargetState artifact with owner-backed custom-state evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileTargetStateTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-targetstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-targetstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1237, animNo: 971 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1238, animNo: 972 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 973 }),
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", animNo: 888 }),
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", animNo: 889 }),
      ]),
    );
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 889]));
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8862 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8862, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8862 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8862 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ id: "p2", source: "imported", actorKind: "player", customOwnerId: undefined, stateNo: 0, ctrl: true }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1238, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8862, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 9, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7015,
            raw: "F7015",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7015,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 19, y: -49 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default TargetState artifact with owner-backed custom-state evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileDefaultTargetStateTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-default-targetstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-default-targetstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1239, animNo: 974 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1240, animNo: 975 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 976 }),
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", animNo: 888 }),
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", animNo: 889 }),
      ]),
    );
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 889]));
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 0 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 0, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 0 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 0 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ id: "p2", source: "imported", actorKind: "player", customOwnerId: undefined, stateNo: 0, ctrl: true }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1240, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 0, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 10, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7016,
            raw: "F7016",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7016,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 20, y: -48 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default Target controllers artifact with target side-effect evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileDefaultTargetControllersTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-default-target-controllers-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-default-target-controllers-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1235, animNo: 968 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1236, animNo: 969 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 970 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", facing: 1, minLife: 958, maxLife: 958 }),
      ]),
    );
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 0 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 0, hasBinding: false, minFrames: 1 },
      {
        ownerId: "p1-helper-0",
        actorId: "p2",
        targetId: 0,
        hasBinding: true,
        minFrames: 1,
        minAge: 1,
        minBindingRemaining: 1,
        maxBindingRemaining: 3,
        bindingOffsetX: 36,
        bindingOffsetY: -12,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 0 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 0, hasBinding: false }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 0, hasBinding: true }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 958, power: 40 })]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1236, targetCount: 0 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 0, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 8, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7014,
            raw: "F7014",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7014,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -50 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default Target artifact with target id 0 evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileDefaultTargetTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-default-target-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-default-target-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1226, animNo: 957 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1227, animNo: 958 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 959 }),
      ]),
    );
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 0 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 0, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 0 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 0, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 3, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7009,
            raw: "F7009",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7009,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 14, y: -52 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjGuarded artifact with helper-local projectile guard evidence", () => {
    const artifact = createSyntheticImportedHelperProjGuardTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projguard-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projguard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x"]));
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1218, animNo: 938 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1219, animNo: 947 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 948 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1219, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8854,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8854 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8854 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7004,
            raw: "F7004",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7004,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 15, y: -63 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "guard",
        hitEffect: expect.objectContaining({ kind: "guard", sparkNo: 7004, offsetX: 15, offsetY: -63 }),
      }),
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8854, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile guard chip KO artifact with round evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGuardKoTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-guard-ko-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-guard-ko-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "audio:playsnd"]);
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, contactKind: "guard" }),
        }),
      ]),
    );
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported Helper Projectile Guard KO projectile for 2000"))).toBe(
      true,
    );
    expect(evidence?.roundFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          state: "ko",
          winner: "Synthetic Imported Helper Projectile Guard KO",
          message: "Synthetic Imported Helper Projectile Guard KO wins",
        }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8920 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8920 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "demo", life: 0 })]),
    );
  });

  it("creates a synthetic imported Helper Projectile guard.kill no-KO artifact with clamp evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGuardKillTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-guard-kill-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "audio:playsnd"]);
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, contactKind: "guard" }),
        }),
      ]),
    );
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported Helper Projectile Guard Kill projectile for 2000"))).toBe(
      true,
    );
    expect(evidence?.roundFrames.some((round) => round.state === "ko")).toBe(false);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1313 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8921, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8921 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8921 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "demo", life: 1 })]),
    );
  });

  it("creates a synthetic imported Helper Projectile guard terminal artifact with hit removal playback evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGuardTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-guard-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-guard-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "audio:playsnd"]);
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, contactKind: "guard" }),
        }),
      ]),
    );
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported Helper Projectile Guard Terminal projectile"))).toBe(
      true,
    );
    expect(evidence?.eventLines.some((line) => line.includes("hit removal anim 1109"))).toBe(true);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1314, animNo: 1107 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1315, animNo: 1108 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1109, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1315 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8922,
            hasHit: true,
            removalReason: "hit",
            terminalReason: "hit",
            terminalDuration: 4,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8922 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8922 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjGuardedTime any artifact with helper-local any-projectile guard evidence", () => {
    const artifact = createSyntheticImportedHelperProjGuardedTimeAnyTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projguardedtime-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projguardedtime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1262, animNo: 989 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1263, animNo: 990 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 991 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1263, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8865,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8865 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8865 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 12, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7019,
            raw: "F7019",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7019,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 16, y: -60 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8865, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjContact artifact with helper-local generic projectile contact evidence", () => {
    const artifact = createSyntheticImportedHelperProjContactTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projcontact-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projcontact-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x"]));
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1220, animNo: 949 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1221, animNo: 950 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 951 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1221, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8855,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8855 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8855 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7004,
            raw: "F7004",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7004,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 15, y: -63 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "guard",
        hitEffect: expect.objectContaining({ kind: "guard", sparkNo: 7004, offsetX: 15, offsetY: -63 }),
      }),
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8855, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjContactTime any artifact with helper-local any-projectile contact evidence", () => {
    const artifact = createSyntheticImportedHelperProjContactTimeAnyTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projcontacttime-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projcontacttime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1264, animNo: 992 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1265, animNo: 993 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 994 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1265, minAge: 2 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8866,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8866 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8866 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 13, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7020,
            raw: "F7020",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7020,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 17, y: -58 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8866, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjContactPersist artifact with projectile contact state-transition evidence", () => {
    const artifact = createSyntheticImportedHelperProjContactPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projcontactpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projcontactpersist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1300, animNo: 1058, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1302, animNo: 1061, moveType: "A" }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1301, animNo: 1059, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1060 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1301, minAge: 3 },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8895,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8895 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8895 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 14, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7021,
            raw: "F7021",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7021,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -57 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8895, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjTime same-id last-contact artifact", () => {
    const artifact = createSyntheticImportedHelperProjTimeSameIdLastContactTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projtime-same-id-last-contact-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projtime-same-id-last-contact-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toEqual(expect.arrayContaining(["guard", "hit"]));
    expect(evidence?.combatReasons).toEqual(expect.arrayContaining(["guard", "hit"]));
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([1308]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Helper", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "helper", { operation: "projectile", minCount: 2 }]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([1308]));
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", id: "p1-projectile-0", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "spawn", id: "p1-projectile-1", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-0", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-1", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1306, animNo: 1065, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1307, animNo: 1066, moveType: "I" }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          source: "effect",
          actorKind: "projectile",
          ownerId: "p1",
          animNo: 1067,
          moveType: "A",
        }),
        expect.objectContaining({
          actorId: "p1-projectile-1",
          source: "effect",
          actorKind: "projectile",
          ownerId: "p1",
          animNo: 1067,
          moveType: "A",
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1307, minAge: 2 },
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8918,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8918 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8918 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 34, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7038,
            raw: "F7038",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7038,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 35, y: -77 },
          }),
        }),
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 35, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7038,
            raw: "F7038",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7038,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 36, y: -78 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-1",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8918, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjTime same-id hit-then-guard artifact", () => {
    const artifact = createSyntheticImportedHelperProjTimeSameIdHitThenGuardTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projtime-same-id-hit-then-guard-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projtime-same-id-hit-then-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toEqual(expect.arrayContaining(["hit", "guard"]));
    expect(evidence?.combatReasons).toEqual(expect.arrayContaining(["hit", "guard"]));
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([1305]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Helper", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "helper", { operation: "projectile", minCount: 2 }]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([1305]));
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", id: "p1-projectile-0", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "spawn", id: "p1-projectile-1", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-0", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-1", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1303, animNo: 1062, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1304, animNo: 1063, moveType: "I" }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          source: "effect",
          actorKind: "projectile",
          ownerId: "p1",
          animNo: 1064,
          moveType: "A",
        }),
        expect.objectContaining({
          actorId: "p1-projectile-1",
          source: "effect",
          actorKind: "projectile",
          ownerId: "p1",
          animNo: 1064,
          moveType: "A",
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1304, minAge: 2 },
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1-helper-0",
        effectId: 8917,
        minAge: 1,
        minPriority: 2,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 8917 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8917 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 32, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7037,
            raw: "F7037",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7037,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 33, y: -75 },
          }),
        }),
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 33, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7037,
            raw: "F7037",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7037,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 34, y: -76 },
          }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-1",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8917, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper HitDef artifact with helper-owned direct combat evidence", () => {
    const artifact = createSyntheticImportedHelperHitDefTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-hitdef-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-hitdef-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1200, animNo: 920 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1222, animNo: 952 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 971 })]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1222, minAge: 2 },
    ]);
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          type: "PlaySnd",
          group: 5,
          index: 0,
          contactKind: "hit",
        }),
      ]),
    );
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          kind: "hit",
          sparkNo: 7006,
          raw: "F7006",
          rawPrefix: "F",
          assetSource: "fightfx",
          assetActionId: 7006,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          offset: { x: 9, y: -58 },
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 0, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7006,
            raw: "F7006",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7006,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 9, y: -58 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        actorId: "p1-helper-0",
        contactKind: "hit",
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7006, offsetX: 9, offsetY: -58 }),
      }),
    ]);
  });

  it("creates a synthetic imported Helper HitDefPersist artifact with persisted helper HitDef evidence", () => {
    const artifact = createSyntheticImportedHelperHitDefPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-hitdefpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-hitdefpersist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1224, animNo: 963 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1225, animNo: 964 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 967 })]),
    );
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          type: "PlaySnd",
          group: 5,
          index: 1,
          stateNo: 1224,
          contactKind: "hit",
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 1, stateNo: 1224, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7006,
            raw: "F7006",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7006,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 11, y: -56 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1225, minAge: 2 },
    ]);
  });

  it("creates a synthetic imported Helper HitCountPersist artifact with persisted helper hit-count evidence", () => {
    const artifact = createSyntheticImportedHelperHitCountPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-hitcountpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-hitcountpersist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1226, animNo: 965 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1227, animNo: 966 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 965 })]),
    );
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          type: "PlaySnd",
          group: 5,
          index: 2,
          stateNo: 1200,
          contactKind: "hit",
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 2, stateNo: 1200, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7007,
            raw: "F7007",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7007,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 13, y: -54 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1227, minAge: 3 },
    ]);
  });

  it("creates a synthetic imported Helper MoveHitPersist artifact with persisted helper MoveHit evidence", () => {
    const artifact = createSyntheticImportedHelperMoveHitPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-movehitpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-movehitpersist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1228, animNo: 967 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1229, animNo: 968 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 964 })]),
    );
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          type: "PlaySnd",
          group: 5,
          index: 3,
          stateNo: 1200,
          contactKind: "hit",
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 3, stateNo: 1200, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7008,
            raw: "F7008",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7008,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 14, y: -50 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1229, minAge: 3 },
    ]);
  });

  it("creates a synthetic imported Helper MoveGuardedPersist artifact with persisted helper MoveGuarded evidence", () => {
    const artifact = createSyntheticImportedHelperMoveGuardedPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-moveguardedpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-moveguardedpersist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1230, animNo: 969 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1231, animNo: 970 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 1000 })]),
    );
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          type: "PlaySnd",
          group: 6,
          index: 4,
          stateNo: 1200,
          contactKind: "guard",
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 4, stateNo: 1200, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7009,
            raw: "F7009",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7009,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 12, y: -48 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1231, minAge: 3 },
    ]);
  });

  it("creates a synthetic imported Helper MoveReversedPersist artifact with persisted helper MoveReversed evidence", () => {
    const artifact = createSyntheticImportedHelperMoveReversedPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-movereversedpersist-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-movereversedpersist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.combatReasons).toContain("reversal");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1232, animNo: 971 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1233, animNo: 972 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", stateNo: 779, animNo: 779, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", stateNo: 779, life: 1000 })]),
    );
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("reversed Helper"))).toBe(true);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1233, minAge: 3 },
    ]);
  });

  it("creates a synthetic imported Helper Target artifact with helper-owned target redirect evidence", () => {
    const artifact = createSyntheticImportedHelperTargetTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-target-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-target-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1-helper-0",
          actorId: "p2",
          targetId: 8877,
          hasBinding: false,
        }),
      ]),
    );
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8877, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1200, animNo: 920 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1223, animNo: 953 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 969 })]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1223, minAge: 2 },
    ]);
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          type: "PlaySnd",
          group: 5,
          index: 1,
          contactKind: "hit",
        }),
      ]),
    );
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          kind: "hit",
          sparkNo: 7007,
          raw: "F7007",
          rawPrefix: "F",
          assetSource: "fightfx",
          assetActionId: 7007,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          offset: { x: 11, y: -56 },
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 1, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7007,
            raw: "F7007",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7007,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 11, y: -56 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        actorId: "p1-helper-0",
        contactKind: "hit",
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7007, offsetX: 11, offsetY: -56 }),
      }),
    ]);
  });

  it("creates a synthetic imported Helper default Target artifact with target id 0 evidence", () => {
    const artifact = createSyntheticImportedHelperDefaultTargetTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-default-target-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-default-target-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 0, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1-helper-0",
          actorId: "p2",
          targetId: 0,
          hasBinding: false,
        }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1200, animNo: 920 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1229, animNo: 961 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 967 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 4, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7010,
            raw: "F7010",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7010,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 13, y: -54 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper bare Target artifact with helper-owned target redirect evidence", () => {
    const artifact = createSyntheticImportedHelperBareTargetTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-bare-target-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-bare-target-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8878, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1-helper-0",
          actorId: "p2",
          targetId: 8878,
          hasBinding: false,
        }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1200, animNo: 920 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1230, animNo: 962 }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 965 })]),
    );
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          type: "PlaySnd",
          group: 5,
          index: 5,
          contactKind: "hit",
        }),
      ]),
    );
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          kind: "hit",
          sparkNo: 7011,
          raw: "F7011",
          rawPrefix: "F",
          assetSource: "fightfx",
          assetActionId: 7011,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          offset: { x: 14, y: -53 },
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 5, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7011,
            raw: "F7011",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7011,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 14, y: -53 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { actorId: "p1-helper-0", kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1230, minAge: 2 },
    ]);
  });

  it("creates a synthetic imported Helper Target controllers artifact with helper-owned mutation evidence", () => {
    const artifact = createSyntheticImportedHelperTargetControllersTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-target-controllers-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-target-controllers-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8879, hasBinding: false, minFrames: 1 },
      {
        ownerId: "p1-helper-0",
        actorId: "p2",
        targetId: 8879,
        hasBinding: true,
        minFrames: 1,
        minAge: 1,
        minBindingRemaining: 1,
        maxBindingRemaining: 3,
        bindingOffsetX: 36,
        bindingOffsetY: -12,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8879, hasBinding: false }),
        expect.objectContaining({
          ownerId: "p1-helper-0",
          actorId: "p2",
          targetId: 8879,
          hasBinding: true,
          bindingOffset: { x: 36, y: -12 },
        }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1200, animNo: 920 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1231, animNo: 963 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", facing: 1 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 944,
      power: 40,
    });
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-helper-0",
        kind: "helper",
        ownerId: "p1",
        effectId: 42,
        name: "Buddy",
        helperStateNo: 1231,
        targetCount: 0,
        minAge: 3,
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          actorKind: "helper",
          effect: expect.objectContaining({ kind: "helper", stateNo: 1231, targetCount: 0 }),
        }),
      ]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          source: "effect",
          actorKind: "helper",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 6, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7012,
            raw: "F7012",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7012,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 16, y: -52 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper TargetState artifact with owner-backed custom-state evidence", () => {
    const artifact = createSyntheticImportedHelperTargetStateTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-targetstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-targetstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 889]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8880 })]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1200, animNo: 920 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1232, animNo: 964 }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 889, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      ctrl: true,
      moveType: "I",
    });
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-helper-0",
        kind: "helper",
        ownerId: "p1",
        effectId: 42,
        name: "Buddy",
        helperStateNo: 1232,
        targetCount: 1,
        minAge: 3,
      },
    ]);
  });

  it("creates a synthetic imported Helper NumExplod artifact with helper-local count evidence", () => {
    const artifact = createSyntheticImportedHelperNumExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-numexplod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-numexplod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "explod", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "explod", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1210, animNo: 930 }),
        expect.objectContaining({ source: "effect", actorKind: "explod", ownerId: "p1", animNo: 942 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1210, minAge: 1 },
      { actorId: "p1-explod-0", kind: "explod", ownerId: "p1", effectId: 8830, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper NumHelper artifact with helper-local count evidence", () => {
    const artifact = createSyntheticImportedHelperNumHelperTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-numhelper-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-numhelper-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1211, animNo: 931 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1211, minAge: 1 },
    ]);
  });

  it("creates a synthetic imported Helper NumProj artifact with helper-local projectile count evidence", () => {
    const artifact = createSyntheticImportedHelperNumProjTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-numproj-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-numproj-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1213, animNo: 933 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 944 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1213, minAge: 1 },
      { actorId: "p1-projectile-0", kind: "projectile", ownerId: "p1", effectId: 8851, minAge: 1, minPriority: 2 },
    ]);
  });

  it("creates a synthetic imported Helper BindToParent artifact with helper-local owner bind evidence", () => {
    const artifact = createSyntheticImportedHelperBindToParentTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-bindtoparent-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-bindtoparent-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          stateNo: 1203,
          animNo: 923,
        }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1203,
        animNo: 923,
        observedPosYAtLeast: -24,
        observedPosYAtMost: -12,
        minFrames: 1,
      },
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        kind: "helper",
        ownerId: "p1",
        effectId: 42,
        name: "Buddy",
        helperStateNo: 1203,
        minAge: 1,
        ownerBindTarget: "parent",
        ownerBindOffsetX: 40,
        ownerBindOffsetY: -18,
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          effect: expect.objectContaining({
            kind: "helper",
            ownerBind: expect.objectContaining({ target: "parent", offset: { x: 40, y: -18 } }),
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper BindToRoot artifact with helper-local owner bind evidence", () => {
    const artifact = createSyntheticImportedHelperBindToRootTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-bindtoroot-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-bindtoroot-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          stateNo: 1204,
          animNo: 924,
        }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1204,
        animNo: 924,
        observedPosYAtLeast: -22,
        observedPosYAtMost: -10,
        minFrames: 1,
      },
    ]);
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        kind: "helper",
        ownerId: "p1",
        effectId: 42,
        name: "Buddy",
        helperStateNo: 1204,
        minAge: 1,
        ownerBindTarget: "root",
        ownerBindOffsetX: -36,
        ownerBindOffsetY: -16,
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          effect: expect.objectContaining({
            kind: "helper",
            ownerBind: expect.objectContaining({ target: "root", offset: { x: -36, y: -16 } }),
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported NumExplod artifact with explod-count branch evidence", () => {
    const artifact = createSyntheticImportedNumExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-numexplod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-numexplod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toContain(274);
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 274]);
  });

  it("creates a synthetic imported RemoveExplod artifact with explod remove evidence", () => {
    const artifact = createSyntheticImportedRemoveExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-removeexplod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-removeexplod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.RemoveExplod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.removeexplod).toBeGreaterThanOrEqual(1);
    expect(evidence?.worldLifecycleEvents.some((event) => event.kind === "explod" && event.type === "remove")).toBe(true);
    const finalStore = artifact.trace.frames.at(-1)?.world?.effectStores.find((store) => store.ownerId === "p1");
    expect(finalStore?.explods).toEqual([]);
  });

  it("creates a synthetic imported reject artifact with HitBy/NotHitBy reason evidence", () => {
    const artifact = createSyntheticImportedRejectTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-reject-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-reject-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.evidence.actorSources).toEqual(["imported"]);
    expect(artifact.gates[0]?.evidence.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedControllers.NotHitBy).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations["eligibility:nothitby"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("reject");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("reject");
  });

  it("creates a synthetic imported HitBy allow artifact with accepted hit evidence", () => {
    const artifact = createSyntheticImportedHitByAllowTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitby-allow-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitby-allow-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitBy"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef", "eligibility:hitby"]);
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        life: 963,
        moveType: "H",
      },
    ]);
    expect(artifact.gates[0]?.evidence.executedControllers.HitBy).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations["eligibility:hitby"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("hit");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("hit");
    expect(artifact.trace.events.some((event) => event.category === "reject")).toBe(false);
  });

  it("creates a synthetic imported HitBy reject artifact with mismatched attr evidence", () => {
    const artifact = createSyntheticImportedHitByRejectTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitby-reject-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitby-reject-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitBy"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef", "eligibility:hitby"]);
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        life: 1000,
        moveType: "I",
      },
    ]);
    expect(artifact.gates[0]?.evidence.executedControllers.HitBy).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations["eligibility:hitby"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("reject");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("reject");
    expect(artifact.trace.events.some((event) => event.category === "hit")).toBe(false);
  });

  it("creates a synthetic imported HitOverride artifact with redirect reason evidence", () => {
    const artifact = createSyntheticImportedHitOverrideTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 777,
        animNo: 777,
        life: 1000,
        moveType: "I",
      },
    ]);
    expect(artifact.gates[0]?.evidence.actorSources).toEqual(["imported"]);
    expect(artifact.gates[0]?.evidence.executedStates).toEqual(expect.arrayContaining([200, 777]));
    expect(artifact.gates[0]?.evidence.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("override");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("override");
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({ stateNo: 777, life: 1000 });
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(true);
  });

  it("creates a synthetic imported HitOverride slot priority artifact", () => {
    const artifact = createSyntheticImportedHitOverrideSlotPriorityTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-slot-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-slot-priority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 778]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([779, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "HitOverride", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "hitoverride", minCount: 2 }]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 778]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([779, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.combatReasons).toContain("override");
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 778,
      animNo: 778,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride guardflag filter artifact", () => {
    const artifact = createSyntheticImportedHitOverrideGuardFlagFilterTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "HitOverride", minCount: 3 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "hitoverride", minCount: 3 }]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.combatReasons).toContain("override");
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedHitOverrideForceAirGuardKeepStateTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride guardflag forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedHitOverrideGuardFlagForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-guardflag-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-guardflag-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 779, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "HitDef",
      { type: "HitOverride", minCount: 3 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      { operation: "hitoverride", minCount: 3 },
    ]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 779, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride plus p2stateno miss artifact", () => {
    const artifact = createSyntheticImportedHitOverrideP2StateNoMissTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-p2stateno-miss-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-p2stateno-miss-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([777, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([777, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reject");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.targetLinks).toEqual([]);
    expect(artifact.trace.events.some((event) => event.category === "reject" && event.line.includes("custom-state HitDef"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride plus p2getp1state zero miss artifact", () => {
    const artifact = createSyntheticImportedHitOverrideP2GetP1StateZeroMissTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-p2getp1state-zero-miss-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-p2getp1state-zero-miss-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([777, 889, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([777, 889, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reject");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.targetLinks).toEqual([]);
    expect(artifact.trace.events.some((event) => event.category === "reject" && event.line.includes("custom-state HitDef"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride plus MissOnOverride zero artifact", () => {
    const artifact = createSyntheticImportedHitOverrideMissOnOverrideZeroTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-missonoverride-zero-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-missonoverride-zero-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride plus MissOnOverride zero forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedHitOverrideMissOnOverrideZeroForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-missonoverride-zero-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-missonoverride-zero-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride default MissOnOverride forceair forceguard keepstate miss artifact", () => {
    const artifact = createSyntheticImportedHitOverrideDefaultMissOnOverrideForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reject");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.targetLinks).toEqual([]);
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "reject" && event.line.includes("custom-state HitDef"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride plus MissOnOverride zero slot priority artifact", () => {
    const artifact = createSyntheticImportedHitOverrideMissOnOverrideZeroSlotPriorityTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-missonoverride-zero-slot-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-missonoverride-zero-slot-priority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 778]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([779, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "HitOverride", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "hitoverride", minCount: 2 }]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 778]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([779, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 778,
      animNo: 778,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride plus MissOnOverride zero guardflag filter artifact", () => {
    const artifact = createSyntheticImportedHitOverrideMissOnOverrideZeroGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-missonoverride-zero-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-missonoverride-zero-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "HitOverride", minCount: 3 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "hitoverride", minCount: 3 }]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride default MissOnOverride guardflag filter miss artifact", () => {
    const artifact = createSyntheticImportedHitOverrideDefaultMissOnOverrideGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-missonoverride-default-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-missonoverride-default-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 779, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "HitOverride", minCount: 3 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "hitoverride", minCount: 3 }]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 779, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("reject");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.targetLinks ?? []).toHaveLength(0);
    expect(artifact.trace.events.some((event) => event.category === "reject" && event.line.includes("custom-state HitDef"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported HitOverride plus MissOnOverride one artifact", () => {
    const artifact = createSyntheticImportedHitOverrideMissOnOverrideOneTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitoverride-missonoverride-one-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitoverride-missonoverride-one-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([777, 888, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([777, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reject");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.targetLinks).toEqual([]);
    expect(artifact.trace.events.some((event) => event.category === "reject" && event.line.includes("missonoverride = 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride plus p2stateno artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideP2StateNoTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-p2stateno-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-p2stateno-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride slot priority artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideSlotPriorityTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-slot-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-slot-priority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 778]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([779, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Projectile",
      { type: "HitOverride", minCount: 2 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "projectile",
      { operation: "hitoverride", minCount: 2 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 778]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([779, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 778,
      animNo: 778,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride plus MissOnOverride zero slot priority artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroSlotPriorityTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 778]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([779, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Projectile",
      { type: "HitOverride", minCount: 2 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "projectile",
      { operation: "hitoverride", minCount: 2 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 778]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([779, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(2);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 778,
      animNo: 778,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride plus MissOnOverride zero guardflag filter artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-missonoverride-zero-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-missonoverride-zero-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", { type: "HitOverride", minCount: 3 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", { operation: "hitoverride", minCount: 3 }]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride default MissOnOverride guardflag filter artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideDefaultMissOnOverrideGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", { type: "HitOverride", minCount: 3 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", { operation: "hitoverride", minCount: 3 }]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride guardflag filter artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideGuardFlagFilterTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-guardflag-filter-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-guardflag-filter-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 779]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Projectile",
      { type: "HitOverride", minCount: 3 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "projectile",
      { operation: "hitoverride", minCount: 3 },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 779]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 779,
      animNo: 779,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride guardflag forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideGuardFlagForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-guardflag-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-guardflag-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([776, 778, 779, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "Projectile",
      { type: "HitOverride", minCount: 3 },
    ]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "projectile",
      { operation: "hitoverride", minCount: 3 },
    ]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([776, 778, 779, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(3);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 2"))).toBe(false);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 5"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride plus MissOnOverride zero artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-missonoverride-zero-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-missonoverride-zero-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride plus MissOnOverride zero forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideMissOnOverrideZeroForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Helper Projectile HitOverride plus default MissOnOverride forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileHitOverrideDefaultMissOnOverrideForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 889, 1286, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 889, 1286, 5000, 150, 151]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(gate?.requirements.requiredTargetLinks).toEqual([
      { ownerId: "p1", actorId: "p2", targetId: 8877 },
      { ownerId: "p1-helper-0", actorId: "p2", targetId: 8877, hasBinding: false, minFrames: 1 },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8877 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8877 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1285, animNo: 1038 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1040 }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1285, targetCount: 1 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8877, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride plus default MissOnOverride forceair forceguard keepstate artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideDefaultMissOnOverrideForceAirGuardKeepStateTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([780, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", "hitoverride"]);
    expect(evidence?.executedStates).toContain(200);
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([780, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("override");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "reject"]));
    expect(evidence?.combatReasons).toContain("override");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit" }),
        }),
      ]),
    );
    expect(
      evidence?.actorFrames.some(
        (frame) => frame.actorId === "p2" && frame.stateType === "A" && frame.physics === "A" && frame.guardingFrames >= 1,
      ),
    ).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "override" && event.line.includes("HitOverride slot 3"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile HitOverride plus MissOnOverride one artifact", () => {
    const artifact = createSyntheticImportedProjectileHitOverrideMissOnOverrideOneTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitoverride-missonoverride-one-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitoverride-missonoverride-one-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([777, 889, 5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", "HitOverride"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", "hitoverride"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([777, 889, 5000, 150, 151]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitOverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitoverride).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reject");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.targetLinks).toEqual([]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hitsRemaining: 1, hasHit: false }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "reject" && event.line.includes("missonoverride = 1"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      life: 1000,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported Projectile ReversalDef artifact before hit or override contact", () => {
    const artifact = createSyntheticImportedProjectileReversalTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777, 888]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override", "reject"]));
    expect(evidence?.combatReasons).toContain("reversal");
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hitsRemaining: 1, hasHit: false }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("reversed"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 888,
      animNo: 888,
      life: 1000,
      moveType: "H",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "H",
    });
  });

  it("creates a synthetic imported helper Projectile ReversalDef artifact before hit or override contact", () => {
    const artifact = createSyntheticImportedHelperProjectileReversalTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777, 888]));
    expect(evidence?.executedStates).not.toContain(1291);
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override", "reject"]));
    expect(evidence?.combatReasons).toContain("reversal");
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1290, targetCount: 0 }),
        }),
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8878, hitsRemaining: 1, hasHit: false }),
        }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("reversed"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 888,
      animNo: 888,
      life: 1000,
      moveType: "H",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "H",
    });
  });

  it("creates a synthetic imported ReversalDef artifact with counter reason evidence", () => {
    const artifact = createSyntheticImportedReversalTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.evidence.actorSources).toEqual(["imported"]);
    expect(artifact.gates[0]?.evidence.executedStates).toEqual(expect.arrayContaining([200, 777, 778]));
    expect(artifact.gates[0]?.evidence.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("reversal");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("reversal");
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("reversed"))).toBe(true);
  });

  it("creates a synthetic imported custom-state ReversalDef artifact before p2stateno entry", () => {
    const artifact = createSyntheticImportedCustomStateReversalTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777, 888]));
    expect(evidence?.executedStates).not.toContain(889);
    expect(evidence?.executedStates).not.toContain(5000);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override", "reject"]));
    expect(evidence?.combatReasons).toContain("reversal");
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("p2->888"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 888,
      animNo: 888,
      customOwnerId: "p2",
      life: 1000,
      moveType: "H",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "H",
    });
  });

  it("creates a synthetic imported guard ReversalDef artifact before guard contact", () => {
    const artifact = createSyntheticImportedGuardReversalTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-guard-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-guard-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777, 888]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["AssertSpecial", "ChangeState", "HitDef", "ReversalDef"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777, 888]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5000, 150, 151]));
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "holdback"]));
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override", "reject"]));
    expect(evidence?.combatReasons).toContain("reversal");
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("p2->888"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 888,
      animNo: 888,
      life: 1000,
      moveType: "H",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "H",
    });
  });

  it("creates a synthetic imported walk-back guard ReversalDef artifact without no-walk stabilization", () => {
    const artifact = createSyntheticImportedWalkBackGuardReversalTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-walkback-guard-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-walkback-guard-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(gate?.requirements.requiredExecutedStates).toEqual([20, 200, 777, 888]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([5000, 150, 151]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "ReversalDef"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([20, 200, 777, 888]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5000, 150, 151]));
    expect(evidence?.executedControllers.AssertSpecial).toBeUndefined();
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.controllerEvents).toEqual(expect.arrayContaining([expect.objectContaining({ actorId: "p2", stateNo: 20, controller: "ReversalDef" })]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p2",
          stateNo: 20,
          animNo: 20,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minPos: expect.objectContaining({ x: expect.any(Number) }),
          maxPos: expect.objectContaining({ x: expect.any(Number) }),
        }),
      ]),
    );
    const walkFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 20);
    expect(walkFrame?.maxPos.x).toBeGreaterThanOrEqual(38);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "holdback"]));
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override", "reject"]));
    expect(evidence?.combatReasons).toContain("reversal");
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("p2->888"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 888,
      animNo: 888,
      life: 1000,
      moveType: "H",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "H",
    });
  });

  it("creates a synthetic imported crouch guard ReversalDef artifact before guard contact", () => {
    const artifact = createSyntheticImportedCrouchGuardReversalTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-crouch-guard-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-crouch-guard-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777, 888]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([5000, 150, 151, 152, 153]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["AssertSpecial", "ChangeState", "HitDef", "ReversalDef"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777, 888]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5000, 150, 151, 152, 153]));
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "holdback", "holddown"]));
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override", "reject"]));
    expect(evidence?.combatReasons).toContain("reversal");
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("p2->888"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 888,
      animNo: 888,
      life: 1000,
      moveType: "H",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "H",
    });
  });

  it("creates a synthetic imported air guard ReversalDef artifact before guard contact", () => {
    const artifact = createSyntheticImportedAirGuardReversalTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-guard-reversal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-air-guard-reversal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 777, 888]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([5000, 150, 151, 152, 153, 154, 155]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["AssertSpecial", "ChangeState", "HitDef", "ReversalDef"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 777, 888]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5000, 150, 151, 152, 153, 154, 155]));
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", stateNo: 40, controller: "ReversalDef" }),
        expect.objectContaining({ actorId: "p2", stateNo: 40, controller: "AssertSpecial" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", stateNo: 40, animNo: 40, stateType: "A", moveType: "I", physics: "A" }),
      ]),
    );
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "holdback"]));
    expect(evidence?.eventCategories).toContain("reversal");
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["hit", "guard", "override", "reject"]));
    expect(evidence?.combatReasons).toContain("reversal");
    expect(artifact.trace.events.some((event) => event.category === "reversal" && event.line.includes("p2->888"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 888,
      animNo: 888,
      life: 1000,
      moveType: "H",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 777,
      animNo: 777,
      life: 1000,
      moveType: "H",
    });
  });

  it("creates a synthetic imported damage-scale artifact with typed multiplier evidence", () => {
    const artifact = createSyntheticImportedDamageScaleTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-damage-scale-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-damage-scale-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedControllers.AttackMulSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.DefenceMulSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["damage-scale:attackmulset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["damage-scale:defencemulset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.eventLines.some((line) => line.includes("for 30"))).toBe(true);
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "imported", life: 970, moveType: "H" })]),
    );
  });

  it("creates a synthetic imported dynamic damage-scale artifact with typed multiplier evidence", () => {
    const artifact = createSyntheticImportedDynamicDamageScaleTraceArtifact({ generatedAt: "2026-07-09T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-damage-scale-dynamic-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-damage-scale-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedControllers.AttackMulSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.DefenceMulSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations["damage-scale:attackmulset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["damage-scale:defencemulset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventLines.some((line) => line.includes("for 30"))).toBe(true);
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "imported", life: 970, moveType: "H" })]),
    );
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ label: "dynamic AttackMulSet resolution order", actorId: "p1", allowSameTick: true }),
        expect.objectContaining({ label: "dynamic DefenceMulSet resolution order", actorId: "p2", allowSameTick: true }),
      ]),
    );
  });

  it("creates a synthetic imported data damage-scale artifact from CNS Data constants", () => {
    const artifact = createSyntheticImportedDataDamageScaleTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-data-damage-scale-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-data-damage-scale-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedControllers.AttackMulSet ?? 0).toBe(0);
    expect(evidence?.executedControllers.DefenceMulSet ?? 0).toBe(0);
    expect(evidence?.executedOperations["damage-scale:attackmulset"] ?? 0).toBe(0);
    expect(evidence?.executedOperations["damage-scale:defencemulset"] ?? 0).toBe(0);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventLines.some((line) => line.includes("for 30"))).toBe(true);
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "imported", life: 970, moveType: "H" })]),
    );
  });

  it("creates a synthetic imported bounds artifact with typed PosFreeze and ScreenBound evidence", () => {
    const artifact = createSyntheticImportedBoundsTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-bounds-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-bounds-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toContain("imported");
    expect(evidence?.executedControllers.PosFreeze).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ScreenBound).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["bounds:posfreeze"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["bounds:screenbound"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          posFreezeX: true,
          posFreezeY: false,
          screenBound: false,
          moveCameraX: false,
          moveCameraY: true,
        }),
      ]),
    );
  });

  it("creates a synthetic imported ScreenBound camera artifact with clamp and camera evidence", () => {
    const artifact = createSyntheticImportedScreenBoundCameraTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-screenbound-camera-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-screenbound-camera-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ScreenBound).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["bounds:screenbound"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.stageFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          stageId: "trace-screenbound-camera-grid",
          bounds: { left: -320, right: 320 },
          minCamera: expect.objectContaining({ x: 0 }),
          maxCamera: expect.objectContaining({ x: 155 }),
        }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          screenBound: false,
          moveCameraX: false,
          maxPos: expect.objectContaining({ x: 375 }),
        }),
      ]),
    );
    expect(artifact.trace.frames.some((frame) => frame.stage?.camera.x === 0)).toBe(true);
  });

  it("creates a synthetic imported dynamic ScreenBound artifact with typed bounds evidence", () => {
    const artifact = createSyntheticImportedDynamicScreenBoundTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-screenbound-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-screenbound-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ScreenBound).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["bounds:screenbound"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.stageFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          stageId: "trace-screenbound-camera-grid",
          bounds: { left: -320, right: 320 },
          minCamera: expect.objectContaining({ x: 0 }),
          maxCamera: expect.objectContaining({ x: 155 }),
        }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          screenBound: false,
          moveCameraX: false,
          moveCameraY: true,
          maxPos: expect.objectContaining({ x: 375 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Gravity artifact with typed vertical velocity evidence", () => {
    const artifact = createSyntheticImportedGravityTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gravity-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.Gravity).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:gravity"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          stateType: "A",
          animNo: 200,
          maxVel: expect.objectContaining({ y: expect.any(Number) }),
        }),
      ]),
    );
    const airborneFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p1" && frame.source === "imported" && frame.animNo === 200);
    expect(airborneFrame?.maxVel.y).toBeGreaterThanOrEqual(0.55);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        animNo: 200,
        stateType: "A",
        observedVelYAtLeast: 0.55,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported kinematic artifact with typed position and velocity evidence", () => {
    const artifact = createSyntheticImportedKinematicTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-kinematic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelMul).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:veladd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velmul"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          maxPos: expect.objectContaining({ x: expect.any(Number) }),
          maxVel: expect.objectContaining({ x: expect.any(Number) }),
          minVel: expect.objectContaining({ y: expect.any(Number) }),
        }),
      ]),
    );
    const kinematicFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p1" && frame.source === "imported" && frame.animNo === 200);
    expect(kinematicFrame?.maxPos.x).toBeGreaterThanOrEqual(-14);
    expect(kinematicFrame?.minPos.y).toBeLessThanOrEqual(-10);
    expect(kinematicFrame?.maxVel.x).toBeGreaterThanOrEqual(6);
    expect(kinematicFrame?.minVel.y).toBeLessThanOrEqual(-1);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        animNo: 200,
        observedPosXAtLeast: -14,
        observedPosYAtMost: -10,
        observedVelXAtLeast: 6,
        observedVelYAtMost: -1,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported dynamic VelAdd artifact with typed kinematic evidence", () => {
    const artifact = createSyntheticImportedDynamicVelAddTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-dynamic-veladd-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "VelSet", "VelAdd"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "kinematic:velset", "kinematic:veladd"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 dynamic VelAdd typed order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VarSet", name: "Dynamic VelAdd Var 0" },
          { stateNo: 200, controller: "VarSet", name: "Dynamic VelAdd Var 1" },
          { stateNo: 200, controller: "VelSet", name: "Dynamic VelAdd Seed Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
          { stateNo: 200, controller: "VelAdd", name: "Dynamic VelAdd Probe" },
          { stateNo: 200, operation: "kinematic:veladd" },
        ],
      },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:veladd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          maxVel: expect.objectContaining({ x: 4 }),
          minVel: expect.objectContaining({ y: -6 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic VelMul artifact with typed kinematic evidence", () => {
    const artifact = createSyntheticImportedDynamicVelMulTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-dynamic-velmul-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "VelSet", "VelMul"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "kinematic:velset", "kinematic:velmul"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 dynamic VelMul typed order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VarSet", name: "Dynamic VelMul Var 0" },
          { stateNo: 200, controller: "VarSet", name: "Dynamic VelMul Var 1" },
          { stateNo: 200, controller: "VelSet", name: "Dynamic VelMul Seed Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
          { stateNo: 200, controller: "VelMul", name: "Dynamic VelMul Probe" },
          { stateNo: 200, operation: "kinematic:velmul" },
        ],
      },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelMul).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velmul"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          maxVel: expect.objectContaining({ x: 8, y: 18 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic PosSet artifact with typed kinematic evidence", () => {
    const artifact = createSyntheticImportedDynamicPosSetTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-dynamic-posset-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "PosSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "kinematic:posset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 dynamic PosSet typed order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VarSet", name: "Dynamic PosSet Var 0" },
          { stateNo: 200, controller: "VarSet", name: "Dynamic PosSet Var 1" },
          { stateNo: 200, controller: "PosSet", name: "Dynamic PosSet Seed Position" },
          { stateNo: 200, operation: "kinematic:posset" },
          { stateNo: 200, controller: "PosSet", name: "Dynamic PosSet Probe" },
          { stateNo: 200, operation: "kinematic:posset" },
        ],
      },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:posset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          maxPos: expect.objectContaining({ x: 16 }),
          minPos: expect.objectContaining({ y: -20 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic PosAdd artifact with typed kinematic evidence", () => {
    const artifact = createSyntheticImportedDynamicPosAddTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-dynamic-posadd-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "PosSet", "PosAdd"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "kinematic:posset", "kinematic:posadd"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 dynamic PosAdd typed order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VarSet", name: "Dynamic PosAdd Var 0" },
          { stateNo: 200, controller: "VarSet", name: "Dynamic PosAdd Var 1" },
          { stateNo: 200, controller: "PosSet", name: "Dynamic PosAdd Seed Position" },
          { stateNo: 200, operation: "kinematic:posset" },
          { stateNo: 200, controller: "PosAdd", name: "Dynamic PosAdd Probe" },
          { stateNo: 200, operation: "kinematic:posadd" },
        ],
      },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:posset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          maxPos: expect.objectContaining({ x: 18 }),
          minPos: expect.objectContaining({ y: -24 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported controller-param bottom artifact with zero fallback evidence", () => {
    const artifact = createSyntheticImportedControllerParamBottomTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-controller-param-bottom-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VelSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["kinematic:velset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 controller-param bottom VelSet order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VelSet", name: "Seed Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
          { stateNo: 200, controller: "VelSet", name: "Bottom Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
        ],
      },
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 200,
        animNo: 200,
        observedVelXAtLeast: 4,
        observedVelXAtMost: 0,
        observedVelYAtLeast: 0,
        observedVelYAtMost: -2,
        minFrames: 2,
      },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          minVel: expect.objectContaining({ x: 0, y: -2 }),
          maxVel: expect.objectContaining({ x: 4, y: 0 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported controller-param Target redirect artifact with redirected value evidence", () => {
    const artifact = createSyntheticImportedControllerParamTargetRedirectTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-controller-param-target-redirect-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "VelSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "kinematic:velset"]);
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 controller-param Target redirect VelSet order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VelSet", name: "Seed Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
          { stateNo: 200, controller: "HitDef" },
          { stateNo: 200, controller: "VelSet", name: "Target Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
        ],
      },
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 200,
        animNo: 200,
        observedVelXAtLeast: 4,
        observedVelXAtMost: 3,
        observedVelYAtLeast: -2,
        observedVelYAtMost: -3,
        minFrames: 2,
      },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          minVel: expect.objectContaining({ y: expect.any(Number) }),
          maxVel: expect.objectContaining({ x: 4 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported controller-param Root redirect artifact with redirected value evidence", () => {
    const artifact = createSyntheticImportedControllerParamRootRedirectTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-controller-param-root-redirect-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VelSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["kinematic:velset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "200 controller-param Root redirect VelSet order",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "VelSet", name: "Seed Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
          { stateNo: 200, controller: "VelSet", name: "Root Controller Param Velocity" },
          { stateNo: 200, operation: "kinematic:velset" },
        ],
      },
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 200,
        animNo: 200,
        observedVelXAtLeast: 5,
        observedVelXAtMost: 4,
        observedVelYAtLeast: -2,
        observedVelYAtMost: -3,
        minFrames: 2,
      },
    ]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          stateNo: 200,
          animNo: 200,
          minVel: expect.objectContaining({ y: -3 }),
          maxVel: expect.objectContaining({ x: 5 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Width artifact with typed collision evidence", () => {
    const artifact = createSyntheticImportedWidthTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-width-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-width-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.Width).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["collision:width"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          bodyWidthFront: 18,
          bodyWidthBack: 44,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.bodyWidth?.front === 18 && actor.bodyWidth.back === 44)).toBe(true);
  });

  it("creates a synthetic imported dynamic Width artifact with typed collision evidence", () => {
    const artifact = createSyntheticImportedDynamicWidthTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-width-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedControllers.Width).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["collision:width"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          bodyWidthFront: 21,
          bodyWidthBack: 43,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.bodyWidth?.front === 21 && actor.bodyWidth.back === 43)).toBe(true);
  });

  it("creates a synthetic imported StateTypeSet artifact with typed metadata evidence", () => {
    const artifact = createSyntheticImportedStateTypeSetTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-statetypeset-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-statetypeset-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          stateType: "C",
          moveType: "A",
          physics: "N",
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some((actor) => actor.stateType === "C" && actor.moveType === "A" && actor.physics === "N"),
    ).toBe(true);
  });

  it("creates a synthetic imported dynamic StateTypeSet artifact with resolved metadata evidence", () => {
    const artifact = createSyntheticImportedDynamicStateTypeSetTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-statetypeset-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-statetypeset-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          stateType: "C",
          moveType: "A",
          physics: "N",
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some((actor) => actor.stateType === "C" && actor.moveType === "A" && actor.physics === "N"),
    ).toBe(true);
  });

  it("creates a synthetic imported PlayerPush artifact with typed collision evidence", () => {
    const artifact = createSyntheticImportedPlayerPushTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-playerpush-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-playerpush-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.PlayerPush).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["collision:playerpush"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          playerPush: false,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.playerPush === false)).toBe(true);
  });

  it("creates a synthetic imported dynamic PlayerPush artifact with typed collision evidence", () => {
    const artifact = createSyntheticImportedDynamicPlayerPushTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-playerpush-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PlayerPush).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["collision:playerpush"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          playerPush: false,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.playerPush === false)).toBe(true);
  });

  it("creates a synthetic imported dynamic PosFreeze artifact with typed bounds evidence", () => {
    const artifact = createSyntheticImportedDynamicPosFreezeTraceArtifact({ generatedAt: "2026-07-08T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-posfreeze-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosFreeze).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["bounds:posfreeze"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          posFreezeX: true,
          posFreezeY: false,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.posFreeze?.x === true && actor.posFreeze.y === false)).toBe(true);
  });

  it("creates a synthetic imported Turn artifact with typed orientation evidence", () => {
    const artifact = createSyntheticImportedTurnTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-turn-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-turn-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.Turn).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["orientation:turn"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          facing: -1,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.facing === -1)).toBe(true);
  });

  it("creates a synthetic imported SprPriority artifact with typed sprite-effect evidence", () => {
    const artifact = createSyntheticImportedSprPriorityTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-sprpriority-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-sprpriority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.SprPriority).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:sprpriority"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          spritePriority: 5,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.spritePriority === 5)).toBe(true);
  });

  it("creates a synthetic imported dynamic SprPriority artifact with expression fallback evidence", () => {
    const artifact = createSyntheticImportedDynamicSprPriorityTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-sprpriority-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-sprpriority-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SprPriority).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:sprpriority"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          spritePriority: 4,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "variable:varset",
      "sprite-effect:sprpriority",
      "hitdef",
    ]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.spritePriority === 4)).toBe(true);
  });

  it("creates a synthetic imported PalFX artifact with typed sprite-effect evidence", () => {
    const artifact = createSyntheticImportedPalFxTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-palfx-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-palfx-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.PalFX).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:palfx"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          paletteFxTime: 18,
          paletteFxAddR: 80,
          paletteFxAddG: -10,
          paletteFxAddB: 255,
          paletteFxMulG: 160,
          paletteFxColor: 256,
          paletteFxInvert: true,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) => actor.id === "p1" && actor.paletteFx?.time === 18 && actor.paletteFx.add[2] === 255 && actor.paletteFx.invert,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported dynamic PalFX artifact with expression fallback evidence", () => {
    const artifact = createSyntheticImportedDynamicPalFxTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-palfx-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-palfx-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PalFX).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:palfx"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          paletteFxTime: 12,
          paletteFxAddR: 64,
          paletteFxAddG: -16,
          paletteFxAddB: 255,
          paletteFxMulR: 224,
          paletteFxMulG: 144,
          paletteFxMulB: 256,
          paletteFxColor: 200,
          paletteFxInvert: true,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) =>
          actor.id === "p1" &&
          actor.paletteFx?.time === 12 &&
          actor.paletteFx.add[2] === 255 &&
          actor.paletteFx.mul[1] === 144 &&
          actor.paletteFx.color === 200 &&
          actor.paletteFx.invert,
      ),
    ).toBe(true);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "variable:varset",
      "sprite-effect:palfx",
      "hitdef",
    ]);
  });

  it("creates a synthetic imported Trans artifact with typed sprite opacity evidence", () => {
    const artifact = createSyntheticImportedTransTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-trans-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-trans-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.Trans).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:trans"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        animNo: 200,
        observedOpacityAtMost: 0.5,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported dynamic Trans artifact with expression fallback evidence", () => {
    const artifact = createSyntheticImportedDynamicTransTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-trans-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-trans-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Trans).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:trans"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          maxOpacity: 0.375,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.renderOpacity === 0.375)).toBe(true);
  });

  it("creates a synthetic imported AngleDraw artifact with typed sprite rotation evidence", () => {
    const artifact = createSyntheticImportedAngleTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-angle-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-angle-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AngleSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleDraw).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angleset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angleadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angledraw"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          animNo: 200,
          minAngle: 55,
          maxAngle: 55,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        animNo: 200,
        observedAngleAtLeast: 55,
        observedAngleAtMost: 55,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported AngleMul artifact with typed sprite rotation evidence", () => {
    const artifact = createSyntheticImportedAngleMulTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-anglemul-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-anglemul-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AngleSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleMul).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleDraw).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angleset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:anglemul"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angledraw"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          animNo: 200,
          minAngle: 45,
          maxAngle: 45,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        animNo: 200,
        observedAngleAtLeast: 45,
        observedAngleAtMost: 45,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported dynamic AngleMul artifact with typed sprite rotation evidence", () => {
    const artifact = createSyntheticImportedDynamicAngleMulTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-anglemul-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-anglemul-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleMul).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleDraw).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angleset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:anglemul"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angledraw"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          animNo: 200,
          minAngle: 45,
          maxAngle: 45,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        animNo: 200,
        observedAngleAtLeast: 45,
        observedAngleAtMost: 45,
        minFrames: 1,
      },
    ]);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p1" && actor.renderAngle === 45)).toBe(true);
  });

  it("creates a synthetic imported dynamic AngleDraw artifact with typed sprite rotation evidence", () => {
    const artifact = createSyntheticImportedDynamicAngleTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-angle-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-angle-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AngleDraw).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angleset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angleadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:angledraw"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          animNo: 200,
          minAngle: 35,
          maxAngle: 35,
          minScale: { x: 2, y: 0.5 },
          maxScale: { x: 2, y: 0.5 },
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        animNo: 200,
        observedAngleAtLeast: 35,
        observedAngleAtMost: 35,
        observedScaleXAtLeast: 2,
        observedScaleXAtMost: 2,
        observedScaleYAtLeast: 0.5,
        observedScaleYAtMost: 0.5,
        minFrames: 1,
      },
    ]);
    expect(
      artifact.trace.finalActors.some(
        (actor) => actor.id === "p1" && actor.renderAngle === 35 && actor.renderScale?.x === 2 && actor.renderScale.y === 0.5,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported EnvColor artifact with typed stage-flash evidence", () => {
    const artifact = createSyntheticImportedEnvColorTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-envcolor-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-envcolor-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.EnvColor).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.envcolor).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredStageFrames).toEqual([
      {
        stageId: "trace-close-training-grid",
        envColorR: 16,
        envColorG: 96,
        envColorB: 255,
        envColorUnder: false,
        observedEnvColorOpacityAtLeast: 0.2,
        minFrames: 1,
      },
    ]);
    expect(evidence?.stageFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          stageId: "trace-close-training-grid",
          envColor: expect.objectContaining({
            color: [16, 96, 255],
            under: false,
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported EnvColor under-layer artifact with typed stage-flash evidence", () => {
    const artifact = createSyntheticImportedEnvColorUnderTraceArtifact({ generatedAt: "2026-07-02T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-envcolor-under-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-envcolor-under-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.EnvColor).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.envcolor).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredStageFrames).toEqual([
      {
        stageId: "trace-close-training-grid",
        envColorR: 16,
        envColorG: 96,
        envColorB: 255,
        envColorUnder: true,
        observedEnvColorOpacityAtLeast: 0.2,
        minFrames: 1,
      },
    ]);
    expect(evidence?.stageFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          stageId: "trace-close-training-grid",
          envColor: expect.objectContaining({
            color: [16, 96, 255],
            under: true,
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic EnvColor artifact with typed expression evidence", () => {
    const artifact = createSyntheticImportedDynamicEnvColorTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-envcolor-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-envcolor-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "EnvColor", "HitDef"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "envcolor", "hitdef"]);
    expect(artifact.gates[0]?.requirements.requiredStageFrames).toEqual([
      {
        stageId: "trace-close-training-grid",
        envColorR: 32,
        envColorG: 128,
        envColorB: 240,
        envColorUnder: true,
        observedEnvColorOpacityAtLeast: 0.2,
        minFrames: 1,
      },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.EnvColor).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.envcolor).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.stageFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          stageId: "trace-close-training-grid",
          envColor: expect.objectContaining({
            color: [32, 128, 240],
            under: true,
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported EnvShake artifact with runtime camera event evidence", () => {
    const artifact = createSyntheticImportedEnvShakeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-envshake-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "EnvShake", "HitDef"]);
    expect(artifact.gates[0]?.requirements.requiredEnvShakeEvents).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        time: 16,
        freq: 30,
        ampl: -7,
        phase: 0.5,
        stateNo: 200,
      },
    ]);
    expect(evidence?.executedControllers.EnvShake).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.envshake).toBeGreaterThanOrEqual(1);
    expect(evidence?.envShakeEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", time: 16, freq: 30, ampl: -7, phase: 0.5, stateNo: 200 }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.envShakeEvents).toEqual(
      expect.arrayContaining([expect.objectContaining({ time: 16, freq: 30, ampl: -7, phase: 0.5, stateNo: 200 })]),
    );
  });

  it("creates a synthetic imported dynamic EnvShake artifact with typed expression evidence", () => {
    const artifact = createSyntheticImportedDynamicEnvShakeTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-envshake-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "EnvShake", "HitDef"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "envshake", "hitdef"]);
    expect(artifact.gates[0]?.requirements.requiredEnvShakeEvents).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        time: 18,
        freq: 45,
        ampl: -9,
        phase: 0.25,
        stateNo: 200,
      },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.EnvShake).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.envshake).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.envShakeEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", time: 18, freq: 45, ampl: -9, phase: 0.25, stateNo: 200 }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.envShakeEvents).toEqual(
      expect.arrayContaining([expect.objectContaining({ time: 18, freq: 45, ampl: -9, phase: 0.25, stateNo: 200 })]),
    );
  });

  it("creates a synthetic imported RemapPal artifact with typed sprite-effect evidence", () => {
    const artifact = createSyntheticImportedRemapPalTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-remappal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-remappal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.RemapPal).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:remappal"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          paletteRemapSourceGroup: 1,
          paletteRemapSourceIndex: 1,
          paletteRemapDestGroup: 2,
          paletteRemapDestIndex: 3,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) => actor.id === "p1" && actor.paletteRemap?.source[0] === 1 && actor.paletteRemap.dest[1] === 3,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported dynamic RemapPal artifact with typed expression evidence", () => {
    const artifact = createSyntheticImportedDynamicRemapPalTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-remappal-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-remappal-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "variable:varset",
      "sprite-effect:remappal",
      "hitdef",
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedControllers.RemapPal).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:remappal"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          paletteRemapSourceGroup: 1,
          paletteRemapSourceIndex: 5,
          paletteRemapDestGroup: 2,
          paletteRemapDestIndex: 7,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) => actor.id === "p1" && actor.paletteRemap?.source[1] === 5 && actor.paletteRemap.dest[1] === 7,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported PalFX + RemapPal artifact with combined palette evidence", () => {
    const artifact = createSyntheticImportedPalFxRemapPalTraceArtifact({ generatedAt: "2026-07-02T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-palfx-remappal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-palfx-remappal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.PalFX).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.RemapPal).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:palfx"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:remappal"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          paletteFxTime: 14,
          paletteFxAddR: 40,
          paletteFxAddG: 24,
          paletteFxAddB: -30,
          paletteFxMulR: 224,
          paletteFxMulG: 192,
          paletteFxMulB: 256,
          paletteFxColor: 180,
          paletteRemapSourceGroup: 1,
          paletteRemapSourceIndex: 0,
          paletteRemapDestGroup: 2,
          paletteRemapDestIndex: 4,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) =>
          actor.id === "p1" &&
          actor.paletteFx?.time === 14 &&
          actor.paletteFx.add[0] === 40 &&
          actor.paletteRemap?.source[1] === 0 &&
          actor.paletteRemap.dest[1] === 4,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported AfterImage artifact with typed sprite-effect evidence", () => {
    const artifact = createSyntheticImportedAfterImageTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-afterimage-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-afterimage-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AfterImage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AfterImageTime).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:afterimage"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:afterimagetime"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          afterImageTime: 20,
          afterImageLength: 4,
          afterImageTimeGap: 2,
          afterImageFrameGap: 1,
          afterImageSampleCount: 1,
          afterImageOpacity: 0.34,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) => actor.id === "p1" && actor.afterImage?.length === 4 && actor.afterImage.sampleCount >= 1,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported dynamic AfterImage artifact with typed expression evidence", () => {
    const artifact = createSyntheticImportedDynamicAfterImageTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-afterimage-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-afterimage-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AfterImage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:afterimage"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          afterImageTime: 18,
          afterImageLength: 5,
          afterImageTimeGap: 2,
          afterImageFrameGap: 3,
          afterImageSampleCount: 1,
          afterImageOpacity: 0.34,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) => actor.id === "p1" && actor.afterImage?.time === 18 && actor.afterImage.length === 5,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported dynamic AfterImageTime artifact with typed expression evidence", () => {
    const artifact = createSyntheticImportedDynamicAfterImageTimeTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-afterimagetime-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-afterimagetime-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AfterImage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.AfterImageTime).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:afterimage"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["sprite-effect:afterimagetime"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          animNo: 200,
          afterImageTime: 14,
          afterImageLength: 4,
          afterImageTimeGap: 1,
          afterImageFrameGap: 1,
          afterImageSampleCount: 1,
          afterImageOpacity: 0.34,
        }),
      ]),
    );
    expect(
      artifact.trace.finalActors.some(
        (actor) => actor.id === "p1" && actor.afterImage?.time === 14 && actor.afterImage.length === 4,
      ),
    ).toBe(true);
  });

  it("creates a synthetic imported HitDef priority artifact with bounded direct-clash evidence", () => {
    const artifact = createSyntheticImportedHitDefPriorityTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitdef-priority-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toEqual(expect.arrayContaining(["hit", "runtime"]));
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.eventLines.some((line) => line.includes("HitDef priority clash") && line.includes("priority 6 beat"))).toBe(true);
    expect(evidence?.eventLines.some((line) => line.includes("hit Synthetic Imported HitDef Priority P2 for 31"))).toBe(true);
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ id: "p1", source: "imported", life: 1000 }),
        expect.objectContaining({ id: "p2", source: "imported", life: 969, moveType: "H" }),
      ]),
    );
  });

  it("creates a synthetic imported HitDef kill artifact with nonlethal final-life evidence", () => {
    const artifact = createSyntheticImportedHitDefKillTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-kill-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-hitdef-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.eventLines.some((line) => line.includes("hit Mira Volt for 2000"))).toBe(true);
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "demo", life: 1, moveType: "H" })]),
    );
  });

  it("creates a synthetic imported HitDef guard.kill artifact with nonlethal guard evidence", () => {
    const artifact = createSyntheticImportedHitDefGuardKillTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-guard-kill-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-hitdef-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported HitDef Guard Kill Attacker for 2000"))).toBe(true);
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "demo", life: 1 })]),
    );
  });

  it("creates a synthetic imported HitDef guard chip KO artifact with round evidence", () => {
    const artifact = createSyntheticImportedHitDefGuardKoTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-guard-ko-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-hitdef-guard-ko-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported HitDef Guard KO for 2000"))).toBe(true);
    expect(evidence?.roundFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          state: "ko",
          winner: "Synthetic Imported HitDef Guard KO",
          message: "Synthetic Imported HitDef Guard KO wins",
        }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "demo", life: 0 })]),
    );
  });

  it("creates a synthetic imported guard artifact with held-back guard reason evidence", () => {
    const artifact = createSyntheticImportedGuardTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-guard-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const defenderFrames = artifact.trace.combatReasons.filter((reason) => reason.reason === "guard");
    expect(defenderFrames.length).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.actorSources).toEqual(["demo", "imported"]);
    expect(artifact.gates[0]?.evidence.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedStates).toContain(260);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 260]);
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("guard");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("guard");
    expect(artifact.trace.events.some((event) => event.category === "guard" && event.line.includes("guarded"))).toBe(true);
  });

  it("creates a synthetic imported HitDef guard-sound artifact", () => {
    const artifact = createSyntheticImportedHitDefGuardSoundTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-guard-sound-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          type: "PlaySnd",
          group: 6,
          index: 0,
          stateNo: 200,
        }),
      ]),
    );
  });

  it("creates a synthetic imported HitDef hit-sound artifact", () => {
    const artifact = createSyntheticImportedHitDefHitSoundTraceArtifact({ generatedAt: "2026-06-28T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-hit-sound-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          type: "PlaySnd",
          group: 5,
          index: 0,
          raw: "S5,0",
          stateNo: 200,
        }),
      ]),
    );
  });

  it("creates a synthetic imported HitDef dynamic hitsound artifact", () => {
    const artifact = createSyntheticImportedHitDefDynamicHitSoundTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-dynamic-hitsound-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "audio:playsnd"]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          type: "PlaySnd",
          group: 5,
          index: 4,
          raw: "Fvar(0),var(1)",
          soundPrefix: "kfm",
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "PlaySnd",
          group: 5,
          index: 4,
          raw: "Fvar(0),var(1)",
          soundPrefix: "kfm",
          contactKind: "hit",
          stateNo: 200,
        }),
      ]),
    );
  });

  it("creates a synthetic imported HitDef dynamic guardsound artifact", () => {
    const artifact = createSyntheticImportedHitDefDynamicGuardSoundTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-dynamic-guardsound-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "audio:playsnd"]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          type: "PlaySnd",
          group: 6,
          index: 4,
          raw: "Fvar(0),var(1)",
          soundPrefix: "kfm",
          contactKind: "guard",
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "PlaySnd",
          group: 6,
          index: 4,
          raw: "Fvar(0),var(1)",
          soundPrefix: "kfm",
          contactKind: "guard",
          stateNo: 200,
        }),
      ]),
    );
  });

  it("creates a synthetic imported HitDef hit-spark artifact", () => {
    const artifact = createSyntheticImportedHitDefHitSparkTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-hit-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "hit",
          sparkNo: 7001,
          raw: "S7001",
          rawPrefix: "S",
          offset: { x: 10, y: -72 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "hit",
        raw: "S7001",
        offsetX: 10,
        offsetY: -72,
      }),
    ]);
  });

  it("creates a synthetic imported HitDef data-spark fallback artifact", () => {
    const artifact = createSyntheticImportedHitDefDataSparkTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-data-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "hit",
          sparkNo: 2,
          raw: "2",
          rawPrefix: undefined,
          offset: { x: -8, y: -54 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "hit",
        raw: "2",
        offsetX: -8,
        offsetY: -54,
      }),
    ]);
  });

  it("creates a synthetic imported HitDef common-spark artifact with asset-frame evidence", () => {
    const artifact = createSyntheticImportedHitDefCommonSparkTraceArtifact({ generatedAt: "2026-06-29T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-common-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "hit",
          sparkNo: 7001,
          raw: "7001",
          rawPrefix: undefined,
          assetSource: "common",
          assetActionId: 7001,
          assetFrameIndex: 0,
          assetFrameOffsetX: 3,
          assetFrameOffsetY: -4,
          assetFrameDuration: 5,
          assetSpriteGroup: 7101,
          assetSpriteIndex: 0,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          assetFrameIndices: [0, 1],
          offset: { x: 16, y: -66 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        assetSource: "common",
        assetFrameOffsetX: 3,
        assetFrameOffsetY: -4,
        assetFrameDuration: 5,
        offsetX: 16,
        offsetY: -66,
        minAssetFrameCount: 2,
        minAssetTotalDuration: 11,
        requiredAssetFrameIndices: [0, 1],
      }),
    ]);
  });

  it("creates a synthetic imported HitDef FightFX-spark artifact with asset-frame evidence", () => {
    const artifact = createSyntheticImportedHitDefFightFxSparkTraceArtifact({ generatedAt: "2026-06-29T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-fightfx-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "hit",
          sparkNo: 7002,
          raw: "F7002",
          rawPrefix: "F",
          assetSource: "fightfx",
          fightFxPrefix: "kfm",
          assetActionId: 7002,
          assetFrameIndex: 0,
          assetFrameOffsetX: 3,
          assetFrameOffsetY: -4,
          assetFrameDuration: 5,
          assetSpriteGroup: 8102,
          assetSpriteIndex: 0,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          assetFrameIndices: [0, 1],
          offset: { x: 18, y: -68 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        assetSource: "fightfx",
        fightFxPrefix: "kfm",
        assetFrameOffsetX: 3,
        assetFrameOffsetY: -4,
        assetFrameDuration: 5,
        offsetX: 18,
        offsetY: -68,
        minAssetFrameCount: 2,
        minAssetTotalDuration: 11,
        requiredAssetFrameIndices: [0, 1],
      }),
    ]);
  });

  it("creates a synthetic imported HitDef hit-effect package artifact with sound and FightFX spark evidence", () => {
    const artifact = createSyntheticImportedHitDefHitEffectPackageTraceArtifact({ generatedAt: "2026-06-29T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-hit-effect-package-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-x-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          type: "PlaySnd",
          group: 5,
          index: 0,
          raw: "F5,0",
          soundPrefix: "kfm",
          stateNo: 200,
        }),
      ]),
    );
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "hit",
          sparkNo: 7002,
          raw: "F7002",
          rawPrefix: "F",
          assetSource: "fightfx",
          assetActionId: 7002,
          assetFrameIndex: 0,
          assetFrameOffsetX: 3,
          assetFrameOffsetY: -4,
          assetFrameDuration: 5,
          assetSpriteGroup: 8102,
          assetSpriteIndex: 0,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          assetFrameIndices: [0, 1],
          offset: { x: 18, y: -68 },
          stateNo: 200,
        }),
      ]),
    );
    const contactPackage = evidence?.contactEffectPackages.find((entry) => entry.contactKind === "hit");
    expect(contactPackage).toMatchObject({
      actorId: "p1",
      source: "imported",
      actorKind: "player",
      contactKind: "hit",
      sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 0, raw: "F5,0", soundPrefix: "kfm", contactKind: "hit" }),
      hitEffect: expect.objectContaining({
        kind: "hit",
        sparkNo: 7002,
        assetSource: "fightfx",
        offset: { x: 18, y: -68 },
        contactKind: "hit",
      }),
    });
    expect(contactPackage?.contactId).toBe(contactPackage?.sound.contactId);
    expect(contactPackage?.contactId).toBe(contactPackage?.hitEffect.contactId);
    expect(contactPackage?.contactTick).toBe(contactPackage?.sound.contactTick);
    expect(contactPackage?.contactTick).toBe(contactPackage?.hitEffect.contactTick);
    expect(artifact.gates[0]?.requirements.requiredSoundEvents).toEqual([
      expect.objectContaining({
        type: "PlaySnd",
        group: 5,
        index: 0,
        raw: "F5,0",
        soundPrefix: "kfm",
      }),
    ]);
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "hit",
        assetSource: "fightfx",
        offsetX: 18,
        offsetY: -68,
        minAssetFrameCount: 2,
        minAssetTotalDuration: 11,
        requiredAssetFrameIndices: [0, 1],
      }),
    ]);
    expect(artifact.gates[0]?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        contactKind: "hit",
        sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 0, raw: "F5,0", soundPrefix: "kfm", requireContactId: true }),
        hitEffect: expect.objectContaining({
          kind: "hit",
          sparkNo: 7002,
          assetSource: "fightfx",
          offsetX: 18,
          offsetY: -68,
          requiredAssetFrameIndices: [0, 1],
          requireContactId: true,
        }),
      }),
    ]);
  });

  it("creates a synthetic imported HitDef guard-spark artifact", () => {
    const artifact = createSyntheticImportedHitDefGuardSparkTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-guard-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "guard",
          sparkNo: 7000,
          raw: "S7000",
          rawPrefix: "S",
          offset: { x: 12, y: -64 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "guard",
        raw: "S7000",
        offsetX: 12,
        offsetY: -64,
      }),
    ]);
  });

  it("creates a synthetic imported HitDef data guard-spark fallback artifact", () => {
    const artifact = createSyntheticImportedHitDefDataGuardSparkTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-data-guard-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "guard",
          sparkNo: 40,
          raw: "40",
          rawPrefix: undefined,
          offset: { x: 13, y: -57 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "guard",
        raw: "40",
        offsetX: 13,
        offsetY: -57,
      }),
    ]);
  });

  it("creates a synthetic imported HitDef common guard-spark artifact with asset-frame evidence", () => {
    const artifact = createSyntheticImportedHitDefCommonGuardSparkTraceArtifact({ generatedAt: "2026-06-29T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-common-guard-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "guard",
          sparkNo: 7003,
          raw: "7003",
          rawPrefix: undefined,
          assetSource: "common",
          assetActionId: 7003,
          assetFrameIndex: 0,
          assetFrameOffsetX: 3,
          assetFrameOffsetY: -4,
          assetFrameDuration: 5,
          assetSpriteGroup: 7103,
          assetSpriteIndex: 0,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          assetFrameIndices: [0, 1],
          offset: { x: 14, y: -62 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "guard",
        assetSource: "common",
        assetFrameOffsetX: 3,
        assetFrameOffsetY: -4,
        assetFrameDuration: 5,
        offsetX: 14,
        offsetY: -62,
        minAssetFrameCount: 2,
        minAssetTotalDuration: 11,
        requiredAssetFrameIndices: [0, 1],
      }),
    ]);
  });

  it("creates a synthetic imported HitDef FightFX guard-spark artifact with asset-frame evidence", () => {
    const artifact = createSyntheticImportedHitDefFightFxGuardSparkTraceArtifact({ generatedAt: "2026-06-29T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-fightfx-guard-spark-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "guard",
          sparkNo: 7004,
          raw: "F7004",
          rawPrefix: "F",
          assetSource: "fightfx",
          assetActionId: 7004,
          assetFrameIndex: 0,
          assetFrameOffsetX: 3,
          assetFrameOffsetY: -4,
          assetFrameDuration: 5,
          assetSpriteGroup: 8104,
          assetSpriteIndex: 0,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          assetFrameIndices: [0, 1],
          offset: { x: 15, y: -63 },
          stateNo: 200,
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "guard",
        assetSource: "fightfx",
        assetFrameOffsetX: 3,
        assetFrameOffsetY: -4,
        assetFrameDuration: 5,
        offsetX: 15,
        offsetY: -63,
        minAssetFrameCount: 2,
        minAssetTotalDuration: 11,
        requiredAssetFrameIndices: [0, 1],
      }),
    ]);
  });

  it("creates a synthetic imported HitDef guard-effect package artifact with sound and FightFX spark evidence", () => {
    const artifact = createSyntheticImportedHitDefGuardEffectPackageTraceArtifact({ generatedAt: "2026-06-29T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-guard-effect-package-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          type: "PlaySnd",
          group: 6,
          index: 0,
          stateNo: 200,
        }),
      ]),
    );
    expect(evidence?.hitEffectEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          kind: "guard",
          sparkNo: 7004,
          raw: "F7004",
          rawPrefix: "F",
          assetSource: "fightfx",
          assetActionId: 7004,
          assetFrameIndex: 0,
          assetFrameOffsetX: 3,
          assetFrameOffsetY: -4,
          assetFrameDuration: 5,
          assetSpriteGroup: 8104,
          assetSpriteIndex: 0,
          assetFrameCount: 2,
          assetTotalDuration: 11,
          assetFrameIndices: [0, 1],
          offset: { x: 15, y: -63 },
          stateNo: 200,
        }),
      ]),
    );
    const contactPackage = evidence?.contactEffectPackages.find((entry) => entry.contactKind === "guard");
    expect(contactPackage).toMatchObject({
      actorId: "p1",
      source: "imported",
      actorKind: "player",
      contactKind: "guard",
      sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, contactKind: "guard" }),
      hitEffect: expect.objectContaining({
        kind: "guard",
        sparkNo: 7004,
        assetSource: "fightfx",
        offset: { x: 15, y: -63 },
        contactKind: "guard",
      }),
    });
    expect(contactPackage?.contactId).toBe(contactPackage?.sound.contactId);
    expect(contactPackage?.contactId).toBe(contactPackage?.hitEffect.contactId);
    expect(contactPackage?.contactTick).toBe(contactPackage?.sound.contactTick);
    expect(contactPackage?.contactTick).toBe(contactPackage?.hitEffect.contactTick);
    expect(artifact.gates[0]?.requirements.requiredSoundEvents).toEqual([
      expect.objectContaining({
        type: "PlaySnd",
        group: 6,
        index: 0,
      }),
    ]);
    expect(artifact.gates[0]?.requirements.requiredHitEffectEvents).toEqual([
      expect.objectContaining({
        kind: "guard",
        assetSource: "fightfx",
        offsetX: 15,
        offsetY: -63,
        minAssetFrameCount: 2,
        minAssetTotalDuration: 11,
        requiredAssetFrameIndices: [0, 1],
      }),
    ]);
    expect(artifact.gates[0]?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        contactKind: "guard",
        sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, requireContactId: true }),
        hitEffect: expect.objectContaining({
          kind: "guard",
          sparkNo: 7004,
          assetSource: "fightfx",
          offsetX: 15,
          offsetY: -63,
          requiredAssetFrameIndices: [0, 1],
          requireContactId: true,
        }),
      }),
    ]);
  });

  it("creates a generic imported guard artifact for imported fighters", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "custom-imported-guard",
      displayName: "Custom Imported Guard",
      guardDamage: 3,
      guardFlag: "MA",
    });

    const artifact = createImportedGuardTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "custom-imported-guard-golden",
      targetLabel: "Custom Imported Guard Route",
    });
    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "custom-imported-guard-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("guard");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("guard");
  });

  it("creates a synthetic imported AssertSpecial unguardable artifact with hit evidence despite held-back input", () => {
    const artifact = createSyntheticImportedAssertSpecialUnguardableTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-unguardable-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-unguardable-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.eventCategories).not.toContain("guard");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(artifact.trace.events.some((event) => event.category === "hit" && event.line.includes("hit"))).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "guard")).toBe(false);
  });

  it("creates a synthetic imported AssertSpecial guard-deny artifact with NoStandGuard hit evidence", () => {
    const artifact = createSyntheticImportedAssertSpecialGuardDenyTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-guarddeny-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-guarddeny-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.eventCategories).not.toContain("guard");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      life: 963,
    });
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ actorId: "p1", source: "imported", moveType: "H" })]),
    );
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "NoStandGuard defender AssertSpecial before guardable HitDef",
        allowSameTick: true,
        steps: [
          { actorId: "p1", stateNo: 0, controller: "AssertSpecial", name: "Passive AssertSpecial" },
          { actorId: "p2", stateNo: 200, controller: "HitDef" },
        ],
      },
    ]);
    expect(artifact.trace.events.some((event) => event.category === "guard")).toBe(false);
  });

  it("creates a synthetic imported AssertSpecial crouch guard-deny artifact with NoCrouchGuard hit evidence", () => {
    const artifact = createSyntheticImportedAssertSpecialCrouchGuardDenyTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-crouch-guarddeny-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-crouch-guarddeny-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "holddown", "x"]));
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.eventCategories).not.toContain("guard");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      life: 963,
    });
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ actorId: "p1", source: "imported", stateType: "C", moveType: "H" })]),
    );
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "NoCrouchGuard defender AssertSpecial before guardable HitDef",
        allowSameTick: true,
        steps: [
          { actorId: "p1", stateNo: 10, controller: "AssertSpecial", name: "AssertSpecial Control Flags" },
          { actorId: "p2", stateNo: 200, controller: "HitDef" },
        ],
      },
    ]);
    expect(artifact.trace.events.some((event) => event.category === "guard")).toBe(false);
  });

  it("creates a synthetic imported AssertSpecial air guard-deny artifact with NoAirGuard hit evidence", () => {
    const artifact = createSyntheticImportedAssertSpecialAirGuardDenyTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-air-guarddeny-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-air-guarddeny-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.eventCategories).not.toContain("guard");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      life: 963,
    });
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ actorId: "p1", source: "imported", stateType: "A", moveType: "H" })]),
    );
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "NoAirGuard defender AssertSpecial before guardable HitDef",
        allowSameTick: true,
        steps: [
          { actorId: "p1", stateNo: 40, controller: "AssertSpecial", name: "AssertSpecial Control Flags" },
          { actorId: "p2", stateNo: 200, controller: "HitDef" },
        ],
      },
    ]);
    expect(artifact.trace.events.some((event) => event.category === "guard")).toBe(false);
  });

  it("creates a synthetic imported AssertSpecial lifetime artifact where expired NoStandGuard allows later guard", () => {
    const artifact = createSyntheticImportedAssertSpecialLifetimeTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-lifetime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-lifetime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.eventCategories).not.toContain("hit");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.combatReasons).not.toContain("hit");
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      life: 995,
    });
    expect(artifact.trace.events.some((event) => event.category === "guard")).toBe(true);
    expect(artifact.trace.events.some((event) => event.category === "hit")).toBe(false);
  });

  it("creates a synthetic imported AssertSpecial control artifact with facing, walk, and invisible evidence", () => {
    const artifact = createSyntheticImportedAssertSpecialControlTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-control-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-control-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["assertspecial"]);
    const actorFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p1" && actor.source === "imported" && actor.animNo === 0);
    expect(actorFrame).toMatchObject({
      animNo: 0,
      facing: 1,
      maxOpacity: 0,
      minOpacity: 0,
    });
    expect(actorFrame?.minVel.x).toBe(0);
    expect(actorFrame?.maxVel.x).toBe(0);
    expect(actorFrame?.minPos.x).toBe(30);
    expect(actorFrame?.maxPos.x).toBe(30);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 0,
      ctrl: true,
    });
  });

  it("creates a synthetic imported AssertSpecial global telemetry artifact", () => {
    const artifact = createSyntheticImportedAssertSpecialGlobalTelemetryTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });
    const requiredGlobalFlags = ["nobardisplay", "nobg", "nofg", "nokosnd", "nomusic"];

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-global-telemetry-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-global-telemetry-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 0,
        ctrl: true,
        assertSpecialGlobalFlags: requiredGlobalFlags,
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 0,
      ctrl: true,
      assertSpecialGlobalFlags: requiredGlobalFlags,
    });
  });

  it("creates a synthetic imported AssertSpecial round-flow telemetry artifact", () => {
    const artifact = createSyntheticImportedAssertSpecialRoundFlowTelemetryTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });
    const requiredGlobalFlags = ["intro", "nokoslow"];

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-round-flow-telemetry-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-round-flow-telemetry-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 0,
        ctrl: true,
        assertSpecialGlobalFlags: requiredGlobalFlags,
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 0,
      ctrl: true,
      assertSpecialGlobalFlags: requiredGlobalFlags,
    });
  });

  it("creates a synthetic imported AssertSpecial NoJuggleCheck telemetry artifact", () => {
    const artifact = createSyntheticImportedAssertSpecialJuggleTelemetryTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const requiredFlags = ["nojugglecheck"];

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-juggle-telemetry-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-juggle-telemetry-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 0,
        ctrl: true,
        assertSpecialFlags: requiredFlags,
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 0,
      ctrl: true,
      assertSpecialFlags: requiredFlags,
    });
  });

  it("creates a synthetic imported AssertSpecial shadow telemetry artifact", () => {
    const artifact = createSyntheticImportedAssertSpecialShadowTelemetryTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-shadow-telemetry-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-shadow-telemetry-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 0, shadowVisible: false, minFrames: 1 },
      { actorId: "p2", actorKind: "player", shadowVisible: false, minFrames: 1 },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", actorKind: "player", shadowVisible: false }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", shadowVisible: false }),
      ]),
    );
    expect(gate?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 0,
        ctrl: true,
        assertSpecialFlags: ["noshadow"],
        assertSpecialGlobalFlags: ["globalnoshadow"],
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 0,
      ctrl: true,
      assertSpecialFlags: ["noshadow"],
      assertSpecialGlobalFlags: ["globalnoshadow"],
    });
  });

  it("creates a synthetic imported AssertSpecial helper/explod shadow artifact", () => {
    const artifact = createSyntheticImportedAssertSpecialHelperExplodShadowTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-helper-explod-shadow-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-helper-explod-shadow-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "explod"]));
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      { actorId: "p1", source: "imported", actorKind: "player", stateNo: 200, shadowVisible: false, minFrames: 1 },
      { actorId: "p2", actorKind: "player", shadowVisible: false, minFrames: 1 },
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        stateNo: 1205,
        animNo: 925,
        shadowVisible: false,
        minFrames: 1,
      },
      { source: "effect", actorKind: "explod", ownerId: "p1", animNo: 939, shadowVisible: false, minFrames: 1 },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", actorKind: "player", stateNo: 200, shadowVisible: false }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", shadowVisible: false }),
        expect.objectContaining({ source: "effect", actorKind: "helper", stateNo: 1205, animNo: 925, shadowVisible: false }),
        expect.objectContaining({ source: "effect", actorKind: "explod", animNo: 939, shadowVisible: false }),
      ]),
    );
    expect(gate?.requirements.requiredWorldLifecycleEvents).toEqual([
      { type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "spawn", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
      { type: "active", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 200,
      assertSpecialGlobalFlags: ["globalnoshadow"],
    });
  });

  it("creates a synthetic imported AssertSpecial TimerFreeze artifact with frozen round timer evidence", () => {
    const artifact = createSyntheticImportedAssertSpecialTimerFreezeTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-timerfreeze-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-timerfreeze-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredRoundFrames).toEqual([
      { state: "fight", message: "Fight", minFrames: 60, observedTimerAtLeast: 2 },
    ]);
    expect(evidence?.roundFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ state: "fight", message: "Fight", minTimer: 2, maxTimer: 2 }),
      ]),
    );
    expect(artifact.trace.frames.some((frame) => frame.round?.state === "timeover")).toBe(false);
  });

  it("creates a synthetic imported AssertSpecial RoundNotOver artifact with fight-state timer-zero evidence", () => {
    const artifact = createSyntheticImportedAssertSpecialRoundNotOverTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-roundnotover-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-roundnotover-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredRoundFrames).toEqual([
      { state: "fight", message: "Fight", minFrames: 60, observedTimerAtMost: 0 },
    ]);
    expect(evidence?.roundFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ state: "fight", message: "Fight", minTimer: 0 }),
      ]),
    );
    expect(artifact.trace.frames.some((frame) => frame.round?.state === "timeover")).toBe(false);
    expect(artifact.trace.frames.some((frame) => frame.round?.state === "fight" && frame.round.timer === 0)).toBe(true);
  });

  it("creates a synthetic imported AssertSpecial NoKO artifact with nonlethal hit evidence", () => {
    const artifact = createSyntheticImportedAssertSpecialNoKoTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-noko-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-noko-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventLines.some((line) => line.includes("hit Synthetic Imported AssertSpecial NoKO Defender for 2000"))).toBe(
      true,
    );
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "NoKO defender AssertSpecial before lethal HitDef",
        allowSameTick: true,
        steps: [
          { actorId: "p2", stateNo: 0, controller: "AssertSpecial", name: "Passive AssertSpecial" },
          { actorId: "p1", stateNo: 200, controller: "HitDef" },
        ],
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")?.life).toBe(1);
  });

  it("creates a synthetic imported default Common1 guard-state artifact", () => {
    const artifact = createSyntheticImportedDefaultGuardStateTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-guard-state-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-default-guard-state-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 150, 151, 200]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.controllerEvents.map((event) => event.controller)).toEqual(
      expect.arrayContaining(["ChangeAnim", "ChangeState", "HitVelSet"]),
    );
    expect(evidence?.controllerEvents.map((event) => event.operation).filter(Boolean)).toEqual(
      expect.arrayContaining(["kinematic:hitvelset"]),
    );
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      moveType: "I",
      ctrl: true,
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      guardSlideTime: 5,
      guardControlTime: 7,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "150/151 named guard-hit controller and typed operation order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 150, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 150, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 151, controller: "HitVelSet", name: "Apply Guard Velocity" },
          { stateNo: 151, operation: "kinematic:hitvelset" },
          { stateNo: 151, controller: "CtrlSet", name: "Regain Guard Control" },
          { stateNo: 151, operation: "resource:ctrlset" },
          { stateNo: 151, controller: "ChangeState", name: "Guard Hit Over" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticStandGuardHitPhysicsFrames());
  });

  it("creates a synthetic imported p2stateno guard-ignored artifact with Common1 guard evidence", () => {
    const artifact = createSyntheticImportedP2StateNoGuardIgnoredTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-p2stateno-guard-ignored-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-p2stateno-guard-ignored-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 150, 151, 200]));
    expect(evidence?.executedStates).not.toContain(888);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 150, 151, 130]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([888]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([defaultStandGuardSlideStopControllerSequence()]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([syntheticStandGuardHoldWalkReturnActorFrameSequence()]);
    expect(evidence?.actorFrames).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 20,
      animNo: 20,
      life: 995,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported default Common1 guard slide-stop artifact", () => {
    const artifact = createSyntheticImportedDefaultGuardSlideStopTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-guard-slide-stop-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-default-guard-slide-stop-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 150, 151, 200]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      syntheticStandGuardHitPhysicsFrames()[0],
      {
        ...syntheticStandGuardHitPhysicsFrames()[1],
        observedVelXAtLeast: 2,
        observedVelXAtMost: 0,
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "150/151 guard slide-stop and control order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 150, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 150, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 151, controller: "HitVelSet", name: "Apply Guard Velocity" },
          { stateNo: 151, operation: "kinematic:hitvelset" },
          { stateNo: 151, controller: "VelSet", name: "Stop Guard Slide" },
          { stateNo: 151, operation: "kinematic:velset" },
          { stateNo: 151, controller: "CtrlSet", name: "Regain Guard Control" },
          { stateNo: 151, operation: "resource:ctrlset" },
          { stateNo: 151, controller: "ChangeState", name: "Guard Hit Over" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported default Common1 guard-hold walk-control artifact", () => {
    const artifact = createSyntheticImportedDefaultGuardHoldWalkReturnTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-guard-hold-walk-return-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-default-guard-hold-walk-return-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 150, 151, 200]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 20,
      animNo: 20,
      source: "imported",
      actorKind: "player",
      life: 995,
      ctrl: true,
      stateType: "S",
      moveType: "I",
      physics: "S",
    });
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      syntheticStandGuardHoldWalkReturnActorFrameSequence(),
    ]);
    expect(syntheticStandGuardHoldWalkReturnActorFrameSequence()).toEqual({
      label: "Synthetic stand guard hold returns to walking control",
      allowSameTick: true,
      steps: [
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 150,
          animNo: 150,
          stateType: "S",
          moveType: "H",
          physics: "N",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 151,
          animNo: 150,
          stateType: "S",
          moveType: "H",
          physics: "S",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 130,
          animNo: 130,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 20,
          animNo: 20,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minFrames: 20,
          observedVelXAtLeast: 1,
        },
      ],
    });
  });

  it("creates a synthetic imported crouch Common1 guard slide-stop artifact", () => {
    const artifact = createSyntheticImportedCrouchGuardSlideStopTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-crouch-guard-slide-stop-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-crouch-guard-slide-stop-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holddown", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 152, 153, 200]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      syntheticCrouchGuardHitPhysicsFrames()[0],
      {
        ...syntheticCrouchGuardHitPhysicsFrames()[1],
        observedVelXAtLeast: 2,
        observedVelXAtMost: 0,
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultCrouchGuardSlideStopControllerSequence(),
    ]);
  });

  it("creates a synthetic imported crouch Common1 guard-hold return artifact", () => {
    const artifact = createSyntheticImportedCrouchGuardHoldCrouchReturnTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-crouch-guard-hold-crouch-return-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-crouch-guard-hold-crouch-return-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "holddown", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 152, 153, 200]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 10,
          animNo: 10,
          stateType: "C",
          moveType: "I",
          physics: "C",
        }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      actorKind: "player",
      stateNo: 10,
      animNo: 10,
      life: 995,
      ctrl: true,
      stateType: "C",
      moveType: "I",
      physics: "C",
    });
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      syntheticCrouchGuardHitPhysicsFrames()[0],
      {
        ...syntheticCrouchGuardHitPhysicsFrames()[1],
        observedVelXAtLeast: 2,
        observedVelXAtMost: 0,
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultCrouchGuardSlideStopControllerSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      syntheticCrouchGuardHoldCrouchReturnActorFrameSequence(),
    ]);
    expect(syntheticCrouchGuardHoldCrouchReturnActorFrameSequence()).toEqual({
      label: "Synthetic crouch guard hold returns to crouch control",
      allowSameTick: true,
      steps: [
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 152,
          animNo: 10,
          stateType: "C",
          moveType: "H",
          physics: "N",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 153,
          animNo: 150,
          stateType: "C",
          moveType: "H",
          physics: "C",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 130,
          animNo: 130,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 10,
          animNo: 10,
          stateType: "C",
          moveType: "I",
          physics: "C",
          minFrames: 20,
        },
      ],
    });
  });

  it("creates a synthetic imported GetHitVar guard timing artifact with branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarGuardTimingTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-guard-timing-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-guard-timing-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 308]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 308, animNo: 308 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      syntheticStandGuardHitPhysicsFrames()[0],
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 151,
        animNo: 150,
        stateType: "S",
        moveType: "H",
        physics: "S",
        minFrames: 2,
        observedPosYAtLeast: 0,
        observedPosYAtMost: 0,
        bodyWidthFront: 39,
        bodyWidthBack: 39,
        playerPush: true,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 308,
        animNo: 308,
        stateType: "S",
        moveType: "I",
        physics: "S",
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar guard kill artifact with branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarGuardKillTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-guard-kill-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 330]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 330, animNo: 330 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "150/151 guard GetHitVar(kill) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 150, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 150, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 151, controller: "HitVelSet", name: "Apply Guard Velocity" },
          { stateNo: 151, operation: "kinematic:hitvelset" },
          { stateNo: 151, controller: "ChangeState", name: "Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar crouch guard kill artifact with branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarCrouchGuardKillTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-crouch-guard-kill-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-crouch-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holddown", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([152, 153, 331]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 331, animNo: 331 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "152/153 crouch guard GetHitVar(kill) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 152, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 152, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 153, controller: "HitVelSet", name: "Apply Crouch Guard Velocity" },
          { stateNo: 153, operation: "kinematic:hitvelset" },
          { stateNo: 153, controller: "ChangeState", name: "Crouch Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar air guard kill artifact with branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarAirGuardKillTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-air-guard-kill-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-air-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([154, 155, 332]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 332, animNo: 332 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "154/155 air guard GetHitVar(kill) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 154, controller: "ChangeAnim", name: "Air Guard Shake Anim" },
          { stateNo: 154, controller: "ChangeState", name: "Air Guard Shake Over" },
          { stateNo: 155, controller: "HitVelSet", name: "Apply Air Guard Velocity" },
          { stateNo: 155, operation: "kinematic:hitvelset" },
          { stateNo: 155, controller: "VelAdd", name: "Apply Air Guard Gravity" },
          { stateNo: 155, controller: "ChangeState", name: "Air Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar guard hitshaketime artifact with branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarGuardHitShakeTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-guard-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-guard-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 311]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 311, animNo: 311 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "150/151 guard GetHitVar(hitshaketime) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 150, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 150, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 151, controller: "HitVelSet", name: "Apply Guard Velocity" },
          { stateNo: 151, operation: "kinematic:hitvelset" },
          { stateNo: 151, controller: "ChangeState", name: "Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar crouch guard hitshaketime artifact with branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarCrouchGuardHitShakeTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-crouch-guard-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-crouch-guard-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holddown", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([152, 153, 314]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 314, animNo: 314 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "152/153 crouch guard GetHitVar(hitshaketime) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 152, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 152, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 153, controller: "HitVelSet", name: "Apply Crouch Guard Velocity" },
          { stateNo: 153, operation: "kinematic:hitvelset" },
          { stateNo: 153, controller: "ChangeState", name: "Crouch Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar air guard hitshaketime artifact with branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarAirGuardHitShakeTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-air-guard-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-air-guard-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([154, 155, 315]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 315, animNo: 315 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "154/155 air guard GetHitVar(hitshaketime) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 154, controller: "ChangeAnim", name: "Air Guard Shake Anim" },
          { stateNo: 154, controller: "ChangeState", name: "Air Guard Shake Over" },
          { stateNo: 155, controller: "HitVelSet", name: "Apply Air Guard Velocity" },
          { stateNo: 155, operation: "kinematic:hitvelset" },
          { stateNo: 155, controller: "VelAdd", name: "Apply Air Guard Gravity" },
          { stateNo: 155, controller: "ChangeState", name: "Air Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a reusable imported default guard-state artifact for imported fighters", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "custom-imported-default-guard-state",
      displayName: "Custom Imported Default Guard State",
      defaultGuardHit: { shakeStateNo: 150, slideStateNo: 151, guardStateNo: 130 },
    });

    const artifact = createImportedDefaultGuardStateTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "custom-imported-default-guard-state-golden",
      targetLabel: "Custom Imported Default Guard State",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "custom-imported-default-guard-state-golden",
        source: "imported",
      },
      gates: [
        {
          label: "custom-imported-default-guard-state-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(artifact.gates[0]?.evidence.executedStates).toEqual(expect.arrayContaining([150, 151]));
    expect(artifact.gates[0]?.evidence.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.evidence.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
  });

  it("keeps guard-hit actor-frame sequence requirements configurable for fixture mirrors", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-guard-hold-return-options",
      displayName: "Synthetic Imported Default Guard Hold Return Options",
      defaultGuardHit: { shakeStateNo: 150, slideStateNo: 151, guardStateNo: 130 },
    });
    const sequence = {
      label: "Synthetic stand guard returns to hold",
      allowSameTick: true,
      steps: [
        {
          actorId: "p2",
          source: "imported" as const,
          actorKind: "player" as const,
          stateNo: 150,
          moveType: "H" as const,
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported" as const,
          actorKind: "player" as const,
          stateNo: 151,
          moveType: "H" as const,
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported" as const,
          actorKind: "player" as const,
          stateNo: 130,
          stateType: "S" as const,
          minFrames: 1,
        },
      ],
    };
    const artifact = createImportedDefaultGuardStateTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-guard-hold-return-options-golden",
      targetLabel: "Synthetic imported default guard hold return options",
      requiredExecutedStates: [200, 150, 151, 130],
      requiredActorFrameSequences: [sequence],
    });

    expect(artifact.gates[0]?.failures).toEqual([]);
    expect(artifact.status).toBe("passed");
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([sequence]);
    expect(artifact.gates[0]?.evidence.executedStates).toEqual(expect.arrayContaining([130, 150, 151, 200]));
  });

  it("exports official KFM guard controller-order requirements for optional fixture QA", () => {
    expect(officialKfmStandGuardHitControllerSequence()).toMatchObject({
      label: "Official KFM 150/151 guard-hit controller order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 150, controller: "ChangeAnim" },
        { stateNo: 150, controller: "ChangeState" },
        { stateNo: 151, controller: "HitVelSet" },
        { stateNo: 151, operation: "kinematic:hitvelset" },
        { stateNo: 151, controller: "ChangeState" },
      ],
    });
    expect(officialKfmStandGuardSlideStopControllerSequence()).toMatchObject({
      label: "Official KFM 150/151 guard slide-stop and HitOver order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 150, controller: "ChangeAnim" },
        { stateNo: 150, controller: "ChangeState" },
        { stateNo: 151, controller: "HitVelSet" },
        { stateNo: 151, operation: "kinematic:hitvelset" },
        { stateNo: 151, controller: "VelSet" },
        { stateNo: 151, operation: "kinematic:velset" },
        { stateNo: 151, controller: "ChangeState" },
      ],
    });
    expect(officialKfmStandGuardHoldReturnActorFrameSequence()).toEqual({
      label: "Official KFM stand guard hit returns to guard hold",
      allowSameTick: true,
      steps: [
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 150,
          animNo: 150,
          stateType: "S",
          moveType: "H",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 151,
          animNo: 150,
          stateType: "S",
          moveType: "H",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 130,
          stateType: "S",
          minFrames: 1,
        },
      ],
    });
    expect(officialKfmStandGuardHoldWalkReturnActorFrameSequence()).toEqual({
      label: "Official KFM stand guard hold returns to walking control",
      allowSameTick: true,
      steps: [
        ...officialKfmStandGuardHoldReturnActorFrameSequence().steps,
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 20,
          animNo: 20,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minFrames: 20,
        },
      ],
    });
    expect(officialKfmCrouchGuardHitControllerSequence().steps).toEqual([
      { stateNo: 152, controller: "ChangeAnim" },
      { stateNo: 152, controller: "ChangeState" },
      { stateNo: 153, controller: "HitVelSet" },
      { stateNo: 153, operation: "kinematic:hitvelset" },
      { stateNo: 153, controller: "ChangeState" },
    ]);
    expect(officialKfmCrouchGuardSlideStopControllerSequence().steps).toEqual([
      { stateNo: 152, controller: "ChangeAnim" },
      { stateNo: 152, controller: "ChangeState" },
      { stateNo: 153, controller: "HitVelSet" },
      { stateNo: 153, operation: "kinematic:hitvelset" },
      { stateNo: 153, controller: "VelSet" },
      { stateNo: 153, operation: "kinematic:velset" },
      { stateNo: 153, controller: "ChangeState" },
    ]);
    expect(officialKfmCrouchGuardHoldCrouchReturnActorFrameSequence()).toEqual({
      label: "Official KFM crouch guard hold returns to crouch control",
      allowSameTick: true,
      steps: [
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 152,
          animNo: 152,
          stateType: "C",
          moveType: "H",
          physics: "N",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 153,
          animNo: 151,
          stateType: "C",
          moveType: "H",
          physics: "C",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 131,
          animNo: 151,
          stateType: "C",
          moveType: "H",
          physics: "C",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 11,
          animNo: 11,
          stateType: "C",
          moveType: "I",
          physics: "C",
          minFrames: 20,
        },
      ],
    });
    expect(officialKfmAirGuardHitControllerSequence().steps).toEqual([
      { stateNo: 154, controller: "ChangeAnim" },
      { stateNo: 154, controller: "ChangeState" },
      { stateNo: 155, controller: "HitVelSet" },
      { stateNo: 155, operation: "kinematic:hitvelset" },
      { stateNo: 155, controller: "VelAdd" },
      { stateNo: 155, controller: "CtrlSet" },
      { stateNo: 155, operation: "resource:ctrlset" },
      { stateNo: 155, controller: "VelSet" },
      { stateNo: 155, operation: "kinematic:velset" },
      { stateNo: 155, controller: "PosSet" },
      { stateNo: 155, operation: "kinematic:posset" },
      { stateNo: 155, controller: "ChangeState" },
      { stateNo: 52, controller: "ChangeState" },
    ]);
    expect(officialKfmAirGuardLandingWalkReturnActorFrameSequence()).toEqual({
      label: "Official KFM air guard landing returns to walk control",
      allowSameTick: true,
      steps: [
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 154,
          animNo: 132,
          stateType: "A",
          moveType: "H",
          physics: "N",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 155,
          animNo: 152,
          stateType: "A",
          moveType: "H",
          physics: "N",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 52,
          animNo: 47,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minFrames: 1,
        },
        {
          actorId: "p2",
          source: "imported",
          actorKind: "player",
          stateNo: 20,
          animNo: 20,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minFrames: 5,
        },
      ],
    });
  });

  it("exports official KFM auto guard-start and guard-end controller-order requirements", () => {
    expect(officialKfmAutoGuardStartControllerSequence()).toMatchObject({
      label: "Official KFM 120/130 auto guard-start controller and typed operation order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 120, controller: "ChangeAnim" },
        { stateNo: 120, controller: "StateTypeSet" },
        { stateNo: 120, operation: "metadata:statetypeset" },
        { stateNo: 120, controller: "ChangeState" },
        { stateNo: 130, controller: "ChangeAnim" },
      ],
    });
    expect(officialKfmAutoGuardEndControllerSequence().steps).toEqual([
      { stateNo: 120, controller: "ChangeAnim" },
      { stateNo: 120, controller: "StateTypeSet" },
      { stateNo: 120, operation: "metadata:statetypeset" },
      { stateNo: 120, controller: "ChangeState" },
      { stateNo: 130, controller: "ChangeAnim" },
      { stateNo: 130, controller: "ChangeState" },
      { stateNo: 0, controller: "VelSet" },
      { stateNo: 0, operation: "kinematic:velset" },
    ]);
  });

  it("exports synthetic auto guard-start and guard-end controller-order requirements", () => {
    expect(syntheticAutoGuardStartControllerSequence()).toEqual({
      label: "Synthetic 120/130 auto guard-start controller order",
      actorId: "p2",
      allowSameTick: true,
      steps: [{ stateNo: 120, controller: "ChangeState", name: "Guard Start Done" }],
    });
    expect(syntheticAutoGuardEndControllerSequence()).toEqual({
      label: "Synthetic 120/130/140 auto guard-end controller order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 120, controller: "ChangeState", name: "Guard Start Done" },
        { stateNo: 130, controller: "ChangeState", name: "Stop Guarding" },
      ],
    });
  });

  it("exports official KFM ground-recovery controller and actor-frame requirements", () => {
    expect(officialKfmGroundRecoveryControllerSequence()).toEqual({
      label: "Official KFM 5050/5200/52 ground-recovery controller and typed operation order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 5050, controller: "VelAdd" },
        { stateNo: 5050, controller: "ChangeState" },
        { stateNo: 5200, controller: "VelAdd" },
        { stateNo: 5200, controller: "SelfState" },
        { stateNo: 52, controller: "VelSet" },
        { stateNo: 52, operation: "kinematic:velset" },
        { stateNo: 52, controller: "PosSet" },
        { stateNo: 52, operation: "kinematic:posset" },
        { stateNo: 52, controller: "ChangeState" },
      ],
    });
    expect(officialKfmGroundRecoveryActorFrameSequence()).toEqual({
      label: "Official KFM 5050/5200/52 ground-recovery actor-frame order",
      steps: [
        expect.objectContaining({
          actorId: "p2",
          stateNo: 5050,
          animNo: 5035,
          stateType: "A",
          moveType: "H",
          physics: "N",
          observedPosYAtMost: -6,
          observedVelXAtLeast: 1,
          observedVelYAtLeast: 5,
          minFrames: 6,
        }),
        expect.objectContaining({
          actorId: "p2",
          stateNo: 5200,
          animNo: 5035,
          stateType: "A",
          moveType: "H",
          physics: "N",
          observedPosYAtMost: 0,
          observedVelXAtLeast: 1,
          observedVelYAtLeast: 7,
          minFrames: 2,
        }),
        expect.objectContaining({
          actorId: "p2",
          stateNo: 52,
          animNo: 47,
          stateType: "S",
          moveType: "I",
          physics: "S",
          observedPosYAtLeast: 0,
          observedPosYAtMost: 0,
          observedVelXAtLeast: 0,
          observedVelXAtMost: 0,
          observedVelYAtLeast: 0,
          observedVelYAtMost: 0,
          minFrames: 1,
        }),
      ],
    });
  });

  it("exports official KFM air-recovery controller and actor-frame requirements", () => {
    expect(officialKfmAirRecoveryControllerSequence()).toEqual({
      label: "Official KFM 5050/5210/52 air-recovery controller and typed operation order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 5050, controller: "VelAdd" },
        { stateNo: 5050, controller: "ChangeState" },
        { stateNo: 5210, controller: "PalFX" },
        { stateNo: 5210, operation: "sprite-effect:palfx" },
        { stateNo: 5210, controller: "PosFreeze" },
        { stateNo: 5210, operation: "bounds:posfreeze" },
        { stateNo: 5210, controller: "Turn" },
        { stateNo: 5210, operation: "orientation:turn" },
        { stateNo: 5210, controller: "NotHitBy" },
        { stateNo: 5210, operation: "eligibility:nothitby" },
        { stateNo: 5210, controller: "VelMul" },
        { stateNo: 5210, controller: "VelAdd" },
        { stateNo: 5210, controller: "ChangeState" },
        { stateNo: 52, controller: "VarSet" },
        { stateNo: 0, controller: "VelSet" },
        { stateNo: 0, operation: "kinematic:velset" },
      ],
    });
    expect(officialKfmAirRecoveryActorFrameSequence()).toEqual({
      label: "Official KFM 5050/5210/52 air-recovery actor-frame order",
      steps: [
        expect.objectContaining({
          actorId: "p2",
          stateNo: 5050,
          animNo: 5035,
          stateType: "A",
          moveType: "H",
          physics: "N",
          minFrames: 1,
        }),
        expect.objectContaining({
          actorId: "p2",
          stateNo: 5210,
          animNo: 5210,
          stateType: "A",
          moveType: "I",
          physics: "N",
          observedVelXAtLeast: 0.6,
          observedVelYAtMost: -3,
          minFrames: 8,
        }),
        expect.objectContaining({
          actorId: "p2",
          stateNo: 52,
          animNo: 47,
          stateType: "S",
          moveType: "I",
          physics: "S",
          minFrames: 1,
        }),
      ],
    });
  });

  it("exports official KFM guard-hit actor-frame physics requirements", () => {
    expect(officialKfmStandGuardHitPhysicsFrames()).toEqual([
      expect.objectContaining({ actorId: "p2", stateNo: 150, animNo: 150, stateType: "S", moveType: "H", physics: "N", minFrames: 5 }),
      expect.objectContaining({ actorId: "p2", stateNo: 151, animNo: 150, stateType: "S", moveType: "H", physics: "S", minFrames: 8 }),
    ]);
    expect(officialKfmCrouchGuardHitPhysicsFrames()).toEqual([
      expect.objectContaining({ actorId: "p2", stateNo: 152, animNo: 152, stateType: "C", moveType: "H", physics: "N", minFrames: 5 }),
      expect.objectContaining({ actorId: "p2", stateNo: 153, animNo: 151, stateType: "C", moveType: "H", physics: "C", minFrames: 8 }),
    ]);
    expect(officialKfmAirGuardHitPhysicsFrames()).toEqual([
      expect.objectContaining({ actorId: "p2", stateNo: 154, animNo: 132, stateType: "A", moveType: "H", physics: "N", minFrames: 5 }),
      expect.objectContaining({
        actorId: "p2",
        stateNo: 155,
        animNo: 152,
        stateType: "A",
        moveType: "H",
        physics: "N",
        minFrames: 10,
        observedVelXAtLeast: 2,
        observedVelYAtLeast: 8,
      }),
      expect.objectContaining({ actorId: "p2", stateNo: 52, animNo: 47, stateType: "S", physics: "S", minFrames: 1 }),
    ]);
  });

  it("exports synthetic guard-hit actor-frame physics requirements", () => {
    expect(syntheticStandGuardHitPhysicsFrames()).toEqual([
      expect.objectContaining({ actorId: "p2", stateNo: 150, animNo: 150, stateType: "S", moveType: "H", physics: "N", minFrames: 5 }),
      expect.objectContaining({
        actorId: "p2",
        stateNo: 151,
        animNo: 150,
        stateType: "S",
        moveType: "H",
        physics: "S",
        minFrames: 8,
        observedVelXAtLeast: 2,
      }),
    ]);
    expect(syntheticCrouchGuardHitPhysicsFrames()).toEqual([
      expect.objectContaining({ actorId: "p2", stateNo: 152, animNo: 10, stateType: "C", moveType: "H", physics: "N", minFrames: 5 }),
      expect.objectContaining({ actorId: "p2", stateNo: 153, animNo: 150, stateType: "C", moveType: "H", physics: "C", minFrames: 8 }),
    ]);
    expect(syntheticAirGuardHitPhysicsFrames()).toEqual([
      expect.objectContaining({ actorId: "p2", stateNo: 154, animNo: 40, stateType: "A", moveType: "H", physics: "N", minFrames: 5 }),
      expect.objectContaining({
        actorId: "p2",
        stateNo: 155,
        animNo: 150,
        stateType: "A",
        moveType: "H",
        physics: "N",
        minFrames: 8,
        observedVelXAtLeast: 2,
        observedVelYAtLeast: 6,
      }),
    ]);
  });

  it("creates a synthetic imported crouch guard-state artifact from command expressions", () => {
    const artifact = createSyntheticImportedCrouchGuardStateTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-crouch-guard-state-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-crouch-guard-state-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holddown", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([152, 153, 200]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.controllerEvents.map((event) => event.controller)).toEqual(
      expect.arrayContaining(["ChangeAnim", "ChangeState", "HitVelSet"]),
    );
    expect(evidence?.controllerEvents.map((event) => event.operation).filter(Boolean)).toEqual(expect.arrayContaining(["kinematic:hitvelset"]));
    expect(evidence?.eventCategories).toContain("guard");
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "152/153 named crouch guard-hit controller order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 152, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 152, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 153, controller: "HitVelSet", name: "Apply Crouch Guard Velocity" },
          { stateNo: 153, operation: "kinematic:hitvelset" },
          { stateNo: 153, controller: "ChangeState", name: "Crouch Guard Hit Over" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticCrouchGuardHitPhysicsFrames());
  });

  it("creates a synthetic imported atomic DB crouch guard-state artifact", () => {
    const artifact = createSyntheticImportedDiagonalCrouchGuardStateTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-diagonal-crouch-guard-state-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-diagonal-crouch-guard-state-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "holddown", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([152, 153, 200]));
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "152/153 named crouch guard-hit controller order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 152, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 152, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 153, controller: "HitVelSet", name: "Apply Crouch Guard Velocity" },
          { stateNo: 153, operation: "kinematic:hitvelset" },
          { stateNo: 153, controller: "ChangeState", name: "Crouch Guard Hit Over" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticCrouchGuardHitPhysicsFrames());
  });

  it("creates a synthetic imported air guard-state artifact", () => {
    const artifact = createSyntheticImportedAirGuardStateTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-guard-state-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-air-guard-state-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(
      expect.arrayContaining(["ChangeState", "CtrlSet", "HitDef", "HitVelSet", "VelAdd"]),
    );
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      expect.objectContaining({
        actorId: "p2",
        source: "imported",
        life: 995,
        ctrl: true,
        stateType: "S",
        moveType: "I",
        physics: "S",
      }),
    ]);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([154, 155, 200]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.controllerEvents.map((event) => event.controller)).toEqual(
      expect.arrayContaining(["ChangeAnim", "ChangeState", "HitVelSet", "VelAdd", "CtrlSet"]),
    );
    expect(evidence?.controllerEvents.map((event) => event.operation).filter(Boolean)).toEqual(
      expect.arrayContaining(["kinematic:hitvelset", "resource:ctrlset"]),
    );
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "154/155 named air guard-hit controller and typed operation order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 154, controller: "ChangeAnim", name: "Air Guard Shake Anim" },
          { stateNo: 154, controller: "ChangeState", name: "Air Guard Shake Over" },
          { stateNo: 155, controller: "HitVelSet", name: "Apply Air Guard Velocity" },
          { stateNo: 155, operation: "kinematic:hitvelset" },
          { stateNo: 155, controller: "VelAdd", name: "Apply Air Guard Gravity" },
          { stateNo: 155, controller: "CtrlSet", name: "Regain Air Guard Control" },
          { stateNo: 155, operation: "resource:ctrlset" },
          { stateNo: 155, controller: "ChangeState", name: "Air Guard Hit Over" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticAirGuardHitPhysicsFrames());
  });

  it("creates a synthetic imported airguard.velocity direct HitDef artifact", () => {
    const artifact = createSyntheticImportedAirGuardVelocityTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-guard-velocity-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-air-guard-velocity-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(8);
    expect(airGuardFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticAirGuardVelocityPhysicsFrames());
  });

  it("creates a synthetic imported default airguard.velocity direct HitDef artifact", () => {
    const artifact = createSyntheticImportedAirGuardVelocityDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-guard-velocity-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-air-guard-velocity-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(9);
    expect(airGuardFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticAirGuardVelocityPhysicsFrames(9, -3.5));
  });

  it("creates a synthetic imported airguard.cornerpush.veloff direct HitDef artifact", () => {
    const artifact = createSyntheticImportedAirGuardCornerPushTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-guard-cornerpush-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-air-guard-cornerpush-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(8);
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticAirGuardCornerPushPhysicsFrames());
  });

  it("creates a synthetic imported default airguard.cornerpush.veloff direct HitDef artifact", () => {
    const artifact = createSyntheticImportedAirGuardCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-guard-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-air-guard-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(8);
    expect(attackerMinVelX).toBeLessThanOrEqual(-2.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticAirGuardCornerPushPhysicsFrames(-2.5));
  });

  it("creates a synthetic imported default guard.velocity direct HitDef artifact", () => {
    const artifact = createSyntheticImportedGuardVelocityDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-guard-velocity-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-guard-velocity-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const guardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 151);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(guardFrame?.maxVel.x).toBeGreaterThanOrEqual(5.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticStandGuardVelocityPhysicsFrames());
  });

  it("creates a synthetic imported default guard timing direct HitDef artifact", () => {
    const artifact = createSyntheticImportedGuardTimingDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-guard-timing-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-guard-timing-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 331]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 331, animNo: 331 })]),
    );
  });

  it("creates a synthetic imported Projectile default guard.velocity artifact", () => {
    const artifact = createSyntheticImportedProjectileGuardVelocityDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-velocity-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-guard-velocity-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const guardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 151);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(guardFrame?.maxVel.x).toBeGreaterThanOrEqual(5.5);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticStandGuardVelocityPhysicsFrames(5.5, 1),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile default guard timing artifact", () => {
    const artifact = createSyntheticImportedProjectileGuardTimingDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-timing-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-guard-timing-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 332]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 332, animNo: 332 })]),
    );
  });

  it("creates a synthetic imported Projectile guard slide-stop artifact", () => {
    const artifact = createSyntheticImportedProjectileGuardSlideStopTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-slide-stop-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-guard-slide-stop-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const guardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 151);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 130]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(guardFrame?.maxVel.x).toBeGreaterThanOrEqual(2);
    expect(guardFrame?.minVel.x).toBeLessThanOrEqual(0);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(
      expect.arrayContaining(["projectile", "kinematic:hitvelset", "kinematic:velset", "resource:ctrlset"]),
    );
  });

  it("creates a synthetic imported Helper Projectile default guard.velocity artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileGuardVelocityDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-guard-velocity-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-guard-velocity-default-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const guardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 151);
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(guardFrame?.maxVel.x).toBeGreaterThanOrEqual(5.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8857 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8857 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticStandGuardVelocityPhysicsFrames(5.5, 1),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1245, animNo: 983 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 985 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default guard timing artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileGuardTimingDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-guard-timing-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-guard-timing-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 333]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8858 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8858 }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 333, animNo: 333 })]),
    );
  });

  it("creates a synthetic imported Helper Projectile guard slide-stop artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileGuardSlideStopTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-guard-slide-stop-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-guard-slide-stop-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const guardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 151);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([150, 151, 130]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(guardFrame?.maxVel.x).toBeGreaterThanOrEqual(2);
    expect(guardFrame?.minVel.x).toBeLessThanOrEqual(0);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8859 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8859 }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredEffectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ kind: "helper", helperStateNo: 1250 }),
        expect.objectContaining({ kind: "projectile", effectId: 8859, hasHit: true }),
      ]),
    );
  });

  it("creates a synthetic imported default guard.cornerpush.veloff direct HitDef artifact", () => {
    const artifact = createSyntheticImportedGuardCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-guard-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-guard-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    const guardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 151);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(guardFrame?.stateType).toBe("S");
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticStandGuardCornerPushPhysicsFrames());
  });

  it("creates a synthetic imported Projectile default guard.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedProjectileGuardCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-guard-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticStandGuardCornerPushPhysicsFrames(-5.5, 1),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default guard.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileGuardCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-guard-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-guard-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticStandGuardCornerPushPhysicsFrames(-5.5, 1),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
  });

  it("creates a synthetic imported default air.cornerpush.veloff direct HitDef artifact", () => {
    const artifact = createSyntheticImportedAirHitCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-hit-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-air-hit-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    const airHitFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 5020);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(airHitFrame?.stateType).toBe("A");
    expect(attackerMinVelX).toBeLessThanOrEqual(-3.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticAirHitCornerPushPhysicsFrames());
  });

  it("creates a synthetic imported Projectile default air.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedProjectileAirHitCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-air-hit-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-air-hit-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-2.5);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirHitCornerPushPhysicsFrames(-2.5),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default air.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileAirHitCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-air-hit-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-air-hit-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-2.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8857 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8857 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirHitCornerPushPhysicsFrames(-2.5),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1245, animNo: 984 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 983 }),
      ]),
    );
  });

  it("creates a synthetic imported down.cornerpush.veloff direct HitDef artifact", () => {
    const artifact = createSyntheticImportedDownHitCornerPushTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-down-hit-cornerpush-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-down-hit-cornerpush-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    const downSetupFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 200);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(downSetupFrame?.stateType).toBe("L");
    expect(attackerMinVelX).toBeLessThanOrEqual(-6.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticDownHitCornerPushPhysicsFrames());
  });

  it("creates a synthetic imported Projectile down.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedProjectileDownHitCornerPushTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-down-hit-cornerpush-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-down-hit-cornerpush-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-6.5);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticDownHitCornerPushPhysicsFrames(),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile down.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileDownHitCornerPushTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-down-hit-cornerpush-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-down-hit-cornerpush-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-6.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8857 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8857 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticDownHitCornerPushPhysicsFrames(),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1245, animNo: 984 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 983 }),
      ]),
    );
  });

  it("creates a synthetic imported default down.cornerpush.veloff direct HitDef artifact", () => {
    const artifact = createSyntheticImportedDownHitCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-down-hit-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-down-hit-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    const downSetupFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 200);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(downSetupFrame?.stateType).toBe("L");
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticDownHitCornerPushPhysicsFrames(-5.5));
  });

  it("creates a synthetic imported Projectile default down.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedProjectileDownHitCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-down-hit-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-down-hit-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticDownHitCornerPushPhysicsFrames(-5.5),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default down.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileDownHitCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-down-hit-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-down-hit-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.StateTypeSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["metadata:statetypeset"]).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8857 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8857 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticDownHitCornerPushPhysicsFrames(-5.5),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1245, animNo: 984 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 983 }),
      ]),
    );
  });

  it("creates a synthetic imported air guard landing artifact", () => {
    const artifact = createSyntheticImportedAirGuardLandingTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-air-guard-landing-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-air-guard-landing-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([52, 154, 155, 200]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 20,
      animNo: 20,
      ctrl: true,
      stateType: "S",
      moveType: "I",
      physics: "S",
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultAirGuardLandingControllerSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(syntheticAirGuardLandingPhysicsFrames());
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      syntheticAirGuardLandingWalkReturnActorFrameSequence(),
    ]);
    expect(syntheticAirGuardLandingWalkReturnActorFrameSequence()).toEqual({
      label: "Synthetic air guard landing returns to walk control",
      allowSameTick: true,
      steps: [
        expect.objectContaining({ actorId: "p2", stateNo: 154, animNo: 40, stateType: "A", moveType: "H", physics: "N" }),
        expect.objectContaining({ actorId: "p2", stateNo: 155, animNo: 150, stateType: "A", moveType: "H", physics: "N" }),
        expect.objectContaining({ actorId: "p2", stateNo: 52, stateType: "S", moveType: "I", physics: "S" }),
        expect.objectContaining({ actorId: "p2", stateNo: 20, animNo: 20, stateType: "S", moveType: "I", physics: "S" }),
      ],
    });
  });

  it("creates a synthetic imported InGuardDist artifact without contact", () => {
    const artifact = createSyntheticImportedInGuardDistTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-inguarddist-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-inguarddist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 200]));
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.combatReasons).toContain("whiff");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(evidence?.combatReasons).not.toContain("hit");
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 130,
      animNo: 130,
      ctrl: false,
      source: "imported",
    });
  });

  it("creates a synthetic imported InGuardDist far artifact that stays idle", () => {
    const artifact = createSyntheticImportedInGuardDistFarTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-inguarddist-far-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-inguarddist-far-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200]));
    expect(evidence?.executedStates).not.toContain(130);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.combatReasons).toContain("whiff");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(evidence?.combatReasons).not.toContain("hit");
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      source: "imported",
    });
  });

  it("creates a synthetic imported guard.dist ReversalDef no-contact artifact", () => {
    const artifact = createSyntheticImportedGuardDistReversalNoContactTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-guarddist-reversal-no-contact-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-guarddist-reversal-no-contact-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([130, 200]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([777, 888, 5000, 150, 151]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([130, 200]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([777, 888, 5000, 150, 151]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ReversalDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.reversaldef).toBeGreaterThanOrEqual(1);
    expect(evidence?.controllerEvents).toEqual(expect.arrayContaining([expect.objectContaining({ actorId: "p2", stateNo: 0, controller: "ReversalDef" })]));
    expect(evidence?.combatReasons).toContain("whiff");
    expect(evidence?.combatReasons).not.toEqual(expect.arrayContaining(["reversal", "guard", "hit", "override", "reject"]));
    expect(evidence?.eventCategories).not.toEqual(expect.arrayContaining(["reversal", "guard", "hit", "override", "reject"]));
    expect(artifact.trace.events.some((event) => event.category === "reversal")).toBe(false);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      stateNo: 200,
      animNo: 200,
      moveType: "A",
    });
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 130,
      animNo: 130,
      ctrl: false,
      source: "imported",
      life: 1000,
      moveType: "I",
    });
  });

  it("creates a synthetic imported automatic guard-start artifact without contact", () => {
    const artifact = createSyntheticImportedAutoGuardStartTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-auto-guard-start-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-auto-guard-start-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([120, 130, 200]));
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.combatReasons).toContain("whiff");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(evidence?.combatReasons).not.toContain("hit");
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 130,
      animNo: 130,
      ctrl: false,
      source: "imported",
    });
    const startFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 120);
    const holdFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 130);
    expect(startFrame?.lastTick).toBeLessThan(holdFrame?.firstTick ?? 0);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([syntheticAutoGuardStartControllerSequence()]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([syntheticAutoGuardStartActorFrameSequence()]);
  });

  it("creates a synthetic imported automatic guard-end artifact without contact", () => {
    const artifact = createSyntheticImportedAutoGuardEndTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-auto-guard-end-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-auto-guard-end-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([120, 130, 140, 200]));
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(2);
    expect(evidence?.combatReasons).toContain("whiff");
    expect(evidence?.combatReasons).not.toContain("guard");
    expect(evidence?.combatReasons).not.toContain("hit");
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      source: "imported",
    });
    const startFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 120);
    const holdFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 130);
    const endFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 140);
    expect(startFrame?.lastTick).toBeLessThan(holdFrame?.firstTick ?? 0);
    expect(holdFrame?.lastTick).toBeLessThan(endFrame?.firstTick ?? 0);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([syntheticAutoGuardEndControllerSequence()]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([syntheticAutoGuardEndActorFrameSequence()]);
  });

  it("creates a synthetic imported hitstun artifact with target get-hit evidence", () => {
    const artifact = createSyntheticImportedHitstunTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitstun-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-hitstun-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const targetActor = artifact.gates[0]?.evidence.finalActors.find((actor) => actor.id === "p2");
    expect(targetActor).toMatchObject({
      actorKind: "player",
      source: "demo",
      animNo: 500,
      moveType: "H",
    });
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("hit");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported fall artifact with partial hitFall evidence", () => {
    const artifact = createSyntheticImportedFallTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-fall-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-fall-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const targetActor = artifact.gates[0]?.evidence.finalActors.find((actor) => actor.id === "p2");
    expect(targetActor).toMatchObject({
      source: "demo",
      animNo: 500,
      moveType: "H",
      hitFall: {
        falling: true,
        damage: 70,
        velocity: { x: 3, y: -6 },
        recover: false,
        recoverTime: 30,
        envShake: { time: 15, freq: 178, ampl: 6, phase: 0 },
      },
    });
    expect(artifact.trace.finalActors[1]?.hitFall).toMatchObject({
      falling: true,
      damage: 70,
      velocity: { x: 3, y: -6 },
    });
  });

  it("creates a synthetic imported common get-hit artifact with fall controller execution evidence", () => {
    const artifact = createSyntheticImportedCommonGetHitTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-common-gethit-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-common-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallDamage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.FallEnvShake).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallvel"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfalldamage"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.fallenvshake).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5100 named custom get-hit controller and typed operation order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 5100, controller: "HitFallVel", name: "Apply Fall Velocity" },
          { stateNo: 5100, operation: "hitfall:hitfallvel" },
          { stateNo: 5100, controller: "HitFallDamage", name: "Apply Fall Damage" },
          { stateNo: 5100, operation: "hitfall:hitfalldamage" },
          { stateNo: 5100, controller: "HitFallSet", name: "Mark Fall Resolved" },
          { stateNo: 5100, operation: "hitfall:hitfallset" },
          { stateNo: 5100, controller: "FallEnvShake", name: "Fall Camera Shake" },
          { stateNo: 5100, operation: "fallenvshake" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredEnvShakeEvents).toEqual([
      {
        actorId: "p2",
        source: "demo",
        actorKind: "player",
        time: 15,
        freq: 178,
        ampl: 6,
        phase: 0,
        stateNo: 5100,
      },
    ]);
    expect(evidence?.envShakeEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p2",
          source: "demo",
          actorKind: "player",
          time: 15,
          freq: 178,
          ampl: 6,
          phase: 0,
          stateNo: 5100,
        }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      stateNo: 5100,
      hitFall: {
        falling: false,
        damage: 0,
        velocity: { x: 2, y: -7 },
        recover: false,
        recoverTime: 30,
      },
    });
  });

  it("creates a synthetic imported fall.defence_up artifact with scaled HitFallDamage evidence", () => {
    const artifact = createSyntheticImportedFallDefenceUpTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-fall-defence-up-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-fall-defence-up-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100]));
    expect(evidence?.executedControllers.HitFallDamage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfalldamage"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 5100,
      life: 858,
      hitFall: {
        falling: false,
        damage: 0,
      },
    });
  });

  it("creates a synthetic imported GetHitVar fall.defence_up artifact with route evidence", () => {
    const artifact = createSyntheticImportedGetHitVarFallDefenceUpTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-fall-defence-up-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-fall-defence-up-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 286]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 286,
      customOwnerId: "p1",
      hitFall: {
        falling: true,
        damage: 70,
      },
    });
  });

  it("creates a synthetic imported GetHitVar fall.recover artifact with CanRecover distinction evidence", () => {
    const artifact = createSyntheticImportedGetHitVarFallRecoverTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-fall-recover-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-fall-recover-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 301]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 5100)?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 301,
      customOwnerId: "p1",
      hitFall: {
        falling: true,
        damage: 70,
        recover: true,
      },
    });
  });

  it("creates a synthetic imported GetHitVar fall metadata artifact with route evidence", () => {
    const artifact = createSyntheticImportedGetHitVarFallMetadataTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-fall-metadata-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-fall-metadata-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 305]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallvel"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 305,
      customOwnerId: "p1",
      hitFall: {
        falling: true,
        damage: 70,
        kill: false,
        velocity: { x: 3, y: -6 },
      },
    });
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p2",
        actorKind: "player",
        source: "demo",
        stateNo: 305,
        customOwnerId: "p1",
        hitFall: {
          falling: true,
          damage: 70,
          kill: false,
          velocityX: 3,
          velocityY: -6,
        },
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar fallcount artifact with post-impact route evidence", () => {
    const artifact = createSyntheticImportedGetHitVarFallCountTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-fallcount-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-fallcount-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 328]));
    expect(evidence?.executedControllers.HitFallDamage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfalldamage"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 328,
      customOwnerId: "p1",
      hitFall: {
        falling: true,
        damage: 0,
        velocity: { x: 3, y: -6 },
      },
    });
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p2",
        actorKind: "player",
        source: "demo",
        stateNo: 328,
        customOwnerId: "p1",
        hitFall: {
          falling: true,
          damage: 0,
          velocityX: 3,
          velocityY: -6,
        },
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar fall.envshake artifact with route evidence", () => {
    const artifact = createSyntheticImportedGetHitVarFallEnvShakeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-fall-envshake-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-fall-envshake-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 306]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallvel"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 306,
      customOwnerId: "p1",
      hitFall: {
        falling: true,
        damage: 70,
        velocity: { x: 3, y: -6 },
        envShake: {
          time: 15,
          freq: 178,
          ampl: 6,
          phase: 0,
        },
      },
    });
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p2",
        actorKind: "player",
        source: "demo",
        stateNo: 306,
        customOwnerId: "p1",
        hitFall: {
          falling: true,
          damage: 70,
          velocityX: 3,
          velocityY: -6,
          envShakeTime: 15,
          envShakeFreq: 178,
          envShakeAmpl: 6,
          envShakePhase: 0,
        },
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar down.recover artifact with alias route evidence", () => {
    const artifact = createSyntheticImportedGetHitVarDownRecoverTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-down-recover-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-down-recover-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 307]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallvel"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 307,
      customOwnerId: "p1",
      hitFall: {
        falling: true,
        damage: 70,
        velocity: { x: 3, y: -6 },
        downRecover: true,
        downRecoverTime: 45,
      },
    });
    expect(artifact.gates[0]?.requirements.requiredFinalActors).toEqual([
      {
        actorId: "p2",
        actorKind: "player",
        source: "demo",
        stateNo: 307,
        customOwnerId: "p1",
        hitFall: {
          falling: true,
          damage: 70,
          velocityX: 3,
          velocityY: -6,
          downRecover: true,
          downRecoverTime: 45,
        },
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar guarded artifact with defender guard-state branch evidence", () => {
    const artifact = createSyntheticImportedGetHitVarGuardedTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-guarded-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-guarded-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 150, 151, 302]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 302)).toMatchObject({
      animNo: 302,
      source: "imported",
      actorKind: "player",
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      ctrl: true,
    });
  });

  it("creates a synthetic imported Projectile GetHitVar guarded artifact with defender guard-state branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGetHitVarGuardedTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-gethitvar-guarded-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-gethitvar-guarded-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 150, 151, 303]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 303)).toMatchObject({
      animNo: 303,
      source: "imported",
      actorKind: "player",
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Projectile", "HitVelSet", "CtrlSet"]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["projectile", "kinematic:hitvelset", "resource:ctrlset"]);
  });

  it("creates a synthetic imported Projectile GetHitVar hit metadata artifact with normal get-hit branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGetHitVarHitMetadataTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-gethitvar-hit-metadata-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-gethitvar-hit-metadata-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 335]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 335, animNo: 335 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 911, moveType: "I" }),
      ]),
    );
    const shakeFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 5000);
    expect(shakeFrame?.maxVel.x).toBe(4);
    expect(shakeFrame?.minVel.y).toBeLessThanOrEqual(-2);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 335,
      moveType: "H",
    });
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 Projectile normal hit GetHitVar metadata branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported Projectile GetHitVar hitid/chainid artifact with normal get-hit branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGetHitVarHitIdChainIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-gethitvar-hitid-chainid-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-gethitvar-hitid-chainid-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 337]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 337, animNo: 337 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 911, moveType: "I" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1-projectile-0", effect: expect.objectContaining({ id: 77, hasHit: true }) }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 78 })]));
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 337,
      moveType: "H",
    });
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 Projectile normal hit GetHitVar(hitid/chainid) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported Projectile GetHitVar hitcount artifact with normal get-hit branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGetHitVarHitCountTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-gethitvar-hitcount-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-gethitvar-hitcount-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 339]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 339, animNo: 339 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 911, moveType: "I" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1-projectile-0", effect: expect.objectContaining({ id: 77, hasHit: true }) }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 339,
      moveType: "H",
    });
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 Projectile normal hit GetHitVar(hitcount) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported Projectile GetHitVar guard kill artifact with defender branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGetHitVarGuardKillTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-gethitvar-guard-kill-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-gethitvar-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 150, 151, 333]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 333)).toMatchObject({
      animNo: 333,
      source: "imported",
      actorKind: "player",
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "150/151 projectile GetHitVar(kill) branch",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 150, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 150, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 151, controller: "HitVelSet", name: "Apply Guard Velocity" },
          { stateNo: 151, operation: "kinematic:hitvelset" },
          { stateNo: 151, controller: "CtrlSet", name: "Regain Guard Control" },
          { stateNo: 151, operation: "resource:ctrlset" },
          { stateNo: 151, controller: "ChangeState", name: "Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported Projectile GetHitVar guard hitshaketime artifact with defender branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGetHitVarGuardHitShakeTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-gethitvar-guard-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-gethitvar-guard-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 150, 151, 312]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 312)).toMatchObject({
      animNo: 312,
      source: "imported",
      actorKind: "player",
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "150/151 projectile GetHitVar(hitshaketime) branch",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 150, controller: "ChangeAnim", name: "Guard Shake Anim" },
          { stateNo: 150, controller: "ChangeState", name: "Guard Shake Over" },
          { stateNo: 151, controller: "HitVelSet", name: "Apply Guard Velocity" },
          { stateNo: 151, operation: "kinematic:hitvelset" },
          { stateNo: 151, controller: "CtrlSet", name: "Regain Guard Control" },
          { stateNo: 151, operation: "resource:ctrlset" },
          { stateNo: 151, controller: "ChangeState", name: "Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported Projectile GetHitVar air guard hitshaketime artifact with defender branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGetHitVarAirGuardHitShakeTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-gethitvar-air-guard-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-gethitvar-air-guard-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 154, 155, 316]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 316)).toMatchObject({
      animNo: 316,
      source: "imported",
      actorKind: "player",
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "154/155 projectile air guard GetHitVar(hitshaketime) branch",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 154, controller: "ChangeAnim", name: "Air Guard Shake Anim" },
          { stateNo: 154, controller: "ChangeState", name: "Air Guard Shake Over" },
          { stateNo: 155, controller: "HitVelSet", name: "Apply Air Guard Velocity" },
          { stateNo: 155, operation: "kinematic:hitvelset" },
          { stateNo: 155, controller: "VelAdd", name: "Apply Air Guard Gravity" },
          { stateNo: 155, controller: "ChangeState", name: "Air Guarded HitVar Branch" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported Projectile airguard.velocity artifact", () => {
    const artifact = createSyntheticImportedProjectileAirGuardVelocityTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-air-guard-velocity-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-air-guard-velocity-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 })]),
    );
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(8);
    expect(airGuardFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardVelocityPhysicsFrames(),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile default airguard.velocity artifact", () => {
    const artifact = createSyntheticImportedProjectileAirGuardVelocityDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-air-guard-velocity-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-air-guard-velocity-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(9);
    expect(airGuardFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardVelocityPhysicsFrames(9, -3.5),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile airguard.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedProjectileAirGuardCornerPushTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-air-guard-cornerpush-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-air-guard-cornerpush-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardCornerPushPhysicsFrames(),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile default airguard.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedProjectileAirGuardCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-air-guard-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-projectile-air-guard-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const evidence = artifact.gates[0]?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-2.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardCornerPushPhysicsFrames(-2.5),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile GetHitVar guarded artifact with defender guard-state branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGetHitVarGuardedTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-gethitvar-guarded-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-gethitvar-guarded-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 150, 151, 304]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 304, animNo: 304 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1244, animNo: 981 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 1200, controller: "Projectile", name: "Helper ProjGuard Spawn" }),
        expect.objectContaining({ actorId: "p1", stateNo: 1200, operation: "projectile" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      ctrl: true,
    });
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitVelSet", "CtrlSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "kinematic:hitvelset", "resource:ctrlset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          label: "helper-local Projectile spawn telemetry",
          actorId: "p1",
          steps: [
            { stateNo: 1200, controller: "Projectile", name: "Helper ProjGuard Spawn" },
            { stateNo: 1200, operation: "projectile" },
          ],
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile GetHitVar hit metadata artifact with normal get-hit branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGetHitVarHitMetadataTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-gethitvar-hit-metadata-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-gethitvar-hit-metadata-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 336]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "audio:playsnd"]);
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 40, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7002,
            raw: "F7002",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7002,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -68 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "hit",
        sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 40, contactKind: "hit" }),
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7002, offsetX: 18, offsetY: -68 }),
      }),
    ]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 336, animNo: 336 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1251, animNo: 1002 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1252, animNo: 1003 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1004 }),
      ]),
    );
    const shakeFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 5000);
    expect(shakeFrame?.maxVel.x).toBe(4);
    expect(shakeFrame?.minVel.y).toBeLessThanOrEqual(-2);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8890 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8890 }),
      ]),
    );
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" }),
        expect.objectContaining({ actorId: "p1", stateNo: 1200, operation: "projectile" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 336,
      moveType: "H",
    });
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          label: "helper-local Projectile spawn telemetry",
          actorId: "p1",
          steps: [
            { stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" },
            { stateNo: 1200, operation: "projectile" },
          ],
        }),
        expect.objectContaining({
          label: "5000 helper Projectile normal hit GetHitVar metadata branch order",
          actorId: "p2",
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile GetHitVar hitid/chainid artifact with normal get-hit branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGetHitVarHitIdChainIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-gethitvar-hitid-chainid-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-gethitvar-hitid-chainid-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 338]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "audio:playsnd"]);
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 41, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7002,
            raw: "F7002",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7002,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -68 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "hit",
        sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 41, contactKind: "hit" }),
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7002, offsetX: 18, offsetY: -68 }),
      }),
    ]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 338, animNo: 338 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1253, animNo: 1005 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1254, animNo: 1006 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1007 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1-projectile-0", effect: expect.objectContaining({ id: 8891, hasHit: true }) }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 78 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 78 }),
      ]),
    );
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" }),
        expect.objectContaining({ actorId: "p1", stateNo: 1200, operation: "projectile" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 338,
      moveType: "H",
    });
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          label: "helper-local Projectile spawn telemetry",
          actorId: "p1",
          steps: [
            { stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" },
            { stateNo: 1200, operation: "projectile" },
          ],
        }),
        expect.objectContaining({
          label: "5000 helper Projectile normal hit GetHitVar(hitid/chainid) branch order",
          actorId: "p2",
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile GetHitVar hitcount artifact with normal get-hit branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGetHitVarHitCountTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-gethitvar-hitcount-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-gethitvar-hitcount-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 340]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "audio:playsnd"]);
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 42, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7002,
            raw: "F7002",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7002,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -68 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "hit",
        sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 42, contactKind: "hit" }),
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7002, offsetX: 18, offsetY: -68 }),
      }),
    ]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 340, animNo: 340 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1255, animNo: 1008 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1256, animNo: 1009 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1010 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1-projectile-0", effect: expect.objectContaining({ id: 8892, hasHit: true }) }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8892 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8892 }),
      ]),
    );
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" }),
        expect.objectContaining({ actorId: "p1", stateNo: 1200, operation: "projectile" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 340,
      moveType: "H",
    });
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          label: "helper-local Projectile spawn telemetry",
          actorId: "p1",
          steps: [
            { stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" },
            { stateNo: 1200, operation: "projectile" },
          ],
        }),
        expect.objectContaining({
          label: "5000 helper Projectile normal hit GetHitVar(hitcount) branch order",
          actorId: "p2",
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile HitCount artifact with helper-local branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileHitCountTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-hitcount-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-hitcount-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "audio:playsnd"]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 43, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7002,
            raw: "F7002",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7002,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -68 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "hit",
        sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 43, contactKind: "hit" }),
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7002, offsetX: 18, offsetY: -68 }),
      }),
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1257, animNo: 1011 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1258, animNo: 1012 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1013 }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8893 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8893 }),
      ]),
    );
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" }),
        expect.objectContaining({ actorId: "p1", stateNo: 1200, operation: "projectile" }),
      ]),
    );
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "helper-local Projectile HitCount spawn telemetry",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 1200, controller: "Projectile", name: "Helper ProjHit Spawn" },
          { stateNo: 1200, operation: "projectile" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported Helper Projectile GetHitVar guard kill artifact with defender branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGetHitVarGuardKillTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-gethitvar-guard-kill-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-gethitvar-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 150, 151, 334]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 334, animNo: 334 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1244, animNo: 981 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8857 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8857 }),
      ]),
    );
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 1200, controller: "Projectile", name: "Helper ProjGuard Spawn" }),
        expect.objectContaining({ actorId: "p1", stateNo: 1200, operation: "projectile" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      ctrl: true,
    });
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitVelSet", "CtrlSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "kinematic:hitvelset", "resource:ctrlset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          label: "helper-local Projectile spawn telemetry",
          actorId: "p1",
          steps: [
            { stateNo: 1200, controller: "Projectile", name: "Helper ProjGuard Spawn" },
            { stateNo: 1200, operation: "projectile" },
          ],
        }),
        expect.objectContaining({
          label: "150/151 helper projectile GetHitVar(kill) branch",
          actorId: "p2",
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile GetHitVar guard hitshaketime artifact with defender branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGetHitVarGuardHitShakeTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 150, 151, 313]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.CtrlSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 313, animNo: 313 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1244, animNo: 981 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(evidence?.controllerEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 1200, controller: "Projectile", name: "Helper ProjGuard Spawn" }),
        expect.objectContaining({ actorId: "p1", stateNo: 1200, operation: "projectile" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      ctrl: true,
    });
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitVelSet", "CtrlSet"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "kinematic:hitvelset", "resource:ctrlset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          label: "helper-local Projectile spawn telemetry",
          actorId: "p1",
          steps: [
            { stateNo: 1200, controller: "Projectile", name: "Helper ProjGuard Spawn" },
            { stateNo: 1200, operation: "projectile" },
          ],
        }),
        expect.objectContaining({
          label: "150/151 helper projectile GetHitVar(hitshaketime) branch",
          actorId: "p2",
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile GetHitVar air guard hitshaketime artifact with defender branch evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileGetHitVarAirGuardHitShakeTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["holdback", "x"]));
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 154, 155, 317]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 317, animNo: 317 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1244, animNo: 981 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      ctrl: true,
    });
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "Helper", "Projectile", "HitVelSet", "VelAdd"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["helper", "projectile", "kinematic:hitvelset"]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          label: "helper-local Projectile spawn telemetry",
          actorId: "p1",
          steps: [
            { stateNo: 1200, controller: "Projectile", name: "Helper ProjGuard Spawn" },
            { stateNo: 1200, operation: "projectile" },
          ],
        }),
        expect.objectContaining({
          label: "154/155 helper projectile air guard GetHitVar(hitshaketime) branch",
          actorId: "p2",
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile airguard.velocity artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileAirGuardVelocityTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-air-guard-velocity-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-air-guard-velocity-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(8);
    expect(airGuardFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1244, animNo: 981 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(gate?.requirements.requiredControllerEventSequences).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ label: "helper-local Projectile spawn telemetry", actorId: "p1" }),
        expect.objectContaining({ label: "154/155 named air guard-hit controller and typed operation order", actorId: "p2" }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardVelocityPhysicsFrames(),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default airguard.velocity artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileAirGuardVelocityDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-air-guard-velocity-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-air-guard-velocity-default-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const airGuardFrame = evidence?.actorFrames.find((actor) => actor.actorId === "p2" && actor.stateNo === 155);
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(airGuardFrame?.maxVel.x).toBeGreaterThanOrEqual(9);
    expect(airGuardFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardVelocityPhysicsFrames(9, -3.5),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile airguard.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileAirGuardCornerPushTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-air-guard-cornerpush-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-air-guard-cornerpush-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-5.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardCornerPushPhysicsFrames(),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile default airguard.cornerpush.veloff artifact", () => {
    const artifact = createSyntheticImportedHelperProjectileAirGuardCornerPushDefaultTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-air-guard-cornerpush-default-golden",
        source: "imported",
      },
      gates: [{ label: "synthetic-imported-helper-projectile-air-guard-cornerpush-default-golden", passed: true, failures: [] }],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    const attackerMinVelX = Math.min(...(evidence?.actorFrames.filter((actor) => actor.actorId === "p1").map((actor) => actor.minVel.x) ?? []));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(attackerMinVelX).toBeLessThanOrEqual(-2.5);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8856 }),
        expect.objectContaining({ ownerId: "p1-helper-0", actorId: "p2", targetId: 8856 }),
      ]),
    );
    expect(gate?.requirements.requiredActorFrames).toEqual(
      expect.arrayContaining([
        ...syntheticAirGuardCornerPushPhysicsFrames(-2.5),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1243, animNo: 980 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 982 }),
      ]),
    );
  });

  it("creates a synthetic imported GetHitVar anim/type artifact with route evidence", () => {
    const artifact = createSyntheticImportedGetHitVarAnimTypeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-animtype-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-animtype-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 287]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 287,
      customOwnerId: "p1",
      hitFall: {
        falling: true,
        damage: 0,
      },
    });
  });

  it("creates a synthetic imported GetHitVar snap offset artifact with route evidence", () => {
    const artifact = createSyntheticImportedGetHitVarSnapTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-snap-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-snap-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5100, 288]));
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "demo",
      stateNo: 288,
      customOwnerId: "p1",
    });
  });

  it("creates a synthetic imported custom-state artifact with owner-backed SelfState evidence", () => {
    const artifact = createSyntheticImportedCustomStateTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 889]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 889, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 890]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Branch" },
          { stateNo: 890, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 890, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 890, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar animtype artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarAnimTypeTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-animtype-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-animtype-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 892]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar animtype branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar AnimType Branch" },
          { stateNo: 892, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar animtype actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 892, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 892, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar type artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarTypeTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-type-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-type-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 897]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar type branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Type Branch" },
          { stateNo: 897, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar type actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 897, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 897, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar yaccel artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarYAccelTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-yaccel-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-yaccel-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 898]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar yaccel branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar YAccel Branch" },
          { stateNo: 898, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar yaccel actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 898, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 898, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar snap artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarSnapTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-snap-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-snap-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 899]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar snap branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Snap Branch" },
          { stateNo: 899, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar snap actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 899, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 899, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar hitcount/hitid/chainid artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarHitCountHitIdChainIdTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 900]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar hitcount/id branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar HitCount Ids Branch" },
          { stateNo: 900, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar hitcount/id actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 900, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 900, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar fall artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarFallTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-fall-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-fall-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 891]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar fall branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Fall Branch" },
          { stateNo: 891, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar fall actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 891, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 891, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar fall metadata artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarFallMetadataTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-fall-metadata-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-fall-metadata-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    const gate = artifact.gates[0];
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 903]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar fall metadata branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Fall Metadata Branch" },
          { stateNo: 903, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar fall metadata actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 903, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 903, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar fall envshake artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarFallEnvShakeTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-fall-envshake-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-fall-envshake-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 902]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar fall envshake branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Fall EnvShake Branch" },
          { stateNo: 902, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar fall envshake actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 902, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 902, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar down recover artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarDownRecoverTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-down-recover-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-down-recover-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 893]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar down recover branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Down Recover Branch" },
          { stateNo: 893, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar down recover actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 893, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 893, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar velocity artifact with owner-backed branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarVelocityTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-velocity-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-velocity-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 894]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "owner custom-state GetHitVar velocity branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Velocity Branch" },
          { stateNo: 894, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state GetHitVar velocity actor-frame order",
        steps: [
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            customOwnerId: "p1",
            stateNo: 888,
            animNo: 888,
            moveType: "H",
            observedVelXAtLeast: 4,
            observedVelXAtMost: 4,
            observedVelYAtLeast: -2,
            observedVelYAtMost: -2,
            minFrames: 1,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            customOwnerId: "p1",
            stateNo: 894,
            animNo: 888,
            moveType: "H",
            minFrames: 1,
          },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p2",
          customOwnerId: "p1",
          stateNo: 888,
          animNo: 888,
          moveType: "H",
          maxVel: expect.objectContaining({ x: 4 }),
          minVel: expect.objectContaining({ y: -2 }),
        }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 894, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar guarded artifact with owner-backed TargetState branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarGuardedTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-guarded-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-guarded-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 895]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetstate"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "guarded contact feeds owner TargetState",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "HitDef" },
          { stateNo: 200, controller: "TargetState", name: "Target Custom State" },
        ],
      },
      {
        label: "owner custom-state guarded GetHitVar branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Guarded Branch" },
          { stateNo: 895, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state guarded GetHitVar actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 895, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 895, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar guard kill artifact with owner-backed TargetState branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarGuardKillTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-guard-kill-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 904]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetstate"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "guard kill contact feeds owner TargetState",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "HitDef" },
          { stateNo: 200, controller: "TargetState", name: "Target Custom State" },
        ],
      },
      {
        label: "owner custom-state guard kill GetHitVar branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Guard Kill Branch" },
          { stateNo: 904, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state guard kill GetHitVar actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 904, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 904, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar guard timing artifact with owner-backed TargetState branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarGuardTimingTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-guard-timing-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-guard-timing-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 901]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetstate"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "guard timing contact feeds owner TargetState",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "HitDef" },
          { stateNo: 200, controller: "TargetState", name: "Target Custom State" },
        ],
      },
      {
        label: "owner custom-state guard timing GetHitVar branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar Guard Timing Branch" },
          { stateNo: 901, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state guard timing GetHitVar actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 901, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 901, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported custom-state GetHitVar isbound artifact with TargetBind-backed owner branch evidence", () => {
    const artifact = createSyntheticImportedCustomStateGetHitVarIsBoundTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-custom-state-gethitvar-isbound-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-custom-state-gethitvar-isbound-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 896]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetBind).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetbind"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetstate"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "direct hit feeds TargetBind before owner TargetState",
        actorId: "p1",
        allowSameTick: true,
        steps: [
          { stateNo: 200, controller: "HitDef" },
          { stateNo: 200, controller: "TargetBind", name: "Target Bind Before Pause" },
          { stateNo: 200, controller: "TargetState", name: "Target Custom State" },
        ],
      },
      {
        label: "owner custom-state isbound GetHitVar branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 888, controller: "ChangeState", name: "Owner Custom State GetHitVar IsBound Branch" },
          { stateNo: 896, controller: "SelfState", name: "Return To Self" },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "owner custom-state isbound GetHitVar actor-frame order",
        steps: [
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H", minFrames: 1 },
          { actorId: "p2", source: "imported", actorKind: "player", customOwnerId: "p1", stateNo: 896, animNo: 888, moveType: "H", minFrames: 1 },
        ],
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 888, animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", stateNo: 896, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported target-owned custom-state artifact for p2getp1state zero", () => {
    const artifact = createSyntheticImportedTargetOwnedCustomStateTraceArtifact({ generatedAt: "2026-06-28T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-owned-custom-state-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-target-owned-custom-state-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: undefined, stateNo: 888, animNo: 888, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates an imported default Common1 get-hit artifact without p2stateno", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-gethit",
      displayName: "Synthetic Imported Default GetHit",
      defaultGetHitState: { stateNo: 5000, animNo: 5000 },
    });
    const artifact = createImportedDefaultGetHitTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-gethit-golden",
      targetLabel: "Synthetic Imported Default GetHit",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-gethit-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000]));
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 5000,
      moveType: "H",
    });
  });

  it("creates an imported default crouch Common1 get-hit artifact without p2stateno", () => {
    const artifact = createSyntheticImportedDefaultCrouchGetHitTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-crouch-gethit-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-crouch-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5010]));
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 5010]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 5010,
      moveType: "H",
    });
  });

  it("creates an imported default air Common1 get-hit artifact without p2stateno", () => {
    const artifact = createSyntheticImportedDefaultAirGetHitTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-air-gethit-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-air-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.actorSources).toEqual(["imported"]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5020]));
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 5020]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 5020,
      moveType: "H",
    });
  });

  it("creates an imported default Common1 progression artifact with HitShakeOver and HitOver", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-gethit-progression",
      displayName: "Synthetic Imported Default GetHit Progression",
      defaultGetHitProgression: { shakeStateNo: 5000, slideStateNo: 5001 },
    });
    const artifact = createImportedDefaultGetHitProgressionTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-gethit-progression-golden",
      targetLabel: "Synthetic Imported Default GetHit Progression",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-gethit-progression-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-gethit-progression-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5000, 5001]));
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultGetHitProgressionControllerSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(defaultGetHitProgressionPhysicsFrames());
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultGetHitProgressionActorFrameSequence(),
    ]);
    expect(
      evidence?.controllerEvents.filter((event) => event.actorId === "p2").map((event) => `${event.stateNo}:${event.controller}`),
    ).toEqual(expect.arrayContaining(["5000:ChangeState", "5001:ChangeState"]));
    const shakeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5000);
    const slideFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5001);
    expect(shakeFrame).toMatchObject({
      animNo: 5000,
      stateType: "S",
      moveType: "H",
      physics: "N",
      clsn1Count: 0,
      clsn2Count: 1,
      frames: 5,
    });
    expect(slideFrame).toMatchObject({
      animNo: 5001,
      stateType: "S",
      moveType: "H",
      physics: "S",
      clsn1Count: 0,
      clsn2Count: 1,
      frames: 8,
    });
    expect(shakeFrame?.lastTick ?? 0).toBeLessThan(slideFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("allows imported default Common1 progression gates to require fixture-specific controller sequences", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-gethit-progression-custom-sequence",
      displayName: "Synthetic Imported Default GetHit Progression Custom Sequence",
      defaultGetHitProgression: { shakeStateNo: 5000, slideStateNo: 5001 },
    });
    const requiredControllerEventSequences = [
      {
        ...defaultGetHitProgressionControllerSequence(),
        label: "custom fixture-specific get-hit order",
      },
    ];
    const artifact = createImportedDefaultGetHitProgressionTraceArtifact(imported, {
      generatedAt: "2026-07-02T00:00:00.000Z",
      targetId: "synthetic-imported-default-gethit-progression-custom-sequence-golden",
      requiredControllerEventSequences,
    });

    expect(artifact.status).toBe("passed");
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual(requiredControllerEventSequences);
  });

  it("creates an imported default crouch Common1 progression artifact with HitShakeOver and HitOver", () => {
    const artifact = createSyntheticImportedDefaultCrouchGetHitProgressionTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-crouch-gethit-progression-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-crouch-gethit-progression-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5010, 5011]));
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultGetHitProgressionControllerSequence(5010, 5011),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual(defaultCrouchGetHitProgressionPhysicsFrames());
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultGetHitProgressionActorFrameSequence(5010, 5011),
    ]);
    const shakeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5010);
    const slideFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5011);
    expect(shakeFrame).toMatchObject({
      animNo: 5010,
      stateType: "C",
      moveType: "H",
      physics: "N",
      clsn1Count: 0,
      clsn2Count: 1,
      frames: 5,
    });
    expect(slideFrame).toMatchObject({
      animNo: 5011,
      stateType: "C",
      moveType: "H",
      physics: "C",
      clsn1Count: 0,
      clsn2Count: 1,
      frames: 8,
    });
    expect(shakeFrame?.lastTick ?? 0).toBeLessThan(slideFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("describes official KFM crouch Common1 progression controller and typed-operation order", () => {
    expect(officialKfmDefaultCrouchGetHitProgressionControllerSequence()).toEqual({
      label: "5010/5011 crouch HitShakeOver, slide damping, and HitOver controller order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 5010, controller: "ChangeAnim" },
        { stateNo: 5010, controller: "ChangeState" },
        { stateNo: 5011, controller: "HitVelSet" },
        { stateNo: 5011, controller: "kinematic:hitvelset", operation: "kinematic:hitvelset" },
        { stateNo: 5011, controller: "VelMul" },
        { stateNo: 5011, controller: "kinematic:velmul", operation: "kinematic:velmul" },
        { stateNo: 5011, controller: "VelSet" },
        { stateNo: 5011, controller: "kinematic:velset", operation: "kinematic:velset" },
        { stateNo: 5011, controller: "DefenceMulSet" },
        { stateNo: 5011, controller: "damage-scale:defencemulset", operation: "damage-scale:defencemulset" },
        { stateNo: 5011, controller: "ChangeState" },
      ],
    });
  });

  it("creates a synthetic imported GetHitVar hittime artifact for normal get-hit CNS", () => {
    const artifact = createSyntheticImportedGetHitVarHitTimeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-hittime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-hittime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 309]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 309, animNo: 309 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 309,
      moveType: "H",
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 normal hit GetHitVar(hittime) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar hitshaketime artifact for normal get-hit CNS", () => {
    const artifact = createSyntheticImportedGetHitVarHitShakeTimeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-hitshaketime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-hitshaketime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 310]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 310, animNo: 310 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 310,
      moveType: "H",
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 normal hit GetHitVar(hitshaketime) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar damage artifact for normal get-hit CNS", () => {
    const artifact = createSyntheticImportedGetHitVarDamageTraceArtifact({ generatedAt: "2026-07-02T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-damage-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-damage-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 323]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 323, animNo: 323 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 323,
      moveType: "H",
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 normal hit GetHitVar(damage) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar kill artifact for normal get-hit CNS", () => {
    const artifact = createSyntheticImportedGetHitVarKillTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-kill-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 329]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 329, animNo: 329 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 329,
      moveType: "H",
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 normal hit GetHitVar(kill) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal Kill Flag Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar hitid/chainid artifact for normal get-hit CNS", () => {
    const artifact = createSyntheticImportedGetHitVarHitIdChainIdTraceArtifact({ generatedAt: "2026-07-02T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-hitid-chainid-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-hitid-chainid-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 326]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 326, animNo: 326 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 326,
      moveType: "H",
    });
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 normal hit GetHitVar(hitid/chainid) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar hitcount artifact for normal get-hit CNS", () => {
    const artifact = createSyntheticImportedGetHitVarHitCountTraceArtifact({ generatedAt: "2026-07-04T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-hitcount-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-hitcount-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 327]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 327, animNo: 327 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 327,
      moveType: "H",
    });
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 normal hit GetHitVar(hitcount) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("creates a synthetic imported GetHitVar velocity artifact for normal get-hit CNS", () => {
    const artifact = createSyntheticImportedGetHitVarVelocityTraceArtifact({ generatedAt: "2026-07-02T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-gethitvar-velocity-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-gethitvar-velocity-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 324]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", stateNo: 324, animNo: 324 }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 324,
      moveType: "H",
    });
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 normal hit GetHitVar(xvel/yvel) branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitTime Branch" }],
      },
    ]);
  });

  it("describes official KFM default Common1 progression physics-frame evidence separately from synthetic traces", () => {
    expect(officialKfmDefaultGetHitProgressionPhysicsFrames()).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5000,
        animNo: 5000,
        stateType: "S",
        moveType: "H",
        physics: "N",
        clsn1Count: 0,
        clsn2Count: 2,
        minFrames: 5,
        observedPosYAtLeast: 0,
        observedPosYAtMost: 0,
        observedVelXAtLeast: 0,
        observedVelXAtMost: 0,
        observedVelYAtLeast: 0,
        observedVelYAtMost: 0,
        bodyWidthFront: 39,
        bodyWidthBack: 39,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5001,
        stateType: "S",
        moveType: "H",
        physics: "S",
        clsn1Count: 0,
        clsn2Count: 2,
        minFrames: 6,
        observedPosYAtLeast: 0,
        observedPosYAtMost: 0,
        observedVelXAtLeast: 1,
        observedVelXAtMost: 0,
        observedVelYAtLeast: 0,
        observedVelYAtMost: 0,
        bodyWidthFront: 39,
        bodyWidthBack: 39,
      },
    ]);
  });

  it("describes official KFM crouch Common1 progression physics-frame evidence separately from synthetic traces", () => {
    expect(officialKfmDefaultCrouchGetHitProgressionPhysicsFrames()).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5010,
        animNo: 5010,
        stateType: "C",
        moveType: "H",
        physics: "N",
        clsn1Count: 0,
        clsn2Count: 2,
        minFrames: 5,
        observedPosYAtLeast: 0,
        observedPosYAtMost: 0,
        observedVelXAtLeast: 0,
        observedVelXAtMost: 0,
        observedVelYAtLeast: 0,
        observedVelYAtMost: 0,
        bodyWidthFront: 39,
        bodyWidthBack: 39,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5011,
        animNo: 5020,
        stateType: "C",
        moveType: "H",
        physics: "C",
        clsn1Count: 0,
        clsn2Count: 3,
        minFrames: 8,
        observedPosYAtLeast: 0,
        observedPosYAtMost: 0,
        observedVelXAtLeast: 1,
        observedVelXAtMost: 0,
        observedVelYAtLeast: 0,
        observedVelYAtMost: 0,
        bodyWidthFront: 39,
        bodyWidthBack: 39,
      },
    ]);
  });

  it("creates an imported default Common1 airborne fall artifact through 5030 and 5050", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-gethit",
      displayName: "Synthetic Imported Default Fall GetHit",
      defaultGetHitFall: { shakeStateNo: 5000, slideStateNo: 5001, airStateNo: 5030, fallStateNo: 5050 },
    });
    const attacker = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-attacker",
      displayName: "Synthetic Imported Default Fall Attacker",
      groundVelocity: [-3, -6],
      fall: { enabled: true, damage: 20, velocity: { x: 3, y: -6 }, recover: false, recoverTime: 30 },
    });
    const artifact = createImportedDefaultFallGetHitTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-fall-gethit-golden",
      targetLabel: "Synthetic Imported Default Fall GetHit",
      attacker,
      requiredControllerEventSequences: [defaultFallGetHitControllerSequence()],
      requiredActorFrameSequences: [defaultFallGetHitActorFrameSequence()],
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-gethit-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGetHitControllerSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([defaultFallGetHitActorFrameSequence()]);
    const shakeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5000);
    const airFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5030);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    expect(shakeFrame?.moveType).toBe("H");
    expect(airFrame?.moveType).toBe("H");
    expect(fallFrame?.moveType).toBe("H");
    expect(shakeFrame?.lastTick ?? 0).toBeLessThan(airFrame?.firstTick ?? 0);
    expect(airFrame?.lastTick ?? 0).toBeLessThan(fallFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      hitFall: {
        falling: true,
        velocity: { y: -6 },
        recover: false,
      },
    });
  });

  it("creates an imported default air Common1 fall artifact through 5020, 5030, and 5050", () => {
    const artifact = createSyntheticImportedDefaultAirFallGetHitTraceArtifact({ generatedAt: "2026-07-02T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-air-fall-gethit-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5020, 5030, 5050]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGetHitControllerSequence(5020),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5020, 5030, 5050]),
    ]);
    const shakeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5020);
    const airFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5030);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    expect(shakeFrame).toMatchObject({ moveType: "H", stateType: "A", physics: "N" });
    expect(airFrame?.moveType).toBe("H");
    expect(fallFrame?.moveType).toBe("H");
    expect(shakeFrame?.lastTick ?? 0).toBeLessThan(airFrame?.firstTick ?? 0);
    expect(airFrame?.lastTick ?? 0).toBeLessThan(fallFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      hitFall: {
        falling: true,
        velocity: { y: -6 },
        recover: false,
      },
    });
  });

  it("creates an imported default air Common1 ground-impact artifact through 5020, 5030, 5050, and 5100", () => {
    const artifact = createSyntheticImportedDefaultAirGroundImpactTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-air-ground-impact-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5020, 5030, 5050, 5100]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallDamage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfalldamage"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGroundImpactControllerSequence(5020),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5020, 5030, 5050, 5100]),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5100,
        stateType: "L",
        moveType: "H",
        physics: "N",
        minFrames: 1,
      },
    ]);
    const shakeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5020);
    const airFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5030);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const groundFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5100);
    expect(shakeFrame).toMatchObject({ moveType: "H", stateType: "A", physics: "N" });
    expect(airFrame?.moveType).toBe("H");
    expect(fallFrame?.moveType).toBe("H");
    expect(groundFrame).toMatchObject({ moveType: "H", stateType: "L", physics: "N" });
    expect(shakeFrame?.lastTick ?? 0).toBeLessThan(airFrame?.firstTick ?? 0);
    expect(airFrame?.lastTick ?? 0).toBeLessThan(fallFrame?.firstTick ?? 0);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(groundFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates an imported default air Common1 lie-down recovery artifact through 5020, 5030, 5050, 5100, 5101, 5110, and 5120", () => {
    const artifact = createSyntheticImportedDefaultAirLieDownRecoveryTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-air-liedown-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(
      expect.arrayContaining([0, 200, 5020, 5030, 5050, 5100, 5101, 5110, 5120]),
    );
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallDamage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallVel).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallvel"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfalldamage"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallset"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGroundImpactControllerSequence(5020),
      defaultFallBounceLieDownControllerSequence(),
      defaultFallLieDownGetUpControllerSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5020, 5030, 5050, 5100, 5101, 5110]),
      defaultFallLieDownGetUpActorFrameSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        animNo: 5110,
        moveType: "H",
        observedHitFallDownRecoverTimeAtLeast: 58,
        observedHitFallDownRecoverTimeAtMost: 54,
        observedHitFallDownRecoverTimeDropAtLeast: 1,
        minFrames: 2,
      },
    ]);
    const frames = new Map(
      [5020, 5030, 5050, 5100, 5101, 5110, 5120].map((stateNo) => [
        stateNo,
        evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === stateNo),
      ]),
    );
    expect(frames.get(5020)).toMatchObject({ moveType: "H", stateType: "A", physics: "N" });
    expect(frames.get(5100)).toMatchObject({ moveType: "H", stateType: "L", physics: "N" });
    expect(frames.get(5101)).toMatchObject({ moveType: "H", stateType: "L", physics: "N" });
    expect(frames.get(5110)).toMatchObject({ moveType: "H", stateType: "L", physics: "N" });
    expect(frames.get(5120)).toMatchObject({ moveType: "I", stateType: "L", physics: "N" });
    expect(frames.get(5020)?.lastTick ?? 0).toBeLessThan(frames.get(5030)?.firstTick ?? 0);
    expect(frames.get(5030)?.lastTick ?? 0).toBeLessThan(frames.get(5050)?.firstTick ?? 0);
    expect(frames.get(5050)?.lastTick ?? 0).toBeLessThan(frames.get(5100)?.firstTick ?? 0);
    expect(frames.get(5100)?.lastTick ?? 0).toBeLessThan(frames.get(5101)?.firstTick ?? 0);
    expect(frames.get(5101)?.lastTick ?? 0).toBeLessThan(frames.get(5110)?.firstTick ?? 0);
    expect(frames.get(5110)?.lastTick ?? 0).toBeLessThan(frames.get(5120)?.firstTick ?? 0);
    const lieDownFrame = frames.get(5110);
    expect(lieDownFrame?.frames).toBeGreaterThanOrEqual(2);
    expect(lieDownFrame?.maxHitFallDownRecoverTime).toBeGreaterThanOrEqual(58);
    expect(lieDownFrame?.minHitFallDownRecoverTime).toBeLessThanOrEqual(54);
    expect(lieDownFrame?.firstHitFallDownRecoverTime).toBeGreaterThanOrEqual(58);
    expect(lieDownFrame?.lastHitFallDownRecoverTime).toBeLessThanOrEqual(54);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("describes official KFM fall get-hit controller evidence without relying on numeric controller names", () => {
    expect(officialKfmFallGetHitControllerSequence()).toEqual({
      label: "Official KFM 5000/5030/5050/5100/5101/5110 fall get-hit controller and typed operation order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 5000, controller: "ChangeState" },
        { stateNo: 5030, controller: "VelAdd" },
        { stateNo: 5030, controller: "HitVelSet" },
        { stateNo: 5030, operation: "kinematic:hitvelset" },
        { stateNo: 5030, controller: "ChangeState" },
        { stateNo: 5050, controller: "VelAdd" },
        { stateNo: 5050, controller: "ChangeState" },
        { stateNo: 5100, controller: "VelSet" },
        { stateNo: 5100, operation: "kinematic:velset" },
        { stateNo: 5100, controller: "ChangeState" },
        { stateNo: 5101, controller: "HitFallVel" },
        { stateNo: 5101, operation: "hitfall:hitfallvel" },
        { stateNo: 5101, controller: "VelAdd" },
        { stateNo: 5101, controller: "ChangeState" },
        { stateNo: 5110, controller: "HitFallDamage" },
        { stateNo: 5110, operation: "hitfall:hitfalldamage" },
        { stateNo: 5110, controller: "VelSet" },
        { stateNo: 5110, operation: "kinematic:velset" },
      ],
    });
  });

  it("creates a synthetic imported default Common1 fall recovery chain artifact", () => {
    const artifact = createSyntheticImportedDefaultFallRecoveryTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-default-fall-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5100, 5101, 5110, 5120]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallDamage).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGetHitControllerSequence(),
      defaultFallLieDownGetUpControllerSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5100, 5101, 5110]),
      defaultFallLieDownGetUpActorFrameSequence(),
    ]);
    const shakeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5000);
    const airFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5030);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const groundFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5100);
    const bounceFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5101);
    const lieDownFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5110);
    const getUpFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5120);
    expect(shakeFrame?.moveType).toBe("H");
    expect(airFrame?.moveType).toBe("H");
    expect(fallFrame?.moveType).toBe("H");
    expect(groundFrame?.moveType).toBe("H");
    expect(bounceFrame?.moveType).toBe("H");
    expect(lieDownFrame?.moveType).toBe("H");
    expect(getUpFrame?.moveType).toBe("I");
    expect(shakeFrame?.lastTick ?? 0).toBeLessThan(airFrame?.firstTick ?? 0);
    expect(airFrame?.lastTick ?? 0).toBeLessThan(fallFrame?.firstTick ?? 0);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(groundFrame?.firstTick ?? 0);
    expect(groundFrame?.lastTick ?? 0).toBeLessThan(bounceFrame?.firstTick ?? 0);
    expect(bounceFrame?.lastTick ?? 0).toBeLessThan(lieDownFrame?.firstTick ?? 0);
    expect(lieDownFrame?.frames).toBeGreaterThanOrEqual(2);
    expect(lieDownFrame?.maxHitFallDownRecoverTime).toBeGreaterThanOrEqual(58);
    expect(lieDownFrame?.minHitFallDownRecoverTime).toBeLessThanOrEqual(54);
    expect(lieDownFrame?.firstHitFallDownRecoverTime).toBeGreaterThanOrEqual(58);
    expect(lieDownFrame?.lastHitFallDownRecoverTime).toBeLessThanOrEqual(54);
    expect(lieDownFrame?.lastTick ?? 0).toBeLessThan(getUpFrame?.firstTick ?? 0);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        animNo: 5110,
        moveType: "H",
        observedHitFallDownRecoverTimeAtLeast: 58,
        observedHitFallDownRecoverTimeAtMost: 54,
        observedHitFallDownRecoverTimeDropAtLeast: 1,
        minFrames: 2,
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported AssertSpecial NoGetUpFromLieDown recovery artifact", () => {
    const artifact = createSyntheticImportedAssertSpecialNoGetUpFromLieDownTraceArtifact({
      generatedAt: "2026-07-03T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-nogetupfromliedown-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-nogetupfromliedown-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5100, 5101, 5110]));
    expect(evidence?.executedStates).not.toContain(5120);
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([5120]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGetHitControllerSequence(),
      defaultFallBounceLieDownControllerSequence(),
      defaultFallLieDownNoGetUpControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5100, 5101, 5110]),
    ]);
    const lieDownFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5110);
    expect(lieDownFrame?.moveType).toBe("H");
    expect(lieDownFrame?.maxHitFallDownRecoverTime).toBeGreaterThanOrEqual(1);
    expect(lieDownFrame?.minHitFallDownRecoverTime).toBeLessThanOrEqual(0);
    expect(lieDownFrame?.firstHitFallDownRecoverTime).toBeGreaterThan(
      lieDownFrame?.lastHitFallDownRecoverTime ?? Number.POSITIVE_INFINITY,
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 5110,
      moveType: "H",
      ctrl: false,
      hitFall: {
        downRecover: true,
        downRecoverTime: 0,
      },
    });
  });

  it("creates a required synthetic imported AssertSpecial NoFastRecoverFromLieDown recovery artifact", () => {
    const artifact = createSyntheticImportedAssertSpecialNoFastRecoverFromLieDownTraceArtifact({
      generatedAt: "2026-07-03T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-assertspecial-nofastrecoverfromliedown-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-assertspecial-nofastrecoverfromliedown-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5100, 5101, 5110, 5120]));
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.assertspecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGetHitControllerSequence(),
      defaultFallBounceLieDownControllerSequence(),
      defaultFallLieDownNoFastRecoverControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5100, 5101, 5110]),
      defaultFallLieDownGetUpActorFrameSequence(),
    ]);
    const lieDownFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5110);
    expect(lieDownFrame?.moveType).toBe("H");
    expect(lieDownFrame?.maxHitFallDownRecoverTime).toBeGreaterThanOrEqual(4);
    expect(lieDownFrame?.minHitFallDownRecoverTime).toBeLessThanOrEqual(0);
    expect(lieDownFrame?.firstHitFallDownRecoverTime).toBeGreaterThan(
      lieDownFrame?.lastHitFallDownRecoverTime ?? Number.POSITIVE_INFINITY,
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
      hitFall: {
        downRecover: true,
        downRecoverTime: 0,
      },
    });
  });

  it("creates a required synthetic imported default lie-down fast recovery artifact", () => {
    const artifact = createSyntheticImportedDefaultLieDownFastRecoveryTraceArtifact({
      generatedAt: "2026-07-04T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-liedown-fast-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-default-liedown-fast-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5100, 5101, 5110, 5120]));
    expect(evidence?.executedControllers.AssertSpecial ?? 0).toBe(0);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallGetHitControllerSequence(),
      defaultFallBounceLieDownControllerSequence(),
      defaultFallLieDownFastRecoverControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5100, 5101, 5110]),
      defaultFallLieDownGetUpActorFrameSequence(),
    ]);
    expect(
      evidence?.controllerEvents.some((event) => event.stateNo === 5110 && event.controller === "ChangeState" && event.name === "Get Up"),
    ).toBe(false);
    const lieDownFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5110);
    expect(lieDownFrame?.moveType).toBe("H");
    expect(lieDownFrame?.maxHitFallDownRecoverTime).toBeGreaterThanOrEqual(4);
    expect(lieDownFrame?.frames).toBeLessThanOrEqual(2);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
      hitFall: {
        downRecover: true,
        downRecoverTime: 0,
      },
    });
  });

  it("creates a synthetic imported default Common1 recovery-input artifact", () => {
    const artifact = createSyntheticImportedDefaultFallRecoveryInputTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-recovery-input-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-default-fall-recovery-input-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5210]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported default fall recovery-input priority artifact", () => {
    const artifact = createSyntheticImportedDefaultFallRecoveryInputPriorityTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-recovery-input-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5000, 5030, 5050, 5210]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5250, 5200]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallset"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultHitFallRecoveryInputPriorityControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultHitFallRecoveryInputPriorityActorFrameSequence(),
    ]);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const recoveryFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5210);
    const probeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5250);
    expect(fallFrame?.maxHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(recoveryFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(recoveryFrame?.firstTick ?? 0);
    expect(probeFrame).toBeUndefined();
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a synthetic imported default Common1 air-recovery velocity artifact", () => {
    const artifact = createSyntheticImportedDefaultFallAirRecoveryVelocityTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-air-recovery-velocity-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-recovery-input-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5210]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    const airRecoveryFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.animNo === 5210,
    );
    expect(airRecoveryFrame?.minVel.x).toBeLessThanOrEqual(0);
    expect(airRecoveryFrame?.maxVel.x).toBeGreaterThanOrEqual(0);
    expect(airRecoveryFrame?.minVel.y).toBeLessThanOrEqual(-2);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        animNo: 5210,
        moveType: "I",
        observedVelXAtLeast: 0,
        observedVelXAtMost: 0,
        observedVelYAtMost: -2,
        minFrames: 1,
      },
    ]);
  });

  it("creates a required synthetic imported official-style Common1 air-recovery landing artifact", () => {
    const artifact = createSyntheticImportedDefaultFallOfficialAirRecoveryTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-official-air-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-recovery-input-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5210, 52]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    const fallFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 5050,
    );
    const airRecoveryFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 5210,
    );
    const landFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 52,
    );
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(airRecoveryFrame?.minVel.x).toBeLessThanOrEqual(0);
    expect(airRecoveryFrame?.maxVel.x).toBeGreaterThanOrEqual(0);
    expect(airRecoveryFrame?.minVel.y).toBeLessThanOrEqual(-2);
    expect(landFrame?.maxPos.y).toBe(0);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "5050 recoverTime countdown before 5210/52 air recovery",
        steps: [
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5050,
            moveType: "H",
            observedHitFallRecoverTimeAtLeast: 1,
            observedHitFallRecoverTimeAtMost: 0,
            observedHitFallRecoverTimeDropAtLeast: 1,
            minFrames: 2,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5210,
            moveType: "I",
            observedHitFallRecoverTimeAtMost: 0,
            observedVelXAtLeast: 0,
            observedVelXAtMost: 0,
            observedVelYAtMost: -2,
            minFrames: 1,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 52,
            moveType: "I",
            observedPosYAtLeast: 0,
            observedPosYAtMost: 0,
            minFrames: 1,
          },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultAirRecoveryLandControllerSequence(),
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported air-entry Common1 recovery-input artifact from 5020", () => {
    const artifact = createSyntheticImportedDefaultAirFallRecoveryInputTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-air-fall-recovery-input-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-recovery-input-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5020, 5030, 5050, 5210, 52]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallset"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultAirFallRecoveryInputActorFrameSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultAirFallRecoveryInputControllerSequence(),
    ]);
    const frames = new Map(
      [5020, 5030, 5050, 5210, 52].map((stateNo) => [
        stateNo,
        evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === stateNo),
      ]),
    );
    expect(frames.get(5020)).toMatchObject({ moveType: "H", stateType: "A", physics: "N" });
    expect(frames.get(5030)).toMatchObject({ moveType: "H" });
    expect(frames.get(5050)).toMatchObject({ moveType: "H" });
    expect(frames.get(5210)).toMatchObject({ moveType: "I" });
    expect(frames.get(52)).toMatchObject({ moveType: "I" });
    expect(frames.get(5020)?.lastTick ?? 0).toBeLessThan(frames.get(5030)?.firstTick ?? 0);
    expect(frames.get(5030)?.lastTick ?? 0).toBeLessThan(frames.get(5050)?.firstTick ?? 0);
    expect(frames.get(5050)?.lastTick ?? 0).toBeLessThan(frames.get(5210)?.firstTick ?? 0);
    expect(frames.get(5210)?.lastTick ?? 0).toBeLessThan(frames.get(52)?.firstTick ?? 0);
    expect(frames.get(5050)?.firstHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(frames.get(5050)?.lastHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(frames.get(5210)?.minVel.y).toBeLessThanOrEqual(-2);
    expect(frames.get(52)?.maxPos.y).toBe(0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported air-entry Common1 recovery-input too-early reject artifact from 5020", () => {
    const artifact = createSyntheticImportedDefaultAirFallRecoveryTooEarlyTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-air-fall-recovery-too-early-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-recovery-too-early-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5020, 5030, 5050]));
    for (const stateNo of [5210, 5200, 5201, 52, 5100]) {
      expect(evidence?.executedStates).not.toContain(stateNo);
    }
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultAirFallRecoveryTooEarlyActorFrameSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultAirFallRecoveryTooEarlyControllerSequence(),
    ]);
    const frames = new Map(
      [5020, 5030, 5050].map((stateNo) => [
        stateNo,
        evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === stateNo),
      ]),
    );
    expect(frames.get(5020)).toMatchObject({ moveType: "H", stateType: "A", physics: "N" });
    expect(frames.get(5030)).toMatchObject({ moveType: "H" });
    expect(frames.get(5050)).toMatchObject({ moveType: "H" });
    expect(frames.get(5020)?.lastTick ?? 0).toBeLessThan(frames.get(5030)?.firstTick ?? 0);
    expect(frames.get(5030)?.lastTick ?? 0).toBeLessThan(frames.get(5050)?.firstTick ?? 0);
    expect(frames.get(5050)?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(frames.get(5050)?.firstHitFallRecoverTime).toBeGreaterThan(
      frames.get(5050)?.lastHitFallRecoverTime ?? Number.POSITIVE_INFINITY,
    );
    expect(frames.get(5050)?.frames).toBeGreaterThanOrEqual(2);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 5050,
      moveType: "H",
      ctrl: false,
    });
  });

  it("creates a required synthetic imported HitFall true and CanRecover false artifact", () => {
    const artifact = createSyntheticImportedHitFallCanRecoverTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitfall-canrecover-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5000, 5030, 5050, 5220]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5210, 5200]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultHitFallCanRecoverProbeControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5220]),
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeMinAtLeast: 1,
        minFrames: 1,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5220,
        moveType: "H",
        observedHitFallRecoverTimeMinAtLeast: 1,
        minFrames: 1,
      },
    ]);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const probeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5220);
    expect(fallFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(probeFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(probeFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported HitFall true and CanRecover true artifact", () => {
    const artifact = createSyntheticImportedHitFallCanRecoverReadyTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitfall-canrecover-ready-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5000, 5030, 5050, 5250]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5210, 5200]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x"]));
    expect(evidence?.activeCommands).not.toEqual(expect.arrayContaining(["recovery"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultHitFallCanRecoverReadyProbeControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5250]),
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeAtMost: 0,
        observedHitFallRecoverTimeDropAtLeast: 1,
        minFrames: 2,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5250,
        moveType: "H",
        observedHitFallRecoverTimeAtMost: 0,
        minFrames: 1,
      },
    ]);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const probeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5250);
    expect(fallFrame?.maxHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(probeFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(probeFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported recovery input priority over CanRecover probe artifact", () => {
    const artifact = createSyntheticImportedHitFallRecoveryInputPriorityTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitfall-recovery-input-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5000, 5030, 5050, 5210]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5250, 5200]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["hitfall:hitfallset"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultHitFallRecoveryInputPriorityControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultHitFallRecoveryInputPriorityActorFrameSequence(),
    ]);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const recoveryFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5210);
    const probeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5250);
    expect(fallFrame?.maxHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(recoveryFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(recoveryFrame?.firstTick ?? 0);
    expect(probeFrame).toBeUndefined();
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported HitFall recover true artifact", () => {
    const artifact = createSyntheticImportedHitFallRecoverTrueTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitfall-recover-true-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5000, 5030, 5050, 5240]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5210, 5200]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultHitFallRecoverTrueProbeControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5240]),
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeMinAtLeast: 1,
        minFrames: 1,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5240,
        moveType: "H",
        observedHitFallRecoverTimeMinAtLeast: 1,
        minFrames: 1,
      },
    ]);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const probeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5240);
    expect(fallFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(probeFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(probeFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported HitFall recover false artifact", () => {
    const artifact = createSyntheticImportedHitFallRecoverFalseTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitfall-recover-false-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-gethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([0, 200, 5000, 5030, 5050, 5230]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5210, 5200]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      defaultHitFallRecoverFalseProbeControllerSequence(),
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallGetHitActorFrameSequence([5000, 5030, 5050, 5230]),
    ]);
    expect(gate?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeMinAtLeast: 1,
        minFrames: 1,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5230,
        moveType: "H",
        observedHitFallRecoverTimeMinAtLeast: 1,
        minFrames: 1,
      },
    ]);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const probeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5230);
    expect(fallFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(probeFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(probeFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a required synthetic imported HitFall false artifact", () => {
    const artifact = createSyntheticImportedHitFallFalseTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitfall-false-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-hitfall-false-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 325]));
    for (const forbiddenState of [5001, 5030, 5050, 5210, 5200]) {
      expect(evidence?.executedStates).not.toContain(forbiddenState);
    }
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x"]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5000 HitFall false branch order",
        actorId: "p2",
        allowSameTick: true,
        steps: [{ stateNo: 5000, controller: "ChangeState", name: "Normal HitFall False Probe" }],
      },
    ]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "5000 normal shake before HitFall false probe",
        steps: [
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5000,
            moveType: "H",
            minFrames: 1,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 325,
            moveType: "H",
            minFrames: 1,
          },
        ],
      },
    ]);
    const shakeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5000);
    const probeFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 325);
    expect(shakeFrame?.lastTick ?? 0).toBeLessThan(probeFrame?.firstTick ?? 0);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 325,
      moveType: "H",
    });
  });

  it("creates a synthetic imported default Common1 recovery-threshold artifact", () => {
    const artifact = createSyntheticImportedDefaultFallRecoveryThresholdTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });
    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-recovery-threshold-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-default-fall-recovery-threshold-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5210]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.source === "imported" && frame.animNo === 5050);
    expect(fallFrame).toMatchObject({
      actorId: "p2",
      source: "imported",
      animNo: 5050,
    });
    expect(fallFrame?.maxHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastHitFallRecoverTime).toBeLessThanOrEqual(0);
    const recoveryFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.source === "imported" && frame.animNo === 5210);
    expect(recoveryFrame).toMatchObject({
      actorId: "p2",
      source: "imported",
      animNo: 5210,
      minHitFallRecoverTime: 0,
    });
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        animNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeAtMost: 0,
        observedHitFallRecoverTimeDropAtLeast: 1,
        minFrames: 2,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        animNo: 5210,
        moveType: "I",
        observedHitFallRecoverTimeAtMost: 0,
        minFrames: 1,
      },
    ]);
  });

  it("creates a synthetic imported default Common1 recovery tick-order artifact", () => {
    const artifact = createSyntheticImportedDefaultFallRecoveryTickOrderTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-recovery-tick-order-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-default-fall-recovery-tick-order-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5210]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    const fallFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.animNo === 5050 && frame.moveType === "H",
    );
    const recoveryFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.animNo === 5210 && frame.moveType === "I",
    );
    expect(fallFrame?.maxHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(recoveryFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.lastTick).toBeLessThan(recoveryFrame?.firstTick ?? 0);
    expect(evidence?.controllerEvents.map((event) => event.controller)).toEqual(
      expect.arrayContaining(["VelAdd", "ChangeState", "VelSet", "HitFallSet"]),
    );
    const fallControllerEvents = evidence?.controllerEvents
      .filter((event) => event.actorId === "p2" && event.stateNo === 5050)
      .map((event) => event.controller);
    expect(fallControllerEvents).toEqual(expect.arrayContaining(["VelAdd", "ChangeState"]));
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "5050 positive recoverTime before 5210 recovery",
        steps: [
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            animNo: 5050,
            moveType: "H",
            observedHitFallRecoverTimeAtLeast: 1,
            observedHitFallRecoverTimeAtMost: 0,
            observedHitFallRecoverTimeDropAtLeast: 1,
            minFrames: 2,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            animNo: 5210,
            moveType: "I",
            observedHitFallRecoverTimeAtMost: 0,
            minFrames: 1,
          },
        ],
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5050 named recovery controller and typed operation order into 5210 settle",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 5050, controller: "VelAdd", name: "Gravity" },
          { stateNo: 5050, controller: "ChangeState", name: "Recovery Input" },
          { stateNo: 5210, controller: "VelSet", name: "Air Recovery Velocity" },
          { stateNo: 5210, operation: "kinematic:velset" },
          { stateNo: 5210, controller: "HitFallSet", name: "Fall Recovery Settled" },
          { stateNo: 5210, operation: "hitfall:hitfallset" },
          { stateNo: 5210, controller: "ChangeState", name: "Stand" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported default Common1 ground-recovery artifact", () => {
    const artifact = createSyntheticImportedDefaultFallGroundRecoveryTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-ground-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-ground-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5200, 5201, 52]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(1);
    const groundRecoveryFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.animNo === 5201,
    );
    expect(groundRecoveryFrame?.minVel.x).toBeLessThanOrEqual(-0.15);
    expect(groundRecoveryFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(groundRecoveryFrame?.minPos.y).toBeLessThanOrEqual(0);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        animNo: 5200,
        moveType: "H",
        minFrames: 1,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        animNo: 5201,
        moveType: "H",
        observedVelXAtMost: -0.15,
        observedVelYAtMost: -3.5,
        observedPosYAtMost: 0,
        minFrames: 1,
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "5050/5200/5201/52 named ground-recovery controller and typed operation order",
        actorId: "p2",
        allowSameTick: true,
        steps: [
          { stateNo: 5050, controller: "VelAdd", name: "Gravity" },
          { stateNo: 5050, controller: "ChangeState", name: "Ground Recovery Input" },
          { stateNo: 5200, controller: "VelAdd", name: "Gravity" },
          { stateNo: 5200, controller: "SelfState", name: "Self Land" },
          { stateNo: 5201, controller: "VelSet", name: "Ground Recovery Velocity" },
          { stateNo: 5201, controller: "PosSet", name: "Ground Recovery Position" },
          { stateNo: 5201, operation: "kinematic:posset" },
          { stateNo: 5201, controller: "NotHitBy", name: "Safe Recovery" },
          { stateNo: 5201, operation: "eligibility:nothitby" },
          { stateNo: 5201, controller: "ChangeState", name: "Land" },
          { stateNo: 52, controller: "VelSet", name: "Land Velocity" },
          { stateNo: 52, operation: "kinematic:velset" },
          { stateNo: 52, controller: "PosSet", name: "Land Position" },
          { stateNo: 52, operation: "kinematic:posset" },
          { stateNo: 52, controller: "CtrlSet", name: "Land Ctrl" },
          { stateNo: 52, operation: "resource:ctrlset" },
        ],
      },
    ]);
  });

  it("creates a synthetic imported default Common1 ground-recovery priority over air recovery artifact", () => {
    const artifact = createSyntheticImportedDefaultFallGroundRecoveryPriorityTraceArtifact({
      generatedAt: "2026-07-02T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-ground-recovery-priority-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-ground-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5200, 5201, 52]));
    expect(evidence?.executedStates).not.toEqual(expect.arrayContaining([5210, 5100, 5101, 5110, 5120]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:hitvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:velset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["resource:ctrlset"]).toBeGreaterThanOrEqual(1);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    const groundFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5200);
    const airFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5210);
    expect(fallFrame?.maxHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(groundFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.lastTick ?? 0).toBeLessThan(groundFrame?.firstTick ?? 0);
    expect(airFrame).toBeUndefined();
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([5210, 5100, 5101, 5110, 5120]);
    expect(gate?.requirements.requiredControllerEventSequences).toEqual([defaultGroundRecoveryControllerSequence()]);
    expect(gate?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "5050 positive-to-zero recoverTime before prioritized 5200 ground recovery",
        steps: [
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5050,
            moveType: "H",
            observedHitFallRecoverTimeAtLeast: 1,
            observedHitFallRecoverTimeAtMost: 0,
            observedHitFallRecoverTimeDropAtLeast: 1,
            minFrames: 2,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5200,
            moveType: "H",
            observedHitFallRecoverTimeAtMost: 0,
            minFrames: 1,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5201,
            moveType: "H",
            observedVelXAtMost: -0.15,
            observedVelYAtMost: -3.5,
            observedPosYAtMost: 0,
            minFrames: 1,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 52,
            moveType: "I",
            observedPosYAtLeast: 0,
            observedPosYAtMost: 0,
            minFrames: 1,
          },
        ],
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a synthetic imported default Common1 recovery-input too-early reject artifact", () => {
    const artifact = createSyntheticImportedDefaultFallRecoveryTooEarlyTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-recovery-too-early-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-default-fall-recovery-too-early-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050]));
    expect(evidence?.executedStates).not.toContain(5210);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.HitVelSet).toBeGreaterThanOrEqual(1);
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    expect(fallFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThan(fallFrame?.lastHitFallRecoverTime ?? Number.POSITIVE_INFINITY);
    expect(fallFrame?.frames).toBeGreaterThanOrEqual(2);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeMinAtLeast: 1,
        observedHitFallRecoverTimeDropAtLeast: 1,
        minFrames: 2,
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 5050,
      moveType: "H",
      ctrl: false,
    });
  });

  it("creates an imported default Common1 fall recovery artifact for official-style fixtures", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-official-recovery",
      displayName: "Synthetic Imported Default Fall Official Recovery",
      defaultGetHitFall: {
        shakeStateNo: 5000,
        slideStateNo: 5001,
        airStateNo: 5030,
        fallStateNo: 5050,
        groundStateNo: 5100,
        bounceStateNo: 5101,
        liedownStateNo: 5110,
        recoverStateNo: 5120,
        includeRecoveryChain: true,
      },
    });
    const artifact = createImportedDefaultFallRecoveryTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-fall-official-recovery-golden",
      targetLabel: "Synthetic imported official-style fall recovery route",
      requiredControllerEventSequences: [defaultFallLieDownGetUpControllerSequence()],
      requiredActorFrameSequences: [defaultFallLieDownGetUpActorFrameSequence()],
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-official-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5100, 5101, 5110, 5120]));
    expect(evidence?.executedControllers.HitFallSet).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      defaultFallLieDownGetUpControllerSequence(),
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      defaultFallLieDownGetUpActorFrameSequence(),
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("describes official KFM fall lie-down get-up controller evidence without relying on numeric controller names", () => {
    expect(officialKfmFallLieDownGetUpControllerSequence()).toEqual({
      label: "Official KFM 5110/5120 lie-down get-up controller and typed operation order",
      actorId: "p2",
      allowSameTick: true,
      steps: [
        { stateNo: 5110, controller: "HitFallDamage" },
        { stateNo: 5110, operation: "hitfall:hitfalldamage" },
        { stateNo: 5110, controller: "VelSet" },
        { stateNo: 5110, operation: "kinematic:velset" },
        { stateNo: 5120, controller: "VelSet" },
        { stateNo: 5120, operation: "kinematic:velset" },
        { stateNo: 5120, controller: "HitFallSet" },
        { stateNo: 5120, operation: "hitfall:hitfallset" },
        { stateNo: 5120, controller: "ChangeState" },
      ],
    });
  });

  it("creates an imported default Common1 recovery-input artifact for official-style fixtures", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-official-recovery-input",
      displayName: "Synthetic Imported Default Fall Official Recovery Input",
      defaultGetHitFall: {
        shakeStateNo: 5000,
        slideStateNo: 5001,
        airStateNo: 5030,
        fallStateNo: 5050,
        recoveryInputStateNo: 5210,
        includeRecoveryInput: true,
      },
    });
    const artifact = createImportedDefaultFallRecoveryInputTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-fall-official-recovery-input-golden",
      targetLabel: "Synthetic imported official-style recovery input route",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-official-recovery-input-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-recovery-input-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5210]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.VelAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates an imported default Common1 recovery-threshold artifact for official-style fixtures", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-official-recovery-threshold",
      displayName: "Synthetic Imported Default Fall Official Recovery Threshold",
      defaultGetHitFall: {
        shakeStateNo: 5000,
        slideStateNo: 5001,
        airStateNo: 5030,
        fallStateNo: 5050,
        recoveryInputStateNo: 5200,
        groundRecoveryStateNo: 5200,
        groundRecoveryLandStateNo: 5201,
        landStateNo: 52,
        includeGroundRecovery: true,
      },
    });
    const artifact = createImportedDefaultFallRecoveryThresholdTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-fall-official-recovery-threshold-golden",
      targetLabel: "Synthetic imported official-style recovery threshold route",
      attacker: createSyntheticImportedTraceFighter({
        id: "synthetic-imported-default-fall-official-recovery-threshold-attacker",
        displayName: "Synthetic Imported Default Fall Official Recovery Threshold Attacker",
        groundVelocity: [-3, -6],
        fall: { enabled: true, damage: 20, velocity: { x: 3, y: -6 }, recover: true, recoverTime: 10 },
      }),
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-official-recovery-threshold-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-ground-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5200, 5201, 52]));
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 5050);
    const recoveryFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 5200);
    expect(fallFrame).toBeDefined();
    expect(recoveryFrame).toBeDefined();
    if (!fallFrame || !recoveryFrame) {
      throw new Error("expected recovery-threshold actor-frame evidence");
    }
    expect(fallFrame?.maxHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(recoveryFrame?.minHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(fallFrame.lastTick).toBeLessThan(recoveryFrame.firstTick);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeAtMost: 0,
        observedHitFallRecoverTimeDropAtLeast: 1,
        minFrames: 2,
      },
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5200,
        moveType: "H",
        observedHitFallRecoverTimeAtMost: 0,
        minFrames: 1,
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "5050 positive recoverTime before 5200 ground recovery",
        steps: [
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5050,
            moveType: "H",
            observedHitFallRecoverTimeAtLeast: 1,
            observedHitFallRecoverTimeAtMost: 0,
            observedHitFallRecoverTimeDropAtLeast: 1,
            minFrames: 2,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5200,
            moveType: "H",
            observedHitFallRecoverTimeAtMost: 0,
            minFrames: 1,
          },
        ],
      },
    ]);
  });

  it("creates an imported default Common1 recovery-input too-early reject artifact for official-style fixtures", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-official-recovery-too-early",
      displayName: "Synthetic Imported Default Fall Official Recovery Too Early",
      defaultGetHitFall: {
        shakeStateNo: 5000,
        slideStateNo: 5001,
        airStateNo: 5030,
        fallStateNo: 5050,
        recoveryInputStateNo: 5210,
        includeRecoveryInput: true,
      },
    });
    const artifact = createImportedDefaultFallRecoveryTooEarlyTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-fall-official-recovery-too-early-golden",
      targetLabel: "Synthetic imported official-style recovery input too-early reject route",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-official-recovery-too-early-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-recovery-too-early-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050]));
    expect(evidence?.executedStates).not.toContain(5210);
    expect(evidence?.executedStates).not.toContain(5200);
    expect(evidence?.executedStates).not.toContain(5201);
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    const fallFrame = evidence?.actorFrames.find((frame) => frame.actorId === "p2" && frame.stateNo === 5050);
    expect(fallFrame?.minHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThan(fallFrame?.lastHitFallRecoverTime ?? Number.POSITIVE_INFINITY);
    expect(fallFrame?.frames).toBeGreaterThanOrEqual(2);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorId: "p2",
        source: "imported",
        actorKind: "player",
        stateNo: 5050,
        moveType: "H",
        observedHitFallRecoverTimeAtLeast: 1,
        observedHitFallRecoverTimeMinAtLeast: 1,
        observedHitFallRecoverTimeDropAtLeast: 1,
        minFrames: 2,
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 5050,
      moveType: "H",
      ctrl: false,
    });
  });

  it("creates an imported default Common1 ground-recovery artifact for official-style fixtures", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-official-ground-recovery",
      displayName: "Synthetic Imported Default Fall Official Ground Recovery",
      defaultGetHitFall: {
        shakeStateNo: 5000,
        slideStateNo: 5001,
        airStateNo: 5030,
        fallStateNo: 5050,
        groundRecoveryStateNo: 5200,
        groundRecoveryLandStateNo: 5201,
        landStateNo: 52,
        includeGroundRecovery: true,
      },
    });
    const artifact = createImportedDefaultFallGroundRecoveryTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-fall-official-ground-recovery-golden",
      targetLabel: "Synthetic imported official-style ground recovery route",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-official-ground-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-ground-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5200, 5201, 52]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VelSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("keeps ground-recovery priority constraints configurable for imported fixture mirrors", () => {
    const imported = createSyntheticImportedTraceFighter({
      id: "synthetic-imported-default-fall-ground-recovery-priority-options",
      displayName: "Synthetic Imported Default Fall Ground Recovery Priority Options",
      defaultGetHitFall: {
        shakeStateNo: 5000,
        slideStateNo: 5001,
        airStateNo: 5030,
        fallStateNo: 5050,
        groundRecoveryStateNo: 5200,
        groundRecoveryLandStateNo: 5201,
        landStateNo: 52,
        includeGroundRecovery: true,
      },
    });
    const artifact = createImportedDefaultFallGroundRecoveryTraceArtifact(imported, {
      generatedAt: "2026-06-25T00:00:00.000Z",
      targetId: "synthetic-imported-default-fall-ground-recovery-priority-options-golden",
      targetLabel: "Synthetic imported ground-recovery priority option route",
      forbiddenExecutedStates: [5210, 5100, 5101, 5110, 5120],
      requiredExecutedOperations: [
        "hitdef",
        "kinematic:hitvelset",
        "kinematic:posset",
        "kinematic:velset",
        "resource:ctrlset",
      ],
      requiredControllerEventSequences: [defaultGroundRecoveryControllerSequence()],
    });

    expect(artifact.status).toBe("passed");
    const gate = artifact.gates[0];
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([5210, 5100, 5101, 5110, 5120]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "kinematic:hitvelset",
      "kinematic:posset",
      "kinematic:velset",
      "resource:ctrlset",
    ]);
    expect(gate?.evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5200, 5201, 52]));
    expect(gate?.evidence?.executedStates).not.toEqual(expect.arrayContaining([5210, 5100, 5101, 5110, 5120]));
    expect(gate?.evidence?.executedOperations).toMatchObject({
      hitdef: expect.any(Number),
      "kinematic:hitvelset": expect.any(Number),
      "kinematic:posset": expect.any(Number),
      "kinematic:velset": expect.any(Number),
      "resource:ctrlset": expect.any(Number),
    });
  });

  it("creates a required synthetic imported official-style Common1 ground-recovery sequence artifact", () => {
    const artifact = createSyntheticImportedDefaultFallOfficialGroundRecoveryTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-fall-official-ground-recovery-golden",
        source: "imported",
      },
      gates: [
        {
          label: "imported-default-fall-ground-recovery-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 5030, 5050, 5200, 5201, 52]));
    expect(evidence?.activeCommands).toEqual(expect.arrayContaining(["x", "recovery"]));
    const fallFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 5050,
    );
    const groundFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 5201,
    );
    const landFrame = evidence?.actorFrames.find(
      (frame) => frame.actorId === "p2" && frame.source === "imported" && frame.stateNo === 52,
    );
    expect(fallFrame?.firstHitFallRecoverTime).toBeGreaterThanOrEqual(1);
    expect(fallFrame?.lastHitFallRecoverTime).toBeLessThanOrEqual(0);
    expect(groundFrame?.minVel.x).toBeLessThanOrEqual(-0.15);
    expect(groundFrame?.minVel.y).toBeLessThanOrEqual(-3.5);
    expect(landFrame?.maxPos.y).toBe(0);
    expect(artifact.gates[0]?.requirements.requiredActorFrameSequences).toEqual([
      {
        label: "5050 recoverTime countdown before 5200/5201/52 ground recovery",
        steps: [
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5050,
            moveType: "H",
            observedHitFallRecoverTimeAtLeast: 1,
            observedHitFallRecoverTimeAtMost: 0,
            observedHitFallRecoverTimeDropAtLeast: 1,
            minFrames: 2,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5200,
            moveType: "H",
            observedHitFallRecoverTimeAtMost: 0,
            minFrames: 1,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 5201,
            moveType: "H",
            observedVelXAtMost: -0.15,
            observedVelYAtMost: -3.5,
            observedPosYAtMost: 0,
            minFrames: 1,
          },
          {
            actorId: "p2",
            source: "imported",
            actorKind: "player",
            stateNo: 52,
            moveType: "I",
            observedPosYAtLeast: 0,
            observedPosYAtMost: 0,
            minFrames: 1,
          },
        ],
      },
    ]);
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      source: "imported",
      stateNo: 0,
      moveType: "I",
      ctrl: true,
    });
  });

  it("creates a synthetic imported state-exit artifact with final idle/control evidence", () => {
    const artifact = createSyntheticImportedStateExitTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-state-exit-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "imported-state-exit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const finalActor = artifact.gates[0]?.evidence.finalActors.find((actor) => actor.id === "p1");
    expect(finalActor).toMatchObject({
      source: "imported",
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
    expect(artifact.trace.finalActors[0]).toMatchObject({
      source: "imported",
      stateNo: 0,
      animNo: 0,
      ctrl: true,
    });
    expect(artifact.gates[0]?.evidence.eventCategories).toContain("hit");
    expect(artifact.gates[0]?.evidence.combatReasons).toContain("hit");
  });

  it("creates a synthetic imported target artifact with typed Target* operation evidence", () => {
    const artifact = createSyntheticImportedTargetTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-target-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.TargetLifeAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetDrop).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetlifeadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetpoweradd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetveladd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetfacing"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetbind"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetdrop"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 }),
        expect.objectContaining({
          ownerId: "p1",
          actorId: "p2",
          targetId: 77,
          hasBinding: true,
          minBindingRemaining: 3,
          maxBindingRemaining: 3,
          bindingOffset: { x: 36, y: -12 },
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { actorId: "p2", actorKind: "player", facing: 1, observedVelXAtLeast: 0.8, observedVelYAtMost: -3 },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p2",
          actorKind: "player",
          facing: 1,
          minVel: expect.objectContaining({ y: expect.any(Number) }),
          maxVel: expect.objectContaining({ x: expect.any(Number) }),
        }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      targetCount: 0,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 943,
      power: 40,
    });
    expect(artifact.trace.finalActors[0]?.actorKind).toBe("player");
  });

  it("creates a synthetic imported Target redirect artifact with target-memory trigger evidence", () => {
    const artifact = createSyntheticImportedTargetRedirectTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-redirect-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-target-redirect-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 286]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 286,
      animNo: 286,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 963,
    });
  });

  it("creates a synthetic imported Target redirect bottom artifact with forbidden branch evidence", () => {
    const artifact = createSyntheticImportedTargetRedirectBottomTraceArtifact({ generatedAt: "2026-07-03T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-redirect-bottom-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-target-redirect-bottom-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 397]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([396]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 397]));
    expect(evidence?.executedStates).not.toContain(396);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 397,
      animNo: 397,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 963,
    });
  });

  it("creates a synthetic imported Target Cond bottom artifact with lazy branch evidence", () => {
    const artifact = createSyntheticImportedTargetCondBottomTraceArtifact({ generatedAt: "2026-07-03T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-cond-bottom-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-target-cond-bottom-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 399]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([398]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 399]));
    expect(evidence?.executedStates).not.toContain(398);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 399,
      animNo: 399,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 963,
    });
  });

  it("creates a synthetic imported Target IfElse bottom artifact with eager branch-result isolation evidence", () => {
    const artifact = createSyntheticImportedTargetIfElseBottomTraceArtifact({ generatedAt: "2026-07-03T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-ifelse-bottom-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-target-ifelse-bottom-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 401]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([400]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 401]));
    expect(evidence?.executedStates).not.toContain(400);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 401,
      animNo: 401,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 963,
    });
  });

  it("creates a synthetic imported default Target redirect artifact with target id 0 evidence", () => {
    const artifact = createSyntheticImportedDefaultTargetRedirectTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-default-target-redirect-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-default-target-redirect-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 269]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 269]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 0 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 269,
      animNo: 269,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 963,
    });
  });

  it("creates a synthetic imported bare Target redirect artifact with current target evidence", () => {
    const artifact = createSyntheticImportedBareTargetRedirectTraceArtifact({ generatedAt: "2026-06-30T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-bare-target-redirect-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-bare-target-redirect-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 270]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 270]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 270,
      animNo: 270,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 963,
    });
  });

  it("creates a synthetic imported dynamic Target redirect artifact with variable-index trigger evidence", () => {
    const artifact = createSyntheticImportedTargetDynamicRedirectTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-dynamic-redirect-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-target-dynamic-redirect-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 287]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 287,
      animNo: 287,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 963,
    });
  });

  it("creates a synthetic imported TargetLifeAdd NoKO artifact", () => {
    const artifact = createSyntheticImportedTargetNoKoTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-target-noko-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-target-noko-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.AssertSpecial).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetLifeAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetlifeadd"]).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredControllerEventSequences).toEqual([
      {
        label: "NoKO defender AssertSpecial before lethal TargetLifeAdd",
        allowSameTick: true,
        steps: [
          { actorId: "p2", stateNo: 0, controller: "AssertSpecial", name: "Passive AssertSpecial" },
          { actorId: "p1", stateNo: 200, controller: "HitDef" },
          { actorId: "p1", stateNo: 200, controller: "TargetLifeAdd", name: "Target Damage" },
        ],
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      source: "imported",
      life: 1,
    });
  });

  it("creates a synthetic imported TargetState artifact with owner-backed SelfState evidence", () => {
    const artifact = createSyntheticImportedTargetStateCustomTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-targetstate-custom-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-targetstate-custom-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 889]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetstate"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 889, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      customOwnerId: undefined,
      stateNo: 0,
      animNo: 0,
      ctrl: true,
      moveType: "I",
    });
  });

  it("creates a synthetic imported BindToTarget Head artifact with parsed Size anchor evidence", () => {
    const artifact = createSyntheticImportedBindToTargetHeadTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-bindtotarget-head-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-bindtotarget-head-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.BindToTarget).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.bindtotarget).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          actorId: "p2",
          targetId: 77,
          hasBinding: true,
          minBindingRemaining: 1,
          maxBindingRemaining: 3,
          bindingOffset: { x: 26, y: -80 },
        }),
      ]),
    );
  });

  it("creates a synthetic imported BindToTarget Mid artifact with parsed Size anchor evidence", () => {
    const artifact = createSyntheticImportedBindToTargetMidTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-bindtotarget-mid-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-bindtotarget-mid-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.BindToTarget).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.bindtotarget).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          actorId: "p2",
          targetId: 77,
          hasBinding: true,
          minBindingRemaining: 1,
          maxBindingRemaining: 3,
          bindingOffset: { x: 24, y: -50 },
        }),
      ]),
    );
  });

  it("creates a synthetic imported TargetBind pause artifact with movetime evidence", () => {
    const artifact = createSyntheticImportedTargetBindPauseTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-targetbind-pause-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-targetbind-pause-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.TargetBind).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetbind"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          actorId: "p2",
          targetId: 77,
          hasBinding: true,
          frames: expect.any(Number),
          minBindingRemaining: 1,
          maxBindingRemaining: 3,
          bindingOffset: { x: 36, y: -12 },
        }),
      ]),
    );
    expect(evidence?.targetLinks.find((link) => link.hasBinding)?.frames).toBeGreaterThanOrEqual(2);
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([expect.objectContaining({ type: "SuperPause", actorId: "p1" })]),
    );
  });

  it("creates a synthetic imported SuperPause artifact with typed pause operation evidence", () => {
    const artifact = createSyntheticImportedSuperPauseTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("pause");
    expect(artifact.gates[0]?.requirements.requiredMatchPauses).toEqual([
      { type: "SuperPause", actorId: "p1", sourceStateNo: 200, darken: true, minFrames: 2, minRemaining: 7, minMoveTime: 1 },
    ]);
    expect(artifact.gates[0]?.requirements.requiredMatchPauseFreezes).toEqual([
      { type: "SuperPause", actorId: "p2", minFrozenFrames: 6 },
    ]);
    expect(evidence?.matchPauses).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorId: "p1",
          sourceStateNo: 200,
          darken: true,
          maxRemaining: 7,
          maxMoveTime: 1,
        }),
      ]),
    );
    expect(evidence?.matchPauses[0]?.frames).toBeGreaterThanOrEqual(2);
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorId: "p2",
          actorKind: "player",
          ownerId: "p2",
          frozenFrames: expect.any(Number),
        }),
      ]),
    );
    expect(evidence?.matchPauseFreezes.find((freeze) => freeze.actorId === "p2")?.frozenFrames).toBeGreaterThanOrEqual(6);
  });

  it("creates a synthetic imported SuperPause artifact with default FightFX anim evidence", () => {
    const artifact = createSyntheticImportedSuperPauseDefaultAnimTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-default-anim-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-default-anim-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "pause:superpause"]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      {
        type: "SuperPause",
        actorId: "p1",
        sourceStateNo: 200,
        darken: true,
        minFrames: 2,
        minRemaining: 7,
        minMoveTime: 1,
        superAnimRaw: "30",
        superAnimSource: "fightfx",
        superAnimActionNo: 30,
        superAnimOffsetX: 0,
        superAnimOffsetY: 0,
      },
    ]);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.matchPauses).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorId: "p1",
          sourceStateNo: 200,
          darken: true,
          maxRemaining: 7,
          maxMoveTime: 1,
          superAnim: {
            raw: "30",
            source: "fightfx",
            actionNo: 30,
            offset: { x: 0, y: 0 },
          },
        }),
      ]),
    );
  });

  it("creates a synthetic imported SuperPause artifact with disabled anim evidence", () => {
    const artifact = createSyntheticImportedSuperPauseAnimDisabledTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-anim-disabled-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-anim-disabled-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "pause:superpause"]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      {
        type: "SuperPause",
        actorId: "p1",
        sourceStateNo: 200,
        darken: true,
        minFrames: 2,
        minRemaining: 7,
        minMoveTime: 1,
        superAnimAbsent: true,
      },
    ]);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    const disabledPause = evidence?.matchPauses.find(
      (pause) => pause.type === "SuperPause" && pause.actorId === "p1" && pause.sourceStateNo === 200,
    );
    expect(disabledPause).toEqual(expect.objectContaining({ darken: true, maxRemaining: 7, maxMoveTime: 1 }));
    expect(disabledPause?.superAnim).toBeUndefined();
  });

  it("creates a synthetic imported SuperPause artifact with pausebg evidence", () => {
    const artifact = createSyntheticImportedSuperPausePauseBgTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-pausebg-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-pausebg-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "pause:superpause"]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      {
        type: "SuperPause",
        actorId: "p1",
        sourceStateNo: 200,
        pauseBg: false,
        darken: true,
        minFrames: 2,
        minRemaining: 7,
        minMoveTime: 1,
      },
    ]);
    const pauseBgPause = evidence?.matchPauses.find(
      (pause) => pause.type === "SuperPause" && pause.actorId === "p1" && pause.sourceStateNo === 200,
    );
    expect(pauseBgPause).toEqual(expect.objectContaining({ pauseBg: false, darken: true, maxRemaining: 7, maxMoveTime: 1 }));
  });

  it("creates a synthetic imported SuperPause artifact with unhittable rejection evidence", () => {
    const artifact = createSyntheticImportedSuperPauseUnhittableTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-unhittable-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-unhittable-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["pause:superpause"]);
    expect(gate?.requirements.requiredEventSubstrings).toEqual(["via SuperPause unhittable"]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      {
        type: "SuperPause",
        actorId: "p1",
        sourceStateNo: 200,
        unhittable: true,
        darken: true,
        minFrames: 2,
        minRemaining: 7,
        minMoveTime: 1,
      },
    ]);
    expect(evidence?.eventCategories).toEqual(expect.arrayContaining(["pause", "reject"]));
    expect(evidence?.combatReasons).toContain("reject");
    expect(evidence?.matchPauses).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "SuperPause", actorId: "p1", sourceStateNo: 200, darken: true, maxRemaining: 7 }),
      ]),
    );
    expect(artifact.trace.events.some((event) => event.line.includes("via SuperPause unhittable"))).toBe(true);
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")).toMatchObject({ source: "imported", life: 1000 });
  });

  it("creates a synthetic imported SuperPause sound artifact with dynamic sound and typed audio evidence", () => {
    const artifact = createSyntheticImportedSuperPauseSoundTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-sound-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-sound-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "pause:superpause", "audio:playsnd"]);
    expect(gate?.requirements.requiredSoundEvents).toEqual([
      { actorId: "p1", type: "PlaySnd", group: 10, index: 0, raw: "Svar(0),var(1)", stateNo: 200 },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", type: "PlaySnd", group: 10, index: 0, raw: "Svar(0),var(1)", stateNo: 200 }),
      ]),
    );
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.soundEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "PlaySnd", group: 10, index: 0, raw: "Svar(0),var(1)", stateNo: 200 }),
      ]),
    );
  });

  it("creates a synthetic imported SuperPause artifact with dynamic param evidence", () => {
    const artifact = createSyntheticImportedSuperPauseDynamicParamsTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-dynamic-params-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-dynamic-params-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "pause:superpause"]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      { type: "SuperPause", actorId: "p1", sourceStateNo: 200, darken: false, minFrames: 3, minRemaining: 9, minMoveTime: 2 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "SuperPause", actorId: "p1", minAdvancedFrames: 2, minPreviousMoveTime: 2 },
    ]);
    expect(gate?.requirements.requiredFinalActors).toEqual([
      expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", power: 75 }),
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(4);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.matchPauses).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorId: "p1",
          sourceStateNo: 200,
          darken: false,
          maxRemaining: 9,
          maxMoveTime: 2,
        }),
      ]),
    );
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "SuperPause", actorId: "p1", advancedFrames: expect.any(Number), maxPreviousMoveTime: 2 }),
      ]),
    );
    expect(evidence?.matchPauseAdvances.find((advance) => advance.actorId === "p1")?.advancedFrames).toBeGreaterThanOrEqual(2);
    expect(evidence?.finalActors).toEqual(expect.arrayContaining([expect.objectContaining({ id: "p1", power: 75 })]));
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p1")?.power).toBe(75);
  });

  it("creates a synthetic imported SuperPause artifact with anim/pos evidence", () => {
    const artifact = createSyntheticImportedSuperPauseAnimPosTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-anim-pos-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-anim-pos-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "pause:superpause"]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      {
        type: "SuperPause",
        actorId: "p1",
        sourceStateNo: 200,
        darken: true,
        minFrames: 2,
        minRemaining: 7,
        minMoveTime: 1,
        superAnimRaw: "S200",
        superAnimSource: "player",
        superAnimActionNo: 200,
        superAnimOffsetX: 24,
        superAnimOffsetY: -48,
      },
    ]);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.matchPauses).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorId: "p1",
          sourceStateNo: 200,
          darken: true,
          maxRemaining: 7,
          maxMoveTime: 1,
          superAnim: {
            raw: "S200",
            source: "player",
            actionNo: 200,
            offset: { x: 24, y: -48 },
          },
        }),
      ]),
    );
  });

  it("creates a synthetic imported SuperPause artifact with dynamic anim/pos evidence", () => {
    const artifact = createSyntheticImportedSuperPauseDynamicAnimPosTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-dynamic-anim-pos-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-dynamic-anim-pos-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "VarSet", "HitDef", "SuperPause"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["variable:varset", "hitdef", "pause:superpause"]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      {
        type: "SuperPause",
        actorId: "p1",
        sourceStateNo: 200,
        darken: true,
        minFrames: 2,
        minRemaining: 7,
        minMoveTime: 1,
        superAnimRaw: "var(6)",
        superAnimSource: "fightfx",
        superAnimActionNo: 7001,
        superAnimOffsetX: 18,
        superAnimOffsetY: -36,
      },
    ]);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(3);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.matchPauses).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorId: "p1",
          sourceStateNo: 200,
          darken: true,
          maxRemaining: 7,
          maxMoveTime: 1,
          superAnim: {
            raw: "var(6)",
            source: "fightfx",
            actionNo: 7001,
            offset: { x: 18, y: -36 },
          },
        }),
      ]),
    );
  });

  it("creates a synthetic imported SuperPause p2defmul artifact with target damage-scale evidence", () => {
    const artifact = createSyntheticImportedSuperPauseP2DefMulTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-p2defmul-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-p2defmul-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "SuperPause", "TargetLifeAdd"]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "pause:superpause", "target:targetlifeadd"]);
    expect(gate?.requirements.requiredFinalActors).toEqual([
      expect.objectContaining({ actorId: "p2", source: "imported", actorKind: "player", life: 953 }),
    ]);
    expect(gate?.requirements.requiredMatchPauseFreezes).toEqual([{ type: "SuperPause", actorId: "p2", minFrozenFrames: 5 }]);
    expect(evidence?.executedControllers.SuperPause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetLifeAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetlifeadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.finalActors).toEqual(expect.arrayContaining([expect.objectContaining({ id: "p2", life: 953 })]));
    expect(artifact.trace.finalActors.find((actor) => actor.id === "p2")?.life).toBe(953);
  });

  it("creates a synthetic imported SuperPause artifact that proves projectile freeze evidence", () => {
    const artifact = createSyntheticImportedSuperPauseProjectileFreezeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-projectile-freeze-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-projectile-freeze-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredEffectKinds).toEqual(["projectile"]);
    expect(gate?.requirements.requiredMatchPauseFreezes).toEqual([
      { type: "SuperPause", actorKind: "player", ownerId: "p2", minFrozenFrames: 6 },
      { type: "SuperPause", actorKind: "projectile", ownerId: "p1", minFrozenFrames: 5 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "SuperPause", actorKind: "projectile", ownerId: "p1", minAdvancedFrames: 1, minPreviousMoveTime: 1 },
    ]);
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorKind: "projectile",
          ownerId: "p1",
          frozenFrames: expect.any(Number),
        }),
      ]),
    );
    expect(evidence?.matchPauseFreezes.find((freeze) => freeze.actorKind === "projectile" && freeze.ownerId === "p1")?.frozenFrames).toBeGreaterThanOrEqual(5);
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorKind: "projectile",
          ownerId: "p1",
          advancedFrames: expect.any(Number),
          maxPreviousMoveTime: 1,
          changedFields: expect.arrayContaining(["animTime", "pos"]),
        }),
      ]),
    );
    expect(evidence?.matchPauseAdvances.find((advance) => advance.actorKind === "projectile" && advance.ownerId === "p1")?.advancedFrames).toBeGreaterThanOrEqual(1);
  });

  it("creates a synthetic imported SuperPause artifact that proves helper and explod freeze evidence", () => {
    const artifact = createSyntheticImportedSuperPauseEffectFreezeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-superpause-effect-freeze-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-superpause-effect-freeze-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredEffectKinds).toEqual(["helper", "explod"]);
    expect(gate?.requirements.requiredMatchPauseFreezes).toEqual([
      { type: "SuperPause", actorKind: "player", ownerId: "p2", minFrozenFrames: 6 },
      { type: "SuperPause", actorKind: "helper", ownerId: "p1", minFrozenFrames: 5 },
      { type: "SuperPause", actorKind: "explod", ownerId: "p1", minFrozenFrames: 5 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "SuperPause", actorKind: "helper", ownerId: "p1", minAdvancedFrames: 1, minPreviousMoveTime: 1 },
      { type: "SuperPause", actorKind: "explod", ownerId: "p1", minAdvancedFrames: 1, minPreviousMoveTime: 1 },
    ]);
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "explod"]));
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["pause:superpause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "SuperPause", actorKind: "helper", ownerId: "p1" }),
        expect.objectContaining({ type: "SuperPause", actorKind: "explod", ownerId: "p1" }),
      ]),
    );
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorKind: "helper",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime"]),
        }),
        expect.objectContaining({
          type: "SuperPause",
          actorKind: "explod",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime"]),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Explod supermovetime artifact with freeze and advance evidence", () => {
    const artifact = createSyntheticImportedExplodSuperMoveTimeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-supermovetime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-supermovetime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "SuperPause", { type: "Explod", minCount: 2 }]);
    expect(gate?.requirements.requiredMatchPauseFreezes).toEqual([
      { type: "SuperPause", actorId: "p1-explod-0", actorKind: "explod", ownerId: "p1", minFrozenFrames: 5 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "SuperPause", actorId: "p1-explod-1", actorKind: "explod", ownerId: "p1", minAdvancedFrames: 3, minPreviousMoveTime: 0 },
    ]);
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(2);
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "SuperPause", actorId: "p1-explod-0", actorKind: "explod", ownerId: "p1" }),
      ]),
    );
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorId: "p1-explod-1",
          actorKind: "explod",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime", "pos"]),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Explod pausemovetime artifact with freeze and advance evidence", () => {
    const artifact = createSyntheticImportedExplodPauseMoveTimeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-pausemovetime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-pausemovetime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", "Pause", { type: "Explod", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", "pause:pause", { operation: "explod", minCount: 2 }]);
    expect(gate?.requirements.requiredMatchPauses).toEqual([
      { type: "Pause", actorId: "p1", sourceStateNo: 200, darken: false, minFrames: 2, minRemaining: 7, minMoveTime: 1 },
    ]);
    expect(gate?.requirements.requiredMatchPauseFreezes).toEqual([
      { type: "Pause", actorId: "p1-explod-0", actorKind: "explod", ownerId: "p1", minFrozenFrames: 5 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "Pause", actorId: "p1-explod-1", actorKind: "explod", ownerId: "p1", minAdvancedFrames: 3, minPreviousMoveTime: 0 },
    ]);
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Pause).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations["pause:pause"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(2);
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "Pause", actorId: "p1-explod-0", actorKind: "explod", ownerId: "p1" }),
      ]),
    );
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "Pause",
          actorId: "p1-explod-1",
          actorKind: "explod",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime", "pos"]),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Explod ignorehitpause artifact with hitpause freeze and advance evidence", () => {
    const artifact = createSyntheticImportedExplodIgnoreHitPauseTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-ignorehitpause-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-ignorehitpause-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Explod", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "explod", minCount: 2 }]);
    expect(gate?.requirements.requiredMatchPauseFreezes).toEqual([
      { type: "HitPause", actorId: "p1-explod-0", actorKind: "explod", ownerId: "p1", minFrozenFrames: 3 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      {
        type: "HitPause",
        actorId: "p1-explod-1",
        actorKind: "explod",
        ownerId: "p1",
        minAdvancedFrames: 3,
        minPreviousMoveTime: 1,
      },
    ]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(2);
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "HitPause", actorId: "p1-explod-0", actorKind: "explod", ownerId: "p1" }),
      ]),
    );
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "HitPause",
          actorId: "p1-explod-1",
          actorKind: "explod",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime", "pos"]),
        }),
      ]),
    );
  });

  it("creates a synthetic imported HitPauseTime ignorehitpause artifact with active-state branch evidence", () => {
    const artifact = createSyntheticImportedHitPauseTimeIgnoreHitPauseTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitpausetime-ignorehitpause-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-hitpausetime-ignorehitpause-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedControllers).toEqual([{ type: "ChangeState", minCount: 2 }, "HitDef"]);
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 220]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "HitPause", actorId: "p1", actorKind: "player", ownerId: "p1", minAdvancedFrames: 1, minPreviousMoveTime: 1 },
    ]);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(2);
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 220]));
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "HitPause",
          actorId: "p1",
          actorKind: "player",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["stateNo", "animNo"]),
        }),
      ]),
    );
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([expect.objectContaining({ type: "HitPause", actorId: "p2", actorKind: "player", ownerId: "p2" })]),
    );
  });

  it("creates a synthetic imported Projectile artifact with typed projectile operation evidence", () => {
    const artifact = createSyntheticImportedProjectileTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toContain(270);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 911, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      { source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 911, moveType: "I", clsn1Count: 0 },
    ]);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 270]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredEffectStores).toEqual([
      { ownerId: "p1", minTotal: 1, minProjectiles: 1, minNextProjectileSerial: 1 },
    ]);
    expect(evidence?.effectStores).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          total: 1,
          projectiles: [expect.stringContaining("projectile")],
          nextSerials: expect.objectContaining({ projectile: 1 }),
        }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile HitCount artifact with owner branch evidence", () => {
    const artifact = createSyntheticImportedProjectileHitCountTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hitcount-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hitcount-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 341]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["projectile", "audio:playsnd"]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 44, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7002,
            raw: "F7002",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7002,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -68 },
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "hit",
        sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 44, contactKind: "hit" }),
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7002, offsetX: 18, offsetY: -68 }),
      }),
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 911, moveType: "I" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1-projectile-0", effect: expect.objectContaining({ id: 77, hasHit: true }) }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      source: "imported",
      stateNo: 341,
    });
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 341]);
  });

  it("creates a synthetic imported Projectile Target redirect artifact with owner target-memory branch evidence", () => {
    const artifact = createSyntheticImportedProjectileTargetRedirectTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-target-redirect-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-target-redirect-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 277]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", source: "imported", actorKind: "player", stateNo: 277, animNo: 277 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 911 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", minLife: 969, maxLife: 969 }),
      ]),
    );
    expect(gate?.requirements.requiredTargetLinks).toEqual([{ ownerId: "p1", actorId: "p2", targetId: 77 }]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", actorKind: "player", life: 969 })]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "hit", terminalReason: "hit" }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported HitDef plus Projectile target mix artifact with distinct target ids", () => {
    const artifact = createSyntheticImportedHitDefProjectileTargetMixTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-hitdef-projectile-target-mix-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-hitdef-projectile-target-mix-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 278]);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 278]));
    expect(evidence?.executedControllers.HitDef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 }),
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 78 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          effect: expect.objectContaining({
            kind: "projectile",
            id: 78,
            hasHit: true,
            removalReason: "hit",
            terminalReason: "hit",
          }),
        }),
      ]),
    );
    expect(evidence?.finalActors.find((actor) => actor.id === "p1")).toMatchObject({
      actorKind: "player",
      source: "imported",
      stateNo: 278,
      animNo: 278,
    });
    expect(evidence?.finalActors.find((actor) => actor.id === "p2")).toMatchObject({
      actorKind: "player",
      life: 932,
    });
  });

  it("creates a synthetic imported Projectile Target controllers artifact with owner target side-effect evidence", () => {
    const artifact = createSyntheticImportedProjectileTargetControllersTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-target-controllers-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-target-controllers-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef ?? 0).toBe(0);
    expect(evidence?.executedControllers.TargetLifeAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetDrop).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetlifeadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetpoweradd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetveladd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetfacing"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetbind"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetdrop"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 912 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", facing: 1, minLife: 949, maxLife: 949 }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77, hasBinding: false }),
        expect.objectContaining({
          ownerId: "p1",
          actorId: "p2",
          targetId: 77,
          hasBinding: true,
          bindingOffset: { x: 36, y: -12 },
        }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ id: "p1", actorKind: "player", source: "imported", targetCount: 0 }),
        expect.objectContaining({ id: "p2", actorKind: "player", life: 949, power: 40 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile default Target controllers artifact with target id zero side-effect evidence", () => {
    const artifact = createSyntheticImportedProjectileDefaultTargetControllersTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-default-target-controllers-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-default-target-controllers-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef ?? 0).toBe(0);
    expect(evidence?.executedControllers.TargetLifeAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.TargetDrop).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetlifeadd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetpoweradd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetvelset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetveladd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetfacing"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetbind"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetdrop"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 915 }),
        expect.objectContaining({ actorId: "p2", actorKind: "player", facing: 1, minLife: 949, maxLife: 949 }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 0, hasBinding: false }),
        expect.objectContaining({
          ownerId: "p1",
          actorId: "p2",
          targetId: 0,
          hasBinding: true,
          bindingOffset: { x: 36, y: -12 },
        }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ id: "p1", actorKind: "player", source: "imported", targetCount: 0 }),
        expect.objectContaining({ id: "p2", actorKind: "player", life: 949, power: 40 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 0, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile TargetState artifact with owner-backed custom-state evidence", () => {
    const artifact = createSyntheticImportedProjectileTargetStateTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-targetstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-targetstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 889]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef ?? 0).toBe(0);
    expect(evidence?.executedControllers.TargetState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.SelfState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetstate"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 913 }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 889, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ id: "p2", customOwnerId: undefined, stateNo: 0, ctrl: true, moveType: "I", life: 969 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile default TargetState artifact with target id zero evidence", () => {
    const artifact = createSyntheticImportedProjectileDefaultTargetStateTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-default-targetstate-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-default-targetstate-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 888, 889]));
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef ?? 0).toBe(0);
    expect(evidence?.executedControllers.TargetState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["target:targetstate"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.targetLinks).toEqual(expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 0 })]));
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 914 }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 888, moveType: "H" }),
        expect.objectContaining({ actorId: "p2", customOwnerId: "p1", animNo: 889, moveType: "H" }),
      ]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ id: "p2", customOwnerId: undefined, stateNo: 0, ctrl: true, moveType: "I", life: 969 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          parentId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 0, hitsRemaining: 0, hasHit: true }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ReceivedDamage artifact with defender-local branch evidence", () => {
    const artifact = createSyntheticImportedProjectileReceivedDamageTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-receiveddamage-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-receiveddamage-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 5000, 280]));
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 5000, 280]);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.targetLinks.some((link) => link.ownerId === "p1" && link.actorId === "p2" && link.targetId === 77)).toBe(true);
    expect(artifact.trace.finalActors.some((actor) => actor.id === "p2" && actor.stateNo === 280)).toBe(true);
  });

  it("creates a synthetic imported Projectile time artifact with ProjHitTime branch evidence", () => {
    const artifact = createSyntheticImportedProjectileTimeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-time-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-time-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toContain(276);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 276]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile hit-time artifact with any-projectile branch evidence", () => {
    const artifact = createSyntheticImportedProjectileHitTimeAnyTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });
    const evidence = artifact.gates[0]?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-hittime-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-hittime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toContain(282);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 282]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile contact-time artifact with any-projectile branch evidence", () => {
    const artifact = createSyntheticImportedProjectileContactTimeAnyTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });
    const evidence = artifact.gates[0]?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-contacttime-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-contacttime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toContain(281);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 281]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile contact-time artifact with fixed-id branch evidence", () => {
    const artifact = createSyntheticImportedProjectileContactTimeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });
    const evidence = artifact.gates[0]?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-contacttime-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-contacttime-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toContain(321);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 321]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile ProjContactPersist artifact with owner-state transition evidence", () => {
    const artifact = createSyntheticImportedProjectileContactPersistTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projcontactpersist-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projcontactpersist-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 348, 349]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 348, animNo: 1070, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 349, animNo: 1071, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", parentId: "p1" }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8896,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8896 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 15, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7022,
            raw: "F7022",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7022,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 16, y: -58 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjContact suffix artifact with second-form trigger evidence", () => {
    const artifact = createSyntheticImportedProjectileContactSuffixTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projcontact-suffix-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projcontact-suffix-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 350, 351]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 350, animNo: 1072, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 351, animNo: 1073, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8897,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8897 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 16, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7023,
            raw: "F7023",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7023,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 17, y: -59 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjContact any-id suffix artifact with omitted and zero-id evidence", () => {
    const artifact = createSyntheticImportedProjectileContactSuffixAnyTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projcontact-suffix-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projcontact-suffix-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 360, 361]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 360, animNo: 1082, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 361, animNo: 1083, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8902,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8902 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 21, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7028,
            raw: "F7028",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7028,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 22, y: -64 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjContact multi-id artifact with wrong-id trap evidence", () => {
    const artifact = createSyntheticImportedProjectileContactMultiIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projcontact-multi-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projcontact-multi-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 362, 363]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([364]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 2, minProjectiles: 2, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 362, animNo: 1084, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 363, animNo: 1085, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8903,
        minAge: 1,
        minPriority: 1,
        minHitsRemaining: 1,
        hasHit: false,
      },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8904,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", id: "p1-projectile-0", ownerId: "p1" }),
        expect.objectContaining({ type: "spawn", id: "p1-projectile-1", ownerId: "p1" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-0", ownerId: "p1" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-1", ownerId: "p1" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8904 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 22, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7029,
            raw: "F7029",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7029,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 23, y: -65 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjHit multi-id artifact with wrong-id trap evidence", () => {
    const artifact = createSyntheticImportedProjectileHitMultiIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projhit-multi-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projhit-multi-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 365, 366]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([367]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 2, minProjectiles: 2, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 365, animNo: 1086, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 366, animNo: 1087, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8905,
        minAge: 1,
        minPriority: 1,
        minHitsRemaining: 1,
        hasHit: false,
      },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8906,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", id: "p1-projectile-0", ownerId: "p1" }),
        expect.objectContaining({ type: "spawn", id: "p1-projectile-1", ownerId: "p1" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-0", ownerId: "p1" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-1", ownerId: "p1" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8906 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 23, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7030,
            raw: "F7030",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7030,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 24, y: -66 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjGuarded multi-id artifact with wrong-id trap evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardedMultiIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projguarded-multi-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projguarded-multi-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 368, 369]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([370]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 2, minProjectiles: 2, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 368, animNo: 1088, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 369, animNo: 1089, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8907,
        minAge: 1,
        minPriority: 1,
        minHitsRemaining: 1,
        hasHit: false,
      },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8908,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", id: "p1-projectile-0", ownerId: "p1" }),
        expect.objectContaining({ type: "spawn", id: "p1-projectile-1", ownerId: "p1" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-0", ownerId: "p1" }),
        expect.objectContaining({ type: "active", id: "p1-projectile-1", ownerId: "p1" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8908 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 24, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7031,
            raw: "F7031",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7031,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 25, y: -67 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjHitTime multi-id artifact with wrong-id trap evidence", () => {
    const artifact = createSyntheticImportedProjectileHitTimeMultiIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projhittime-multi-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projhittime-multi-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 371, 372]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([373]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 2, minProjectiles: 2, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 371, animNo: 1090, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 372, animNo: 1091, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8909,
        minAge: 1,
        minPriority: 1,
        minHitsRemaining: 1,
        hasHit: false,
      },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8910,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8910 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 25, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7032,
            raw: "F7032",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7032,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 26, y: -68 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjContactTime multi-id artifact with wrong-id trap evidence", () => {
    const artifact = createSyntheticImportedProjectileContactTimeMultiIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projcontacttime-multi-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projcontacttime-multi-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 374, 375]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([376]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 2, minProjectiles: 2, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 374, animNo: 1092, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 375, animNo: 1093, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8911,
        minAge: 1,
        minPriority: 1,
        minHitsRemaining: 1,
        hasHit: false,
      },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8912,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8912 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 26, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7033,
            raw: "F7033",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7033,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 27, y: -69 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjGuardedTime multi-id artifact with wrong-id trap evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardedTimeMultiIdTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projguardedtime-multi-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projguardedtime-multi-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 377, 378]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([379]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 2, minProjectiles: 2, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 377, animNo: 1094, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 378, animNo: 1095, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8913,
        minAge: 1,
        minPriority: 1,
        minHitsRemaining: 1,
        hasHit: false,
      },
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8914,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8914 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 27, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7034,
            raw: "F7034",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7034,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 28, y: -70 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjTime same-id last-contact artifact", () => {
    const artifact = createSyntheticImportedProjectileTimeSameIdLastContactTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projtime-same-id-last-contact-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projtime-same-id-last-contact-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toEqual(expect.arrayContaining(["guard", "hit"]));
    expect(evidence?.combatReasons).toEqual(expect.arrayContaining(["guard", "hit"]));
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 380, 381]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([382]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 1, minProjectiles: 1, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 380, animNo: 1096, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 381, animNo: 1097, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8915,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8915 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 28, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7035,
            raw: "F7035",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7035,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 29, y: -71 },
          }),
        }),
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 29, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7035,
            raw: "F7035",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7035,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 30, y: -72 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjTime same-id hit-then-guard artifact", () => {
    const artifact = createSyntheticImportedProjectileTimeSameIdHitThenGuardTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projtime-same-id-hit-then-guard-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projtime-same-id-hit-then-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toEqual(expect.arrayContaining(["hit", "guard"]));
    expect(evidence?.combatReasons).toEqual(expect.arrayContaining(["hit", "guard"]));
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 383, 384]);
    expect(gate?.requirements.forbiddenExecutedStates).toEqual([385]);
    expect(gate?.requirements.requiredExecutedControllers).toEqual(["ChangeState", "HitDef", { type: "Projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredExecutedOperations).toEqual(["hitdef", { operation: "projectile", minCount: 2 }]);
    expect(gate?.requirements.requiredEffectStores).toEqual([{ ownerId: "p1", minTotal: 1, minProjectiles: 1, minNextProjectileSerial: 2 }]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 383, animNo: 1098, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 384, animNo: 1099, moveType: "I" }),
        expect.objectContaining({ actorId: "p1-projectile-0", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
        expect.objectContaining({ actorId: "p1-projectile-1", source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-1",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8916,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8916 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 30, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7036,
            raw: "F7036",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7036,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 31, y: -73 },
          }),
        }),
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 31, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7036,
            raw: "F7036",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7036,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 32, y: -74 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjHit suffix artifact with second-form trigger evidence", () => {
    const artifact = createSyntheticImportedProjectileHitSuffixTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projhit-suffix-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projhit-suffix-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 352, 353]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 352, animNo: 1074, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 353, animNo: 1075, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8898,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8898 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 17, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7024,
            raw: "F7024",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7024,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -60 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjHit any-id suffix artifact with omitted and zero-id evidence", () => {
    const artifact = createSyntheticImportedProjectileHitSuffixAnyTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projhit-suffix-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projhit-suffix-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 356, 357]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 356, animNo: 1078, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 357, animNo: 1079, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8900,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8900 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 19, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7026,
            raw: "F7026",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7026,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 20, y: -62 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjGuarded suffix artifact with second-form trigger evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardedSuffixTraceArtifact({ generatedAt: "2026-07-05T00:00:00.000Z" });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projguarded-suffix-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projguarded-suffix-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 354, 355]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 354, animNo: 1076, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 355, animNo: 1077, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8899,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8899 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 18, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7025,
            raw: "F7025",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7025,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 19, y: -61 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile ProjGuarded any-id suffix artifact with omitted and zero-id evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardedSuffixAnyTraceArtifact({
      generatedAt: "2026-07-05T00:00:00.000Z",
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-projguarded-suffix-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-projguarded-suffix-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(gate?.requirements.requiredExecutedStates).toEqual([200, 358, 359]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ actorId: "p1", stateNo: 358, animNo: 1080, moveType: "A" }),
        expect.objectContaining({ actorId: "p1", stateNo: 359, animNo: 1081, moveType: "I" }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 910 }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        actorId: "p1-projectile-0",
        kind: "projectile",
        ownerId: "p1",
        parentId: "p1",
        effectId: 8901,
        minAge: 1,
        minPriority: 1,
        maxHitsRemaining: 0,
        hasHit: true,
      },
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 8901 })]),
    );
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 20, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7027,
            raw: "F7027",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7027,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 21, y: -63 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile guarded-time artifact with any-projectile branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardedTimeAnyTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });
    const evidence = artifact.gates[0]?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guardedtime-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-guardedtime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toContain(279);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 279]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile guarded-time artifact with fixed-id branch evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardedTimeTraceArtifact({ generatedAt: "2026-07-01T00:00:00.000Z" });
    const evidence = artifact.gates[0]?.evidence;

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guardedtime-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-guardedtime-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedStates).toContain(322);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 322]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile motion artifact with accel and scale evidence", () => {
    const artifact = createSyntheticImportedProjectileMotionTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-motion-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-motion-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "projectile",
          ownerId: "p1",
          animNo: 910,
          maxVel: expect.objectContaining({ x: expect.any(Number) }),
          minScale: { x: 1.75, y: 0.5 },
          maxScale: { x: 1.75, y: 0.5 },
        }),
      ]),
    );
    const projectileFrame = evidence?.actorFrames.find(
      (frame) => frame.source === "effect" && frame.actorKind === "projectile" && frame.ownerId === "p1" && frame.animNo === 910,
    );
    expect(projectileFrame?.maxVel.x).toBeGreaterThanOrEqual(5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "projectile",
        ownerId: "p1",
        animNo: 910,
        moveType: "A",
        minFrames: 3,
        observedVelXAtLeast: 5,
        observedScaleXAtLeast: 1.75,
        observedScaleXAtMost: 1.75,
        observedScaleYAtLeast: 0.5,
        observedScaleYAtMost: 0.5,
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredEffectPayloads).toEqual([
      { kind: "projectile", ownerId: "p1", effectId: 77, minAge: 2 },
    ]);
  });

  it("creates a synthetic imported Projectile velmul artifact with velocity decay evidence", () => {
    const artifact = createSyntheticImportedProjectileVelMulTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-velmul-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-velmul-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    const projectileFrame = evidence?.actorFrames.find(
      (frame) => frame.source === "effect" && frame.actorKind === "projectile" && frame.ownerId === "p1" && frame.animNo === 910,
    );
    expect(projectileFrame?.maxVel.x).toBeGreaterThanOrEqual(8);
    expect(projectileFrame?.minVel.x).toBeLessThanOrEqual(2);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "projectile",
        ownerId: "p1",
        animNo: 910,
        moveType: "A",
        minFrames: 3,
        observedVelXAtLeast: 8,
        observedVelXAtMost: 2,
      },
    ]);
  });

  it("creates a synthetic imported ModifyProjectile artifact with live projectile mutation evidence", () => {
    const artifact = createSyntheticImportedModifyProjectileTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-modifyprojectile-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-modifyprojectile-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ModifyProjectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.modifyprojectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "projectile",
          ownerId: "p1",
          animNo: 910,
          maxScale: expect.objectContaining({ x: 2 }),
          minScale: expect.objectContaining({ y: 0.5 }),
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "HitDef",
      "Projectile",
      "ModifyProjectile",
    ]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "projectile",
      "modifyprojectile",
    ]);
    expect(artifact.gates[0]?.requirements.requiredEffectPayloads).toEqual([
      {
        kind: "projectile",
        ownerId: "p1",
        effectId: 77,
        minAge: 3,
        minPriority: 3,
        minHitsRemaining: 4,
        scaleX: 2,
        scaleY: 0.5,
        edgeBound: 48,
        stageBound: 32,
        heightBound: { low: -96, high: 64 },
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            edgeBound: 48,
            stageBound: 32,
            heightBound: { low: -96, high: 64 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic ModifyProjectile bounds artifact", () => {
    const artifact = createSyntheticImportedModifyProjectileDynamicBoundsTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-modifyprojectile-dynamic-bounds-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-modifyprojectile-dynamic-bounds-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(4);
    expect(evidence?.executedControllers.ModifyProjectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(4);
    expect(evidence?.executedOperations.modifyprojectile).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredEffectPayloads).toEqual([
      {
        kind: "projectile",
        ownerId: "p1",
        effectId: 77,
        minAge: 3,
        edgeBound: 52,
        stageBound: 36,
        heightBound: { low: -144, high: 72 },
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            edgeBound: 52,
            stageBound: 36,
            heightBound: { low: -144, high: 72 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported ModifyProjectile omitted bounds artifact", () => {
    const artifact = createSyntheticImportedModifyProjectileOmittedBoundsTraceArtifact({
      generatedAt: "2026-07-08T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-modifyprojectile-omitted-bounds-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-modifyprojectile-omitted-bounds-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ModifyProjectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.modifyprojectile).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredEffectPayloads).toEqual([
      {
        kind: "projectile",
        ownerId: "p1",
        effectId: 77,
        minAge: 3,
        minRemoveTime: 28,
        minPriority: 4,
        minHitsRemaining: 5,
        scaleX: 2,
        scaleY: 0.75,
        edgeBound: 34,
        stageBound: 26,
        heightBound: { low: -132, high: 68 },
      },
    ]);
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            priority: 4,
            hitsRemaining: 5,
            scale: { x: 2, y: 0.75 },
            edgeBound: 34,
            stageBound: 26,
            heightBound: { low: -132, high: 68 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported dynamic ModifyProjectile params artifact", () => {
    const artifact = createSyntheticImportedModifyProjectileDynamicParamsTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-modifyprojectile-dynamic-params-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-modifyprojectile-dynamic-params-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(15);
    expect(evidence?.executedControllers.ModifyProjectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(15);
    expect(evidence?.executedOperations.modifyprojectile).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "VarSet",
      "HitDef",
      "Projectile",
      "ModifyProjectile",
    ]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "variable:varset",
      "hitdef",
      "projectile",
      "modifyprojectile",
    ]);
    expect(artifact.gates[0]?.requirements.requiredEffectPayloads).toEqual([
      {
        kind: "projectile",
        ownerId: "p1",
        effectId: 77,
        minAge: 3,
        minRemoveTime: 42,
        minSpritePriority: 7,
        minPriority: 5,
        minHitsRemaining: 6,
        scaleX: 3,
        scaleY: 1,
      },
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "projectile",
          ownerId: "p1",
          animNo: 910,
          maxVel: expect.objectContaining({ x: expect.any(Number) }),
          maxScale: expect.objectContaining({ x: 3 }),
          minScale: expect.objectContaining({ y: 1 }),
        }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            removeTime: 42,
            spritePriority: 7,
            priority: 5,
            hitsRemaining: 6,
            removeOnHit: false,
            scale: { x: 3, y: 1 },
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported ModifyExplod artifact with live visual explod mutation evidence", () => {
    const artifact = createSyntheticImportedModifyExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-modifyexplod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-modifyexplod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ModifyExplod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.modifyexplod).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "explod",
          ownerId: "p1",
          animNo: 931,
          maxScale: expect.objectContaining({ x: 2 }),
          minScale: expect.objectContaining({ y: 0.5 }),
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredExecutedControllers).toEqual([
      "ChangeState",
      "HitDef",
      "Explod",
      "ModifyExplod",
    ]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual([
      "hitdef",
      "explod",
      "modifyexplod",
    ]);
  });

  it("creates a synthetic imported Projectile contact artifact with ProjContact branch evidence", () => {
    const artifact = createSyntheticImportedProjectileContactTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-contact-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-contact-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedStates).toContain(272);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 272]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef", "projectile", "audio:playsnd"]);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "hit",
          sound: expect.objectContaining({ type: "PlaySnd", group: 5, index: 0, contactKind: "hit" }),
          hitEffect: expect.objectContaining({
            kind: "hit",
            sparkNo: 7002,
            raw: "F7002",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7002,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 18, y: -68 },
          }),
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "hit",
        hitEffect: expect.objectContaining({ kind: "hit", sparkNo: 7002, offsetX: 18, offsetY: -68 }),
      }),
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile multi-hit artifact with projhits and projmisstime evidence", () => {
    const artifact = createSyntheticImportedProjectileMultiHitTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-multihit-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-multihit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.eventLines.filter((line) => line.includes("projectile hit") && line.includes("Mira Volt"))).toHaveLength(2);
    expect(evidence?.eventLines.some((line) => line.includes("hits remaining 1") && line.includes("miss 3"))).toBe(true);
    expect(evidence?.eventLines.some((line) => line.includes("hits remaining 0") && line.includes("miss 0"))).toBe(true);
    expect(artifact.gates[0]?.requirements.requiredEventSubstrings).toEqual([
      "projectile hit",
      "hits remaining 1",
      "miss 3",
      "hits remaining 0",
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile guard artifact with shared guard evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-guard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["audio:playsnd"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedStates).toContain(271);
    expect(artifact.gates[0]?.requirements.requiredExecutedStates).toEqual([200, 271]);
    expect(artifact.gates[0]?.requirements.requiredExecutedOperations).toEqual(["hitdef", "projectile", "audio:playsnd"]);
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.contactEffectPackages).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1",
          source: "imported",
          actorKind: "player",
          contactKind: "guard",
          sound: expect.objectContaining({ type: "PlaySnd", group: 6, index: 0, contactKind: "guard" }),
          hitEffect: expect.objectContaining({
            kind: "guard",
            sparkNo: 7004,
            raw: "F7004",
            rawPrefix: "F",
            assetSource: "fightfx",
            assetActionId: 7004,
            assetFrameCount: 2,
            assetTotalDuration: 11,
            offset: { x: 15, y: -63 },
          }),
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredContactEffectPackages).toEqual([
      expect.objectContaining({
        contactKind: "guard",
        hitEffect: expect.objectContaining({ kind: "guard", sparkNo: 7004, offsetX: 15, offsetY: -63 }),
      }),
    ]);
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(artifact.trace.events.some((event) => event.category === "guard" && event.line.includes("projectile"))).toBe(true);
  });

  it("creates a synthetic imported Projectile guard chip KO artifact with round evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardKoTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-ko-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-guard-ko-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported Projectile Guard KO projectile for 2000"))).toBe(true);
    expect(evidence?.roundFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          state: "ko",
          winner: "Synthetic Imported Projectile Guard KO",
          message: "Synthetic Imported Projectile Guard KO wins",
        }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "demo", life: 0 })]),
    );
  });

  it("creates a synthetic imported Projectile guard.kill no-KO artifact with clamp evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardKillTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-kill-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-guard-kill-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.activeCommands).toContain("x");
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported Projectile Guard Kill projectile for 2000"))).toBe(true);
    expect(evidence?.roundFrames.some((frame) => frame.state === "ko")).toBe(false);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: false, hitsRemaining: 1 }),
        }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
    expect(evidence?.finalActors).toEqual(
      expect.arrayContaining([expect.objectContaining({ id: "p2", source: "demo", life: 1 })]),
    );
  });

  it("creates a synthetic imported Projectile guard terminal artifact with hit removal playback evidence", () => {
    const artifact = createSyntheticImportedProjectileGuardTerminalTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-guard-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-guard-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.eventLines.some((line) => line.includes("guarded Synthetic Imported Projectile Guard Terminal projectile"))).toBe(true);
    expect(evidence?.eventLines.some((line) => line.includes("hit removal anim 912"))).toBe(true);
    expect(artifact.gates[0]?.requirements.requiredEventSubstrings).toEqual([
      "guarded Synthetic Imported Projectile Guard Terminal projectile",
      "hit removal anim 912",
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 912, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: true,
            removalReason: "hit",
            terminalReason: "hit",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile remove terminal artifact with timeout playback evidence", () => {
    const artifact = createSyntheticImportedProjectileRemoveTerminalTraceArtifact({ generatedAt: "2026-07-06T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-remove-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-remove-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(artifact.gates[0]?.requirements.requiredEventSubstrings).toBeUndefined();
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 919, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: false,
            hitsRemaining: 1,
            removalReason: "timeout",
            terminalReason: "timeout",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks ?? []).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile remove hit fallback terminal artifact with timeout playback evidence", () => {
    const artifact = createSyntheticImportedProjectileRemoveHitFallbackTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-remove-hit-fallback-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-remove-hit-fallback-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(artifact.gates[0]?.requirements.requiredEventSubstrings).toBeUndefined();
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 920, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: false,
            hitsRemaining: 1,
            removalReason: "timeout",
            terminalReason: "timeout",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks ?? []).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile bounds remove terminal artifact with visible bounds evidence", () => {
    const artifact = createSyntheticImportedProjectileBoundsRemoveTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-bounds-remove-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-bounds-remove-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 923, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: false,
            hitsRemaining: 1,
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks ?? []).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile default bounds terminal artifact", () => {
    const artifact = createSyntheticImportedProjectileDefaultBoundsTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-default-bounds-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-default-bounds-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 927, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: false,
            hitsRemaining: 1,
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks ?? []).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile localcoord default bounds terminal artifact", () => {
    const artifact = createSyntheticImportedProjectileLocalCoordDefaultBoundsTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-localcoord-default-bounds-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-localcoord-default-bounds-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 928, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            edgeBound: 80,
            stageBound: 80,
            heightBound: { low: -480, high: 2 },
            removalReason: "bounds",
            terminalReason: "bounds",
          }),
        }),
      ]),
    );
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      {
        kind: "projectile",
        ownerId: "p1",
        effectId: 77,
        edgeBound: 80,
        stageBound: 80,
        heightBound: { low: -480, high: 2 },
        hasHit: false,
        minHitsRemaining: 1,
        removalReason: "bounds",
        terminalReason: "bounds",
        minTerminalAge: 1,
        minTerminalDuration: 2,
      },
    ]);
  });

  it("creates a synthetic imported Projectile projstagebound terminal artifact with explicit stage-bound evidence", () => {
    const artifact = createSyntheticImportedProjectileStageBoundTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-stagebound-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-stagebound-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 924, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: false,
            hitsRemaining: 1,
            stageBound: 24,
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks ?? []).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile projedgebound terminal artifact with explicit edge-bound evidence", () => {
    const artifact = createSyntheticImportedProjectileEdgeBoundTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-edgebound-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-edgebound-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 925, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: false,
            hitsRemaining: 1,
            edgeBound: 24,
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks ?? []).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile projheightbound terminal artifact with explicit height-bound evidence", () => {
    const artifact = createSyntheticImportedProjectileHeightBoundTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-heightbound-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-heightbound-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.HitDef).toBeUndefined();
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.hitdef).toBeUndefined();
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 926, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          actorId: "p1-projectile-0",
          ownerId: "p1",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: false,
            hitsRemaining: 1,
            heightBound: { low: -120, high: 60 },
            removalReason: "bounds",
            terminalReason: "bounds",
            terminalDuration: 2,
          }),
        }),
      ]),
    );
    expect(evidence?.targetLinks ?? []).not.toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p1", actorId: "p2", targetId: 77 })]),
    );
  });

  it("creates a synthetic imported Projectile clash artifact with runtime event evidence", () => {
    const artifact = createSyntheticImportedProjectileClashTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-clash-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-clash-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("runtime");
    expect(evidence?.eventLines.some((line) => line.includes("Projectile clash"))).toBe(true);
    expect(evidence?.eventLines.some((line) => line.includes("p1-projectile-0 cancel removal anim 913"))).toBe(true);
    expect(evidence?.eventLines.some((line) => line.includes("p2-projectile-0 cancel removal anim 914"))).toBe(true);
    expect(artifact.gates[0]?.requirements.requiredEventSubstrings).toEqual([
      "Projectile clash",
      "p1-projectile-0 cancel removal anim 913",
      "p2-projectile-0 cancel removal anim 914",
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 913, moveType: "I", clsn1Count: 0 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p2", animNo: 914, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile priority-cancel artifact with winner-store evidence", () => {
    const artifact = createSyntheticImportedProjectilePriorityCancelTraceArtifact({
      generatedAt: "2026-06-25T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-priority-cancel-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-priority-cancel-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("projectile");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("runtime");
    expect(
      evidence?.eventLines.some(
        (line) =>
          line.includes("canceled") &&
          line.includes("3 > 1") &&
          line.includes("winner priority 3 -> 2") &&
          line.includes("p2-projectile-0 cancel removal anim 915"),
      ),
    ).toBe(true);
    expect(artifact.gates[0]?.requirements.requiredEventSubstrings).toEqual([
      "Projectile clash",
      "canceled",
      "3 > 1",
      "winner priority 3 -> 2",
      "p2-projectile-0 cancel removal anim 915",
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p2", animNo: 915, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectStores).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          total: expect.any(Number),
          projectiles: expect.arrayContaining([expect.stringContaining("p1-projectile")]),
        }),
        expect.objectContaining({
          ownerId: "p2",
          nextSerials: expect.objectContaining({ projectile: 1 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile cancel remove fallback terminal artifact with visible fallback evidence", () => {
    const artifact = createSyntheticImportedProjectileCancelRemoveFallbackTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-cancel-remove-fallback-terminal-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-cancel-remove-fallback-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventCategories).toContain("runtime");
    expect(
      evidence?.eventLines.some(
        (line) =>
          line.includes("Projectile clash") &&
          line.includes("canceled") &&
          line.includes("3 > 1") &&
          line.includes("p2-projectile-0 cancel removal anim 921"),
      ),
    ).toBe(true);
    expect(artifact.gates[0]?.requirements.requiredEventSubstrings).toEqual([
      "Projectile clash",
      "canceled",
      "3 > 1",
      "p2-projectile-0 cancel removal anim 921",
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p2", animNo: 921, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p2",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 77,
            hasHit: true,
            removalReason: "cancel",
            terminalReason: "cancel",
            removeAnimNo: 921,
            terminalDuration: 2,
          }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile cancel-time artifact with owner-state branch evidence", () => {
    const artifact = createSyntheticImportedProjectileCancelTimeTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-canceltime-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-canceltime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 283]));
    expect(evidence?.eventLines.some((line) => line.includes("p2-projectile-0 cancel removal anim 915"))).toBe(true);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p2", animNo: 915, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p2",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "cancel", terminalReason: "cancel" }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile cancel-time dynamic-id artifact with owner-state branch evidence", () => {
    const artifact = createSyntheticImportedProjectileCancelTimeDynamicTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-canceltime-dynamic-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-canceltime-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 318]));
    expect(evidence?.eventLines.some((line) => line.includes("p2-projectile-0 cancel removal anim 916"))).toBe(true);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p2", animNo: 916, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p2",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "cancel", terminalReason: "cancel" }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile cancel-time any-id artifact with owner-state branch evidence", () => {
    const artifact = createSyntheticImportedProjectileCancelTimeAnyTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-canceltime-any-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-canceltime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 320]));
    expect(evidence?.eventLines.some((line) => line.includes("p2-projectile-0 cancel removal anim 918"))).toBe(true);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p2", animNo: 918, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p2",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "cancel", terminalReason: "cancel" }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Projectile cancel-time var-id artifact with owner-state branch evidence", () => {
    const artifact = createSyntheticImportedProjectileCancelTimeVarTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-projectile-canceltime-var-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-projectile-canceltime-var-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.executedControllers.ChangeState).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.VarSet).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["variable:varset"]).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedStates).toEqual(expect.arrayContaining([200, 319]));
    expect(evidence?.eventLines.some((line) => line.includes("p2-projectile-0 cancel removal anim 917"))).toBe(true);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p2", animNo: 917, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p2",
          effect: expect.objectContaining({ kind: "projectile", id: 77, hasHit: true, removalReason: "cancel", terminalReason: "cancel" }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjCancelTime any artifact with helper-local cancel evidence", () => {
    const artifact = createSyntheticImportedHelperProjCancelTimeAnyTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projcanceltime-any-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projcanceltime-any-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventLines.some((line) => line.includes("p1-projectile-0 cancel removal anim 998"))).toBe(true);
    expect(gate?.requirements.requiredEventSubstrings).toEqual([
      "Projectile clash",
      "canceled",
      "3 > 1",
      "p1-projectile-0 cancel removal anim 998",
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1266, animNo: 995 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1267, animNo: 996 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 998, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8867, hasHit: true, removalReason: "cancel", terminalReason: "cancel" }),
        }),
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1267 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper Projectile cancel remove fallback terminal artifact with visible fallback evidence", () => {
    const artifact = createSyntheticImportedHelperProjectileCancelRemoveFallbackTerminalTraceArtifact({
      generatedAt: "2026-07-06T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projectile-cancel-remove-fallback-terminal-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projectile-cancel-remove-fallback-terminal-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["helper", "projectile"]));
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventLines.some((line) => line.includes("p1-projectile-0 cancel removal anim 1028"))).toBe(true);
    expect(gate?.requirements.requiredEventSubstrings).toEqual([
      "Projectile clash",
      "canceled",
      "3 > 1",
      "p1-projectile-0 cancel removal anim 1028",
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "active", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1272, animNo: 1025 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1273, animNo: 1026 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1028, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({
            kind: "projectile",
            id: 8870,
            hasHit: true,
            removalReason: "cancel",
            terminalReason: "cancel",
            removeAnimNo: 1028,
            terminalDuration: 2,
          }),
        }),
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1273 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjCancelTime fixed-id artifact with helper-local cancel evidence", () => {
    const artifact = createSyntheticImportedHelperProjCancelTimeIdTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projcanceltime-id-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projcanceltime-id-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventLines.some((line) => line.includes("p1-projectile-0 cancel removal anim 1008"))).toBe(true);
    expect(gate?.requirements.requiredEventSubstrings).toEqual([
      "Projectile clash",
      "canceled",
      "3 > 1",
      "p1-projectile-0 cancel removal anim 1008",
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p2", rootId: "p2", parentId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1", rootId: "p1", parentId: "p1-helper-0" }),
      ]),
    );
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1268, animNo: 1005 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1269, animNo: 1006 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1008, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1269 }),
        }),
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "projectile", id: 8868, removalReason: "cancel", terminalReason: "cancel" }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper ProjCancelTime dynamic-id artifact with helper-local cancel evidence", () => {
    const artifact = createSyntheticImportedHelperProjCancelTimeDynamicTraceArtifact({
      generatedAt: "2026-07-01T00:00:00.000Z",
    });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-projcanceltime-dynamic-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-projcanceltime-dynamic-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.eventLines.some((line) => line.includes("p1-projectile-0 cancel removal anim 1018"))).toBe(true);
    expect(gate?.requirements.requiredEventSubstrings).toEqual([
      "Projectile clash",
      "canceled",
      "3 > 1",
      "p1-projectile-0 cancel removal anim 1018",
    ]);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1270, animNo: 1015 }),
        expect.objectContaining({ source: "effect", actorKind: "helper", ownerId: "p1", stateNo: 1271, animNo: 1016 }),
        expect.objectContaining({ source: "effect", actorKind: "projectile", ownerId: "p1", animNo: 1018, moveType: "I", clsn1Count: 0 }),
      ]),
    );
    expect(evidence?.effectPayloads).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          parentId: "p1-helper-0",
          effect: expect.objectContaining({ kind: "projectile", id: 8869, hasHit: true, removalReason: "cancel", terminalReason: "cancel" }),
        }),
        expect.objectContaining({
          ownerId: "p1",
          effect: expect.objectContaining({ kind: "helper", id: 42, stateNo: 1271 }),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper artifact with typed helper operation evidence", () => {
    const artifact = createSyntheticImportedHelperTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("helper");
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredWorldLifecycleEvents).toEqual([
      { type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" },
    ]);
    expect(artifact.gates[0]?.requirements.requiredEffectStores).toEqual([
      { ownerId: "p1", minTotal: 1, minHelpers: 1, minNextHelperSerial: 1 },
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "helper", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectStores).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          total: 1,
          helpers: [expect.stringContaining("helper")],
          nextSerials: expect.objectContaining({ helper: 1 }),
        }),
      ]),
    );
    const helperFrame = artifact.trace.frames.find((frame) =>
      frame.world?.lifecycle.some((actor) => actor.kind === "helper" && actor.status === "spawned"),
    );
    expect(helperFrame?.world?.spawnedThisTick.some((id) => id.includes("helper"))).toBe(true);
    expect(helperFrame?.world?.eventsThisTick).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "helper", layer: "effect", ownerId: "p1" }),
      ]),
    );
    expect(helperFrame?.world?.effectStores.find((store) => store.ownerId === "p1")).toMatchObject({
      total: 1,
      helpers: [expect.stringContaining("helper")],
      nextSerials: {
        helper: 1,
      },
    });
    expect(helperFrame?.world?.lifecycle.find((actor) => actor.kind === "helper")).toMatchObject({
      ownerId: "p1",
      rootId: "p1",
      parentId: "p1",
      layer: "effect",
    });
  });

  it("creates a synthetic imported Helper velocity artifact with moving helper evidence", () => {
    const artifact = createSyntheticImportedHelperVelocityTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-velocity-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-velocity-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("helper");
    expect(evidence?.executedControllers.Helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.helper).toBeGreaterThanOrEqual(1);
    expect(evidence?.actorFrames).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          source: "effect",
          actorKind: "helper",
          ownerId: "p1",
          animNo: 920,
          maxVel: expect.objectContaining({ x: 3 }),
          minVel: expect.objectContaining({ y: -1 }),
        }),
      ]),
    );
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        animNo: 920,
        observedVelXAtLeast: 3,
        observedVelYAtMost: -1,
        observedPosXAtLeast: -180,
        observedPosYAtMost: -36,
        minFrames: 2,
      },
    ]);
  });

  it("creates a synthetic imported Helper scale artifact with render-scale evidence", () => {
    const artifact = createSyntheticImportedHelperScaleTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-scale-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-scale-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    const scaledFrame = evidence?.actorFrames.find((frame) => frame.actorKind === "helper" && frame.animNo === 920);
    expect(scaledFrame?.maxScale.x).toBeGreaterThanOrEqual(2);
    expect(scaledFrame?.minScale.y).toBeLessThanOrEqual(0.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        source: "effect",
        actorKind: "helper",
        ownerId: "p1",
        animNo: 920,
        observedScaleXAtLeast: 2,
        observedScaleYAtMost: 0.5,
        minFrames: 1,
      },
    ]);
    expect(artifact.gates[0]?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1200, scaleX: 2, scaleY: 0.5 },
    ]);
  });

  it("creates a synthetic imported Helper supermovetime artifact with pause-budget advance evidence", () => {
    const artifact = createSyntheticImportedHelperSuperMoveTimeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-supermovetime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-supermovetime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1200, minSuperMoveTime: 1 },
    ]);
    expect(gate?.requirements.requiredMatchPauseFreezes).toEqual([
      { type: "SuperPause", actorKind: "player", ownerId: "p2", minFrozenFrames: 6 },
      { type: "SuperPause", actorKind: "explod", ownerId: "p1", minFrozenFrames: 5 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "SuperPause", actorKind: "helper", ownerId: "p1", minAdvancedFrames: 3, minPreviousMoveTime: 0 },
    ]);
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "SuperPause",
          actorKind: "helper",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime"]),
        }),
      ]),
    );
  });

  it("creates a synthetic imported Helper pausemovetime artifact with pause-budget advance evidence", () => {
    const artifact = createSyntheticImportedHelperPauseMoveTimeTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-pausemovetime-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-pausemovetime-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1200, minPauseMoveTime: 1 },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "Pause", actorKind: "helper", ownerId: "p1", minAdvancedFrames: 3, minPreviousMoveTime: 0 },
    ]);
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "Pause",
          actorKind: "helper",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime"]),
        }),
      ]),
    );
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([expect.objectContaining({ type: "Pause", actorKind: "explod", ownerId: "p1" })]),
    );
  });

  it("creates a synthetic imported Helper ignorehitpause artifact with hitpause advance evidence", () => {
    const artifact = createSyntheticImportedHelperIgnoreHitPauseTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-helper-ignorehitpause-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-helper-ignorehitpause-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const gate = artifact.gates[0];
    const evidence = gate?.evidence;
    expect(gate?.requirements.requiredEffectPayloads).toEqual([
      { kind: "helper", ownerId: "p1", effectId: 42, name: "Buddy", helperStateNo: 1200, ignoreHitPause: true },
    ]);
    expect(gate?.requirements.requiredMatchPauseAdvances).toEqual([
      { type: "HitPause", actorKind: "helper", ownerId: "p1", minAdvancedFrames: 3, minPreviousMoveTime: 1 },
    ]);
    expect(evidence?.matchPauseAdvances).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          type: "HitPause",
          actorKind: "helper",
          ownerId: "p1",
          changedFields: expect.arrayContaining(["animTime"]),
        }),
      ]),
    );
    expect(evidence?.matchPauseFreezes).toEqual(
      expect.arrayContaining([expect.objectContaining({ type: "HitPause", actorKind: "explod", ownerId: "p1" })]),
    );
  });

  it("creates a synthetic imported Explod artifact with typed explod operation evidence", () => {
    const artifact = createSyntheticImportedExplodTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(artifact.gates[0]?.requirements.requiredWorldLifecycleEvents).toEqual([
      { type: "spawn", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1" },
      { type: "active", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1" },
    ]);
    expect(artifact.gates[0]?.requirements.requiredEffectStores).toEqual([
      { ownerId: "p1", minTotal: 1, minExplods: 1, minNextExplodSerial: 1 },
    ]);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1" }),
        expect.objectContaining({ type: "active", kind: "explod", ownerId: "p1", rootId: "p1", parentId: "p1" }),
      ]),
    );
    expect(evidence?.effectStores).toEqual(
      expect.arrayContaining([
        expect.objectContaining({
          ownerId: "p1",
          total: 1,
          explods: [expect.stringContaining("explod")],
          nextSerials: expect.objectContaining({ explod: 1 }),
        }),
      ]),
    );
    const explodFrame = artifact.trace.frames.find((frame) =>
      frame.world?.lifecycle.some((actor) => actor.kind === "explod" && actor.status === "spawned"),
    );
    expect(explodFrame?.world?.spawnedThisTick.some((id) => id.includes("explod"))).toBe(true);
    expect(explodFrame?.world?.eventsThisTick).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "explod", layer: "effect", ownerId: "p1" }),
      ]),
    );
    expect(explodFrame?.world?.effectStores.find((store) => store.ownerId === "p1")).toMatchObject({
      total: 1,
      explods: [expect.stringContaining("explod")],
      nextSerials: {
        explod: 1,
      },
    });
    expect(explodFrame?.world?.lifecycle.find((actor) => actor.kind === "explod")).toMatchObject({
      ownerId: "p1",
      rootId: "p1",
      parentId: "p1",
      layer: "effect",
    });
  });

  it("creates a synthetic imported moving Explod artifact with velocity evidence", () => {
    const artifact = createSyntheticImportedExplodVelocityTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-velocity-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-velocity-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    const movingFrame = evidence?.actorFrames.find((frame) => frame.actorKind === "explod" && frame.animNo === 931);
    expect(movingFrame?.minPos.x).toBeLessThanOrEqual(-110);
    expect(movingFrame?.maxPos.x).toBeGreaterThanOrEqual(-60);
    expect(movingFrame?.maxVel.x).toBeGreaterThanOrEqual(7);
    expect(movingFrame?.maxVel.y).toBeGreaterThanOrEqual(0);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorKind: "explod",
        ownerId: "p1",
        animNo: 931,
        minFrames: 3,
        observedPosXAtMost: -110,
        observedPosXAtLeast: -60,
        observedVelXAtLeast: 7,
        observedVelYAtLeast: 0,
      },
    ]);
  });

  it("creates a synthetic imported bound Explod artifact with owner movement evidence", () => {
    const artifact = createSyntheticImportedExplodBindTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-bind-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-bind-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.PosAdd).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations["kinematic:posadd"]).toBeGreaterThanOrEqual(1);
    const boundFrame = evidence?.actorFrames.find((frame) => frame.actorKind === "explod" && frame.animNo === 932);
    expect(boundFrame?.minPos.x).toBeLessThanOrEqual(-120);
    expect(boundFrame?.maxPos.x).toBeGreaterThanOrEqual(-80);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorKind: "explod",
        ownerId: "p1",
        animNo: 932,
        minFrames: 3,
        observedPosXAtMost: -120,
        observedPosXAtLeast: -80,
      },
    ]);
  });

  it("creates a synthetic imported scaled Explod artifact with renderer scale evidence", () => {
    const artifact = createSyntheticImportedExplodScaleTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-scale-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-scale-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    const scaledFrame = evidence?.actorFrames.find((frame) => frame.actorKind === "explod" && frame.animNo === 933);
    expect(scaledFrame?.maxScale.x).toBeGreaterThanOrEqual(2);
    expect(scaledFrame?.minScale.y).toBeLessThanOrEqual(0.5);
    expect(artifact.gates[0]?.requirements.requiredActorFrames).toEqual([
      {
        actorKind: "explod",
        ownerId: "p1",
        animNo: 933,
        minFrames: 3,
        observedScaleXAtLeast: 2,
        observedScaleYAtMost: 0.5,
      },
    ]);
  });

  it("creates a synthetic imported Explod removeongethit artifact with lifecycle evidence", () => {
    const artifact = createSyntheticImportedExplodRemoveOnGetHitTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-removeongethit-golden",
        source: "mixed",
      },
      gates: [
        {
          label: "synthetic-imported-explod-removeongethit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toContain("explod");
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "explod", ownerId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "explod", ownerId: "p2" }),
      ]),
    );
    expect(evidence?.effectStores).toEqual(
      expect.arrayContaining([expect.objectContaining({ ownerId: "p2", explods: ["p2-explod-0"], nextSerials: { explod: 1, helper: 0, projectile: 0 } })]),
    );
  });

  it("creates a synthetic imported Explod projectile removeongethit artifact with lifecycle evidence", () => {
    const artifact = createSyntheticImportedExplodRemoveOnProjectileHitTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-removeonprojectilehit-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-explod-removeonprojectilehit-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["projectile", "explod"]));
    expect(evidence?.eventCategories).toContain("hit");
    expect(evidence?.combatReasons).toContain("hit");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "explod", ownerId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "explod", ownerId: "p2" }),
      ]),
    );
    expect(evidence?.effectStores).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", projectiles: ["p1-projectile-0"], nextSerials: { explod: 0, helper: 0, projectile: 1 } }),
        expect.objectContaining({ ownerId: "p2", explods: ["p2-explod-0"], nextSerials: { explod: 1, helper: 0, projectile: 0 } }),
      ]),
    );
  });

  it("creates a synthetic imported Explod projectile guard removeongethit artifact with lifecycle evidence", () => {
    const artifact = createSyntheticImportedExplodRemoveOnProjectileGuardTraceArtifact({ generatedAt: "2026-06-25T00:00:00.000Z" });

    expect(artifact).toMatchObject({
      status: "passed",
      target: {
        id: "synthetic-imported-explod-removeonprojectileguard-golden",
        source: "imported",
      },
      gates: [
        {
          label: "synthetic-imported-explod-removeonprojectileguard-golden",
          passed: true,
          failures: [],
        },
      ],
    });
    const evidence = artifact.gates[0]?.evidence;
    expect(evidence?.effectKinds).toEqual(expect.arrayContaining(["projectile", "explod"]));
    expect(evidence?.eventCategories).toContain("guard");
    expect(evidence?.combatReasons).toContain("guard");
    expect(evidence?.executedControllers.Projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedControllers.Explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.projectile).toBeGreaterThanOrEqual(1);
    expect(evidence?.executedOperations.explod).toBeGreaterThanOrEqual(1);
    expect(evidence?.worldLifecycleEvents).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ type: "spawn", kind: "projectile", ownerId: "p1" }),
        expect.objectContaining({ type: "remove", kind: "projectile", ownerId: "p1" }),
        expect.objectContaining({ type: "spawn", kind: "explod", ownerId: "p2" }),
        expect.objectContaining({ type: "remove", kind: "explod", ownerId: "p2" }),
      ]),
    );
    expect(evidence?.effectStores).toEqual(
      expect.arrayContaining([
        expect.objectContaining({ ownerId: "p1", projectiles: ["p1-projectile-0"], nextSerials: { explod: 0, helper: 0, projectile: 1 } }),
        expect.objectContaining({ ownerId: "p2", explods: ["p2-explod-0"], nextSerials: { explod: 1, helper: 0, projectile: 0 } }),
      ]),
    );
  });

  it("keeps golden trace scripts explicit and labeled", () => {
    expect(nativeHitScript().at(0)?.label).toBe("native-hold-punch");
    expect(nativeWhiffScript().at(0)?.label).toBe("native-whiff-punch");
    expect(importedBasicMovementScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "basic-movement-idle-prime",
      "basic-movement-walk-forward",
      "basic-movement-return-idle",
      "basic-movement-crouch",
      "basic-movement-crouch-release",
      "basic-movement-jump",
      "basic-movement-airborne-settle",
      "basic-movement-grounded-idle",
    ]);
    expect(importedGuardScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-guard-x", "guard-settle"]);
    expect(importedDefaultGuardStateScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-guard-state-x",
      "default-guard-state-settle",
    ]);
    expect(importedDefaultCrouchGuardStateScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-crouch-guard-state-x",
      "default-crouch-guard-state-settle",
    ]);
    expect(importedDefaultDiagonalCrouchGuardStateScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-diagonal-crouch-guard-state-x",
      "default-diagonal-crouch-guard-state-settle",
    ]);
    expect(importedInGuardDistScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-inguarddist-near-x",
      "imported-inguarddist-hold",
    ]);
    expect(importedAutoGuardStartScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-auto-guard-start-x",
    ]);
    expect(importedAutoGuardEndScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-auto-guard-end-start",
      "imported-auto-guard-end-release",
    ]);
    expect(importedDelayedXScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "stage-time-wait",
      "stage-time-x",
      "stage-time-settle",
    ]);
    expect(importedCommonGetHitScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-common-gethit-x",
      "common-gethit-settle",
    ]);
    expect(importedCustomStateScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-custom-state-x",
      "custom-state-return-settle",
    ]);
    expect(importedDefaultGetHitScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-default-gethit-x"]);
    expect(importedDefaultCrouchGetHitScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-crouch-gethit-prep",
      "imported-default-crouch-gethit-x",
    ]);
    expect(importedDefaultAirGetHitScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-air-gethit-jump",
      "imported-default-air-gethit-x",
    ]);
    expect(importedDefaultGetHitProgressionScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-gethit-progress-x",
      "default-gethit-progress-settle",
    ]);
    expect(importedDefaultFallGetHitScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-fall-gethit-x",
      "default-fall-gethit-settle",
    ]);
    expect(importedDefaultAirFallGetHitScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-air-fall-gethit-jump",
      "imported-default-air-fall-gethit-x",
      "default-air-fall-gethit-settle",
    ]);
    expect(importedDefaultFallRecoveryScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-fall-recovery-x",
      "default-fall-recovery-chain",
    ]);
    expect(importedDefaultFallRecoveryInputScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-fall-recovery-input-x",
      "default-fall-recovery-input-window",
      "default-fall-recovery-input",
      "default-fall-recovery-input-settle",
    ]);
    expect(importedDefaultFallOfficialRecoveryInputScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-fall-official-recovery-input-x",
      "default-fall-official-recovery-input-window",
      "default-fall-official-recovery-input",
      "default-fall-official-recovery-input-settle",
    ]);
    expect(importedDefaultFallOfficialRecoveryTooEarlyScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-fall-official-recovery-too-early-x",
      "default-fall-official-recovery-too-early-window",
      "default-fall-official-recovery-too-early-input",
      "default-fall-official-recovery-too-early-settle",
    ]);
    expect(importedDefaultFallOfficialGroundRecoveryScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-fall-official-ground-recovery-x",
      "default-fall-official-ground-recovery-window",
      "default-fall-official-ground-recovery-input",
      "default-fall-official-ground-recovery-settle",
    ]);
    expect(importedDefaultFallOfficialRecoveryScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-default-fall-official-recovery-x",
      "default-fall-official-recovery-chain",
    ]);
    expect(importedXHitstunScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-hitstun-x"]);
    expect(importedXStateExitScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-state-exit-x",
      "state-exit-settle",
    ]);
    expect(importedHitDefPriorityScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-hitdef-priority-x",
      "hitdef-priority-settle",
    ]);
    expect(importedHitDefKillScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-hitdef-kill-x"]);
    expect(importedHitDefGuardKillScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-hitdef-guard-kill-x",
      "hitdef-guard-kill-settle",
    ]);
    expect(importedTargetScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-target-x", "target-settle"]);
    expect(importedTargetBindPauseScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-targetbind-pause-x",
      "targetbind-pause-settle",
    ]);
    expect(importedSuperPauseScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-superpause-x", "superpause-settle"]);
    expect(importedSuperPauseProjectileScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-superpause-projectile-x",
      "superpause-projectile-settle",
    ]);
    expect(importedSuperPauseEffectScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-superpause-effect-x",
      "superpause-effect-settle",
    ]);
    expect(importedProjectileScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-projectile-x", "projectile-settle"]);
    expect(importedProjectileMotionScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-projectile-motion-x",
      "projectile-motion-settle",
    ]);
    expect(importedProjectileVelMulScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-projectile-velmul-x",
      "projectile-velmul-settle",
    ]);
    expect(importedProjectileGuardScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-projectile-guard-x",
      "projectile-guard-settle",
    ]);
    expect(importedProjectileMultiHitScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-projectile-multihit-x",
      "projectile-multihit-settle",
    ]);
    expect(importedProjectileClashScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-projectile-clash-x",
      "projectile-clash-settle",
    ]);
    expect(importedProjectilePriorityCancelScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "imported-projectile-priority-cancel-x",
      "projectile-priority-cancel-settle",
    ]);
    expect(importedHelperScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-helper-x", "helper-settle"]);
    expect(importedExplodScript().map((frame) => frame.label).filter(Boolean)).toEqual(["imported-explod-x", "explod-settle"]);
    expect(importedRemoveOnGetHitExplodScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "seed-removeongethit-explod",
      "hit-removeongethit-owner",
      "removeongethit-settle",
    ]);
    expect(importedProjectileRemoveOnGetHitExplodScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "seed-projectile-removeongethit-explod",
      "projectile-hit-removeongethit-owner",
      "projectile-removeongethit-settle",
    ]);
    expect(importedProjectileGuardRemoveOnGetHitExplodScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "seed-projectile-guard-removeongethit-explod",
      "projectile-guard-removeongethit-owner",
      "projectile-guard-removeongethit-settle",
    ]);
    expect(qcfXScript().map((frame) => frame.label).filter(Boolean)).toEqual(["qcf-d", "qcf-df", "qcf-f", "qcf-x", "settle"]);
    expect(qcfXContactScript().map((frame) => frame.label).filter(Boolean)).toEqual([
      "qcf-d",
      "qcf-df",
      "qcf-f",
      "qcf-x",
      "qcf-contact-settle",
    ]);
    expect(qcfXContactScript()).toHaveLength(31);
  });

  it("describes official KFM QCF x contact trace sequence requirements", () => {
    expect(officialKfmQcfXControllerSequence().steps).toEqual([
      { stateNo: 20, controller: "ChangeState" },
      { stateNo: 1000, controller: "PosAdd" },
      { stateNo: 1000, operation: "kinematic:posadd" },
      { stateNo: 1000, controller: "HitDef" },
      { stateNo: 1000, operation: "hitdef" },
    ]);

    expect(officialKfmQcfXActorFrameSequence().steps).toEqual([
      expect.objectContaining({
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 11,
        animNo: 11,
        minFrames: 4,
      }),
      expect.objectContaining({
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 20,
        animNo: 20,
        minFrames: 2,
      }),
      expect.objectContaining({
        actorId: "p1",
        source: "imported",
        actorKind: "player",
        stateNo: 1000,
        animNo: 1000,
        minFrames: 8,
      }),
    ]);
  });
});
