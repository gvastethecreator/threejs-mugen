# Project Context

`mugen-web-sandbox` is a private/local TypeScript + Three.js project for a progressive MUGEN/IKEMEN-GO browser port. It is also the first module of a future creator studio and modular browser game engine.

This file is the short domain map for agents. Detailed state lives in the docs listed below; do not replace those docs with this summary.

## Current Product Shape

- **Runtime Mode**: playable fight sandbox with local generated/native fighters, stages, input, HUD, hitboxes, hit pause, hit stun, life/power, trace evidence, and partial imported fighter routes.
- **Inspector Mode**: local ZIP/folder loader for MUGEN character/stage resources, parsers, animation preview, collision boxes, compatibility reports, and unsupported-feature diagnostics.
- **Studio Mode**: project workbench for assets, evidence, build/export status, debug state, module status, source provenance, and future authoring tools.

## North Star

Build a Three.js runtime and creator workflow that can expand from:

```txt
private playable MUGEN-like sandbox
  -> fixture-backed MUGEN compatibility layers
  -> IKEMEN-GO scan/report bridge
  -> evidence-first Creator Studio
  -> reusable modular browser game engine
```

Near-term language must stay honest: this is **partial MUGEN compatibility with trace and fixture gates**, plus **IKEMEN scanner/reporting first**. It is not a full MUGEN or IKEMEN-GO port yet.

Current implementation cursor:

- Current R1 custom-state ReversalDef checkpoint: required `synthetic-imported-custom-state-reversal.json` checksum `18065db0` / final checksum `ac8d0073` promotes bounded direct custom-state `ReversalDef` priority into `pnpm qa:trace`. The route executes imported direct `HitDef p2stateno = 889` / `p2getp1state = 1` against a defender with active `ReversalDef p1stateno = 777` / `p2stateno = 888`; the trace stops at the reversal result, requires typed `hitdef` / `reversaldef` operation evidence plus reversal event/combat-reason evidence, forbids the HitDef owner-backed custom state `889`, default get-hit `5000`, and guard states `150` / `151`, and ends P1 in defender-owned custom state/action `888` (`customOwnerId = p2`) while P2 enters state/action `777`, both at life `1000`. Current trace aggregate is 469/469 artifacts, 439 required and 30 optional. Official Elecbyte docs define ReversalDef contact, `reversal.attr`, and `p1stateno`/`p2stateno`; Projectile docs were checked while rejecting unclear projectile reflection/removal scope. Claim allowed: bounded direct owner-backed `p2stateno` custom-state entry can be countered by active ReversalDef before state `889` is entered, then route attacker through ReversalDef `p2stateno`. Claim blocked: guard/custom-state breadth beyond this direct route, projectile reflection/removal semantics after reversal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, score movement, and full ReversalDef parity.
- Previous R1 helper Projectile ReversalDef checkpoint: required `synthetic-imported-helper-projectile-reversal.json` checksum `a1d82380` / final checksum `ca66a49a` promotes bounded helper-parented/root-owned Projectile `ReversalDef` priority into `pnpm qa:trace`. A visual Helper spawns Projectile id `8878` with `ownerId = p1`, `rootId = p1`, and `parentId = p1-helper-0`; the trace stops at the initial reversal contact, requires typed `helper` / `projectile` / `reversaldef` operation evidence plus reversal event/combat-reason evidence, keeps helper target memory empty and the Projectile payload unconsumed at the sampled frame, forbids hit/guard/override/reject routes, and ends P2 in state/action `777` while P1 enters owner-backed state/action `888`, both at life `1000`. That trace aggregate was 468/468 artifacts, 438 required and 30 optional.
- Previous R1 player Projectile ReversalDef checkpoint: required `synthetic-imported-projectile-reversal.json` checksum `5c4ddf48` / final checksum `bb0bbb99` promotes bounded player-owned Projectile `ReversalDef` priority into `pnpm qa:trace`. That route executes imported Projectile id `77`, stops at the initial reversal contact, requires typed `projectile` / `reversaldef` operation evidence plus reversal event/combat-reason evidence, keeps the Projectile payload unconsumed at the sampled frame, and ends P2 in state/action `777` while P1 enters owner-backed state/action `888`, both at life `1000`.
- Previous R1 SuperPause unhittable checkpoint: required `synthetic-imported-superpause-unhittable.json` checksum `1598af8f` / final checksum `f59f5704` promotes Elecbyte's default `unhittable = 1` source immunity into `pnpm qa:trace` as bounded match-pause combat metadata. The route executes imported SuperPause while a demo striker attempts same-tick direct contact, requires match-pause/freeze plus reject evidence `via SuperPause unhittable`, and proves the source actor stays at life `1000`. Current trace aggregate is 466/466 artifacts, 436 required and 30 optional. Official Elecbyte docs define `unhittable` as making the player unable to be hit during SuperPause by default and `unhittable = 0` as the opt-out. Claim allowed: bounded imported default SuperPause prevents direct same-tick contact against the pause source through the match-pause combat filter; focused runtime tests cover direct, player Projectile, and helper-direct rejection plus `unhittable = 0` opt-out metadata. Claim blocked: exact MUGEN/IKEMEN projectile/helper/team breadth, reversal priority, exact pause layering, renderer/super-background presentation, score movement, and full SuperPause parity.
- Previous R1 SuperPause pausebg checkpoint: required `synthetic-imported-superpause-pausebg.json` checksum `49bcfe16` / final checksum `397a8fae` promotes Elecbyte's `pausebg = 0` background-update flag into `pnpm qa:trace` as bounded metadata. The route executes imported `SuperPause pausebg = 0` and now requires match-pause/freeze evidence plus trace-gated `pauseBg = false`. That trace aggregate passed 465/465 artifacts, 435 required and 30 optional. Official Elecbyte docs define `pausebg = 0` as letting the background continue updating during pause and default `pausebg = 1` as stopping it. Claim allowed: bounded imported SuperPause with `pausebg = 0` carries `pauseBg = false` through match-pause snapshot and trace evidence. Claim blocked: actual renderer/background update parity, exact stage/BGCtrl pause timing, renderer visual suppression/playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, super backgrounds, helper/redirect ownership, score movement, and full super presentation parity.
- Previous R1 SuperPause anim-disabled checkpoint: required `synthetic-imported-superpause-anim-disabled.json` checksum `fc7a2ca4` / final checksum `5be3ca6c` promotes Elecbyte's `anim = -1` no-animation route into `pnpm qa:trace`. The route executes imported `SuperPause anim = -1` and requires match-pause/freeze evidence plus trace-gated absence of `superAnim` metadata through `superAnimAbsent = true`.
- Previous R1 SuperPause default anim checkpoint: required `synthetic-imported-superpause-default-anim.json` checksum `318c5e9f` / final checksum `747e7619` promotes Elecbyte's omitted-`anim` default into `pnpm qa:trace`. The route executes imported `SuperPause` without an `anim` param and requires match-pause/freeze evidence plus trace-gated `superAnim` metadata: raw `30`, source `fightfx`, action `30`, and offset `0,0`.
- Previous R1 SuperPause dynamic anim/pos checkpoint: required `synthetic-imported-superpause-dynamic-anim-pos.json` checksum `e6bfbf75` / final checksum `eb49d9db` promotes bounded dynamic `SuperPause anim = var(6)` plus `pos = var(7),var(8)` metadata into `pnpm qa:trace`. The route requires match-pause/freeze evidence and trace-gated `superAnim` fields: raw `var(6)`, source `fightfx`, action `7001`, and offset `18,-36`.
- Previous R1 SuperPause anim/pos checkpoint: required `synthetic-imported-superpause-anim-pos.json` checksum `f7dcdc9d` / final checksum `7bd4afe8` promotes bounded explicit `SuperPause anim = S200` plus `pos = 24,-48` metadata into `pnpm qa:trace`.
- Previous R1 SuperPause dynamic params checkpoint: required `synthetic-imported-superpause-dynamic-params.json` current checksum `052bb481` / final checksum `1847a3f3` promotes bounded dynamic `SuperPause time/movetime/darken/poweradd` fallback into `pnpm qa:trace`. The route seeds `var(2..5)`, observes remaining `9`, movetime `2`, `darken = false`, and final P1 power `75`. Its current checksum intentionally includes default `superAnim` metadata from the default-anim cut.
- Previous R1 dynamic HitDef guardsound checkpoint: required `synthetic-imported-hitdef-dynamic-guardsound.json` checksum `cb061b1c` / final checksum `8d25e54e` promotes bounded guarded direct-contact `HitDef guardsound = Fvar(0),var(1)` into `pnpm qa:trace`. The route seeds `var(0)=6` and `var(1)=4`, activates the `HitDef`, forces a guard contact, and requires attacker-side `PlaySnd` contact telemetry for group `6`, index `4`, raw `Fvar(0),var(1)`, `contactKind = guard`, and `soundPrefix = kfm` without typed `audio:*` evidence. That trace aggregate passed 457/457 artifacts, 427 required and 30 optional. Official Elecbyte docs define numeric controller params as expression-capable unless otherwise specified and define `HitDef hitsound/guardsound` as sound group/index pairs. Claim allowed: bounded imported direct `HitDef` guard-sound refs can resolve expression-backed group/index values at HitDef activation and carry F-prefixed FightFX sound metadata into guard contact sound telemetry. Claim blocked: exact SND playback/archive lookup, channel priority/timing/mixing, helper/redirect ownership, score movement, and full audio parity.
- Previous R1 dynamic HitDef hitsound checkpoint: required `synthetic-imported-hitdef-dynamic-hitsound.json` checksum `c891e888` / final checksum `a0d1bbfc` promotes bounded direct-contact `HitDef hitsound = Fvar(0),var(1)` into `pnpm qa:trace`. The route seeds `var(0)=5` and `var(1)=4`, activates the `HitDef`, and requires attacker-side `PlaySnd` contact telemetry for group `5`, index `4`, raw `Fvar(0),var(1)`, and `soundPrefix = kfm` without typed `audio:*` evidence. That trace aggregate passed 456/456 artifacts, 426 required and 30 optional. Claim allowed: bounded imported direct `HitDef` hit-sound refs can resolve expression-backed group/index values at HitDef activation and carry F-prefixed FightFX sound metadata into contact sound telemetry. Claim blocked: SuperPause sound refs, exact SND playback/archive lookup, channel priority/timing/mixing, helper/redirect ownership, score movement, and full audio parity.
- Previous R1 dynamic sound-value checkpoint: required `synthetic-imported-sound-dynamic-value.json` checksum `cd0bf458` / final checksum `0ded35cd` promotes bounded active imported dynamic `PlaySnd value` group/index params into `pnpm qa:trace`. The route seeds `var(0)=5` and `var(1)=3`, executes `PlaySnd value = Fvar(0),var(1), channel = 4`, and requires sound-event telemetry for `PlaySnd` group `5`, index `3`, channel `4`, and `soundPrefix = kfm`. Typed `audio:*` operation evidence stays absent while `variable:varset` and `hitdef` evidence remain present. That trace aggregate passed 455/455 artifacts, 425 required and 30 optional. Official Elecbyte docs define `PlaySnd value = group_no, sound_no`, allow `F` prefix for common/fight SND, and state numeric controller params are expression-capable unless otherwise specified. Claim allowed: bounded active imported dynamic `PlaySnd value` group/index refs, including F-prefixed FightFX sound metadata, can resolve through runtime expression fallback into trace-gated sound-event telemetry. Claim blocked: SuperPause sound refs, typed-operation lowering for dynamic audio params, exact Web Audio archive lookup/panning/channel priority/timing/mixing, helper/redirect ownership, score movement, and full audio parity.
- Previous R1 dynamic sound-pan checkpoint: required `synthetic-imported-sound-dynamic-pan.json` checksum `24c0cce2` / final checksum `dea16ed4` promotes bounded active imported dynamic `PlaySnd` / `SndPan` / `StopSnd` numeric audio params into `pnpm qa:trace`. The route seeds `var(0)=-24`, `var(1)=2`, and `var(2)=64`, executes `PlaySnd value = S5,2, channel = var(1), pan = var(0)`, `SndPan channel = var(1), abspan = var(2)`, and `StopSnd channel = var(1)`, then requires sound-event telemetry for `PlaySnd` channel `2` pan `-24`, `SndPan` channel `2` absPan `64`, and `StopSnd` channel `2`. Typed `audio:*` operation evidence stays absent while `variable:varset` and `hitdef` evidence remain present. That trace aggregate passed 454/454 artifacts, 424 required and 30 optional. Official Elecbyte docs define `SndPan channel/pan/abspan`, `StopSnd channel`, and numeric controller params as expression-capable unless otherwise specified. Claim allowed: bounded active imported dynamic audio numeric params can resolve through runtime expression fallback into trace-gated sound-event telemetry. Claim blocked: typed-operation lowering for dynamic audio params, exact Web Audio panning, channel priority/timing/mixing, helper/redirect ownership, score movement, and full audio parity.
- Previous R1 PlayerPush dynamic checkpoint: required `synthetic-imported-playerpush-dynamic.json` checksum `13c5f954` / final checksum `0627d0e5` promotes bounded active imported dynamic `PlayerPush value` expression fallback into `pnpm qa:trace`. The route seeds `var(0)=0`, executes `PlayerPush value = var(0)`, then requires imported actor-frame/final `playerPush = false`; typed `collision:playerpush` operation evidence stays absent while `variable:varset` and `hitdef` evidence remain present. That trace aggregate passed 453/453 artifacts, 423 required and 30 optional. Official Elecbyte docs define `PlayerPush value` as a one-tick push-checking flag and numeric controller params may be expressions. Claim allowed: bounded active imported dynamic `PlayerPush value` params can resolve through runtime expression fallback into trace-gated body-push telemetry. Claim blocked: dynamic typed-operation lowering for `PlayerPush`, exact overlap resolution, team/helper ownership, exact tick order, score movement, and full constraint parity.
- Previous R1 Width dynamic checkpoint: required `synthetic-imported-width-dynamic.json` checksum `79baa5de` / final checksum `395b0b1a` promotes bounded active imported dynamic `Width player` expression fallback into `pnpm qa:trace`. The route seeds `var(0)=21` and `var(1)=43`, executes `player = var(0),var(1)`, then requires imported actor-frame/final body-width telemetry `front = 21`, `back = 43`; typed `collision:width` operation evidence stays absent while `variable:varset` and `hitdef` evidence remain present. That trace aggregate passed 452/452 artifacts, 422 required and 30 optional. Official Elecbyte docs define `Width` as a temporary 1-tick width-bar change with `player` and alternate `value` params, and numeric controller params may be expressions. Claim allowed: bounded active imported dynamic `Width player` params can resolve through active controller expression fallback into trace-gated body-width telemetry. Claim blocked: dynamic typed-operation lowering for `Width`, edge width parity, exact push overlap, team/helper ownership, exact tick order, score movement, and full constraint parity.
- Previous R1 EnvColor dynamic checkpoint: required `synthetic-imported-envcolor-dynamic.json` checksum `dbe548a7` / final checksum `2ff8dd42` promotes bounded active imported dynamic `EnvColor value/time/under` fallback into `pnpm qa:trace`. The route seeds `var(0)=32`, `var(1)=128`, `var(2)=240`, `var(3)=14`, and `var(4)=1`, executes `value = var(0),var(1),var(2)`, `time = var(3)`, and `under = var(4)`, then requires stage-frame color telemetry `32,128,240` with `under = true`; typed `envcolor` operation evidence stays absent. That trace aggregate passed 451/451 artifacts, 421 required and 30 optional. Official Elecbyte docs define `EnvColor value`, `time`, and `under`, and numeric controller params may be expressions. Claim allowed: bounded active imported dynamic `EnvColor` params can resolve through active controller expression fallback into trace-gated stage-flash telemetry. Claim blocked: dynamic typed-operation lowering for `EnvColor`, exact MUGEN/IKEMEN blend math, layer/window behavior, pause timing, renderer parity, score movement, and full presentation parity.
- Previous R1 EnvShake dynamic checkpoint: required `synthetic-imported-envshake-dynamic.json` checksum `90955e75` / final checksum `1af5fbf6` promotes bounded active imported dynamic `EnvShake time/freq/ampl/phase` fallback into `pnpm qa:trace`. The route seeds `var(0)=18`, `var(1)=45`, `var(2)=-9`, and `fvar(0)=0.25`, executes `time = var(0)`, `freq = var(1)`, `ampl = var(2)`, and `phase = fvar(0)`, then requires imported runtime camera-shake telemetry `time = 18`, `freq = 45`, `ampl = -9`, and `phase = 0.25`; typed `envshake` operation evidence stays absent. That trace aggregate passed 450/450 artifacts, 420 required and 30 optional. Official Elecbyte docs define `EnvShake time` as required shake duration, `freq` as shake speed, `ampl` as shake amplitude, and `phase` as phase offset. Claim allowed: bounded active imported dynamic `EnvShake` params can resolve through active controller expression fallback into trace-gated camera-shake telemetry. Claim blocked: dynamic typed-operation lowering for `EnvShake`, `mul` support, exact MUGEN/IKEMEN camera waveform, pause/stage/layer interaction, helper ownership, screenpack ownership, score movement, and full presentation parity.
- Previous R1 AngleMul dynamic checkpoint: required `synthetic-imported-anglemul-dynamic.json` checksum `418ed880` / final checksum `4a6a3045` promotes bounded active imported dynamic `AngleMul value = fvar(0)` fallback into `pnpm qa:trace`. The route seeds `var(0)=30` and `fvar(0)=1.5`, executes `AngleSet value = var(0)`, `AngleMul value = fvar(0)`, and static `AngleDraw`, then requires imported actor-frame telemetry `renderAngle = 45`; typed `sprite-effect:angleset` and `sprite-effect:anglemul` evidence stay absent while static `sprite-effect:angledraw` remains present. That trace aggregate passed 449/449 artifacts, 419 required and 30 optional. Official Elecbyte docs define `AngleMul value` as an `angle_multiplier` float that multiplies the drawing angle used by `AngleDraw`. Claim allowed: bounded active imported dynamic `AngleMul value` can resolve through the sprite-effect expression fallback and multiply render-angle telemetry. Claim blocked: dynamic typed-operation lowering for `AngleMul`, exact MUGEN/IKEMEN axis pivot, collision rotation/scale, draw-order interaction, palette interaction, helper/redirect ownership, score movement, and full presentation parity.
- Previous R1 AngleMul checkpoint: required `synthetic-imported-anglemul.json` checksum `e0dae072` / final checksum `5048aa5c` promotes static `AngleMul value` into typed `sprite-effect:anglemul` evidence. The route executes `AngleSet value = 30`, `AngleMul value = 1.5`, and `AngleDraw`, then requires imported actor-frame telemetry `renderAngle = 45`. That trace aggregate passed 448/448 artifacts, 418 required and 30 optional. Claim allowed: bounded active imported static `AngleMul` can multiply sprite rotation state and reach trace-gated render-angle telemetry. Claim blocked: exact MUGEN/IKEMEN axis pivot, collision rotation/scale, draw-order interaction, palette interaction, helper/redirect ownership, score movement, and full presentation parity.
- Previous R1 Angle dynamic checkpoint: required `synthetic-imported-angle-dynamic.json` checksum `8f788bf8` promotes bounded active-state dynamic `AngleSet value`, `AngleAdd value`, and `AngleDraw value/scale` expression fallback into `pnpm qa:trace`. The route seeds owner-local `var(0..3)` plus `fvar(0)=0.5`, executes `AngleSet value = var(0)`, `AngleAdd value = var(1)`, and `AngleDraw value = var(2), scale = var(3),fvar(0)`, and requires imported actor-frame/final telemetry `renderAngle = 35` plus `renderScale = 2,0.5` without typed `sprite-effect:angle*` operation evidence. That trace aggregate is 447/447 artifacts, 417 required and 30 optional. Official Elecbyte docs define `AngleSet value`, `AngleDraw value`, and `AngleDraw scale` as floats and state rotation/scaling does not affect collision boxes. Claim allowed: bounded active imported dynamic `Angle*` params can resolve through the sprite-effect boundary into render-angle/render-scale telemetry. Claim blocked: typed-operation lowering for dynamic angle params, exact MUGEN/IKEMEN axis pivot, collision rotation/scale, draw-order interaction, palette interaction, helper/redirect ownership, score movement, and full presentation parity.
- Previous R1 AfterImageTime dynamic checkpoint: required `synthetic-imported-afterimagetime-dynamic.json` checksum `16edc106` promotes bounded active-state dynamic `AfterImageTime value/time` expression fallback into `pnpm qa:trace`. The route seeds owner-local `var(0)=14`, runs static `AfterImage` plus `AfterImageTime value = var(0)`, and requires imported actor-frame/final telemetry `afterImageTime = 14`, `afterImageLength = 4`, `afterImageTimeGap = 1`, `afterImageFrameGap = 1`, at least one sample, and opacity `0.34` without typed `sprite-effect:afterimagetime` operation evidence. That trace aggregate passed 446/446 artifacts, 416 required and 30 optional. Official Elecbyte docs state numeric controller params may be expressions and define `AfterImageTime time` plus alternate `value`. Claim allowed: bounded active imported dynamic `AfterImageTime` params can resolve through the sprite-effect boundary into ghost-trail duration telemetry. Claim blocked: typed-operation lowering for dynamic AfterImageTime params, exact no-active-afterimage behavior, trail blending, palette math, sampling cadence, renderer parity, helper/redirect ownership, score movement, and full MUGEN/IKEMEN presentation parity.
- Previous R1 AfterImage dynamic checkpoint: required `synthetic-imported-afterimage-dynamic.json` checksum `2342c3f1` promotes bounded active-state dynamic `AfterImage` expression fallback into `pnpm qa:trace`. The route seeds owner-local `var(0..7)`, executes `AfterImage time/length/timegap/framegap/paladd/palmul` from expressions with `trans = add`, and requires imported actor-frame/final telemetry `afterImageTime = 18`, `afterImageLength = 5`, `afterImageTimeGap = 2`, `afterImageFrameGap = 3`, at least one sample, and opacity `0.34` without typed `sprite-effect:afterimage` operation evidence. That trace aggregate passed 445/445 artifacts, 415 required and 30 optional. Official Elecbyte docs state numeric controller params may be expressions and define `AfterImage` `time`, `length`, `timegap`, `framegap`, `paladd`, `palmul`, and `trans`. Claim allowed: bounded active imported dynamic `AfterImage` params can resolve through the sprite-effect boundary into ghost-trail telemetry. Claim blocked: typed-operation lowering for dynamic AfterImage params, exact trail blending, palette math, sampling cadence, renderer parity, helper/redirect ownership, score movement, and full MUGEN/IKEMEN presentation parity.
- Previous R1 Trans dynamic checkpoint: required `synthetic-imported-trans-dynamic.json` checksum `91a7baf9` promotes bounded active-state dynamic `Trans alpha` expression fallback into `pnpm qa:trace`. The route seeds owner-local `var(0)=96` and `var(1)=160`, executes `Trans trans = addalpha` plus `alpha = var(0),var(1)`, and requires imported actor-frame/final telemetry `renderOpacity = 0.375` without typed `sprite-effect:trans` operation evidence. That trace aggregate passed 444/444 artifacts, 414 required and 30 optional. Official Elecbyte docs state numeric controller params may be expressions and define `Trans` `alpha = source_alpha, dest_alpha` for additive transparency. Claim allowed: bounded active imported dynamic `Trans alpha` params can resolve through the sprite-effect boundary into render-opacity telemetry. Claim blocked: typed-operation lowering for dynamic alpha, exact add/sub alpha math, palette/remap interaction, draw-order parity, renderer parity, helper/redirect ownership, score movement, and full MUGEN/IKEMEN presentation parity.
- Previous R1 PalFX dynamic checkpoint: required `synthetic-imported-palfx-dynamic.json` checksum `c56e955a` promotes bounded active-state dynamic `PalFX time/add/mul/color/invertall` expression fallback into `pnpm qa:trace`. The route seeds owner-local `var(0..7)`, executes dynamic PalFX material params, and requires imported actor-frame/final telemetry `paletteFx.time = 12`, `add [64,-16,255]`, `mul [224,144,256]`, `color = 200`, and `invert = true` without typed `sprite-effect:palfx` operation evidence. That trace aggregate passed 443/443 artifacts, 413 required and 30 optional. Official Elecbyte docs state numeric controller params may be expressions and define `PalFX` `time`, `add`, `mul`, `invertall`, and `color`. Claim allowed: bounded active imported dynamic `PalFX` material params can resolve through the sprite-effect boundary into palette-effect telemetry. Claim blocked: typed-operation lowering for dynamic params, `sinadd`, exact palette math/blend/remap order, renderer parity, helper/redirect ownership, score movement, and full MUGEN/IKEMEN presentation parity.
- Previous R1 SprPriority dynamic checkpoint: required `synthetic-imported-sprpriority-dynamic.json` checksum `b57c1bfa` promotes bounded active-state dynamic `SprPriority value` expression fallback into `pnpm qa:trace`. The route seeds owner-local `var(0) = 4`, executes `SprPriority value = var(0)`, and requires imported actor-frame/final telemetry `spritePriority = 4` without typed `sprite-effect:sprpriority` operation evidence. That checkpoint passed 442/442 artifacts, 412 required and 30 optional. Official Elecbyte docs state numeric controller params may be expressions and define `SprPriority value` as an int priority level. Claim allowed: bounded active imported dynamic `SprPriority value` params can resolve through the sprite-effect boundary into sprite-priority telemetry. Claim blocked: typed-operation lowering for dynamic params, exact layer/shadow/helper/Explod draw-order parity, renderer parity, helper/redirect ownership, score movement, and full MUGEN/IKEMEN presentation parity.
- Previous R1 RemapPal dynamic checkpoint: required `synthetic-imported-remappal-dynamic.json` checksum `a44ec542` promotes bounded active-state dynamic `RemapPal source/dest` expression fallback into `pnpm qa:trace`. The route seeds owner-local `var(0)` / `var(1)`, executes `RemapPal source = 1,var(0)` and `dest = 2,var(1)`, and requires final imported actor `paletteRemap` telemetry `source [1,5] -> dest [2,7]`. That checkpoint passed 441/441 artifacts, 411 required and 30 optional. Official Elecbyte docs state numeric controller params may be expressions and define `RemapPal` `source` / `dest`. Claim allowed: bounded active imported dynamic `RemapPal` params can resolve through the sprite-effect boundary into palette-remap telemetry. Claim blocked: typed-operation lowering for dynamic params, exact source-bank/default/removal semantics, ACT/SFF pixel parity, truecolor/PNG remap, helper/redirect ownership, exact PalFX order/math, renderer parity, score movement, and full MUGEN/IKEMEN palette parity.
- Current R1 AssertSpecial checkpoint: required `synthetic-imported-assertspecial-juggle-telemetry.json` checksum `9436dfa0` promotes bounded official `AssertSpecial NoJuggleCheck` telemetry into `pnpm qa:trace`. Static `NoJuggleCheck` lowers into typed `assertspecial` operation evidence, runtime execution stores `noJuggleCheck`, and final imported actor evidence requires normalized `assertSpecialFlags: ["nojugglecheck"]`. Current trace aggregate is 440/440 artifacts, 410 required and 30 optional. Official Elecbyte `AssertSpecial` docs list `nojugglecheck`. Claim allowed: bounded parsing, normalization, runtime flag storage, and final-actor telemetry for `NoJuggleCheck`. Claim blocked: juggle-point accounting, actual juggle bypass behavior, helper/team/global ownership, pause layering, score movement, and full MUGEN/IKEMEN juggle parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projtime-same-id-last-contact.json` checksum `4e74aec3` promotes bounded helper-local same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` guard-then-hit last-contact-kind arbitration into `pnpm qa:trace`. A visual Helper spawns two helper-parented owner-side Projectiles with id `8918` (`ownerId/rootId = p1`, `parentId = p1-helper-0`): first contact is guarded and emits `S6,34`, FightFX `F7038`, and `sparkxy = 35,-77`; later contact hits and emits `S5,35`, FightFX `F7038`, and `sparkxy = 36,-78`. Helper route `1200 -> 1306 -> 1307` only succeeds when fixed-id plus ID `0` hit/contact time reads are active and fixed-id plus ID `0` guarded time reads are inactive; forbidden helper state `1308` proves stale helper-local same-id guarded time does not survive the later hit. Current trace aggregate is 439/439 artifacts, 409 required and 30 optional. Official Elecbyte docs define `Proj*Time` as last-projectile-contact reads where ID `0` skips projectile-ID filtering, and Elecbyte Projectile docs state helper-created projectiles become root-owned. Together with `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b`, both helper-local same-ID two-contact orders are now required trace gates. This is bounded helper-local owner-side same-ID timing evidence only; exact Proj* tick order/lifetime, helper custom-state breadth beyond these routes, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b` promotes bounded helper-local same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` hit-then-guard last-contact-kind arbitration into `pnpm qa:trace`. A visual Helper spawns two helper-parented owner-side Projectiles with id `8917` (`ownerId/rootId = p1`, `parentId = p1-helper-0`): first contact hits and emits `S5,32`, FightFX `F7037`, and `sparkxy = 33,-75`; later contact is guarded and emits `S6,33`, FightFX `F7037`, and `sparkxy = 34,-76`. Helper route `1200 -> 1303 -> 1304` only succeeds when fixed-id plus ID `0` guarded/contact time reads are active and fixed-id plus ID `0` hit time reads are inactive; forbidden helper state `1305` proves stale helper-local same-id hit time does not survive the later guard. Runtime fix: helper-local `runtimeHelperProjectileContactTime` now selects the latest helper-parented Projectile contact for the requested id/any-id before checking hit vs guard kind, instead of aggregating stale kind-specific times. That checkpoint passed 438/438 artifacts, 408 required and 30 optional. Official Elecbyte docs define `Proj*Time` as last-projectile-contact reads where ID `0` skips projectile-ID filtering, and Elecbyte Projectile docs state helper-created projectiles become root-owned. This is bounded helper-local owner-side same-ID hit-then-guard timing evidence only; exact Proj* tick order/lifetime, helper custom-state breadth beyond this route, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projtime-same-id-hit-then-guard.json` checksum `d49ee334` completes the bounded player-owned same-ID `Proj*Time` two-order pair with hit-then-guard last-contact-kind arbitration. P1 spawns two Projectiles with id `8916`: first contact hits and emits `S5,30`, FightFX `F7036`, and `sparkxy = 31,-73`; later contact is guarded and emits `S6,31`, FightFX `F7036`, and `sparkxy = 32,-74`. Owner state routes `200 -> 383 -> 384` only when `ProjGuardedTime(8916) >= 1`, `ProjGuardedTime(0) >= 1`, `ProjContactTime(8916) >= 1`, `ProjContactTime(0) >= 1`, `ProjHitTime(8916) < 0`, and `ProjHitTime(0) < 0`; forbidden state `385` proves stale same-id hit time does not survive the later guard. That checkpoint passed 437/437 artifacts, 407 required and 30 optional. Official Elecbyte docs define `Proj*Time` as last-projectile-contact reads where ID `0` skips projectile-ID filtering; Elecbyte CNS docs list legacy `ProjHit` / `ProjContact` / `ProjGuarded` as superseded by `Proj*Time`. This is bounded two-contact same-id owner-state timing evidence only; exact Proj* tick order/lifetime, helper custom-state breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projtime-same-id-last-contact.json` checksum `fb4c2450` promotes bounded player-owned same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` guard-then-hit last-contact-kind arbitration into `pnpm qa:trace`. P1 spawns two same-id Projectiles with id `8915`: first contact is guarded and emits `S6,28`, FightFX `F7035`, and `sparkxy = 29,-71`; later contact hits and emits `S5,29`, FightFX `F7035`, and `sparkxy = 30,-72`. Owner state routes `200 -> 380 -> 381` only when `ProjHitTime(8915) >= 1`, `ProjHitTime(0) >= 1`, `ProjContactTime(8915) >= 1`, `ProjContactTime(0) >= 1`, `ProjGuardedTime(8915) < 0`, and `ProjGuardedTime(0) < 0`; forbidden state `382` proves stale same-id guarded time does not survive the later hit. That checkpoint passed 436/436 artifacts, 406 required and 30 optional. Official Elecbyte docs define `Proj*Time` as last-projectile-contact reads where ID `0` skips projectile-ID filtering; Elecbyte CNS docs list legacy `ProjHit` / `ProjContact` / `ProjGuarded` as superseded by `Proj*Time`. This remains required as the guard-then-hit half of the same-id pair; exact Proj* tick order/lifetime, helper Projectile/custom-state persistence breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projhittime-multi-id.json` checksum `5d897825`, `synthetic-imported-projectile-projcontacttime-multi-id.json` checksum `d9b3cecf`, and `synthetic-imported-projectile-projguardedtime-multi-id.json` checksum `e52d0d01` promote bounded player-owned Projectile `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` multi-id arbitration evidence into `pnpm qa:trace`. P1 spawns two Projectiles per route: wrong-id/non-contact ids `8909` / `8911` / `8913` stay isolated by forbidden trap states `373` / `376` / `379`, while valid hit/contact/guard ids `8910` / `8912` / `8914` route owner state `200 -> 371 -> 372`, `200 -> 374 -> 375`, and `200 -> 377 -> 378` through fixed-id `ProjHitTime(8910) >= 1` / `ProjContactTime(8912) >= 1` / `ProjGuardedTime(8914) >= 1` plus ID `0` any-projectile reads. Evidence includes two Projectile controller/op executions per route, two active Projectile payloads, owner target-links for ids `8910` / `8912` / `8914`, hit sound `S5,25`, guard sounds `S6,26` / `S6,27`, FightFX `F7032` / `F7033` / `F7034`, and authored `sparkxy = 26,-68` / `27,-69` / `28,-70`. Current trace aggregate is 435/435 artifacts, 405 required and 30 optional. Official Elecbyte docs define `ProjHitTime`, `ProjContactTime`, and `ProjGuardedTime` as required-ID trigger reads where ID `0` skips projectile-ID filtering and no matching last contact returns `-1`; Elecbyte CNS docs list the legacy `ProjHit` / `ProjContact` / `ProjGuarded` forms as superseded by the `Proj*Time` triggers. This is bounded two-projectile owner-state timing arbitration evidence only; exact Proj* tick order/lifetime, same-ID selection priority, helper Projectile/custom-state persistence breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, visual/audio parity beyond the bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projhit-multi-id.json` checksum `ab0f3fb3` and `synthetic-imported-projectile-projguarded-multi-id.json` checksum `023921e3` promote bounded player-owned Projectile `ProjHit` / `ProjGuarded` multi-id arbitration evidence into `pnpm qa:trace`. P1 spawns two Projectiles per route: wrong-id/non-contact ids `8905` / `8907` stay isolated by forbidden trap states `367` / `370`, while valid hit/guard ids `8906` / `8908` route owner state `200 -> 365 -> 366` and `200 -> 368 -> 369` through fixed-id `ProjHit8906 = 1, >= 1` / `ProjGuarded8908 = 1, >= 1`, omitted-ID `ProjHit = 1, >= 1` / `ProjGuarded = 1, >= 1`, and ID `0` first-form `ProjHit0 = 1` / `ProjGuarded0 = 1`. Evidence includes two Projectile controller/op executions per route, two active Projectile payloads, owner target-links for ids `8906` / `8908`, hit sound `S5,23`, guard sound `S6,24`, FightFX `F7030` / `F7031`, and authored `sparkxy = 24,-66` / `25,-67`. That checkpoint passed 432/432 artifacts, 402 required and 30 optional, and remains required. Official Elecbyte docs define fixed ID filters plus omitted-ID any-projectile behavior for `ProjHit` / `ProjGuarded`, and ID `0` equivalence for `ProjGuarded`. This is bounded two-projectile owner-state arbitration evidence only; exact Proj* tick order/lifetime, same-ID selection priority, helper Projectile/custom-state persistence breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, score movement, and full MUGEN/IKEMEN Projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projcontact-multi-id.json` checksum `e790ec3e` promotes bounded player-owned Projectile `ProjContact` multi-id arbitration evidence into `pnpm qa:trace`. P1 spawns two Projectiles: wrong-id/non-contact Projectile id `8903` and valid guarded Projectile id `8904`. Owner state `200` must not route through forbidden wrong-id trap state `364`, then routes `200 -> 362 -> 363` only when fixed-id `ProjContact8904 = 1, >= 1` and any-id `ProjContact = 1, >= 1` are both true, with an additional ID `0` first-form `ProjContact0 = 1` branch, two active Projectile payloads, owner target-link for id `8904`, guard sound `S6,22`, FightFX `F7029`, and authored `sparkxy = 23,-65` evidence. That checkpoint passed 430/430 artifacts, 400 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projcontact-suffix-any.json` checksum `2fb80418` promotes bounded Elecbyte omitted-ID / ID `0` any-projectile legacy Projectile `ProjContact` suffix syntax into `pnpm qa:trace`. P1 Projectile id `8902` is guarded and routes owner `200 -> 360 -> 361` through omitted-ID second-form `ProjContact = 1, >= 1` plus ID `0` first-form `ProjContact0 = 1`, keeping active post-contact projectile payload, owner target-link, guard sound `S6,21`, FightFX `F7028`, and authored `sparkxy = 22,-64` evidence. That checkpoint passed 429/429 artifacts, 399 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projhit-suffix-any.json` checksum `35ffd57d` and `synthetic-imported-projectile-projguarded-suffix-any.json` checksum `4000bc4f` promote bounded omitted-ID / ID `0` any-projectile legacy Projectile `ProjHit` and `ProjGuarded` suffix syntax into `pnpm qa:trace`. That checkpoint passed 428/428 artifacts, 398 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projhit-suffix.json` checksum `dd3db5ee` and `synthetic-imported-projectile-projguarded-suffix.json` checksum `80bbe439` promote bounded fixed-id Elecbyte legacy Projectile `ProjHit[ID] = value, [oper] value2` and `ProjGuarded[ID] = value, [oper] value2` suffix syntax into `pnpm qa:trace`. That checkpoint passed 426/426 artifacts, 396 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projcontact-suffix.json` checksum `c904ded7` proves bounded Elecbyte legacy Projectile `ProjContact[ID] = value, [oper] value2` suffix syntax through owner `200 -> 350 -> 351` after guarded Projectile id `8897` contact with active projectile payload, owner target-link, guard event/reason, and guard sound/FightFX package evidence. That checkpoint passed 424/424 artifacts, 394 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projcontactpersist.json` checksum `8e678b1b` promotes bounded player-owned Projectile `ProjContact` state-transition evidence into `pnpm qa:trace`. P1 spawns Projectile id `8896` with `projremove = 0`, P2 guards it, owner state `200` observes `ProjContact(8896)` / `ProjContactTime(8896)`, transitions into state `348`, and branches to state `349` from that later owner `StateDef` while preserving active post-contact projectile payload, owner target-link, guard event/reason, and guard sound/FightFX package evidence. This remains required and does not claim exact tick order/lifetime, multi-projectile selection, helper/custom-state breadth, redirects, teams, score movement, or full Projectile parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projcontactpersist.json` checksum `65639428` promotes bounded helper Projectile `ProjContact` state-transition evidence into `pnpm qa:trace`. A visual Helper spawns helper-parented owner-side Projectile id `8895`, P2 guards it, helper state `1300` observes `ProjContact(8895)` / `ProjContactTime(8895)`, transitions into helper state `1302`, and then branches to helper state `1301` from that later StateDef while preserving helper/projectile lifecycle and owner target-link evidence. That trace aggregate was 422/422 artifacts, 392 required and 30 optional. This is bounded helper-local ProjContact/ProjContactTime state-transition evidence only; exact helper Projectile contact lifetime/order, multi-projectile selection, helper-owned custom-state targets, score movement, and full helper Projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-movereversedpersist.json` checksum `ef8ffdf5` promotes bounded helper-owned reversed `StateDef movehitpersist` into `pnpm qa:trace`. A helper-local `HitDef` is countered by defender-side `ReversalDef`, records helper-side `MoveReversed`, immediately transitions into helper state `1232` with `movehitpersist = 1`, keeps `MoveReversed >= 1` readable while `MoveContact = 0`, `MoveHit = 0`, `MoveGuarded = 0`, `HitCount = 0`, and `UniqHitCount = 0`, and routes helper `1200 -> 1232 -> 1233` with defender reversal state `779` evidence. That trace aggregate was 421/421 artifacts, 391 required and 30 optional. Official Elecbyte CNS docs define `movehitpersist` as the StateDef flag that carries Move* trigger info from the previous state; Elecbyte Trigger Reference defines MoveReversed as the trigger for an attack reversed by P2 and says MoveContact/MoveGuarded/MoveHit/MoveReversed reset after state transition unless `movehitpersist` overrides it; Elecbyte ReversalDef docs define the counter side-effect. This is bounded helper-owned reversed Move* state-transition evidence only; helper Projectile/custom-state movehitpersist breadth, exact reversal priority/target-state semantics, multi-hit/multi-target/team counting, exact target lifetime/tick order, exact helper hitpause lifetime, score movement, and full MUGEN/IKEMEN helper Move* lifetime parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-moveguardedpersist.json` checksum `d5ce7897` promotes bounded helper-owned guarded `StateDef movehitpersist` into `pnpm qa:trace`. A helper-local guarded `HitDef` contact records helper-side `MoveContact` / `MoveGuarded`, immediately transitions into helper state `1230` with `movehitpersist = 1`, keeps those Move* triggers readable while `MoveHit = 0`, `HitCount = 0`, and `UniqHitCount = 0`, and routes helper `1200 -> 1230 -> 1231` with helper-owned guard sound/FightFX evidence. That trace aggregate was 420/420 artifacts, 390 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-movehitpersist.json` checksum `2354ef95` promotes bounded helper-owned `StateDef movehitpersist` into `pnpm qa:trace`. A helper-local `HitDef` contact records helper-side `MoveContact` / `MoveHit`, immediately transitions into helper state `1228` with `movehitpersist = 1`, keeps those Move* triggers readable while `HitCount = 0` and `UniqHitCount = 0`, and routes helper `1200 -> 1228 -> 1229` with helper-owned sound/FightFX evidence. That trace aggregate was 419/419 artifacts, 389 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-hitcountpersist.json` checksum `fc9588d8` promotes bounded helper-owned `StateDef hitcountpersist` into `pnpm qa:trace`. A helper-local `HitDef` contact records helper-side `HitCount` / `UniqHitCount`, immediately transitions into helper state `1226` with `hitcountpersist = 1`, keeps those counters readable while `MoveHit = 0`, and routes helper `1200 -> 1226 -> 1227` with helper-owned sound/FightFX evidence. That trace aggregate was 418/418 artifacts, 388 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-hitdefpersist.json` checksum `9d5c64c4` promotes bounded helper-owned `StateDef hitdefpersist` into `pnpm qa:trace`. A helper-local `HitDef` activates in helper state `1200`, immediately transitions into helper state `1224` with `hitdefpersist = 1`, remains active there, hits from the destination state with helper-owned sound/FightFX evidence, then routes helper `1200 -> 1224 -> 1225`. That trace aggregate was 417/417 artifacts, 387 required and 30 optional. Previous direct `synthetic-imported-hitdefpersist.json` checksum `4bb3e86c` remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-movehitpersist.json` checksum `5c1ef583` promotes bounded direct `StateDef movehitpersist` into `pnpm qa:trace`. A direct `HitDef` contact records attacker-side `MoveContact` / `MoveHit`, enters state `344` with `movehitpersist = 1`, proves `MoveContact >= 1 && MoveHit >= 1` while `HitCount = 0 && UniqHitCount = 0`, and routes P1 `200 -> 344 -> 345`. That trace aggregate was 415/415 artifacts, 385 required and 30 optional. This remains required and proves bounded direct HitDef state-transition Move* evidence only; guarded/reversed breadth, helper/projectile/custom-state movehitpersist breadth, exact combo UI accumulation, multi-hit/multi-target/team counting, and full Move* lifetime parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitcountpersist.json` checksum `6f032088` promotes bounded direct `StateDef hitcountpersist` into `pnpm qa:trace`. A direct `HitDef` contact records attacker-side `HitCount` / `UniqHitCount`, enters state `342` with `hitcountpersist = 1`, proves `HitCount >= 1 && UniqHitCount >= 1` while `MoveHit = 0`, and routes P1 `200 -> 342 -> 343`. That trace aggregate was 414/414 artifacts, 384 required and 30 optional. This remains required and proves bounded direct HitDef state-transition hit-count evidence only; helper/projectile/custom-state hitcountpersist breadth, exact combo UI accumulation, multi-hit/multi-target/team counting, and full hit-count lifetime parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitcount.json` checksum `97a1b671` and `synthetic-imported-helper-projectile-hitcount.json` checksum `19d1c22c` promote bounded Projectile normal-hit attacker-side `HitCount` / `UniqHitCount` contact memory into `pnpm qa:trace`. Player-owned Projectile contact now feeds owner `MoveContact` / `MoveHit` / `HitCount` and branches P1 to state `341` through `HitCount >= 1 && UniqHitCount >= 1`, with projectile lifecycle evidence and target link `p1 -> p2 / 77`. Helper-parented/root-owned Projectile contact now mirrors hit-count memory into the spawning helper, exposes helper-local `HitCount` / `UniqHitCount`, branches helper `1257 -> 1258`, and preserves target links `p1 -> p2 / 8893` and `p1-helper-0 -> p2 / 8893` plus helper/projectile lifecycle payload evidence. That trace aggregate was 413/413 artifacts, 383 required and 30 optional. Official Elecbyte docs define `HitCount` / `UniqHitCount` as current attack-move counters and `Projectile` as HitDef-parameter compatible/root-owned when helper-created. This is bounded Projectile/helper Projectile attacker-side hit-count evidence only; exact combo accumulation, Projectile/helper hitcountpersist breadth, chain-hit eligibility arbitration, multi-hit/multi-target/team counting, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitCount parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-gethitvar-hitcount.json` trace checksum `fa445b05` / final checksum `0c2197f7` and `synthetic-imported-helper-projectile-gethitvar-hitcount.json` trace checksum `ded0c9b3` / final checksum `bba71972` promote bounded Projectile normal-hit `GetHitVar(hitcount)` metadata from HitDef `numhits` into `pnpm qa:trace`. The player-owned route keeps Projectile lifetime `projhits = 1` separate from HitDef `numhits = 3`, routes P2 through defender-owned Common1-style `5000 -> 339`, records target link `p1 -> p2 / 77`, and proves `GetHitVar(hitcount) = 3` plus `!GetHitVar(guarded)`. The helper-parented/root-owned route keeps helper-spawned Projectile lifetime `projhits = 1` separate from HitDef `numhits = 4`, routes P2 through `5000 -> 340`, and preserves `p1 -> p2 / 8892` plus `p1-helper-0 -> p2 / 8892` target links with helper/projectile lifecycle payload evidence. That trace aggregate was 411/411 artifacts, 381 required and 30 optional. Official Elecbyte docs define `Projectile` HitDef-parameter inheritance, Projectile-specific `projhits`, helper-created Projectiles becoming root-owned, and `GetHitVar(hitcount)`. This is bounded player-owned/helper-parented Projectile normal-hit hitcount metadata evidence only; exact combo accumulation, chain-hit eligibility arbitration, multi-hit timing, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/GetHitVar parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-gethitvar-hitid-chainid.json` trace checksum `80392a85` / final checksum `514a6803` and `synthetic-imported-helper-projectile-gethitvar-hitid-chainid.json` trace checksum `24df4416` / final checksum `f84c36ad` promote bounded Projectile normal-hit `GetHitVar(hitid/chainid)` metadata into `pnpm qa:trace`. The player-owned route preserves separate Projectile effect id `77` and HitDef `id = 78` / `chainID = 44`, routes P2 through defender-owned Common1-style `5000 -> 337`, records target link `p1 -> p2 / 78`, and proves `GetHitVar(hitid) = 78`, `GetHitVar(chainid) = 44`, plus `!GetHitVar(guarded)`. The helper-parented/root-owned route preserves Projectile effect id `8891` and the same HitDef id/chainID, routes P2 through `5000 -> 338`, and preserves `p1 -> p2 / 78` plus `p1-helper-0 -> p2 / 78` target links with helper/projectile lifecycle payload evidence. That trace aggregate was 409/409 artifacts, 379 required and 30 optional, and remains required. Official Elecbyte docs define `Projectile` HitDef-parameter inheritance, helper-created Projectiles becoming root-owned, and `GetHitVar` params including `hitid` and `chainid`. This is bounded player-owned/helper-parented Projectile normal-hit id metadata evidence only; exact chain-hit eligibility arbitration, combo accumulation, multi-hit timing, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/GetHitVar parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-gethitvar-hit-metadata.json` trace checksum `6b7ad6e5` / final checksum `36365083` and `synthetic-imported-helper-projectile-gethitvar-hit-metadata.json` trace checksum `4a65158a` / final checksum `dfbc9fe9` promote bounded Projectile normal-hit `GetHitVar(damage/hittime/xvel/yvel)` metadata into `pnpm qa:trace`. The player-owned Projectile route uses Projectile id `77`, routes P2 through defender-owned Common1-style `5000 -> 335`, and proves `damage = 31`, `hittime = 13`, `xvel = 4`, `yvel = -2`, plus target link and projectile lifecycle evidence. The helper-parented route uses helper-spawned/root-owned Projectile id `8890`, routes P2 through `5000 -> 336`, proves `damage = 37`, `hittime = 14`, `xvel = 4`, `yvel = -2`, and preserves `p1 -> p2 / 8890` plus `p1-helper-0 -> p2 / 8890` target links with helper/projectile lifecycle payload evidence. That trace aggregate was 407/407 artifacts, 377 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-guard-slide-stop.json` trace checksum `965c2d12` / final checksum `0973a73c` and `synthetic-imported-helper-projectile-guard-slide-stop.json` trace checksum `6c42a378` / final checksum `df8b7a42` extend the existing direct `synthetic-imported-default-guard-slide-stop.json` guard-control proof to player-owned Projectile and helper-parented Projectile stand-guard routes. Both routes author explicit `guard.slidetime = 5` and `guard.ctrltime = 7`, keep P2 in Common1-style `150 -> 151 -> 130`, and require ordered `HitVelSet`, `VelSet`, `CtrlSet`, final `ChangeState`, and typed `kinematic:hitvelset` / `kinematic:velset` / `resource:ctrlset` evidence. Helper Projectile also retains owner/helper target links plus helper/projectile lifecycle payload evidence. That checkpoint passed 405/405 artifacts, 375 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-guard-timing-default.json` trace checksum `859cb873` / final checksum `bae55cbc`, `synthetic-imported-projectile-guard-timing-default.json` trace checksum `21dc44c4` / final checksum `1c3d9c20`, and `synthetic-imported-helper-projectile-guard-timing-default.json` trace checksum `d421498c` / final checksum `1733494a` promote official default guard timing derivation into `pnpm qa:trace`. Direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` routes omit `guard.hittime`, `guard.slidetime`, and `guard.ctrltime`, derive through `ground.hittime -> guard.hittime -> guard.slidetime -> guard.ctrltime`, keep P2 in stand guard states `150 -> 151`, and require `GetHitVar(hittime/slidetime/ctrltime)` evidence. Helper Projectile keeps owner/helper target links plus helper/projectile lifecycle payload evidence. That trace aggregate was 403/403 artifacts, 373 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-guard-velocity-default.json` trace checksum `e6bd9b40`, `synthetic-imported-projectile-guard-velocity-default.json` trace checksum `b72451a4`, and `synthetic-imported-helper-projectile-guard-velocity-default.json` trace checksum `2067ba99` promote official default `guard.velocity` derivation into `pnpm qa:trace`. That trace aggregate was 400/400 artifacts, 370 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-guard-cornerpush-default.json` trace checksum `95293bc4`, `synthetic-imported-projectile-guard-cornerpush-default.json` trace checksum `58798e7a`, and `synthetic-imported-helper-projectile-guard-cornerpush-default.json` trace checksum `292b2015` promote bounded default `guard.cornerpush.veloff` derivation into `pnpm qa:trace`. That trace aggregate was 397/397 artifacts, 367 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-down-hit-cornerpush-default.json` trace checksum `04557813`, `synthetic-imported-projectile-down-hit-cornerpush-default.json` trace checksum `b302e3b9`, and `synthetic-imported-helper-projectile-down-hit-cornerpush-default.json` trace checksum `fe0c3ff1` promote bounded default `down.cornerpush.veloff` derivation into `pnpm qa:trace`. That trace aggregate was 394/394 artifacts, 364 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-down-hit-cornerpush.json` trace checksum `b372c07b`, `synthetic-imported-projectile-down-hit-cornerpush.json` trace checksum `5f2a653f`, and `synthetic-imported-helper-projectile-down-hit-cornerpush.json` trace checksum `5231ad5c` promote bounded explicit `down.cornerpush.veloff` runtime selection into `pnpm qa:trace`. `CombatResolver` selects `downCornerPush` when the defender is already `StateType = L`, before ground/air fallback. Direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` routes force P2 into a passive imported `StateTypeSet` liedown setup, hit at the stage edge, require P2 `stateType = L` actor-frame evidence plus attacker/owner X velocity evidence from `down.cornerpush.veloff = 7`, and keep helper Projectile owner/helper target links plus helper/projectile lifecycle payload evidence. That trace aggregate was 391/391 artifacts, 361 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-hit-cornerpush-default.json` trace checksum `73129a04`, `synthetic-imported-projectile-air-hit-cornerpush-default.json` trace checksum `9bfae4d6`, and `synthetic-imported-helper-projectile-air-hit-cornerpush-default.json` trace checksum `9c81047d` promote bounded default `air.cornerpush.veloff` derivation into `pnpm qa:trace`. Direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` routes omit corner-push params, hit an airborne defender at the stage edge, derive air-hit corner push through `air.cornerpush.veloff` -> `ground.cornerpush.veloff` plus the existing guard/ground velocity fallback path, keep P2 in Common1-style airborne hit state `5020`, and require attacker/owner X velocity evidence while helper Projectile also retains owner/helper target links plus helper/projectile lifecycle payload evidence. That trace aggregate was 388/388 artifacts, 358 required and 30 optional. Official Elecbyte docs define HitDef corner-push defaults, Projectile inherits HitDef parameters, and helper-created Projectiles are root-owned. This is bounded default direct/player/helper Projectile air-hit corner-push evidence only; exact corner-push timing/decay, wall friction, exact air get-hit physics/timing, hit effects, throws, teams/simul, score movement, and full MUGEN/IKEMEN Common1/projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-guard-cornerpush-default.json` trace checksum `c32781ad`, `synthetic-imported-projectile-air-guard-cornerpush-default.json` trace checksum `90f5e385`, and `synthetic-imported-helper-projectile-air-guard-cornerpush-default.json` trace checksum `0271a2b9` promote bounded default `airguard.cornerpush.veloff` derivation into `pnpm qa:trace`. That trace aggregate was 385/385 artifacts, 355 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-guard-cornerpush.json` trace checksum `9fdb8a81`, `synthetic-imported-projectile-air-guard-cornerpush.json` trace checksum `15f26082`, and `synthetic-imported-helper-projectile-air-guard-cornerpush.json` trace checksum `35d7148b` promote bounded explicit `airguard.cornerpush.veloff` into `pnpm qa:trace`. Direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` routes parse and preserve the HitDef corner-push family, guard an airborne defender at the stage edge, keep P2 in Common1-style state `155`, and require attacker/owner X velocity evidence showing corner pushback while helper Projectile also retains owner/helper target links plus helper/projectile lifecycle payload evidence. That trace aggregate was 382/382 artifacts, 352 required and 30 optional. This is bounded direct/player/helper Projectile air-guard corner-push evidence only; exact corner-push timing/decay, wall friction, exact air guard physics/landing/timing, guard effects, throws, teams/simul, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-guard-velocity-default.json` trace checksum `b1710269`, `synthetic-imported-projectile-air-guard-velocity-default.json` trace checksum `bd1a774e`, and `synthetic-imported-helper-projectile-air-guard-velocity-default.json` trace checksum `3351e770` promote official default `airguard.velocity` derivation into `pnpm qa:trace`. Direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` routes omit `airguard.velocity`, author `air.velocity = -6,-8`, derive air-guard X/Y velocity as `air.x * 1.5` and `air.y / 2`, keep the airborne defender in Common1-style state `155`, and require actor-frame velocity evidence with X at least `9` and Y at most `-3.5`; helper Projectile also retains owner/helper target links plus helper/projectile lifecycle payload evidence. That trace aggregate was 379/379 artifacts, 349 required and 30 optional. Official Elecbyte docs define the `airguard.velocity` default from `air.velocity`, and Projectile inherits HitDef parameters. This is bounded direct/player/helper Projectile default air-guard velocity evidence only; exact air guard physics/landing/timing, guard effects, throws, teams/simul, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-default-guardflag-filter.json` trace checksum `05725ecb`, `synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter.json` trace checksum `c1402d31`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter.json` trace checksum `889d77c1` promote bounded default `missonoverride = -1` custom-state HitOverride guardflag breadth into `pnpm qa:trace`. Direct `HitDef` (`p2stateno = 888`) omits `missonoverride`, carries `guardflag = H`, and rejects before target memory, override state entry, owner-backed custom-state `888`, default get-hit, or guard states; P2 remains idle/control with life `1000`. Player-owned Projectile id `77` and helper-parented Projectile id `8882` both omit `missonoverride`, use `p2stateno = 889` / `p2getp1state = 1` / `guardflag = H`, skip slots `1 -> 776` with `guardflag.not = HA` and `2 -> 778` with `guardflag = A`, select slot `5 -> 779`, suppress projectile custom-state `889`, and end P2 in state/action `779`, life `1000`, moveType `I`; the helper route also records owner/helper target links, helper `targetCount = 1`, projectile `hasHit = true` / `hitsRemaining = 0`, and suppresses helper branch `1293`. That trace aggregate was 370/370 artifacts, 340 required and 30 optional. Official Elecbyte docs define `p2stateno` / `p2getp1state`, Projectile HitDef-param inheritance, helper-spawned Projectile root ownership, and HitOverride slot semantics; local Ikemen-GO source parses `missonoverride`, `guardflag`, and `guardflag.not`, scans slots low-to-high, applies guardflag filters before selection, and uses default custom-state miss only for non-projectile contacts when `missonoverride = -1`. This is bounded direct/player/helper default custom-state guardflag-filter evidence only; exact guard timing/guarded contact semantics, exact target lifetime, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero-guardflag-filter.json` trace checksum `058b335f`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` trace checksum `af29f125`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` trace checksum `9edbf3d0` promote bounded explicit `missonoverride = 0` custom-state HitOverride guardflag filtering into `pnpm qa:trace`. Direct `HitDef` (`p2stateno = 888`), player-owned Projectile id `77` (`p2stateno = 889`), and helper-parented Projectile id `8881` (`p2stateno = 889`) all use `guardflag = H`; P2 installs attr-matching slots `1 -> 776` with `guardflag.not = HA`, `2 -> 778` with `guardflag = A`, and `5 -> 779` with `guardflag = H`. All three routes skip slots `1` and `2`, select slot `5`, suppress the custom state, forbid default get-hit/guard states, and end P2 in state/action `779`, life `1000`, moveType `I`; the helper route also records owner/helper target links, helper `targetCount = 1`, projectile `hasHit = true` / `hitsRemaining = 0`, and suppresses helper branch `1291`. That trace aggregate was 367/367 artifacts, 337 required and 30 optional. Official Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; Elecbyte HitOverride docs define active override slots; local Ikemen-GO source parses guardflag filters and scans active HitOverride slots low-to-high. This is bounded direct/player/helper explicit `missonoverride = 0` custom-state guardflag-filter evidence only; exact guard timing/guarded contact semantics, forceair/forceguard priority combinations, exact target lifetime, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority.json` trace checksum `9a5a149f` plus `synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority.json` trace checksum `96b6b7de` promote bounded Projectile explicit `missonoverride = 0` custom-state HitOverride slot-priority evidence into `pnpm qa:trace`. The player-owned route fires Projectile id `77` with `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`; the helper-parented route has a visual Helper spawn owner-side Projectile id `8878` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`. P2 installs matching slots `5 -> 779` and `2 -> 778`; both routes select slot `2`, emit override telemetry, suppress projectile custom-state `889`, forbid default get-hit/guard states, and end P2 in state/action `778` / life `1000` / moveType `I`; the helper route also records target links `p1 -> p2 / 8878` and `p1-helper-0 -> p2 / 8878`, helper payload `targetCount = 1`, projectile payload `hasHit = true` / `hitsRemaining = 0`, and suppresses helper `ProjHit` branch `1288`. That trace aggregate was 364/364 artifacts, 334 required and 30 optional. Official Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; Elecbyte HitOverride docs define slot setup and redirect semantics; local Ikemen-GO source scans HitOverride slots low-to-high and skips the default non-projectile custom-state miss for Projectile contacts with explicit `missonoverride = 0`. This is bounded player-owned/helper-parented Projectile explicit custom-state slot-priority evidence only; exact guard timing/guarded contact semantics, custom-state guardflag breadth beyond the latest explicit route, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` trace checksum `fb964bfb` promotes bounded helper-parented Projectile default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. A visual Helper spawns owner-side Projectile id `8877` with `parentId = p1-helper-0`, `p2stateno = 889` / `p2getp1state = 1`, and omitted `missonoverride`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records owner target link `p1 -> p2 / 8877` plus helper target link `p1-helper-0 -> p2 / 8877`, consumes/removes the projectile, selects slot `3`, emits override telemetry, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889` and helper `ProjHit` branch `1286`, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`; states `780`, `889`, `1286`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 362/362 artifacts, 332 required and 30 optional. Official Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; Elecbyte HitOverride docs define `forceair` and custom-state miss behavior; local Ikemen-GO source applies default custom-state HitOverride miss only to non-projectile contacts when `missonoverride = -1`, then applies `forceair` / `forceguard`, suppresses override state entry with `keepstate`, and suppresses projectile custom-state entry after an override matches. This is bounded helper-parented Projectile default `missonoverride = -1` custom-state forceair/forceguard/keepstate evidence only; custom-state guardflag inheritance/timing, final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN helper Projectile HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` trace checksum `4ce42cf3` promotes bounded player-owned Projectile default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. P1 fires Projectile id `77` with `p2stateno = 889` / `p2getp1state = 1` and omits `missonoverride`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile, selects slot `3`, emits override telemetry, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889`, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`; states `780`, `889`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 361/361 artifacts, 331 required and 30 optional. This is bounded player-owned Projectile default `missonoverride = -1` custom-state forceair/forceguard/keepstate evidence only; custom-state guardflag inheritance/timing, final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` trace checksum `20e40425` promotes bounded direct-HitDef default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` miss evidence into `pnpm qa:trace`. P1 declares `p2stateno = 888` / `p2getp1state = 1` and omits `missonoverride`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact rejects before target memory, damage, guard, forceair/forceguard actor frames, keepstate redirect handling, override state `780`, owner-backed custom-state `888`, default get-hit state `5000`, or guard states `150` / `151`. Final P2 remains idle state/action `0`, life `1000`, control true, moveType `I`; no target links are recorded. That trace aggregate was 360/360 artifacts, 330 required and 30 optional. Official Elecbyte HitOverride docs define `forceair` and custom-state miss behavior, Elecbyte HitDef docs define `p2stateno` / `p2getp1state`, and local Ikemen-GO source returns miss for default `missonoverride = -1` on direct non-projectile custom-state contacts before applying force flags. This is bounded direct default custom-state miss force-flag evidence only; custom-state guardflag inheritance/timing, final-frame forced aerial persistence on non-rejected routes, exact guarded get-hit variable/chip semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` trace checksum `e23a33af` promotes bounded helper-parented Projectile explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. A visual Helper spawns owner-side Projectile id `8876` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records owner target link `p1 -> p2 / 8876` plus helper target link `p1-helper-0 -> p2 / 8876`, consumes/removes the projectile, selects slot `3`, emits override combat/event telemetry containing `HitOverride slot 3`, records helper payload `targetCount = 1` plus projectile payload `hasHit = true` / `hitsRemaining = 0`, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889` and helper `ProjHit` branch `1284` because the override matched, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`; states `780`, `889`, `1284`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 359/359 artifacts, 329 required and 30 optional. Official Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; Elecbyte HitOverride docs define `forceair` and custom-state miss behavior; local Ikemen-GO source applies default custom-state HitOverride miss only to non-projectile contacts when `missonoverride = -1`, applies `forceair` / `forceguard`, suppresses override state entry with `keepstate`, and suppresses projectile custom-state entry after an override matches. This is bounded helper-parented Projectile explicit `missonoverride = 0` custom-state forceair/forceguard/keepstate evidence only; custom-state guardflag inheritance/timing, final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN helper Projectile HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` trace checksum `15bc955b` promotes bounded player-owned Projectile explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. P1 fires Projectile id `77` with `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile, selects slot `3`, emits override combat/event telemetry containing `HitOverride slot 3`, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889` because the override matched, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`; states `780`, `889`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 358/358 artifacts, 328 required and 30 optional. Official Elecbyte Projectile docs state Projectile takes HitDef parameters; Elecbyte HitOverride docs define `forceair` and custom-state miss behavior; local Ikemen-GO source applies default custom-state HitOverride miss only to non-projectile contacts when `missonoverride = -1`, applies `forceair` / `forceguard`, suppresses override state entry with `keepstate`, and suppresses projectile custom-state entry after an override matches. This is bounded player-owned Projectile explicit `missonoverride = 0` custom-state forceair/forceguard/keepstate evidence only; helper-parented Projectile default `missonoverride` custom-state breadth, custom-state guardflag inheritance/timing, final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` trace checksum `4d9043a5` promotes bounded direct-HitDef explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. P1 declares `p2stateno = 888`, `p2getp1state = 1`, and `missonoverride = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, selects slot `3`, emits override combat/event telemetry containing `HitOverride slot 3`, keeps P2 in state/action `0` instead of entering state `780`, suppresses owner-backed custom-state `888` because the override matched, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`; states `780`, `888`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 357/357 artifacts, 327 required and 30 optional. This is bounded explicit `missonoverride = 0` direct custom-state forceair/forceguard/keepstate evidence only; helper/projectile custom-state force flags, custom-state guardflag inheritance/timing, final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, guard KO/no-KO flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-forceair-forceguard-keepstate.json` trace checksum `84dc3969` promotes bounded helper-parented Projectile `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. A visual Helper spawns owner-side Projectile id `8875` with `parentId = p1-helper-0`, `p2stateno = 889`, and `p2getp1state = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records owner target link `p1 -> p2 / 8875` plus helper target link `p1-helper-0 -> p2 / 8875`, selects slot `3`, emits override combat/event telemetry, records projectile payload evidence with `hasHit = true` / `hitsRemaining = 0`, keeps P2 in state/action `0` instead of entering state `780`, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`; projectile `p2stateno` state `889`, helper `ProjHit` branch `1282`, override state `780`, default get-hit state `5000`, and guard states `150` / `151` are forbidden. That trace aggregate was 356/356 artifacts, 326 required and 30 optional. Official Elecbyte Projectile docs state Projectile takes HitDef parameters and Elecbyte HitOverride docs define `forceair`; local Ikemen-GO source also parses/stores/applies `forceguard` and `keepstate`, with `keepstate` suppressing the override state change when `isProjectile` is true. This is bounded helper-parented Projectile forceair/forceguard/keepstate evidence only; final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN helper Projectile HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-forceair-forceguard-keepstate.json` trace checksum `3806a769` promotes bounded player-owned Projectile `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. P1 fires Projectile id `77` with `p2stateno = 889` / `p2getp1state = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, selects slot `3`, emits override combat/event telemetry, records projectile hit/removal lifecycle evidence, keeps P2 in state/action `0` instead of entering state `780`, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`; projectile `p2stateno` state `889`, override state `780`, default get-hit state `5000`, and guard states `150` / `151` are forbidden. That trace aggregate was 355/355 artifacts, 325 required and 30 optional. This is bounded player-owned Projectile forceair/forceguard/keepstate evidence only; final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-forceair-forceguard-keepstate.json` trace checksum `19787fb2` promotes bounded direct-HitDef `HitOverride forceair` / `forceguard` / `keepstate` evidence into `pnpm qa:trace`. P1 hits with `attr = S,NA`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, selects slot `3`, emits override combat/event telemetry, keeps P2 in state/action `0` instead of entering state `780`, and actor-frame evidence observes P2 as aerial/guarding (`stateType = A`, `physics = A`, `guardingFrames >= 1`). Final P2 returns to idle state/action `0`, life `1000`, moveType `I`. That trace aggregate was 354/354 artifacts, 324 required and 30 optional. This is bounded direct-HitDef forceair/forceguard/keepstate evidence only; final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state forceair/forceguard/keepstate breadth, custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-guardflag-filter.json` trace checksum `41a87267` promotes bounded helper-parented Projectile `HitOverride guardflag` / `guardflag.not` slot filtering into `pnpm qa:trace`. A visual Helper spawns owner-side Projectile id `8874` with `parentId = p1-helper-0`, `guardflag = H`, `p2stateno = 889`, and `p2getp1state = 0`; P2 installs attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`; contact records owner target link `p1 -> p2 / 8874` plus helper target link `p1-helper-0 -> p2 / 8874`, marks projectile payload `hitsRemaining = 0` / `hasHit = true`, skips slots `1` and `2`, selects slot `5`, redirects P2 through state `779` / life `1000`, and forbids states `776`, `778`, `889`, helper `ProjHit` branch `1280`, `5000`, `150`, and `151`. That trace aggregate was 353/353 artifacts, 323 required and 30 optional. Official Elecbyte docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; Elecbyte tutorial docs define HitDef `guardflag`; local Ikemen-GO source stores `guardflag` / `guardflag_not`, checks them while scanning slots low-to-high, and applies those checks when `isProjectile` is true. This is bounded helper-parented Projectile HitOverride guardflag filtering only; custom-state guardflag inheritance/timing, custom-state forceair/forceguard/keepstate breadth, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN helper Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-guardflag-filter.json` trace checksum `a51e82ec` promotes bounded player-owned Projectile `HitOverride guardflag` / `guardflag.not` slot filtering into `pnpm qa:trace`. P1 fires Projectile id `77` with `guardflag = H` and `p2stateno = 889`; P2 installs attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile as a hit, skips slots `1` and `2`, selects slot `5`, redirects P2 through state `779` / life `1000`, and forbids states `776`, `778`, `889`, `5000`, `150`, and `151`. That trace aggregate was 352/352 artifacts, 322 required and 30 optional. Official Elecbyte docs state Projectile takes HitDef parameters and define HitDef `guardflag`; local Ikemen-GO source stores `guardflag` / `guardflag_not` and checks them while scanning slots low-to-high. This is bounded player-owned Projectile HitOverride guardflag filtering only; custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-guardflag-filter.json` trace checksum `b88a2da3` promotes bounded direct-HitDef `HitOverride guardflag` / `guardflag.not` slot filtering into `pnpm qa:trace`. P1 hits with `attr = S,NA` and `guardflag = H`; P2 installs attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`; contact records target link `p1 -> p2 / 77`, skips slots `1` and `2`, selects slot `5`, redirects P2 through state `779` / life `1000`, and forbids states `776`, `778`, `5000`, `150`, and `151`. That trace aggregate was 351/351 artifacts, 321 required and 30 optional. Official Elecbyte docs define HitOverride slots/redirect semantics and HitDef guard metadata; local Ikemen-GO source stores `guardflag` / `guardflag_not` and checks them while scanning slots low-to-high. This is bounded direct-HitDef HitOverride guardflag filtering only; custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero-slot-priority.json` trace checksum `92fefd6a` promotes bounded direct-HitDef `missonoverride = 0` custom-state slot-priority behavior into `pnpm qa:trace`. P1 declares `p2stateno = 888`, `p2getp1state = 1`, and explicit `missonoverride = 0`; P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; contact records target link `p1 -> p2 / 77`, selects slot `2`, redirects P2 through state `778` / life `1000`, and forbids state `779`, owner-backed custom state `888`, and default Common1 get-hit/guard states `5000`, `150`, and `151`. That trace aggregate was 350/350 artifacts, 320 required and 30 optional. Official Elecbyte docs define `p2stateno` / `p2getp1state` custom-state routing and HitOverride slots/redirect semantics; local Ikemen-GO source stores `hover[slot]`, scans slots low-to-high, and treats `missonoverride = 0` as the opt-out from the default direct custom-state HitOverride miss. This is bounded direct-HitDef `missonoverride = 0` slot priority only; helper/projectile custom-state slot-priority breadth, helper/projectile `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-slot-priority.json` trace checksum `1d058518` promotes bounded helper-parented Projectile HitOverride slot-priority behavior into `pnpm qa:trace`. A visual Helper spawns Projectile id `8873` with `parentId = p1-helper-0`, `p2stateno = 889`, and `p2getp1state = 0`; P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; contact records owner target link `p1 -> p2 / 8873` plus helper target link `p1-helper-0 -> p2 / 8873`, marks projectile payload `hitsRemaining = 0` / `hasHit = true`, selects slot `2`, redirects P2 through state `778` / life `1000`, and forbids state `779`, projectile custom state `889`, helper `ProjHit` branch `1278`, and default Common1 get-hit/guard states `5000`, `150`, and `151`. That trace aggregate was 349/349 artifacts, 319 required and 30 optional. Official Elecbyte docs define HitOverride slots `0-7`, redirect semantics, Projectile HitDef-param inheritance, and root ownership for helper-spawned projectiles; local Ikemen-GO source stores `hover[slot]`, scans slots low-to-high, and selects the first matching slot. This is bounded helper-parented Projectile HitOverride slot priority only; custom-state slot-priority breadth, `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-slot-priority.json` trace checksum `378d9ce8` promotes bounded player-owned Projectile HitOverride slot-priority behavior into `pnpm qa:trace`. P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; P1 fires a player-owned Projectile id `77` with `p2stateno = 889`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile as a hit, selects slot `2`, redirects P2 through state `778` / life `1000`, and forbids state `779`, projectile custom state `889`, and default Common1 get-hit/guard states `5000`, `150`, and `151`. That trace aggregate was 348/348 artifacts, 318 required and 30 optional. Official Elecbyte docs define HitOverride slots `0-7` and redirect semantics; local Ikemen-GO source stores `hover[slot]`, scans slots low-to-high, and selects the first matching slot. This is bounded player-owned Projectile HitOverride slot priority only; custom-state slot-priority breadth, `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-slot-priority.json` trace checksum `8de62354` promotes bounded direct-HitDef HitOverride slot-priority behavior into `pnpm qa:trace`. P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; P1 hits with `attr = S,NA`; the resolver selects slot `2`, redirects P2 through state `778` / life `1000`, and forbids state `779` plus default Common1 get-hit/guard states `5000`, `150`, and `151`. That trace aggregate was 347/347 artifacts, 317 required and 30 optional. Official Elecbyte docs define HitOverride slot setup and redirect semantics; local Ikemen-GO source stores `hover[slot]`, scans slots low-to-high, and selects the first matching slot. This is bounded direct-HitDef HitOverride slot priority only; helper-parented Projectile/custom-state slot-priority breadth, `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero.json` trace checksum `62d7d6b8` promotes bounded helper-parented Projectile `missonoverride = 0` HitOverride redirect behavior into `pnpm qa:trace`. A visual Helper spawns Projectile id `8872` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and explicit `missonoverride = 0`; P2 has active matching `HitOverride` slot `777`; contact records owner target link `p1 -> p2 / 8872` plus helper target link `p1-helper-0 -> p2 / 8872`; projectile payload evidence reaches `hitsRemaining = 0` / `hasHit = true`; and P2 redirects through state `777` / life `1000` instead of missing, entering projectile custom state `889`, taking helper `ProjHit` branch `1276`, or default Common1 get-hit/guard states `5000`, `150`, and `151`. That trace aggregate was 346/346 artifacts, 316 required and 30 optional. Official Elecbyte docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; local Ikemen-GO source only forces projectile HitOverride miss when `missonoverride == 1`, so explicit `missonoverride = 0` keeps redirect behavior. This is bounded helper-parented Projectile `missonoverride = 0` plus HitOverride redirect only; broader `missonoverride` custom-state breadth, helper/projectile/custom-state slot-priority breadth, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, helper/projectile custom-state guard metadata, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-zero.json` trace checksum `5c12f3cc` promotes bounded player-owned Projectile `missonoverride = 0` HitOverride redirect behavior into `pnpm qa:trace`. A player-owned Projectile declares `p2stateno = 889` / `p2getp1state = 1` / `missonoverride = 0`, P2 has active matching `HitOverride` slot `777`, contact records target id `77`, consumes/removes the projectile as a hit, and redirects P2 through state `777` / life `1000` instead of missing or entering projectile custom state `889`; states `889`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 345/345 artifacts, 315 required and 30 optional. Official Elecbyte docs state Projectile takes HitDef parameters; local Ikemen-GO source only forces projectile HitOverride miss when `missonoverride == 1`, so explicit `missonoverride = 0` keeps redirect behavior. This is bounded player-owned Projectile `missonoverride = 0` plus HitOverride redirect only; helper-parented Projectile `missonoverride = 0` is covered by the latest checkpoint, while broader `missonoverride` custom-state breadth, exact HitOverride slot priority, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, helper/projectile custom-state guard metadata, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-one.json` trace checksum `a99979bb` promotes bounded helper-parented Projectile `missonoverride = 1` HitOverride miss behavior into `pnpm qa:trace`. A visual Helper spawns Projectile id `8871` with `parentId = p1-helper-0` and `missonoverride = 1`; P2 has active matching `HitOverride` slot `777`; contact rejects before owner/helper target memory, projectile contact consumption, damage, guard, normal HitOverride redirect, helper `ProjHit` branch `1275`, or projectile custom-state entry; no owner/helper target id is recorded, P2 remains state `0` / life `1000` / control, the projectile remains active with `hitsRemaining = 1` / `hasHit = false`, and states `777`, `889`, `1275`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 344/344 artifacts, 314 required and 30 optional. Official Elecbyte docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; local Ikemen-GO source returns miss when `missonoverride = 1` and active HitOverride matches. This is bounded helper-parented Projectile `missonoverride = 1` plus HitOverride miss only; player-owned Projectile `missonoverride = 0` is covered by the latest checkpoint, while helper-parented Projectile `missonoverride = 0`, exact HitOverride slot priority, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, helper/projectile custom-state guard metadata, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-one.json` trace checksum `2dc86467` promotes bounded player-owned Projectile `missonoverride = 1` HitOverride miss behavior into `pnpm qa:trace`. A player-owned Projectile declares `missonoverride = 1`, P2 has active matching `HitOverride` slot `777`, and contact rejects before target memory, projectile contact consumption, damage, guard, normal HitOverride redirect, or projectile custom-state entry; no target id is recorded, P2 remains state `0` / life `1000` / control, the projectile remains active with `hitsRemaining = 1`, and states `777`, `889`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 343/343 artifacts, 313 required and 30 optional. Official Elecbyte docs state Projectile takes HitDef parameters; local Ikemen-GO source returns miss when `missonoverride = 1` and active HitOverride matches. This is bounded player-owned Projectile `missonoverride = 1` plus HitOverride miss only; helper-parented Projectile `missonoverride = 1` is covered by the latest checkpoint, while `missonoverride = 0` projectile/custom-state breadth, exact HitOverride slot priority, exact projectile target lifetime/tick order, helper-owned custom-state tables, helper/projectile custom-state guard metadata, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-p2stateno.json` trace checksum `ce4c1d9a` promotes bounded helper-parented owner-side `Projectile p2stateno` HitOverride behavior into `pnpm qa:trace`. A visual Helper spawns Projectile id `8870` with `parentId = p1-helper-0`, `p2stateno = 889`, and `p2getp1state = 0`; P2 has active matching `HitOverride` slot `777`; contact records owner target link `p1 -> p2 / 8870` plus helper target link `p1-helper-0 -> p2 / 8870`; P2 redirects through state `777` / life `1000` instead of entering projectile state `889`, helper `ProjHit` branch `1273`, or default Common1 get-hit/guard states `5000`, `150`, and `151`. That trace aggregate was 342/342 artifacts, 312 required and 30 optional. Official Elecbyte docs state Projectile takes HitDef parameters; local Ikemen-GO source applies the default `p1stateno`/`p2stateno` HitOverride miss only for non-projectile contacts. This is bounded helper-parented Projectile `p2stateno` plus HitOverride redirect only; player-owned Projectile `missonoverride = 1` is covered by the latest checkpoint, while helper Projectile `missonoverride` gate, `missonoverride = 0` projectile/custom-state breadth, exact HitOverride slot priority, exact helper-projectile target lifetime/tick order, helper-owned custom-state tables, helper/projectile custom-state guard metadata, exact guard KO/no-KO round flow, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-p2stateno.json` trace checksum `2ec0725a` promotes bounded IKEMEN-style player-owned `Projectile p2stateno` HitOverride behavior into `pnpm qa:trace`. A player-owned Projectile declares `p2stateno = 889` / `p2getp1state = 0`, P2 has an active matching `HitOverride` slot redirecting to `777`, and contact records target id `77` plus redirects P2 through HitOverride state `777` instead of taking the direct-HitDef custom-state miss path or entering projectile state `889`; states `889`, `5000`, `150`, and `151` are forbidden, and P2 remains life `1000`. That trace aggregate was 341/341 artifacts, 311 required and 30 optional. This is bounded player-owned Projectile `p2stateno` plus HitOverride redirect only; helper-parented Projectile `p2stateno` is covered by the latest checkpoint, while projectile `missonoverride` breadth, exact HitOverride slot priority, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-p2getp1state-zero-miss.json` trace checksum `656730c8` promotes bounded IKEMEN direct `HitDef p2stateno` / `p2getp1state = 0` HitOverride miss behavior into `pnpm qa:trace`. A direct `HitDef` declares `p2stateno = 889` / `p2getp1state = 0`, P2 has an active matching `HitOverride` slot redirecting to `777`, P2-owned state `889` data exists, and combat rejects contact before target memory, damage, guard, normal HitOverride redirect, or target-owned custom-state entry; no target id is recorded, P2 remains state `0` / life `1000`, and states `777`, `889`, `888`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 340/340 artifacts, 310 required and 30 optional. Official Elecbyte docs define `p2getp1state = 0` as preventing P2 from using P1 state/animation data; local Ikemen-GO source still misses direct non-projectile `p2stateno` HitDefs when active HitOverride matches by default. This is bounded direct-HitDef target-owned `p2getp1state = 0` HitOverride miss only; helper-projectile `p2stateno`, exact slot priority, helper/projectile `missonoverride` breadth, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-one.json` trace checksum `78cfedf4` promotes bounded IKEMEN `HitDef missonoverride = 1` behavior into `pnpm qa:trace`. A direct `HitDef` declares `missonoverride = 1` without `p1stateno` / `p2stateno`, P2 has an active matching `HitOverride` slot redirecting to `777`, and combat rejects contact before target memory, damage, guard, normal HitOverride redirect, or custom-state entry; no target id is recorded, P2 remains state `0` / life `1000`, and states `777`, `888`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 339/339 artifacts, 309 required and 30 optional. Local Ikemen-GO source defines `missonoverride = 1` as an unconditional miss when HitOverride matches. This is bounded direct-HitDef owner-backed `missonoverride = 1` HitOverride miss only; helper/projectile `p2stateno`, exact slot priority, helper/projectile `missonoverride` breadth, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero.json` trace checksum `8ffd5678` promotes bounded IKEMEN `HitDef missonoverride = 0` behavior into `pnpm qa:trace`. A direct `HitDef` declares `p2stateno = 888` / `p2getp1state = 1` / `missonoverride = 0`, P2 has an active matching `HitOverride` slot redirecting to `777`, and combat redirects through the override instead of defaulting to the custom-state miss path; target id `77` is recorded, state `777` executes, and states `888`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 338/338 artifacts, 308 required and 30 optional. Official Elecbyte HitOverride docs define the default custom-state miss conditions; local Ikemen-GO source defines `missonoverride = 0` as the explicit opt-out from that default miss. This is bounded direct-HitDef owner-backed `missonoverride = 0` HitOverride redirect only; helper/projectile `p2stateno`, exact slot priority, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-p2stateno-miss.json` trace checksum `6f41eeb1` promotes bounded direct-HitDef HitOverride/custom-state miss behavior into `pnpm qa:trace`. A direct `HitDef` declares `p2stateno = 888` / `p2getp1state = 1`, P2 has an active matching `HitOverride` slot redirecting to `777`, and combat rejects contact before target memory, damage, guard, normal HitOverride redirect, or custom-state entry; states `777`, `888`, `5000`, `150`, and `151` are forbidden. That trace aggregate was 337/337 artifacts, 307 required and 30 optional. This is bounded direct-HitDef owner-backed custom-state HitOverride invalidation only; helper/projectile `p2stateno`, exact slot priority, `missonoverride = 1` breadth, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN HitOverride/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-p2stateno-guard-ignored.json` trace checksum `76d1becd` promotes bounded direct-HitDef `p2stateno` guard-ignore behavior into `pnpm qa:trace`. A guarded direct `HitDef` declares `p2stateno = 888` / `p2getp1state = 1`, records target memory for id `77`, and P2 still routes through defender-owned Common1-style guard-hit states `150 -> 151 -> 130 -> 20` with slide-stop/control evidence while state `888` is forbidden. That trace aggregate was 336/336 artifacts, 306 required and 30 optional. This is bounded successful-guard behavior only; exact guard KO/no-KO round flow, helper/projectile `p2stateno` behavior, exact guard timing/proximity/effects, throws, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-guard-kill.json` trace checksum `c889a534` promotes bounded owner-backed custom-state guarded `GetHitVar(kill)` metadata into `pnpm qa:trace`. A guarded direct `HitDef` with `guard.kill = 0` records target memory, owner-local `TargetState` sends P2 into P1-owned state data, and `GetHitVar(kill) = 0 && GetHitVar(guarded) = 1 && GetHitVar(hitshaketime) > 0` branches P2 `888 -> 904` before `SelfState` returns P2 to state `0`/control. That trace aggregate was 335/335 artifacts, 305 required and 30 optional. This is bounded direct-HitDef guard kill metadata inheritance while executing attacker-owned custom state data only; exact guard KO/no-KO round flow, exact guard timing/proximity/effects, helper/projectile custom-state guard kill inheritance, throws, teams/simul, visual/audio parity, score movement, and full custom-state/guard parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-gethitvar-guard-kill.json` trace checksum `7f9aa699` promotes bounded helper-parented Projectile guarded `guard.kill = 0` metadata into `pnpm qa:trace`. A helper-spawned Projectile with `parentId = p1-helper-0` is guarded by P2, records owner and helper target memory for target id `8857`, preserves projectile `guard.kill = false`, P2 enters defender-owned Common1-style guard-hit states `150 -> 151`, and `GetHitVar(kill) = 0 && GetHitVar(guarded)` branches to state/action `334` with helper-local `Projectile` controller/op telemetry from helper state `1200`. That trace aggregate was 334/334 artifacts, 304 required and 30 optional. This is bounded helper-parented Projectile guard kill metadata only; exact helper Projectile guard KO/no-KO round flow, exact helper/projectile target lifetime/tick order, exact guard timing/proximity/effects, visual/audio parity, score movement, and full guard/projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-gethitvar-guard-kill.json` trace checksum `3feae5a7` promotes bounded player-owned Projectile guarded `guard.kill = 0` metadata into `pnpm qa:trace`. A player-owned Projectile guarded by P2 records projectile target memory, preserves projectile `guard.kill = false`, P2 enters defender-owned Common1-style guard-hit states `150 -> 151`, and `GetHitVar(kill) = 0 && GetHitVar(guarded)` branches to state/action `333`. That trace aggregate was 333/333 artifacts, 303 required and 30 optional. This is bounded player-owned Projectile guard kill metadata only; helper/projectile custom-state guard kill inheritance, exact guard KO/no-KO round flow, exact projectile target lifetime/tick order, exact guard timing/proximity/effects, visual/audio parity, score movement, and full guard/projectile parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-air-guard-kill.json` trace checksum `4382207e` promotes bounded defender-owned air guard-hit `GetHitVar(kill)` metadata into `pnpm qa:trace`. A direct air guard against `HitDef guard.kill = 0` records target memory and stored guard kill metadata, P2 enters defender-owned Common1-style air guard-hit states `154 -> 155`, and `GetHitVar(kill) = 0 && GetHitVar(guarded)` branches to state/action `332`. That trace aggregate was 332/332 artifacts, 302 required and 30 optional. This is bounded direct-HitDef air guard kill metadata only; exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact air guard timing/landing/proximity/effects, visual/audio parity, score movement, and full guard parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-crouch-guard-kill.json` trace checksum `2976fb8c` promotes bounded defender-owned crouch guard-hit `GetHitVar(kill)` metadata into `pnpm qa:trace`. A direct crouch guard against `HitDef guard.kill = 0` records target memory and stored guard kill metadata, P2 enters defender-owned Common1-style crouch guard-hit states `152 -> 153`, and `GetHitVar(kill) = 0 && GetHitVar(guarded)` branches to state/action `331`. That trace aggregate was 331/331 artifacts, 301 required and 30 optional. This is bounded direct-HitDef crouch guard kill metadata only; exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact guard timing/proximity/effects, visual/audio parity, score movement, and full guard parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-guard-kill.json` trace checksum `abb4e468` promotes bounded defender-owned stand guard-hit `GetHitVar(kill)` metadata into `pnpm qa:trace`. A direct stand guard against `HitDef guard.kill = 0` records target memory and stored guard kill metadata, P2 enters defender-owned Common1-style guard-hit states `150 -> 151`, and `GetHitVar(kill) = 0 && GetHitVar(guarded)` branches to state/action `330`. That trace aggregate was 330/330 artifacts, 300 required and 30 optional. This is bounded direct-HitDef stand guard kill metadata only; exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact guard timing/proximity/effects, visual/audio parity, score movement, and full guard parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-kill.json` trace checksum `ef5ffabf` promotes bounded defender-owned normal-hit `GetHitVar(kill)` metadata into `pnpm qa:trace`. A direct normal `HitDef kill = 0` records target memory and stored normal hit kill metadata, P2 enters defender-owned Common1-style state `5000`, and `GetHitVar(kill) = 0 && !GetHitVar(guarded)` branches `5000 -> 329`. That trace aggregate was 329/329 artifacts, 299 required and 30 optional. This is bounded direct-HitDef normal kill metadata only; KO/round-flow behavior, helper/projectile/custom-state inheritance, exact target lifetime/tick order, visual/audio parity, score movement, and full get-hit parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-fall-metadata.json` trace checksum `4a3a1c6b` promotes bounded owner-backed custom-state `GetHitVar(fall.damage/fall.kill/fall.xvel/fall.yvel)` metadata inheritance into `pnpm qa:trace`. A direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` records target memory and stored fall damage/kill/velocity values, P2 executes P1-owned state data, and `GetHitVar(fall) && GetHitVar(fall.damage) = 70 && GetHitVar(fall.kill) = 0 && GetHitVar(fall.xvel) = 3 && GetHitVar(fall.yvel) = -6` branches `888 -> 903`; actor frames preserve `customOwnerId = p1`, target-link evidence preserves target id `77`, and `SelfState` returns P2 to state `0`/control. That trace aggregate was 328/328 artifacts, 298 required and 30 optional. This is bounded direct-HitDef fall metadata inheritance while executing attacker-owned custom state data only; exact metadata lifetime/stacking, helper/projectile inheritance, throws, teams/simul, exact bind/tick order, visual/audio parity, score movement, and full custom-state fall parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-fall-envshake.json` trace checksum `5c9d1653` promotes bounded owner-backed custom-state fall envshake metadata inheritance into `pnpm qa:trace`. A direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` records target memory and fall envshake values, P2 executes P1-owned state data, and `GetHitVar(fall) && GetHitVar(fall.envshake.time) = 15 && GetHitVar(fall.envshake.freq) = 178 && GetHitVar(fall.envshake.ampl) = 6 && GetHitVar(fall.envshake.phase) = 0` branches `888 -> 902`; actor frames preserve `customOwnerId = p1`, target-link evidence preserves target id `77`, and `SelfState` returns P2 to state `0`/control. That trace aggregate was 327/327 artifacts, 297 required and 30 optional. This is bounded direct-HitDef fall envshake metadata inheritance while executing attacker-owned custom state data only; exact camera waveform, pause/stage/layer interaction, metadata lifetime/stacking, helper/projectile inheritance, throws, teams/simul, exact bind/tick order, visual/audio parity, score movement, and full custom-state fall presentation parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-guard-timing.json` trace checksum `ba77beec` promotes bounded owner-backed custom-state guarded timing metadata into `pnpm qa:trace`. A guarded direct `HitDef` records target memory, owner-local `TargetState` sends P2 into P1-owned state data, and `GetHitVar(guarded) = 1 && GetHitVar(slidetime) = 5 && GetHitVar(ctrltime) = 7 && GetHitVar(hitshaketime) > 0` branches `888 -> 901`; actor frames preserve `customOwnerId = p1`, target-link evidence preserves target id `77`, and `SelfState` returns P2 to state `0`/control. That trace aggregate was 326/326 artifacts, 296 required and 30 optional. This is bounded guarded target-memory-to-custom-state timing inheritance only; `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, visual/audio parity, score movement, and full custom-state/get-hit parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid.json` trace checksum `250f77c2` promotes bounded owner-backed custom-state `GetHitVar(hitcount/hitid/chainid)` metadata inheritance into `pnpm qa:trace`. Direct `HitDef id = 77, chainID = 43, numhits = 3` plus `p2stateno = 888` / `p2getp1state = 1` routes P2 into P1-owned state data, branches `888 -> 900` through `GetHitVar(hitcount) = 3 && GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, keeps target-link evidence for target id `77`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 325/325 artifacts, 295 required and 30 optional. This is bounded direct-HitDef numhits/id/chainID metadata inheritance in one attacker-owned custom-state route only; exact combo accumulation, chain-hit eligibility arbitration, helper/projectile inheritance, exact target lifetime, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN custom-state/get-hit parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-fallcount.json` trace checksum `c391d938` promotes bounded imported CNS `GetHitVar(fallcount)` evidence into `pnpm qa:trace`. A fall `HitDef` routes P2 into owner-backed Common1-style state `5100`; `HitFallDamage` records one ground-impact count and consumes `fall.damage`, then the state branches `5100 -> 328` through `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0`. That trace aggregate was 324/324 artifacts, 294 required and 30 optional. This is one bounded state-`5100` ground-impact read only; exact multi-ground-hit combo accumulation, non-Common1 ground-impact detection, lifetime/reset parity, helper/projectile/custom-state inheritance, score movement, and full fall/get-hit parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-snap.json` trace checksum `ce4680b9` promotes bounded owner-backed custom-state `GetHitVar(xoff/yoff/zoff)` snap-offset metadata inheritance into `pnpm qa:trace`. Direct `HitDef snap = 16,-24` plus `p2stateno = 888` / `p2getp1state = 1` routes P2 into P1-owned state data, branches `888 -> 899` through `GetHitVar(xoff) = 16 && GetHitVar(yoff) = -24 && GetHitVar(zoff) = 0`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 323/323 artifacts, 293 required and 30 optional. This is bounded direct-HitDef snap metadata inheritance in one attacker-owned custom-state route only; exact throw positioning, z-axis support, guard snap behavior, helper/projectile inheritance, broader custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN throw/custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-snap.json` trace checksum `312a53fc` promotes bounded owner-backed get-hit `GetHitVar(xoff/yoff/zoff)` snap-offset metadata into `pnpm qa:trace`. `RuntimeGetHitVars` stores `hitOffset`, `RuntimeHitVarSystem` reads `xoff`, `yoff`, and `zoff` with `0` fallback, and static `HitDef snap = 16,-24` survives typed compilation/import/direct-combat hit-var copies. Direct `HitDef snap` now applies a bounded contact position snap relative to the attacker, then P2 enters owner-backed state `5100` and branches `5100 -> 288` through `GetHitVar(xoff) = 16 && GetHitVar(yoff) = -24 && GetHitVar(zoff) = 0`. That trace aggregate was 322/322 artifacts, 292 required and 30 optional. This is bounded direct-HitDef snap metadata and simple direct-contact positioning only; exact throw positioning, z-axis support, guard snap behavior, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN throw/get-hit parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-hitcount.json` trace checksum `a4685842` promotes bounded defender-owned normal get-hit `GetHitVar(hitcount)` metadata into `pnpm qa:trace`. `RuntimeGetHitVars` stores `hitCount`, `RuntimeHitVarSystem` reads `hitcount` with `0` fallback, and static `HitDef numhits` survives typed compilation/import/direct-combat hit-var copies. Direct `HitDef numhits = 3` records defender hit vars, P2 enters defender-owned state `5000`, and branches `5000 -> 327` through `GetHitVar(hitcount) = 3 && !GetHitVar(guarded)`. That trace aggregate was 321/321 artifacts, 291 required and 30 optional. This is bounded direct-HitDef numhits metadata only; exact combo accumulation, multi-hit timing, helper/projectile/custom-state inheritance breadth, teams/simul, exact target lifetime/tick order, score movement, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-hitid-chainid.json` trace checksum `18df99ed` promotes bounded defender-owned normal get-hit `GetHitVar(hitid)` / `GetHitVar(chainid)` metadata into `pnpm qa:trace`. `RuntimeGetHitVars` stores `hitId` and `chainId`, `RuntimeHitVarSystem` reads `hitid` with `0` fallback and `chainid` with `-1` fallback, and static `HitDef chainID` survives typed compilation/import/direct-combat hit-var copies. Direct `HitDef id = 77, chainID = 43` records target memory, P2 enters defender-owned state `5000`, branches `5000 -> 326` through `GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && !GetHitVar(guarded)`, and keeps target-link evidence for target id `77`. That trace aggregate was 320/320 artifacts, 290 required and 30 optional. This is bounded direct-HitDef id/chainID metadata only; exact chain-hit eligibility arbitration, helper/projectile/custom-state inheritance breadth, teams/simul, exact target lifetime/tick order, score movement, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-yaccel.json` trace checksum `549fe48d` promotes bounded owner-backed custom-state `GetHitVar(yaccel)` metadata inheritance into `pnpm qa:trace`. `RuntimeHitVarSystem` reads stored direct-HitDef `yaccel` metadata before the legacy `0.44` fallback; the synthetic route stores `yaccel = 0.62`, routes P2 through P1-owned state data, branches `888 -> 898` through `GetHitVar(yaccel) = .62 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 319/319 artifacts, 289 required and 30 optional. This is bounded direct-hit yaccel metadata for an owner-backed custom-state route only; exact physics integration, fall acceleration arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-type.json` trace checksum `38542874` promotes bounded owner-backed custom-state `GetHitVar(type)` metadata inheritance into `pnpm qa:trace`. `RuntimeHitVarSystem` exposes `GetHitVar(type)` from stored direct-hit ground type metadata; the synthetic route stores `ground.type = Low` and `air.type = Trip`, routes P2 through P1-owned state data, branches `888 -> 897` through `GetHitVar(type) = 2 && GetHitVar(groundtype) = 2 && GetHitVar(airtype) = 3 && !GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 318/318 artifacts, 288 required and 30 optional. This is bounded direct-hit type metadata for an owner-backed custom-state route only; exact get-hit animation selection, air-hit arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-isbound.json` trace checksum `d25307e9` promotes bounded owner-backed custom-state `GetHitVar(isbound)` metadata inheritance into `pnpm qa:trace`. A direct `HitDef` records target memory, P1 executes `TargetBind` before owner-local `TargetState`, P2 enters P1-owned state data, branches `888 -> 896` through `GetHitVar(isbound) = 1 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 317/317 artifacts, 287 required and 30 optional. This is bounded TargetBind subject metadata for a direct-HitDef target-memory-to-custom-state route only; throws, exact bind tick-order/lifetime, visual bind parity, helper/root/parent redirects, teams/simul, `p2stateno`-on-guard behavior, score movement, and full MUGEN/IKEMEN custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-guarded.json` trace checksum `54f62821` promotes bounded owner-backed custom-state `GetHitVar` guarded metadata inheritance into `pnpm qa:trace`. A guarded direct `HitDef` records target memory, P1 executes owner-local `TargetState`, P2 enters P1-owned state data, branches `888 -> 895` through `GetHitVar(guarded) = 1 && GetHitVar(hitshaketime) > 0 && GetHitVar(hittime) > 0`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 316/316 artifacts, 286 required and 30 optional. This is bounded guarded target-memory-to-custom-state metadata inheritance only; `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-velocity.json` trace checksum `e9d8da9e` promotes bounded owner-backed custom-state `GetHitVar` velocity metadata inheritance into `pnpm qa:trace`. Direct `HitDef p2stateno = 888` with `p2getp1state = 1` and `ground.velocity = 4,-2` routes P2 into P1-owned state data, branches `888 -> 894` through `GetHitVar(xvel) = 4 && GetHitVar(yvel) = -2 && !GetHitVar(fall) && !GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, observes impact velocity extrema in state `888`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 315/315 artifacts, 285 required and 30 optional. This is bounded direct-hit velocity metadata inheritance only; exact velocity lifetime after later physics/controllers, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-down-recover.json` trace checksum `5bd94568` promotes bounded owner-backed custom-state `GetHitVar` down-recovery metadata inheritance into `pnpm qa:trace`. Direct fall `HitDef p2stateno = 888` with `p2getp1state = 1` routes P2 into P1-owned state data, branches `888 -> 893` through `GetHitVar(down.recover) = 1`, `GetHitVar(down.recovertime) = 45`, and alias `GetHitVar(recovertime) = 45`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState` to P2 state `0`/control. That trace aggregate was 314/314 artifacts, 284 required and 30 optional. This is bounded direct-hit down-recovery metadata inheritance only; exact lie-down tables, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, exact recovery threshold behavior, score movement, and full MUGEN/IKEMEN custom-state parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-controller-param-parentroot.json` trace checksum `9ad71f4e` promotes bounded helper-local controller-parameter `Parent` / `Root` redirect evidence into `pnpm qa:trace`. The synthetic imported fixture spawns a first-generation visual Helper, runs helper-local dynamic `VelSet x = Parent,Life - 995` / `y = Root,StateNo - 203`, then routes to state `1401` / anim `941` only after the helper reaches velocity `5,-3`. That trace aggregate was 309/309 artifacts, 279 required and 30 optional. This is bounded helper-local `VelSet` parameter evidence only; nested helper ancestry where root differs from parent, helper-spawned helpers, player `Parent` controller-param redirects, dynamic-parameter typed lowering, recursive redirection, debug warning text, teams/simul, score movement, and full helper/controller expression parity remain blocked.
- Current R2 sprite-effect ownership checkpoint: `RuntimeSpriteEffectControllerWorld` owns active-state sprite-effect dispatch for `SprPriority`, `PalFX`, `RemapPal`, `AfterImage`, `AfterImageTime`, `Trans`, and `Angle*`. It handles controller telemetry, typed `sprite-effect:*` operation selection, dynamic `SprPriority` / `PalFX` / `RemapPal` / `Trans alpha` / `AfterImage` / `AfterImageTime` / `Angle value/scale` resolver handoff, and mutation into `RuntimeSpriteEffectWorld` while `PlayableMatchRuntime` keeps trigger filtering, active-state order, hitpause selection, expression-context construction, and render projection. This is ownership cleanup only; exact visual tick order, palette/remap math, Trans alpha math, angle pivot/scale parity, helper/redirect ownership, renderer parity, score movement, and full presentation parity remain blocked.
- Previous R2 ownership checkpoint: `RuntimeMatchEnvColorBridgeWorld` owns the bounded match-level EnvColor callback bridge used by active, pause, and hitpause ignored-controller routes. `PlayableMatchRuntime` now delegates controller source, optional typed `envcolor` operation data, runtime tick, and `RuntimeEnvColorWorld` emission through one named boundary. Focused `RuntimeMatchEnvColorBridgeSystem` coverage proves typed-operation forwarding, tick preservation, stage-flash projection, and zero-time no-event behavior. This is ownership cleanup only; exact EnvColor blend math, layer/window ordering, pause layering/timing, renderer parity, score movement, and full presentation parity remain blocked.
- Previous R2 ownership checkpoint: `RuntimeControllerExpressionContextSystem` owns the shared raw controller-number expression context used by passive/runtime controller worlds. Extracted consumers route dynamic numeric params through one redirect-aware helper for `Target(...)`, `Parent`, `Root`, `Const`, `HitPauseTime`, `StageTime`, and `GetHitVar` reads instead of duplicating local evaluator setup. Focused `RuntimeControllerExpressionContextSystem` coverage proves redirect-aware numeric evaluation plus helper/team metadata forwarding. This is ownership cleanup only; no new expression language support, no `ID`/player unique-id semantics, no recursive redirection, no helper/team scope expansion, no score movement, and no full controller VM parity are claimed.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-assertspecial-helper-explod-shadow.json` trace checksum `83f61b48` adds bounded `GlobalNoShadow` helper/explod actor-frame evidence on top of the earlier shadow renderer cut. The fixture asserts `GlobalNoShadow` in imported state `200`, spawns a helper-parented Explod, and requires `shadowVisible=false` actor-frame evidence for P1, P2, the helper, and the explod. Previous required `synthetic-imported-assertspecial-shadow-telemetry.json` trace checksum `2b9c8fac` remains the local/global player shadow telemetry oracle for normalized `noshadow` and `globalnoshadow`, and `RuntimeSnapshotWorld.match()` plus `CharacterRenderer` still own bounded snapshot/render shadow suppression. At that checkpoint trace aggregate was 308/308 artifacts, 278 required and 30 optional. Exact MUGEN/IKEMEN shadow skew, stage shadow parameters, projectile shadow semantics, pause/layer behavior, exact global/team/helper ownership beyond the current spawned helper/explod route, score movement, and full renderer/global ownership parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-assertspecial-global-telemetry.json` checksum `fc793d29` promotes bounded `AssertSpecial` global flag telemetry into required trace evidence. The synthetic imported fixture executes official-style grouped `AssertSpecial` controllers for normalized `nobardisplay`, `nobg`, `nofg`, `nokosnd`, and `nomusic`, and the gate requires final-actor `assertSpecialGlobalFlags` evidence. This is telemetry/gate coverage only; lifebar hiding, stage BG/FG rendering suppression, KO sound suppression, music pause, exact global/team/helper ownership, pause/layer behavior, score movement, and full MUGEN/IKEMEN global flag parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-default-liedown-fast-recovery.json` checksum `74bdac97` promotes the positive side of bounded fast lie-down recovery into required trace evidence. The synthetic imported fixture omits the authored `5110` `Get Up` `ChangeState`, keeps fresh `recovery` input active while P2 is in Common1-style state `5110` with positive `down.recovertime`, and still routes `5110 -> 5120 -> 0` through `RuntimeRecoverySystem.advanceCommon1LieDownRecovery`. Previous `synthetic-imported-assertspecial-nofastrecoverfromliedown.json` checksum `74bf5d85` remains required and proves the IKEMEN `NoFastRecoverFromLieDown` flag suppresses that bounded fast shortcut until `down.recovertime` reaches `0`. This is bounded synthetic recovery evidence only; exact input-mashing thresholds, exact Common1 controller-loop timing, public KFM proof, global/team/helper ownership, pause/hitpause layering, score movement, and full recovery parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-controller-param-root-redirect.json` checksum `1d4a73f7` promotes bounded dynamic controller-parameter `Root` redirect support into required trace evidence. A synthetic imported state seeds P1 velocity with static `VelSet x = 4, y = -2`, then runs dynamic `VelSet` params that read `Root, Life` and `Root, StateNo` from the current player root context and set P1 velocity to `5,-3`; the gate requires named controller order, the static typed `kinematic:velset` seed, and actor-frame velocity evidence. This is bounded current player `VelSet` controller-param Root redirect evidence only; player `Parent` controller-param redirects, nested helper ancestry where root differs from parent, helper-spawned helpers, dynamic-parameter typed lowering, recursive redirect evaluation, debug warning text, broad bottom/redirect parity for every controller/parameter family, teams/simul, helper-owned custom-state target breadth, score movement, and full MUGEN/IKEMEN controller/expression parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-controller-param-target-redirect.json` checksum `55bb7b1f` promotes bounded dynamic controller-parameter `Target(77)` redirect support into required trace evidence. A synthetic imported state seeds P1 velocity with static `VelSet x = 4, y = -2`, lands a direct `HitDef` target id `77`, then runs dynamic `VelSet` params that read `Target(77), Life` and set P1 velocity to `3,-3`; the gate requires target-link evidence, named controller order, the static typed `kinematic:velset` seed, `HitDef`, and actor-frame velocity evidence. This is bounded current `VelSet` controller-param Target redirect evidence only; broad controller-param redirect parity remains blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-controller-param-bottom.json` checksum `28ef21ad` promotes bounded controller-parameter bottom fallback into required trace evidence. A synthetic imported state seeds P1 velocity with static `VelSet x = 4, y = -2`, then runs a dynamic `VelSet` whose `Parent,Vel X` / `Parent,Vel Y` parameters evaluate through a missing parent redirect and collapse to `0,0`; actor-frame evidence observes velocity range `4 -> 0` and `-2 -> 0`, and named controller order preserves the static typed `kinematic:velset` seed before the dynamic fallback controller. This is bounded current controller-param bottom evidence only; broad bottom parity remains blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-target-ifelse-bottom.json` checksum `be7554d4` promotes bounded `IfElse(...)` invalid-redirect branch-result isolation into required trace evidence. A direct `HitDef` target-memory route now forbids state `400` when `IfElse(0, MoveHit >= 1, (Target(999), Life = 0))` selects the missing target branch, then falls through to state/action `401` when `IfElse(1, MoveHit >= 1, (Target(999), Life = 0))` selects the valid `MoveHit` branch while the unused missing branch remains isolated from the returned result. Focused CNS coverage also proves `IfElse(...)` remains eager and still reports warning-style unsupported diagnostics for unused missing redirects, unlike `Cond(...)`. This is bounded `IfElse` branch-result isolation evidence only; Cond-style lazy evaluation, recursive redirection, debug warning text, broader invalid-destination bottom parity, teams/simul, target mutation through redirects, score movement, and full MUGEN/IKEMEN redirect/bottom parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-target-cond-bottom.json` checksum `e882a2bb` promotes bounded `Cond(...)` invalid-redirect bottom isolation into required trace evidence. A direct `HitDef` target-memory route forbids state `398` when `Cond(1, (Target(999), Life = 0), MoveHit >= 1)` selects the missing target branch, then falls through to state/action `399` when `Cond(0, (Target(999), Life = 0), MoveHit >= 1)` skips that missing target branch and selects the valid fallback. Focused CNS coverage also proves skipped `Cond(...)` branches do not call the unsupported-redirect reporter while selected missing redirects still fail closed. This is bounded `Cond` branch-selection evidence only; recursive redirection, debug warning text, broader invalid-destination bottom parity, teams/simul, target mutation through redirects, score movement, and full MUGEN/IKEMEN redirect/bottom parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-target-redirect-bottom.json` checksum `5e50a90a` promotes bounded invalid-redirect `bottom` propagation into required trace evidence. A direct `HitDef` target-memory route forbids state `396` when `(Target(999), Life = 0) || 1` evaluates with a missing target destination, then falls through to state/action `397`; focused CNS coverage also proves missing `Parent` redirects propagate through composite expressions while unused `IfElse(...)` branches stay isolated. This is bounded parser-expression bottom evidence only; recursive redirection, debug warning text, broader invalid-destination bottom parity, teams/simul, target mutation through redirects, score movement, and full redirect/bottom parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-parentroot.json` checksum `5154220c` promotes bounded helper-local `Parent` / `Root` redirect support from focused/unit evidence into a required trace. A visual Helper routes from state `1200` to `1400` / anim `940` only after reading cloned owner/root runtime state through `Parent,StateNo`, `Parent,Vel X`, `Root,Anim`, and a redirected-value `ChangeState` expression. This is read-only first-generation visual-helper evidence only; nested helper ancestry where root differs from parent, player-state parent/root ownership, indexed/team/helper-owned redirects beyond caller-provided `EnemyNear(index)` lists, redirect mutation, keyctrl, score movement, and full MUGEN/IKEMEN helper redirect parity remain blocked.
- Latest R2 ownership checkpoint: `RuntimeFighterAdvanceHookSetWorld` owns bounded per-fighter advance hook-set construction outside `PlayableMatchRuntime`. Sprite effects, hit eligibility, HitOverride ticking, contact timers, state clock, frame constraints, recovery windows, stun, move lifecycle, kinematics, animation, active controllers, recovery landing, lie-down recovery, and frozen-position preservation now pass through one named seam before `RuntimeFighterAdvanceWorld` executes. Focused `RuntimeFighterAdvanceHookSetSystem`, `RuntimeFighterAdvanceSystem`, and `RuntimeMatchFighterAdvanceSystem` coverage proves the handoff. This is ownership cleanup only; no new player-advance semantics, exact MUGEN/IKEMEN tick order, persistent-controller timing, helper/team/redirect actor advance semantics, recovery/stun/physics arbitration, visual/audio parity, score movement, or full player VM parity is claimed.
- Previous R2 ownership checkpoint: `RuntimeActiveExpressionContextWorld` owns bounded active CNS expression-context factory construction outside `PlayableMatchRuntime`. Dynamic controller-param fallback and trigger evaluation share stage bounds/time, owner const routing, runtime RNG, animation timing callbacks, and `InGuardDist` through one named seam before `RuntimeDispatchEvaluationWorld` / `RuntimeTriggerEvaluationWorld` evaluate expressions. Focused coverage proves the handoff. This is ownership cleanup only; no new expression semantics, exact CNS VM timing, helper/team/redirect expansion, exact `InGuardDist` parity, deterministic MUGEN/IKEMEN RNG stream parity, visual/audio parity, score movement, or full expression/trigger VM parity is claimed.
- Previous focused R1 recovery checkpoint: required `synthetic-imported-assertspecial-nogetupfromliedown.json` checksum `4c3b6281` promotes bounded IKEMEN `AssertSpecial NoGetUpFromLieDown` support from unit-only to trace-gated evidence. Static flags lower into typed `assertspecial` operation data, runtime execution exposes `noGetUpFromLieDown`, and the Common1 hardcoded `5110 -> 5120` get-up transition stays suppressed while state `5110` counts `down.recovertime` to `0`; the trace forbids `5120` and leaves P2 in `5110`. Focused `RuntimeRecoverySystem`, `RuntimeCompiler`, and `RuntimeCnsSubset` coverage still proves the compiler/runtime seam. This is bounded synthetic get-up suppression evidence only; public KFM proof, exact controller-loop tick order, global/team/helper ownership, pause/hitpause layering, score movement, and full recovery parity are not claimed.
- Latest runtime/QA hardening checkpoint: `scripts/qa_traces.cjs` now requires `synthetic-imported-default-fall-official-air-recovery` in the required artifact coverage-summary contract, matching the already required `synthetic-imported-default-fall-official-air-recovery.json` checksum `b0363be9` artifact. This is evidence-pipeline hardening only; no runtime behavior, checksum, score, public KFM, exact threshold, velocity, or full recovery-parity claim changed.
- Previous required R1 Common1 checkpoint: `synthetic-imported-air-guard-landing.json` checksum `d6986d7f` is required by `pnpm qa:trace` and proves bounded synthetic air guard landing walk-control: held-back airborne P2 blocks an A-guardable direct `HitDef`, routes defender-owned `154 -> 155 -> 52 -> 20`, preserves active `holdback`/`x`, executes `155` `HitVelSet`, `VelAdd`, `CtrlSet`, landing `VelSet`, landing `PosSet`, and `ChangeState` with typed `kinematic:*` / `resource:ctrlset` ops, records a state-`52` y = 0 actor frame, and ends in imported walk state/action `20` with control. At that checkpoint trace aggregate was 296/296 artifacts, 266 required and 30 optional. This is bounded portable synthetic evidence only; exact MUGEN/IKEMEN air guard physics, exact landing timing, state-`52` internals, proximity guard, guard effects, visual/audio parity, score movement, public KFM support, and full guard parity remain blocked.
- Latest optional private-fixture R1 Common1 checkpoint: `kfm-official-default-air-guard-state.json` checksum `f4378971` passes when `.scratch/fixtures/kfm-official.zip` exists and requires real KFM/Common1 air guard-hit actor-frame order `154 -> 155 -> 52 -> 20`, with final held-back walking state/action `20` and control. This is stricter private-fixture evidence on the existing optional KFM air guard route only; public bundled KFM support, required portable KFM-specific fixture coverage when the private fixture is absent, exact air guard physics, exact landing timing, state-`52` internals, guard-start/end, proximity guard, guard effects, sparks, sounds, visual/audio parity, score movement, and full guard parity remain blocked.
- Previous optional private-fixture R1 Common1 checkpoint: `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` passes when `.scratch/fixtures/kfm-official.zip` exists and proves real KFM/Common1 crouch guard-hit `152 -> 153 -> 131 -> 11` with preserved `153` slide-stop controller/typed-operation order, active `holdback`/`holddown`/`x`, and final KFM crouch state/action `11` with control. Previous required portable R1 Common1 checkpoint: `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699` is required by `pnpm qa:trace` and proves a portable bounded crouch guard-hit route: P2 blocks direct contact while holding down-back, routes `152 -> 153 -> 130 -> 10`, preserves the stricter `153` slide-stop controller/typed-operation order, keeps active `holdback`/`holddown`/`x` command evidence, and ends in imported crouch state/action `10` with control. At that checkpoint trace aggregate was 296/296 artifacts, 266 required and 30 optional. Previous required R1 guard checkpoint: `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c` proves the matching portable stand guard-hit route `150 -> 151 -> 130 -> 20`. Previous optional private-fixture stand mirror: `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da` proves the same real KFM/Common1 stand guard-hit walk-control shape when `.scratch/fixtures/kfm-official.zip` exists. Previous required R1 Common1 checkpoint: `synthetic-imported-default-fall-ground-recovery-priority.json` checksum `e83b2db7` proves bounded near-ground recovery priority through `5000 -> 5030 -> 5050 -> 5200 -> 5201 -> 52 -> 0` while `5210` and lie-down chain states remain forbidden. This is private-fixture KFM crouch plus bounded synthetic crouch/stand guard-hold return and ground-over-air recovery evidence only; public bundled KFM support, required portable KFM-specific fixture coverage when the private fixture is absent, exact guard-hold duration, guard timing/proximity/effects, exact ground/air recovery arbitration thresholds, exact recovery tables, velocity math, controller-loop timing, visual/audio parity, score movement, and full guard/fall/recovery parity remain blocked. Previous R1 Common1 checkpoint: `synthetic-imported-hitfall-recovery-input-priority.json` checksum `bae07bde` proves bounded generic recovery input priority over a competing `HitFall && CanRecover` probe through `5000 -> 5030 -> 5050 -> 5210 -> 0`.
- Previous R1 Common1 checkpoint: `synthetic-imported-hitfall-recover-false.json` checksum `236df0a8` remains required by `pnpm qa:trace` and proves a bounded synthetic `HitFall && !GetHitVar(fall.recover) && !CanRecover` branch inside defender-owned fall state `5050`: P2 takes a fall `HitDef` with `fall.recover = 0` and no `p2stateno`, routes `5000 -> 5030 -> 5050 -> 5230 -> 0`, executes ordered `HitVelSet`, `VelAdd`, and named `HitFall Recover Disabled Probe` `ChangeState`, requires positive `fall.recovertime` metadata in `5050` and `5230`, and forbids recovery states `5210`/`5200`. This is bounded recover-disabled trigger/order evidence only; exact recovery threshold tables, exact Common1 controller-loop timing, recovery arbitration, fall/CanRecover precedence, visual/audio parity, score movement, and full fall/recovery parity remain blocked.
- Previous R1 Common1 checkpoint: `synthetic-imported-hitfall-false.json` checksum `1d538e43` remains required by `pnpm qa:trace` and proves a bounded synthetic normal get-hit `!HitFall && !GetHitVar(fall)` branch: P2 takes a direct `HitDef` without fall metadata or `p2stateno`, enters defender-owned `5000`, branches through named `Normal HitFall False Probe` into state/action `325`, requires ordered actor-frame evidence `5000 -> 325`, final P2 state `325` / moveType `H`, and forbids fall/recovery states `5001`, `5030`, `5050`, `5210`, and `5200`. This is bounded HitFall-false trigger evidence only; exact normal get-hit timing, fall arbitration, custom-state/helper/team inheritance, visual/audio parity, score movement, and full Common1 get-hit parity remain blocked.
- Previous R1 Common1 checkpoint: `synthetic-imported-hitfall-canrecover.json` checksum `7cf7ab46` remains required by `pnpm qa:trace` and proves a bounded synthetic `HitFall && !CanRecover` branch inside defender-owned fall state `5050`: P2 takes a fall `HitDef` without `p2stateno`, routes `5000 -> 5030 -> 5050 -> 5220 -> 0`, executes ordered `HitVelSet`, `VelAdd`, and named `HitFall CanRecover Probe` `ChangeState`, requires positive `fall.recovertime` evidence in `5050` and `5220`, and forbids recovery states `5210`/`5200`. That checkpoint passed at 277/277 artifacts, 252 required and 25 optional. This is bounded trigger/order evidence only; exact recovery threshold tables, exact Common1 controller-loop timing, recovery arbitration, visual/audio parity, score movement, and full fall/recovery parity remain blocked.
- Previous R1 Common1 checkpoint now superseded by the latest required air guard landing walk-control wording: `synthetic-imported-air-guard-landing.json` checksum `d6986d7f` still owns the portable synthetic route and now requires `154 -> 155 -> 52 -> 20` plus final walk/control evidence, not only the earlier `52` landing bucket.
- Latest R1 audio checkpoint: `synthetic-imported-sound.json` checksum `cc9c8c49` now requires static `PlaySnd lowpriority = 1`, `volume = -8`, `volumescale = 50`, `freqmul = 0.5`, `loop = 1`, `pan = 32`, and `abspan = -64` to survive compiler/runtime/trace handoff as `RuntimeSoundEvent.lowPriority` / `legacyVolume` / `volumeScale` / `freqMul` / `loop` / `pan` / `absPan` beside `PlaySnd channel = 2`, `SndPan channel = 2` / `pan = -48`, and `StopSnd channel = 2` telemetry. Focused tests also prove static `abspan` lowering/event projection and bounded panner normalization. `MugenAudioSystem` routes explicit-channel decisions, gain, playback rate, and stereo pan through named pure boundaries: normal `PlaySnd` replaces an active non-negative channel, low-priority `PlaySnd` skips an already active explicit channel, `SndPan` updates an active explicit channel panner, `StopSnd channel = -1` / omitted channel stops all tracked plus unchannelled sources, `volumescale` scales decodable SND playback within 0-100 bounds, `freqmul` applies bounded playback-rate scaling, `loop = 1` enables Web Audio source looping, `pan` resolves from actor position/facing/camera, and `abspan` resolves from screen center before both clamp to Web Audio `[-1, 1]`. This is bounded Web Audio channel arbitration plus legacy-volume diagnostics, `volumescale` / `freqmul` / `loop` / `pan` / `abspan` / `SndPan` only; exact priority classes, dynamic params, pre-RC8 legacy `volume` gain semantics, global channel fallback, pause/superpause audio behavior, timing/mixing, score movement, and full MUGEN/IKEMEN audio parity remain blocked.
- Latest R2 presentation snapshot ownership checkpoint: `RuntimeMatchPresentationSnapshotWorld` owns bounded match presentation snapshot input construction outside `PlayableMatchRuntime`: camera shake, stage flash, and per-player effect snapshot groups are collected through one seam before `RuntimeSnapshotWorld.match()` builds the renderer-independent snapshot. Focused `RuntimeMatchPresentationSnapshotSystem` coverage proves shake/flash/effect-group forwarding and P1/P2 ordering. This is ownership cleanup only; exact stage/motif camera logic, effect lifecycle semantics, renderer/audio parity, visual/debug UI parity, score movement, and full match snapshot parity remain blocked.
- Previous R2 active-controller telemetry ownership checkpoint: `RuntimeActiveControllerTelemetryWorld` owns bounded active-controller telemetry hook construction outside `PlayableMatchRuntime`: active state hooks, side-effect dispatchers, and fallback runtime-controller dispatch now share one controller/operation telemetry hook set before forwarding into `RuntimeCompatibilityTelemetryWorld`. Focused `RuntimeActiveControllerTelemetrySystem` coverage proves controller and operation forwarding through the seam. This is ownership cleanup only; exact telemetry event semantics, imported-only filtering, event retention limits, helper/team/redirect telemetry breadth, visual/debug UI parity, score movement, and full CNS VM parity remain blocked.
- Previous R2 combat/helper ownership checkpoint: `RuntimeMatchCombatStateHooksWorld` owns bounded combat state-hook adapter construction outside `PlayableMatchRuntime`: direct/projectile combat hooks preserve state-owner availability and entry options, while helper combat hooks keep self-owned availability checks and still forward entry options into state entry. Focused `RuntimeMatchCombatStateHooksSystem` coverage proves both contracts; the match runtime now creates both hook sets through the seam before `RuntimeMatchCombatBridgeWorld` forwards them into direct/projectile/helper combat. This is ownership cleanup only; helper-owned custom-state table breadth, throws, teams/simul actor registries, multi-target helper ownership, exact combat/helper tick order, visual/audio parity, score movement, and full combat/helper VM parity remain blocked.
- Previous R2 helper/effect lifecycle ownership checkpoint: `RuntimeMatchOpponentContextWorld` owns bounded current 1v1 match-opponent context construction for lifecycle bridges outside `RuntimeMatchInteractionWorld`, `RuntimePausedMatchWorld`, and `RuntimeHitPauseWorld`: mirrored P1/P2 opponent selection, singleton lifecycle `opponents` list projection, and unknown-actor fail-closed behavior now route through one named seam. Focused `RuntimeMatchOpponentContextSystem` coverage proves mirrored contexts and fail-closed behavior; existing match/pause/hitpause coverage proves active, pause, and hitpause callers still forward the same direct opponent plus explicit one-opponent lifecycle list. This is ownership cleanup only; real teams/simul roster ownership, automatic multi-opponent match discovery, helper-owned opponent roster discovery, richer identity beyond actor refs, exact helper lifecycle/pause/combat ordering, visual/audio parity, score movement, and full match/helper VM parity remain blocked.
- Previous R2 helper/effect lifecycle ownership checkpoint: `RuntimeEffectHelperContextWorld` owns bounded visual Helper advance context construction outside `RuntimeEffectLifecycleWorld`: complete-owner validation, parent/root runtime-state projection, current opponent id/state fallback, explicit lifecycle opponent-list to nearest-order helper `opponentRoster` projection, explicit roster override, target-candidate forwarding, and helper `TargetState` / telemetry hook forwarding now sit behind one named seam. Focused `RuntimeEffectHelperContextSystem` coverage proves nearest roster ordering, explicit roster preservation, target/hook forwarding, and incomplete-owner fail-closed behavior; `EffectLifecycleSystem` coverage proves the lifecycle caller still forwards the same options. This is ownership cleanup only; real teams/simul lifecycle roster ownership, automatic multi-opponent match discovery, helper-owned opponent roster discovery, richer identity beyond ids/runtime state, exact helper lifecycle/pause/combat ordering, visual/audio parity, score movement, and full Helper VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchHelperProjectileTargetWorld` owns the bounded match-level helper-parented Projectile target-memory bridge outside `PlayableMatchRuntime`. The active post-fighter combat route now forwards owner, defender, projectile, and `RuntimeTargetWorld` through one named seam before lower helper target-memory logic runs. Focused `RuntimeMatchHelperProjectileTargetSystem` coverage proves forwarding and owner-projectile fail-closed behavior. This is ownership cleanup only; helper-owned Projectile contact timing, helper-owned custom-state tables, teams/simul actor registries, multi-target helper ownership, exact target lifetime, visual/audio parity, score movement, and full Helper/Projectile VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchHelperBindingWorld` owns bounded match-level helper callback wiring outside `PlayableMatchRuntime`: helper-owned `TargetState` owner handlers and helper-local Projectile telemetry handlers now attach through one named seam, while target entry still delegates to `RuntimeMatchHelperTargetStateWorld` and telemetry filtering still delegates to `RuntimeHelperTelemetryWorld`. Focused `RuntimeMatchHelperBindingSystem` coverage proves owner-specific target-state route forwarding, stale handler replacement, helper-state/owner-state telemetry attribution, and non-Projectile telemetry ignore behavior. This is ownership cleanup only; helper-owned custom-state tables, throws, teams/simul actor registries, multi-target helper ownership, exact helper TargetState/projectile timing, broad helper telemetry semantics, visual/audio parity, score movement, and full Helper VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchHelperTargetStateWorld` owns bounded match-roster target resolution for helper-owned `TargetState` entry outside `PlayableMatchRuntime`: helper owner validation still routes through `RuntimeHelperTargetStateWorld`, but the match-level actor roster lookup, state availability hook, and state-entry hook now sit behind a named seam. Focused `RuntimeMatchHelperTargetStateSystem` coverage proves roster-backed target resolution, stale target payload isolation, missing-target no-op behavior, and owner-mismatch fail-closed behavior. This is ownership cleanup only; helper-owned custom-state tables, throws, teams/simul actor registries, multi-target helper ownership, exact helper TargetState timing, visual/audio parity, score movement, and full Helper VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeSnapshotWorld.match()` owns the bounded full match snapshot envelope outside `PlayableMatchRuntime`: selected P1 action, playback/speed/toggles, match pause handoff, stage/camera snapshot, round snapshot, player actor snapshots, effect snapshots, compatibility-session handoff, and 80-line log trimming route through one named boundary. Focused `RuntimeSnapshotSystem` coverage proves envelope fields, actor/effect clone isolation, and log cap behavior. This is ownership cleanup only; snapshot schema changes, compatibility telemetry semantics, exact camera/effect/renderer parity, visual/audio parity, score movement, and full match VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchTickBranchWorld` owns bounded per-tick branch arbitration outside `PlayableMatchRuntime`: hitpause consumes the tick before pause/active, match pause consumes the tick before active when hitpause is clear, and active match runs only when neither pause layer owns the tick. Focused `RuntimeMatchTickBranchSystem` coverage proves the order. This is ownership cleanup only; exact MUGEN/IKEMEN pause layering/arbitration, hitpause/pause tick semantics, helper/team actor scheduling, visual/audio parity, score movement, and full match VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchStepWorld` owns bounded public match-step cadence outside `PlayableMatchRuntime`: stopped playback returns a snapshot without ticking frame clock, forced steps advance exactly once, sub-1x speed samples on frame-clock divisors, multi-step speed loops advance up to the requested iteration count, and round-over checks stop remaining iterations. Focused `RuntimeMatchStepSystem` coverage proves those cadence edges. This is ownership cleanup only; exact MUGEN/IKEMEN pause/round arbitration, frame pacing, replay/rollback timing, helper/team actor scheduling, visual/audio parity, score movement, and full match VM parity remain blocked.
- Previous R1 required trace restoration checkpoint: `synthetic-imported-envshake.json` checksum `061f17d5` is again emitted as a required `pnpm qa:trace` artifact. It proves bounded imported `EnvShake` controller evidence through `ChangeState`, `EnvShake`, `HitDef`, typed `envshake` operation telemetry, and a `RuntimeEnvShakeEvent` for P1 with `time = 16`, `freq = 30`, `ampl = -7`, `phase = 0.5`, and `stateNo = 200`. Current `pnpm qa:trace` aggregate after later Common1 gates is 280/280 artifacts, 255 required and 25 optional. This restores required evidence already backed by preset/unit coverage; it does not add exact camera waveform, pause/stage/layer behavior, helper/redirect ownership, visual parity, score movement, or full MUGEN/IKEMEN presentation parity.
- Previous R1 required Common1 crouch guard slide-stop checkpoint: `synthetic-imported-crouch-guard-slide-stop.json` checksum `2bea7311` proves bounded crouch guard-hit routing through defender-owned `152 -> 153 -> 130` after a direct guardable `HitDef`, with active `holddown`/`x` command evidence, required controller/typed-operation order `152:ChangeAnim -> 152:ChangeState -> 153:HitVelSet -> kinematic:hitvelset -> 153:VelSet -> kinematic:velset -> 153:CtrlSet -> resource:ctrlset -> 153:ChangeState`, plus actor-frame velocity evidence showing crouch guard-slide velocity before stop/control. Newer required `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699` extends this portable synthetic route with active down-back evidence and final state/action `10` control; newer optional `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` proves the private real KFM route `152 -> 153 -> 131 -> 11` when the local fixture exists. Current `pnpm qa:trace` aggregate is 296/296 artifacts, 266 required and 30 optional. This is bounded Common1-style crouch guard slide-stop/control, synthetic return-to-crouch-control, and optional private KFM route-shape evidence only; exact guard timing, proximity guard, guard effects, controller-loop tick parity, visual/audio parity, score movement, public KFM support, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required Common1 stand guard slide-stop checkpoint: `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641` proves bounded stand guard-hit routing through defender-owned `150 -> 151 -> 130` after a direct guardable `HitDef`, with required controller/typed-operation order `150:ChangeAnim -> 150:ChangeState -> 151:HitVelSet -> kinematic:hitvelset -> 151:VelSet -> kinematic:velset -> 151:CtrlSet -> resource:ctrlset -> 151:ChangeState`, plus actor-frame velocity evidence showing guard-slide velocity before stop/control. That required checkpoint passed `pnpm qa:trace` at 271/271 artifacts, 248 required and 23 optional. This is bounded Common1-style stand guard slide-stop/control evidence only; exact guard timing, proximity guard, guard effects, crouch/air parity, controller-loop tick parity, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous required R1 guard checkpoint: `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c` promotes the stand guard-hold walk-control route into portable synthetic coverage. Latest optional R1 KFM/Common1 guard checkpoints: `kfm-official-default-air-guard-state.json` checksum `f4378971`, `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da`, and `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` pass when private `.scratch/fixtures/kfm-official.zip` is present. They extend the existing air/stand/crouch KFM guard routes with actor-frame sequence evidence that real KFM/Common1 air guard-hit `154 -> 155 -> 52 -> 20` lands and resumes held-back walking state/action `20` with control, real KFM/Common1 stand guard-hit `150 -> 151` returns through observable guard-hold state `130` and then resumes held-back walking state/action `20` with control, and real KFM/Common1 crouch guard-hit `152 -> 153` returns through observable crouch guard-hold state `131` and then resumes held-down crouch state/action `11` with control. The previous optional stand hold-only mirror remains `kfm-official-default-guard-hold-return.json` checksum `885bb1da`; the previous optional crouch slide-stop mirror remains `kfm-official-default-crouch-guard-slide-stop.json` checksum `d11153d0`. At that checkpoint, `pnpm qa:trace` passed 296/296 artifacts, 266 required and 30 optional; current aggregate is 444/444 artifacts, 414 required and 30 optional after the dynamic `Trans alpha` gate. This is bounded synthetic plus private-fixture confidence only; public bundled KFM support, required portable KFM-specific fixture coverage when the private fixture is absent, exact guard-hold duration, exact guard timing, proximity guard, guard effects, exact air guard physics/landing timing/state-`52` internals, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchActiveWorld` owns bounded normal active-match orchestration outside `PlayableMatchRuntime` after hitpause/pause gates: round timer tick, normal command-buffer writes, input control, fighter advance, post-fighter interaction/combat, and round-finish handling route through one named boundary. Focused `RuntimeMatchActiveSystem` coverage proves order and sub-world result preservation. This is ownership cleanup only; exact MUGEN/IKEMEN active tick order, pause-start arbitration, helper/team/redirect actor ownership, combat priority parity, visual/audio parity, score movement, and full match VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchHitPauseWorld` owns bounded normal active-match hitpause handoff outside `PlayableMatchRuntime`: the loop now delegates the `RuntimeHitPauseWorld.advanceRuntime` route, carries P1/P2 inputs, stage/runtime ticks, paused presentation lifecycle, and ignorehitpause controller execution through one named boundary before the normal pause branch. Focused `RuntimeMatchHitPauseSystem` coverage proves delegate payload forwarding and paused/not-paused result preservation. This is ownership cleanup only; exact MUGEN/IKEMEN hitpause tick order, pause arbitration/layering, helper/team/redirect hitpause ownership, visual/audio parity, score movement, and full match VM parity remain blocked.
- Previous R2 MatchWorld ownership checkpoint: `RuntimeMatchFrameStartWorld` owns bounded normal active-match frame-start ordering outside `PlayableMatchRuntime`: per-frame hit/assert flag reset, pre-facing `AssertSpecial`, and auto-facing route through one named boundary before hitpause/pause handling. Focused `RuntimeMatchFrameStartSystem` coverage proves reset-before-assert and assert-before-facing order for both players. This is ownership cleanup only; exact MUGEN/IKEMEN frame-start tick order, pause/hitpause arbitration, helper/team/redirect frame-start ownership, visual/audio parity, score movement, and full match VM parity remain blocked.
- Previous R1 required GetHitVar velocity checkpoint: `synthetic-imported-gethitvar-velocity.json` checksum `878a03f7` proves a defender-owned Common1-style normal get-hit CNS can branch from state `5000` into state/action `324` through `GetHitVar(xvel) = 4 && GetHitVar(yvel) = -2 && !GetHitVar(fall) && !GetHitVar(guarded)` after direct `HitDef` contact with authored `ground.velocity = 4,-2`. `RuntimeExpressionContextWorld` and the shared `runtimeHitVar` path expose the stored normal hit velocity as stable impact metadata for get-hit CNS. That checkpoint passed `pnpm qa:trace` at 270/270 artifacts, 247 required and 23 optional. This is bounded direct-contact normal-hit velocity metadata trigger evidence only; exact velocity lifetime after later physics/controllers, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Previous R1 required GetHitVar damage checkpoint: `synthetic-imported-gethitvar-damage.json` checksum `2c726114` proves a defender-owned Common1-style normal get-hit CNS can branch from state `5000` into state/action `323` through `GetHitVar(damage) = 37 && !GetHitVar(guarded)` after direct `HitDef` contact. Direct and projectile combat now store bounded applied damage in shared `RuntimeGetHitVars.damage`, and `RuntimeExpressionContextWorld` exposes it through the existing `HitVar` read path. This is bounded normal-hit damage metadata trigger evidence only; exact damage lifetime/rounding, guard-chip semantics, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Previous R1 required Common1 default crouch get-hit progression checkpoint: `synthetic-imported-default-crouch-gethit-progression.json` checksum `fd986a9e` proves a held-crouch imported defender can route a direct `HitDef` without `p2stateno` into defender-owned `5010`, advance through `HitShakeOver` into `5011`, and return through `HitOver` to idle/control. Evidence includes ordered controller events for `5010:ChangeState -> 5011:ChangeState`, ordered actor frames `5010 -> 5011`, crouch `stateType = C`, `physics = N/C`, `Clsn2` telemetry, final checksum `d6b64044`, and gate label `imported-default-crouch-gethit-progression-golden`. This is bounded crouch HitShakeOver/HitOver progression evidence only; exact MUGEN/IKEMEN tick timing, exact crouch get-hit animation/slide tables, fall routing, custom-state/helper/team breadth, visual/audio parity, score movement, and full Common1 get-hit parity remain blocked.
- Latest optional R1 KFM/Common1 crouch get-hit progression checkpoint: `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae` passes when private `.scratch/fixtures/kfm-official.zip` is present. The gate confirms real KFM can hold crouch, execute state `11` before contact, enter Common1 `5010`, advance to `5011` through `HitShakeOver`, and return to `0`/control through `HitOver` after a direct `HitDef` without `p2stateno`; KFM-specific actor frames pin `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width `39/39`, final checksum `f469a942`, and gate label `imported-default-crouch-gethit-progression-golden`. This is private-fixture confidence only; public KFM support, exact tick timing, exact crouch get-hit animation/slide tables beyond this frame profile, fall routing, custom-state/helper/team breadth, visual/audio parity, score movement, and full Common1 get-hit parity remain blocked.
- Latest optional R1 KFM/Common1 air-entry recovery fixture checkpoint: `kfm-official-default-air-fall-recovery-input.json` checksum `3bce8aba` and `kfm-official-default-air-fall-recovery-too-early.json` checksum `b199382a` pass when private `.scratch/fixtures/kfm-official.zip` is present. These gates confirm real KFM airborne recovery routes through `5020 -> 5030 -> 5035 -> 5050`, including KFM's intermediate `5035` state: the input route accepts `command = "recovery"` after the bounded `5030` countdown reaches `0`, enters `5210`, lands through `52`, and returns to idle/control; the too-early route keeps P2 in `5050` while `fall.recovertime` stays positive and forbids `5210`, `5200`, `5201`, `52`, `5100`, `5101`, `5110`, and `5120`. Current trace aggregate is 266/266 artifacts, 244 required and 22 optional. This is private-fixture confidence only; public KFM support, exact thresholds, velocity math, controller-loop timing, visual/audio parity, score movement, and full Common1 recovery parity remain blocked.
- Latest R1 required Common1 default air recovery too-early checkpoint: `synthetic-imported-default-air-fall-recovery-too-early.json` checksum `48a2e708` proves an airborne imported defender can start in defender-owned state `5020` after a fall `HitDef` without `p2stateno`, route `5020 -> 5030 -> 5050`, keep `command = "recovery"` active too early, and stay in `5050` while `fall.recovertime` remains positive. Evidence includes active commands `x` / `recovery`, forbidden recovery/landing states `5210`, `5200`, `5201`, `52`, `5100`, `5101`, `5110`, and `5120`, `5050` recoverTime from `11` to `6`, final checksum `cdd20fed`, and gate label `imported-default-fall-recovery-too-early-golden`. Current trace aggregate is 264/264 artifacts, 244 required and 20 optional. This is bounded air-entry early-recovery rejection evidence only; exact MUGEN/IKEMEN recovery threshold tables, exact air get-hit animation choice, exact velocity math, exact controller-loop timing, recovery arbitration between air/ground branches, visual/audio parity, score movement, and full Common1 recovery parity remain blocked.
- Previous R1 required Common1 default air recovery-input checkpoint: `synthetic-imported-default-air-fall-recovery-input.json` checksum `334a419e` proves an airborne imported defender can start in defender-owned state `5020` after a fall `HitDef` without `p2stateno`, route `5020 -> 5030 -> 5050`, accept `command = "recovery"` after positive-to-zero `fall.recovertime`, enter `5210`, land through `52`, and return to idle/control. Evidence includes active commands `x` / `recovery`, `5210` air-recovery velocity, `52` y = 0 landing, final checksum `c5038a9d`, and gate label `imported-default-fall-recovery-input-golden`. That checkpoint raised trace aggregate to 263/263 artifacts, 243 required and 20 optional. This is bounded air-entry recovery input evidence only; exact MUGEN/IKEMEN recovery threshold tables, exact air get-hit animation choice, exact velocity math, exact controller-loop timing, recovery arbitration between air/ground branches, visual/audio parity, score movement, and full Common1 recovery parity remain blocked.
- Previous R1 required Common1 default air lie-down recovery checkpoint: `synthetic-imported-default-air-liedown-recovery.json` checksum `56a8f236` proves an airborne imported defender can route through defender-owned states `5020 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0` after a fall `HitDef` without `p2stateno`, with `5101` `HitFallVel`, `5110` `HitFallDamage`, `hitfall:hitfallvel` / `hitfall:hitfalldamage` evidence, bounded `5110` `downRecoverTime` countdown from `59` to `53`, final checksum `20c045a3`, and gate label `imported-default-fall-gethit-golden`. That checkpoint raised trace aggregate to 262/262 artifacts, 242 required and 20 optional. This is bounded state/order, bounce, lie-down countdown, get-up, and idle-return evidence only; exact air get-hit animation choice, exact `HitShakeOver` / `HitOver` timing, exact ground-impact timing/position, exact bounce physics, exact lie-down duration tables, recovery input, landing nuance, controller-loop tick order, visual/audio parity, score movement, and full Common1 fall/get-hit parity remain blocked.
- Previous R1 required Common1 default air ground-impact checkpoint: `synthetic-imported-default-air-ground-impact.json` checksum `0ba3c80f` proves an airborne imported defender can route through defender-owned states `5020 -> 5030 -> 5050 -> 5100` after a fall `HitDef` without `p2stateno`, with `5100` `HitFallDamage` / `hitfall:hitfalldamage` evidence, final checksum `cb7f1043`, and gate label `imported-default-fall-gethit-golden`. That checkpoint raised trace aggregate to 261/261 artifacts, 241 required and 20 optional. This is bounded state/order plus ground-impact controller evidence only; exact air get-hit animation choice, exact `HitShakeOver` / `HitOver` timing, exact ground-impact timing/position, bounce physics, lie-down timing, landing, recovery input, controller-loop tick order, visual/audio parity, score movement, and full Common1 fall/get-hit parity remain blocked.
- Previous R1 required Common1 default air fall get-hit checkpoint: `synthetic-imported-default-air-fall-gethit.json` checksum `1230a2f3` proves an airborne imported defender can route through defender-owned states `5020 -> 5030 -> 5050` after a fall `HitDef` without `p2stateno`, with final checksum `2ad2abf9` and gate label `imported-default-fall-gethit-golden`. That checkpoint raised trace aggregate to 260/260 artifacts, 240 required and 20 optional. This is bounded state/order evidence only; exact air get-hit animation choice, exact `HitShakeOver` / `HitOver` timing, ground impact, bounce, lie-down, landing, recovery input, controller-loop tick order, visual/audio parity, score movement, and full Common1 fall/get-hit parity remain blocked.
- Previous R1 required Common1 default air get-hit checkpoint: `synthetic-imported-default-air-gethit.json` checksum `dc4fb7c9` proves an airborne imported defender can enter defender-owned state `5020` after a direct `HitDef` without `p2stateno`, with final checksum `2fd842bb` and gate label `imported-default-air-gethit-golden`. That checkpoint raised trace aggregate to 259/259 artifacts, 239 required and 20 optional. This is state-selection evidence only; exact air get-hit animation choice, fall route, landing route, air recovery, hitshake/hitover progression, controller-loop tick order, visual/audio parity, score movement, and full Common1 get-hit parity remain blocked.
- Previous R1 required Common1 default crouch get-hit checkpoint: `synthetic-imported-default-crouch-gethit.json` checksum `7ec18c61` proves a held-crouch imported defender can enter defender-owned state `5010` after a direct `HitDef` without `p2stateno`, with final checksum `1c47d038` and gate label `imported-default-crouch-gethit-golden`. That checkpoint raised trace aggregate to 258/258 artifacts, 238 required and 20 optional. This is state-selection evidence only; exact crouch get-hit animation choice, slide timing, fall routing, hitshake/hitover progression, controller-loop tick order, visual/audio parity, score movement, and full Common1 get-hit parity remain blocked.
- Previous R1 required Projectile fixed-id contact/guard-time checkpoint: `synthetic-imported-projectile-contacttime-id.json` checksum `e9ebf36a` and `synthetic-imported-projectile-guardedtime-id.json` checksum `dfd08f28` prove bounded owner-state `ProjContactTime(77)` and `ProjGuardedTime(77)` routing after player-owned Projectile contact/guard markers, routing P1 into state/actions `321` and `322` with hit/guard event/reason, Projectile lifecycle, effect-store, and target-link evidence. That checkpoint raised trace aggregate to 257/257 artifacts, 237 required and 20 optional. This is fixed-id contact/guard-time trigger evidence only; exact contact/guard tick-order/lifetime, multi-projectile same-id selection, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, score movement, and full Projectile timing parity remain blocked.
- Previous R1 required Projectile cancel-time owner any-id checkpoint: `synthetic-imported-projectile-canceltime-any.json` checksum `5bff1961` proves bounded owner-state `ProjCancelTime(0)` routing after that owner's player-owned Projectile id `77` is canceled, routing P2 into state/action `320` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `918`, projectile lifecycle, effect-store, and payload evidence. This is owner-state any-id cancel-time trigger evidence for one canceled Projectile route only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration beyond this route, exact priority classes, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, score movement, and full Projectile cancel parity remain blocked.
- Previous R1 required Projectile cancel-time owner var-id checkpoint: `synthetic-imported-projectile-canceltime-var.json` checksum `e057e102` proves bounded owner-state `ProjCancelTime(var(0))` routing after owner-local `VarSet` seeds the canceled player-owned Projectile id `77`, routing P2 into state/action `319` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `917`, projectile lifecycle, effect-store, and payload evidence. This is owner-state var-backed cancel-time trigger evidence only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration, exact priority classes, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, score movement, and full Projectile cancel parity remain blocked.
- Previous R1 required Projectile cancel-time owner dynamic-id checkpoint: `synthetic-imported-projectile-canceltime-dynamic.json` checksum `0da26c87` proves bounded owner-state expression-derived `ProjCancelTime(77 + var(0))` routing after that owner's player-owned Projectile is canceled by an opposing Projectile clash, routing P2 into state/action `318` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `916`, projectile lifecycle, effect-store, and payload evidence. This is owner-state dynamic-id cancel-time trigger evidence only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile id `0` selection, exact priority classes, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, score movement, and full Projectile cancel parity remain blocked.
- Previous R1 required Helper Projectile cancel-time dynamic-id checkpoint: `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2` proves bounded helper-local expression-derived `ProjCancelTime(8869 + var(0))` routing after that helper-parented owner-side Projectile is canceled by an opposing Projectile clash, routing the Helper from state/action `1270` into `1271` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `1018`, helper/projectile lifecycle, effect-store, and payload evidence. Focused helper-local runtime coverage separately proves nonzero `ProjCancelTime(var(n))` id selection against helper-parented canceled Projectiles. This is helper-local dynamic-id cancel-time trigger evidence only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, visual/audio parity, score movement, and full Helper/Projectile cancel parity remain blocked.
- Previous R1 required Helper Projectile cancel-time fixed-id checkpoint: `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176` proves bounded helper-local `ProjCancelTime(8868)` routing after that helper-parented owner-side Projectile is canceled by an opposing Projectile clash, routing the Helper from state/action `1268` into `1269` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `1008`, helper/projectile lifecycle, effect-store, and payload evidence. This is helper-local fixed-id cancel-time trigger evidence only; exact cancel tick-order/lifetime, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, visual/audio parity, score movement, and full Helper/Projectile cancel parity remain blocked.
- Previous R1 required Helper Projectile cancel-time any checkpoint: `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01` proves bounded helper-local `ProjCancelTime(0)` routing after a helper-parented owner-side Projectile is canceled by an opposing Projectile clash, routing the Helper from state/action `1266` into `1267` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `998`, helper/projectile lifecycle, effect-store, and payload evidence. This is helper-local cancel-time trigger evidence only; exact cancel tick-order/lifetime, exact priority classes, multi-projectile any-id selection semantics beyond this route, helper-owned custom states, redirects, teams/simul, visual/audio parity, score movement, and full Helper/Projectile cancel parity remain blocked.
- Previous R1 required Projectile cancel-time checkpoint: `synthetic-imported-projectile-canceltime.json` checksum `64e8dec4` proves bounded owner-state `ProjCancelTime(77)` routing after that owner's player-owned Projectile is canceled by an opposing Projectile clash, routing P2 into state/action `283` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback, projectile lifecycle, effect-store, and payload evidence. This is owner-state cancel-time trigger evidence only; exact cancel tick-order/lifetime, exact priority classes, multi-projectile id `0` selection, redirects, teams/simul, visual/audio parity, score movement, and full Projectile cancel parity remain blocked.
- Previous R1 required Helper Projectile guard/contact-time any checkpoint: `synthetic-imported-helper-projguardedtime-any.json` checksum `1f1a38e4` and `synthetic-imported-helper-projcontacttime-any.json` checksum `0d9f7829` prove bounded helper-local `ProjGuardedTime(0)` and `ProjContactTime(0)` any-projectile routing after helper-parented Projectile guard/contact markers, routing the Helper into state/actions `1263` and `1265` with guard event/reason, helper/projectile lifecycle, effect-store, target-link, and sound/FightFX package evidence. This is helper-local guard/contact-time trigger evidence only; exact contact/guard tick-order/lifetime, multi-projectile any-id selection, helper-owned custom-state targets, redirects, teams/simul, visual/audio parity, score movement, and full Helper/Projectile parity remain blocked.
- Previous R1 required Helper Projectile hit-time any checkpoint: `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b` proves bounded helper-local `ProjHitTime(0)` any-projectile hit-time routing after a helper-parented Projectile hit, routing the Helper into state/action `1261` with hit event/reason, helper/projectile lifecycle, effect-store, target-link, and sound/FightFX package evidence. This is helper-local hit-time trigger evidence only; exact hit tick-order/lifetime, multi-projectile any-id selection, helper-owned custom-state targets, redirects, teams/simul, visual/audio parity, score movement, and full Helper/Projectile parity remain blocked.
- Previous R1 required Projectile hit-time any checkpoint: `synthetic-imported-projectile-hittime-any.json` checksum `47c1cf7f` proves bounded owner-state `ProjHitTime(0)` any-projectile hit-time routing after a player-owned Projectile hit, routing P1 into state/action `282` with hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. This is hit-time trigger evidence only; exact hit tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, visual/audio parity, score movement, and full Projectile hit parity remain blocked.
- Previous R1 required Projectile contact-time any checkpoint: `synthetic-imported-projectile-contacttime-any.json` checksum `f1751155` proves bounded owner-state `ProjContactTime(0)` any-projectile contact-time routing after a player-owned Projectile contact, routing P1 into state/action `281` with hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. This is contact-time trigger evidence only; exact contact tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, visual/audio parity, score movement, and full Projectile contact parity remain blocked.
- Previous R1 required Projectile guarded-time any checkpoint: `synthetic-imported-projectile-guardedtime-any.json` checksum `c8473340` proves bounded owner-state `ProjGuardedTime(0)` any-projectile guard-time routing after a player-owned Projectile guard, routing P1 into state/action `279` with guard event/reason, Projectile lifecycle, effect-store, and target-link evidence. This is guarded contact-time trigger evidence only; exact guard tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, visual/audio parity, score movement, and full Projectile guard parity remain blocked.
- Previous R1 required HitDef plus Projectile target-memory mix checkpoint: `synthetic-imported-hitdef-projectile-target-mix.json` checksum `e98d4857` proves bounded owner-local target memory can retain separate direct `HitDef` id `77` and player-owned `Projectile` id `78` in one active state, then route P1 through `NumTarget(77)`, `Target(77), Life`, `NumTarget(78)`, and `Target(78), Life` into state/action `278`. This is target-memory trigger evidence only; Target* mutation mixing, helper-owned projectile targets, helper-owned custom state tables, teams/simul, multi-target selection, exact target lifetime/tick order, visual parity, score movement, and full target/combat parity remain blocked.
- Previous R1 required helper Projectile air guard hitshake-time checkpoint: `synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3c3f2e25` proves bounded defender-owned Common1-style air guard-hit CNS can branch from `155` into state/action `317` through runtime-backed `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` after a helper-parented Projectile air guard, with helper-local Projectile spawn telemetry, helper/projectile lifecycle, and owner/helper target-link evidence. This is helper-parented Projectile air guard hitshake timing metadata trigger evidence only; helper-owned custom states, exact helper Projectile air guard timing/landing/effects, projectile presentation, custom-state inheritance breadth, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required player Projectile air guard hitshake-time checkpoint: `synthetic-imported-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3fcf1421` proves bounded defender-owned Common1-style air guard-hit CNS can branch from `155` into state/action `316` through runtime-backed `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` after a player-owned Projectile air guard. This is player-owned Projectile air guard hitshake timing metadata trigger evidence only; exact air guard timing/landing/proximity, projectile presentation, custom-state inheritance breadth, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required air guard hitshake-time checkpoint: `synthetic-imported-gethitvar-air-guard-hitshaketime.json` checksum `703e9328` proves bounded defender-owned Common1-style air guard-hit CNS can branch from `155` into state/action `315` through runtime-backed `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` after a direct `HitDef` air guard. This is direct-`HitDef` air guard hitshake timing metadata trigger evidence only; exact air guard timing/landing/proximity, guard end/effects, helper/projectile air-guard variants, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required crouch guard hitshake-time checkpoint: `synthetic-imported-gethitvar-crouch-guard-hitshaketime.json` checksum `b31d1dac` proves bounded defender-owned Common1-style crouch guard-hit CNS can branch from `153` into state/action `314` through runtime-backed `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` after a direct `HitDef` crouch guard. This is crouch guard hitshake timing metadata trigger evidence only; exact crouch guard timing/proximity, guard end/effects, helper/projectile crouch-guard variants, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required helper Projectile guard hitshake-time checkpoint: `synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime.json` checksum `64a1a8bd` proves bounded defender-owned Common1-style guard-hit CNS can branch from `151` into state/action `313` through runtime-backed `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` after a helper-parented Projectile guard. This is helper-parented Projectile guard hitshake timing metadata trigger evidence only; helper-owned custom states, exact helper Projectile guard timing/effects, projectile visual/audio parity, custom-state inheritance breadth, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required player Projectile guard hitshake-time checkpoint: `synthetic-imported-projectile-gethitvar-guard-hitshaketime.json` checksum `724f66d6` proves bounded defender-owned Common1-style guard-hit CNS can branch from `151` into state/action `312` through runtime-backed `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` after a player-owned Projectile guard. This is player-owned Projectile guard hitshake timing metadata trigger evidence only; exact hitpause lifetime, guard timing/effects, projectile visual/audio parity, custom-state inheritance breadth, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required guard hitshake-time checkpoint: `synthetic-imported-gethitvar-guard-hitshaketime.json` checksum `31d76de9` proves bounded defender-owned Common1-style guard-hit CNS can branch from `151` into state/action `311` through runtime-backed `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` after a direct `HitDef` guard. This is direct-`HitDef` guard hitshake timing metadata trigger evidence only; exact hitpause lifetime, guard timing/proximity, guard end, guard effects, custom-state/helper/projectile inheritance breadth, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required normal hitshake-time checkpoint: `synthetic-imported-gethitvar-hitshaketime.json` checksum `655107b9` proves bounded defender-owned Common1-style normal get-hit CNS can branch from `5000` into state/action `310` through runtime-backed `GetHitVar(hitshaketime) > 0 && !GetHitVar(guarded)` after a direct `HitDef` hit. This is direct-contact normal hitshake timing metadata trigger evidence only; exact hitpause tick lifetime, hitshake lifetime, custom-state/helper/projectile inheritance breadth, visual parity, score movement, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Previous R1 required normal hit-time checkpoint: `synthetic-imported-gethitvar-hittime.json` checksum `a11beef0` proves bounded defender-owned Common1-style normal get-hit CNS can branch from `5000` into state/action `309` through runtime-backed `GetHitVar(hittime) > 0 && !GetHitVar(guarded)` after a direct `HitDef` hit. This is direct-contact normal hit timing metadata trigger evidence only; exact hitstun tick order, custom-state/helper/projectile inheritance breadth, visual parity, score movement, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Previous R1 required guard timing hit-var checkpoint: `synthetic-imported-gethitvar-guard-timing.json` checksum `cf92c669` proves bounded defender-owned Common1-style guard-hit CNS can branch from `151` into state/action `308` through runtime-backed `GetHitVar(hittime) > 0`, `GetHitVar(slidetime) = 5`, and `GetHitVar(ctrltime) = 7` after a direct `HitDef` guard. This is direct-`HitDef` guard timing metadata trigger evidence only; exact guard timing/proximity, guard end, guard effects, projectile/helper/custom-state inheritance, visual parity, score movement, and full MUGEN/IKEMEN guard parity remain blocked.
- Previous R1 required get-hit down-recovery metadata checkpoint: `synthetic-imported-gethitvar-down-recover.json` checksum `b8a7aef0` proves bounded owner-backed get-hit state routing: after a fall `HitDef`, P2 enters attacker-owned state `5100`, then `GetHitVar(down.recover) = 1`, `GetHitVar(down.recovertime) = 45`, and `GetHitVar(recovertime) = 45` route into state/action `307` before lie-down recovery consumes the timer.
- Previous R1 required get-hit env-shake metadata checkpoint: `synthetic-imported-gethitvar-fall-envshake.json` checksum `6364632a` proves bounded owner-backed get-hit state routing through `GetHitVar(fall.envshake.time) = 15`, `GetHitVar(fall.envshake.freq) = 178`, `GetHitVar(fall.envshake.ampl) = 6`, and `GetHitVar(fall.envshake.phase) = 0` into state/action `306` before `FallEnvShake` presentation executes.
- Previous R1 required get-hit fallcount checkpoint: `synthetic-imported-gethitvar-fallcount.json` checksum `c391d938` proves bounded owner-backed get-hit state routing where `HitFallDamage` in state `5100` records one ground impact, consumes stored `fall.damage`, and `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0` branches into state/action `328`.
- Previous R1 required get-hit metadata checkpoint: `synthetic-imported-gethitvar-fall-metadata.json` checksum `474fa734` proves bounded owner-backed get-hit state routing through `GetHitVar(fall.damage) = 70`, `GetHitVar(fall.kill) = 0`, `GetHitVar(fall.xvel) = 3`, and `GetHitVar(fall.yvel) = -6` into state/action `305` before `HitFallDamage` resolves.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-teamside.json` checksum `f55695b7` proves bounded imported State -1 side-context routing: P1 presses `x`, then `TeamSide = 1` and `EnemyNear, TeamSide = 2` route into state/action `299` without `HitDef` / combat side effects. Current trace aggregate is 228/228 artifacts, 208 required and 20 optional. This is one-on-one sandbox side-context evidence only; teams/simul/turns, helper-owned opponent lists, indexed opponent selection, visual parity, score movement, and full MUGEN/IKEMEN `TeamSide` semantics remain blocked.
- Previous R1 required animation-controller evidence checkpoint: `synthetic-imported-changeanim2-elem.json` checksum `b0b46d33` proves bounded active-state `ChangeAnim2 value = 207`, `elem = 3`, and `elemtime = 1` can retarget to a known state-owner AIR action with authored frame durations `[4,5,6]`; final P1 evidence ends at `frameIndex = 2` and `animTime = 20`. Current trace aggregate was 227/227 artifacts, 207 required and 20 optional. This is one portable owner-AIR element/time positioning route only; missing-action fallback, redirects, helper/custom-state ownership, multi-import SFF namespaces, exact controller tick order, visual parity, score movement, and full animation-source parity remain blocked.
- Previous R1 required get-hit trigger evidence checkpoint: `synthetic-imported-helper-projectile-gethitvar-guarded.json` checksum `2b413bd7` proves bounded helper-parented `Projectile` guard metadata can survive into defender-owned Common1-style guard-hit CNS: P2 blocks a helper-spawned Projectile with `parentId = p1-helper-0`, runs `150 -> 151`, executes `HitVelSet` / `CtrlSet` typed ops, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `304` before returning to idle/control. The trace also requires helper/projectile lifecycle, payload, owner/helper target-link evidence, and helper-local `Projectile` controller/op telemetry from helper state `1200`. This is portable helper-parented Projectile guard metadata trigger evidence only; custom-state guarded metadata, exact helper Projectile guard timing/effects, visual parity, score movement, and full Common1/guard parity remain blocked.
- Previous R1 required get-hit trigger evidence checkpoint: `synthetic-imported-projectile-gethitvar-guarded.json` checksum `a0104472` proves bounded player-owned `Projectile` guard metadata can survive into defender-owned Common1-style guard-hit CNS: P2 blocks a Projectile-only attack, runs `150 -> 151`, executes `HitVelSet` / `CtrlSet` typed ops, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `303` before returning to idle/control. This is portable player-owned Projectile guard metadata trigger evidence only; custom-state guarded metadata, exact guard timing/effects, visual parity, score movement, and full Common1/guard parity remain blocked.
- Previous R1 required get-hit trigger evidence checkpoint: `synthetic-imported-gethitvar-guarded.json` checksum `7c36defb` proves bounded direct-`HitDef` guard metadata can survive into defender-owned Common1-style guard-hit CNS: P2 blocks P1, runs `150 -> 151`, executes `HitVelSet` / `CtrlSet` typed ops, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `302` before returning to idle/control. This is portable direct guard metadata trigger evidence only; custom-state guarded metadata, exact guard timing/effects, visual parity, score movement, and full Common1/guard parity remain blocked.
- Previous R1 required get-hit trigger evidence checkpoint: `synthetic-imported-gethitvar-fall-recover.json` checksum `259b300f` proves bounded owner-backed get-hit routing can distinguish `GetHitVar(fall.recover)` from `CanRecover`: P2 enters attacker-owned state `5100` after a fall `HitDef`, branches through `GetHitVar(fall.recover) = 1 && GetHitVar(fall.recovertime) > 0 && !CanRecover` into state/action `301`, and preserves positive recovery-timer evidence before recovery is allowed. This is portable current get-hit metadata trigger evidence only; exact recovery threshold tables, custom-state lifetime, helper/projectile inheritance, controller-loop timing, visual parity, score movement, and full Common1/get-hit parity remain blocked.
- Previous R1 required animation-trigger evidence checkpoint: `synthetic-imported-animelem-offset.json` checksum `4484031d` proves bounded imported active-state legacy `AnimElem` elapsed-time routing: P1 enters state `200`, advances through authored AIR frame durations `[2,6,4]`, and exits through `AnimElem = 2, = 4` into state/action `300` without `HitDef` / combat side effects. That trace aggregate was 222/222 artifacts, 202 required and 20 optional. Previous `synthetic-imported-animelem.json` checksum `683d9a10` still gates the one-tick `AnimElem = 2, = 0` route into state/action `299`. This is portable current-actor animation-element trigger evidence only; AIR loop semantics, negative-duration semantics, helper/state-owner namespaces, persistent-controller timing, visual parity, score movement, and full animation VM parity remain blocked.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-owner-metrics.json` checksum `1a61aaeb` proves bounded imported State -1 owner metric routing: P1 presses `x`, then `StateNo = 0`, `Anim = 0`, `Time >= 0`, `Life = 1000`, `Power = 0`, `Pos X < 0`, `Pos Y = 0`, `Vel X = 0`, and `Vel Y = 0` route into state/action `298` without `HitDef` / combat side effects. `RuntimeExpressionContextWorld` exposes the post-transition `stateElapsed = -1` sentinel as observable `Time` / `StateTime` zero in expression reads. This is portable owner metric trigger evidence only; exact VM tick ordering, helper/team/redirect state namespaces, localcoord scaling, visual parity, score movement, and full MUGEN/IKEMEN trigger parity remain blocked.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-p2-distance.json` checksum `2c584be0` proves bounded imported State -1 spacing routing: P1 presses `x`, then `P2Dist X = 55`, `P2Dist Y = 0`, `P2BodyDist X = 7`, and `P2BodyDist Y = 0` route into state/action `297` without `HitDef` / combat side effects. This is portable two-actor opponent-spacing evidence only; teams/simul, helpers, exact opponent selection, localcoord scaling, push/corner adjustment, score movement, and full MUGEN/IKEMEN spacing parity remain blocked.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-state-context.json` checksum `cb9c3d1e` proves bounded imported State -1 context routing: P1 presses `x`, then `ctrl`, `StateType = S`, `MoveType = I`, and `Physics = S` route into state/action `295` without `HitDef` / combat side effects. This is portable trigger-context evidence only; helper/team/redirect metadata ownership, exact controller-loop ordering, score movement, and full MUGEN/IKEMEN trigger parity remain blocked.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-gametime.json` checksum `bab573f3` proves bounded imported State -1 global tick routing: P1 delays the `x` command until `GameTime >= 4`, then routes into state/action `294` without `HitDef` / combat side effects. This is portable trigger evidence only; exact pause accounting, replay/rollback timing, multi-round timer ownership, IKEMEN round-system behavior, score movement, and full MUGEN/IKEMEN global timing parity remain blocked.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-edge-distance.json` checksum `785de452` proves bounded imported State -1 edge-distance routing into state/action `293` through supplied stage bounds. This is trigger evidence only; exact camera/screen edge parity, localcoord scaling, push/corner behavior, teams/simul/helper namespace breadth, visual parity, score movement, and full edge-distance parity remain blocked.
- Previous R1 Common1 full-chain recovery checkpoint: required `synthetic-imported-default-fall-recovery.json` checksum `d83797d9` now requires bounded imported P2 controller/actor-frame order across `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120`, including `5110` `hitFall.downRecoverTime` countdown-range and first-to-last-drop evidence before `5120`. That checkpoint passed 214/214 artifacts, 194 required and 20 optional. This is synthetic Common1 route evidence only; exact controller-loop tick order, bounce physics, threshold/down-recovery tables, public KFM parity, score movement, and full Common1 VM parity remain blocked.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-animelemtime.json` checksum `2036557d` proves bounded imported active-state animation-element routing: P1 enters state `200`, advances through authored AIR frame durations `[2,4,4]`, and exits through `AnimElemTime(2) = 2` into state `292` without `HitDef` / combat side effects. This is portable trigger evidence only; exact AIR loop semantics, invalid-element bottom values, negative-duration semantics, state-owner/helper namespaces, persistent-controller timing, exact controller-loop tick order, score movement, and full animation VM parity remain blocked.
- Previous R1 required trigger evidence checkpoint: `synthetic-imported-animtime.json` checksum `9e42b546` proves bounded imported active-state animation-end routing: P1 enters state `200`, remains there for the authored AIR action duration, and exits through `AnimTime = 0` into state `291` without `HitDef` / combat side effects. `synthetic-imported-selfanimexist.json` checksum `99930032` remains the previous own-animation lookup proof. Redirected animation owners, helper/parent/root lookup, common/FightFX namespaces, exact animation lookup parity, score movement, and full MUGEN/IKEMEN trigger parity remain blocked.
- Latest optional KFM special-route precision: `kfm-official-qcf-x.json` now passes with checksum `349cd995` and requires real KFM `QCF_x` input buffering into state `1000`, ordered `11 -> 20 -> 1000` actor-frame evidence, ordered `ChangeState` / `PosAdd` / `HitDef` controller and `kinematic:posadd` / `hitdef` operation evidence, attacker-side `PlaySnd` telemetry for move sound `S0,3` and contact hit sound `S5,4`, inherited `[Data] sparkno = 2` hit-spark telemetry with authored `sparkxy = -10,-60`, shared sound+spark contact package metadata, target link `p1 -> p2 / 0`, final P1 `power = 35` / `targetCount = 1`, and final P2 `life = 910` / hitfall telemetry. This is private-fixture confidence for one KFM Kung Fu Palm route only; no public bundled asset, exact timing/pushback, renderer/audio playback parity, broad special-move compatibility, score movement, or full MUGEN/IKEMEN parity claim.
- Latest R2 player Projectile default Target-controller checkpoint: required `synthetic-imported-projectile-default-target-controllers.json` checksum `1c1a3e77` proves a player-owned Projectile-only hit with omitted `projid` / `id` defaults target memory to id `0`, then later executes owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` against P2. Evidence includes Projectile effect anim `915`, lifecycle spawn/remove, effect payload `ownerId = p1` / `effectId = 0` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, required target side-effect controller/operation execution, target link id `0`, TargetBind offset `36,-12`, P1 final `targetCount = 0`, final P2 `life = 949` / `power = 40`, and no direct `HitDef` controller evidence in the isolated fixture. Current trace aggregate is 208/208 artifacts, 188 required and 20 optional. Previous default-id player Projectile TargetState proof remains required as `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa`, previous explicit-id player Projectile TargetState proof remains required as `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962`, previous explicit-id player Projectile Target-controller proof remains required as `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2`, and previous player Projectile target redirect proof remains required as `synthetic-imported-projectile-target-redirect.json` checksum `cd099094`. This adds player-owned Projectile default-id Target side effects to the existing direct player Target*, player Projectile target trigger reads, explicit-id/default-id player Projectile Target side effects, explicit/default player Projectile TargetState, helper direct/Projectile explicit/default/bare target memory, bounded helper-owned direct-HitDef and explicit/default helper-parented Projectile Target side effects, and helper TargetState evidence; Target* mutation mixing, helper-owned custom state tables, throws, teams/simul, multi-target/helper-owned opponent selection, exact target lifetime/tick order, exact final-animation parity, visual parity, and full target/combat/custom-state parity remain blocked.
- Previous R2 player Projectile default TargetState checkpoint: required `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa` proves a player-owned Projectile-only hit can record default target link `p1 -> p2 / 0`, then later execute owner-local `TargetState value = 888` and route P2 through attacker-owned custom-state data `888 -> 889` before `SelfState` returns P2 to state `0`/control.
- Previous R2 player Projectile explicit TargetState checkpoint: required `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962` proves a player-owned Projectile-only hit can record owner target link `p1 -> p2 / 77`, then later execute owner-local `TargetState value = 888` and route P2 through attacker-owned custom-state data `888 -> 889` before `SelfState` returns P2 to state `0`/control.
- Previous R2 player Projectile Target-controller checkpoint: required `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2` proves a player-owned Projectile-only hit can record owner target link `p1 -> p2 / 77`, then execute owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` against P2 without direct `HitDef` controller evidence in the isolated fixture. Evidence includes Projectile effect anim `912`, lifecycle spawn/remove, effect payload `ownerId = p1` / `effectId = 77` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, TargetBind offset `36,-12`, P1 final `targetCount = 0`, and final P2 `life = 949` / `power = 40`.
- Previous R2 player Projectile target redirect checkpoint: required `synthetic-imported-projectile-target-redirect.json` checksum `cd099094` proves a player-owned Projectile-only hit can record owner target link `p1 -> p2 / 77`, branch owner state `200 -> 277` through `NumTarget(77)` plus `Target(77), Life <= 969`, and leave final P2 `life = 969`. Evidence includes Projectile effect anim `911`, lifecycle spawn/remove, effect payload `ownerId = p1` / `effectId = 77` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, and no direct HitDef controller requirement in this fixture.
- Latest R1 Common1 checkpoint: required `synthetic-imported-default-fall-official-air-recovery.json` checksum `b0363be9` now proves an official-style synthetic `5050 -> 5210 -> 52 -> 0` air-recovery route with positive-to-zero `fall.recovertime` actor-frame sequence, `5210` air-recovery velocity telemetry, landing-state `52` y = 0 evidence, ordered `VelAdd` / `ChangeState` / `VelSet` / `HitFallSet` / `CtrlSet` / typed `kinematic:*` / `hitfall:hitfallset` / `resource:ctrlset` operation evidence, and final idle/control. Current trace aggregate is 187/187 artifacts, 167 required and 20 optional. This is Common1 recovery precision only; exact threshold tables, velocity math, controller-loop timing, public bundled KFM support, and full recovery parity remain blocked.
- Latest optional KFM/Common1 fixture precision: `kfm-official-default-fall-recovery-input.json` still passes with checksum `151486e1`, but now its optional local fixture gate requires ordered real KFM `5050 -> 5210 -> 52 -> 0` actor-frame evidence plus bounded controller/typed-operation order through `5050` `VelAdd` / `ChangeState`, `5210` `PalFX` / `PosFreeze` / `Turn` / `NotHitBy` / `VelMul` / `VelAdd` / `ChangeState`, `52` landing evidence, and state `0` `VelSet`. This is private-fixture confidence only; no public bundled asset, score movement, exact recovery thresholds, exact velocity math, or full controller-loop parity claim.
- Latest optional KFM/Common1 ground fixture precision: `kfm-official-default-fall-ground-recovery-priority.json` passes with checksum `6d361534`, reusing the real KFM ground-recovery route while additionally forbidding generic air recovery `5210` and lie-down chain states. `kfm-official-default-fall-ground-recovery.json` still passes with the same checksum and requires real KFM actor-frame order for `5050 -> 5200 -> 52` with bounded fall/down velocity telemetry plus the existing controller/typed-operation order through `5050` `VelAdd` / `ChangeState`, `5200` `VelAdd` / `SelfState`, and `52` landing `VelSet` / `PosSet` / `kinematic:*` / `ChangeState`. `5201` remains required as an executed state, but no summarized `5201` actor-frame bucket is claimed. This is private-fixture confidence only; exact thresholds, velocity math, full tick-order parity, guard-state parity, public bundled KFM support, and score movement remain blocked.
- Previous R1 Common1 checkpoint: required `synthetic-imported-default-fall-official-ground-recovery.json` checksum `74b72495` proves an official-style synthetic `5050 -> 5200 -> 5201 -> 52 -> 0` ground-recovery route with positive-to-zero `fall.recovertime` actor-frame sequence, recovery/land kinematic operation order, `NotHitBy` safety telemetry, and final idle/control.
- Previous helper/effect checkpoint: required `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2`, `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176`, `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01`, `synthetic-imported-helper-projguardedtime-any.json` checksum `1f1a38e4`, `synthetic-imported-helper-projcontacttime-any.json` checksum `0d9f7829`, and `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b` prove bounded helper-local dynamic/fixed/any-id `Proj*Time(...)` routes for helper-parented Projectile cancel/guard/contact/hit markers. Previous fixed-id `synthetic-imported-helper-projcontact.json` checksum `4dcbdd25` proves bounded helper-local `ProjContact(8855)` / `ProjContactTime(8855) >= 1` routing after a helper-parented owner-side Projectile is guarded by P2. Previous `synthetic-imported-helper-projguard.json` checksum `d2e2f20d` remains the helper-local Projectile guard proof, `synthetic-imported-helper-projhit.json` checksum `2d9a281e` remains the helper-local Projectile hit/contact proof, `synthetic-imported-helper-modifyprojectile.json` checksum `09d3f7e4` remains the helper-local Projectile mutation proof, and previous `synthetic-imported-helper-numproj.json` checksum `3312a554` remains the helper-local projectile-count proof against helper-parented Projectiles only.
- Latest asset compatibility checkpoint: `CompatibilityReport.sounds` now summarizes parsed SND archives with declared total, decoded WAV count, unsupported count, format counts, sample-rate counts, and channel counts. Loader reports use the already parsed `soundArchive`; this is inspection/compatibility evidence only, not exact audio playback parity.
- Previous R1 evidence checkpoint: required `synthetic-imported-default-fall-official-recovery-threshold.json` checksum `86804271` and `synthetic-imported-default-fall-official-recovery-too-early.json` checksum `ef945ff5` now promote official-style synthetic Common1 recovery threshold / too-early rejection routes into `pnpm qa:trace`.
- Latest R2 ownership cleanup checkpoint: `RuntimeFighterAdvanceWorld` now owns bounded per-fighter advance order from `PlayableMatchRuntime`: sprite-effect tick, hit eligibility slots, HitOverride slots, contact timers, render-angle reset, state clock, frame constraints, recovery-window tick, preserve-moveType read, stun, move lifecycle, kinematics, animation, active controllers, ground-recovery landing, lie-down recovery, and frozen-position preservation. `PlayableMatchRuntime` still supplies concrete worlds, state/action callbacks, active-controller execution, and stage/tick context. This is ownership cleanup only; exact MUGEN/IKEMEN player tick order, persistent-controller timing, helper/team/redirect actor advance semantics, recovery/stun/physics arbitration, visual parity, score movement, and full VM parity remain blocked.
- Previous R2 ownership cleanup checkpoint: `RuntimeActiveControllerDispatchWorld` owns bounded active-controller route orchestration after scan/trigger pass: active state mutation dispatch, runtime-controller execution handoff, active side-effect dispatch, and unsupported-controller fail-soft routing are selected through one named boundary. `PlayableMatchRuntime` still supplies concrete hooks, world instances, frame lookup, target-entry callbacks, telemetry callbacks, stage/tick context, and active-loop order. This is ownership cleanup only; exact CNS VM tick order, persistent-controller semantics, helper/team/redirect scopes, side-effect ordering parity, missing-action fallback parity, target/combat/presentation semantic parity, visual parity, score movement, and full active-controller parity remain blocked.
- Previous R2 ownership cleanup checkpoint: `RuntimeFighterStateWorld` now owns bounded fighter runtime-state construction outside `PlayableMatchRuntime`: runtime resource maxima, damage multipliers from CNS constants, initial action/frame/control/resource state, command buffers, contact memory, telemetry buckets, injected world references, deterministic RNG seed, and lazy runtime-program compilation. `PlayableMatchRuntime` still supplies stage starts, actor ids, definitions, and injected match worlds. This is ownership cleanup only; exact player lifecycle parity, helper/custom-state clone breadth, team/simul roster ownership, intro/round lifecycle, visual parity, score movement, and full actor registry parity remain blocked.
- Previous R2 ownership cleanup checkpoint: `RuntimeMatchResetWorld` now owns bounded match reset orchestration outside `PlayableMatchRuntime`: round timer reset, pause reset, EnvColor reset, effect actor store reset, in-place P1/P2 recreation, helper TargetState handler reattachment, and reset logging. `PlayableMatchRuntime` still supplies concrete fighter construction, stage starts, injected worlds, and field assignment. This is ownership cleanup only; exact round-flow parity, continue/round intro semantics, helper/custom-state reset breadth, screenpack/lifebar reset behavior, visual parity, score movement, and full MUGEN/IKEMEN match lifecycle parity remain blocked.
- Previous R2 ownership cleanup checkpoint: `RuntimeHelperTargetStateWorld` now owns helper TargetState handler attach/re-attach wiring for both match actors before helper-local `TargetState` controllers call back into owner-backed state entry. `PlayableMatchRuntime` still supplies concrete target lookup, state availability, state entry mutation, and broader helper/effect/controller order. This is ownership cleanup only; helper-owned custom state table parity, throws, teams/simul, multi-target/helper-owned opponent selection, exact helper TargetState timing, visual parity, score movement, and full Helper VM parity remain blocked.
- Previous R2 ownership cleanup checkpoint: `RuntimeFrameWorld` owns bounded current AIR frame lookup plus cloned `Clsn1` / `Clsn2` projection for runtime and snapshot consumers. `PlayableMatchRuntime` now routes current-frame reads, guard-distance hurtbox fallback, and `AfterImage` sample frame reads through that boundary while preserving direct frame `Clsn1` handoff for `ReversalDef`; `RuntimeSnapshotWorld` uses the same boundary for active move hitboxes, frame hitboxes, and missing-frame default hurtbox fallback. This is ownership cleanup only; exact collision priority, frame timing, guard-distance thresholds, rotated/scaled boxes, helper/team/redirect collision ownership, renderer parity, score movement, and full collision VM parity remain blocked.
- Previous R2 ownership cleanup checkpoint: `RuntimeAfterImageSampleWorld` owns bounded `AfterImage` sample projection from actor runtime state plus current AIR frame before `RuntimeSpriteEffectWorld` captures ghost-trail samples. This preserves cloned position, facing, self/state-owner sprite owner metadata, and sprite group/index/offset projection while `PlayableMatchRuntime` still owns current-frame lookup, controller ordering, hitpause selection, and render integration. This is ownership cleanup only; exact ghost-trail cadence, blend/material parity, helper/team/redirect presentation ownership, renderer parity, score movement, and full presentation parity remain blocked.
- Previous R2 ownership checkpoint: `RuntimeCombatResolutionWorld` owns bounded active direct/projectile contact orchestration from `PlayableMatchRuntime`.
- Do not reselect the just-closed `synthetic-imported-custom-state-gethitvar-guard-kill`, `synthetic-imported-helper-projectile-gethitvar-guard-kill`, `synthetic-imported-projectile-gethitvar-guard-kill`, `synthetic-imported-gethitvar-air-guard-kill`, `synthetic-imported-gethitvar-crouch-guard-kill`, `synthetic-imported-gethitvar-guard-kill`, `synthetic-imported-gethitvar-kill`, `synthetic-imported-custom-state-gethitvar-fall-metadata`, or `synthetic-imported-custom-state-gethitvar-fall-envshake` trace gates, `RuntimeMatchPresentationSnapshotWorld`, `RuntimeActiveControllerTelemetryWorld`, or `RuntimeMatchCombatStateHooksWorld` ownership seams, or the older crouch Common1 `5010 -> 5011 -> 0` get-hit progression gate, as fresh next work.
- Useful next runtime work remains another R1 guard/FightFX/Common1 precision cut or a deeper R2 helper/effect/combat ownership seam; do not reselect the just-closed `RuntimeSpriteEffectControllerWorld` RemapPal dispatch, `RuntimeSpriteEffectControllerWorld` Trans dispatch, `RuntimeFighterAdvanceHookSetWorld`, `RuntimeActiveExpressionContextWorld`, GetHitVar damage gate, Helper Projectile cancel-time dynamic/fixed/any-id, Helper Projectile hit-time any, Projectile hit-time any, Projectile contact-time any, Projectile guarded-time any, HitDef plus Projectile target-memory mix, custom-state GetHitVar fall metadata gate, GetHitVar guard timing gate, GetHitVar down-recover gate, GetHitVar fall env-shake gate, GetHitVar fall metadata gate, TeamSide trigger gate, ChangeAnim2 elem/elemtime gate, AnimElem offset gate, AnimElem start gate, full-chain fall recovery gate, AnimElemTime gate, AnimTime gate, active-controller dispatch, active side-effect dispatch, active state/animation dispatch, fighter-state factory, match reset, helper TargetState handler binding, match helper TargetState actor-resolution, frame/collision, afterimage sample, player Projectile default Target-controller, player Projectile default TargetState, player Projectile explicit TargetState, player Projectile Target-controller, player Projectile target redirect, helper Projectile bare Target, helper Projectile default TargetState, helper Projectile TargetState, helper Projectile default Target-controller, helper Projectile Target-controller, helper TargetState, helper target-controller, helper bare-target, helper explicit-id/default target memory, helper-owned HitDef direct-combat, official-style air-recovery, official-style ground-recovery, SND compatibility report summary, official-style recovery threshold/too-early promotion, combat-resolution, target-candidate, expression-context, state-transition, animation-controller, or kinematic-controller cuts.

## Authoritative Docs

Read in this order for broad work:

1. `AGENTS.md` - local working rules, verification rules, and skill setup.
2. `docs/ROADMAP_PROGRESS_SYSTEM.md` - source-of-truth stack, package lifecycle, horizon ladder, update matrix.
3. `docs/ROADMAP_RELEASE_TARGETS.md` - release trains, usable milestone gates, and score-movement rules.
4. `docs/ROADMAP_EXECUTION_BOARD.md` - current queue, package acceptance, and handoff map.
5. `docs/PROGRESS_TRACKER.md` - compact score, evidence snapshot, next cuts.
6. `docs/WORKPLAN.md` - current execution authority.
7. `docs/BUILD_EXECUTION_BACKLOG.md` - append-only implementation history and backlog.
8. `docs/ENGINE_PORT_ARCHITECTURE.md` - parser/compiler/runtime/render/audio/debug boundaries.
9. `docs/CONTROLLER_SUPPORT_REGISTRY.md` - controller support levels and evidence requirements.
10. `docs/SUPPORTED_FEATURES.md` - supported/partial/unsupported feature matrix.
11. `docs/QA_AND_ACCEPTANCE_GATES.md` - closeout gates, trace rules, fixture rules, visual QA rules.

Durable decisions:

- `docs/adr/0001-roadmap-control-and-local-issues.md`

Use roadmap/vision docs when scope decisions are needed:

- `docs/APPROVED_HORIZON_PLAN.md`
- `docs/HORIZON_IMPLEMENTATION_BLUEPRINT.md`
- `docs/MASTER_CONSTRUCTION_PLAN.md`
- `docs/CONSTRUCTION_WAVES.md`
- `docs/PORTING_ROADMAP.md`
- `docs/ENGINE_STUDIO_ROADMAP.md`
- `docs/CREATOR_STUDIO_AND_MODULAR_ENGINE.md`
- `docs/MODULE_BOUNDARY_CONTRACT.md`

Use local tracker docs for current work slicing:

- `.scratch/roadmap/PRD.md`
- `.scratch/roadmap/issues/`

## Domain Vocabulary

- **Compatibility gate**: deterministic trace/test/fixture proof for one bounded behavior.
- **Claim allowed**: exact behavior the current evidence proves.
- **Claim blocked**: parity or edge cases that remain unsupported or unproven.
- **Imported fighter**: fighter loaded from MUGEN-like package files.
- **Native/generated fighter**: project-owned atlas-backed fighter created for this sandbox.
- **MatchWorld**: renderer-independent ownership/evidence boundary for actors, effects, lifecycle, targets, and snapshots.
- **ControllerOp**: typed runtime operation lowered from CNS/controller data.
- **RuntimeTrace**: deterministic evidence artifact for runtime behavior gates.
- **Fixture gate**: optional or required character/stage package evidence, usually local-only and not redistributed.

## Hard Rules

- No commercial or third-party character assets in repo.
- Do not hardcode one character, stage, or fixture path into runtime behavior.
- Do not count generated/native fighters as imported MUGEN compatibility.
- Do not claim full parity from parser counts or partial runtime gates.
- Every unsupported parser/runtime/render/audio feature must be visible in reports or docs.
- Frontend/render changes require visual QA before closeout.
- Runtime compatibility changes require trace evidence or focused tests plus docs.

## Current Build Priority

```txt
RuntimeTrace and ControllerOp coverage
  -> MatchWorld ownership
  -> KFM/Common1 fixture precision
  -> Studio Evidence/Build trust workflow
  -> generated asset authoring and QA
  -> IKEMEN profile scanner
  -> shared engine contracts
  -> first non-fighting module slice
```

When choosing next work, prefer the smallest cut that improves evidence, runtime correctness, or Studio trust without inflating compatibility claims.
