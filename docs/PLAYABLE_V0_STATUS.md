# Playable v0 Status

This document is the working truth for the private sandbox milestone. It separates the playable local prototype from real MUGEN compatibility.

## Closed In The Current Playable Prototype

- Vite, TypeScript, Three.js local app.
- First screen opens in Runtime Mode.
- Three active local original fighters are selectable: Nova Boxer, Mira Volt, Rook Apprentice.
- The local runtime fighters load atlas textures from `public/characters/*/sprite-sheet-alpha.png` and `manifest.json.frame_layout`.
- Nova Boxer, Mira Volt, and Rook Apprentice include freshly generated imagegen sprite-sheet sources, normalized atlas frames, QA contact sheets/GIFs, `sprite-atlas-builder` contracts, and MUGEN-lite `.def/.air/.cmd/.cns` templates under `public/characters/<fighter>/mugen/`.
- The three active fighters use 8-frame, slow-cadence `walk-forward` rows generated as new sprites, not mechanical repairs. The current rows were replaced from fresh one-row imagegen outputs, recomposed into the atlas, and checked with `sprite-atlas-builder/scripts/check_motion_variation.py --states walk-forward`. The heuristic returns `ok: true` with one conservative low-body-bob warning per fighter, so the runtime surfaces `walk QA warn 1` and the contact sheets/browser remain the final visual check.
- Crouch and jump scale are now controlled by character-level atlas normalization instead of per-frame fitting. Browser screenshots verify crouch as `anim 10`/`stateType C` and jump as `anim 40`/`stateType A` without the previous oversized pose effect.
- Runtime Mode now shows those local atlas motion reports as `walk QA` badges in the roster, HUD, compatibility panel, exported JSON, and browser diagnostics through `window.__MUGEN_WEB_SANDBOX__.runtimeRoster` / `.atlasMotionQa`.
- Rooftop Dojo is the default original native stage, with imagegen-generated PNG art, floor, bounds, parallax layer, camera, grid, and axis.
- Training Grid remains available as a geometry-only fallback stage.
- P1 can walk, crouch, jump, punch, and kick.
- CPU P2 pressures, walks into range, and attacks.
- Combat has hurtboxes, active hitboxes, hit pause, hit stun, pushback, life damage, and power gain.
- Runtime has stage HUD, life/power bars, timer, fight/KO/time-over message, pause, step, speed, reset, and mobile touch controls.
- Runtime and Inspector share the same `MugenSnapshot` render path.
- Inspector loads ZIP/folder characters and parses DEF/AIR/CMD/CNS/ST enough to inspect.
- Inspector State Browser now exposes per-state animation lookup, supported/unsupported controller counts, trigger-supported counts, HitDef markers, and compact controller strips so CNS coverage can be audited without opening JSON.
- ZIP/folder packages can also expose `stages/*.def`; imported stage definitions can be selected in Runtime Mode and currently drive match display name, camera, side bounds, player starts/facing, static SFF-backed normal BG sprites, action-backed BG animations, basic BG tiling/parallax, and placeholder fallback layers.
- SFF detection is non-crashing. SFF v1/8-bit PCX sprites can render in Inspector Mode, including previous-palette subfiles without embedded PCX palettes; SFF v2 metadata/palettes plus RAW/RLE8/RLE5/LZ5 decoding are wired. Current local fixture QA decodes official MUGEN 1.1 KFM 281/281 sprites, KFM720 281/281 sprites, CodeFuMan 274/274 sprites, and SF3 Ryu 1285/1285 sprites.
- Loaded decoded characters can be routed into Runtime Mode through an AIR/SFF/CMD bridge. The route maps standard action IDs to idle, walk, crouch, jump, and hitstun, and uses parsed CMD `[State -1]` `ChangeState` entries for simple attacks such as KFM stand light punch and Light Kung Fu Palm (`~D, DF, F, x`).
- Imported runtime QA now verifies real external fixtures through the browser: official KFM and CodeFuMan both route physical `A` (MUGEN `x`) into state `200`, execute a `HitDef`, and render their real SFF sprites; official KFM and CodeFuMan can route `QCF_x` into state `1000` when the command sequence is stepped correctly.
- Latest local visual QA for this controller-support pass ran on `http://127.0.0.1:5297/` because port `5173` was occupied by another local app. It uploaded `.scratch/fixtures/kfm-official.zip`, returned to Runtime Mode, pressed physical `A`, and verified Kung Fu Man in state/action `200`, `HitDef` executed, Mountainside Temple rendered, and target controllers (`TargetBind`, `TargetFacing`, `TargetLifeAdd`, `TargetState`) counted as recognized controllers instead of unsupported. Evidence lives under `.scratch/qa/target-controller-qa/`. Synthetic runtime tests cover actual target memory side effects because the visual KFM punch capture does not always connect with Mira at the sampled spacing.
- Partial `Pause`/`SuperPause` controllers now run through `PauseSystem`, freeze the two-actor match, honor source `movetime`, expose pause telemetry in `MugenSnapshot.matchPause`, show the pause in HUD/debug panels, and render a dark Three.js overlay for `SuperPause` `darken`. `SuperPause` also applies simple `poweradd` through a bounded delta returned by the system and now supports bounded `pausebg = 0` metadata, omitted default, disabled, explicit, and dynamic `anim/pos` metadata, bounded dynamic numeric params, and bounded positive `p2defmul` current-target damage scaling: required `synthetic-imported-superpause-pausebg.json` checksum `49bcfe16` / final checksum `397a8fae` proves `pausebg = 0` reaches match-pause evidence as `pauseBg = false`; required `synthetic-imported-superpause-anim-disabled.json` checksum `fc7a2ca4` / final checksum `5be3ca6c` proves `anim = -1` leaves no optional `superAnim` snapshot metadata through `superAnimAbsent = true`; required `synthetic-imported-superpause-default-anim.json` checksum `318c5e9f` / final checksum `747e7619` proves omitted `anim` resolves to default FightFX action `30` with offset `0,0`; required `synthetic-imported-superpause-dynamic-anim-pos.json` checksum `e6bfbf75` / final checksum `eb49d9db` proves `anim = var(6)` and `pos = var(7),var(8)` resolve as trace-gated `superAnim` raw/source/action/offset metadata; required `synthetic-imported-superpause-anim-pos.json` checksum `f7dcdc9d` / final checksum `7bd4afe8` proves `anim = S200` and `pos = 24,-48` survive as trace-gated player-AIR metadata; required `synthetic-imported-superpause-dynamic-params.json` checksum `052bb481` / final checksum `1847a3f3` proves `time = var(2)`, `movetime = var(3)`, `darken = var(4)`, and `poweradd = var(5)` resolve to max remaining `9`, max move time `2`, `darken = false`, and final P1 power `75`; required `synthetic-imported-superpause-p2defmul.json` checksum `ec1ba95e` / final checksum `6d009665` proves `p2defmul = 2` maps to temporary damage multiplier `0.5`, so a source-movetime `TargetLifeAdd -20` applies as `-10` after direct target memory exists and final P2 life reaches `953`. Bounded projectile/helper/explod source-movetime gates exist, and visual Explods now have bounded `ignorehitpause`, `pausemovetime`, and `supermovetime` actor budgets. This is deliberately not full super-pause parity: typed-operation lowering for dynamic pause params, bottom-to-zero exactness, actual renderer/background update parity, exact stage/BGCtrl pause timing, renderer visual suppression/playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, `p2defmul = 0` / `Super.TargetDefenceMul`, exact scaling stack order, sparks, sound timing, helper VM pause rules, exact effect layering, super background effects, and exact pause layering remain future work.
- Round state is now both system-owned and trace-gated: `RuntimeRoundSystem` owns bounded timer/KO/time-over/winner/message/reset behavior through a resolved `RuntimeRoundTiming` contract, opens phase `4` during the shared post-round window, and `RuntimeMatchRoundWorld` holds that clock while imported win-pose `RoundNotOver` remains asserted for KO or time-over. `PlayableMatchRuntimeOptions.roundTiming` can provide bounded timing overrides, and imported `fight.def` `[Round]` values can populate the same contract when no override exists, while the source-backed `45`/`45`/`255` defaults preserve the existing snapshot contract. `RuntimeRoundWinPose/v0` owns the bounded phase-4 `180/170/175` handoff when reserved actions are available, with readiness derived from the same timing contract; focused T278 proof now reaches draw state `175` before terminal close when both imported roots provide it. `synthetic-imported-round-ko.json` checksum `bfd5f073` proves KO `RoundSnapshot` evidence and final P2 life `0`, and `synthetic-imported-round-timeover.json` checksum `7d9f7907` proves bounded `timeover`/draw evidence through a short `roundTimerFrames` fixture. Exact MUGEN/IKEMEN release parsing, `over.forcewintime`, frame-start ordering, fade/motif/dialogue release, Common1/ZSS winpose execution, lifebars, teams, intros, and screenpacks remain future work.
- T279 adds bounded `over.hittime` mapping and an internal `roundNoDamage` gate: during the source-mapped KO/time-over interval, direct HitDef, reversal, priority, projectile, helper, and HitDef target-commit admission is skipped while target/effect maintenance, body push, bindings, clamping, and presentation continue. T280 extends that flag through imported player/root active and State -1 resource-controller dispatch, suppressing life, red-life, guard, dizzy, and power writes while preserving controller telemetry, `CtrlSet`, state progression, and presentation. Helper/effect-owner resources, `Target*` resource controllers, exact frame order, release choreography, and full MUGEN/IKEMEN round parity remain future work.
- T281 maps imported `over.forcewintime` into the round timing contract and
  holds phase 3 at the wait boundary when an active root is still controllable,
  standing, idle, and outside animation 5. The local post-round frame remains
  stable until the roots become ready or the bounded force window expires;
  time-over skips the animation-5 readiness portion. Source-less/demo timing
  keeps the prior behavior through a zero force default. T282 additionally
  carries imported `fadeout.time`/`fadeout.col`, extends source-derived
  terminal timing through `max(over.time, fadeout.time)`, publishes bounded
  `RuntimeRoundFade/v0`, and renders the active fade above the stage overlays.
  Exact `intro`/`winwaittime` frame ordering, fight-screen fade assets, Common1/
  ZSS release, motif, dialogue, skip, and team choreography remain future
  work.
- T284 adds the bounded FightScreen asset continuation: imported
  `fadeout.anim`/`fadeout.snd` are parsed, resolved against FightFX AIR/SFF,
  included in the additive round-fade snapshot, rendered with frame timing and
  fallback diagnostics, and played once through the global FightFX audio route.
  Exact localcoord/motif transforms, fade-in/release choreography,
  dialogue/skip, Common1/ZSS, teams/Turns, rollback/netplay, and full parity
  remain future work.
- Partial `PlaySnd`/`SndPan`/`StopSnd` controller events now lower static params, including static `PlaySnd lowpriority`, legacy `volume`, `volumescale`, `freqmul`, `loop`, `pan`, `abspan`, and static `SndPan channel/pan`, into typed `audio:playsnd` / `audio:sndpan` / `audio:stopsnd` operation evidence before running through `RuntimeAudioWorld` and `AudioEventSystem`, producing bounded `RuntimeSoundEvent` snapshot telemetry for the debug UI, Web Audio adapter, and required `synthetic-imported-sound.json` trace evidence with checksum `cc9c8c49`. Required `synthetic-imported-hitdef-dynamic-hitsound.json` checksum `fe3c0f3d` / final checksum `855df386` adds bounded dynamic direct `HitDef hitsound = Fvar(0),var(1)` typed telemetry, preserving contact group `5`, index `4`, raw sound-ref telemetry, `soundPrefix = kfm`, and typed `audio:playsnd` evidence for `F5,4`. Required `synthetic-imported-hitdef-dynamic-guardsound.json` checksum `bb38362a` / final checksum `3e0ddeb0` adds bounded dynamic guarded direct `HitDef guardsound = Fvar(0),var(1)` typed telemetry, preserving guard contact group `6`, index `4`, raw sound-ref telemetry, contact kind `guard`, `soundPrefix = kfm`, and typed `audio:playsnd` evidence for `F6,4`. Required `synthetic-imported-superpause-sound.json` checksum `3e19cb86` / final checksum `c5fb9428` adds bounded dynamic `SuperPause sound = Svar(0),var(1)` typed telemetry, preserving pause-start group `10`, index `0`, raw sound-ref telemetry, and typed `audio:playsnd` evidence for `S10,0`. Required `synthetic-imported-sound-dynamic-pan.json` checksum `879afcf4` / final checksum `b780e5e9` adds bounded dynamic numeric channel/pan/abspan typed telemetry through `audio:playsnd`, `audio:sndpan`, and `audio:stopsnd`. Required `synthetic-imported-sound-dynamic-value.json` checksum `bcdafe32` / final checksum `31b8a7b3` adds bounded dynamic `PlaySnd value = Fvar(0),var(1)` typed telemetry, preserving raw `Fvar(0),var(1)`, group `5`, index `3`, channel `4`, and `soundPrefix = kfm`. The Web Audio adapter has bounded explicit-channel behavior for replace / low-priority skip / pan / stop-all plus bounded `volumescale` gain scaling, diagnostic-only legacy `volume`, `freqmul` playback-rate scaling, source looping, and stereo panning from actor/camera-relative `pan` or screen-center `abspan`. Browser decoding/playback still depends on imported `.snd` archives with decodable RIFF/WAVE payloads and user-gesture audio unlock; exact priority classes, exact panning semantics, pre-RC8 legacy volume gain semantics, projectile/helper contact-sound operation telemetry, super-background audio, and timing parity remain future work.
- Partial `Gravity` now has required imported trace evidence: `synthetic-imported-gravity.json` checksum `92161764` proves airborne state `200` executes typed `kinematic:gravity` and exposes vertical velocity telemetry. Exact `yaccel`, floor snap, pause/tick order, helper/custom-state ownership, and full air-physics parity remain future work.
- Static movement/position controllers now have required imported trace evidence: `synthetic-imported-kinematic.json` checksum `92804390` proves state `200` executes typed `kinematic:velset`, `kinematic:veladd`, `kinematic:velmul`, `kinematic:posset`, and `kinematic:posadd` with bounded actor-frame position/velocity telemetry. Dynamic active-state movement has started moving from raw fallback into typed evidence: `synthetic-imported-const-controller-param.json` checksum `2dad3a50` proves dynamic `VelSet` telemetry for bounded `Const240p` / `Const480p` / `Const720p` params, `synthetic-imported-dynamic-veladd.json` checksum `daf99fb4` proves dynamic `VelAdd` telemetry for `var(...)` expression params, and `synthetic-imported-dynamic-velmul.json` checksum `4d241401` proves dynamic `VelMul` telemetry for `var(...)` expression params. Dynamic typed lowering for every kinematic controller, exact physics/tick order, floor snapping, helper/custom-state ownership, and full movement parity remain future work.
- Partial `StateTypeSet` now has required static and bounded dynamic imported trace evidence: `synthetic-imported-statetypeset.json` proves static typed `metadata:statetypeset` telemetry, and `synthetic-imported-statetypeset-dynamic.json` checksum `577404e4` / final checksum `083a76de` proves active-state `IfElse(var(n), enum, enum)` state/move/physics params resolve to actor metadata `C/A/N` with typed operation telemetry. Broad string-param parity, exact physics side effects, exact tick order, helper/team/redirect ownership, and full StateTypeSet parity remain future work.
- Common1-style fall recovery now has positive threshold evidence, bounded first-to-last `hitFall.recoverTime` drop evidence, bounded actor-frame tick-order evidence, bounded air/ground velocity evidence, negative required evidence, and optional real-fixture confirmation: `synthetic-imported-default-fall-recovery-threshold.json` checksum `7bb15a5f` observes `5050` while `hitFall.recoverTime` is positive, proves a summarized `5050` drop to `recoverTime = 0`, and then observes `5210` with `recoverTime = 0`; `synthetic-imported-default-fall-recovery-tick-order.json` checksum `e2691aab` requires that dropping `5050` countdown evidence before `5210` recovery evidence; `synthetic-imported-default-fall-air-recovery-velocity.json` checksum `560f6308` gates `5210` air-recovery velocity telemetry; `synthetic-imported-default-fall-ground-recovery.json` checksum `7945fd93` gates `5050 -> 5200 -> 5201 -> 52 -> 0` with `SelfState`, `VelSet`, `PosSet`, and actor-frame velocity telemetry; `synthetic-imported-default-fall-recovery-too-early.json` checksum `050e7e3c` proves `command = "recovery"` can be active before `fall.recovertime` expires without entering `5210` while `5050` actor-frame evidence keeps minimum `recoverTime >= 1`; `kfm-official-default-fall-recovery-too-early.json` checksum `0ad7ae02` confirms the same bounded reject window against local official KFM, forbidding `5210`, `5200`, and `5201` while the defender remains in `5050` with positive-minimum recovery-time evidence; `kfm-official-default-fall-recovery-threshold.json` checksum `19ec5148` confirms real KFM can hold positive recover time in state `5050`, prove a first-to-last `recoverTime` drop in that summarized bucket, and then accept the ground recovery branch `5200 -> 5201 -> 52 -> 0`. Exact recovery threshold tables, full velocity math, and controller/VM tick order remain future work.
- Partial `EnvShake`/`FallEnvShake` now run through `EnvShakeSystem`, which produces bounded shake event telemetry, emits typed `fallenvshake` operation evidence for compiled fall-shake controllers, resolves deterministic camera-shake offsets in the stage snapshot before Three.js renders them, and is trace-gated by required static `synthetic-imported-envshake.json` checksum `061f17d5` plus dynamic `synthetic-imported-envshake-dynamic.json` checksum `e1bf593f` / final checksum `8f52f1f4`. Dynamic `EnvShake time/freq/ampl/phase` now resolves through active controller expression fallback and records typed `envshake` operation evidence after resolution. Exact MUGEN/Ikemen waveform, `mul`, pause interaction, helper/redirect ownership, screenpack ownership, and stage/layer interactions remain future work.
- Partial `EnvColor` now runs through `RuntimeEnvColorWorld` / `EnvColorSystem`, which produces bounded stage-flash telemetry from static or dynamic `value`/`time`/`under`, records typed `envcolor` operation evidence for static params and resolved dynamic params, and renders a Three.js stage overlay from the snapshot. `synthetic-imported-envcolor.json` proves the static upper route with checksum `956b0f4b`, `synthetic-imported-envcolor-under.json` proves the static under route with checksum `0a7b5c96`, and `synthetic-imported-envcolor-dynamic.json` proves dynamic `value/time/under` typed telemetry with checksum `845c3d5e` / final checksum `282fc77f`. Exact MUGEN/Ikemen blend math, stage layer/window ordering, pause timing, renderer parity, helper/redirect ownership, and full presentation parity remain future work.
- Partial `SprPriority`, `PalFX`, `Trans`, `AngleSet`/`AngleAdd`/`AngleMul`/`AngleDraw`, `AfterImage`, and `AfterImageTime` now run through `SpriteEffectSystem`, which produces actor snapshot sprite priority, tint/opacity, render-angle, and ghost-trail sample data for Three.js. Exact palette math, blending modes, palette remap interaction, exact AngleDraw axis pivot/collision rotation, and engine-perfect afterimage timing remain future work.
- Dynamic `Trans alpha` params now have required runtime trace evidence through `synthetic-imported-trans-dynamic.json` checksum `4bffcd82` / final checksum `5beea0f0`: owner-local vars can resolve through active expression fallback into bounded `renderOpacity = 0.375` telemetry with typed `sprite-effect:trans` evidence after runtime expression resolution. Exact add/sub alpha math, draw-order interaction, and renderer parity remain future work.
- Partial non-colliding `Explod` actors now run through `ExplodSystem`, which owns typed `explod` `ControllerOp` consumption when compiled, spawn, simple typed `RemoveExplod` data, bounded owner-side `bindtime` for `p1`/`front`/`back`, bounded `vel`/`accel` movement, bounded static `scale`, bounded hit-pause advance through `ignorehitpause`, bounded `Pause`/`SuperPause` actor budgets through `pausemovetime`/`supermovetime`, animation advance, and effect actor snapshots. Exact bind tick-order/anchor parity, exact velocity/accel parity, exact scaling parity, exact pause layering, ownpal/remappal, FightFX/common animation routing, and advanced remove triggers remain future work.
- Partial visual `Helper` actors now run through `HelperSystem`, which owns typed `helper` `ControllerOp` consumption when compiled, spawn, stage-bounded removal, animation advance, owner sprite metadata, bounded `NumHelper(id)` trigger counts, effect actor snapshots, and a bounded helper-local micro-VM for state/action/kinematic/control/metadata/resource/variable trigger branches, helper-local `PlaySnd`/`StopSnd` event telemetry, helper-local direct `HitDef` combat, explicit/default/bare helper direct target memory plus helper-parented Projectile explicit/default target memory through `NumTarget(id)` / `Target(id), Life` / bare `Target, Life`, bounded helper-owned direct-HitDef and helper-parented Projectile `TargetLifeAdd` / `TargetPowerAdd` / `TargetVelSet` / `TargetVelAdd` / `TargetFacing` / `TargetBind` / `TargetDrop`, bounded owner-backed helper `TargetState` from direct HitDef and helper-parented Projectile target memory, helper-local static visual `Explod` / `Projectile` handoff, helper-local `NumExplod(id)` / `NumHelper(id)` / `NumProjID(id)` count reads against the bounded owner-side effect store, plus `DestroySelf` removal. Indexed redirects, helper `TargetState` breadth beyond the owner-backed direct-HitDef/helper-parented Projectile gates plus multi-target parity, key control, exact helper resource semantics, helper fvar/sysvar `VarRandom`, helper-owned Projectile combat/contact beyond bounded target memory, exact helper-local sound timing/channel/redirect ownership, palette ownership, and exact pause/lifecycle rules remain future work.
- Partial visual `Helper` actors now also carry bounded static `velset`/`vel`/`velocity` into actor snapshots; `synthetic-imported-helper-velocity.json` proves observed helper motion with `velset = 3,-1`, checksum `dc0fb203`. This remains visual-helper motion plus helper snapshot resource projection only, not exact helper VM/resource semantics, helper combat, helper-owned projectiles, exact helper physics, or full helper parity.
- Partial visual `Helper` actors now also carry bounded static `scale` plus `size.xscale`/`size.yscale` into effect payloads and `runtime.renderScale`; `synthetic-imported-helper-scale.json` proves render-scale evidence with `scale = 2,0.5`, checksum `a161d896`. This remains visual-helper scale plus helper snapshot resource projection only, not exact helper VM/resource semantics, helper collision-scale parity, ownpal/palette ownership, or full helper parity.
- Partial visual `Helper` actors now carry bounded `ignorehitpause`, `pausemovetime`, and `supermovetime` metadata and can advance during paused presentation passes when their matching budget permits. `synthetic-imported-helper-supermovetime.json` proves `supermovetime = 4` advances a Helper during `SuperPause` after source movetime expires, checksum `857f927e`. This remains visual-helper pause-budget behavior plus helper snapshot resource projection only, not exact Helper VM/resource semantics, helper combat, helper-owned projectiles, redirects, exact pause layering, or full helper parity.
- Required trace coverage now also proves the other two visual Helper pause-budget paths: `synthetic-imported-helper-pausemovetime.json` for `pausemovetime = 4`, checksum `bfe83f64`, and `synthetic-imported-helper-ignorehitpause.json` for `ignorehitpause = 1`, checksum `7d008a66`.
- Partial colliding `Projectile` actors now run through `ProjectileSystem`, which owns typed `projectile` `ControllerOp` consumption when compiled, spawn, velocity/acceleration/velocity-multiplier/animation advance, bounded static render scale, hitbox projection, stage-bounded removal, guard metadata, bounded terminal playback for resolved `projhitanim`/`projremanim`/`projcancelanim` AIR actions, owner sprite metadata, bounded `NumProj` / `NumProjID(id)` trigger counts, and effect actor snapshots. `ModifyProjectile` now executes a bounded owner-side live-projectile mutation path for static velocity, acceleration, velocity multiplier, scale, remove time, priority, hit budget, miss time, and remove-on-hit params. `RuntimeProjectileCombatWorld` owns the bounded two-actor projectile contact/reject/HitOverride/hit-or-guard/cleanup loop plus bounded equal-priority trade and higher-priority `projpriority` cancel with winner-priority decrement through `RuntimeEffectActorWorld`; contact resolution can mark bounded `ProjContact`/`ProjHit`/`ProjGuarded(projid)` owner trigger memory, removal logs expose the chosen hit/remove/cancel animation metadata, and trace actor-frame gates prove visible terminal playback for covered synthetic routes. Exact priority classes, dynamic ModifyProjectile expressions, exact projectile count/lifetime parity, exact contact-trigger timing/lifetime, exact terminal timing, full guard effects/timing, helper-owned projectile rules, exact `velmul` tick-order parity, scaled collision parity, and pause rules remain future work.
- `EffectActorSystem` now wraps those three effect actor families in one mutable per-fighter store, owning serial allocation, bounded spawn lists, advance/removal mutations, hit-removal pruning, snapshot handoff, and read-only store summaries. `RuntimeEffectActorWorld` wraps those stores behind a world-style contract with separate active-effect and presentation-effect advance passes plus a bounded projectile-combat handoff; `MatchWorld` now creates that world, injects it into `PlayableMatchRuntime`, and consumes the summaries as actor-registry `effectStores`. `FighterMatchState` no longer retains the raw effect actor store. The integrated runtime still resolves controller owner/action/position data, so the next ownership step is moving exact combat parity and exact pause/tick decisions behind `MatchWorld`.
- Partial visual `Explod` actors expose bounded `NumExplod(id)` trigger counts from the same effect-actor store; `synthetic-imported-numexplod.json` proves `NumExplod(9000)` can branch after visual Explod spawn, `synthetic-imported-removeexplod.json` proves bounded `RemoveExplod id = 9000` removal with MatchWorld lifecycle evidence plus an empty final P1 Explod store, `synthetic-imported-explod-bind.json` proves bounded owner-side `bindtime` follows P1 while the owner moves, `synthetic-imported-explod-velocity.json` proves bounded `vel`/`accel` visual movement with observed position/velocity actor-frame evidence, `synthetic-imported-explod-scale.json` proves bounded static `scale` reaches render snapshots/projection with observed actor-frame scale evidence, `synthetic-imported-explod-ignorehitpause.json` proves one source-owned Explod freezes during hitpause while a sibling with `ignorehitpause = 1` advances, checksum `a351168e`, `synthetic-imported-explod-pausemovetime.json` proves one source-owned Explod freezes during regular Pause while a sibling with `pausemovetime = 4` advances after source `movetime`, checksum `467c2030`, and `synthetic-imported-explod-supermovetime.json` proves the matching SuperPause route with checksum `1d99f66f`. Exact Explod binding/tick-order parity, advanced remove triggers, FightFX/common animation routing, ownpal/remappal, exact scaling parity, exact pause layering, and lifetime/physics parity remain future work.
- Partial target memory, target bindings, and simplified Target* side effects now run through `TargetSystem`, which owns bounded recent-target records, wildcard id matching, expiry, `TargetDrop` `excludeID`/`keepone` filtering with omitted `keepone` defaulting to `1`, binding duration, bound-position resolution, bounded `NumTarget(id)` trigger reads, and partial `TargetLifeAdd`/`TargetPowerAdd`/`TargetVel*`/`TargetFacing`/`TargetBind`/`BindToTarget`/`TargetState` application through a runtime-system contract. `RuntimeTargetWorld` now wraps target remember/advance/snapshot/link/count/find/controller application behind a world-style boundary; `MatchWorld` creates and injects it into `PlayableMatchRuntime`, and actor-registry `targetLinks` are derived through `RuntimeTargetWorld.snapshotLinks` instead of duplicated registry logic. `ActorSnapshot.runtime.targetRefs`/`targetBindings`/`bindToTarget` and `MatchWorld.targetLinks` expose recent target memory, active TargetBind binds, and active owner-side BindToTarget binds to Debug Studio and trace `world` evidence; `synthetic-imported-numtarget.json` proves `NumTarget(77)` can branch after direct `HitDef` target memory, while `synthetic-imported-bindtotarget-head.json` and `synthetic-imported-bindtotarget-mid.json` prove parsed target `[Size] head.pos` / `mid.pos` can drive bounded Head/Mid-anchor BindToTarget offsets. `synthetic-imported-custom-state.json` now proves a bounded attacker-owned `HitDef p2stateno -> ChangeState -> SelfState` route with P2 `customOwnerId = p1` actor-frame evidence and final return to state `0`/control; `synthetic-imported-targetstate-custom.json` proves the matching target-memory-driven `TargetState -> ChangeState -> SelfState` route with typed `target:targetstate` evidence and checksum `fedaf0a4`. The target controller effects still operate only on the two-actor sandbox's recent hit/projectile target; `BindToTarget` supports static `Foot`, `Mid`, and `Head` offsets with parsed target `[Size]` anchors when available; full redirects, team/multi-target rules, helper parent/root semantics, target persistence parity, exact bind tick-order, and complete custom-state ownership remain future work.
- Partial combat contact and result math now run through `CombatResolver`, which owns facing-aware box projection, collision overlap checks, simplified `HitBy`/`NotHitBy` eligibility, `HitOverride` lookup, held-back guard detection, attack/defense scaling from both partial controllers and CNS `[Data] attack` / `defence`, bounded nonlethal direct `kill = 0`, guarded `guard.kill = 0`, defender-side `AssertSpecial NoKO` clamps, bounded defender-side `AssertSpecial NoStandGuard` / `NoCrouchGuard` / `NoAirGuard` guard denial, and hit/guard result calculation. `RuntimeDirectCombatWorld` owns the bounded same-tick direct `HitDef` priority clash mutation plus direct hit/guard result mutation. Imported defenders holding back can now enter and leave a bounded Common1-style automatic guard route such as `120 -> 130 -> 140 -> 0` around `InGuardDist`; required traces cover both a near `guard.dist` trigger and a far-reject route that leaves the defender idle/control. Guarded imported defenders can enter known defender-owned Common1-style stand/crouch guard-hit states such as `150 -> 151` and `152 -> 153` when present, and both `p2stateno` plus `TargetState` owner-backed custom-state chains are trace-gated. Broader state transitions, target side effects, exact projectile lifecycle parity, exact HitDef priority/KO/no-KO classes beyond bounded clamps, exact attack/defence stacking and rounding, exact proximity/air guard behavior, guard effects, guard-end timing, fall routing, helper combat, complete custom-state ownership, and full IKEMEN/MUGEN combat timing remain future work.
- Required auto guard traces now include minimum controller-event and state-order evidence: `synthetic-imported-auto-guard-start.json` checksum `0c734290` gates P2 state `120` `ChangeState` `Guard Start Done` plus `120` before `130`, and `synthetic-imported-auto-guard-end.json` checksum `d1dc0aa3` gates P2 `120` `Guard Start Done` before state `130` `ChangeState` `Stop Guarding`, actor-frame `120 -> 130 -> 140`, plus final idle/control. This is bounded controller/actor-frame order evidence, not exact proximity guard, full Common1 controller loop, or full guard-end parity.
- `MatchWorld` now exposes an actor registry from snapshots through `getActorRegistry()`, indexing players and effect actors by id, kind, owner, root, parent, target links derived by `RuntimeTargetWorld`, lightweight lifecycle status, per-tick `spawn`/`active`/`remove` events, and `RuntimeEffectActorWorld` store summaries without changing match behavior checksums. Runtime Debugger and the new Studio `Debug` surface render the registry, target links, event count, store count, per-owner effect totals, and next serials; `RuntimeTraceArtifact` frame summaries preserve `world.lifecycle`, `world.eventsThisTick`, bounded `world.recentEvents`, `world.targetLinks`, and `world.effectStores` metadata; `pnpm qa:smoke` verifies the registry, lifecycle event stream, and effect store summaries through the UI/QA bridge. This is lifecycle/store/target-memory evidence over `EffectActorSystem` and `TargetSystem`, not full world-owned tick order, helper VM, exact target semantics, or exact parent/root parity yet.
- Partial `AssertSpecial` now records normalized flags in runtime snapshots/debug UI, recognizes it as a runtime-supported controller for reports, applies `NoWalk` and `NoAutoTurn` to the simple runtime control flow, maps `Invisible` to render opacity, blocks bounded stand/crouch/air guard through `NoStandGuard`/`NoCrouchGuard`/`NoAirGuard`, has a required one-frame `Time < 1` expiry trace where later held-back contact guards after `NoStandGuard` stops evaluating, treats attacker-side `Unguardable` as an unblocked hit in the partial hit/guard resolver, and uses defender-side `NoKO` as a bounded nonlethal damage clamp. Other flags such as intro/global/shadow and exact broader lifetime/no-KO/round-flow semantics are telemetry until the matching systems exist.
- Compatibility reports are visible and exportable as JSON. Imported-character reports now separate parsed statedefs, recognized executable controller states, trigger-clean CMD state-entry controllers, runtime-routable state targets, and session-executed imported states instead of collapsing those layers into one "executable" number. Imported stage summaries are also exported and shown in the UI with DEF/SFF/music presence, decoded stage sprite counts, static/animated BG coverage, tiled layers, and unsupported stage features.
- A first compiler layer now exists under `src/mugen/compiler/`. It compiles parsed CMD commands, trigger expressions, and state controllers into typed IR and a `CompileReport`. Loaded characters now cache one `RuntimeProgramIr`; imported fighters carry that program into the match runtime; active-state controllers, State -1 setup/routing, and trigger evaluation read the cached IR instead of recompiling controllers during ticks. `StateProgramExecutor` now classifies each `ControllerIr` into control-flow, animation, shared runtime, side-effect, or unsupported dispatches before `PlayableMatchRuntime` applies the behavior. Compatibility reports use the same compiler output for compiled/routable/unsupported counts, and runtime sessions now expose `executedOperations` for typed `ControllerOp` side effects such as `hitdef`, `kinematic:*` including `kinematic:hitvelset`, `bounds:*`, `collision:*`, `metadata:*`, `orientation:*`, `sprite-effect:*` including `sprite-effect:sprpriority`, `sprite-effect:palfx`, `sprite-effect:trans`, `sprite-effect:remappal`, `sprite-effect:afterimage`, and `sprite-effect:afterimagetime`, `envcolor`, `target:*`, `pause:*`, `projectile`, `helper`, `explod`, `hitfall:*`, `fallenvshake`, `reversaldef`, and `damage-scale:*`. Larger visual/combat side-effect controllers still execute from parsed params through `controller.source`, so this is an architectural migration point rather than full bytecode/VM parity.
- Static `Width player/value` now compiles into `collision:width`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-width.json` with actor body-width evidence. Dynamic `Width player/value` params now also resolve through active expression fallback and record typed `collision:width` telemetry, with `synthetic-imported-width-dynamic.json` proving `var(0)=21` / `var(1)=43` body-width telemetry. This only proves bounded player body-width/push separation, not exact edge-width semantics, exact overlap resolution, team/helper ownership, or tick-order parity.
- Static `StateTypeSet statetype/movetype/physics` now compiles into `metadata:statetypeset`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-statetypeset.json` with actor-frame `C/A/N` state metadata evidence. This only proves bounded metadata plumbing, not exact MUGEN/IKEMEN physics side effects or tick-order parity.
- Static `PlayerPush value` now compiles into `collision:playerpush`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-playerpush.json` with actor-frame `playerPush = false` evidence. Dynamic `PlayerPush value` params now also resolve through active expression fallback and record typed `collision:playerpush` telemetry, with `synthetic-imported-playerpush-dynamic.json` proving `var(0)=0` body-push telemetry. This only proves bounded body-push flag plumbing, not exact overlap resolution, team/helper push rules, or tick-order parity.
- Static `Turn` now compiles into `orientation:turn`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-turn.json` with actor-frame `facing = -1` evidence. This only proves bounded facing-flip plumbing, not exact auto-facing, team/helper/target-facing parity, or tick-order parity.
- Static `SprPriority value/priority` now compiles into `sprite-effect:sprpriority`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-sprpriority.json` with actor-frame `spritePriority = 5` evidence. This only proves bounded renderer-ordering telemetry, not exact layer, shadow, helper, Explod, or draw-order parity.
- Static `PalFX time/add/mul/color/invert` now compiles into `sprite-effect:palfx`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-palfx.json` with actor-frame material telemetry evidence. This only proves bounded material telemetry, not exact palette math, blending, `sinadd`, remap interaction, or timing parity.
- Static `Trans trans/value` now compiles into `sprite-effect:trans`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-trans.json` with actor-frame render-opacity telemetry evidence. This only proves bounded opacity telemetry, not exact add/sub alpha math, palette/remap interaction, draw-order interaction, or timing parity.
- Static `AngleSet value`, `AngleAdd value`, `AngleMul value`, and `AngleDraw` compile into `sprite-effect:angleset`, `sprite-effect:angleadd`, `sprite-effect:anglemul`, and `sprite-effect:angledraw`, run through the imported runtime path, rotate the Three.js sprite quad from snapshot `renderAngle`, and are trace-gated by `synthetic-imported-angle.json` checksum `127250a6` plus `synthetic-imported-anglemul.json` checksum `e0dae072`. Dynamic active-state `AngleSet value`, `AngleAdd value`, `AngleMul value`, and `AngleDraw value/scale` now also resolve owner-local var/fvar params into typed Angle sprite-effect telemetry through required `synthetic-imported-angle-dynamic.json` checksum `13560dcd` / final checksum `4d7c4726` and `synthetic-imported-anglemul-dynamic.json` checksum `0bb54a1c` / final checksum `c9f2b557`, preserving `renderAngle = 35`, `renderScale = 2,0.5`, and `renderAngle = 45` evidence. This only proves bounded rotation/scale telemetry/rendering, not exact MUGEN/IKEMEN axis pivot, rotated collision boxes, draw-order/scale/palette interaction, renderer parity, helper/redirect ownership, or timing parity.
- Static `EnvColor value/time/under` now compiles into `envcolor`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-envcolor.json` with stage-frame color/opacity evidence. This only proves bounded stage-flash telemetry/rendering, not exact blend math, layer/window behavior, pause timing, or presentation parity.
- Static `RemapPal source/dest` now compiles into `sprite-effect:remappal`, runs through the imported runtime path, and is trace-gated by `synthetic-imported-remappal.json` with actor-frame palette-remap telemetry evidence. This only proves bounded telemetry, not ACT/SFF pixel remapping, palette application, PalFX interaction, or timing parity.
- Static `AfterImage time/length/timegap/framegap/paladd/palmul/trans` and static `AfterImageTime time/value` now compile into `sprite-effect:afterimage` / `sprite-effect:afterimagetime`, run through the imported runtime path, and are trace-gated by `synthetic-imported-afterimage.json` with actor-frame ghost-trail telemetry evidence. This only proves bounded trail telemetry, not exact blend modes, palette/remap interaction, sampling cadence, persistence edge cases, or timing parity.
- Runtime compatibility sessions now include active imported command names, routed state IDs, and the last routed State -1 entry so command/debug failures can be audited without guessing.
- Runtime replay evidence now has a renderer-independent `RuntimeTrace` harness for scripted input frames, compact actor/effect/round/compatibility summaries, runtime log events, combat reasons, typed operation counts, per-frame checksums, and final deterministic checksums. `evaluateRuntimeTraceGate` can now assert trace evidence for actor source/kind, routed states, executed states/controllers, executed typed operations, active imported commands, effect kinds, event categories, combat reasons, final actor state/control/life, target-link ownership/binding evidence, actor-frame observed min/max position/velocity/scale, actor-frame body-width/body-push/sprite-priority/palette-fx/palette-remap/facing/state-metadata evidence, and actor-frame/final-actor `customOwnerId`. `RuntimeTraceArtifact` exports those checksums, compact per-frame summaries, final actors/effects, events, combat reasons, script frames, and gate results as `runtime-trace-artifact/v0` JSON, and Studio Build can export plus display the latest current P1/P2/stage smoke trace artifact in its QA Evidence panel. Studio Evidence now provides a first URL-addressable browser for all/gated/asset/trace/compile/compatibility/diagnostic records with an attention queue, a bounded session history of recent trace artifacts, browser-local persisted trace history, first current-vs-persisted trace comparison by checksum plus frame/event/gate/pass deltas, a metric/gate Trace Comparison Review, and a basic Trace Frame Scrubber for selecting frame checksum/event rows. The latest aggregate is exposed to QA automation at `window.__MUGEN_WEB_SANDBOX__.studioEvidence`; the latest trace artifact remains exposed at `.traceArtifact`, recent in-session artifacts are exposed at `.traceArtifacts`, persisted entries at `.storedTraceEvidence`, comparisons at `.studioEvidence.persistedTraceComparisons`, and selected frame data at `.traceFrameScrubber`. Unit coverage includes native hit/whiff traces, synthetic imported CMD/CNS HitDef hit and HitBy/NotHitBy reject traces that require typed `hitdef` operation evidence, synthetic imported bounded direct `HitDef priority`, `kill = 0`, `guard.kill = 0`, `AssertSpecial NoStandGuard` / `NoCrouchGuard` / `NoAirGuard` guard-deny, and `AssertSpecial NoKO` traces, synthetic imported ReversalDef and AttackMulSet/DefenceMulSet damage-scale traces that require typed `reversaldef` / `damage-scale:*` operation evidence, synthetic imported Common1-style stand/crouch guard-hit traces that prove `150 -> 151`, `152 -> 153`, and typed `kinematic:hitvelset`, a synthetic imported common get-hit trace that requires typed `hitfall:*` and `fallenvshake` evidence, synthetic imported custom-state ownership traces that prove `p2stateno` and TargetState routes through `888 -> 889 -> 0` with `customOwnerId = p1`, synthetic imported default Common1 entry, stand progression, fall-branch, and bounded recovery-chain traces that prove `5000`, `5000 -> 5001 -> 0`, `5000 -> 5030 -> 5050`, and `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0`, synthetic imported Width/PlayerPush/Turn/StateTypeSet/SprPriority/PalFX/RemapPal traces that prove bounded body-width/body-push/facing/state-metadata/sprite-priority/material/palette-remap telemetry evidence, a synthetic imported Target* trace that requires typed `target:*` operation evidence plus `targetLinks` ownership/binding evidence after real target memory, synthetic imported `NumTarget(77)`, `NumTarget(0)` via `synthetic-imported-default-numtarget.json` checksum `5869ebbd`, `NumExplod(9000)`, `NumHelper(42)`, and `NumProjID(77)` branch traces, synthetic imported `RemoveExplod` removal evidence, synthetic imported Explod `bindtime` owner-follow evidence, synthetic imported Explod `vel`/`accel` movement evidence, synthetic imported Explod static `scale` evidence, synthetic imported Projectile hit, Projectile guard, and Projectile multi-hit traces that require typed `projectile` operation evidence plus projectile effect actor lifecycle, target-link evidence, and hit/guard/cooldown events, a synthetic imported Helper trace that requires typed `helper` operation evidence plus a helper effect actor, a synthetic imported Explod trace that requires typed `explod` operation evidence plus an explod effect actor, optional local official KFM traces for stand light punch `x -> 200`, Light Kung Fu Palm `D, DF, F, x -> 1000`, default stand/crouch guard-hit `150 -> 151` and `152 -> 153`, default Common1 `5000`, default stand progression `5000 -> 5001 -> 0`, default fall/ground-impact/bounce/lie-down entry `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110`, and lie-down/get-up completion `5110 -> 5120 -> 0` when `.scratch/fixtures/kfm-official.zip` is present, and artifact pass/fail serialization. `pnpm qa:trace` now exports the same evidence as release-style JSON under `.scratch/qa/trace-gates/`, including required native/synthetic gates and optional official KFM gates when the local fixture exists. Imported multi-script fixture packs, full replay trace diff, and release evidence bundles are future work.
- `synthetic-imported-helper-projectile-bare-target.json` is now part of required `qa:trace` evidence, checksum `8c9129c1`, proving bounded helper-parented Projectile id `8863` memory can feed helper-local bare `Target, Life` after `NumTarget(8863)`, with owner/helper target links, helper state `1242/978`, helper `targetCount = 1`, projectile `parentId = p1-helper-0` / `effectId = 8863`, final P2 `life = 982`, and shared `S5,11` / FightFX `F7017` contact-package evidence. The default TargetState sibling gate `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1`, explicit TargetState sibling gate `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3`, default Target-controller sibling gate `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae`, and explicit Target-controller sibling gate `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8` remain required. Helper-owned custom state tables, throws, teams, multi-target selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, and full helper Projectile Target parity remain future work.
- `synthetic-imported-hitdefattr.json` is now part of required `qa:trace` evidence, proving a bounded `HitDefAttr = SC, NA, SA, HA` plus `MoveContact` branch from state `200` to `266` against the current active imported `HitDef` attr.
- `synthetic-imported-movehit-counter.json` is now part of required `qa:trace` evidence, proving bounded direct `MoveHit >= 1` routing after imported `HitDef` contact. `synthetic-imported-movehitreset.json` checksum `719c2cc7` proves partial `MoveHitReset` clears state-local direct contact memory before a later `MoveHit >= 1` branch can fire. Exact first-tick timing, hitpause accounting, helpers/redirects/teams, and full MUGEN/IKEMEN contact-trigger parity remain future work.
- `synthetic-imported-hitcount.json` is now part of required `qa:trace` evidence, proving bounded direct `HitCount`/`UniqHitCount` routing after imported `HitDef` contact. This is two-actor direct-hit evidence only; guard/projectile counts, helpers, multi-target uniqueness, combo lifetime, and exact MUGEN/IKEMEN parity remain future work.
- `synthetic-imported-hitadd.json` is now part of required `qa:trace` evidence, checksum `910574ea`, proving static `HitAdd value` can add to bounded current-state direct `HitCount` telemetry after imported `HitDef` contact while `UniqHitCount` stays tied to target uniqueness. Combo lifetime, guard/projectile counts, helpers, redirects, teams, and exact MUGEN/IKEMEN parity remain future work.
- `synthetic-imported-variable.json` is now part of required `qa:trace` evidence, checksum `3b33f7a8`, proving static typed `VarSet`, `VarAdd`, `VarRandom`, and `VarRangeSet` can fill owner-local vars and route through `var(...)` triggers. Exact MUGEN random stream parity, variable scopes, helper/parent/root redirects, sysvar/fvar lifetime parity, IKEMEN map vars, dynamic lowering, and full CNS VM parity remain future work.
- `synthetic-imported-resource.json` is now part of required `qa:trace` evidence, checksum `7bbcb2e4`, proving static typed `LifeAdd`, `LifeSet`, `PowerAdd`, and `PowerSet` can mutate owner-local life/power, expose life `750` and power `900` in actor-frame evidence, and route through `Life` / `Power` triggers. Exact resource scaling, helper/parent/root redirects, team modes, round/KO flow, dynamic lowering, and full CNS VM parity remain future work.
- `synthetic-imported-control.json` is now part of required `qa:trace` evidence, checksum `80c4c446`, proving static typed `CtrlSet` can restore owner-local control through `resource:ctrlset` evidence. Dynamic expressions, helper/parent/root redirects, exact state-entry control timing, and full CNS control parity remain future work.
- `synthetic-imported-animation.json` is now part of required `qa:trace` evidence, checksum `ee651cbc`, proving bounded `ChangeAnim` can retarget active imported AIR action `205` and partial `ChangeAnim2` can retarget owner-backed action `206` with actor-frame/final-animation evidence. Missing-action fallback, exact `elem`/`elemtime`, redirects, helper/custom-state ownership, multi-import SFF namespaces, and full MUGEN/IKEMEN animation-source parity remain future work.
- `synthetic-imported-assertspecial-guarddeny.json`, `synthetic-imported-assertspecial-crouch-guarddeny.json`, `synthetic-imported-assertspecial-air-guarddeny.json`, and `synthetic-imported-assertspecial-lifetime.json` are now required `qa:trace` evidence, with checksums `f636748d`, `e47a0cb1`, `62179385`, and `181ded30`, proving bounded defender-owned `NoStandGuard`, `NoCrouchGuard`, and `NoAirGuard` deny matching state-type guards, resolve otherwise guardable `HitDef` contacts as hits, expose hitstun actor-frame evidence, avoid guard evidence, require defender-side `AssertSpecial` before attacker-side guardable `HitDef` for the stand/crouch/air denial routes, and prove a one-frame `NoStandGuard` expiry route where later held-back contact guards. Broader AssertSpecial lifetime/persistence layering, priority, global/team/helper ownership, pause layering, KFM/Common1 confirmation, guard effects, and full MUGEN/IKEMEN guard parity remain future work.
- `synthetic-imported-receiveddamage.json` is now part of required `qa:trace` evidence, proving bounded defender-local `ReceivedDamage`/`ReceivedHits` routing after a direct imported `HitDef` sends the defender into state `5000` and then state `280`. This is two-actor direct-hit evidence only; guard chip, projectile damage, target-controller damage, helpers, teams, exact trigger lifetime, and exact MUGEN/IKEMEN parity remain future work.
- `synthetic-imported-projectile-receiveddamage.json` is now part of required `qa:trace` evidence, proving bounded defender-local `ReceivedDamage`/`ReceivedHits` routing after a projectile hit sends the defender into state `5000` and then state `280`. This is two-actor projectile-hit evidence only; projectile guard, target-controller damage, helpers, teams, exact trigger lifetime, and exact MUGEN/IKEMEN parity remain future work.
- `synthetic-imported-reversal.json` now also proves bounded `MoveReversed >= 1` routing for the attacker after a defender-side partial `ReversalDef` counter and hitpause. This is two-actor direct-reversal evidence only; projectile/helper/custom-state reversals, redirect ownership, trigger lifetime, priority, and exact MUGEN/IKEMEN parity remain future work.
- `synthetic-imported-prevmovetype.json`, `synthetic-imported-prevanim.json`, and `synthetic-imported-prevstatetype.json` now prove bounded `PrevMoveType = A`, `PrevAnim = 205`, and `PrevStateType = A` routing for imported state changes, complementing `PrevStateNo`. This is two-actor previous-state/animation metadata only; redirects, helpers, teams, custom-state ownership edges, and exact tick order remain future work.
- `MatchWorld` is now the app-facing match boundary and preserves current `PlayableMatchRuntime` behavior through delegation. `ActorSnapshot` now carries `actorKind`, `ownerId`, `rootId`, and `parentId` for players and current helper/projectile/explod effects, while `spriteOwnerId` remains separate for owner-backed rendering. A lightweight lifecycle registry, target-link registry, and lifecycle event stream now exist for debug/evidence reads and trace artifacts; exact target semantics, helper-owned execution, exact parent/root semantics, and full world-owned tick order remain future work behind that facade.
- UI state for mode, roster, and inspector tab is URL-backed for repeatable local QA.
- Browser diagnostics are exposed at `window.__MUGEN_WEB_SANDBOX__`.
- Browser diagnostics now include the loaded character compatibility report, resolved file paths, parser diagnostics, stage reports, runtime roster QA, active Studio tab, Studio summary/project manifest/compiled runtime manifest/stored projects, renderer diagnostics, and audio diagnostics for Playwright verification.
- `pnpm qa:smoke` now provides a repeatable Playwright baseline for Runtime desktop/mobile and Studio Build/Evidence. It starts Vite automatically unless `QA_BASE_URL` is supplied, exports a smoke `runtime-trace-artifact/v0`, checks the QA bridge, requires actionable selected-asset and Evidence next-action fields, verifies persisted local trace evidence history, and saves screenshots plus `diagnostics.json` under `.scratch/qa/qa-smoke/`.
- Studio Mode is available as a first project-workbench shell. It keeps the current match playtest running in Workbench/Assets/Debug/Evidence/Modules/Build, switches the center viewport to AIR preview in Studio Inspector, provides URL-addressable `Workbench`, `Assets`, `Inspector`, `Debug`, `Evidence`, `Modules`, and `Build` surfaces, derives asset provenance/module/gate summaries from runtime/import state through `StudioModel`, exports/reopens/saves a `project.json`-style manifest in browser-local recent projects, records `sourcePackages` relink metadata for imported ZIP/folder sources, compiles it to `runtime-manifest/v0`, exports and previews `runtime-trace-artifact/v0` smoke evidence from the selected playtest setup, exports an `export-bundle/v0` ZIP package with project/runtime contracts, source-runtime maps, evidence, compatibility report, README, latest trace when available, browser-fetchable local assets, and current-session imported ZIP/folder source files with bytes/checksums, applies available P1/P2/stage entry IDs, and exposes the same tab/summary/assets/evidence/manifest/compiled/trace/package/recent entries at `window.__MUGEN_WEB_SANDBOX__.studioTab`, `.studio`, `.studioAssets`, `.studioEvidence`, `.project`, `.compiledProject`, `.traceArtifact`, `.traceArtifacts`, `.storedTraceEvidence`, `.traceFrameScrubber`, `.projectBundle`, and `.storedProjects`; the active actor registry is also exposed as `.actorRegistry`. Studio Assets has a dense asset browser with provenance/status filters, URL-backed asset selection cards, asset triage metrics, selected asset detail, playtest-entry replacement flow, source/runtime mapping, visual dependency graph, dependency drilldown, missing/partial reference summary, related evidence, and an attention queue; Studio Build has readiness rows for runnable/partial/blocked/exportable/package-bundle/source-package state and manual ZIP/Folder relink actions that validate reopened source packages by required paths. Asset/gate/evidence/build rows now carry first actionable status fields such as severity, impact, linked evidence ids, blockers, exportability, and next action, including legacy-manifest normalization for older project records. Exported trace artifacts persist into bounded browser-local evidence history with project entry/source-package metadata, stale/current status, comparison records, and frame scrubber state. It does not persist workspace edits, automatically reacquire imported source files without user relink, resolve missing external assets, or replay full trace timelines yet.
- The workspace shell now has a compact mode header with live runtime/inspector/studio status, a keyboard skip link to the Three.js viewport, clearer focus states, resilient badge overflow, and collapsible heavy debug sections for Actor Registry, Combat Debugger, Frame Inspector, and Compatibility Report. The latest Studio UI polish adds surface summaries in the Studio tab rail, visible asset/evidence filter counts, stronger selected/focus states, status-coded asset rows with a separate exportability lane, renamed focus/action panels instead of nested card copy, a smaller diagnostic console dock, a denser Studio Command Palette `Action index` with result counts, surface column, and status signal column, plus a compact viewport instrument strip that shows the active Studio surface without adding another card over the playtest. This improves day-one usability and mobile scan length, but it is still a product-shell polish pass, not the full Character/Stage editing studio.

## Current Explod RemoveOnGetHit Cut

- Partial `Explod` now supports bounded `removeongethit = 1` pruning for owner-side visual Explods when the owner enters current direct and projectile hit/guard routes.
- Covered routes: direct hit, direct guard, `HitOverride` force-guard, Reversal victim paths, projectile hit, and projectile guard paths that call the shared get-hit marker.
- Required traces: `synthetic-imported-explod-removeongethit.json`, `synthetic-imported-explod-removeonprojectilehit.json`, and `synthetic-imported-explod-removeonprojectileguard.json`.
- Remaining gaps: exact tick order, helper-bound Explod cleanup timing, custom-state edge cases, FightFX/common animation routing, and broader remove-trigger semantics.

## Current Explod Pause-Budget Cut

- Partial `Explod` now carries bounded `ignorehitpause`, `pausemovetime`, and `supermovetime` through compiled `ExplodControllerOp` data and raw controller fallback.
- Hit pause advances only visual Explods with `ignorehitpause = 1`.
- `Pause`/`SuperPause` first honor source `movetime`, then paused presentation passes advance only Explods with remaining `pausemovetime`/`supermovetime` budget.
- Required traces: `synthetic-imported-explod-ignorehitpause.json`, checksum `a351168e`; `synthetic-imported-explod-pausemovetime.json`, checksum `467c2030`; and `synthetic-imported-explod-supermovetime.json`, checksum `1d99f66f`.
- Remaining gaps: exact pause layering, helper-bound Explod pause timing, sound/spark timing, super backgrounds, FightFX/common animation routing, and full MUGEN/IKEMEN tick-order parity.

## Current Projectile Motion Cut

- Partial `Projectile` now carries bounded `accel`, `velmul`, and `projscale`/`scale` through compiled `ProjectileControllerOp` data and raw controller fallback.
- Active projectile presentation advance applies acceleration after velocity movement, matching the existing bounded Explod integration pattern.
- Non-default projectile scale reaches `renderScale` snapshots for Three.js projection; non-default acceleration/scale are also visible in effect payloads for Debug/Evidence inspection.
- Required traces: `synthetic-imported-projectile-motion.json`, checksum `4e5c5114`, proving observed projectile velocity growth plus `projscale = 1.75,0.5` actor-frame scale evidence; and `synthetic-imported-projectile-velmul.json`, checksum `749b8552`, proving observed X velocity decay from `velmul = 0.5,1`.
- Remaining gaps: exact `velmul` tick-order parity, scaled collision boxes, exact contact/removal tick timing, helper-owned projectiles, and full MUGEN/IKEMEN projectile parity.

## Still Not Real MUGEN Runtime Compatibility

- Imported SFF v1/8-bit PCX and supported SFF v2 sprites can render in Inspector Mode and in the generic imported Runtime route.
- ACT palettes are parsed only as a placeholder layer.
- CNS controllers are parsed/indexed and a small tick-by-tick subset executes for imported characters: state/anim changes, partial `ChangeAnim`/`ChangeAnim2` element positioning, partial `ChangeState` `anim` override/preserve behavior, partial `ChangeAnim2` owner AIR/sprite routing, velocity/position changes, gravity, ctrl/life/power/vars, `VelMul`, partial `HitVelSet`, partial `HitFallVel`, partial `HitFallDamage`, partial `HitFallSet`, partial `FallEnvShake`, `StateTypeSet`, `LifeAdd`, `LifeSet`, `PowerSet`, partial `VarRandom`, `VarRangeSet`, partial `AssertSpecial`, partial `PlayerPush`, `Turn`, partial `PosFreeze`, partial `ScreenBound`, partial `HitBy`/`NotHitBy`, partial `HitOverride`, partial `ReversalDef`, partial `DefenceMulSet`, partial `AttackMulSet`, partial target controllers (`TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, `TargetDrop`, controller-owner `TargetState`), partial `Pause`/`SuperPause`, partial `HitDef`, partial `Width`, partial `SprPriority`, partial `PalFX`, partial `Trans`, partial `AngleSet`/`AngleAdd`/`AngleMul`/`AngleDraw`, partial `RemapPal` telemetry, partial `AfterImage`/`AfterImageTime`, partial `Explod`/`RemoveExplod` including bounded `ignorehitpause`/`pausemovetime`/`supermovetime`, partial `Helper` with helper-local `CtrlSet`/`StateTypeSet`/`VarSet`/`VarAdd`/`VarRangeSet` VM support, partial `Projectile`, partial `PlaySnd`/`StopSnd`, partial `EnvShake`, true no-op `ForceFeedback`, debug clipboard no-ops `DisplayToClipboard` / `AppendToClipboard` / `ClearClipboard`, deprecated dust presentation no-op `MakeDust`, and `Null`. Variable support includes deterministic sandbox-side `VarRandom` int assignment for bounded owner-local branches and bounded helper-local non-random variable branches; exact MUGEN random stream plus parent/root/team/helper redirect scopes remain incomplete. Broader side-effect controllers are still not complete.
- `DestroySelf` is accepted and counted as a helper-lifecycle no-op in imported player-state CNS telemetry, and removes the current visual Helper when executed inside the bounded helper-local micro-VM. Recursive cleanup, same-frame interruption, parent/root ownership, and full helper-owned VM semantics remain incomplete.
- CMD command matching is wired for `[State -1]` `ChangeState` routing, including `[Remap]` button aliases, partial `buffer.hitpause`, partial `steptime`, exact 8-way direction matching, duplicate command-name alternatives, `|` step alternatives, `$` direction families, partial `/` hold, `~` release, numeric charge windows such as `~30$D`, same-frame button combos, and fresh activations for plain inputs. The expression subset now covers common KFM/Common1 trigger forms such as `vel x/y`, `pos x/y`, `p2bodydist x/y`, bounded `Facing`/`P2Facing`/`P2Life`/`P2Power`/`NumEnemy`, bounded `Name`/`P1Name`/`P2Name`/`AuthorName`, bounded `EnemyNear, ...` opponent redirects including `EnemyNear, AuthorName`, `PrevStateNo`/`PrevAnim`/`PrevMoveType`/`PrevStateType`, bounded `StageTime`/`GameTime`, `Const(...)`, `GetHitVar(...)`, `SelfAnimExist(...)`, bounded `SelfStateNoExist(...)`, bounded `SelfCommand`, `IfElse(...)`, `AnimElemTime(...)` from current AIR frame timing, `HitDefAttr = ...` and `HitDefAttr(...)` against the current active `HitDef` attr, bounded direct `ReceivedDamage`/`ReceivedHits`, `NumTarget(id)`, `NumExplod(id)`, `NumHelper(id)`, `NumProj`, `NumProjID(id)`, `MoveContact`/`MoveHit`/`MoveGuarded`, `ProjContact`/`ProjHit`/`ProjGuarded(projid)`, `HitShakeOver`, `HitOver`, `command = "name"` / `command != "name"` / `SelfCommand = "name"` inside simple composite expressions, and simple range comparisons, but many CNS semantics are still approximate.
- Imported characters such as Kung Fu Man are partially playable through the imported bridge; their full authored CNS/CMD behavior is not the runtime source of truth yet.
- SND archives are parsed when they contain embedded RIFF/WAVE payloads, `RuntimeAudioWorld` / `AudioEventSystem` emit inspectable `PlaySnd`/`StopSnd` events, and `PlaySnd` can trigger partial Web Audio playback after browser user-gesture unlock. Full MUGEN sound channel semantics are still not implemented.
- Stage `.def` parsing is partial: it extracts core metadata and runtime setup, can render static normal BG sprites and action-backed BG animations from the stage SFF when decoded, applies bounded tiling plus simple horizontal parallax, and executes a bounded BGCtrl renderer subset for recognized `Visible`/`Enabled`/position/velocity/anim/sine controller types. Exact MUGEN BGCtrl timing/parity, parallax/tiling/window/mask/trans behavior, shadows/reflections, 3D model stages, and exact zoom behavior are not implemented.

## Next Hard Gate

To call an imported real MUGEN character partially playable, the next gate is:

1. Broaden tick-by-tick CNS controller execution for one known character, preferably KFM.
2. Expand side-effect controllers beyond the current movement/state/partial-hit/partial-guard/partial-hit-eligibility/partial-target/pause/audio/shake/tint/trail/effect/helper/projectile/custom-state-entry subset, especially full `HitDef` attr/priority semantics, exact target/custom-state ownership, exact `Pause`/`SuperPause` layering, exact `PosFreeze`/`ScreenBound` tick/camera parity, richer `.snd` channel behavior, helper state machines, projectile edge cases, and visual/sound hooks.
3. Report every skipped trigger/controller in `CompatibilityReport`.
4. Verify multiple KFM attacks, real Common1 fall/bounce/recovery states, guard-start/end/guard-hit paths, exact proximity behavior, and state exits visually in Runtime Mode with CNS/CMD-driven state changes.

