# QA And Acceptance Gates

This project has two kinds of quality gates: code gates and playable evidence gates. Both matter. Parser tests can prove data behavior; browser screenshots prove that the Three.js/runtime/UI path still works.

## Standard End-Of-Round Checks

Run these at the end of a focused implementation round:

```bash
pnpm test
pnpm typecheck
pnpm build
```

Do not run the full suite after every tiny edit unless debugging a specific failure. Keep the full checks for the close of the round.

## Visual QA Requirement

Any frontend, renderer, runtime snapshot, debug panel, stage, or sprite-pipeline change needs browser verification before it is called done.

Minimum evidence:

- Screenshot of default Runtime Mode.
- Screenshot or diagnostics for the affected panel/feature.
- If imported content is affected, upload a local fixture and capture the relevant Inspector/Runtime state.
- Save machine-readable diagnostics when possible through `window.__MUGEN_WEB_SANDBOX__`.

Preferred evidence path:

```txt
.scratch/qa/<feature-name>/
  diagnostics.json
  runtime.png
  inspector.png
  notes.md
```

## Repeatable Browser Smoke

`pnpm qa:smoke` is the baseline visual/runtime smoke gate. It makes routine browser verification repeatable instead of relying only on ad-hoc Playwright scripts.

Minimum smoke route set:

| Route | Required Evidence |
| --- | --- |
| `/?mode=match&p1=nova-boxer&p2=mira-volt&stage=rooftop-dojo` | Desktop and narrow screenshots; nonblank canvas; stage, HUD, two fighters, and debug bridge diagnostics. |
| `/?mode=studio&studio=workbench&p1=nova-boxer&p2=mira-volt&stage=rooftop-dojo` | Workbench command center screenshot; project readiness lanes; operator priority; surface jump buttons for Assets/Evidence/Build/Debug; primary actions for Playtest, MUGEN ZIP intake, trace export, and runtime compile; no horizontal overflow on desktop or 1024px tablet layout; visible stage status; command palette opens with search focus, traps keyboard focus, closes on Escape, and restores focus to the launcher. |
| `/?mode=studio&studio=build&p1=nova-boxer&p2=mira-volt&stage=rooftop-dojo` | Build Center screenshot; `compiledProject`; smoke trace artifact after export; `export-bundle/v0` ZIP with project/runtime/source-map/evidence/report files plus browser-fetchable local assets/checksums and `sourcePackages` linked/missing metadata. |
| Reopened `project.json` with missing `sourcePackages` | Source-package relink screenshot; Build Center shows ZIP/Folder relink actions before load; loading the KFM fixture ZIP through the relink button changes the package from `missing` to `linked`, removes stale missing-source warnings, and keeps required path metadata visible. |
| `/?mode=studio&studio=assets&p1=nova-boxer&p2=mira-volt&stage=rooftop-dojo` | Asset Library screenshot; `studioAssets` exposed through the QA bridge; provenance/status filters render generated assets; selected asset detail includes impact, exportability, evidence ids, and next action; playtest-entry replacement flow, source/runtime mapping, visual dependency graph, dependency drilldown, missing/partial references, and related evidence are visible. |
| `/?mode=studio&studio=stage&p1=nova-boxer&p2=mira-volt&stage=bgctrl-lab` | Native BGCtrl Lab screenshot and canvas pixel stats; `snapshot.stage.bgControllers` exposes controller groups; Stage Studio shows bounded BGCtrl rows and a nonblank canvas for visible renderer execution. |
| `/?mode=studio&studio=debug&p1=nova-boxer&p2=mira-volt&stage=rooftop-dojo` | Runtime Debug Studio screenshot; `actorRegistry` exposed through the QA bridge; actor ownership visible; URL-backed targets/effects/pause/audio lenses render; target/effect/pause lenses expose trace-world evidence when a trace artifact exists; effect world evidence can jump to the Evidence frame scrubber. |
| `/?mode=studio&studio=evidence&p1=nova-boxer&p2=mira-volt&stage=rooftop-dojo` | Evidence Browser screenshot; `studioEvidence` records for asset, compile, trace/gate, compatibility, or diagnostics plus a top actionable next step from the QA bridge. After trace export, persisted local trace evidence history must be visible and exposed through `studioEvidence.stats.persistedTraceArtifacts` / `storedTraceEvidence`; `studioEvidence.persistedTraceComparisons` must compare that stored trace to the current artifact, the UI must show Trace Comparison Review metric/gate diff rows, and `traceFrameScrubber` must expose/select frame timeline data plus a World Delta block when the selected frame has `world` evidence. |

Minimum diagnostics fields:

- `mode`
- selected P1/P2/stage ids
- renderer status
- runtime snapshot summary
- runtime roster
- atlas motion QA summary
- compiled project when Studio Build is open
- trace artifact and Studio Evidence records when exported/open
- Studio actionable status fields for selected asset/evidence: impact, next action, exportability, and blockers when present
- persisted Studio evidence counts and stored trace evidence entries after exporting a trace artifact

Default output path:

```txt
.scratch/qa/qa-smoke/
  diagnostics.json
  runtime-desktop.png
  runtime-mobile.png
  runtime-canvas.png
  studio-workbench.png
  studio-workbench-tablet.png
  studio-command-palette.png
  studio-build.png
  studio-assets.png
  studio-evidence.png
  studio-debug.png
  studio-debug-pause.png
  studio-debug-evidence-mobile.png
  trace-artifact.json
```

The command starts a local Vite server automatically. Set `QA_BASE_URL=http://host:port` to verify an already-running app instead.

Feature-specific milestone closeout can still use focused Playwright scripts when a deeper fixture or interaction is required, but `pnpm qa:smoke` is now the first reusable gate for the default Runtime and Studio Build/Evidence paths. Runtime Debug Studio smoke also validates URL-backed debug lenses, including the effects drilldown contract and the Pause / HitPause panel contract even when no live effect or active hitpause frame is captured.

## Trace Evidence Schema Notes

`RuntimeTrace` actor-frame summaries include `guardingFrames`, the number of frames in that summarized bucket where the actor was guarding. A gate can set `requiredActorFrames[].guarding = true` to require guarding frames without splitting historical state/action/physics buckets; with `minFrames`, the minimum applies to matching guarded frames. This is used by `synthetic-imported-helper-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json`, `synthetic-imported-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json`, `synthetic-imported-hitoverride-guardflag-forceair-forceguard-keepstate.json`, `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json`, `synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json`, `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json`, `synthetic-imported-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json`, `synthetic-imported-helper-projectile-hitoverride-forceair-forceguard-keepstate.json`, `synthetic-imported-projectile-hitoverride-forceair-forceguard-keepstate.json`, and `synthetic-imported-hitoverride-forceair-forceguard-keepstate.json` to prove bounded `forceguard` frame evidence while preserving older guard-route `minFrames` gates. `synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` intentionally requires reject telemetry and absence of target links/aerial/guarding actor-frame evidence because the default direct custom-state miss short-circuits before force flags are applied.

Latest required `synthetic-imported-helper-dynamic-posadd.json` checksum `97ec15d0` proves bounded helper-local dynamic `PosAdd` params can resolve `x = Parent,Life - 984` and `y = Root,StateNo - 220`, apply through the helper runtime dispatcher after a static `PosSet` seed, and emit typed `kinematic:posadd` telemetry before routing to state/action `1405` / anim `945`. The gate requires active `x` command evidence, `ChangeState`, `HitDef`, `Helper`, `PosSet`, `PosAdd`, `hitdef`, `helper`, `kinematic:posset`, `kinematic:posadd`, and helper actor-frame position telemetry `18,-17`. `pnpm qa:trace` passes 517/517 artifacts, 486 required and 31 optional. Dynamic typed lowering for every kinematic controller, helper-local dynamic typed telemetry beyond current `VelSet` / `VelAdd` / `VelMul` / `PosSet` / `PosAdd`, nested helper ancestry, helper-spawned helpers, player `Parent` controller-param redirects, recursive redirection, teams/simul/helper breadth, score movement, and full movement/helper expression parity remain blocked.

Previous required `synthetic-imported-helper-dynamic-posset.json` checksum `50596bc2` remains required for bounded helper-local dynamic `PosSet` params resolving `x = Parent,Life - 984` and `y = Root,StateNo - 220` with typed `kinematic:posset` telemetry before routing to state/action `1404` / anim `944`.

Previous required `synthetic-imported-helper-dynamic-velmul.json` checksum `08220a98` remains required for bounded helper-local dynamic `VelMul` params resolving `x = Parent,Life - 998` and `y = Root,StateNo - 202` with typed `kinematic:velmul` telemetry before routing to state/action `1403` / anim `943`.

Previous required `synthetic-imported-helper-dynamic-veladd.json` checksum `fbb8bcae` remains required for bounded helper-local dynamic `VelAdd` params resolving `x = Parent,Life - 996` and `y = Root,StateNo - 205` with typed `kinematic:veladd` telemetry before routing to state/action `1402` / anim `942`.

Previous required `synthetic-imported-helper-controller-param-parentroot.json` checksum `94919326` remains required for bounded helper-local dynamic `VelSet` params resolving `x = Parent,Life - 995` and `y = Root,StateNo - 203` with typed `kinematic:velset` telemetry before routing to state/action `1401` / anim `941`.

Previous required `synthetic-imported-dynamic-posset.json` checksum `aeb730fb` proves bounded active-state dynamic `PosSet` params can resolve `x = var(0) + 5` and `y = var(1) - 2`, apply through runtime dispatch, and emit typed `kinematic:posset` telemetry after `VarSet` seeds and a static `PosSet` seed. That checkpoint passed 512/512 artifacts, 481 required and 31 optional.

Previous required `synthetic-imported-dynamic-velmul.json` checksum `4d241401` proves bounded active-state dynamic `VelMul` params can resolve `x = var(0) * 0.5` and `y = 0 - var(1)`, apply through runtime dispatch, and emit typed `kinematic:velmul` telemetry after `VarSet` seeds and a static `VelSet` seed. That checkpoint passed 511/511 artifacts, 480 required and 31 optional.

Previous required `synthetic-imported-dynamic-veladd.json` checksum `daf99fb4` proves bounded active-state dynamic `VelAdd` params can resolve `x = var(0) + 1` and `y = var(1) - 1`, apply through runtime dispatch, and emit typed `kinematic:veladd` telemetry after `VarSet` seeds and a static `VelSet` seed. That checkpoint passed 510/510 artifacts, 479 required and 31 optional.

Previous required `synthetic-imported-const-controller-param.json` checksum `2dad3a50` proves bounded active-state `VelSet` params can evaluate `Const240p`, `Const480p`, and `Const720p` against a 640x480 player localcoord. The gate requires active `x` command evidence, state/action `200`, `ChangeState`, two `VelSet` controller events, two `kinematic:velset` operation events covering the static seed and dynamic resolved controller, and actor-frame velocity telemetry `x = 12` / `y = -6`. That checkpoint passed 509/509 artifacts, 478 required and 31 optional. Broad coordinate translation across all controller params, dynamic typed-operation lowering beyond active-state `VelSet`, renderer/screenpack viewport ownership, camera animation parity, IKEMEN `config.json` execution, full viewport/camera/screenpack split, helper/team/simul namespace breadth, score movement, and full viewport parity remain blocked.

Previous required `synthetic-imported-const-coordinate.json` checksum `ea879c1b` proves bounded imported State -1 routing through `Const240p(3) = 6`, `Const480p(6) = 6`, and `Const720p(12) = 6` for a 640x480 player localcoord. The gate requires active `x` command evidence, state/action `9304`, and final actor state `9304`, and remains required.

Previous required `synthetic-imported-config-gamespace.json` checksum `2f3c0a63` proves bounded imported State -1 routing through parsed-config-equivalent `ScreenWidth = 1280`, `ScreenHeight = 720`, `GameWidth = 2560`, and `GameHeight = 1440` at camera zoom `0.5`, with INI `[Config] GameWidth` / `GameHeight` preferred over stage localcoord. The gate requires active `x` command evidence, state/action `9303`, and final actor state `9303`. `pnpm qa:trace` now passes 507/507 artifacts, 476 required and 31 optional. Broader player-local coordinate translation, renderer/screenpack viewport ownership, camera animation parity, IKEMEN `config.json` execution, full viewport/camera/screenpack split, helper/team/simul namespace breadth, score movement, and full viewport parity remain blocked.

Previous required `synthetic-imported-screenspace.json` checksum `5330bacd` proves bounded imported State -1 routing through `ScreenWidth = 640` and `ScreenHeight = 480` while the same zoom `0.5` route validates `GameWidth = 1280` and `GameHeight = 960`. The gate requires active `x` command evidence, state/action `9302`, and final actor state `9302`, and remains required.

Previous required `synthetic-imported-gamespace.json` checksum `b6f248ab` proves bounded imported State -1 routing through `GameWidth = 640` and `GameHeight = 480` from stage localcoord plus camera zoom context and remains required.

Previous required `synthetic-imported-modifyprojectile-omitted-bounds.json` checksum `24cbb1dc` / final checksum `e94d1480` and `synthetic-imported-helper-modifyprojectile-omitted-bounds.json` checksum `9db04bbc` / final checksum `555d744b` prove bounded owner-side/helper-local `ModifyProjectile` preserve existing explicit Projectile removal bounds when later mutation omits `projedgebound`, `projstagebound`, and `projheightbound`. The gates require non-bound mutation evidence plus preserved live Projectile payload bounds. That checkpoint passed 504/504 artifacts, 473 required and 31 optional. Previous owner/helper dynamic params (`6ffbef92` / `5665a98e`, `2d88a550` / `edb6d2d2`), helper/owner dynamic bounds gates (`f582153e` / `adc63407`, `e2f7a077` / `aa78704a`), paired localcoord default-bound gates (`af7ee80e` / `46b0164c`), 240p helper/player default bounds, and helper/player explicit height/edge/stage bound gates remain required.

Previous required `synthetic-imported-projectile-bounds-remove-terminal.json` checksum `1d7479d3` / final checksum `39b81931` proves bounded player-owned Projectile bounds-removal terminal playback and remains required.

Previous required `synthetic-imported-helper-projectile-cancel-remove-fallback-terminal.json` checksum `cf33c924` / final checksum `bd3a1279` remains required as bounded helper-parented/root-owned cancel fallback evidence.

Previous required `synthetic-imported-projectile-cancel-remove-fallback-terminal.json` checksum `4966ed30` / final checksum `98170d08` proves bounded player-owned Projectile cancel-removal fallback playback from omitted `projcancelanim` to authored `projremanim = 921` and remains required.

Previous required `synthetic-imported-projectile-remove-hit-fallback-terminal.json` checksum `3bbdfbfc` / final checksum `76ca3f77` proves bounded player-owned Projectile timeout-removal fallback playback from omitted `projremanim` to authored `projhitanim = 920` and remains required.

Previous required `synthetic-imported-projectile-remove-terminal.json` checksum `8a65629c` / final checksum `7ba3479b` proves bounded player-owned Projectile timeout-removal terminal playback from authored `projremanim = 919` and remains required.

Previous required `synthetic-imported-helper-projectile-guard-terminal.json` checksum `4e6644f4` / final checksum `ef2e177d` proves bounded helper-parented/root-owned Projectile guarded-contact terminal playback. The gate spawns helper `p1-helper-0`, then root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, id `8922`, and authored `projhitanim = 1109`; it requires typed helper/projectile operation evidence, helper state evidence `1314 -> 1315`, Projectile spawn/remove lifecycle evidence, owner and helper target-link evidence, guard event/reason evidence, event text `hit removal anim 1109`, projectile actor-frame evidence for terminal anim `1109`, and terminal payload evidence with `hasHit = true`, `removalReason = hit`, `terminalReason = hit`, and terminal duration at least `2`. That checkpoint passed 481/481 artifacts, 451 required and 30 optional. Exact terminal timing, exact sprite/layer/palette parity, helper-owned custom-state terminal parity, team/simul breadth, score movement, and full helper Projectile guard round-flow parity remain blocked.

Previous required `synthetic-imported-projectile-guard-terminal.json` checksum `26f1e7f9` / final checksum `f9df24d0` proves bounded player-owned Projectile guarded-contact terminal playback and remains required. That checkpoint passed 480/480 artifacts, 450 required and 30 optional.

Previous required `synthetic-imported-projectile-guard-kill.json` checksum `905eb8e3` / final checksum `c6cc7787` proves bounded player-owned Projectile `guard.kill = 0` nonlethal guard-chip clamp behavior. The gate executes imported Projectile id `77` with `damage = 31,2000` and explicit `guard.kill = 0`; it requires typed projectile operation evidence, Projectile spawn/remove lifecycle evidence, target-link evidence, guard event/reason evidence, no KO round frame, and final P2 life `1`. That checkpoint passed 479/479 artifacts, 449 required and 30 optional. Exact KO slowdown/lifebar/guard-finish timing, exact no-KO guard recovery timing, team/simul guard KO/no-KO breadth, score movement, and full Projectile guard round-flow parity remain blocked.

Previous required `synthetic-imported-helper-projectile-guard-kill.json` checksum `2c09049f` / final checksum `653039f6` proves bounded helper-parented/root-owned Projectile `guard.kill = 0` nonlethal guard-chip clamp behavior. The gate spawns helper `p1-helper-0`, then root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, `damage = 18,2000`, and explicit `guard.kill = 0`; it requires typed helper/projectile operation evidence, helper/projectile spawn+active evidence, owner/helper target-link evidence, guard event/reason evidence, helper branch state `1313`, no KO round frame, and final P2 life `1`. That checkpoint passed 478/478 artifacts, 448 required and 30 optional.

Previous required `synthetic-imported-helper-projectile-guard-ko.json` checksum `49c99564` / final checksum `560dc839` proves bounded helper-parented/root-owned Projectile default lethal guard-chip KO behavior. The gate spawns helper `p1-helper-0`, then root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, `damage = 18,2000`, and default `guard.kill`; it requires typed helper/projectile operation evidence, helper/projectile spawn evidence, owner/helper target-link evidence, guard event/reason evidence, round KO winner/message evidence, and final P2 life `0`. That checkpoint passed 477/477 artifacts, 447 required and 30 optional.

Previous required `synthetic-imported-projectile-guard-ko.json` checksum `2285474a` / final checksum `c968c723` proves bounded player-owned Projectile default lethal guard-chip KO behavior. The gate executes an imported Projectile with `damage = 31,2000` and default `guard.kill`, requires typed `projectile` operation evidence, Projectile lifecycle and target-link evidence, guard event/reason evidence, round KO winner/message evidence, and final P2 life `0`. That checkpoint passed 476/476 artifacts, 446 required and 30 optional.

Previous required `synthetic-imported-hitdef-guard-ko.json` checksum `b7db75f4` / final checksum `0f9afa50` proves bounded default lethal direct guard-chip KO behavior. The gate executes a guarded imported direct `HitDef` with `guard.damage = 2000` and default `guard.kill`, requires typed `hitdef` operation evidence, guard event/reason evidence, round KO winner/message evidence, and final P2 life `0`. That checkpoint passed 475/475 artifacts, 445 required and 30 optional.

Previous required `synthetic-imported-guarddist-reversal-no-contact.json` checksum `ca20c823` / final checksum `2bc9b86d` proves bounded negative `guard.dist` / `ReversalDef` priority. The gate executes a guardable imported direct `HitDef` with `guard.dist = 96` in the near-but-not-contacting guard-distance stage while P2 has active `ReversalDef p1stateno = 777` / `p2stateno = 888` and an explicit `InGuardDist` guard-start route. It requires typed `hitdef` and `reversaldef` operation evidence, P2 state-0 `ReversalDef` controller events, guard-start `ChangeState` evidence, whiff combat reason, final P2 state/action `130`, and forbids reversal/get-hit/guard-hit states `777`, `888`, `5000`, `150`, and `151`. That checkpoint passed 474/474 artifacts, 444 required and 30 optional. Exact guard-distance boxes, positive proximity-only `guard.dist` ReversalDef contact, exact guard-start timing, crouch/air breadth beyond existing gates, custom-state breadth beyond direct routes, projectile reflection/removal semantics after reversal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, and full ReversalDef/guard parity remain blocked.

Previous required `synthetic-imported-air-guard-reversal.json` checksum `966b17b8` / final checksum `2fa19142` proves bounded air guard-input direct `ReversalDef` priority and remains required.

Previous required `synthetic-imported-crouch-guard-reversal.json` checksum `405f475e` / final checksum `d1f39c08` proves bounded crouch guard-input direct `ReversalDef` priority and remains required.

Previous required `synthetic-imported-guard-reversal.json` checksum `6f8df3a4` / final checksum `e0771a15` proves bounded held-back stand guard-input direct `ReversalDef` priority and remains required.

Previous required `synthetic-imported-custom-state-reversal.json` checksum `18065db0` / final checksum `ac8d0073` proves bounded direct custom-state `ReversalDef` priority. The gate executes imported direct `HitDef p2stateno = 889` / `p2getp1state = 1` against a defender with active `ReversalDef p1stateno = 777` / `p2stateno = 888`, stops at the reversal result, requires typed `hitdef` and `reversaldef` operation evidence, emits reversal event/combat reason evidence, forbids the HitDef owner-backed custom state `889`, default get-hit `5000`, and guard states `150` / `151`, and ends P1 in defender-owned custom state/action `888` (`customOwnerId = p2`) while P2 enters state/action `777`, both at life `1000`. That checkpoint passed 469/469 artifacts, 439 required and 30 optional. Custom-state breadth beyond this direct route, projectile reflection/removal semantics after reversal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, and full ReversalDef parity remain blocked.

Previous required `synthetic-imported-helper-projectile-reversal.json` checksum `a1d82380` / final checksum `ca66a49a` proves bounded helper-parented/root-owned Projectile `ReversalDef` priority. The gate creates a visual Helper that spawns Projectile id `8878` with `ownerId = p1`, `rootId = p1`, and `parentId = p1-helper-0`, stops at the initial reversal contact, requires typed `helper`, `projectile`, and `reversaldef` operation evidence, emits reversal event/combat reason evidence, keeps helper target memory empty and the Projectile payload unconsumed at that sampled frame, and ends P2 in state/action `777` while P1 enters owner-backed state/action `888` with both actors at life `1000`. That checkpoint passed 468/468 artifacts, 438 required and 30 optional. Projectile reflection/removal semantics after reversal, helper-owned custom-state tables, broader guard/custom-state counter breadth, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, and full ReversalDef parity remain blocked.

Previous required `synthetic-imported-projectile-reversal.json` checksum `5c4ddf48` / final checksum `bb0bbb99` proves bounded player-owned Projectile `ReversalDef` priority. The gate executes imported Projectile id `77`, stops at the initial reversal contact, requires typed `projectile` and `reversaldef` operation evidence, emits reversal event/combat reason evidence, keeps the Projectile payload unconsumed at that sampled frame, and ends P2 in state/action `777` while P1 enters owner-backed state/action `888` with both actors at life `1000`. That checkpoint passed 467/467 artifacts, 437 required and 30 optional. Projectile reflection/removal semantics after reversal, guard/custom-state counter breadth, exact attr grammar, hitpause/tick order, and full ReversalDef parity remain blocked.

Previous required `synthetic-imported-superpause-pausebg.json` checksum `49bcfe16` / final checksum `397a8fae` proves bounded imported `SuperPause pausebg = 0` metadata. The gate executes `pausebg = 0`, requires match-pause/freeze evidence, and observes `pauseBg = false`. That checkpoint passed 465/465 artifacts, 435 required and 30 optional. Actual renderer/background update parity, exact stage/BGCtrl pause timing, renderer visual suppression/playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, `unhittable`, helper/team/redirect ownership, and full super presentation parity remain blocked.

Previous required `synthetic-imported-superpause-anim-disabled.json` checksum `fc7a2ca4` / final checksum `5be3ca6c` proves bounded imported `SuperPause anim = -1` metadata suppression. The gate executes `anim = -1`, requires match-pause/freeze evidence, and observes `superAnimAbsent = true`.

Previous required `synthetic-imported-superpause-default-anim.json` checksum `318c5e9f` / final checksum `747e7619` proves bounded omitted `SuperPause anim` default metadata. The gate omits `anim`, requires match-pause/freeze evidence, and observes `superAnim` metadata `raw = 30`, `source = fightfx`, `actionNo = 30`, and `offset = 0,0`.

Previous required `synthetic-imported-superpause-dynamic-params.json` checksum `052bb481` / final checksum `1847a3f3` proves bounded dynamic `SuperPause time/movetime/darken/poweradd` fallback. The gate seeds `var(2)=9`, `var(3)=2`, `var(4)=0`, and `var(5)=75`, runs `SuperPause time = var(2), movetime = var(3), darken = var(4), poweradd = var(5)`, requires match-pause/freeze and P1 source-movetime evidence, and observes max remaining `9`, max move time `2`, `darken = false`, and final P1 power `75`. Current checksum includes default `superAnim` metadata from the default-anim cut. Typed-operation lowering for dynamic pause params, bottom-to-zero exactness, Pause-over-Pause/SuperPause preemption/delay, `pausebg`, `unhittable`, helper/team/redirect ownership, and full pause VM parity remain blocked.

Previous required `synthetic-imported-superpause-p2defmul.json` checksum `ec1ba95e` / final checksum `6d009665` proves bounded positive `SuperPause p2defmul` target damage scaling. The gate starts SuperPause after direct target memory exists, runs `SuperPause p2defmul = 2`, requires match-pause/freeze and P1 source-movetime evidence, and observes final P2 life `953` after the initial hit leaves P2 at `963` and later non-absolute `TargetLifeAdd -20` applies as `-10` through temporary damage multiplier `0.5`. Current checksum includes default `superAnim` metadata from the default-anim cut. `p2defmul = 0` / `Super.TargetDefenceMul`, exact recovery lifetime, stacking, helper/redirect/multi-target ownership, and full super damage-scaling parity remain blocked.

Previous required `synthetic-imported-superpause-sound.json` checksum `de3ac4b2` / final checksum `6e227fe6` proves bounded `SuperPause sound` dynamic fallback. The gate seeds `var(0)=10` and `var(1)=0`, runs `SuperPause sound = Svar(0),var(1)`, requires match-pause/freeze evidence, and emits attacker-side `PlaySnd` telemetry for group `10`, index `0`, raw `Svar(0),var(1)` while typed `audio:*` operation evidence stays absent. Current checksum includes default `superAnim` metadata from the default-anim cut. Exact common/player SND archive lookup/channel priority/timing/mixing, super-background audio, helper/redirect ownership, and full audio parity remain blocked.

Previous required `synthetic-imported-hitdef-dynamic-guardsound.json` checksum `cb061b1c` / final checksum `8d25e54e` proves bounded direct `HitDef guardsound` dynamic fallback. The gate seeds `var(0)=6` and `var(1)=4`, runs guarded `HitDef guardsound = Fvar(0),var(1)`, then requires attacker-side guard contact `PlaySnd` telemetry for group `6`, index `4`, raw `Fvar(0),var(1)`, `contactKind = guard`, and `soundPrefix = kfm` while typed `audio:*` operation evidence stays absent. That checkpoint passed 457/457 artifacts, 427 required and 30 optional. Exact SND playback/archive lookup/channel priority/timing/mixing, helper/redirect ownership, and full audio parity remain blocked.

Previous required `synthetic-imported-sound-dynamic-pan.json` checksum `24c0cce2` / final checksum `dea16ed4` proves bounded active-state dynamic audio numeric fallback. The gate seeds `var(0)=-24`, `var(1)=2`, and `var(2)=64`, runs `PlaySnd value = S5,2, channel = var(1), pan = var(0)`, `SndPan channel = var(1), abspan = var(2)`, and `StopSnd channel = var(1)`, then requires `PlaySnd`, `SndPan`, and `StopSnd` sound-event telemetry while typed `audio:*` operation evidence stays absent. That checkpoint passed 454/454 artifacts, 424 required and 30 optional. Typed-operation lowering for dynamic audio params, exact Web Audio panning, channel priority/timing/mixing, helper/redirect ownership, and full audio parity remain blocked.

Latest required `synthetic-imported-helper-projtime-same-id-last-contact.json` checksum `4e74aec3` proves bounded helper-local same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` guard-then-hit last-contact-kind arbitration. The gate spawns two root-owned helper-parented Projectiles with id `8918`, records a guarded contact first and a hit later, routes helper `1200 -> 1306 -> 1307` through `ProjHitTime(8918) >= 1`, `ProjHitTime(0) >= 1`, `ProjContactTime(8918) >= 1`, `ProjContactTime(0) >= 1`, `ProjGuardedTime(8918) < 0`, and `ProjGuardedTime(0) < 0`, and forbids helper trap state `1308` when stale same-id guarded time would survive. It requires two Projectile controller/op executions, helper/projectile lifecycle rows, owner/root `p1`, parent `p1-helper-0`, owner target-link id `8918`, guard package `S6,34` / `F7038` / `sparkxy = 35,-77`, and hit package `S5,35` / `F7038` / `sparkxy = 36,-78`. `pnpm qa:trace` now passes 439/439 artifacts, 409 required and 30 optional. Exact `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` tick order/lifetime, helper custom-state breadth beyond these routes, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, and full Projectile parity remain blocked.

Previous required `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b` proves bounded helper Projectile same-ID hit-then-guard `Proj*Time` last-contact-kind arbitration and remains required.

Previous required `synthetic-imported-projectile-projtime-same-id-hit-then-guard.json` checksum `d49ee334` proves bounded player Projectile same-ID hit-then-guard `Proj*Time` last-contact-kind arbitration and remains required.

Previous required `synthetic-imported-projectile-projtime-same-id-last-contact.json` checksum `fb4c2450` proves bounded player Projectile same-ID guard-then-hit `Proj*Time` last-contact-kind arbitration and remains required.

Previous required `synthetic-imported-projectile-projhittime-multi-id.json` checksum `5d897825`, `synthetic-imported-projectile-projcontacttime-multi-id.json` checksum `d9b3cecf`, and `synthetic-imported-projectile-projguardedtime-multi-id.json` checksum `e52d0d01` prove bounded player Projectile `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` multi-id arbitration and remain required.

Previous required `synthetic-imported-projectile-projhit-multi-id.json` checksum `ab0f3fb3` and `synthetic-imported-projectile-projguarded-multi-id.json` checksum `023921e3` prove bounded player Projectile `ProjHit` / `ProjGuarded` multi-id arbitration and remain required.

Previous required `synthetic-imported-projectile-projcontact-multi-id.json` checksum `e790ec3e` proves bounded player Projectile `ProjContact` multi-id arbitration and remains required.

Previous required `synthetic-imported-projectile-projcontact-suffix-any.json` checksum `2fb80418` proves bounded player Projectile omitted-ID / ID `0` any-projectile legacy `ProjContact` suffix syntax and remains required.

Previous required `synthetic-imported-projectile-projhit-suffix-any.json` checksum `35ffd57d`, `synthetic-imported-projectile-projguarded-suffix-any.json` checksum `4000bc4f`, `synthetic-imported-projectile-projhit-suffix.json` checksum `dd3db5ee`, and `synthetic-imported-projectile-projguarded-suffix.json` checksum `80bbe439` prove bounded player Projectile hit/guard legacy suffix syntax and remain required.

Previous required `synthetic-imported-projectile-projcontactpersist.json` checksum `8e678b1b` proves bounded player Projectile `ProjContact` state-transition behavior and remains required.

Previous required `synthetic-imported-helper-projcontactpersist.json` checksum `65639428` proves bounded helper Projectile `ProjContact` state-transition behavior and remains required.

Previous required `synthetic-imported-helper-movereversedpersist.json` checksum `ef8ffdf5` proves bounded helper-owned reversed `StateDef movehitpersist` behavior. That checkpoint passed 421/421 artifacts, 391 required and 30 optional, and remains required.

Previous required `synthetic-imported-helper-moveguardedpersist.json` checksum `d5ce7897` proves bounded helper-owned guarded `StateDef movehitpersist` behavior. That checkpoint passed 420/420 artifacts, 390 required and 30 optional, and remains required. Previous helper hit-route `synthetic-imported-helper-movehitpersist.json` checksum `2354ef95` remains required too.

Previous required `synthetic-imported-helper-hitcountpersist.json` checksum `fc9588d8` proves bounded helper-owned `StateDef hitcountpersist` behavior. That checkpoint passed 418/418 artifacts, 388 required and 30 optional, and remains required.

Previous required `synthetic-imported-helper-hitdefpersist.json` checksum `9d5c64c4` proves bounded helper-owned `StateDef hitdefpersist` behavior. That checkpoint passed 417/417 artifacts, 387 required and 30 optional, and remains required. Previous direct `synthetic-imported-hitdefpersist.json` checksum `4bb3e86c` remains required. Multi-HitDef stacking, player/helper Projectile hitdefpersist breadth, custom-state hitdefpersist breadth, ReversalDef interactions, exact combo UI accumulation, multi-hit/multi-target/team counting, chain-hit eligibility arbitration, exact hitpause/target lifetime, and full HitDef lifetime parity remain blocked.

Previous required `synthetic-imported-movehitpersist.json` checksum `5c1ef583` proves bounded direct `StateDef movehitpersist` behavior. That checkpoint passed 415/415 artifacts, 385 required and 30 optional, and remains required.

Previous required `synthetic-imported-hitcountpersist.json` checksum `6f032088` proves bounded direct `StateDef hitcountpersist` behavior. That checkpoint passed 414/414 artifacts, 384 required and 30 optional, and remains required.

Previous required `synthetic-imported-projectile-hitcount.json` checksum `97a1b671` and `synthetic-imported-helper-projectile-hitcount.json` checksum `19d1c22c` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit attacker-side `HitCount` / `UniqHitCount` contact memory. These gates route P1 to state `341` through owner `HitCount >= 1 && UniqHitCount >= 1`, route helper `1257 -> 1258` through helper-local `HitCount >= 1 && UniqHitCount >= 1`, and require owner/helper target links plus projectile/helper lifecycle evidence. That checkpoint passed 413/413 artifacts, 383 required and 30 optional, and remains required.

Previous required `synthetic-imported-projectile-gethitvar-hitcount.json` trace checksum `fa445b05` / final checksum `0c2197f7` and `synthetic-imported-helper-projectile-gethitvar-hitcount.json` trace checksum `ded0c9b3` / final checksum `bba71972` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit `GetHitVar(hitcount)` metadata. These gates route P2 through defender-owned Common1-style `5000 -> 339/340`, require target/lifecycle evidence, preserve HitDef `numhits = 3/4` separately from Projectile lifetime `projhits = 1`, and prove `!GetHitVar(guarded)`. That checkpoint passed 411/411 artifacts, 381 required and 30 optional, and remains required. Exact combo accumulation, chain-hit eligibility arbitration, multi-hit timing, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, and custom-state inheritance remain blocked.

Previous required `synthetic-imported-projectile-gethitvar-hitid-chainid.json` trace checksum `80392a85` / final checksum `514a6803` and `synthetic-imported-helper-projectile-gethitvar-hitid-chainid.json` trace checksum `24df4416` / final checksum `f84c36ad` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit `GetHitVar(hitid/chainid)` metadata. Those gates remain required.

Previous required `synthetic-imported-projectile-gethitvar-hit-metadata.json` trace checksum `6b7ad6e5` / final checksum `36365083` and `synthetic-imported-helper-projectile-gethitvar-hit-metadata.json` trace checksum `4a65158a` / final checksum `dfbc9fe9` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit `GetHitVar(damage/hittime/xvel/yvel)` metadata. Those gates remain required.

Previous required `synthetic-imported-projectile-guard-slide-stop.json` trace checksum `965c2d12` / final checksum `0973a73c` and `synthetic-imported-helper-projectile-guard-slide-stop.json` trace checksum `6c42a378` / final checksum `df8b7a42` extend the existing direct `synthetic-imported-default-guard-slide-stop.json` proof to player-owned and helper-parented Projectile stand-guard routes. Those gates remain required; exact guard control tick order, including HitOver vs CtrlSet parity, remains blocked.

Previous required `synthetic-imported-guard-velocity-default.json` checksum `e6bd9b40`, `synthetic-imported-projectile-guard-velocity-default.json` checksum `b72451a4`, and `synthetic-imported-helper-projectile-guard-velocity-default.json` checksum `2067ba99` prove bounded official default `guard.velocity` derivation for direct HitDef, player-owned Projectile, and helper-parented Projectile stand-guard routes. These gates remain required alongside the guard timing gates.

Previous required `synthetic-imported-air-hit-cornerpush-default.json` checksum `73129a04`, `synthetic-imported-projectile-air-hit-cornerpush-default.json` checksum `9bfae4d6`, and `synthetic-imported-helper-projectile-air-hit-cornerpush-default.json` checksum `9c81047d` prove bounded default `air.cornerpush.veloff` derivation for direct HitDef, player-owned Projectile, and helper-parented Projectile air-hit routes. These gates require omitted corner-push params, stage-edge airborne hit contact, Common1-style state `5020` hit frames, attacker/owner X velocity evidence from derived corner pushback, and required coverage-summary presence. The helper Projectile route also requires owner/helper target links and lifecycle payload evidence. That checkpoint passed 388/388 artifacts, 358 required and 30 optional, and remains required.

Previous required `synthetic-imported-air-guard-cornerpush-default.json` checksum `c32781ad`, `synthetic-imported-projectile-air-guard-cornerpush-default.json` checksum `90f5e385`, and `synthetic-imported-helper-projectile-air-guard-cornerpush-default.json` checksum `0271a2b9` prove bounded default `airguard.cornerpush.veloff` support. Previous required `synthetic-imported-air-guard-cornerpush.json` checksum `9fdb8a81`, `synthetic-imported-projectile-air-guard-cornerpush.json` checksum `15f26082`, and `synthetic-imported-helper-projectile-air-guard-cornerpush.json` checksum `35d7148b` prove bounded explicit `airguard.cornerpush.veloff` support. These remain required alongside the default air-hit derivation gates.

Previous required `synthetic-imported-air-guard-velocity-default.json` checksum `b1710269`, `synthetic-imported-projectile-air-guard-velocity-default.json` checksum `bd1a774e`, and `synthetic-imported-helper-projectile-air-guard-velocity-default.json` checksum `3351e770` prove bounded default `airguard.velocity` derivation for direct HitDef, player-owned Projectile, and helper-parented Projectile guard routes. These remain required alongside the corner-push gates.

Previous required `synthetic-imported-air-guard-velocity.json` checksum `5ebc1e7b`, `synthetic-imported-projectile-air-guard-velocity.json` checksum `0094c369`, and `synthetic-imported-helper-projectile-air-guard-velocity.json` checksum `b547dfb3` prove bounded explicit `airguard.velocity` support. These remain required alongside the default-derivation and corner-push gates.

Previous required `synthetic-imported-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `35fa8224`, `synthetic-imported-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `1fd6c321`, and `synthetic-imported-helper-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `7efa40bb` prove bounded HitOverride guardflag filtering before selected-slot force flag application. These gates require three active `HitOverride` controllers, skip the `guardflag.not = HA` and `guardflag = A` slots for incoming `guardflag = H`, select slot `5 -> 779`, keep P2 in state/action `0` because `keepstate = 1`, require aerial/guarding actor-frame evidence, and keep default get-hit/guard states forbidden. Player/helper Projectile routes also require projectile lifecycle evidence; helper route also requires owner/helper target links and helper branch suppression. That trace aggregate was 373/373 artifacts, 343 required and 30 optional.

Previous required `synthetic-imported-hitoverride-missonoverride-default-guardflag-filter.json` checksum `05725ecb`, `synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter.json` checksum `c1402d31`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter.json` checksum `889d77c1` prove bounded default `missonoverride = -1` custom-state HitOverride guardflag filtering. The direct-HitDef gate requires the default custom-state miss path to reject before target memory, override/custom-state/default-get-hit/guard states, and target links. The player/helper Projectile gates require three active `HitOverride` controllers, skip the `guardflag.not = HA` and `guardflag = A` slots for incoming `guardflag = H`, select slot `5 -> 779`, suppress projectile custom-state `889`, and keep default get-hit/guard states forbidden. That trace aggregate was 370/370 artifacts, 340 required and 30 optional.

Previous required `synthetic-imported-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `058b335f`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `af29f125`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `9edbf3d0` prove bounded explicit `missonoverride = 0` custom-state HitOverride guardflag filtering. These gates require three active `HitOverride` controllers, skip the `guardflag.not = HA` and `guardflag = A` slots for incoming `guardflag = H`, select slot `5 -> 779`, suppress direct/projectile custom states, and keep default get-hit/guard states forbidden. That trace aggregate was 367/367 artifacts, 337 required and 30 optional.

For Runtime Mode, `pnpm qa:smoke` warms the Three.js renderer, resets the round, drives a native close-contact attack route, and requires `renderer.hitSparks.active > 0`, `renderer.hitSparks.sources.player > 0`, `renderer.hitSparks.resolvedSprites > 0`, and resolved spark frame/sprite/axis diagnostics on both desktop and mobile captures. This proves the bounded `HitSparkRenderer` path can resolve an `S`-prefixed player AIR spark frame into a sprite texture from snapshot `HitSpark` events and expose the selected AIR frame, SFF sprite dimensions/axis, and local sprite position through the QA bridge; focused loader/runtime/renderer tests prove optional `data/fight.def` / FightFX AIR/SFF discovery, decoded system-SFF provider registration, unprefixed common/default and `F` FightFX refs, bounded global sprite lookup frames, and preservation of runtime-provided package-backed `assetFrame` metadata from `hitSparkLibraries`. It does not prove advanced binding, layering, timing, scale, palette, motif/screenpack ownership, or full MUGEN/IKEMEN spark parity.

## Compatibility Acceptance Levels

Use these labels consistently:

| Level | Meaning |
| --- | --- |
| Parsed | The file or syntax is read into a model. |
| Decoded | Binary/image/audio payload is usable by the browser. |
| Recognized | The engine knows the feature name and can report it. |
| Compiled | The feature has a typed runtime representation. |
| Executed Partial | The runtime performs a documented subset. |
| Executed Parity | Behavior is expected to match MUGEN/IKEMEN for the tested cases. |
| Unsupported | The feature is known but not implemented. |
| Unknown | The feature was encountered but not classified yet. |

Avoid saying "supported" without naming the level.

## Current Runtime Fixture Gate Notes

- Required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority.json` checksum `9a5a149f` plus `synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority.json` checksum `96b6b7de` are the latest bounded Projectile explicit `missonoverride = 0` custom-state `HitOverride` slot-priority oracles. They must require P2 matching slots `5 -> 779` and `2 -> 778`, player Projectile id `77` and helper Projectile id `8878` with `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`, executed `ChangeState` / `Projectile` / at least two `HitOverride` evidence, helper route also `Helper`, typed operation evidence, active `x`, override event/reason containing `HitOverride slot 2`, target links `p1 -> p2 / 77`, `p1 -> p2 / 8878`, and `p1-helper-0 -> p2 / 8878`, helper/projectile lifecycle-payload evidence, final P2 state/action `778` / life `1000` / moveType `I`, and forbidden states `779`, `889`, `5000`, `150`, `151`, plus helper branch `1288`. This proves bounded player-owned/helper-parented Projectile explicit custom-state lowest-slot priority only; custom-state guardflag inheritance/timing, exact guard KO/no-KO round flow, and full Projectile HitOverride/custom-state parity remain blocked.
- Previous required `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `fb964bfb` remains the bounded helper-parented Projectile default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` oracle. It must require a Helper-spawned Projectile id `8877` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and omitted `missonoverride`, P2 active slot `3 -> 780` with `forceair = 1`, `forceguard = 1`, and `keepstate = 1`, executed `ChangeState` / `Helper` / `Projectile` / `HitOverride` evidence, typed `helper` / `projectile` / `hitoverride` operation evidence, active `x` command evidence, target links `p1 -> p2 / 8877` and `p1-helper-0 -> p2 / 8877`, helper/projectile lifecycle-payload evidence, override event/reason evidence containing `HitOverride slot 3`, actor-frame evidence with `stateType = A`, `physics = A`, and `guardingFrames >= 1`, final P2 state/action `0` / life `1000` / moveType `I`, and forbidden states `780`, `889`, `1286`, `5000`, `150`, and `151`. This proves bounded default helper-parented Projectile custom-state forceair/forceguard/keepstate behavior only; custom-state guardflag inheritance/timing, exact guard KO/no-KO round flow, and full helper Projectile HitOverride/custom-state parity remain blocked.
- Previous required `synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `4ce42cf3` remains the bounded player-owned Projectile default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` oracle.
- Previous required `synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `20e40425` remains the bounded direct-HitDef default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` miss oracle. It must require a direct `HitDef` with `p2stateno = 888`, `p2getp1state = 1`, and omitted `missonoverride`, P2 active slot `3 -> 780` with `forceair = 1`, `forceguard = 1`, and `keepstate = 1`, executed `ChangeState` / `HitDef` / `HitOverride` evidence, typed `hitdef` / `hitoverride` operation evidence, active `x` command evidence, reject event/reason evidence containing the custom-state HitDef path, no target-link evidence, final P2 state/action `0` / life `1000` / ctrl true / moveType `I`, and forbidden states `780`, `888`, `5000`, `150`, and `151`. This proves bounded default direct custom-state miss behavior only; custom-state guardflag inheritance/timing, exact guard KO/no-KO round flow, and full HitOverride/custom-state parity remain blocked.
- Previous required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `e23a33af` remains the bounded helper-parented Projectile explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` oracle. It must require a Helper-spawned Projectile id `8876` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`, P2 active slot `3 -> 780` with `forceair = 1`, `forceguard = 1`, and `keepstate = 1`, executed `ChangeState` / `Helper` / `Projectile` / `HitOverride` evidence, typed `helper` / `projectile` / `hitoverride` operation evidence, active `x` command evidence, target links `p1 -> p2 / 8876` and `p1-helper-0 -> p2 / 8876`, helper/projectile lifecycle-payload evidence, override event/reason evidence containing `HitOverride slot 3`, actor-frame evidence with `stateType = A`, `physics = A`, and `guardingFrames >= 1`, final P2 state/action `0` / life `1000` / moveType `I`, and forbidden states `780`, `889`, `1284`, `5000`, `150`, and `151`. This proves bounded explicit `missonoverride = 0` helper-parented Projectile custom-state forceair/forceguard/keepstate behavior only; custom-state guardflag inheritance/timing, exact guard KO/no-KO round flow, and full HitOverride/custom-state parity remain blocked.
- Previous required `synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `15bc955b` remains the bounded player-owned Projectile explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` oracle. It must require a Projectile with `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`, P2 active slot `3 -> 780`, target link `p1 -> p2 / 77`, projectile lifecycle/payload evidence, actor-frame evidence with `stateType = A`, `physics = A`, and `guardingFrames >= 1`, final P2 state/action `0` / life `1000` / moveType `I`, and forbidden states `780`, `889`, `5000`, `150`, and `151`.

- Required `synthetic-imported-helper-projectile-hitoverride-guardflag-filter.json` checksum `41a87267` is the latest bounded helper-parented Projectile `HitOverride guardflag` / `guardflag.not` oracle. It must require P2 attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`, a visual Helper-spawned Projectile id `8874` carrying `guardflag = H`, `parentId = p1-helper-0`, `p2stateno = 889`, and `p2getp1state = 0`, executed `ChangeState` / `Helper` / `Projectile` / at least three `HitOverride` controller evidence, typed `helper` / `projectile` plus at least three `hitoverride` operation evidence, active `x` command evidence, helper/projectile lifecycle and payload evidence, target links `p1 -> p2 / 8874` and `p1-helper-0 -> p2 / 8874`, override event/reason evidence containing `HitOverride slot 5`, final P2 state `779` / anim `779` / life `1000` / moveType `I`, and forbidden states `776`, `778`, `889`, `1280`, `5000`, `150`, and `151`. This proves bounded helper-parented Projectile guardflag filtering before slot priority only; custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, and full helper Projectile/HitOverride parity remain blocked.
- Previous required `synthetic-imported-projectile-hitoverride-guardflag-filter.json` checksum `a51e82ec` remains the bounded player-owned Projectile `HitOverride guardflag` / `guardflag.not` oracle. It must require P2 attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`, a player-owned Projectile id `77` carrying `guardflag = H` and `p2stateno = 889`, executed `ChangeState` / `Projectile` / at least three `HitOverride` controller evidence, typed `projectile` plus at least three `hitoverride` operation evidence, active `x` command evidence, projectile lifecycle/payload evidence, target link `p1 -> p2 / 77`, override event/reason evidence containing `HitOverride slot 5`, final P2 state `779` / anim `779` / life `1000` / moveType `I`, and forbidden states `776`, `778`, `889`, `5000`, `150`, and `151`. This proves bounded player-owned Projectile guardflag filtering before slot priority only; custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, and full Projectile/HitOverride parity remain blocked.
- Previous required `synthetic-imported-hitoverride-guardflag-filter.json` checksum `b88a2da3` remains the bounded direct-HitDef `HitOverride guardflag` / `guardflag.not` oracle. It must require P2 attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`, a direct `HitDef attr = S,NA` with `guardflag = H`, executed `ChangeState` / `HitDef` / at least three `HitOverride` controller evidence, typed `hitdef` plus at least three `hitoverride` operation evidence, active `x` command evidence, target link `p1 -> p2 / 77`, override event/reason evidence containing `HitOverride slot 5`, final P2 state `779` / anim `779` / life `1000` / moveType `I`, and forbidden states `776`, `778`, `5000`, `150`, and `151`. This proves bounded direct-HitDef guardflag filtering before slot priority only; custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, and full HitOverride parity remain blocked.
- Required `synthetic-imported-hitoverride-missonoverride-zero-slot-priority.json` checksum `92fefd6a` is the previous bounded direct-HitDef `missonoverride = 0` custom-state HitOverride slot-priority oracle. It must require P2 matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` installed in high-to-low controller order, a direct `HitDef` with `p2stateno = 888`, `p2getp1state = 1`, and explicit `missonoverride = 0`, executed `ChangeState` / `HitDef` / at least two `HitOverride` controller evidence, typed `hitdef` plus at least two `hitoverride` operation evidence, active `x` command evidence, target link `p1 -> p2 / 77`, override event/reason evidence containing `HitOverride slot 2`, final P2 state `778` / anim `778` / life `1000` / moveType `I`, and forbidden states `779`, `888`, `5000`, `150`, and `151`. This proves bounded direct-HitDef `missonoverride = 0` lowest matching active HitOverride slot priority only; the player/helper Projectile explicit custom-state counterparts are covered by the newer slot-priority gates, while custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, and full HitOverride parity remain blocked.
- Required `synthetic-imported-helper-projectile-hitoverride-slot-priority.json` checksum `1d058518` remains the bounded helper-parented Projectile HitOverride slot-priority oracle. It must require P2 matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` installed in high-to-low controller order, a visual Helper-spawned Projectile id `8873` with `parentId = p1-helper-0` and `p2stateno = 889`, executed `ChangeState` / `Helper` / `Projectile` / at least two `HitOverride` controller evidence, typed `helper` / `projectile` plus at least two `hitoverride` operation evidence, active `x` command evidence, helper/projectile lifecycle and payload evidence, target links `p1 -> p2 / 8873` and `p1-helper-0 -> p2 / 8873`, override event/reason evidence containing `HitOverride slot 2`, final P2 state `778` / anim `778` / life `1000` / moveType `I`, and forbidden states `779`, `889`, `1278`, `5000`, `150`, and `151`. This proves bounded helper-parented Projectile lowest matching active HitOverride slot priority only; explicit `missonoverride = 0` custom-state slot priority is covered by the newer helper gate, while guardflag edge timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, and full Projectile/HitOverride parity remain blocked.
- Previous required `synthetic-imported-projectile-hitoverride-slot-priority.json` checksum `378d9ce8` remains the bounded player-owned Projectile HitOverride slot-priority oracle. It must require P2 matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` installed in high-to-low controller order, P1 Projectile id `77` with `p2stateno = 889`, executed `ChangeState` / `Projectile` / at least two `HitOverride` controller evidence, typed `projectile` plus at least two `hitoverride` operation evidence, active `x` command evidence, projectile lifecycle/payload evidence, target link `p1 -> p2 / 77`, override event/reason evidence containing `HitOverride slot 2`, final P2 state `778` / anim `778` / life `1000` / moveType `I`, and forbidden states `779`, `889`, `5000`, `150`, and `151`. This proves bounded player-owned Projectile lowest matching active HitOverride slot priority only; explicit `missonoverride = 0` custom-state slot priority is covered by the newer player gate, while guardflag edge timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, and full Projectile/HitOverride parity remain blocked.
- Previous required `synthetic-imported-hitoverride-slot-priority.json` checksum `8de62354` remains the bounded direct-HitDef HitOverride slot-priority oracle. It must require P2 matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` installed in high-to-low controller order, P1 `HitDef attr = S,NA`, executed `ChangeState` / `HitDef` / at least two `HitOverride` controller evidence, typed `hitdef` plus at least two `hitoverride` operation evidence, active `x` command evidence, override event/reason evidence containing `HitOverride slot 2`, final P2 state `778` / anim `778` / life `1000` / moveType `I`, and forbidden states `779`, `5000`, `150`, and `151`. This proves bounded direct-HitDef lowest matching active HitOverride slot priority only; explicit custom-state slot-priority breadth is covered by the `missonoverride = 0` direct/player/helper gates, while guardflag edge timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, and full HitOverride parity remain blocked.
- Previous required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero.json` checksum `62d7d6b8` remains the bounded helper-parented Projectile `missonoverride = 0` HitOverride oracle. It must require a Helper-spawned Projectile declaring `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`, defender active matching `HitOverride` slot `777`, executed `Helper` / `Projectile` / `HitOverride` controller evidence, typed `helper` / `projectile` / `hitoverride` operation evidence, active `x` command evidence, override event/reason evidence, owner/helper target-link evidence for id `8872`, helper/projectile lifecycle and payload evidence with projectile `hitsRemaining = 0` / `hasHit = true`, final P2 state `777` / life `1000`, and forbidden states `889`, `1276`, `5000`, `150`, and `151`. This proves bounded helper-parented Projectile `missonoverride = 0` HitOverride redirect before projectile custom-state entry only; explicit custom-state slot-priority breadth is covered by the newer player/helper gates, while broader `missonoverride` custom-state breadth, exact guard KO/no-KO round flow, and full Projectile/HitOverride parity remain blocked. Previous required `synthetic-imported-projectile-hitoverride-missonoverride-zero.json` checksum `5c12f3cc` remains the bounded player-owned Projectile `missonoverride = 0` HitOverride oracle; previous required `synthetic-imported-helper-projectile-hitoverride-missonoverride-one.json` checksum `a99979bb` remains the bounded helper-parented Projectile `missonoverride = 1` HitOverride oracle; previous required `synthetic-imported-projectile-hitoverride-missonoverride-one.json` checksum `2dc86467` remains the bounded player-owned Projectile `missonoverride = 1` HitOverride oracle; previous required `synthetic-imported-helper-projectile-hitoverride-p2stateno.json` checksum `ce4c1d9a` remains the bounded helper-parented Projectile `p2stateno` HitOverride redirect oracle; previous required `synthetic-imported-projectile-hitoverride-p2stateno.json` checksum `2ec0725a` remains the bounded player-owned Projectile `p2stateno` HitOverride redirect oracle.
- Required `synthetic-imported-hitoverride-p2getp1state-zero-miss.json` checksum `656730c8` remains the previous bounded IKEMEN direct-HitDef `p2getp1state = 0` HitOverride oracle. It must require a direct `HitDef` declaring `p2stateno = 889` / `p2getp1state = 0`, defender active matching `HitOverride` slot `777`, target-owned state `889` data present, executed `HitDef` and `HitOverride` controller evidence, typed `hitdef` and `hitoverride` operation evidence, active `x` command evidence, reject event/reason evidence, no target-link evidence, final P2 state `0` / life `1000` / control, and forbidden states `777`, `889`, `888`, `5000`, `150`, and `151`. This proves bounded direct-HitDef target-owned `p2getp1state = 0` HitOverride miss before target/damage/guard/redirect/state entry only; helper/projectile `p2stateno`, exact slot priority, `missonoverride = 0` projectile/custom-state breadth, exact guard KO/no-KO round flow, and full HitOverride/custom-state parity remain blocked. Previous required `synthetic-imported-hitoverride-missonoverride-one.json` checksum `78cfedf4` remains the bounded IKEMEN direct-HitDef `missonoverride = 1` HitOverride miss oracle; previous required `synthetic-imported-hitoverride-missonoverride-zero.json` checksum `8ffd5678` remains the bounded IKEMEN direct-HitDef `missonoverride = 0` HitOverride redirect oracle; previous required `synthetic-imported-hitoverride-p2stateno-miss.json` checksum `6f41eeb1` remains the bounded default direct-HitDef HitOverride plus owner-backed `p2stateno` miss oracle.
- Previous required `synthetic-imported-p2stateno-guard-ignored.json` checksum `76d1becd` remains the bounded direct-HitDef `p2stateno` ignored-on-successful-guard oracle. It must require a guarded direct `HitDef` declaring `p2stateno = 888` / `p2getp1state = 1`, defender-owned Common1-style route `150 -> 151 -> 130 -> 20`, forbidden state `888`, executed `HitDef` / `HitVelSet` / `VelSet` / `CtrlSet` evidence, typed `hitdef` / `kinematic:hitvelset` / `kinematic:velset` / `resource:ctrlset` operation evidence, target-link evidence for target id `77`, guard event/reason evidence, active `holdback` / `x` command evidence, and final P2 walk/control evidence. This proves successful-guard `p2stateno` ignore behavior on the current bounded direct-HitDef route only; exact guard KO/no-KO round flow, helper/projectile `p2stateno` behavior, and full guard parity remain blocked.
- Previous required `synthetic-imported-custom-state-gethitvar-guard-kill.json` checksum `c889a534` remains the bounded owner-backed custom-state guard-hit `GetHitVar(kill)` oracle. It must require a guarded direct `HitDef` with `guard.kill = 0`, owner-local `TargetState`, executed `HitDef` / `TargetState` / `ChangeState` / `SelfState` operation evidence, target-link evidence for target id `77`, guard event/reason evidence, P2 attacker-owned actor-frame evidence for states `888` and `904`, a branch through `GetHitVar(kill) = 0 && GetHitVar(guarded) = 1`, routed attacker state `200`, active `holdback` / `x` command evidence, and final P2 control evidence. This proves direct-HitDef custom-state guard-kill metadata on the current bounded TargetState route only; exact guard KO/no-KO round flow, helper/projectile custom-state guard kill inheritance, and full custom-state/guard parity remain blocked. Previous required `synthetic-imported-helper-projectile-gethitvar-guard-kill.json` checksum `7f9aa699` remains the matching helper-parented Projectile route through `150 -> 151 -> 334`; previous required `synthetic-imported-projectile-gethitvar-guard-kill.json` checksum `3feae5a7` remains the matching player-owned Projectile route through `150 -> 151 -> 333`; previous required `synthetic-imported-gethitvar-air-guard-kill.json` checksum `4382207e` remains the matching air guard route through `154 -> 155 -> 332`; previous required `synthetic-imported-gethitvar-crouch-guard-kill.json` checksum `2976fb8c` remains the matching crouch guard route through `152 -> 153 -> 331`; previous required `synthetic-imported-gethitvar-guard-kill.json` checksum `abb4e468` remains the matching stand guard route through `150 -> 151 -> 330`.
- Required `synthetic-imported-gethitvar-kill.json` checksum `ef5ffabf` remains the bounded defender-owned normal-hit `GetHitVar(kill)` oracle. It must require a direct normal `HitDef kill = 0`, defender-owned Common1-style state `5000`, executed `HitDef` operation evidence, target-link evidence, a branch through `GetHitVar(kill) = 0 && !GetHitVar(guarded)` into state/action `329`, routed attacker state `200`, and final P2 hit-state evidence. This proves normal-hit kill metadata on the current direct-HitDef route only; KO/round-flow behavior, helper/projectile/custom-state inheritance, exact target lifetime/tick order, and full get-hit parity remain blocked.
- Required `synthetic-imported-gethitvar-fallcount.json` checksum `c391d938` is the bounded imported CNS `GetHitVar(fallcount)` oracle. It must require a fall `HitDef`, owner-backed state `5100`, executed `HitFallDamage` plus `hitfall:hitfalldamage` operation evidence, a branch through `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0` into state/action `328`, routed attacker state `200`, and final P2 hit-fall evidence with spent `fall.damage = 0`. This proves one Common1-style ground impact count on the current route only; exact combo accumulation over multiple ground hits, lifetime/reset rules, non-Common1 impact detection, helper/projectile/custom-state inheritance, and full fall/get-hit parity remain blocked.
- Required `synthetic-imported-basic-movement.json` checksum `917ff3e5` is the current bounded imported movement oracle. It must require scripted `F`, `D`, and `U` input, imported P1 actor-frame evidence for walk state/action `20`, crouch `10`, airborne jump `40`, ordered movement evidence `20 -> 10 -> 40`, and final idle/control `0`. This proves only the current sandbox imported input-control route, not exact MUGEN/IKEMEN Common1 movement semantics, CMD command-priority quirks, command-buffer raw direction evidence, landing timing, collision/platform physics, AI behavior, visual parity, or full movement parity.
- Required `synthetic-imported-assertspecial-helper-explod-shadow.json` trace checksum `83f61b48` is the current bounded `AssertSpecial GlobalNoShadow` helper/explod shadow oracle. It must require `AssertSpecial` controller evidence, typed `assertspecial` operation evidence, helper/explod lifecycle evidence, and actor-frame `shadowVisible=false` evidence for P1, P2, the helper, and the explod while a helper-parented Explod route is active. Required `synthetic-imported-assertspecial-shadow-telemetry.json` trace checksum `2b9c8fac` remains the local/global player shadow telemetry oracle. These prove current bounded snapshot/render suppression only; exact shadow skew/stage parameters, projectile shadow semantics, exact global/team/helper ownership beyond the spawned helper/explod route, pause/layer behavior, and full shadow/global renderer parity remain blocked.
- Required `synthetic-imported-assertspecial-global-telemetry.json` checksum `fc793d29` remains the bounded `AssertSpecial` global telemetry oracle. It must require `AssertSpecial` controller evidence, typed `assertspecial` operation evidence, and final imported actor `assertSpecialGlobalFlags` for `nobardisplay`, `nobg`, `nofg`, `nokosnd`, and `nomusic`. This proves telemetry/gate coverage only; lifebar hiding, stage BG/FG suppression, KO sound suppression, music pause, exact global/team/helper ownership, pause/layer behavior, and full global flag parity remain blocked.
- Required `synthetic-imported-assertspecial-round-flow-telemetry.json` checksum `10f95bdb` is the bounded `AssertSpecial Intro` / `NoKOSlow` telemetry oracle. It must require `AssertSpecial` controller evidence, typed `assertspecial` operation evidence, and final imported actor `assertSpecialGlobalFlags` for `intro` and `nokoslow`, with runtime `intro` / `noKoSlow` booleans. This proves telemetry/gate coverage only; intro state ownership, KO slow-motion suppression, winpose/round transitions, helper/team/global ownership, pause/layer behavior, and full MUGEN/IKEMEN round-flow parity remain blocked.
- Optional private-fixture `kfm-official-basic-movement.json` checksum `ef30066c` confirms the local official KFM fixture can traverse authored walk `20`, crouch prep `11`, jump `41`, and final idle/control `0` from the same scripted `F`, `D`, and `U` route. It must stay optional and must not be used as public KFM support or exact movement parity evidence.
- Required `synthetic-imported-palfx-remappal.json` checksum `ba5fc1e6` is the current bounded palette/presentation telemetry oracle. It must require imported state `200`, active command `x`, executed `PalFX`, `RemapPal`, and `HitDef` controllers, typed `sprite-effect:palfx`, `sprite-effect:remappal`, and `hitdef` operation evidence, plus actor-frame telemetry for `PalFX time/add/mul/color` and `RemapPal source/dest` on the same imported actor. This trace proves combined telemetry only; adjacent parser/render tests now prove the first ACT/SFF indexed texture handoff. Exact MUGEN/IKEMEN palette math, `sinadd`, blend order, source-bank semantics, truecolor/PNG remap, helper/redirect palette ownership, and full presentation parity remain blocked.
- Focused ACT/SFF palette gates cover `CompatibilityReport.palettes`, DEF `pal1..pal12` ACT loading, indexed SFF pixel preservation, `SffSpriteProvider` ACT destination remap, and `CharacterRenderer` palette-remap context forwarding. These tests are the current deterministic proof for pixel handoff; they do not replace a future imported browser fixture or exact palette parity oracle.
- Required `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699` is the current portable Common1 crouch guard-hold crouch-control oracle. It must require direct guarded contact while holding down-back, defender-owned route `152 -> 153 -> 130 -> 10`, active `holdback`, `holddown`, and `x` command evidence, ordered crouch slide-stop controller/typed-operation evidence through `153` `HitVelSet`, `VelSet`, `CtrlSet`, and final `ChangeState`, actor-frame evidence for crouch guard-slide velocity before stop/control, the actor-frame sequence `152 -> 153 -> 130 -> 10`, and final imported P2 crouch state/action `10` with `ctrl = true`. This proves bounded synthetic crouch guard-hold return to crouch-control only; public KFM support, exact guard-hold duration, exact guard timing, proximity guard, guard effects, air guard-hold parity, visual/audio parity, and full Common1 guard parity remain blocked.
- Required `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c` is the current Common1 stand guard-hold walk-control oracle. It must require direct guarded contact, defender-owned route `150 -> 151 -> 130 -> 20`, active `holdback` and `x` command evidence, ordered stand slide-stop controller/typed-operation evidence through `151` `HitVelSet`, `VelSet`, `CtrlSet`, and final `ChangeState`, actor-frame evidence for guard-slide velocity before stop/control, and final imported P2 walking state/action `20` with `ctrl = true`. This proves bounded synthetic stand guard-hold return to walking only; exact guard-hold duration, exact guard timing, proximity guard, guard effects, crouch/air guard-hold parity, visual/audio parity, and full Common1 guard parity remain blocked.
- Required `synthetic-imported-default-fall-ground-recovery-priority.json` checksum `e83b2db7` is the current Common1 ground-recovery priority oracle. It must require a fall `HitDef` with `fall.recover = 1` and without `p2stateno`, defender-owned route `5000 -> 5030 -> 5050 -> 5200 -> 5201 -> 52 -> 0`, active `recovery` command evidence, positive-to-zero `fall.recovertime`, named `Ground Recovery Input` `ChangeState`, ordered ground-recovery typed kinematic/control evidence, final idle/control, and forbidden generic air-recovery/liedown states `5210`/`5100`/`5101`/`5110`/`5120`. This proves bounded near-ground recovery selection only; exact recovery thresholds, controller-loop timing, ground/air arbitration thresholds, velocity math, and full Common1 fall/recovery parity remain blocked.
- Required `synthetic-imported-default-fall-recovery-input-priority.json` checksum `f5e72e07` is the previous Common1 recovery-input priority oracle. It must require a fall `HitDef` with `fall.recover = 1` and without `p2stateno`, defender-owned route `5000 -> 5030 -> 5050 -> 5210 -> 0`, active `recovery` command evidence, named `Recovery Input` `ChangeState`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` / `VelSet` / `kinematic:velset` / `HitFallSet` / `hitfall:hitfallset` evidence, final idle/control, and forbidden competing probe/ground-recovery states `5250`/`5200`. This proves bounded recovery-input priority only; exact recovery thresholds, controller-loop timing, ground/air arbitration, velocity math, and full Common1 fall/recovery parity remain blocked.
- Required `synthetic-imported-hitfall-canrecover-ready.json` checksum `c0097d7f` is the previous Common1 CanRecover-ready fall trigger oracle. It must require a fall `HitDef` with `fall.recover = 1` and without `p2stateno`, defender-owned route `5000 -> 5030 -> 5050 -> 5250 -> 0`, named `HitFall CanRecover Ready Probe` `ChangeState` evidence through `HitFall && CanRecover`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` evidence, actor-frame `5050` positive-to-zero `fall.recovertime` drop, no active `recovery` command, final idle/control, and forbidden recovery states `5210`/`5200`. This proves bounded readiness-trigger routing only; exact recovery thresholds, controller-loop timing, velocity math, and full Common1 fall/recovery parity remain blocked.
- Required `synthetic-imported-hitfall-recover-true.json` checksum `f1e3424a` remains the previous Common1 recover-enabled fall trigger oracle. It must require a fall `HitDef` with `fall.recover = 1` and without `p2stateno`, defender-owned route `5000 -> 5030 -> 5050 -> 5240 -> 0`, named `HitFall Recover Enabled Probe` `ChangeState` evidence through `HitFall && GetHitVar(fall.recover) && !CanRecover`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` evidence, actor-frame order, positive `fall.recovertime` metadata in `5050` and `5240`, final idle/control, and forbidden recovery states `5210`/`5200`. This proves bounded recover-enabled-but-not-yet-recoverable fall routing only; exact recovery thresholds, controller-loop timing, recovery arbitration, fall/CanRecover precedence, and full Common1 fall/recovery parity remain blocked.
- Required `synthetic-imported-hitfall-recover-false.json` checksum `236df0a8` remains the Common1 recover-disabled fall trigger oracle. It must require a fall `HitDef` with `fall.recover = 0` and without `p2stateno`, defender-owned route `5000 -> 5030 -> 5050 -> 5230 -> 0`, named `HitFall Recover Disabled Probe` `ChangeState` evidence, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` evidence, actor-frame order, positive `fall.recovertime` metadata in `5050` and `5230`, final idle/control, and forbidden recovery states `5210`/`5200`. This proves bounded recover-disabled fall routing only; exact recovery thresholds, controller-loop timing, recovery arbitration, fall/CanRecover precedence, and full Common1 fall/recovery parity remain blocked.
- Required `synthetic-imported-hitfall-false.json` checksum `1d538e43` remains the previous Common1 normal get-hit trigger oracle. It must require a direct non-fall `HitDef` without `p2stateno`, defender-owned route `5000 -> 325`, named `Normal HitFall False Probe` `ChangeState` evidence, actor-frame order `5000 -> 325`, final P2 state `325` / moveType `H`, and forbidden fall/recovery states `5001`, `5030`, `5050`, `5210`, and `5200`. This proves bounded `!HitFall` / `!GetHitVar(fall)` routing only; exact normal get-hit timing, fall arbitration, custom-state/helper/team inheritance, and full Common1 get-hit parity remain blocked.
- Required `synthetic-imported-hitfall-canrecover.json` checksum `7cf7ab46` is the previous Common1 fall trigger oracle. It must require a fall `HitDef` without `p2stateno`, defender-owned route `5000 -> 5030 -> 5050 -> 5220 -> 0`, ordered `HitVelSet`, `kinematic:hitvelset`, `VelAdd`, and named `HitFall CanRecover Probe` `ChangeState` evidence, positive `fall.recovertime` evidence in `5050` and `5220`, forbidden recovery states `5210`/`5200`, and final idle/control. This proves bounded `HitFall` true while `CanRecover` is false routing only; exact threshold tables, controller-loop timing, recovery arbitration, and full Common1 fall/recovery parity remain blocked.
- Required `synthetic-imported-custom-state-gethitvar-fall-metadata.json` checksum `4a3a1c6b` is the current owner-backed custom-state fall damage/kill/velocity metadata oracle. It must require direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` contact, a branch through `GetHitVar(fall) && GetHitVar(fall.damage) = 70 && GetHitVar(fall.kill) = 0 && GetHitVar(fall.xvel) = 3 && GetHitVar(fall.yvel) = -6` into state/action `903`, routed attacker state `200`, executed `HitDef` operation evidence, target-link evidence for target id `77`, owner-backed actor-frame `customOwnerId = p1` evidence for states `888` and `903`, final P2 state/action `0` with control after `SelfState`, and hit event/reason evidence. This is custom-state `GetHitVar(fall.damage/fall.kill/fall.xvel/fall.yvel)` trigger inheritance evidence only; exact metadata lifetime/stacking, helper/projectile inheritance breadth, teams/simul, throws, and full MUGEN/IKEMEN custom-state fall parity remain blocked.
- Required `synthetic-imported-custom-state-gethitvar-fall-envshake.json` checksum `5c9d1653` remains the owner-backed custom-state fall envshake metadata oracle. It must require direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` contact, a branch through `GetHitVar(fall) && GetHitVar(fall.envshake.time) = 15 && GetHitVar(fall.envshake.freq) = 178 && GetHitVar(fall.envshake.ampl) = 6 && GetHitVar(fall.envshake.phase) = 0` into state/action `902`, routed attacker state `200`, executed `HitDef` operation evidence, target-link evidence for target id `77`, owner-backed actor-frame `customOwnerId = p1` evidence for states `888` and `902`, final P2 state/action `0` with control after `SelfState`, and hit event/reason evidence. This is custom-state `GetHitVar(fall.envshake.*)` trigger inheritance evidence only; exact camera waveform, pause/stage/layer interaction, metadata lifetime/stacking, helper/projectile inheritance breadth, teams/simul, throws, and full MUGEN/IKEMEN custom-state fall presentation parity remain blocked.
- Required `synthetic-imported-custom-state-gethitvar-snap.json` checksum `ce4680b9` remains the owner-backed custom-state snap metadata oracle. It must require direct `HitDef snap = 16,-24` contact with `p2stateno = 888`, a branch through `GetHitVar(xoff) = 16 && GetHitVar(yoff) = -24 && GetHitVar(zoff) = 0` into state/action `899`, routed attacker state `200`, executed `HitDef` operation evidence, owner-backed actor-frame `customOwnerId = p1` evidence for states `888` and `899`, final P2 state/action `0` with control after `SelfState`, and hit event/reason evidence. This is custom-state `GetHitVar(xoff/yoff/zoff)` trigger inheritance evidence only; exact throw positioning, z-axis support, guard snap behavior, helper/projectile inheritance breadth, teams/simul, and full MUGEN/IKEMEN throw/custom-state parity remain blocked.
- Required `synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid.json` checksum `250f77c2` remains the owner-backed custom-state hitcount/id metadata oracle. It must require direct `HitDef id = 77, chainID = 43, numhits = 3` contact with `p2stateno = 888` / `p2getp1state = 1`, a branch through `GetHitVar(hitcount) = 3 && GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)` into state/action `900`, routed attacker state `200`, executed `HitDef` operation evidence, target-link evidence for target id `77`, owner-backed actor-frame `customOwnerId = p1` evidence for states `888` and `900`, final P2 state/action `0` with control after `SelfState`, and hit event/reason evidence. This is custom-state `GetHitVar(hitcount/hitid/chainid)` trigger inheritance evidence only; exact combo accumulation, chain-hit eligibility arbitration, helper/projectile inheritance breadth, teams/simul, exact target lifetime/tick order, and full MUGEN/IKEMEN custom-state/get-hit parity remain blocked.
- Required `synthetic-imported-custom-state-gethitvar-guard-timing.json` checksum `ba77beec` remains the previous owner-backed custom-state guarded timing oracle. It must require guarded direct `HitDef` contact, owner-local `TargetState`, a branch through `GetHitVar(guarded) = 1 && GetHitVar(slidetime) = 5 && GetHitVar(ctrltime) = 7 && GetHitVar(hitshaketime) > 0` into state/action `901`, routed attacker state `200`, executed `HitDef` and `TargetState` operation evidence, target-link evidence for target id `77`, owner-backed actor-frame `customOwnerId = p1` evidence for states `888` and `901`, final P2 state/action `0` with control after `SelfState`, and guard event/reason evidence. This is custom-state guarded timing trigger inheritance evidence only; `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, and full MUGEN/IKEMEN custom-state/get-hit parity remain blocked.
- Required `synthetic-imported-gethitvar-snap.json` checksum `312a53fc` remains the owner-backed get-hit snap metadata oracle. It must require direct `HitDef snap = 16,-24` contact, owner-backed state `5100`, a branch through `GetHitVar(xoff) = 16 && GetHitVar(yoff) = -24 && GetHitVar(zoff) = 0` into state/action `288`, routed attacker state `200`, executed `HitDef` operation evidence, final P2 state/action `288`, and hit event/reason evidence. This is direct-contact `GetHitVar(xoff/yoff/zoff)` trigger evidence plus simple attacker-relative positioning only; exact throw positioning, z-axis support, guard snap behavior, helper/projectile/custom-state inheritance breadth, teams/simul, and full MUGEN/IKEMEN throw/get-hit parity remain blocked.
- Required `synthetic-imported-gethitvar-hitcount.json` checksum `a4685842` remains the normal-hit hit-count metadata oracle. It must require direct `HitDef numhits = 3` contact, defender-owned Common1-style state `5000`, a branch through `GetHitVar(hitcount) = 3 && !GetHitVar(guarded)` into state/action `327`, routed attacker state `200`, executed `HitDef` operation evidence, final P2 state/action `327`, and hit event/reason evidence. This is direct-contact `GetHitVar(hitcount)` trigger evidence only; exact combo accumulation, multi-hit timing, helper/projectile/custom-state inheritance breadth, teams/simul, target lifetime/tick order, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Required `synthetic-imported-gethitvar-hitid-chainid.json` checksum `18df99ed` remains the normal-hit id/chain metadata oracle. It must require direct `HitDef id = 77, chainID = 43` contact, defender-owned Common1-style state `5000`, a branch through `GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && !GetHitVar(guarded)` into state/action `326`, routed attacker state `200`, executed `HitDef` operation evidence, target-link evidence for target id `77`, final P2 state/action `326`, and hit event/reason evidence. This is direct-contact `GetHitVar(hitid/chainid)` trigger evidence only; exact chain-hit eligibility arbitration, helper/projectile/custom-state inheritance breadth, teams/simul, target lifetime/tick order, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Required `synthetic-imported-gethitvar-velocity.json` checksum `878a03f7` remains the normal-hit velocity metadata oracle. It must require direct `HitDef` contact with authored `ground.velocity = 4,-2`, defender-owned Common1-style state `5000`, a branch through `GetHitVar(xvel) = 4 && GetHitVar(yvel) = -2 && !GetHitVar(fall) && !GetHitVar(guarded)` into state/action `324`, routed attacker state `200`, executed `HitDef` operation evidence, final P2 state/action `324`, hit event/reason evidence, and actor-frame velocity telemetry matching `4,-2`. This is direct-contact `GetHitVar(xvel/yvel)` trigger evidence only; exact velocity lifetime after later physics/controllers, helper/projectile/custom-state inheritance breadth, teams/simul, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Required `synthetic-imported-gethitvar-damage.json` checksum `2c726114` remains the normal-hit damage metadata oracle. It must require direct `HitDef` contact, defender-owned Common1-style state `5000`, a branch through `GetHitVar(damage) = 37 && !GetHitVar(guarded)` into state/action `323`, routed attacker state `200`, executed `HitDef` operation evidence, final P2 state/action `323`, and hit event/reason evidence. This is direct-contact `GetHitVar(damage)` trigger evidence only; exact damage lifetime/rounding, guard-chip semantics, helper/projectile/custom-state inheritance breadth, teams/simul, and full MUGEN/IKEMEN get-hit parity remain blocked.
- Optional private KFM `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae` is the current crouch Common1 fixture-confidence oracle when `.scratch/fixtures/kfm-official.zip` exists. It must require real KFM held-crouch prep state `11`, Common1 `5010 -> 5011 -> 0` through `HitShakeOver` / `HitOver`, final state `0`/control, `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width `39/39`, bounded crouch slide velocity, and ordered `5010` `ChangeAnim` / `ChangeState`, `5011` `HitVelSet`, `VelMul`, `VelSet`, `DefenceMulSet`, plus typed-operation evidence. This is optional private fixture confidence only; skipped optional fixtures are not passes, and public KFM support/full parity remain blocked.
- Optional private KFM `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` is the current crouch guard-hold fixture-confidence oracle when `.scratch/fixtures/kfm-official.zip` exists. It must require real KFM/Common1 route `152 -> 153 -> 131 -> 11`, active `holdback`, `holddown`, and `x`, real state `153` `HitVelSet`, typed `kinematic:hitvelset`, `VelSet`, typed `kinematic:velset`, `CtrlSet`, typed `resource:ctrlset`, final `ChangeState`, actor-frame sequence evidence, and final KFM crouch state/action `11` with control. This is optional private fixture confidence only; skipped optional fixtures are not passes, and public KFM support/full guard parity remain blocked.
- Optional private KFM `kfm-official-default-crouch-guard-slide-stop.json` checksum `d11153d0` is the previous crouch guard slide-stop fixture-confidence oracle when `.scratch/fixtures/kfm-official.zip` exists. It must require real KFM Common1 state `153` to execute `HitVelSet`, typed `kinematic:hitvelset`, `VelSet`, typed `kinematic:velset`, `CtrlSet`, typed `resource:ctrlset`, and final `ChangeState` after direct guarded contact while holding down-back. This is optional private fixture confidence only; skipped optional fixtures are not passes, and public KFM support/full guard parity remain blocked.
- Optional private KFM `kfm-official-default-air-fall-recovery-input.json` checksum `a431028a` and `kfm-official-default-air-fall-recovery-too-early.json` checksum `a1db1589` are current air-entry Common1 fixture-confidence oracles when `.scratch/fixtures/kfm-official.zip` exists. They must require real KFM `5020 -> 5030 -> 5035 -> 5050` evidence; the input route must then enter `5210`, land through `52`, and return to `0`/control after KFM's `5030` countdown reaches `0`, while the too-early route must remain in `5050` with positive `fall.recovertime` and forbid `5210`, `5200`, `5201`, `52`, `5100`, `5101`, `5110`, and `5120`. This is optional private fixture confidence only; skipped optional fixtures are not passes, and public KFM support/full parity remain blocked.
- Required `synthetic-imported-projectile-contacttime-id.json` checksum `e9ebf36a` and `synthetic-imported-projectile-guardedtime-id.json` checksum `dfd08f28` are the current owner-state Projectile fixed-id contact/guard-time oracles. They must require a player-owned Projectile contact/guard marker, route the owner through `ProjContactTime(77) >= 1` into state/action `321` and `ProjGuardedTime(77) >= 1` into state/action `322`, and include hit/guard event/reason, projectile lifecycle, effect-store, and target-link evidence. This is fixed-id contact/guard-time evidence only; exact contact/guard tick-order/lifetime, multi-projectile same-id selection, helper-owned projectile routing, redirects, teams/simul, and full Projectile timing parity remain blocked.
- Required `synthetic-imported-projectile-canceltime-any.json` checksum `5bff1961` remains the owner-state Projectile any-id cancel-time oracle. It must require player-owned Projectile id `77` to be canceled by an opposing Projectile clash, route that Projectile owner through `ProjCancelTime(0) >= 0` from state/action `200` into `320`, record clash/cancel runtime-event evidence, preserve loser `projcancelanim` terminal playback anim `918`, and include projectile lifecycle, effect-store, and payload evidence. This is owner-state any-id trigger evidence for one canceled Projectile route only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration beyond this route, exact priority classes, helper-owned projectile routing, redirects, teams/simul, and full Projectile cancel parity remain blocked.
- Required `synthetic-imported-projectile-canceltime-var.json` checksum `e057e102` remains the owner-state Projectile var-id cancel-time oracle. It must require owner-local `VarSet` to seed player-owned Projectile id `77`, that Projectile to be canceled by an opposing Projectile clash, route that Projectile owner through `ProjCancelTime(var(0)) >= 0` from state/action `200` into `319`, record clash/cancel runtime-event evidence, preserve loser `projcancelanim` terminal playback anim `917`, and include projectile lifecycle, effect-store, and payload evidence. This is owner-state var-backed trigger evidence only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration, exact priority classes, helper-owned projectile routing, redirects, teams/simul, and full Projectile cancel parity remain blocked.
- Required `synthetic-imported-projectile-canceltime-dynamic.json` checksum `0da26c87` remains the owner-state Projectile dynamic-id cancel-time oracle. It must require a player-owned Projectile id `77` to be canceled by an opposing Projectile clash, route that Projectile owner through expression-derived `ProjCancelTime(77 + var(0)) >= 0` from state/action `200` into `318`, record clash/cancel runtime-event evidence, preserve loser `projcancelanim` terminal playback anim `916`, and include projectile lifecycle, effect-store, and payload evidence. This is owner-state dynamic-id trigger evidence only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration, exact priority classes, helper-owned projectile routing, redirects, teams/simul, and full Projectile cancel parity remain blocked.
- Required `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2` is the current helper Projectile dynamic-id cancel-time oracle. It must require helper-parented owner-side Projectile id `8869` to be canceled by an opposing Projectile clash, route that Helper through expression-derived `ProjCancelTime(8869 + var(0)) >= 0` from state/action `1270` into `1271`, record clash/cancel runtime-event evidence, preserve loser `projcancelanim` terminal playback anim `1018`, and include helper/projectile lifecycle, effect-store, and payload evidence. Focused helper-local runtime coverage separately proves nonzero `ProjCancelTime(var(n))` id selection against helper-parented canceled Projectiles. This is helper-local dynamic-id trigger evidence only; exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, and full Helper/Projectile cancel parity remain blocked.
- Required `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176` remains the helper Projectile fixed-id cancel-time oracle. It must require a helper-parented owner-side Projectile id `8868` to be canceled by an opposing Projectile clash, route that Helper through `ProjCancelTime(8868) >= 0` from state/action `1268` into `1269`, record clash/cancel runtime-event evidence, preserve loser `projcancelanim` terminal playback anim `1008`, and include helper/projectile lifecycle, effect-store, and payload evidence. This is helper-local fixed-id trigger evidence only; exact cancel tick-order/lifetime, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, and full Helper/Projectile cancel parity remain blocked.
- Required `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01` remains the helper Projectile any-id cancel-time oracle. It must require a helper-parented owner-side Projectile to be canceled by an opposing Projectile clash, route that Helper through `ProjCancelTime(0) >= 0` from state/action `1266` into `1267`, record clash/cancel runtime-event evidence, preserve loser `projcancelanim` terminal playback anim `998`, and include helper/projectile lifecycle, effect-store, and payload evidence. This is helper-local trigger evidence only; exact cancel tick-order/lifetime, exact priority classes, multi-projectile any-id selection semantics beyond this route, helper-owned custom states, redirects, teams/simul, and full Helper/Projectile cancel parity remain blocked.
- Required `synthetic-imported-projectile-canceltime.json` checksum `64e8dec4` remains the owner-state Projectile cancel-time oracle. It requires a player-owned Projectile to be canceled by an opposing Projectile clash, routes that Projectile owner through `ProjCancelTime(77) >= 0` into state/action `283`, records clash/cancel runtime-event evidence, preserves loser `projcancelanim` terminal playback, and includes projectile lifecycle, effect-store, and payload evidence. This is owner-state trigger evidence only; exact cancel tick-order/lifetime, exact priority classes, multi-projectile any-id arbitration, redirects, teams/simul, and full Projectile cancel parity remain blocked.
- Optional private-fixture `kfm-official-qcf-x.json` checksum `349cd995` is the current KFM special-route oracle. It must require real `QCF_x` input into state `1000`, ordered actor-frame/controller/typed-operation evidence, contact `S5,4` sound telemetry, inherited `[Data] sparkno = 2` hit-spark telemetry with `sparkxy = -10,-60`, shared sound+spark contact package metadata, target-link evidence, and bounded final hitfall/damage state. This is fixture confidence only; it does not prove public bundled KFM support, exact playback/render timing, broad special parity, or full MUGEN/IKEMEN parity.
- Required `synthetic-imported-animelem-offset.json` checksum `4484031d` is the current active-state legacy `AnimElem` elapsed-time oracle. It must require imported state `200` to advance through authored AIR frame durations `[2,6,4]`, then route through `AnimElem = 2, = 4` into state/action `300` without `HitDef` side effects. Required `synthetic-imported-animelem.json` checksum `683d9a10` remains the paired start-of-element oracle for `AnimElem = 2, = 0` into state/action `299`. This is trigger-read evidence only; AIR loop semantics, negative-duration semantics, helper/state-owner namespaces, and full animation VM parity remain blocked.
- Required `synthetic-imported-owner-metrics.json` checksum `1a61aaeb` remains the owner metric trigger oracle. It must require imported State -1 routing through bounded `StateNo`, `Anim`, `Time`, `Life`, `Power`, `Pos X/Y`, and `Vel X/Y` reads into state/action `298`; it also depends on `RuntimeExpressionContextWorld` exposing the internal post-transition `stateElapsed = -1` sentinel as observable CNS `Time` / `StateTime` zero. This is trigger-read evidence only; exact VM tick ordering, helper/team/redirect namespaces, localcoord scaling, and full MUGEN/IKEMEN trigger parity remain blocked.
- Required `synthetic-imported-hitdef-data-spark.json` checksum `d950796c` and `synthetic-imported-hitdef-data-guard-spark.json` checksum `7bd9d162` are the portable `HitDef` default-spark fallback oracles. They must prove omitted `sparkno` / `guard.sparkno` inherit parsed CNS `[Data]` constants through typed `HitDef` `ControllerOp` data and emit bounded hit/guard `HitSpark` telemetry with authored `sparkxy` offsets. This is fallback/event telemetry only; exact FightFX/common lookup, renderer timing, layering, scale, palette, SND playback, and full effect parity remain blocked.

## Fixture Matrix

Every major compatibility pass should consider:

| Fixture | Required Evidence |
| --- | --- |
| Native generated roster | Runtime screenshot and controls/combat smoke. |
| Official KFM | Upload, decoded sprite rendering, compatibility JSON, one routed attack. |
| KFM720 | Upload and localcoord/scale sanity check. |
| CodeFuMan | SFF v1 PCX path and at least one routed attack. |
| SF3 Ryu demo | Parser/report stress test; runtime playability is not required yet. |
| Imported stage | Stage report, rendered background/floor if decoded, bounded BG `trans` metadata/material handoff and bounded rectangular `window`/`maskwindow` clipping when declared, fallback if not. |

## Report Honesty Rules

- A parser count is not runtime compatibility.
- A recognized controller is not an executable controller.
- An executable controller with ignored params is partial.
- Runtime session execution is separate from static compatibility.
- A feature that works only for native generated fighters must not be described as imported MUGEN compatibility.

## Deterministic Trace Evidence

Runtime compatibility gates should include replay-style evidence when they change tick order, command routing, controller execution, combat, actor ownership, or pause behavior.

Minimum trace evidence:

- Scripted input frames.
- Final actor state/control constraints and frame checksum.
- State/controller routing summary where compatibility data exists.
- Typed controller-operation evidence where the gate depends on compiled IR side effects such as `HitDef`, `Gravity`, `HitVelSet`, `PosFreeze`/`ScreenBound`, `AttackMulSet`/`DefenceMulSet`, `Target*`, `Pause`/`SuperPause`, `SprPriority`, `PalFX`, `Trans`, `AngleSet`/`AngleAdd`/`AngleMul`/`AngleDraw`, `EnvColor`, `RemapPal`, `AfterImage`/`AfterImageTime`, `Projectile`, `ModifyProjectile`, `Helper`, `Explod`, or `ModifyExplod`.
- New runtime log events categorized as hit, guard, reject, override, reversal, pause, round, or runtime.
- Combat reason evidence for hit, guard, whiff, reject, override, and reversal when the trace gate depends on combat behavior.
- Match-pause snapshot evidence for `Pause`/`SuperPause` gates, including source actor/state, darken, remaining frames, source `movetime`, frozen actor/effect evidence when the gate depends on opponent or effect freeze, and actor/effect advance evidence when the gate depends on source `movetime`; hitpause actor/effect freeze/advance evidence when the gate depends on `ignorehitpause`.
- Sound-event evidence for `PlaySnd`/`StopSnd` gates, including `RuntimeAudioWorld` event insertion, actor id, controller type or HitDef/Projectile sound source, parsed `Sgroup,index`, channel, optional `lowPriority` / `volumeScale` / `legacyVolume` / `freqMul` / `loop` / `pan` / `absPan`, state, tick data, and optional `contactId` / `contactTick` / `contactKind` metadata for direct `HitDef` or bounded `Projectile` contact packages.
- Compatibility-report SND summary evidence: `CompatibilityReport.sounds` must stay derived from the parsed `SndArchive` and report total declared sounds, decoded WAV count, unsupported/raw count, format counts, sample-rate counts, and channel counts without claiming playback parity.
- Hit-effect evidence for `HitSpark` gates, including `RuntimeHitEffectWorld` event insertion, actor id, raw spark ref, raw prefix, parsed `sparkNo`, `sparkxy` offset, state/tick data, optional direct/projectile contact package metadata, and bounded asset-frame metadata fields (`assetSource`, `assetActionId`, `assetFrameIndex`, `assetSpriteGroup`, `assetSpriteIndex`, `assetFrameCount`, `assetTotalDuration`, `assetFrameIndices`) when player/common/FightFX AIR frames are resolved before renderer handoff. Gates that depend on omitted `HitDef` spark refs should also verify parsed CNS `[Data] sparkno` / `guard.sparkno` fallback survives the typed `ControllerOp` compile path when constants are available. Requirements can also demand `offsetX`, `offsetY`, `minAssetFrameCount`, `minAssetTotalDuration`, and `requiredAssetFrameIndices`; `requiredContactEffectPackages` can require one sound event and one hit-effect event to share the same non-empty contact id, contact tick, hit/guard kind, and authored spark offset.
- Hit-effect evidence for bounded `HitDef` spark gates, including `RuntimeHitEffectWorld` event insertion, actor id/source/kind, spark number, raw prefix, optional `sparkxy` offset, state, and tick data.
- EnvShake event evidence for `EnvShake` and `FallEnvShake` gates, including actor id/source/kind, `time`, `freq`, `ampl`, `phase`, state, and tick data; `RuntimeEnvShakeWorld` owns the bounded event insertion and camera-shake projection used by runtime snapshots.
- Target-link evidence for `Target*` gates, including active binding metadata when the gate depends on a bound target surviving a pause or source-movetime path.
- Helper-local redirect/bind/effect evidence for `Helper` VM gates, including helper actor frame/payload state, owner id, helper id, bounded owner-bind target/offset telemetry when binding is claimed, helper target-link evidence when explicit-id/default/bare helper `HitDef` or helper-parented `Projectile` target memory is claimed, helper `parentId` metadata when helper-local visual effects are claimed, and the specific bounded route such as `synthetic-imported-helper-target.json` checksum `68f95b67` for helper-local explicit-id `HitDef` target memory plus `NumTarget(id)` / `Target(id), Life`, `synthetic-imported-helper-default-target.json` checksum `e1bcced0` for helper-local omitted-id `HitDef` target memory plus `NumTarget(0)` / `Target(0), Life`, `synthetic-imported-helper-bare-target.json` checksum `15f3c1db` for helper-local explicit-id `HitDef` target memory plus bare `Target, Life`, `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1` for helper-parented Projectile target memory plus bare `Target, Life`, `synthetic-imported-helper-target-controllers.json` checksum `61f4c61e` for bounded helper-owned direct-HitDef Target side effects, `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8` for bounded helper-owned Target side effects after helper-parented Projectile explicit-id target memory, `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae` for the matching omitted-`projid` / omitted-`id` default target id `0` Target side effects after helper-parented Projectile target memory, `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3` for helper-parented Projectile explicit target memory feeding helper-owned `TargetState` into owner-backed state data, `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1` for helper-parented Projectile omitted-id/default target id `0` memory feeding helper-owned `TargetState` into owner-backed state data, `synthetic-imported-helper-targetstate.json` checksum `011633b8` for helper-local `TargetState` into owner-backed state data, `synthetic-imported-helper-projectile-target.json` checksum `49261b53` for helper-parented owner-side Projectile explicit-id target memory plus owner/helper target-link evidence and `NumTarget(id)` / `Target(id), Life`, `synthetic-imported-helper-projectile-default-target.json` checksum `b0daddf6` for helper-parented owner-side Projectile omitted-id target memory plus owner/helper target-link evidence and `NumTarget(0)` / `Target(0), Life`, `synthetic-imported-helper-parentroot.json` checksum `5154220c` for helper-local `Parent,StateNo` / `Parent,Vel X` / `Root,Anim` redirect routing into state `1400` / anim `940`, `synthetic-imported-helper-explod.json` checksum `87ae363f` for static helper-local Explod handoff, `synthetic-imported-helper-projectile.json` checksum `b6269136` for static helper-local Projectile spawn, `synthetic-imported-helper-modifyprojectile.json` checksum `09d3f7e4` for static helper-local ModifyProjectile mutation by id after helper-local spawn, `synthetic-imported-helper-projhit.json` checksum `2d9a281e` for helper-local `ProjHit(id)` branch evidence after helper-parented Projectile contact, `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b` for helper-local `ProjHitTime(0)` any-projectile branch evidence, `synthetic-imported-helper-projguardedtime-any.json` checksum `1f1a38e4` for helper-local `ProjGuardedTime(0)` any-projectile branch evidence, `synthetic-imported-helper-projcontacttime-any.json` checksum `0d9f7829` for helper-local `ProjContactTime(0)` any-projectile branch evidence, `synthetic-imported-helper-projguard.json` checksum `d2e2f20d` for helper-local `ProjGuarded(id)` / `ProjGuardedTime(id)` branch evidence after helper-parented Projectile guard contact, `synthetic-imported-helper-projcontact.json` checksum `4dcbdd25` for helper-local `ProjContact(id)` / `ProjContactTime(id)` branch evidence after helper-parented Projectile generic contact, `synthetic-imported-helper-removeexplod.json` checksum `ff8658a2` for static helper-local RemoveExplod cleanup by id after helper-local spawn, `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` for static helper-local ModifyExplod mutation by id after helper-local spawn, `synthetic-imported-helper-numexplod.json` checksum `4328278a` for helper-local `NumExplod(id)` branch evidence after helper-local spawn, `synthetic-imported-helper-numhelper.json` checksum `4e32e951` for helper-local `NumHelper(id)` branch evidence, `synthetic-imported-helper-bindtoroot.json` checksum `bf72306c` for static `BindToRoot` root binding, `synthetic-imported-helper-bindtoparent.json` checksum `f9922c0e` for static `BindToParent` owner binding, or `synthetic-imported-helper-enemynear.json` checksum `35498955` for `EnemyNear, StateNo` plus `EnemyNear, Life`.
- Round-frame evidence for round gates, including `RoundSnapshot.state`, winner, HUD message, observed timer range, first/last tick, and frame count when a gate depends on KO/time-over/fight-state visibility.
- `MatchWorld` lifecycle event evidence for actor/effect spawn, active, and remove when the gate depends on world ownership.
- Focused target-world tests for ownership-only cuts where `RuntimeTargetWorld` snapshots runtime target refs/bindings for `MatchWorld` without changing trace behavior.
- Focused pause-world tests for ownership-only cuts where `RuntimePauseWorld` owns current `Pause`/`SuperPause` state without changing trace behavior.
- `RuntimeEffectActorWorld` store evidence for owner/family counts and serial progression when a gate depends on helper/projectile/explod ownership.
- Effect-payload evidence for typed helper/projectile/explod fields when a gate depends on concrete runtime payload data such as helper id/name/state, projectile hit/removal/terminal metadata, or Explod bind/scale/pause flags.
- Actor-frame and stage-frame evidence for effect motion, scaling, body-width changes, body-push flags, sprite priority, palette/material/opacity/rotation telemetry, stage flash telemetry, ghost-trail telemetry, facing flips, hit-fall recovery/down-recovery timers, kinematic position/velocity changes, one-frame bounds/camera flags, stage camera ranges, custom-state ownership gates, or ordered actor-frame sequences, including observed min/max position, velocity, scale, `hitFall.recoverTime`, `hitFall.downRecoverTime`, `Width` body dimensions, `PlayerPush` state, `SprPriority` renderer ordering telemetry, `PalFX` bounded color telemetry, `Trans` bounded opacity telemetry, `AngleDraw` bounded render-angle/render-scale telemetry, `EnvColor` bounded stage color/opacity telemetry, `RemapPal` bounded palette-remap telemetry, `AfterImage` bounded sample/opacity telemetry, `Turn` facing, `PosFreeze`/`ScreenBound` flags, camera ranges, bounds, and `customOwnerId` where relevant.
- Common1-style guard-hit gates that claim state physics should pair controller-event sequence evidence with actor-frame state/physics/body/push telemetry. Stand/crouch/air synthetic routes must name the bounded states they prove, preserve unchanged checksums unless behavior intentionally drifts, and keep exact proximity guard, guard effects, air landing parity, and full VM parity out of the allowed claim.
- A note when evidence covers only native generated fighters instead of imported MUGEN fixtures.

`RuntimeTrace` is the current in-process harness for this. `evaluateRuntimeTraceGate` turns a trace into explicit pass/fail evidence for actor sources/kinds, effect kinds, routed states, executed states, executed controllers, ordered controller-event sequences by controller, optional controller name, and typed operation, active commands, event categories, combat reasons, match-pause snapshots including optional `SuperPause` `superAnim` metadata, match-pause actor/effect advance and freeze evidence, sound events, hit-effect events, shared contact-effect packages, EnvShake events, world lifecycle events, effect actor stores, effect payloads, target links, round-frame KO/time-over winner/message/timer evidence, stage-frame camera/bounds/color ranges, actor-frame/final-actor `customOwnerId`, actor-frame `StateTypeSet` metadata, actor-frame `Width` body dimensions, actor-frame `PlayerPush` state, actor-frame `SprPriority` sprite priority, actor-frame `PalFX` material telemetry, actor-frame `Trans` render-opacity telemetry, actor-frame `AngleDraw` render-angle/render-scale telemetry, stage-frame `EnvColor` color/opacity telemetry, actor-frame `RemapPal` palette-remap telemetry, actor-frame `AfterImage` ghost-trail telemetry, actor-frame `Turn` facing, actor-frame kinematic position/velocity, actor-frame `Gravity` velocity, actor-frame `PosFreeze`/`ScreenBound` flags, ordered actor-frame sequences, and final actor state/control/life requirements. `RuntimeTraceArtifact` packages the trace checksum, per-frame stage/world/round summaries, typed `Explod`/`Helper`/`Projectile` effect payload summaries, effect-payload field deltas, final actors/effects, events, combat reasons, script, gate requirements, gate evidence, and gate results into `runtime-trace-artifact/v0` JSON; render-opacity, render-angle, render-scale, and stage-flash gate evidence are included in behavior checksums while sound-event, hit-effect, contact-package, controller-event, match-pause `superAnim`, and EnvShake-event telemetry are inspection evidence excluded from checksums. `pnpm qa:trace` also writes `diagnostics.json` with a `coverage` matrix for controller families, typed operations, effect kinds, combat reasons, match-pause advance/freeze routes, sound-event types, hit-effect event kinds, EnvShake event routes, world lifecycle routes, round-frame routes, target-link routes, effect-store routes, effect-payload kinds, and effect-payload delta routes; that coverage matrix is itself gated for the current critical runtime spine (`hitdef`, `kinematic:velset`, `kinematic:veladd`, `kinematic:velmul`, `kinematic:posset`, `kinematic:posadd`, `kinematic:gravity`, `kinematic:hitvelset`, `bounds:posfreeze`, `bounds:screenbound`, `collision:width`, `collision:playerpush`, `synthetic-imported-playerpush-dynamic`, `metadata:statetypeset`, `orientation:turn`, `sprite-effect:sprpriority`, `sprite-effect:palfx`, `sprite-effect:trans`, `sprite-effect:angleset`, `sprite-effect:angleadd`, `sprite-effect:anglemul`, `sprite-effect:angledraw`, `envcolor`, `sprite-effect:remappal`, `sprite-effect:afterimage`, `sprite-effect:afterimagetime`, `target:targetbind`, `pause:superpause`, `projectile`, `modifyprojectile`, `helper`, `explod`, `modifyexplod`, `reversaldef`, `damage-scale:attackmulset`, `damage-scale:defencemulset`, `resource:ctrlset`, `PlaySnd`/`StopSnd` sound-event evidence including bounded HitDef hit-sound, guard-sound, combined HitDef hit/guard-effect package routes, and bounded Projectile hit/guard package routes, `HitSpark:hit` / `HitSpark:guard` hit-effect evidence including bounded HitDef hit-spark, guard-spark, data fallback hit/guard spark, combined HitDef hit/guard-effect package routes, and bounded Projectile hit/guard package routes, EnvShake event evidence, the bounded direct `HitDef` priority and nonlethal kill/NoKO artifacts, bounded `AssertSpecial` TimerFreeze/RoundNotOver/Intro/NoKOSlow telemetry artifacts, bounded `projectile:hits` / `projectile:removal` / `projectile:terminal` / `explod:bindRemaining` payload deltas, required `requiredEffectPayloads` for Projectile/Helper/Explod golden routes, and the bounded SuperPause actor/effect routes). Studio Build can export a smoke trace artifact for the selected playtest setup, and Studio Evidence can filter recent in-session trace artifacts with related gate/asset/compile/compatibility/diagnostic records. Studio Evidence also keeps a bounded browser-local trace history, compares persisted artifacts against the current trace by checksum plus frame/event/gate/pass deltas, shows a first Trace Comparison Review for metrics and gate rows, and exposes a basic Trace Frame Scrubber; rollback, netplay, and full replay remain future work.

Previous required runtime trace addendum: `synthetic-imported-guarddist-reversal-no-contact.json` checksum `ca20c823` / final checksum `2bc9b86d` proves bounded negative `guard.dist` / `ReversalDef` priority. The route executes a guardable imported direct `HitDef` with `guard.dist = 96` in the near-but-not-contacting guard-distance stage while P2 has active `ReversalDef p1stateno = 777` / `p2stateno = 888` and an explicit `InGuardDist` guard-start route, requires typed `hitdef` and `reversaldef` operation evidence, P2 state-0 `ReversalDef` controller evidence, active `x` command evidence, emits whiff combat-reason evidence, forbids reversal/get-hit/guard-hit states `777`, `888`, `5000`, `150`, and `151`, and ends P2 in state/action `130` at life `1000`. That checkpoint passed 474/474 artifacts, 444 required and 30 optional. This proves bounded no-contact guard-distance reversal rejection only, not exact guard-distance boxes, positive proximity-only `guard.dist` ReversalDef contact, exact guard-start timing, custom-state breadth beyond direct routes, projectile reflection/removal after reversal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, score movement, or full ReversalDef/guard parity.

Previous required runtime trace addendum: `synthetic-imported-walkback-guard-reversal.json` checksum `70c83b8c` / final checksum `b7a8cb9d` proves bounded authored walk-back state-20 direct `ReversalDef` priority and remains required.

Previous required runtime trace addendum: `synthetic-imported-air-guard-reversal.json` checksum `966b17b8` / final checksum `2fa19142` proves bounded air guard-input direct `ReversalDef` priority and remains required.

Previous required runtime trace addendum: `synthetic-imported-crouch-guard-reversal.json` checksum `405f475e` / final checksum `d1f39c08` proves bounded crouch guard-input direct `ReversalDef` priority and remains required.

Previous required runtime trace addendum: `synthetic-imported-guard-reversal.json` checksum `6f8df3a4` / final checksum `e0771a15` proves bounded held-back stand guard-input direct `ReversalDef` priority and remains required.

Previous required runtime trace addendum: `synthetic-imported-custom-state-reversal.json` checksum `18065db0` / final checksum `ac8d0073` proves bounded direct custom-state `ReversalDef` priority. The route executes imported direct `HitDef p2stateno = 889` / `p2getp1state = 1` against a defender with active `ReversalDef p1stateno = 777` / `p2stateno = 888`, stops at the reversal result, requires typed `hitdef` and `reversaldef` operation evidence, emits reversal event/combat-reason evidence, forbids the HitDef owner-backed custom state `889`, default get-hit `5000`, and guard states `150` / `151`, and ends P1 in defender-owned custom state/action `888` (`customOwnerId = p2`) while P2 enters state/action `777`, both at life `1000`. That checkpoint passed 469/469 artifacts, 439 required and 30 optional. This proves bounded direct custom-state reversal priority only, not custom-state breadth beyond this direct route, projectile reflection/removal after reversal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, score movement, or full ReversalDef parity.

Previous required runtime trace addendum: `synthetic-imported-helper-projectile-reversal.json` checksum `a1d82380` / final checksum `ca66a49a` proves bounded helper-parented/root-owned Projectile `ReversalDef` priority. The route creates a visual Helper that spawns Projectile id `8878` with `ownerId = p1`, `rootId = p1`, and `parentId = p1-helper-0`, stops at the initial reversal contact, requires typed `helper`, `projectile`, and `reversaldef` operation evidence, emits reversal event/combat-reason evidence, keeps helper target memory empty and the Projectile payload unconsumed at the sampled frame, and ends P2 in state/action `777` while P1 enters owner-backed state/action `888`, both at life `1000`. That checkpoint passed 468/468 artifacts, 438 required and 30 optional. This proves bounded helper-parented Projectile reversal priority only, not projectile reflection/removal after reversal, helper-owned custom-state tables, broader guard/custom-state counter breadth, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, score movement, or full ReversalDef parity.

Previous required runtime trace addendum: `synthetic-imported-projectile-reversal.json` checksum `5c4ddf48` / final checksum `bb0bbb99` proves bounded player-owned Projectile `ReversalDef` priority. The route executes imported Projectile id `77`, stops at the initial reversal contact, requires typed `projectile` and `reversaldef` operation evidence, emits reversal event/combat-reason evidence, keeps the Projectile payload unconsumed at the sampled frame, and ends P2 in state/action `777` while P1 enters owner-backed state/action `888`, both at life `1000`. That checkpoint passed 467/467 artifacts, 437 required and 30 optional. This proves bounded player Projectile reversal priority only, not projectile reflection/removal after reversal, guard/custom-state counter breadth, exact attr grammar, hitpause/tick order, score movement, or full ReversalDef parity.

Previous required runtime trace addendum: `synthetic-imported-superpause-pausebg.json` checksum `49bcfe16` / final checksum `397a8fae` proves bounded imported `SuperPause pausebg = 0` metadata. The route executes typed SuperPause evidence with `pausebg = 0`, requires match-pause/freeze evidence, records `hitdef` and `pause:superpause` operation evidence, and observes required `pauseBg = false`. That checkpoint passed 465/465 artifacts, 435 required and 30 optional. This proves bounded SuperPause pause-background snapshot evidence only, not actual renderer/background update parity, exact stage/BGCtrl pause timing, renderer visual suppression/playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, `unhittable`, helper/team/redirect ownership, score movement, or full super presentation parity.

Previous required runtime trace addendum: `synthetic-imported-superpause-anim-disabled.json` checksum `fc7a2ca4` / final checksum `5be3ca6c` proves bounded imported `SuperPause anim = -1` metadata suppression. The route executes typed SuperPause evidence with `anim = -1`, requires match-pause/freeze evidence, records `hitdef` and `pause:superpause` operation evidence, and observes required `superAnimAbsent = true`.

Previous required runtime trace addendum: `synthetic-imported-superpause-default-anim.json` checksum `318c5e9f` / final checksum `747e7619` proves bounded imported omitted `SuperPause anim` default metadata. The route omits `anim`, executes typed SuperPause evidence, requires match-pause/freeze evidence, records `hitdef` and `pause:superpause` operation evidence, and observes `superAnim` metadata `raw = 30`, `source = fightfx`, `actionNo = 30`, and `offset = 0,0`.

Previous required runtime trace addendum: `synthetic-imported-width-dynamic.json` checksum `79baa5de` / final checksum `395b0b1a` proves bounded active imported dynamic `Width player` fallback. The route seeds `var(0)=21` and `var(1)=43`, executes `Width player = var(0),var(1)`, requires no typed `collision:width` operation evidence, and observes actor-frame/final body-width telemetry `front = 21`, `back = 43`. That checkpoint passed 452/452 artifacts, 422 required and 30 optional. This proves bounded dynamic Width fallback and telemetry only, not dynamic typed lowering for `Width`, edge width parity, exact push overlap, team/helper ownership, exact tick order, score movement, or full constraint parity.

Previous required runtime trace addendum: `synthetic-imported-envcolor-dynamic.json` checksum `dbe548a7` / final checksum `2ff8dd42` proves bounded active imported dynamic `EnvColor value/time/under` fallback. The route seeds `var(0)=32`, `var(1)=128`, `var(2)=240`, `var(3)=14`, and `var(4)=1`, executes `EnvColor value = var(0),var(1),var(2), time = var(3), under = var(4)`, requires no typed `envcolor` operation evidence, and observes stage-frame telemetry `color = 32,128,240`, `under = true`. That checkpoint passed 451/451 artifacts, 421 required and 30 optional. This proves bounded dynamic EnvColor fallback and telemetry only, not dynamic typed lowering for `EnvColor`, exact blend math, layer/window behavior, pause timing, renderer parity, score movement, or full presentation parity.

Previous required runtime trace addendum: `synthetic-imported-envshake-dynamic.json` checksum `90955e75` / final checksum `1af5fbf6` proves bounded active imported dynamic `EnvShake time/freq/ampl/phase` fallback. The route seeds `var(0)=18`, `var(1)=45`, `var(2)=-9`, and `fvar(0)=0.25`, executes `EnvShake time = var(0), freq = var(1), ampl = var(2), phase = fvar(0)`, requires no typed `envshake` operation evidence, and observes runtime env-shake telemetry `time = 18`, `freq = 45`, `ampl = -9`, and `phase = 0.25`. That aggregate was 450/450 artifacts, 420 required and 30 optional. This proves bounded dynamic EnvShake fallback and telemetry only, not dynamic typed lowering for `EnvShake`, `mul`, exact camera waveform, pause/stage/layer interaction, helper ownership, screenpack ownership, score movement, or full presentation parity.

Previous required runtime trace addendum: `synthetic-imported-anglemul-dynamic.json` checksum `418ed880` / final checksum `4a6a3045` proves bounded active imported dynamic `AngleMul value` fallback. The route seeds `var(0)=30` and `fvar(0)=1.5`, executes `AngleSet value = var(0)`, `AngleMul value = fvar(0)`, and static `AngleDraw`, requires no typed `sprite-effect:anglemul` operation evidence, and observes imported actor-frame `renderAngle = 45`. That aggregate was 449/449 artifacts, 419 required and 30 optional. This proves bounded dynamic sprite-effect fallback and telemetry only, not dynamic typed lowering for `AngleMul`, exact axis pivot, collision rotation/scale, draw-order/palette interaction, renderer parity, helper/redirect ownership, score movement, or full presentation parity.

Previous required runtime trace addendum: `synthetic-imported-anglemul.json` checksum `e0dae072` / final checksum `5048aa5c` proves bounded active imported static `AngleMul value` lowering. The route executes `AngleSet value = 30`, `AngleMul value = 1.5`, and `AngleDraw`, requires typed `sprite-effect:anglemul` operation evidence, and observes imported actor-frame `renderAngle = 45`. That aggregate was 448/448 artifacts, 418 required and 30 optional. This proves bounded static sprite-effect multiplication and telemetry only, not exact axis pivot, collision rotation/scale, draw-order/palette interaction, renderer parity, helper/redirect ownership, score movement, or full presentation parity.

Previous required runtime trace addendum: `synthetic-imported-angle-dynamic.json` checksum `8f788bf8` proves bounded active imported dynamic `AngleSet value`, `AngleAdd value`, and `AngleDraw value/scale` expression fallback. The route seeds `var(0)=40`, `var(1)=-10`, `var(2)=35`, `var(3)=2`, and `fvar(0)=0.5`, forbids typed `sprite-effect:angle*` operation evidence for dynamic params, and requires imported actor-frame/final `renderAngle = 35` plus `renderScale = 2,0.5`. That aggregate was 447/447 artifacts, 417 required and 30 optional. This proves bounded sprite-effect expression fallback and telemetry only, not typed lowering for dynamic angle params, exact axis pivot, collision rotation/scale, draw-order/palette interaction, renderer parity, helper/redirect ownership, score movement, or full presentation parity.

Previous projectile trace addendum: `synthetic-imported-projectile-projhit-multi-id.json` checksum `ab0f3fb3` and `synthetic-imported-projectile-projguarded-multi-id.json` checksum `023921e3` prove bounded player Projectile `ProjHit` / `ProjGuarded` multi-id arbitration. The gates spawn wrong-id/non-contact Projectile ids `8905` / `8907` and valid hit/guard Projectile ids `8906` / `8908`, forbid wrong-id trap states `367` / `370`, route owner through `200 -> 365 -> 366` and `200 -> 368 -> 369` via fixed-id, any-id, and ID `0` forms, and preserve two Projectile payloads, owner target-link ids `8906` / `8908`, hit/guard sounds, and FightFX spark package evidence. That checkpoint aggregate was 432/432 artifacts, 402 required and 30 optional. Previous multi-id ProjContact, any-id/fixed-id ProjHit/ProjGuarded/ProjContact, player/helper Projectile `ProjContact` fixed-id suffix/state-transition, helper reversed/guarded/hit-route `movehitpersist`, helper `hitcountpersist`, helper `hitdefpersist`, direct `hitdefpersist`, direct `movehitpersist`, direct `hitcountpersist`, Projectile attacker-side `HitCount` / `UniqHitCount`, Projectile `GetHitVar(hitcount)`, `hitid/chainid`, `damage/hittime/xvel/yvel`, guard slide-stop/control, default `guard.hittime/slidetime/ctrltime`, default `guard.velocity`, default/explicit `guard.cornerpush.veloff`, and default/explicit `down.cornerpush.veloff` gates remain required. This does not prove exact `ProjHit` / `ProjGuarded` / `ProjContact` tick order/lifetime, same-ID selection priority, helper Projectile/custom-state persistence breadth, Move* interaction breadth, Projectile/custom-state hitcountpersist breadth, exact reversal priority/target-state semantics, exact combo accumulation, chain-hit eligibility arbitration, multi-hit/multi-target/team counting, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, throws, teams/simul, score movement, or full Projectile parity.

`pnpm qa:trace` is the repeatable trace gate command. It writes artifacts to `.scratch/qa/trace-gates/`:

- `native-hit.json`: required native generated-roster hit route with hit combat reason evidence.
- `native-whiff.json`: required native generated-roster active-Clsn1 whiff route with inferred whiff combat reason evidence.
- `synthetic-imported-x.json`: required imported CMD/CNS State -1 `x -> 200` route with `HitDef`, typed `hitdef` operation, hit event, hit combat reason evidence, and a bounded `MoveHit` owner-state branch into state `261`.
- `synthetic-imported-movecontact.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact and a bounded `MoveContact` owner-state branch into state `262`. This proves direct contact-trigger plumbing only, not exact first-tick timing or lifetime parity.
- `synthetic-imported-movehit-counter.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact and a bounded `MoveHit >= 1` owner-state branch into state `263`. This proves direct contact-age plumbing only, not exact first-tick timing, hitpause accounting, or lifetime parity.
- `synthetic-imported-movehitreset.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact where typed `contact:movehitreset` clears direct contact memory before the later `MoveHit >= 1` branch can enter state `263`. Current checksum: `719c2cc7`. This proves bounded direct contact reset only, not projectile contact reset, exact first-tick timing, hitpause accounting, helper/redirect/team ownership, or lifetime parity.
- `synthetic-imported-hitcount.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact and bounded `HitCount >= 1 && UniqHitCount >= 1` owner-state branch into state `264`. This proves current two-actor hit-count plumbing only, not multi-target, helper-owned, projectile, combo-lifetime, or exact parity.
- `synthetic-imported-hitadd.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact where static `HitAdd value = 2` executes through typed `contact:hitadd`, then bounded `HitCount >= 3 && UniqHitCount = 1` routes the owner into state `265`. Current checksum: `910574ea`. This proves current-state direct hit-count addition only, not combo lifetime, guard/projectile counts, helper ownership, redirects, teams, or exact parity.
- `synthetic-imported-variable.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact where static `VarSet`, `VarAdd`, `VarRandom`, and `VarRangeSet` execute through typed `variable:*` operations, fill `var(7)`, `var(5)`, and `var(2..4)`, then route into state `288` through `var(...)` triggers. Current checksum: `3b33f7a8`. This proves bounded owner-local variable controller plumbing and deterministic sandbox-side random assignment only, not exact MUGEN random stream parity, exact variable scopes, helper/parent/root redirects, sysvar/fvar lifetime parity, IKEMEN map vars, dynamic lowering, or full CNS VM parity.
- `synthetic-imported-resource.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact where static `LifeAdd`, `LifeSet`, `PowerAdd`, and `PowerSet` execute through typed `resource:*` operations, mutate owner life/power, then route into state `289` through `Life` / `Power` triggers with actor-frame and final P1 evidence for life `750` and power `900`. Current checksum: `7bbcb2e4`. This proves bounded owner-local resource controller plumbing only, not exact resource scaling, helper/parent/root redirects, team modes, round/KO flow, dynamic lowering, or full CNS VM parity.
- `synthetic-imported-control.json`: required imported CMD/CNS State -1 `x -> 200` route where static `CtrlSet` executes twice through typed `resource:ctrlset` operation evidence and leaves final P1 owner control restored in state `200`. Current checksum: `80c4c446`. This proves bounded owner-local control flag plumbing only, not dynamic expressions, helper/redirect ownership, exact state-entry control timing, or full CNS control parity.
- `synthetic-imported-animation.json`: required imported CMD/CNS State -1 `x -> 200` route where `ChangeAnim` retargets the active AIR action to `205`, partial `ChangeAnim2` retargets to owner-backed action `206`, and actor-frame/final-animation evidence proves the state ends at `animNo = 206`. Current checksum: `ee651cbc`. This proves bounded owner-state animation retargeting only, not missing-action fallback, full `elem`/`elemtime` parity, redirects, helper/custom-state ownership, multi-import SFF namespaces, or exact MUGEN/IKEMEN animation-source parity.
- `synthetic-imported-changeanim2-elem.json`: required imported CMD/CNS State -1 `x -> 200` route where active-state `ChangeAnim2 value = 207`, `elem = 3`, and `elemtime = 1` retargets to a known state-owner AIR action with authored frame durations `[4,5,6]`; final P1 evidence ends at `frameIndex = 2` and `animTime = 20`. Current checksum: `b0b46d33`. This proves one bounded active-state owner-AIR element/time positioning route, not missing-action fallback, redirects, helper/custom-state ownership, multi-import SFF namespaces, exact controller tick order, or full animation-source parity.
- `synthetic-imported-sound.json`: required imported CMD/CNS State -1 `x -> 200` route where partial `PlaySnd`, `SndPan`, and `StopSnd` controllers emit bounded sound-event evidence for `S5,0` on channel `2`, including static `PlaySnd lowpriority = 1` as `lowPriority: true`, `volumescale = 50` as `volumeScale: 50`, legacy `volume = -8` as `legacyVolume: -8`, `freqmul = 0.5` as `freqMul: 0.5`, `loop = 1` as `loop: true`, `pan = 32` as `pan: 32`, a second static `PlaySnd value = S5,1`, `channel = 3`, `abspan = -64` probe as `absPan: -64`, and static `SndPan pan = -48`, through `RuntimeAudioWorld` and typed `audio:playsnd` / `audio:sndpan` / `audio:stopsnd` operation evidence. Current checksum: `cc9c8c49`. This proves runtime event telemetry, bounded explicit-channel arbitration, bounded `volumescale` gain handoff, legacy `volume` diagnostics, bounded `freqmul` playback-rate handoff, loop metadata, bounded static `pan` / `abspan` event handoff, and bounded active-channel `SndPan` handoff only, not SND decode coverage, Web Audio timing/mixing, SuperPause sound refs, exact panning semantics, pre-RC8 `volume` gain semantics, full priority classes, helper/redirect ownership, or exact MUGEN/IKEMEN audio parity.
- `synthetic-imported-sound-dynamic-pan.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `PlaySnd channel = var(1), pan = var(0)`, dynamic `SndPan channel = var(1), abspan = var(2)`, and dynamic `StopSnd channel = var(1)` resolve from owner-local vars, skip typed `audio:*` operation evidence because the params are dynamic, and expose sound-event telemetry for `PlaySnd` channel `2` pan `-24`, `SndPan` channel `2` absPan `64`, and `StopSnd` channel `2`. Current checksum: `24c0cce2`; final checksum: `dea16ed4`. This proves bounded dynamic audio numeric fallback only, not SuperPause sound refs, typed-operation lowering for dynamic audio params, exact Web Audio panning, channel priority/timing/mixing, helper/redirect ownership, or full audio parity.
- `synthetic-imported-sound-dynamic-value.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `PlaySnd value = Fvar(0),var(1)` resolves group/index from owner-local vars, skips typed `audio:*` operation evidence because the sound ref is dynamic, and exposes sound-event telemetry for group `5`, index `3`, channel `4`, and `soundPrefix = kfm`. Current checksum: `cd0bf458`; final checksum: `0ded35cd`. This proves bounded dynamic `PlaySnd value` fallback only, not SuperPause sound refs, typed-operation lowering for dynamic audio params, exact Web Audio archive lookup, channel priority/timing/mixing, helper/redirect ownership, or full audio parity.
- `synthetic-imported-hitdef-dynamic-hitsound.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `HitDef hitsound = Fvar(0),var(1)` resolves group/index from owner-local vars at HitDef activation, skips typed `audio:*` operation evidence because this is contact sound telemetry, and exposes attacker-side contact `PlaySnd` telemetry for group `5`, index `4`, raw `Fvar(0),var(1)`, and `soundPrefix = kfm`. Current checksum: `c891e888`; final checksum: `a0d1bbfc`. This proves bounded dynamic HitDef hitsound fallback only, not SuperPause sound refs, exact SND playback/archive lookup, channel priority/timing/mixing, helper/redirect ownership, or full audio parity.
- `synthetic-imported-hitdef-dynamic-guardsound.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `HitDef guardsound = Fvar(0),var(1)` resolves group/index from owner-local vars at HitDef activation, skips typed `audio:*` operation evidence because this is guard contact sound telemetry, and exposes attacker-side guard contact `PlaySnd` telemetry for group `6`, index `4`, raw `Fvar(0),var(1)`, `contactKind = guard`, and `soundPrefix = kfm`. Current checksum: `cb061b1c`; final checksum: `8d25e54e`. This proves bounded dynamic HitDef guardsound fallback only, not SuperPause sound refs, exact SND playback/archive lookup, channel priority/timing/mixing, helper/redirect ownership, or full audio parity.
- `synthetic-imported-superpause-sound.json`: required imported CMD/CNS route where dynamic `SuperPause sound = Svar(0),var(1)` resolves group/index from owner-local vars at pause start, skips typed `audio:*` operation evidence because this is pause-start sound telemetry, and exposes attacker-side `PlaySnd` telemetry for group `10`, index `0`, raw `Svar(0),var(1)`. Current checksum: `de3ac4b2`; final checksum: `6e227fe6`. This proves bounded dynamic SuperPause sound fallback only, not exact common/player SND archive lookup, channel priority/timing/mixing, super-background audio, helper/redirect ownership, or full audio parity.
- `synthetic-imported-noop.json`: required imported CMD/CNS State -1 `x -> 200` route where `Null`, browser no-op `ForceFeedback`, debug clipboard no-ops `DisplayToClipboard` / `AppendToClipboard` / `ClearClipboard`, deprecated dust presentation no-op `MakeDust`, and helper-lifecycle no-op `DestroySelf` execute before a simple `HitDef` route and remain visible as typed `noop:*` operation telemetry. Current checksum: `2877b222`. This proves bounded no-op controller acceptance/counting only for imported active player states, not device force feedback, debug text rendering, clipboard output, dust rendering, helper removal/lifecycle side effects from active states, helper/redirect ownership, exact no-op timing, or full CNS VM parity. Focused `EffectActorSystem` coverage separately proves `DestroySelf` removes only current visual Helper actors inside the bounded helper-local micro-VM.
- `synthetic-imported-receiveddamage.json`: required imported CMD/CNS State -1 `x -> 200` route with direct HitDef contact where the defender enters state `5000` and branches through bounded `ReceivedDamage > 0 && ReceivedHits >= 1` into state `280`. This proves current two-actor direct defender-local received-damage plumbing only, not guard chip, projectile damage, target-controller damage, helpers, teams, exact lifetime, hitpause accounting, or full MUGEN/IKEMEN parity.
- `synthetic-imported-projectile-receiveddamage.json`: required imported CMD/CNS State -1 `x -> 200` route with bounded Projectile contact where the defender enters state `5000` and branches through bounded `ReceivedDamage > 0 && ReceivedHits >= 1` into state `280`. This proves current two-actor projectile defender-local received-damage plumbing only, not projectile guard, target-controller damage, helpers, teams, exact lifetime, hitpause accounting, or full MUGEN/IKEMEN parity.
- `synthetic-imported-gethitvar-guarded.json`: required imported CMD/CNS route where P2 blocks a direct `HitDef`, executes defender-owned guard-hit `150 -> 151` with `HitVelSet` / `CtrlSet` typed operation evidence, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `302` before returning to idle/control. Current checksum: `7c36defb`. This proves bounded direct guard metadata survives into guard-hit CNS, not projectile/helper/custom-state guarded metadata, exact guard timing/effects, or full Common1/guard parity.
- `synthetic-imported-projectile-gethitvar-guarded.json`: required imported CMD/CNS route where P2 blocks a player-owned Projectile-only attack, executes defender-owned guard-hit `150 -> 151` with `HitVelSet` / `CtrlSet` typed operation evidence, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `303` before returning to idle/control. Current checksum: `a0104472`. This proves bounded player-owned Projectile guard metadata survives into guard-hit CNS, not helper-parented Projectile guarded metadata, custom-state guarded metadata, exact guard timing/effects, or full Common1/guard parity.
- `synthetic-imported-helper-projectile-gethitvar-guarded.json`: required imported CMD/CNS route where P2 blocks a helper-parented Projectile (`parentId = p1-helper-0`), executes defender-owned guard-hit `150 -> 151` with `HitVelSet` / `CtrlSet` typed operation evidence, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `304` before returning to idle/control. Current checksum: `2b413bd7`. This proves bounded helper-parented Projectile guard metadata survives into guard-hit CNS with helper/projectile lifecycle, payload, target-link evidence, and helper-local `Projectile` controller/op telemetry from helper state `1200`; it does not prove custom-state guarded metadata, exact guard timing/effects, or full Common1/guard parity.
- `synthetic-imported-gethitvar-fall-recover.json`: required imported CMD/CNS route where a recoverable fall `HitDef` sends P2 into owner-backed state `5100`, then `GetHitVar(fall.recover) = 1 && GetHitVar(fall.recovertime) > 0 && !CanRecover` routes P2 into state/action `301`. Current checksum: `259b300f`. This proves the bounded HitDef recovery-allowed flag is distinct from countdown-gated recovery availability, not exact recovery threshold tables, custom-state lifetime, helper/projectile inheritance, or full Common1/get-hit parity.
- `synthetic-imported-gethitvar-fall-metadata.json`: required imported CMD/CNS route where a fall `HitDef` sends P2 into owner-backed state `5100`, then `GetHitVar(fall.damage) = 70`, `GetHitVar(fall.kill) = 0`, `GetHitVar(fall.xvel) = 3`, and `GetHitVar(fall.yvel) = -6` route P2 into state/action `305` before `HitFallDamage` resolves. Current checksum: `474fa734`. This proves bounded stored direct-`HitDef` fall damage, kill, and velocity metadata reads during get-hit CNS, not exact fall metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, tick-order parity, or full Common1/get-hit parity.
- `synthetic-imported-hitoverride-guardflag-filter.json`: required imported CMD/CNS route where a direct `HitDef attr = S,NA` / `guardflag = H` hits a defender with attr-matching `HitOverride` slots `1 -> 776` using `guardflag.not = HA`, `2 -> 778` using `guardflag = A`, and `5 -> 779` using `guardflag = H`; slots `1` and `2` are skipped, slot `5` redirects to state/action `779`, target link `p1 -> p2 / 77` is recorded, and states `776`, `778`, `5000`, `150`, and `151` are forbidden. Current checksum: `b88a2da3`. This proves bounded direct-HitDef HitOverride guardflag filtering only, not custom-state guardflag inheritance/timing, custom-state inheritance/timing, forceair/forceguard/keepstate combinations, or full HitOverride parity.
- `synthetic-imported-p2stateno-guard-ignored.json`: required imported CMD/CNS route where a guarded direct `HitDef` declares `p2stateno = 888` / `p2getp1state = 1`, records target memory, and P2 still routes through defender-owned Common1-style `150 -> 151 -> 130 -> 20` while state `888` is forbidden. Current checksum: `76d1becd`. This proves bounded successful-guard `p2stateno` ignore behavior, not exact guard KO/no-KO round flow, broader HitOverride interaction beyond the separate miss oracle, helper/projectile `p2stateno` behavior, or full guard parity.
- `synthetic-imported-custom-state-gethitvar-guard-kill.json`: required imported CMD/CNS route where a guarded direct `HitDef` with `guard.kill = 0` records target memory, owner-local `TargetState` sends P2 into attacker-owned state data, then `GetHitVar(kill) = 0 && GetHitVar(guarded) = 1 && GetHitVar(hitshaketime) > 0` routes P2 `888 -> 904` before `SelfState` returns to idle/control. Current checksum: `c889a534`. This proves bounded stored direct-`HitDef` guard-kill metadata reads during attacker-owned custom-state CNS, not exact guard KO/no-KO round flow, helper/projectile custom-state guard kill inheritance, or full custom-state/guard parity.
- `synthetic-imported-helper-projectile-gethitvar-guard-kill.json`: required imported CMD/CNS route where a helper-parented Projectile (`parentId = p1-helper-0`) with `guard.kill = 0` sends P2 through defender-owned Common1-style states `150 -> 151`, then `GetHitVar(kill) = 0 && GetHitVar(guarded)` routes P2 into state/action `334` with helper-local `Projectile` controller/op telemetry, helper/projectile lifecycle evidence, and owner/helper target links for target id `8857`. Current checksum: `7f9aa699`. This proves bounded stored helper-parented Projectile guard-kill metadata reads during guard-hit CNS; direct-HitDef custom-state guard kill is covered separately by `synthetic-imported-custom-state-gethitvar-guard-kill.json`. It does not prove helper/projectile custom-state guard kill inheritance, exact helper Projectile guard KO/no-KO round flow, exact helper/projectile target lifetime/tick order, or full Common1/guard Projectile parity.
- `synthetic-imported-projectile-gethitvar-guard-kill.json`: required imported CMD/CNS route where a player-owned Projectile with `guard.kill = 0` sends P2 through defender-owned Common1-style states `150 -> 151`, then `GetHitVar(kill) = 0 && GetHitVar(guarded)` routes P2 into state/action `333`. Current checksum: `3feae5a7`. This proves bounded stored player-owned Projectile guard-kill metadata reads during guard-hit CNS; helper-parented Projectile guard kill is covered separately by `synthetic-imported-helper-projectile-gethitvar-guard-kill.json`, and direct-HitDef custom-state guard kill is covered separately by `synthetic-imported-custom-state-gethitvar-guard-kill.json`. It does not prove helper/projectile custom-state guard kill inheritance, exact guard KO/no-KO round flow, exact projectile target lifetime/tick order, or full Common1/guard Projectile parity.
- `synthetic-imported-gethitvar-air-guard-kill.json`: required imported CMD/CNS route where a direct air guard against `HitDef guard.kill = 0` sends P2 through defender-owned Common1-style states `154 -> 155`, then `GetHitVar(kill) = 0 && GetHitVar(guarded)` routes P2 into state/action `332`. Current checksum: `4382207e`. This proves bounded stored direct-`HitDef` air-guard kill metadata reads during guard-hit CNS, not exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact air guard timing/landing/proximity/effects, or full Common1/guard parity.
- `synthetic-imported-gethitvar-crouch-guard-kill.json`: required imported CMD/CNS route where a direct crouch guard against `HitDef guard.kill = 0` sends P2 through defender-owned Common1-style states `152 -> 153`, then `GetHitVar(kill) = 0 && GetHitVar(guarded)` routes P2 into state/action `331`. Current checksum: `2976fb8c`. This proves bounded stored direct-`HitDef` crouch-guard kill metadata reads during guard-hit CNS, not exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact guard timing/proximity/effects, or full Common1/guard parity.
- `synthetic-imported-gethitvar-guard-kill.json`: required imported CMD/CNS route where a direct stand guard against `HitDef guard.kill = 0` sends P2 through defender-owned Common1-style states `150 -> 151`, then `GetHitVar(kill) = 0 && GetHitVar(guarded)` routes P2 into state/action `330`. Current checksum: `abb4e468`. This remains the bounded stored direct-`HitDef` stand-guard kill metadata read during guard-hit CNS.
- `synthetic-imported-gethitvar-kill.json`: required imported CMD/CNS route where a normal `HitDef kill = 0` sends P2 into defender-owned Common1-style state `5000`, then `GetHitVar(kill) = 0 && !GetHitVar(guarded)` routes P2 into state/action `329`. Current checksum: `ef5ffabf`. This proves bounded stored direct-`HitDef` normal kill metadata reads during get-hit CNS, not KO/round-flow behavior, helper/projectile/custom-state inheritance, exact target lifetime/tick-order parity, or full Common1/get-hit parity.
- `synthetic-imported-gethitvar-fallcount.json`: required imported CMD/CNS route where a fall `HitDef` sends P2 into owner-backed state `5100`, `HitFallDamage` records one Common1-style ground impact and consumes stored `fall.damage`, then `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0` routes P2 into state/action `328`. Current checksum: `c391d938`. This proves one bounded post-impact fallcount read during get-hit CNS, not multi-ground-hit combo accumulation, non-Common1 impact detection, lifetime/reset parity, helper/projectile/custom-state inheritance, tick-order parity, or full fall/get-hit parity.
- `synthetic-imported-gethitvar-fall-envshake.json`: required imported CMD/CNS route where a fall `HitDef` sends P2 into owner-backed state `5100`, then `GetHitVar(fall.envshake.time) = 15`, `GetHitVar(fall.envshake.freq) = 178`, `GetHitVar(fall.envshake.ampl) = 6`, and `GetHitVar(fall.envshake.phase) = 0` route P2 into state/action `306` before `FallEnvShake` presentation executes. Current checksum: `6364632a`. This proves bounded stored direct-`HitDef` fall env-shake metadata reads during get-hit CNS, not exact camera waveform, pause/stage/layer interaction, metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, tick-order parity, or full fall presentation parity.
- `synthetic-imported-gethitvar-down-recover.json`: required imported CMD/CNS route where a fall `HitDef` sends P2 into owner-backed state `5100`, then `GetHitVar(down.recover) = 1`, `GetHitVar(down.recovertime) = 45`, and alias `GetHitVar(recovertime) = 45` route P2 into state/action `307` before lie-down recovery consumes the timer. Current checksum: `b8a7aef0`. This proves bounded stored direct-`HitDef` down-recovery flag/timer reads during get-hit CNS, not exact liedown tables, 5110/5120 tick order, metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, tick-order parity, or full Common1 recovery parity.
- `synthetic-imported-custom-state-gethitvar-down-recover.json`: required imported CMD/CNS route where a direct fall `HitDef` uses `p2stateno = 888` and `p2getp1state = 1`, sends P2 into P1-owned state data, branches `888 -> 893` through `GetHitVar(down.recover) = 1`, `GetHitVar(down.recovertime) = 45`, and alias `GetHitVar(recovertime) = 45`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState`. Current checksum: `5bd94568`. This proves bounded down-recovery metadata reads while executing attacker-owned custom state data, not exact lie-down tables, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, exact recovery threshold behavior, or full custom-state parity.
- `synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid.json`: required imported CMD/CNS route where a direct `HitDef` uses `p2stateno = 888`, `p2getp1state = 1`, `id = 77`, `chainID = 43`, and `numhits = 3`, sends P2 into P1-owned state data, branches `888 -> 900` through `GetHitVar(hitcount) = 3 && GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1` and target-link id `77`, and returns through `SelfState`. Current checksum: `250f77c2`. This proves bounded direct-hit numhits/id/chainID metadata reads while executing attacker-owned custom state data, not exact combo accumulation, chain-hit arbitration, helper/projectile inheritance, exact target lifetime, teams/simul, exact bind/tick order, or full custom-state parity.
- `synthetic-imported-custom-state-gethitvar-type.json`: required imported CMD/CNS route where a direct `HitDef` uses `p2stateno = 888`, `p2getp1state = 1`, `ground.type = Low`, and `air.type = Trip`, sends P2 into P1-owned state data, branches `888 -> 897` through `GetHitVar(type) = 2`, `GetHitVar(groundtype) = 2`, `GetHitVar(airtype) = 3`, and `!GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState`. Current checksum: `38542874`. This proves bounded direct-hit type metadata reads while executing attacker-owned custom state data, not exact get-hit animation selection, air-hit arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind tick-order, or full custom-state parity.
- `synthetic-imported-custom-state-gethitvar-yaccel.json`: required imported CMD/CNS route where a direct `HitDef` uses `p2stateno = 888`, `p2getp1state = 1`, and `yaccel = 0.62`, sends P2 into P1-owned state data, branches `888 -> 898` through `GetHitVar(yaccel) = .62`, `GetHitVar(hittime) > 0`, and `!GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState`. Current checksum: `549fe48d`. This proves bounded direct-hit yaccel metadata reads while executing attacker-owned custom state data, not exact physics integration, fall acceleration arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind tick-order, or full custom-state parity.
- `synthetic-imported-custom-state-gethitvar-guarded.json`: required imported CMD/CNS route where guarded direct `HitDef` target memory feeds typed owner-local `TargetState`, sends P2 into P1-owned state data, branches `888 -> 895` through `GetHitVar(guarded) = 1`, `GetHitVar(hitshaketime) > 0`, and `GetHitVar(hittime) > 0`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState`. Current checksum: `54f62821`. This proves bounded guarded target-memory-to-custom-state metadata reads while executing attacker-owned custom state data, not `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, or full custom-state parity.
- `synthetic-imported-custom-state-gethitvar-guard-timing.json`: required imported CMD/CNS route where guarded direct `HitDef` target memory feeds typed owner-local `TargetState`, sends P2 into P1-owned state data, branches `888 -> 901` through `GetHitVar(guarded) = 1`, `GetHitVar(slidetime) = 5`, `GetHitVar(ctrltime) = 7`, and `GetHitVar(hitshaketime) > 0`, preserves actor-frame `customOwnerId = p1` and target-link id `77`, and returns through `SelfState`. Current checksum: `ba77beec`. This proves bounded guarded slide/control timing metadata reads while executing attacker-owned custom state data, not `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, or full custom-state parity.
- `synthetic-imported-custom-state-gethitvar-isbound.json`: required imported CMD/CNS route where direct `HitDef` target memory feeds `TargetBind` before typed owner-local `TargetState`, sends P2 into P1-owned state data, branches `888 -> 896` through `GetHitVar(isbound) = 1`, positive `GetHitVar(hittime)`, and `!GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState`. Current checksum: `d25307e9`. This proves bounded TargetBind subject metadata reads while executing attacker-owned custom state data, not throws, exact bind tick-order/lifetime, visual bind parity, helper/root/parent redirects, teams/simul, `p2stateno`-on-guard behavior, or full custom-state parity.
- `synthetic-imported-custom-state-gethitvar-velocity.json`: required imported CMD/CNS route where a direct `HitDef` uses `p2stateno = 888`, `p2getp1state = 1`, and `ground.velocity = 4,-2`, sends P2 into P1-owned state data, branches `888 -> 894` through `GetHitVar(xvel) = 4`, `GetHitVar(yvel) = -2`, `!GetHitVar(fall)`, and `!GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, observes impact velocity in state `888`, and returns through `SelfState`. Current checksum: `e9d8da9e`. This proves bounded direct-hit velocity metadata reads while executing attacker-owned custom state data, not exact velocity lifetime after later physics/controllers, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, or full custom-state parity.
- `synthetic-imported-gethitvar-hitshaketime.json`: required imported CMD/CNS route where P2 takes a direct `HitDef`, enters defender-owned normal get-hit state `5000`, then branches through `GetHitVar(hitshaketime) > 0 && !GetHitVar(guarded)` into state/action `310`. Current checksum: `655107b9`. This proves bounded direct-contact normal hitshake timing metadata reads during get-hit CNS, not exact hitpause tick lifetime, hitshake lifetime, custom-state/helper/projectile inheritance breadth, tick-order parity, or full Common1 get-hit parity.
- `synthetic-imported-gethitvar-hittime.json`: required imported CMD/CNS route where P2 takes a direct `HitDef`, enters defender-owned normal get-hit state `5000`, then branches through `GetHitVar(hittime) > 0 && !GetHitVar(guarded)` into state/action `309`. Current checksum: `a11beef0`. This proves bounded direct-contact normal hit timing metadata reads during get-hit CNS, not exact hitstun tick order, hitshake lifetime, custom-state/helper/projectile inheritance breadth, tick-order parity, or full Common1 get-hit parity.
- `synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime.json`: required imported CMD/CNS route where P2 blocks a helper-parented Projectile while airborne and holding back, executes defender-owned air guard-hit `154 -> 155` with `HitVelSet` and `VelAdd` evidence, then branches from state `155` through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `317`. Current checksum: `3c3f2e25`. This proves bounded helper-parented Projectile air guard hitshake timing metadata reads during air guard-hit CNS with helper-local Projectile spawn telemetry plus helper/projectile lifecycle and owner/helper target-link evidence, not helper-owned custom states, exact helper Projectile air guard timing/landing/effects, projectile presentation, custom-state inheritance, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-projectile-gethitvar-air-guard-hitshaketime.json`: required imported CMD/CNS route where P2 blocks a player-owned Projectile while airborne and holding back, executes defender-owned air guard-hit `154 -> 155` with `HitVelSet` and `VelAdd` evidence, then branches from state `155` through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `316`. Current checksum: `3fcf1421`. This proves bounded player-owned Projectile air guard hitshake timing metadata reads during air guard-hit CNS, not exact air guard timing/landing/proximity, projectile presentation, custom-state inheritance, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-gethitvar-air-guard-hitshaketime.json`: required imported CMD/CNS route where P2 blocks a direct `HitDef` while airborne and holding back, executes defender-owned air guard-hit `154 -> 155` with `HitVelSet` and `VelAdd` evidence, then branches from state `155` through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `315`. Current checksum: `703e9328`. This proves bounded direct-`HitDef` air guard hitshake timing metadata reads during air guard-hit CNS, not exact air guard timing/landing/proximity, guard end/effects, helper/projectile air-guard variants, custom-state inheritance, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-gethitvar-crouch-guard-hitshaketime.json`: required imported CMD/CNS route where P2 blocks a direct `HitDef` while holding down-back, executes defender-owned crouch guard-hit `152 -> 153` with `HitVelSet` typed operation evidence, then branches from state `153` through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `314`. Current checksum: `b31d1dac`. This proves bounded direct-`HitDef` crouch guard hitshake timing metadata reads during crouch guard-hit CNS, not exact crouch guard timing/proximity, guard end/effects, helper/projectile crouch-guard variants, custom-state inheritance, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime.json`: required imported CMD/CNS route where P2 blocks a helper-parented Projectile, executes defender-owned guard-hit `150 -> 151` with `HitVelSet` / `CtrlSet` typed operation evidence, then branches from state `151` through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `313`. Current checksum: `64a1a8bd`. This proves bounded helper-parented Projectile guard hitshake timing metadata reads during guard-hit CNS plus helper/projectile lifecycle, payload, target-link, and helper-local `Projectile` telemetry, not helper-owned custom states, exact helper Projectile guard timing/effects, custom-state guarded metadata, projectile visual/audio parity, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-projectile-gethitvar-guard-hitshaketime.json`: required imported CMD/CNS route where P2 blocks a player-owned Projectile, executes defender-owned guard-hit `150 -> 151` with `HitVelSet` / `CtrlSet` typed operation evidence, then branches from state `151` through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `312`. Current checksum: `724f66d6`. This proves bounded player-owned Projectile guard hitshake timing metadata reads during guard-hit CNS, not custom-state guarded metadata, exact hitpause lifetime, guard timing/effects, projectile visual/audio parity, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-gethitvar-guard-hitshaketime.json`: required imported CMD/CNS route where P2 blocks a direct `HitDef`, executes defender-owned guard-hit `150 -> 151` with `HitVelSet` typed operation evidence, then branches from state `151` through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `311`. Current checksum: `31d76de9`. This proves bounded direct-`HitDef` guard hitshake timing metadata reads during guard-hit CNS, not exact hitpause lifetime, guard timing/proximity, guard end/effects, projectile/helper/custom-state inheritance, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-gethitvar-guard-timing.json`: required imported CMD/CNS route where P2 blocks a direct `HitDef`, executes defender-owned guard-hit `150 -> 151` with `HitVelSet` typed operation evidence, then branches from state `151` through `GetHitVar(hittime) > 0`, `GetHitVar(slidetime) = 5`, and `GetHitVar(ctrltime) = 7` into state/action `308`. Current checksum: `cf92c669`. This proves bounded direct-`HitDef` guard timing metadata reads during guard-hit CNS, not exact guard timing/proximity, guard end, guard effects, projectile/helper/custom-state inheritance, tick-order parity, or full Common1 guard parity.
- `synthetic-imported-hitdef-projectile-target-mix.json`: required imported CMD/CNS State -1 `x -> 200` route where direct `HitDef` id `77` and player-owned `Projectile` id `78` both hit P2 in one active state, MatchWorld exposes both owner target links, and P1 routes into state/action `278` through `NumTarget(77)`, `Target(77), Life`, `NumTarget(78)`, and `Target(78), Life`. Current checksum: `e98d4857`. This proves bounded mixed direct/projectile owner target-memory reads only, not Target* mutation mixing, helper-owned projectile targets, teams, multi-targets, exact target lifetime, or full target/combat parity.
- `synthetic-imported-projectile-contacttime-id.json`: required imported CMD/CNS State -1 `x -> 200` route where a player-owned Projectile contacts P2 and owner-state CNS routes through fixed-id `ProjContactTime(77) >= 1` into state/action `321`. Current checksum: `e9ebf36a`. This proves bounded fixed-id player Projectile contact-time reads only, not exact contact tick-order/lifetime, multi-projectile same-id selection, helper-owned Projectile routing, redirects, teams, or full Projectile timing parity.
- `synthetic-imported-projectile-guardedtime-id.json`: required imported CMD/CNS State -1 `x -> 200` route where P2 guards a player-owned Projectile and owner-state CNS routes through fixed-id `ProjGuardedTime(77) >= 1` into state/action `322`. Current checksum: `dfd08f28`. This proves bounded fixed-id player Projectile guarded-time reads only, not exact guard tick-order/lifetime, multi-projectile same-id selection, helper-owned Projectile routing, redirects, teams, or full Projectile timing parity.
- `synthetic-imported-projectile-target-controllers.json`: required imported CMD/CNS State -1 `x -> 200` route with a player-owned Projectile-only contact that records target id `77`, then executes owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` without direct HitDef controller evidence. Current checksum: `8c7bd6c2`. This proves bounded player Projectile Target side effects with final P1 `targetCount = 0` and P2 `life = 949` / `power = 40`, not Target* mutation mixing, teams, multi-targets, exact target lifetime, or full Projectile target parity.
- `synthetic-imported-projectile-default-target-controllers.json`: required imported CMD/CNS State -1 `x -> 200` route with a player-owned Projectile-only contact where omitted `projid` / `id` defaults target memory to id `0`, then executes owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` without direct HitDef controller evidence. Current checksum: `1c1a3e77`. This proves bounded player Projectile default Target side effects with target link `p1 -> p2 / 0`, projectile anim `915`, effect id `0`, TargetBind offset `36,-12`, final P1 `targetCount = 0`, and final P2 `life = 949` / `power = 40`, not Target* mutation mixing, teams, multi-targets, exact target lifetime, or full Projectile target parity.
- `synthetic-imported-projectile-targetstate.json`: required imported CMD/CNS State -1 `x -> 200` route with a player-owned Projectile-only contact that records target id `77`, then executes delayed owner-local `TargetState value = 888`. The gate requires owner target link `p1 -> p2 / 77`, projectile anim `913`, projectile spawn/remove lifecycle, effect payload `ownerId = p1` / `effectId = 77` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, executed `ChangeState`, `TargetState`, and `SelfState`, P2 custom-owner actor frames for states `888` and `889`, and final P2 state `0`/control with life `969`. Current checksum: `dd1c7962`. This proves bounded player Projectile TargetState into attacker-owned state data, not Target* mutation mixing, helper-owned custom state tables, throws, teams, multi-targets, exact target lifetime, exact final-animation parity, or full Projectile TargetState parity.
- `synthetic-imported-projectile-default-targetstate.json`: required imported CMD/CNS State -1 `x -> 200` route with a player-owned Projectile-only contact where omitted `projid` / `id` defaults target memory to id `0`, then executes delayed owner-local `TargetState value = 888`. The gate requires owner target link `p1 -> p2 / 0`, projectile anim `914`, projectile spawn/remove lifecycle, effect payload `ownerId = p1` / `effectId = 0` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, executed `ChangeState`, `TargetState`, and `SelfState`, P2 custom-owner actor frames for states `888` and `889`, and final P2 state `0`/control with life `969`. Current checksum: `8f35f1fa`. This proves bounded player Projectile default TargetState into attacker-owned state data, not Target* mutation mixing, helper-owned custom state tables, throws, teams, multi-targets, exact target lifetime, exact final-animation parity, or full Projectile default TargetState parity.
- `synthetic-imported-prevstateno.json`: required imported CMD/CNS route where an imported attack state records previous state number and branches through `PrevStateNo = 200`. This proves bounded previous-state-number plumbing only, not exact state-owner, redirect, helper/team, or tick-order parity.
- `synthetic-imported-prevmovetype.json`: required imported CMD/CNS route where an imported attack state records previous move type and branches through `PrevMoveType = A` from an intermediate state into state `270`. This proves bounded previous-movetype plumbing only, not exact state-owner, redirect, helper/team, or tick-order parity.
- `synthetic-imported-prevanim.json`: required imported CMD/CNS route where an imported attack state changes animation to `205`, records that previous animation during `ChangeState`, and branches through `PrevAnim = 205` from an intermediate state into state `276`. This proves bounded previous-animation plumbing only, not exact state-owner, redirect, helper/team, custom-state, or tick-order parity.
- `synthetic-imported-prevstatetype.json`: required imported CMD/CNS route where an imported airborne attack state records previous state type and branches through `PrevStateType = A` from an intermediate state into state `272`. This proves bounded previous-statetype plumbing only, not exact state-owner, redirect, helper/team, or tick-order parity.
- `synthetic-imported-identity.json`: required imported CMD/CNS State -1 route where `Name`, `P1Name`, `P2Name`, `AuthorName`, and `EnemyNear, AuthorName` all match the current two-actor runtime metadata before routing into state `276`. Current checksum: `c9be5cf1`. This proves bounded identity trigger plumbing only, not teams, multiple opponents, helper-owned identities, player-indexed identity selection, parent/root/target identity redirects, or exact MUGEN/IKEMEN string parity.
- `synthetic-imported-enemynear-index.json`: required imported CMD/CNS State -1 route where `EnemyNear(1), StateNo` is a forbidden trap and `EnemyNear(0), StateNo` routes P1 into state `284`. Current checksum: `b97e2eda`. This proves bounded one-on-one indexed EnemyNear trigger routing and fail-closed positive-index behavior only, not team/simul nth-nearest selection, helper-owned opponent lists, redirect mutation, dynamic index parity, or exact IKEMEN/MUGEN opponent selection semantics.
- `synthetic-imported-teamside.json`: required imported CMD/CNS State -1 route where `x` plus `TeamSide = 1` and `EnemyNear, TeamSide = 2` routes P1 into state/action `299` without `HitDef` / combat side effects. Current checksum: `f55695b7`. This proves bounded one-on-one side-context trigger plumbing only, not team/simul/turns, indexed opponent selection, helper-owned opponent lists, dynamic side ownership, or full MUGEN/IKEMEN `TeamSide` semantics.
- `synthetic-imported-p2-state-context.json`: required imported CMD/CNS State -1 route where `x` plus `P2StateType = S` and `P2MoveType = I` routes into state/action `296` without `HitDef` / combat side effects. Current checksum: `caf32557`. This proves bounded current-opponent metadata trigger plumbing only, not teams/simul, helpers, custom-state opponent ownership, exact opponent selection, persistent-controller parity, or full trigger parity.
- `synthetic-imported-p2-distance.json`: required imported CMD/CNS State -1 route where `x` plus `P2Dist X = 55`, `P2Dist Y = 0`, `P2BodyDist X = 7`, and `P2BodyDist Y = 0` routes into state/action `297` without `HitDef` / combat side effects. Current checksum: `2c584be0`. This proves bounded current-opponent spacing trigger plumbing only, not teams/simul, helpers, exact opponent selection, localcoord scaling, push/corner adjustment, persistent-controller parity, or full spacing parity.
- `synthetic-imported-selfstatenoexist.json`: required imported CMD/CNS State -1 route where `SelfStateNoExist(277)` accepts an existing own state and `!SelfStateNoExist(9999)` rejects a missing own state. This proves bounded own-state lookup only, not redirect, helper, parent/root, custom-state owner, or exact lookup parity.
- `synthetic-imported-selfanimexist.json`: required imported CMD/CNS State -1 route where `SelfAnimExist(200)` accepts an existing own AIR action and `!SelfAnimExist(9999)` rejects a missing own action before routing P1 into state `290`. Current checksum: `99930032`. This proves bounded own-animation lookup only, not redirected animation owners, helper/parent/root lookup, common/FightFX namespaces, or exact lookup parity.
- `synthetic-imported-animtime.json`: required imported CMD/CNS route where P1 enters state `200`, remains there for the bounded authored AIR action duration, then routes through active-state `AnimTime = 0` into state `291` without `HitDef` / combat side effects. Current checksum: `9e42b546`. This proves current actor animation-end trigger plumbing only, not exact AIR negative-duration semantics, looped-action semantics, state-owner/helper namespaces, persistent-controller timing, or full animation VM parity.
- `synthetic-imported-animelemtime.json`: required imported CMD/CNS route where P1 enters state `200`, advances through bounded authored AIR frame durations `[2,4,4]`, then routes through active-state `AnimElemTime(2) = 2` into state `292` without `HitDef` / combat side effects. Current checksum: `2036557d`. This proves current actor animation-element elapsed-time trigger plumbing only, not exact AIR loop semantics, invalid-element bottom values, negative-duration semantics, state-owner/helper namespaces, persistent-controller timing, or full animation VM parity.
- `synthetic-imported-edge-distance.json`: required imported CMD/CNS State -1 route where P1 presses `x`, evaluates `FrontEdgeDist = 340`, `BackEdgeDist = 300`, `FrontEdgeBodyDist = 301`, and `BackEdgeBodyDist = 261` against supplied close-training stage bounds, then routes into state/action `293` without `HitDef` / combat side effects. Current checksum: `785de452`. This proves current bounded stage edge-distance trigger plumbing only, not exact camera/screen edge parity, localcoord scaling, push/corner behavior, teams/simul/helper namespace breadth, or full edge-distance parity.
- `synthetic-imported-selfcommand.json`: required imported CMD/CNS State -1 route where `SelfCommand = "x"` reads the current owner command buffer and routes into state `278`. This proves bounded owner-command lookup only, not helper/team/redirect command ownership or exact IKEMEN/MUGEN command lookup parity.
- `synthetic-imported-stagetime.json`: required imported CMD/CNS State -1 route where `StageTime >= 3` reads the current match tick and routes into state `279`. This proves bounded global tick trigger plumbing only, not stage script, pause, replay, rollback, round-system, or exact timing parity.
- `synthetic-imported-gametime.json`: required imported CMD/CNS State -1 route where delayed `x` waits until `GameTime >= 4`, then routes into state/action `294` without `HitDef` / combat side effects. Current checksum: `bab573f3`. This proves bounded global game-time trigger plumbing only, not exact pause accounting, replay/rollback timing, multi-round timer ownership, IKEMEN round-system behavior, or full global timing parity.
- `synthetic-imported-state-context.json`: required imported CMD/CNS State -1 route where `x` plus `ctrl`, `StateType = S`, `MoveType = I`, and `Physics = S` routes into state/action `295` without `HitDef` / combat side effects. Current checksum: `cb9c3d1e`. This proves bounded owner-context trigger plumbing only, not helper/team/redirect metadata ownership, exact controller-loop ordering, persistent-controller parity, or full trigger parity.
- `synthetic-imported-round-ko.json`: required imported CMD/CNS State -1 `x -> 200` route where a lethal bounded `HitDef` drives sandbox round state to KO, `RuntimeTraceGate.requiredRoundFrames` requires KO/winner/message evidence, and final P2 life is `0`. Current checksum: `bfd5f073`. This proves only bounded `RoundSnapshot` trace visibility for KO, not exact MUGEN/IKEMEN KO slowdown, round transitions, lifebars, intros/winposes, teams, screenpacks, or timer parity.
- `synthetic-imported-round-timeover.json`: required mixed imported/demo route where a short `roundTimerFrames` fixture drives sandbox round state to `timeover`, `RuntimeTraceGate.requiredRoundFrames` requires draw/winner/message evidence, and observed timer reaches `0`. Current checksum: `7d9f7907`. This proves only bounded `RoundSnapshot` trace visibility for time-over/draw, not exact MUGEN/IKEMEN timer speed, round transitions, lifebars, intros/winposes, teams, screenpacks, or timeout parity.
- `synthetic-imported-reject.json`: required imported CMD/CNS State -1 `x -> 200` route where contact is rejected by `NotHitBy`, while still proving typed `hitdef` plus `eligibility:nothitby` operation execution. Current checksum: `5aca7dc0`. This proves the bounded deny-slot reject path only, not exact attr grammar, slot priority, helper/custom-state ownership, or full hit-eligibility timing parity.
- `synthetic-imported-hitby-allow.json`: required imported CMD/CNS State -1 `x -> 200` route where defender-side `HitBy value = S,NA` accepts a matching `HitDef attr = S,NA`, records typed `eligibility:hitby` operation evidence, resolves a real hit, and ends with P2 life `963`. Current checksum: `c75d5c7d`. This proves the bounded allow-slot accept path only, not exact attr grammar, slot priority, helper/custom-state ownership, or full hit-eligibility timing parity.
- `synthetic-imported-hitby-reject.json`: required imported CMD/CNS State -1 `x -> 200` route where defender-side `HitBy value = S,NT` rejects mismatched `HitDef attr = S,NA`, records typed `eligibility:hitby` operation evidence, emits reject event/combat-reason evidence, and ends with P2 life `1000`. Current checksum: `65185fd1`. This proves the bounded allow-slot mismatch reject path only, not exact attr grammar, slot priority, helper/custom-state ownership, or full hit-eligibility timing parity.
- `synthetic-imported-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where a defender-side `ReversalDef` with active `Clsn1` counters a matching incoming `HitDef`, records typed `reversaldef` operation evidence, emits reversal event/combat reason evidence, routes the defender into state `777`, and routes the attacker through bounded `MoveReversed >= 1` into state `778` after hitpause. This proves bounded counter/trigger plumbing only, not exact reversal priority, guard/projectile/helper/custom-state interactions, trigger lifetime, or full MUGEN/IKEMEN parity.
- `synthetic-imported-guarddist-reversal-no-contact.json`: required imported CMD/CNS `guard.dist` / `ReversalDef` no-contact route where a guardable direct `HitDef` has `guard.dist = 96`, P2 has active `ReversalDef p1stateno = 777` / `p2stateno = 888`, the stage is near enough for explicit `InGuardDist` guard-start but Clsn1 boxes do not contact, and the trace records typed `hitdef` / `reversaldef`, P2 state-0 `ReversalDef` controller evidence, whiff combat reason, final P2 state/action `130`, and forbidden states `777`, `888`, `5000`, `150`, and `151`. This proves bounded no-contact guard-distance reversal rejection only, not exact guard-distance boxes, positive proximity-only `guard.dist` ReversalDef contact, exact guard-start timing, custom-state breadth beyond direct routes, projectile reflection/removal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, or full ReversalDef/guard parity.
- `synthetic-imported-walkback-guard-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where a guardable direct `HitDef` contacts a defender holding back after entering authored walk state `20` with active `ReversalDef p1stateno = 777` / `p2stateno = 888`, records typed `hitdef` and `reversaldef` operation evidence, emits state-20 controller/actor-frame evidence with observed X movement, emits reversal event/combat reason evidence, forbids default get-hit `5000` and stand guard states `150` / `151`, routes P1 into state/action `888`, and routes P2 into state/action `777` at the reversal sample. This proves bounded authored walk-back state-20 direct reversal priority only, not exact guard-distance boxes, positive proximity-only `guard.dist` ReversalDef contact, crouch/air breadth, custom-state breadth beyond direct routes, projectile reflection/removal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, or full ReversalDef parity.
- `synthetic-imported-air-guard-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where an air-guardable direct `HitDef` contacts a no-walk-stabilized defender in state `40` with active `ReversalDef p1stateno = 777` / `p2stateno = 888`, records typed `assertspecial`, `hitdef`, and `reversaldef` operation evidence, emits state-40 controller/actor-frame evidence, emits reversal event/combat reason evidence, forbids default get-hit `5000`, stand guard states `150` / `151`, crouch guard states `152` / `153`, and air guard states `154` / `155`, routes P1 into state/action `888`, and routes P2 into state/action `777` at the reversal sample. This proves bounded air guard-input direct reversal priority only, not proximity-only `guard.dist` ReversalDef contact, exact guard-distance boxes, custom-state breadth beyond direct routes, projectile reflection/removal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, or full ReversalDef parity.
- `synthetic-imported-crouch-guard-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where a guardable direct `HitDef` contacts a no-walk-stabilized defender holding down-back in state `10` with active `ReversalDef p1stateno = 777` / `p2stateno = 888`, records typed `assertspecial`, `hitdef`, and `reversaldef` operation evidence, emits reversal event/combat reason evidence, forbids default get-hit `5000`, stand guard states `150` / `151`, and crouch guard states `152` / `153`, routes P1 into state/action `888`, and routes P2 into state/action `777` at the reversal sample. This proves bounded crouch guard-input direct reversal priority only, not proximity-only `guard.dist` ReversalDef contact, exact guard-distance boxes, custom-state breadth beyond direct routes, projectile reflection/removal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, or full ReversalDef parity.
- `synthetic-imported-guard-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where a guardable direct `HitDef` contacts a no-walk-stabilized defender holding back with active `ReversalDef p1stateno = 777` / `p2stateno = 888`, records typed `assertspecial`, `hitdef`, and `reversaldef` operation evidence, emits reversal event/combat reason evidence, forbids default get-hit `5000` and guard states `150` / `151`, routes P1 into state/action `888`, and routes P2 into state/action `777` at the reversal sample. This proves bounded stand guard-input direct reversal priority only.
- `synthetic-imported-custom-state-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where a direct `HitDef p2stateno = 889` / `p2getp1state = 1` contacts a defender-side active `ReversalDef p1stateno = 777` / `p2stateno = 888`, records typed `hitdef` and `reversaldef` operation evidence, emits reversal event/combat reason evidence, forbids state `889`, default get-hit `5000`, and guard states `150` / `151`, routes P1 into defender-owned custom state/action `888`, and routes P2 into state/action `777` at the reversal sample. This proves bounded direct custom-state reversal priority only, not custom-state breadth beyond this direct route, projectile reflection/removal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, or full ReversalDef parity.
- `synthetic-imported-projectile-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where a player-owned Projectile id `77` contacts a defender-side active `ReversalDef`, records typed `projectile` and `reversaldef` operation evidence, emits reversal event/combat reason evidence, routes the defender into state/action `777`, and routes the attacker into owner-backed state/action `888` at the initial reversal sample. This proves bounded player Projectile reversal priority only, not projectile reflection/removal after reversal, guard/custom-state counter breadth, exact attr grammar, hitpause/tick order, or full ReversalDef parity.
- `synthetic-imported-helper-projectile-reversal.json`: required imported CMD/CNS State -1 `x -> 200` route where a visual Helper spawns Projectile id `8878` with `ownerId = p1`, `rootId = p1`, and `parentId = p1-helper-0`; that Projectile contacts a defender-side active `ReversalDef`, records typed `helper`, `projectile`, and `reversaldef` operation evidence, emits reversal event/combat reason evidence, keeps helper target memory empty and the Projectile payload unconsumed, routes the defender into state/action `777`, and routes the attacker into owner-backed state/action `888` at the initial reversal sample. This proves bounded helper-parented/root-owned Projectile reversal priority only, not projectile reflection/removal after reversal, helper-owned custom-state tables, guard/custom-state counter breadth, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, or full ReversalDef parity.
- `synthetic-imported-damage-scale.json`: required imported CMD/CNS State -1 `x -> 200` route where static `AttackMulSet` and `DefenceMulSet` compile into typed `damage-scale:*` operations, execute before a `HitDef`, and produce final target life evidence for the bounded scaled-damage result. This proves current outgoing/incoming multiplier plumbing only, not exact MUGEN/IKEMEN scaling order across helpers, projectiles, guards, custom states, or round edge cases.
- `synthetic-imported-bounds.json`: required imported CMD/CNS State -1 `x -> 200` route where static `PosFreeze` and `ScreenBound` compile into typed `bounds:*` operations, execute during state `200`, and expose actor-frame `posFreeze` plus screen/camera-bound flags. This proves current one-frame bounds plumbing only, not exact camera, screen-edge, or tick-order parity.
- `synthetic-imported-screenbound-camera.json`: required imported CMD/CNS State -1 `x -> 200` route near the right stage bound where static `ScreenBound value = 0, movecamera = 0,1` plus static `PosAdd` lets P1 exceed `boundright` for the tick and excludes P1 from X camera centering. This proves the current bounded clamp/camera path only, not exact MUGEN/IKEMEN camera, zoom, screen-edge, or tick-order parity.
- `synthetic-imported-gravity.json`: required imported CMD/CNS State -1 `x -> 200` route where airborne state `200` executes the real `Gravity` controller through typed `kinematic:gravity` operation evidence and exposes vertical velocity telemetry. Current checksum: `92161764`. This proves bounded vertical acceleration only, not exact `yaccel`, floor snap, pause/tick order, or full air-physics parity.
- `synthetic-imported-kinematic.json`: required imported CMD/CNS State -1 `x -> 200` route where static `VelSet`, `VelAdd`, `VelMul`, `PosSet`, and `PosAdd` execute through typed `kinematic:*` operation evidence and expose bounded actor-frame position/velocity telemetry. Current checksum: `92804390`. This proves bounded static movement/position controller plumbing only, not dynamic expressions, exact physics/tick order, floor snapping, helper/custom-state ownership, or full movement parity.
- `synthetic-imported-width.json`: required imported CMD/CNS State -1 `x -> 200` route where static `Width player = 18,44` compiles into typed `collision:width`, executes during state `200`, and exposes actor-frame/final body-width evidence. Current checksum: `b1c2aab2`. This proves bounded body-width/push-separation plumbing only, not exact edge/player semantics, exact push overlap resolution, or tick-order parity.
- `synthetic-imported-width-dynamic.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `Width player = var(0),var(1)` resolves from owner-local vars, skips typed `collision:width` operation evidence because those params are dynamic, and exposes body-width telemetry `front = 21`, `back = 43`. Current checksum: `79baa5de`; final checksum: `395b0b1a`. This proves bounded dynamic Width fallback only, not dynamic typed lowering, edge width parity, exact push overlap, team/helper ownership, exact tick order, or full constraint parity.
- `synthetic-imported-statetypeset.json`: required imported CMD/CNS State -1 `x -> 200` route where static `StateTypeSet statetype = C, movetype = A, physics = N` compiles into typed `metadata:statetypeset`, executes during state `200`, and exposes actor-frame state metadata evidence. Current checksum: `bc3ec456`. This proves bounded metadata plumbing only, not exact physics/tick-order interactions or full MUGEN/IKEMEN state-type parity.
- `synthetic-imported-playerpush.json`: required imported CMD/CNS State -1 `x -> 200` route where static `PlayerPush value = 0` compiles into typed `collision:playerpush`, executes during state `200`, and exposes actor-frame `playerPush = false` evidence. Current checksum: `f775c46b`. This proves bounded body-push flag plumbing only, not exact overlap resolution, team/helper push rules, or tick-order parity.
- `synthetic-imported-playerpush-dynamic.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `PlayerPush value = var(0)` resolves from owner-local vars, skips typed `collision:playerpush` operation evidence because the param is dynamic, and exposes actor-frame/final `playerPush = false`. Current checksum: `13c5f954`; final checksum: `0627d0e5`. This proves bounded dynamic PlayerPush fallback only, not dynamic typed lowering, exact overlap resolution, team/helper ownership, exact tick order, or full constraint parity.
- `synthetic-imported-turn.json`: required imported CMD/CNS State -1 `x -> 200` route where static `Turn` compiles into typed `orientation:turn`, executes during state `200`, and exposes actor-frame `facing = -1` evidence. Current checksum: `9b7936e7`. This proves bounded facing-flip plumbing only, not exact auto-facing, tick-order, team/helper/target-facing parity, or full orientation semantics.
- `synthetic-imported-sprpriority.json`: required imported CMD/CNS State -1 `x -> 200` route where static `SprPriority value = 5` compiles into typed `sprite-effect:sprpriority`, executes during state `200`, and exposes actor-frame `spritePriority = 5` evidence. Current checksum: `2ea86059`. This proves bounded renderer-ordering telemetry only, not exact layer, shadow, helper, Explod, or draw-order parity.
- `synthetic-imported-palfx.json`: required imported CMD/CNS State -1 `x -> 200` route where static `PalFX` compiles into typed `sprite-effect:palfx`, executes during state `200`, and exposes actor-frame clamped material telemetry (`time = 18`, `add = 80,-10,255`, `mul.g = 160`, `color = 256`, `invert = true`). Current checksum: `8208535e`. This proves bounded material telemetry only, not exact palette math, blending, `sinadd`, remap interaction, or timing parity.
- `synthetic-imported-trans.json`: required imported CMD/CNS State -1 `x -> 200` route where static `Trans trans = addalpha,128,128` compiles into typed `sprite-effect:trans`, executes during state `200`, and exposes actor-frame render-opacity telemetry (`opacity <= 0.5`). Current checksum: `020c52c7`. This proves bounded opacity telemetry only, not exact add/sub alpha math, palette/remap interaction, draw-order interaction, or timing parity.
- `synthetic-imported-angle.json`: required imported CMD/CNS State -1 `x -> 200` route where static `AngleSet value = 45`, `AngleAdd value = 10`, and `AngleDraw` compile into typed `sprite-effect:angleset`, `sprite-effect:angleadd`, and `sprite-effect:angledraw`, execute during state `200`, and expose actor-frame render-angle telemetry (`angle = 55`). Current checksum: `127250a6`. This proves bounded sprite-quad rotation telemetry/rendering only, not exact axis pivot, collision-box rotation, dynamic angle expressions, draw-order/scale/palette interaction, or timing parity.
- `synthetic-imported-anglemul.json`: required imported CMD/CNS State -1 `x -> 200` route where static `AngleSet value = 30`, `AngleMul value = 1.5`, and `AngleDraw` compile into typed `sprite-effect:angleset`, `sprite-effect:anglemul`, and `sprite-effect:angledraw`, execute during state `200`, and expose actor-frame render-angle telemetry (`angle = 45`). Current checksum: `e0dae072`; final checksum: `5048aa5c`. This proves bounded static sprite-quad angle multiplication telemetry/rendering only, not dynamic typed lowering, exact axis pivot, collision-box rotation, draw-order/scale/palette interaction, or timing parity.
- `synthetic-imported-anglemul-dynamic.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `AngleSet value = var(0)` and `AngleMul value = fvar(0)` resolve from owner-local variables, skip typed `sprite-effect:angleset` / `sprite-effect:anglemul` operation evidence because those params are dynamic, and expose actor-frame render-angle telemetry (`angle = 45`) through static `AngleDraw`. Current checksum: `418ed880`; final checksum: `4a6a3045`. This proves bounded dynamic AngleMul fallback only, not dynamic typed lowering, exact axis pivot, collision-box rotation, draw-order/scale/palette interaction, or timing parity.
- `synthetic-imported-angle-dynamic.json`: required imported CMD/CNS State -1 `x -> 200` route where active dynamic `AngleSet value`, `AngleAdd value`, and `AngleDraw value/scale` expressions resolve from owner-local vars/fvars, skip typed `sprite-effect:angle*` operation evidence because params are dynamic, and expose actor-frame/final render telemetry (`angle = 35`, `scale = 2,0.5`). Current checksum: `8f788bf8`; final checksum: `6122b59e`. This proves bounded dynamic sprite-effect fallback only, not dynamic typed lowering, exact axis pivot, collision-box rotation/scale, draw-order/palette interaction, or timing parity.
- `synthetic-imported-envcolor.json`: required imported CMD/CNS State -1 `x -> 200` route where static `EnvColor value = 16,96,255`, `time = 12`, `under = 0` compiles into typed `envcolor`, executes during state `200`, and exposes stage-frame color/opacity telemetry through `RuntimeEnvColorWorld`. Current checksum: `956b0f4b`. This proves bounded stage-flash telemetry/rendering only, not exact blend math, stage layer/window behavior, pause timing, or presentation parity.
- `synthetic-imported-envcolor-dynamic.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `EnvColor value = var(0),var(1),var(2)`, `time = var(3)`, and `under = var(4)` resolve from owner-local vars, skip typed `envcolor` operation evidence because those params are dynamic, and expose stage-frame color/layer telemetry `32,128,240` / `under = true`. Current checksum: `dbe548a7`; final checksum: `2ff8dd42`. This proves bounded dynamic EnvColor fallback only, not dynamic typed lowering, exact blend math, layer/window behavior, pause timing, renderer parity, or full presentation parity.
- `synthetic-imported-envshake.json`: required imported CMD/CNS State -1 `x -> 200` route where static `EnvShake time = 16`, `freq = 30`, `ampl = -7`, and `phase = 0.5` lowers into typed `envshake` operation evidence and emits runtime shake-event evidence from state `200`. Current checksum: `061f17d5`. This proves bounded typed camera-shake event telemetry only, not dynamic params, `mul`, exact pause/stage/layer interaction, helper/redirect ownership, or waveform parity.
- `synthetic-imported-envshake-dynamic.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `EnvShake time = var(0)`, `freq = var(1)`, `ampl = var(2)`, and `phase = fvar(0)` resolve from owner-local vars/fvars, skip typed `envshake` operation evidence because those params are dynamic, and emit runtime shake-event telemetry `time = 18`, `freq = 45`, `ampl = -9`, and `phase = 0.25`. Current checksum: `90955e75`; final checksum: `1af5fbf6`. This proves bounded dynamic EnvShake fallback only, not dynamic typed lowering, `mul`, exact camera waveform, pause/stage/layer interaction, helper ownership, or screenpack parity.
- `synthetic-imported-remappal.json`: required imported CMD/CNS State -1 `x -> 200` route where static `RemapPal source = 1,1` / `dest = 2,3` compiles into typed `sprite-effect:remappal`, executes during state `200`, and exposes actor-frame palette-remap telemetry. Current checksum: `0bd6615d`. This proves bounded telemetry only, not ACT/SFF pixel remapping, palette application, PalFX interaction, or timing parity.
- `synthetic-imported-palfx-remappal.json`: required imported CMD/CNS State -1 `x -> 200` route where static `PalFX` and static `RemapPal` execute on the same imported actor/state, require typed `sprite-effect:palfx` and `sprite-effect:remappal` operation evidence, and expose combined actor-frame palette telemetry. Current checksum: `ba5fc1e6`. This proves bounded same-actor palette telemetry only, not ACT/SFF pixel remapping, exact palette math, `sinadd`, blend order, renderer parity, or timing parity.
- `synthetic-imported-afterimage.json`: required imported CMD/CNS State -1 `x -> 200` route where static `AfterImage` and `AfterImageTime` compile into typed `sprite-effect:afterimage` / `sprite-effect:afterimagetime`, execute during state `200`, and expose actor-frame ghost-trail telemetry (`time = 20`, `length = 4`, `timegap = 2`, `framegap = 1`, sample count at least `1`, opacity `0.34`). Current checksum: `a254802b`. This proves bounded trail telemetry only, not exact blend modes, palette/remap interaction, sampling cadence, persistence edge cases, or timing parity.
- `synthetic-imported-hitdef-priority.json`: required imported CMD/CNS route where both imported players activate direct `HitDef` attacks on the same tick, the higher numeric `priority` wins the bounded clash, the lower-priority direct attack is consumed before it can deal damage, and final life/event evidence proves only the winner hits. This proves only the current direct-attack priority subset, not exact MUGEN/IKEMEN priority classes, reversal/projectile/helper/custom-state priority, or multi-hit priority.
- `synthetic-imported-hitdef-kill.json`: required imported CMD/CNS route where a lethal direct `HitDef` with `kill = 0` leaves the defender at life `1`. This proves only the bounded nonlethal direct-hit clamp, not exact KO, round, no-KO, helper, projectile, or custom-state semantics.
- `synthetic-imported-projectile-guard-kill.json`: required imported CMD/CNS route where a player-owned Projectile with `guard.kill = 0` and `damage = 31,2000` clamps the guarded defender at life `1`, preserves Projectile target-link evidence, and emits no round KO frame. This proves only bounded player-owned Projectile guard-chip no-KO, not exact KO slowdown/lifebar/guard-finish timing, exact no-KO guard recovery timing, team/simul breadth, effect terminal timing, or full MUGEN/IKEMEN Projectile guard round-flow parity.
- `synthetic-imported-helper-projectile-guard-kill.json`: required imported CMD/CNS route where a helper-parented/root-owned Projectile with `guard.kill = 0` and `damage = 18,2000` clamps the guarded defender at life `1`, preserves owner/helper target-link evidence, and emits no round KO frame. This proves only bounded helper-parented/root-owned Projectile guard-chip no-KO, not exact KO slowdown/lifebar/guard-finish timing, exact no-KO guard recovery timing, helper-owned custom states, team/simul breadth, effect terminal timing, or full MUGEN/IKEMEN helper Projectile guard round-flow parity.
- `synthetic-imported-helper-projectile-guard-ko.json`: required imported CMD/CNS route where a helper-parented/root-owned Projectile with default `guard.kill` and `damage = 18,2000` leaves the guarded defender at life `0`, preserves owner/helper target-link evidence, and emits round KO winner/message evidence. This proves only bounded helper-parented/root-owned Projectile guard-chip KO, not exact KO slowdown, lifebar behavior, guard-finish timing, helper-owned custom states, team/simul breadth, effect terminal timing, or full MUGEN/IKEMEN helper Projectile guard round-flow parity.
- `synthetic-imported-projectile-guard-ko.json`: required imported CMD/CNS route where a player-owned Projectile with default `guard.kill` and `damage = 31,2000` leaves the guarded defender at life `0` and emits round KO winner/message evidence. This proves only bounded player-owned Projectile guard-chip KO, not exact KO slowdown, lifebar behavior, guard-finish timing, helper Projectile guard KO/no-KO, effect terminal timing, or full MUGEN/IKEMEN projectile guard round-flow parity.
- `synthetic-imported-hitdef-guard-ko.json`: required imported CMD/CNS route where a lethal guarded `HitDef` with default `guard.kill` and `guard.damage = 2000` leaves the defender at life `0` and emits round KO winner/message evidence. This proves only bounded default direct guard-chip KO, not exact KO slowdown, lifebar behavior, guard-finish timing, helper/projectile guard KO, or full MUGEN/IKEMEN guard round-flow parity.
- `synthetic-imported-hitdef-guard-kill.json`: required imported CMD/CNS route where a lethal guarded `HitDef` with `guard.kill = 0` leaves the defender at life `1` while still producing guard evidence. This proves only the bounded nonlethal chip clamp, not exact guard KO/round, guard effects, helper/projectile guard, or full MUGEN/IKEMEN parity.
- `synthetic-imported-basic-movement.json`: required imported input-control route where scripted directions drive P1 through bounded walk `20`, crouch `10`, jump `40`, and final idle/control `0`. Current checksum: `917ff3e5`. This proves only current sandbox movement routing, not exact Common1 movement, CMD priority, landing, collision, AI, visual parity, or full movement parity.
- `synthetic-imported-assertspecial-control.json`: required imported CMD/CNS route where static `AssertSpecial` flags `NoAutoTurn`, `NoWalk`, and `Invisible` lower into typed `assertspecial` operation evidence, hold facing before automatic facing, suppress walk drift, and expose render-opacity telemetry. Current checksum: `144ac7bc`. This proves only bounded typed control-flag telemetry, not global flag behavior, helper/team ownership, pause-layer lifetimes, flag priority, or full MUGEN/IKEMEN `AssertSpecial` parity.
- `synthetic-imported-assertspecial-timerfreeze.json`: required imported CMD/CNS route where actor-local `AssertSpecial TimerFreeze` lowers into typed `assertspecial` operation evidence and freezes the current active fight timer. Current checksum: `408528f1`. The gate holds a 61-frame fight timer at displayed timer `2` for 70 active frames with `state = fight` and `message = Fight`. This proves only bounded current P1/P2 active-match timer freeze, not exact global/team/helper ownership, pause-layer interaction, lifebar display, intro/round-transition timing, timer speed, or full MUGEN/IKEMEN timer parity.
- `synthetic-imported-assertspecial-round-flow-telemetry.json`: required imported CMD/CNS route where official `AssertSpecial Intro` and `NoKOSlow` lower into typed `assertspecial` operation evidence and final actor `assertSpecialGlobalFlags` telemetry. Current checksum: `10f95bdb`. This proves only bounded official flag telemetry, not intro state ownership, KO slow-motion suppression, winpose/round transitions, helper/team/global ownership, pause-layer interaction, or full MUGEN/IKEMEN round-flow parity.
- `synthetic-imported-assertspecial-noko.json`: required imported CMD/CNS route where defender-side `AssertSpecial NoKO` clamps lethal direct `HitDef` damage to life `1`, and ordered controller-event evidence proves P2 state `0` `Passive AssertSpecial` executes before P1 state `200` lethal `HitDef`. Current checksum: `f2f60521`. This proves only the bounded defender-side NoKO clamp and minimum flag-before-hit order, not exact round flow, KO flags, helpers, projectiles, custom states, shadows, or full MUGEN/IKEMEN no-KO parity.
- `synthetic-imported-target-noko.json`: required imported CMD/CNS target-memory route where defender-side `AssertSpecial NoKO` clamps lethal `TargetLifeAdd` target-controller damage to life `1`, and ordered controller-event evidence proves P2 state `0` `Passive AssertSpecial` executes before P1 state `200` `HitDef` and later `TargetLifeAdd`. Current checksum: `321a1eba`. This proves only the bounded target-controller NoKO clamp plus target-link id `77` evidence, not exact NoKO lifetime, round flow, helper/root/parent redirects, teams, multi-target behavior, or full MUGEN/IKEMEN target/no-KO parity.
- `synthetic-imported-assertspecial-unguardable.json`: required imported CMD/CNS route where attacker-side `AssertSpecial Unguardable` turns held-back defender contact into hit evidence instead of guard evidence. Current checksum: `e84aa12d`. The `pnpm qa:trace` coverage contract now also requires this artifact to appear in required coverage summaries, not only in the artifact list. This proves only bounded attacker-side unguardable guard denial, not exact priority, lifetime/persistence, pause layering, helper/team/global ownership, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-assertspecial-nogetupfromliedown.json`: required imported CMD/CNS route where defender-side `AssertSpecial NoGetUpFromLieDown` executes in Common1-style state `5110`, lowers into typed `assertspecial` evidence, counts `down.recovertime` to `0`, forbids the hardcoded `5120` get-up transition, and leaves final P2 in `5110`. Current checksum: `4c3b6281`; final checksum: `87533e25`. This proves only bounded hardcoded lie-down get-up suppression, not exact controller-loop timing, public KFM data, global/team/helper ownership, pause layering, or full recovery parity.
- `synthetic-imported-assertspecial-nofastrecoverfromliedown.json`: required imported CMD/CNS route where defender-side `AssertSpecial NoFastRecoverFromLieDown` executes in Common1-style state `5110`, lowers into typed `assertspecial` evidence, keeps fresh `recovery` command input active, suppresses the bounded hardcoded fast lie-down shortcut while `down.recovertime` counts from positive to `0`, then lets `5110 -> 5120 -> 0` execute. Current checksum: `74bf5d85`; final checksum: `e70201f3`. This proves only bounded synthetic fast lie-down shortcut suppression, not exact mashing thresholds, controller-loop timing, public KFM data, global/team/helper ownership, pause layering, or full recovery parity.
- `synthetic-imported-default-liedown-fast-recovery.json`: required imported CMD/CNS route where the defender reaches Common1-style state `5110` with positive `down.recovertime`, keeps fresh `recovery` command input active, omits the authored `5110` `Get Up` `ChangeState`, and still routes `5110 -> 5120 -> 0` through the bounded runtime fast-recovery shortcut. Current checksum: `74bdac97`; final checksum: `41f481dc`. This proves only bounded synthetic positive fast lie-down recovery, not exact mashing thresholds, controller-loop timing, public KFM data, global/team/helper ownership, pause layering, or full recovery parity.
- `synthetic-imported-assertspecial-guarddeny.json`: required imported CMD/CNS route where defender-side `AssertSpecial NoWalk` + `NoStandGuard` denies standing guard for a held-back defender, ordered controller-event evidence proves P1 state `0` `Passive AssertSpecial` executes before P2 state `200` guardable `HitDef`, records hit event/combat reason evidence, avoids guard evidence, and leaves final defender life at `963`. Current checksum: `fd2eb239`. This proves only bounded defender-owned stand-guard denial plus minimum flag-before-hit order, not broader AssertSpecial lifetime/persistence layering, global/team/helper ownership, pause layering, priority, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-assertspecial-crouch-guarddeny.json`: required imported CMD/CNS route where defender-side `AssertSpecial NoCrouchGuard` in crouch state `10` denies crouch guard for a held down-back defender, ordered controller-event evidence proves P1 state `10` `AssertSpecial Control Flags` executes before P2 state `200` guardable `HitDef`, records hit event/combat reason evidence, avoids guard evidence, and leaves final defender life at `963`. Current checksum: `757cb87a`. This proves only bounded defender-owned crouch-guard denial plus minimum flag-before-hit order, not exact guard priority, lifetime, helper/team/global ownership, pause layering, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-assertspecial-air-guarddeny.json`: required imported CMD/CNS route where defender-side `AssertSpecial NoAirGuard` in airborne state `40` denies air guard for a held-back defender, ordered controller-event evidence proves P1 state `40` `AssertSpecial Control Flags` executes before P2 state `200` guardable `HitDef`, records hit event/combat reason evidence, avoids guard evidence, and leaves final defender life at `963`. Current checksum: `810a1679`. This proves only bounded defender-owned air-guard denial plus minimum flag-before-hit order, not exact air-guard physics, landing, lifetime, helper/team/global ownership, pause layering, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-assertspecial-lifetime.json`: required imported CMD/CNS route where defender-side `AssertSpecial NoStandGuard` is gated by `Time < 1`, expires, and later held-back contact resolves as a guard with final defender life `995`. Current checksum: `7d359a76`. This proves only bounded per-tick flag reset/trigger expiry for this owner-state path, not broad AssertSpecial persistence, helper/team/global ownership, pause layering, priority, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-guard.json`: required imported CMD/CNS State -1 `x -> 200` route where held-back defender input turns real `HitDef` contact into guard event/combat reason evidence and evaluates a bounded `MoveGuarded` branch back in the owner state. This proves guard contact-trigger plumbing only, not exact guard timing, pause timing, or full MUGEN/IKEMEN contact-trigger parity.
- `synthetic-imported-hitdef-guard-sound.json`: required imported CMD/CNS State -1 `x -> 200` route where held-back guard contact emits bounded attacker-side `PlaySnd` telemetry from `guardsound = S6,0`. Current checksum: `fdf1f7f6`. This proves HitDef guard-sound event plumbing only, not exact SND playback, FightFX/common sound fallback, channel priority, audio timing/mixing, guard sparks, or full guard-effect parity.
- `synthetic-imported-hitdef-hit-sound.json`: required imported CMD/CNS State -1 `x -> 200` route where direct hit contact emits bounded attacker-side `PlaySnd` telemetry from `hitsound = S5,0`. Current checksum: `6fc00d8a`. This proves HitDef hit-sound event plumbing only, not exact SND playback, FightFX/common sound fallback, channel priority, audio timing/mixing, hit sparks, or full hit-effect parity.
- `synthetic-imported-hitdef-hit-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where direct hit contact emits bounded attacker-side `HitSpark` telemetry from `sparkno = S7001` plus `sparkxy = 10,-72`. Current checksum: `b6554124`. This proves HitDef hit-spark event plumbing only, not exact package-backed common/FightFX sprite lookup, visual binding, render timing, layering, scale, palette, or full hit-effect parity.
- `synthetic-imported-hitdef-data-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where direct hit contact emits bounded attacker-side `HitSpark` telemetry from omitted `sparkno` inheriting parsed CNS `[Data] sparkno = 2`, plus `sparkxy = -8,-54`. Current checksum: `d950796c`. This proves HitDef data-default hit-spark fallback plumbing only, not exact common/FightFX lookup, visual binding, render timing, layering, scale, palette, or full hit-effect parity.
- `synthetic-imported-hitdef-guard-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where held-back guard contact emits bounded attacker-side `HitSpark` telemetry from `guard.sparkno = S7000` plus `sparkxy = 12,-64`. Current checksum: `72c8fa3a`. This proves HitDef guard-spark event plumbing only, not exact package-backed common/FightFX sprite lookup, visual binding, render timing, layering, scale, palette, or full guard-effect parity.
- `synthetic-imported-hitdef-data-guard-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where held-back guard contact emits bounded attacker-side `HitSpark` telemetry from omitted `guard.sparkno` inheriting parsed CNS `[Data] guard.sparkno = 40`, plus `sparkxy = 13,-57`. Current checksum: `7bd9d162`. This proves HitDef data-default guard-spark fallback plumbing only, not exact common/FightFX lookup, visual binding, render timing, layering, scale, palette, or full guard-effect parity.
- `synthetic-imported-hitdef-common-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where direct hit contact emits bounded attacker-side `HitSpark` telemetry from unprefixed `sparkno = 7001` and requires resolved common/default AIR metadata (`assetSource = common`, action `7001`, first frame `0`, sprite `7101,0`, at least 2 asset frames, total authored duration at least `11`, frame indices `[0, 1]`). Current checksum: `5ea054d7`. This proves bounded multi-frame trace metadata for a supplied common/default spark library only, not exact common sprite lookup, render timing, layering, scale, palette, motif ownership, or full hit-effect parity.
- `synthetic-imported-hitdef-fightfx-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where direct hit contact emits bounded attacker-side `HitSpark` telemetry from `sparkno = F7002` and requires resolved FightFX AIR metadata (`assetSource = fightfx`, action `7002`, first frame `0`, sprite `8102,0`, at least 2 asset frames, total authored duration at least `11`, frame indices `[0, 1]`). Current checksum: `11537b56`. This proves bounded multi-frame trace metadata for a supplied FightFX spark library only, not exact FightFX sprite lookup, render timing, layering, scale, palette, motif ownership, or full hit-effect parity.
- `synthetic-imported-hitdef-common-guard-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where guarded contact emits bounded attacker-side `HitSpark` telemetry from unprefixed `guard.sparkno = 7003` and requires resolved common/default AIR metadata (`assetSource = common`, action `7003`, first frame `0`, sprite `7103,0`, at least 2 asset frames, total authored duration at least `11`, frame indices `[0, 1]`). Current checksum: `7650a09c`. This proves bounded common/default guard-spark metadata only, not exact visual timing, layering, scale, palette, motif ownership, or full guard-effect parity.
- `synthetic-imported-hitdef-fightfx-guard-spark.json`: required imported CMD/CNS State -1 `x -> 200` route where guarded contact emits bounded attacker-side `HitSpark` telemetry from `guard.sparkno = F7004` and requires resolved FightFX AIR metadata (`assetSource = fightfx`, action `7004`, first frame `0`, sprite `8104,0`, at least 2 asset frames, total authored duration at least `11`, frame indices `[0, 1]`). Current checksum: `32f3e92d`. This proves bounded FightFX guard-spark metadata only, not exact visual timing, layering, scale, palette, motif ownership, or full guard-effect parity.
- `synthetic-imported-hitdef-hit-effect-package.json`: required imported CMD/CNS State -1 `x -> 200` route where one direct hit contact emits bounded attacker-side `PlaySnd` telemetry from `hitsound = F5,0` with `soundPrefix = kfm` and bounded attacker-side FightFX `HitSpark` telemetry from `sparkno = F7002` with selected-frame/multi-frame AIR metadata. Current checksum: `46aa5ce1`. This proves combined prefixed hit sound + FightFX hit-spark evidence plus selected AIR-frame offset/duration metadata on the same contact only, not exact event ordering inside the tick, channel priority/mixing/timing, visual timing, layering, scale, palette, motif ownership, or full hit-effect parity.
- `synthetic-imported-hitdef-guard-effect-package.json`: required imported CMD/CNS State -1 `x -> 200` route where one guarded contact emits bounded attacker-side `PlaySnd` telemetry from `guardsound = S6,0` and bounded attacker-side FightFX `HitSpark` telemetry from `guard.sparkno = F7004` with selected-frame/multi-frame AIR metadata. Current checksum: `1c3167b7`. This proves combined guard sound + FightFX guard-spark evidence plus selected AIR-frame offset/duration metadata on the same contact only, not exact event ordering inside the tick, SND playback, channel priority, visual timing, layering, scale, palette, motif ownership, or full guard-effect parity.
- `pnpm qa:smoke` runtime route now presses `KeyA`, samples `HitSparkRenderer` diagnostics as soon as the spark becomes active, then captures screenshots; it requires an active resolved-sprite spark after the native hit route on desktop and mobile. Focused tests cover bounded common/FightFX system lookup-frame creation, optional `fight.def` / FightFX AIR/SFF package loading, decoded system-SFF provider registration, and preservation of runtime-provided package-backed common/FightFX frames. This proves bounded visible rendering for recent snapshot `HitSpark` events and the package-frame handoff contract, not exact imported system-asset parity.
- `synthetic-imported-inguarddist.json`: required imported CMD/CNS route where a near-but-not-contacting attack satisfies `InGuardDist`, parses/respects bounded `guard.dist`, and routes an imported defender through guard-start state `130` via an explicit CNS controller while the trace remains a whiff rather than a hit/guard contact. This proves trigger plumbing only, not automatic MUGEN/IKEMEN guard-start, exact proximity guard, or guard-end parity.
- `synthetic-imported-inguarddist-far.json`: required imported CMD/CNS route where the same near-but-not-contacting setup uses a too-small static `guard.dist`, records only whiff evidence, and leaves the defender in state `0` with control instead of entering guard-start state `130`. Current checksum: `d6d99236`. This proves bounded false-positive rejection for the current two-actor guard-distance box only, not exact MUGEN/IKEMEN proximity guard boxes, push timing, or guard-end parity.
- `synthetic-imported-auto-guard-start.json`: required imported CMD/CNS route where a near-but-not-contacting attack plus held-back defender input triggers the runtime's bounded automatic guard-start rule and enters defender-owned Common1-style states `120 -> 130` before contact. Current checksum: `0c734290`. The gate now requires ordered controller-event evidence for P2 state `120` `ChangeState` `Guard Start Done` plus ordered actor-frame evidence that `120` appears before `130`. This proves the first automatic guard-start bridge plus minimum controller/state-order evidence only, not exact proximity guard, guard end, guard effects, air guard, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-auto-guard-end.json`: required imported CMD/CNS route where the same bounded auto guard-start path later exits through defender-owned Common1-style state `140` and returns to `0`/control after `InGuardDist` is no longer true. Current checksum: `d1dc0aa3`. The gate now requires ordered controller-event evidence for P2 state `120` `Guard Start Done` before state `130` `ChangeState` `Stop Guarding`, ordered actor-frame evidence for `120 -> 130 -> 140`, plus final idle/control evidence. This proves the first automatic guard-end bridge plus minimum controller/state-order evidence only, not exact guard-end timing, proximity guard, guard effects, air guard, full Common1 controller-loop parity, or full MUGEN/IKEMEN guard parity.
- `kfm-official-auto-guard-start.json`: optional local official KFM fixture route where held-back input plus bounded `InGuardDist` enters real Common1 `120 -> 130` and now requires ordered controller/typed-operation evidence for `120` `ChangeAnim` -> `StateTypeSet` -> `metadata:statetypeset` -> `ChangeState` -> `130` `ChangeAnim`. Current local checksum: `ad493cde`. This is private fixture evidence only, not public bundled KFM support or exact proximity/effect parity.
- `kfm-official-auto-guard-end.json`: optional local official KFM fixture route where the same bounded auto guard-start path leaves through real Common1 `140` and returns to idle/control, now requiring ordered `120 -> 130` guard-start evidence, `130` stop-guarding `ChangeState`, and return-to-idle `VelSet` / `kinematic:velset`. Current local checksum: `ee962d04`. This is private fixture evidence only, not exact guard-end timing, proximity guard, sparks, sounds, or full parity.
- `synthetic-imported-default-guard-state.json`: required imported CMD/CNS State -1 `x -> 200` route where a held-back imported defender blocks a HitDef and enters its own Common1-style stand guard-hit states `150 -> 151`, including ordered named controller/typed-operation evidence for `ChangeAnim` -> `ChangeState` -> `HitVelSet` -> `kinematic:hitvelset` -> `CtrlSet` -> `resource:ctrlset` -> `ChangeState`, plus actor-frame state/physics/body/push telemetry and bounded stand guard velocity evidence. Current checksum: `016938a1`. This proves bounded stand guard-hit state/controller/frame routing only, not proximity guard, guard-start/end, crouch/air guard transitions, sparks, sounds, or exact MUGEN/IKEMEN guard parity.
- `synthetic-imported-default-guard-slide-stop.json`: required imported CMD/CNS route where direct guarded contact sends the defender through Common1-style stand guard-hit states `150 -> 151 -> 130`, then requires `151` slide/control precision: `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, and final `ChangeState` must appear in order after the `150` guard-shake handoff. Actor-frame evidence must show bounded guard-slide velocity before stop/control. Current checksum: `a9663641`. This proves bounded stand guard slide-stop/control order only, not exact guard timing, proximity guard, guard effects, crouch/air parity, controller-loop tick parity, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-crouch-guard-slide-stop.json`: required imported CMD/CNS route where held-down-back guarded contact sends the defender through Common1-style crouch guard-hit states `152 -> 153 -> 130`, then requires `153` slide/control precision: `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, and final `ChangeState` must appear in order after the `152` guard-shake handoff. Actor-frame evidence must show bounded crouch guard-slide velocity before stop/control, and active-command evidence must include `holddown` plus `x`. Current checksum: `2bea7311`. This proves bounded crouch guard slide-stop/control order only, not exact guard timing, proximity guard, guard effects, air parity, controller-loop tick parity, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-crouch-guard-state.json`: required imported CMD/CNS State -1 `x -> 200` route where a held-down-back imported defender blocks a HitDef, evaluates a Common1-style composite command expression such as `151 + 2*(command = "holddown")`, enters crouch guard-hit state `153` through `152 -> 153`, and requires ordered named controller/typed-operation evidence for `ChangeAnim` -> `ChangeState` -> `HitVelSet` -> `kinematic:hitvelset` -> `CtrlSet` -> `resource:ctrlset` -> `ChangeState`, plus actor-frame crouch state/physics/body/push telemetry. Current checksum: `6c4321af`. This proves bounded crouch branch/controller/frame routing only, not exact proximity guard, guard-start/end, guard effects, air guard, or full crouch guard parity.
- `synthetic-imported-diagonal-crouch-guard-state.json`: required imported CMD/CNS State -1 `x -> 200` route where atomic `DB` input, without separate `B`/`D` samples, still counts as held-back plus held-down for bounded guard detection and Common1-style `holdback`/`holddown` command checks, then requires the same ordered `152 -> 153` crouch guard-hit controller/operation route plus actor-frame crouch state/physics/body/push telemetry. Current checksum: `1dd33fb5`. This proves diagonal-input normalization plus bounded crouch controller/frame routing for the current runtime only, not exact MUGEN/IKEMEN input conflict or guard parity.
- `synthetic-imported-air-guard-state.json`: required imported CMD/CNS State -1 `x -> 200` route where an airborne held-back imported defender blocks an `A`-guardable HitDef, enters Common1-style air guard-hit states `154 -> 155`, executes `HitVelSet`, `VelAdd`, and `CtrlSet`, requires ordered named controller/typed-operation evidence for `ChangeAnim` -> `ChangeState` -> `HitVelSet` -> `kinematic:hitvelset` -> `VelAdd` -> `CtrlSet` -> `resource:ctrlset` -> `ChangeState`, actor-frame air state/physics/body/push telemetry, and bounded air guard velocity evidence before returning to a grounded idle/control snapshot. Current checksum: `ce9cc9ba`. This proves bounded air guard-hit `154 -> 155` controller/frame routing only, not exact air guard landing, physics, sparks, sounds, proximity guard, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-air-guard-landing.json`: required imported CMD/CNS route where an airborne held-back imported defender blocks an `A`-guardable direct HitDef, routes `154 -> 155 -> 52 -> 20`, executes ordered `154` `ChangeAnim` / `ChangeState`, `155` `HitVelSet`, `kinematic:hitvelset`, `VelAdd`, `CtrlSet`, `resource:ctrlset`, landing `VelSet`, `kinematic:velset`, landing `PosSet`, `kinematic:posset`, and final `ChangeState`, includes a state-`52` y = 0 actor-frame bucket, and requires final imported walking state/action `20` with control. Current checksum: `d6986d7f`; final checksum: `92c1ead0`. This proves bounded synthetic air guard landing walk-control only, not exact air guard physics/timing, state-`52` internal controller parity, proximity guard, guard effects, visual/audio parity, or full MUGEN/IKEMEN guard parity.
- `synthetic-imported-hitstun.json`: required imported CMD/CNS State -1 `x -> 200` route where the target final actor evidence reaches the sandbox's current partial get-hit snapshot (`animNo=500`, `moveType=H`).
- `synthetic-imported-fall.json`: required imported CMD/CNS State -1 `x -> 200` route where simple `fall.*` HitDef params survive into final target `hitFall` evidence. This proves metadata capture only, not common-state, bounce, liedown, or recovery parity.
- `synthetic-imported-common-gethit.json`: required imported CMD/CNS State -1 `x -> 200` route where `p2stateno` sends the defender into an attacker-owned custom get-hit state and partial `HitFallVel`, `HitFallDamage`, `HitFallSet`, and `FallEnvShake` controllers execute with ordered controller-event evidence plus `hitfall:*` and `fallenvshake` typed operation evidence. The required order is `HitFallVel -> hitfall:hitfallvel -> HitFallDamage -> hitfall:hitfalldamage -> HitFallSet -> hitfall:hitfallset -> FallEnvShake -> fallenvshake` in state `5100`. The gate also requires bounded FallEnvShake `RuntimeEnvShakeWorld` event telemetry for P2 state `5100` with `time = 15`, `freq = 178`, `ampl = 6`, and `phase = 0`. Current checksum: `713e49b7`. This proves controller-flow plus bounded camera-shake event plumbing only, not KFM/Common1 fall, bounce, liedown, recovery, exact waveform, pause/layer, or stage parity.
- `synthetic-imported-default-gethit.json`: required imported CMD/CNS State -1 `x -> 200` route where a `HitDef` without `p2stateno` routes an imported standing defender into its own Common1-style state `5000`. This proves defender-owned standing default get-hit entry only, not exact `HitShakeOver`, `HitOver`, bounce, liedown, or recovery parity.
- `synthetic-imported-default-crouch-gethit.json`: required imported CMD/CNS route where P2 holds crouch, P1 enters `200`, and a direct `HitDef` without `p2stateno` routes the imported defender into its own Common1-style state `5010`. Current checksum: `7ec18c61`; final checksum: `1c47d038`. This proves bounded crouch default get-hit state selection only, not exact crouch get-hit animation, slide timing, fall routing, `HitShakeOver`/`HitOver` progression, controller-loop timing, or full Common1 get-hit parity.
- `synthetic-imported-default-air-gethit.json`: required imported CMD/CNS route where P2 jumps, P1 enters `200`, and a direct `HitDef` without `p2stateno` routes the imported defender into its own Common1-style state `5020`. Current checksum: `dc4fb7c9`; final checksum: `2fd842bb`. This proves bounded air default get-hit state selection only, not exact air get-hit animation, fall route, landing route, air recovery, `HitShakeOver`/`HitOver` progression, controller-loop timing, or full Common1 get-hit parity.
- `synthetic-imported-default-air-fall-gethit.json`: required imported CMD/CNS route where P2 jumps, P1 enters `200`, and a fall `HitDef` without `p2stateno` routes the imported defender through its own Common1-style states `5020 -> 5030 -> 5050`. Current checksum: `1230a2f3`; final checksum: `2ad2abf9`. This proves bounded air fall get-hit state order only, not exact air get-hit animation, exact `HitShakeOver` / `HitOver` timing, ground impact, bounce, lie-down, landing, recovery input, controller-loop timing, or full Common1 fall/get-hit parity.
- `synthetic-imported-default-air-ground-impact.json`: required imported CMD/CNS route where P2 jumps, P1 enters `200`, and a fall `HitDef` without `p2stateno` routes the imported defender through its own Common1-style states `5020 -> 5030 -> 5050 -> 5100`, including `5100` `HitFallDamage` / `hitfall:hitfalldamage` evidence. Current checksum: `0ba3c80f`; final checksum: `cb7f1043`. This proves bounded air fall get-hit state order plus ground-impact controller evidence only, not exact air get-hit animation, exact `HitShakeOver` / `HitOver` timing, exact ground-impact timing/position, bounce physics, lie-down timing, landing, recovery input, controller-loop timing, or full Common1 fall/get-hit parity.
- `synthetic-imported-default-air-liedown-recovery.json`: required imported CMD/CNS route where P2 jumps, P1 enters `200`, and a fall `HitDef` without `p2stateno` routes the imported defender through its own Common1-style states `5020 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0`, including `5101` `HitFallVel`, `5110` `HitFallDamage`, typed `hitfall:hitfallvel` / `hitfall:hitfalldamage`, bounded `5110` `downRecoverTime` countdown, and final idle/control evidence. Current checksum: `56a8f236`; final checksum: `20c045a3`. This proves bounded air fall get-hit state order plus bounce, lie-down countdown, get-up, and idle-return evidence only, not exact air get-hit animation, exact `HitShakeOver` / `HitOver` timing, exact ground-impact timing/position, exact bounce physics, exact lie-down duration tables, recovery input, landing nuance, controller-loop timing, or full Common1 fall/get-hit parity.
- `synthetic-imported-default-air-fall-recovery-input.json`: required imported CMD/CNS route where P2 jumps, P1 enters `200`, and a fall `HitDef` without `p2stateno` routes the imported defender through `5020 -> 5030 -> 5050`, accepts `command = "recovery"` after positive-to-zero `fall.recovertime`, enters `5210`, lands through `52`, and returns to idle/control. Current checksum: `334a419e`; final checksum: `c5038a9d`. This proves bounded air-entry recovery-input evidence only, not exact recovery threshold tables, exact air get-hit animation, exact velocity math, exact controller-loop timing, recovery arbitration between air/ground branches, or full Common1 recovery parity.
- `synthetic-imported-default-air-fall-recovery-too-early.json`: required imported CMD/CNS route where P2 jumps, P1 enters `200`, and a fall `HitDef` without `p2stateno` routes the imported defender through `5020 -> 5030 -> 5050`, keeps `command = "recovery"` active too early, forbids `5210`, `5200`, `5201`, `52`, `5100`, `5101`, `5110`, and `5120`, and leaves P2 in `5050` while `fall.recovertime` remains positive. Current checksum: `48a2e708`; final checksum: `cdd20fed`. This proves bounded air-entry early recovery rejection evidence only, not exact recovery threshold tables, exact air get-hit animation, exact velocity math, exact controller-loop timing, recovery arbitration between air/ground branches, or full Common1 recovery parity.
- `synthetic-imported-default-gethit-progression.json`: required imported CMD/CNS State -1 `x -> 200` route where `HitShakeOver` advances defender-owned state `5000` into `5001` and `HitOver` returns the defender to state `0` with control. Current checksum: `ef2a67f8`. The gate requires ordered P2 controller-event evidence for `5000` `ChangeState` before `5001` `ChangeState`, ordered actor-frame evidence for `5000 -> 5001`, required stand shake/slide actor-frame telemetry for `stateType`, `moveType`, `physics`, `Clsn1`/`Clsn2`, bounded velocity, and final idle/control evidence. This proves the bounded stand get-hit progression order plus frame-level physics evidence only, not exact controller tick timing, exact `HitShakeOver`/`HitOver` timing, fall, bounce, liedown, air recovery, or guard-state parity.
- `synthetic-imported-default-fall-gethit.json`: required imported CMD/CNS State -1 `x -> 200` route where a fall HitDef without `p2stateno` branches a defender-owned Common1-style state from `5000` into `5030` and `5050`. Checksum `6af73a91` now requires ordered P2 actor-frame evidence for `5000 -> 5030 -> 5050` with `moveType = H`, plus controller/typed-operation order through `5000` `ChangeState`, `5030` `VelAdd` / `HitVelSet` / `kinematic:hitvelset` / `ChangeState`, and `5050` `VelAdd` / `ChangeState`. This proves the bounded airborne fall branch only, not exact ground-impact `5100`, bounce, liedown, recovery input, or guard-state parity.
- `synthetic-imported-default-fall-recovery.json`: required imported CMD/CNS State -1 `x -> 200` route where a bounded defender-owned Common1-style fall chain executes `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0`. Current checksum: `d83797d9`. The gate now requires ordered controller/typed-operation evidence for `5000` `ChangeState`, `5030` `VelAdd` / `HitVelSet` / `kinematic:hitvelset` / `ChangeState`, `5050` `VelAdd` / `ChangeState`, then `5110` `ChangeState` `Get Up` before `5120` `VelSet` / `kinematic:velset` / `HitFallSet` / `hitfall:hitfallset` / `ChangeState`; it also requires actor-frame order `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120`, with `5110` lie-down `moveType = H` preceding `5120` get-up `moveType = I` while `5110` exposes a bounded `hitFall.downRecoverTime` countdown range (`max >= 58`, `min <= 54`, first-to-last drop `>= 1`, at least 2 summarized frames). This proves bounded airborne fall get-hit, ground-impact, bounce, lie-down timer countdown, get-up, and fall-recovery-settle order only, not official KFM parity, exact tick-order parity, recovery-input command branching, exact `down.recovertime` tables, or engine-perfect bounce physics.
- `synthetic-imported-default-fall-recovery-input.json`: required imported CMD/CNS State -1 `x -> 200` route where `fall.recover = 1`, `fall.recovertime` counts down, `CanRecover` becomes true, and defender input `x+y` activates `command = "recovery"` to route a bounded Common1-style fall state from `5050` into `5210` before returning to idle/control. This proves the current synthetic recovery-input branch only, not official KFM ground-recovery `5200/5201`, exact ground/air thresholds, exact velocities, or tick-order parity.
- `synthetic-imported-default-fall-ground-recovery-priority.json`: required imported CMD/CNS State -1 `x -> 200` route where `fall.recover = 1`, `command = "recovery"` stays active near ground after `fall.recovertime` reaches zero, and ground recovery routes the defender `5000 -> 5030 -> 5050 -> 5200 -> 5201 -> 52 -> 0` while generic air recovery state `5210` and lie-down chain states are forbidden. Current checksum: `e83b2db7`. This proves bounded ground-over-air recovery selection only, not exact threshold tables, ground/air arbitration constants, velocities, or tick-order parity.
- `synthetic-imported-default-fall-recovery-input-priority.json`: required imported CMD/CNS State -1 `x -> 200` route where `fall.recover = 1`, `command = "recovery"` stays active long enough to overlap `CanRecover`, and recovery input routes the defender `5000 -> 5030 -> 5050 -> 5210 -> 0` while the competing `HitFall && CanRecover` probe state `5250` is forbidden. Current checksum: `f5e72e07`. This proves bounded recovery-input priority only, not exact threshold tables, ground/air arbitration, velocities, or tick-order parity.
- `synthetic-imported-hitfall-canrecover-ready.json`: required imported CMD/CNS State -1 `x -> 200` route where `fall.recover = 1`, `fall.recovertime` counts down from positive to zero in state `5050`, and `HitFall && CanRecover` routes the defender into state/action `5250` without active `recovery` command. Current checksum: `c0097d7f`. This proves bounded CanRecover-ready trigger evidence only, not exact threshold tables, velocities, or tick-order parity.
- `synthetic-imported-default-fall-recovery-threshold.json`: required imported CMD/CNS State -1 `x -> 200` route where actor-frame evidence observes the defender in `5050` while `hitFall.recoverTime` is still positive, requires a bounded first-to-last `recoverTime` drop inside that summarized `5050` bucket, and later observes `5210` with `recoverTime = 0` after `CanRecover` plus `command = "recovery"` routes. Current checksum: `7bb15a5f`. This proves the bounded countdown/threshold handoff only, not exact recovery timing, velocities, or tick-order parity.
- `synthetic-imported-default-fall-official-recovery-threshold.json`: required imported CMD/CNS State -1 `x -> 200` route where an official-style synthetic Common1 branch observes `5050` with positive `hitFall.recoverTime`, requires the countdown to drop to `0`, and then accepts recovery into `5200 -> 5201 -> 52 -> 0`. Current checksum: `86804271`. This proves the portable official-style ground-recovery threshold route only, not exact threshold tables, velocity math, public KFM support, or VM tick-order parity.
- `synthetic-imported-default-fall-recovery-tick-order.json`: required imported CMD/CNS State -1 `x -> 200` route where `requiredActorFrameSequences` proves summarized actor-frame evidence for `5050` with positive `hitFall.recoverTime`, a first-to-last `recoverTime` drop, and ordering before `5210` with `recoverTime = 0`, while `requiredControllerEventSequences` proves the bounded named controller/typed-operation route `5050` `VelAdd` `Gravity` -> `ChangeState` `Recovery Input` -> `5210` `VelSet` `Air Recovery Velocity` -> `kinematic:velset` -> `HitFallSet` `Fall Recovery Settled` -> `hitfall:hitfallset` -> `ChangeState` `Stand`. Current checksum: `e2691aab`. This proves the trace-gate actor-frame plus named controller/operation ordering contract only, not exact VM/controller tick order or official KFM threshold tables.
- `synthetic-imported-default-fall-air-recovery-velocity.json`: required imported CMD/CNS State -1 `x -> 200` route where bounded recovery input enters `5210` and actor-frame evidence observes air-recovery `VelSet` telemetry before returning to idle/control. Current checksum: `560f6308`. This proves the bounded synthetic air-recovery velocity route only, not exact recovery velocity math, threshold tables, or tick-order parity.
- `synthetic-imported-default-fall-official-air-recovery.json`: required imported CMD/CNS State -1 `x -> 200` route where official-style synthetic air recovery waits through positive-to-zero `hitFall.recoverTime`, accepts `command = "recovery"` while airborne, enters `5210`, lands through `52`, and returns to idle/control. The gate requires `5050 -> 5210 -> 52` actor-frame sequence evidence plus ordered `VelAdd`, `ChangeState`, `VelSet`, `HitFallSet`, `CtrlSet`, `kinematic:*`, `hitfall:hitfallset`, and `resource:ctrlset` operation evidence. Current checksum: `b0363be9`. The `pnpm qa:trace` coverage contract now also requires this artifact to appear in required coverage summaries, not only in the generated artifact list. This proves the portable official-style air-recovery sequence and required coverage presence only, not exact threshold tables, velocity math, public KFM support, visual parity, or full VM tick-order parity.
- `synthetic-imported-default-fall-ground-recovery.json`: required imported CMD/CNS State -1 `x -> 200` route where bounded near-ground recovery input selects `5050 -> 5200 -> 5201 -> 52 -> 0`, executes `SelfState`, `VelSet`, and `PosSet`, exposes actor-frame velocity evidence for the synthetic `velocity.air.gethit.groundrecover.*` constants, and now requires the ordered named controller/typed-operation sequence `5050 VelAdd -> ChangeState`, `5200 VelAdd -> SelfState`, `5201 VelSet -> PosSet -> kinematic:posset -> NotHitBy -> eligibility:nothitby -> ChangeState`, then `52 VelSet -> kinematic:velset -> PosSet -> kinematic:posset -> CtrlSet -> resource:ctrlset`. Current checksum: `7945fd93`. This proves the bounded synthetic ground-recovery selection/velocity/order route only, not exact recovery threshold tables, full velocity math, or full VM tick-order parity.
- `synthetic-imported-default-fall-official-ground-recovery.json`: required imported CMD/CNS State -1 `x -> 200` route where official-style synthetic near-ground recovery input selects `5050 -> 5200 -> 5201 -> 52 -> 0`, requires positive-to-zero `hitFall.recoverTime` actor-frame sequence evidence in `5050`, observes bounded `5201` recovery velocity/position telemetry, observes `52` landing y = 0 evidence, and requires the ordered named controller/typed-operation sequence through `VelAdd`, `ChangeState`, `SelfState`, `VelSet`, `PosSet`, `NotHitBy`, `CtrlSet`, `kinematic:*`, `eligibility:nothitby`, and `resource:ctrlset`. Current checksum: `74b72495`. This proves the portable official-style ground-recovery sequence only, not exact threshold tables, velocity math, public KFM support, visual parity, or full VM tick-order parity.
- `synthetic-imported-default-fall-recovery-too-early.json`: required imported CMD/CNS State -1 `x -> 200` route where defender input activates `command = "recovery"` while `fall.recovertime` is still positive, the gate requires state `5050` actor-frame evidence with minimum observed `recoverTime >= 1` plus first-to-last countdown drop, forbids state `5210`, and requires final defender state `5050`. Current checksum: `050e7e3c`. This proves the bounded early-input reject path only, not exact recovery thresholds, velocities, or tick-order parity.
- `synthetic-imported-default-fall-official-recovery-too-early.json`: required imported CMD/CNS State -1 `x -> 200` route where an official-style synthetic Common1 branch detects early `command = "recovery"` while `hitFall.recoverTime` is still positive, requires state `5050` actor-frame evidence with minimum observed `recoverTime >= 1`, forbids recovery states, and keeps the defender in `5050`. Current checksum: `ef945ff5`. This proves the portable official-style early-input rejection window only, not exact recovery thresholds, velocities, or tick-order parity.
- `synthetic-imported-state-exit.json`: required imported CMD/CNS State -1 `x -> 200` route where the attack hits, finishes recovery, and final P1 evidence returns to idle/control (`stateNo=0`, `animNo=0`, `ctrl=true`).
- `synthetic-imported-custom-state.json`: required imported CMD/CNS route where both fighters are synthetic imported definitions, `HitDef p2stateno = 888` with `p2getp1state = 1` routes P2 into a P1-owned custom state, P1-owned `ChangeState` chains P2 to state/action `889`, and `SelfState` returns P2 to state `0` with control. Current checksum: `bf632df3`. This proves only the bounded two-actor owner-backed custom-state entry/chain/return path through actor-frame `customOwnerId = p1` evidence, not throws, redirects, helpers/root/parent ownership, teams, or exact tick order.
- `synthetic-imported-custom-state-gethitvar.json`: required imported CMD/CNS route where `HitDef p2stateno = 888` with `p2getp1state = 1` routes P2 into P1-owned state data, then a P1-owned custom state branches through `GetHitVar(damage) = 37 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)` into state `890` before `SelfState` returns P2 to state `0` with control. Current checksum: `40705e74`. This proves only bounded owner-backed custom-state access to direct-hit `GetHitVar` metadata with ordered `888 -> 890 -> SelfState` evidence, not guard metadata inheritance, throws, helper/root/parent redirects, teams, exact bind/tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-animtype.json`: required imported CMD/CNS route where the same owner-backed `HitDef p2stateno = 888` path branches through `GetHitVar(animtype)`, `GetHitVar(groundtype)`, `GetHitVar(airtype)`, and `GetHitVar(isbound)` into state `892` before `SelfState` returns P2 to state `0` with control. Current checksum: `bbe8777c`. This proves only bounded owner-backed custom-state access to direct-hit type metadata with ordered `888 -> 892 -> SelfState` evidence, not exact get-hit animation selection, guard metadata inheritance, throws, helper/root/parent redirects, teams, exact bind/tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-type.json`: required imported CMD/CNS route where the same owner-backed `HitDef p2stateno = 888` path stores `ground.type = Low` and `air.type = Trip`, then branches through `GetHitVar(type)`, `GetHitVar(groundtype)`, `GetHitVar(airtype)`, and `!GetHitVar(guarded)` into state `897` before `SelfState` returns P2 to state `0` with control. Current checksum: `38542874`. This proves only bounded owner-backed custom-state access to direct-hit type metadata with ordered `888 -> 897 -> SelfState` evidence, not exact get-hit animation selection, air-hit arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams, exact bind tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-yaccel.json`: required imported CMD/CNS route where the same owner-backed `HitDef p2stateno = 888` path stores `yaccel = 0.62`, then branches through `GetHitVar(yaccel)`, `GetHitVar(hittime)`, and `!GetHitVar(guarded)` into state `898` before `SelfState` returns P2 to state `0` with control. Current checksum: `549fe48d`. This proves only bounded owner-backed custom-state access to direct-hit yaccel metadata with ordered `888 -> 898 -> SelfState` evidence, not exact physics integration, fall acceleration arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams, exact bind tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-fall.json`: required imported CMD/CNS route where the same owner-backed `HitDef p2stateno = 888` path branches through `GetHitVar(fall)`, `GetHitVar(fall.recover)`, positive `GetHitVar(fall.recovertime)`, `GetHitVar(fall.xvel)`, and `GetHitVar(fall.yvel)` into state `891` before `SelfState` returns P2 to state `0` with control. Current checksum: `2ccfeb43`. This proves only bounded owner-backed custom-state access to direct-hit fall metadata with ordered `888 -> 891 -> SelfState` evidence, not guard metadata inheritance, throws, helper/root/parent redirects, teams, exact bind/tick order, exact recovery threshold behavior, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-guarded.json`: required imported CMD/CNS route where guarded direct-HitDef target memory plus owner-local `TargetState` branches through `GetHitVar(guarded)`, `GetHitVar(hitshaketime)`, and `GetHitVar(hittime)` into state `895` before `SelfState` returns P2 to state `0` with control. Current checksum: `54f62821`. This proves only bounded owner-backed custom-state access to guarded hit metadata with ordered guard contact -> `TargetState` -> `888 -> 895 -> SelfState` evidence, not `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams, exact bind/tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-guard-timing.json`: required imported CMD/CNS route where guarded direct-HitDef target memory plus owner-local `TargetState` branches through `GetHitVar(guarded)`, `GetHitVar(slidetime)`, `GetHitVar(ctrltime)`, and `GetHitVar(hitshaketime)` into state `901` before `SelfState` returns P2 to state `0` with control. Current checksum: `ba77beec`. This proves only bounded owner-backed custom-state access to guarded slide/control timing metadata with ordered guard contact -> `TargetState` -> `888 -> 901 -> SelfState` evidence, not `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams, exact bind/tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-isbound.json`: required imported CMD/CNS route where direct-HitDef target memory plus `TargetBind` and owner-local `TargetState` branches through `GetHitVar(isbound)`, positive `GetHitVar(hittime)`, and `!GetHitVar(guarded)` into state `896` before `SelfState` returns P2 to state `0` with control. Current checksum: `d25307e9`. This proves only bounded owner-backed custom-state access to TargetBind subject metadata with ordered hit -> `TargetBind` -> `TargetState` -> `888 -> 896 -> SelfState` evidence, not throws, exact bind tick-order/lifetime, visual bind parity, helper/root/parent redirects, teams, `p2stateno`-on-guard behavior, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-custom-state-gethitvar-velocity.json`: required imported CMD/CNS route where the same owner-backed `HitDef p2stateno = 888` path with `ground.velocity = 4,-2` branches through `GetHitVar(xvel)`, `GetHitVar(yvel)`, `!GetHitVar(fall)`, and `!GetHitVar(guarded)` into state `894` before `SelfState` returns P2 to state `0` with control. Current checksum: `e9d8da9e`. This proves only bounded owner-backed custom-state access to direct-hit velocity metadata with ordered `888 -> 894 -> SelfState` evidence, not exact velocity lifetime after later physics/controllers, throws, helper/root/parent redirects, teams, exact bind/tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-target-owned-custom-state.json`: required imported CMD/CNS route where both fighters are synthetic imported definitions, `HitDef p2stateno = 888` with `p2getp1state = 0` routes P2 into defender-owned state/action `888`, actor-frame evidence keeps `customOwnerId` absent, and P2 `SelfState` returns to state `0` with control. Current checksum: `410fb8c0`. This proves only the bounded two-actor target-owned custom-state route, not throws, redirects, helpers/root/parent ownership, teams, exact bind/tick order, or full MUGEN/IKEMEN custom-state parity.
- `synthetic-imported-targetstate-custom.json`: required imported CMD/CNS route where direct `HitDef` target memory feeds typed `TargetState value = 888`, routes P2 into P1-owned state data, P1-owned `ChangeState` chains P2 to state/action `889`, and `SelfState` returns P2 to state `0` with control. Current checksum: `fedaf0a4`. This proves only the bounded two-actor TargetState owner-backed custom-state route through target-link plus actor-frame `customOwnerId = p1` evidence, not throws, redirects, helpers/root/parent ownership, teams, or exact tick order.
- `synthetic-imported-target.json`: required imported CMD/CNS target-memory route with typed `hitdef`, `bindtotarget`, and `target:*` operation evidence for `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, `BindToTarget`, and `TargetDrop`; it now also requires world-visible target-link/binding evidence, an actor-frame P2 `facing = 1` plus velocity observation after TargetVel* controllers, final P1 `targetCount = 0` after TargetDrop cleanup, and final P2 `life = 943` / `power = 40`. Current checksum: `f5a16dc9`. This proves the bounded two-actor Target* side-effect route, not full redirect, helper/root/parent, team, multi-target, or exact target lifetime parity.
- `synthetic-imported-target-redirect.json`: required imported CMD/CNS target-memory route where direct `HitDef` target id `77` feeds bounded static `Target(77), Life < 1000` trigger redirect reads and routes P1 from state `200` to `286` with target-link evidence for P2. Current checksum: `89580963`. This proves bounded two-player static target trigger reads only, not helper/projectile targets, mutation through redirects, teams, multi-target selection, exact target lifetime/tick order, or full target redirect parity.
- `synthetic-imported-target-redirect-bottom.json`: required imported CMD/CNS target-memory route where direct `HitDef` contact exists but `Target(999)` is missing inside `(Target(999), Life = 0) || 1`; the gate forbids state `396`, requires fallback state/action `397`, and keeps target-link evidence for the valid direct target id `77`. Current checksum: `5e50a90a`. This proves bounded invalid-redirect bottom propagation in current parenthesized parser expressions only, not recursive redirection, debug warning text, `Cond` isolation, broader bottom parity, teams, multi-target selection, mutation through redirects, or full target redirect parity.
- `synthetic-imported-target-cond-bottom.json`: required imported CMD/CNS target-memory route where direct `HitDef` contact exists but `Target(999)` is missing inside a `Cond(...)` branch; the gate forbids state `398` when the missing-target branch is selected, requires fallback state/action `399` when the missing-target branch is skipped, and keeps target-link evidence for the valid direct target id `77`. Current checksum: `e882a2bb`. This proves bounded `Cond(...)` selected-branch evaluation and unused-branch bottom isolation only, not recursive redirection, debug warning text, broader bottom parity, teams, multi-target selection, mutation through redirects, or full target redirect parity.
- `synthetic-imported-target-ifelse-bottom.json`: required imported CMD/CNS target-memory route where direct `HitDef` contact exists but `Target(999)` is missing inside an `IfElse(...)` branch; the gate forbids state `400` when the missing-target branch is selected, requires fallback state/action `401` when the selected `MoveHit` branch succeeds and the missing-target branch is unused, and keeps target-link evidence for the valid direct target id `77`. Current checksum: `be7554d4`. This proves bounded `IfElse(...)` returned-branch isolation only, not Cond-style lazy evaluation, recursive redirection, debug warning text, broader bottom parity, teams, multi-target selection, mutation through redirects, or full target redirect parity.
- `synthetic-imported-controller-param-root-redirect.json`: required imported CMD/CNS controller-param route where static `VelSet x = 4, y = -2` seeds P1 velocity, then dynamic `VelSet x = Root, Life - 995, y = Root, StateNo - 203` resolves through current player root context and reaches velocity `5,-3`. Current checksum: `1d4a73f7`. This proves bounded controller-parameter Root redirect evaluation for current dynamic `VelSet` execution only, not player Parent controller-param redirects, nested helper ancestry where root differs from parent, helper-spawned helpers, dynamic-parameter typed lowering, recursive redirect evaluation, debug warning text, broad bottom/redirect parity, helper/team ownership, or full controller/expression parity.
- `synthetic-imported-helper-controller-param-parentroot.json`: required imported CMD/CNS helper-local controller-param route where a first-generation visual Helper runs dynamic `VelSet x = Parent,Life - 995, y = Root,StateNo - 203`, emits typed `kinematic:velset` telemetry, reaches velocity `5,-3`, and routes to state/action `1401` / anim `941`. Current checksum: `94919326`. This proves bounded helper-local controller-parameter Parent/Root redirect evaluation plus typed operation telemetry for this dynamic `VelSet` route only, not nested helper ancestry where root differs from parent, helper-spawned helpers, player Parent controller-param redirects, helper-local dynamic typed lowering beyond this route, recursive redirect evaluation, debug warning text, broader helper-owned controller breadth, teams/simul, or full controller/expression parity.
- `synthetic-imported-helper-dynamic-veladd.json`: required imported CMD/CNS helper-local controller-param route where a first-generation visual Helper runs static `VelSet` plus dynamic `VelAdd x = Parent,Life - 996, y = Root,StateNo - 205`, emits typed `kinematic:velset` and `kinematic:veladd` telemetry, reaches velocity `5,-3`, and routes to state/action `1402` / anim `942`. Current checksum: `fbb8bcae`. This proves bounded helper-local controller-parameter Parent/Root redirect evaluation plus typed operation telemetry for this dynamic `VelAdd` route only, not nested helper ancestry where root differs from parent, helper-spawned helpers, player Parent controller-param redirects, helper-local dynamic typed lowering beyond current `VelSet` / `VelAdd` / `VelMul` / `PosSet` routes, recursive redirect evaluation, debug warning text, broader helper-owned controller breadth, teams/simul, or full controller/expression parity.
- `synthetic-imported-helper-dynamic-velmul.json`: required imported CMD/CNS helper-local controller-param route where a first-generation visual Helper runs static `VelSet` plus dynamic `VelMul x = Parent,Life - 998, y = Root,StateNo - 202`, emits typed `kinematic:velset` and `kinematic:velmul` telemetry, reaches velocity `4,-6`, and routes to state/action `1403` / anim `943`. Current checksum: `08220a98`. This proves bounded helper-local controller-parameter Parent/Root redirect evaluation plus typed operation telemetry for this dynamic `VelMul` route only, not nested helper ancestry where root differs from parent, helper-spawned helpers, player Parent controller-param redirects, helper-local dynamic typed lowering beyond current `VelSet` / `VelAdd` / `VelMul` / `PosSet` routes, recursive redirect evaluation, debug warning text, broader helper-owned controller breadth, teams/simul, or full controller/expression parity.
- `synthetic-imported-helper-dynamic-posset.json`: required imported CMD/CNS helper-local controller-param route where a first-generation visual Helper runs static `PosSet` plus dynamic `PosSet x = Parent,Life - 984, y = Root,StateNo - 220`, emits typed `kinematic:posset` telemetry, reaches position `16,-20`, and routes to state/action `1404` / anim `944`. Current checksum: `50596bc2`. This proves bounded helper-local controller-parameter Parent/Root redirect evaluation plus typed operation telemetry for this dynamic `PosSet` route only, not nested helper ancestry where root differs from parent, helper-spawned helpers, player Parent controller-param redirects, helper-local dynamic typed lowering beyond current `VelSet` / `VelAdd` / `VelMul` / `PosSet` routes, recursive redirect evaluation, debug warning text, broader helper-owned controller breadth, teams/simul, or full controller/expression parity.
- `synthetic-imported-helper-dynamic-posadd.json`: required imported CMD/CNS helper-local controller-param route where a first-generation visual Helper runs static `PosSet` plus dynamic `PosAdd x = Parent,Life - 984, y = Root,StateNo - 220`, emits typed `kinematic:posset` and `kinematic:posadd` telemetry, reaches position `18,-17`, and routes to state/action `1405` / anim `945`. Current checksum: `97ec15d0`. This proves bounded helper-local controller-parameter Parent/Root redirect evaluation plus typed operation telemetry for this dynamic `PosAdd` route only, not nested helper ancestry where root differs from parent, helper-spawned helpers, player Parent controller-param redirects, helper-local dynamic typed lowering beyond current `VelSet` / `VelAdd` / `VelMul` / `PosSet` / `PosAdd` routes, recursive redirect evaluation, debug warning text, broader helper-owned controller breadth, teams/simul, or full controller/expression parity.
- `synthetic-imported-controller-param-target-redirect.json`: required imported CMD/CNS controller-param route where static `VelSet x = 4, y = -2` seeds P1 velocity, direct `HitDef id = 77` records target memory, then dynamic `VelSet x = Target(77), Life - 960, y = Target(77), Life - 966` resolves through target redirect context and reaches velocity `3,-3`. Current checksum: `55bb7b1f`. This proves bounded controller-parameter Target redirect evaluation for current dynamic `VelSet` execution only, not player Parent controller-param redirects, nested helper ancestry where root differs from parent, helper-spawned helpers, dynamic-parameter typed lowering, recursive redirect evaluation, debug warning text, broad bottom/redirect parity, helper/team ownership, or full controller/expression parity.
- `synthetic-imported-controller-param-bottom.json`: required imported CMD/CNS controller-param route where static `VelSet x = 4, y = -2` seeds P1 velocity before dynamic `VelSet x = Parent,Vel X, y = Parent,Vel Y` evaluates through a missing parent redirect and falls back to `0,0`. Current checksum: `28ef21ad`. This proves bounded controller-parameter bottom fallback to `0` for current dynamic `VelSet` execution only, not player Parent controller-param redirect support, nested helper ancestry where root differs from parent, helper-spawned helpers, dynamic-parameter typed lowering, recursive redirect evaluation, debug warning text, broad bottom parity, helper/team ownership, or full controller/expression parity.
- `synthetic-imported-target-dynamic-redirect.json`: required imported CMD/CNS target-memory route where direct `HitDef` target id `77` plus owner-local `VarSet` `var(0) = 77` feeds bounded dynamic `Target(var(0)), Life < 1000` trigger redirect reads and routes P1 from state `200` to `287` with target-link evidence for P2. Current checksum: `9985b62a`. This proves bounded executable target-id expressions only, not unsupported or negative target-id expressions, helper/projectile targets, mutation through redirects, teams, multi-target selection, exact target lifetime/tick order, or full target redirect parity.
- `synthetic-imported-target-noko.json`: required imported CMD/CNS target-memory route with typed `target:targetlifeadd` operation evidence, ordered P2 `AssertSpecial NoKO` before P1 `HitDef` and `TargetLifeAdd`, target-link id `77`, and final P2 life `1`. Current checksum: `321a1eba`. This proves bounded no-KO clamping for target-controller damage only, not exact NoKO persistence, round/KO semantics, redirects, helpers/root/parent, teams, multi-targets, or exact target lifetime parity.
- `synthetic-imported-bindtotarget-head.json`: required imported CMD/CNS route where both fighters are synthetic imported definitions, the target parses `[Size] head.pos = 6,-72`, `BindToTarget pos = 20,-8,Head` executes after real `HitDef` target memory, and `MatchWorld.targetLinks` exposes the resolved owner-to-target binding offset `26,-80`. Current checksum: `474deaa2`. This proves only the bounded two-actor Head anchor path, not redirects, helper/root/parent target ownership, teams, exact bind tick order, or full throw/custom-state parity.
- `synthetic-imported-bindtotarget-mid.json`: required imported CMD/CNS route where both fighters are synthetic imported definitions, the target parses `[Size] mid.pos = 4,-42`, `BindToTarget pos = 20,-8,Mid` executes after real `HitDef` target memory, and `MatchWorld.targetLinks` exposes the resolved owner-to-target binding offset `24,-50`. Current checksum: `c65ebf56`. This proves only the bounded two-actor Mid anchor path, not redirects, helper/root/parent target ownership, teams, exact bind tick order, or full throw/custom-state parity.
- `synthetic-imported-numtarget.json`: required imported CMD/CNS State -1 `x -> 200` route where direct `HitDef` target memory makes `NumTarget(77) > 0` true and branches back in the owner state to `263`. This proves bounded target-memory trigger plumbing only, not redirect stacks, helper ownership, multi-target teams, exact target lifetime, or full MUGEN/IKEMEN target parity.
- `synthetic-imported-default-numtarget.json`: required imported CMD/CNS State -1 `x -> 200` route where direct `HitDef` without `id` defaults target memory to id `0`, exposes target link `p1 -> p2 / 0`, and makes `NumTarget(0) > 0` branch back in the owner state to `268`. Current checksum: `5869ebbd`. This proves bounded omitted-id target-memory trigger plumbing only, not redirect stacks, helper ownership, multi-target teams, exact target lifetime, or full MUGEN/IKEMEN target parity.
- `synthetic-imported-default-target-redirect.json`: required imported CMD/CNS State -1 `x -> 200` route where direct `HitDef` without `id` defaults target memory to id `0`, exposes target link `p1 -> p2 / 0`, and makes `Target(0), Life < 1000` branch back in the owner state to `269`. Current checksum: `d43caabf`. This proves bounded omitted-id target redirect trigger reads only, not Target* mutation, helper/projectile target redirects, multi-target teams, exact target lifetime, or full MUGEN/IKEMEN target redirect parity.
- `synthetic-imported-bare-target-redirect.json`: required imported CMD/CNS State -1 `x -> 200` route where direct `HitDef` target id `77` exposes target link `p1 -> p2 / 77`, and makes bare `Target, Life < 1000` branch back in the owner state to `270`. Current checksum: `f9c90aa8`. This proves bounded current-target redirect trigger reads only, not Target* mutation, helper/projectile target redirects, multi-target teams, exact target lifetime, or full MUGEN/IKEMEN target redirect parity.
- `synthetic-imported-numhelper.json`: required imported CMD/CNS State -1 `x -> 200` route where a visual `Helper` spawn makes `NumHelper(42) > 0` true and branches back in the owner state to `264`. This proves bounded helper-count trigger plumbing only, not helper VM execution, redirects, keyctrl, parent/root semantics, helper combat, or full Helper parity.
- `synthetic-imported-helper-velocity.json`: required imported CMD/CNS route where a visual `Helper` consumes bounded static `velset = 3,-1` and trace actor-frame evidence records observed helper velocity/position movement. Current checksum: `dc0fb203`. This proves bounded visual helper motion plus helper snapshot resource projection only, not exact helper VM/resource semantics, helper physics parity, redirects, keyctrl, parent/root semantics, helper combat, or full Helper parity.
- `synthetic-imported-helper-scale.json`: required imported CMD/CNS route where a visual `Helper` consumes bounded static `scale = 2,0.5` and trace actor-frame plus helper payload evidence records render-scale movement. Current checksum: `a161d896`. This proves bounded visual helper scale plus helper snapshot resource projection only, not exact helper VM/resource semantics, collision-scale parity, ownpal/palette ownership, redirects, keyctrl, parent/root semantics, helper combat, or full Helper parity.
- `synthetic-imported-helper-supermovetime.json`: required imported CMD/CNS route where a visual `Helper` with bounded `supermovetime = 4` advances during `SuperPause` after source movetime expires while the opponent and sibling Explod freeze. Current checksum: `857f927e`. This proves bounded visual helper pause-budget handling plus helper snapshot resource projection only, not exact helper VM/resource semantics, exact pause layering/tick order, redirects, keyctrl, parent/root semantics, helper combat, or full Helper parity.
- `synthetic-imported-helper-pausemovetime.json`: required imported CMD/CNS route where a visual `Helper` with bounded `pausemovetime = 4` advances during regular `Pause` after source movetime expires while the opponent and sibling Explod freeze. Current checksum: `bfe83f64`. This proves bounded visual helper regular-pause budget handling plus helper snapshot resource projection only, not exact helper VM/resource semantics, exact pause layering/tick order, redirects, keyctrl, parent/root semantics, helper combat, or full Helper parity.
- `synthetic-imported-helper-ignorehitpause.json`: required imported CMD/CNS route where a visual `Helper` with `ignorehitpause = 1` advances through hitpause while sibling Explods provide freeze/advance contrast. Current checksum: `7d008a66`. This proves bounded visual helper hitpause advance plus helper snapshot resource projection only, not exact helper VM/resource semantics, exact hitpause layering/tick order, redirects, keyctrl, parent/root semantics, helper combat, or full Helper parity.
- `synthetic-imported-helper-explod.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes from state `1200` to `1205` / anim `925`, and spawns owner-side Explod anim `939` with `parentId = p1-helper-0`. Current checksum: `87ae363f`. This proves static helper-local visual Explod handoff only, not helper-bound Explod timing, helper-owned Projectile combat/contact presentation, exact helper `HitDef` lifetime/multi-hit parity, full helper combat/contact parity, dynamic params, redirects beyond the current bounded slice, or full Helper/Explod parity.
- `synthetic-imported-helper-parentroot.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, reads cloned owner/root runtime state through `Parent,StateNo`, `Parent,Vel X`, and `Root,Anim`, then routes from state `1200` to `1400` / anim `940`. Current checksum: `5154220c`. This proves bounded read-only helper-local Parent/Root redirect routing only, not invalid-destination bottom parity, nested helper ancestry where root differs from parent, player-state parent/root ownership, teams/simul, redirect mutation, keyctrl, or full helper redirect parity.
- `synthetic-imported-helper-projectile.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes from state `1200` to `1212` / anim `932`, and spawns owner-side Projectile anim `943` with `parentId = p1-helper-0`. Current checksum: `b6269136`. This proves static helper-local Projectile spawn only, not helper-owned Projectile combat/contact presentation, exact projectile namespace scopes, or full Helper/Projectile parity.
- `synthetic-imported-helper-removeexplod.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1206 -> 1207` / anims `926` and `927`, spawns owner-side Explod anim `940`, and emits helper-parented Explod `remove` lifecycle evidence for static id `8810`. Current checksum: `ff8658a2`. This proves static helper-local visual RemoveExplod cleanup by id only, not helper-owned effect namespaces, dynamic remove params, exact helper-bound timing, FightFX/common routing, full helper combat/contact parity, or full Helper/Explod parity.
- `synthetic-imported-helper-modifyexplod.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1208 -> 1209` / anims `928` and `929`, spawns owner-side Explod anim `941`, and mutates that helper-parented Explod by static id with velocity, scale, sprite-priority, pause-budget, remove-time, remove-on-get-hit, and ignore-hitpause payload evidence. Current checksum: `0749041c`. This proves static helper-local visual ModifyExplod mutation only, not helper-owned effect namespaces, dynamic modify params, position rebinding, exact helper-bound timing, FightFX/common routing, full helper combat/contact parity, or full Helper/Explod parity.
- `synthetic-imported-helper-modifyprojectile.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1214 -> 1215` / anims `934` and `935`, spawns owner-side Projectile anim `945`, and mutates that helper-parented Projectile by static id with velocity, scale, sprite-priority, remove-time, priority, hit-budget, and miss-time payload evidence. Current checksum: `09d3f7e4`. This proves static helper-local visual ModifyProjectile mutation only, not helper-owned Projectile combat/contact presentation, dynamic projectile params, exact namespace scopes, `ProjContact` timing, or full Helper/Projectile parity.
- `synthetic-imported-helper-projhit.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1216 -> 1217` / anims `936` and `937`, spawns owner-side Projectile anim `946`, and branches on `ProjHit(8853)` after that helper-parented Projectile contacts P2 with hit/contact payload evidence. Current checksum: `2d9a281e`. The gate also requires owner-side target-link evidence, shared `hitsound = S5,0` / FightFX `sparkno = F7002` package telemetry, and Projectile payload `parentId = p1-helper-0`. This proves helper-local Projectile contact-trigger routing plus bounded owner-side package telemetry, not exact `ProjContact` / `ProjHit` timing and lifetime, dynamic projectile params, exact namespace scopes, or full Helper/Projectile parity.
- `synthetic-imported-helper-projhittime-any.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1260 -> 1261` / anims `986` and `987`, spawns owner-side Projectile anim `988`, and branches on `ProjHitTime(0) >= 0` after that helper-parented Projectile hits P2. Current checksum: `bca9f47b`. The gate also requires owner-side target-link evidence, shared `hitsound = S5,12` / FightFX `sparkno = F7018` package telemetry, and Projectile payload `parentId = p1-helper-0`. This proves bounded helper-local any-projectile hit-time reads for helper-parented Projectiles, not exact hit timing/lifetime, multi-projectile any-id selection, redirects, teams, or full Helper/Projectile parity.
- `synthetic-imported-helper-projguardedtime-any.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1262 -> 1263` / anims `989` and `990`, spawns owner-side Projectile anim `991`, and branches on `ProjGuardedTime(0) >= 0` after that helper-parented Projectile is guarded by P2. Current checksum: `1f1a38e4`. The gate also requires owner-side target-link evidence, shared `guardsound = S6,12` / FightFX `guard.sparkno = F7019` package telemetry, and Projectile payload `parentId = p1-helper-0`. This proves bounded helper-local any-projectile guard-time reads for helper-parented Projectiles, not exact guard timing/lifetime, multi-projectile any-id selection, redirects, teams, or full Helper/Projectile parity.
- `synthetic-imported-helper-projcontacttime-any.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1264 -> 1265` / anims `992` and `993`, spawns owner-side Projectile anim `994`, and branches on `ProjContactTime(0) >= 0` after that helper-parented Projectile records contact through a guarded P2 contact. Current checksum: `0d9f7829`. The gate also requires owner-side target-link evidence, shared `guardsound = S6,13` / FightFX `guard.sparkno = F7020` package telemetry, and Projectile payload `parentId = p1-helper-0`. This proves bounded helper-local any-projectile contact-time reads for helper-parented Projectiles, not exact contact timing/lifetime, multi-projectile any-id selection, redirects, teams, or full Helper/Projectile parity.
- `synthetic-imported-helper-projguard.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1218 -> 1219` / anims `938` and `947`, spawns owner-side Projectile anim `948`, and branches on `ProjGuarded(8854)` plus `ProjGuardedTime(8854) >= 1` after that helper-parented Projectile is guarded by P2 with guard/contact payload evidence. Current checksum: `d2e2f20d`. The gate also requires owner-side target-link evidence, shared `guardsound = S6,0` / FightFX `guard.sparkno = F7004` package telemetry, and Projectile payload `parentId = p1-helper-0`. This proves helper-local Projectile guard-trigger routing plus bounded owner-side package telemetry, not exact `ProjContact` / `ProjHit` / `ProjGuarded` timing and lifetime, dynamic projectile params, exact namespace scopes, or full Helper/Projectile parity.
- `synthetic-imported-helper-projcontact.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes through `1200 -> 1220 -> 1221` / anims `949` and `950`, spawns owner-side Projectile anim `951`, and branches on `ProjContact(8855)` plus `ProjContactTime(8855) >= 1` after that helper-parented Projectile records generic contact through a guarded P2 contact with guard/contact payload evidence. Current checksum: `4dcbdd25`. The gate also requires owner-side target-link evidence, shared `guardsound = S6,0` / FightFX `guard.sparkno = F7004` package telemetry, and Projectile payload `parentId = p1-helper-0`. This proves helper-local generic Projectile contact-trigger routing plus bounded owner-side package telemetry, not exact `ProjContact` / `ProjHit` / `ProjGuarded` timing and lifetime, dynamic projectile params, exact namespace scopes, or full Helper/Projectile parity.
- `synthetic-imported-helper-hitdef.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes into state `1200`, activates a helper-owned direct `HitDef`, damages P2 for 29, emits helper-side `S5,0` plus FightFX `F7006` contact-package telemetry, and branches to `1222` / anim `952` after `EnemyNear, Life` observes the hit. Current checksum: `89f9e876`. This proves bounded helper-local direct HitDef contact only, not helper target mutation/multi-target parity, helper-owned custom state tables, throws, teams, exact helper hitpause/tick order, exact helper `HitDef` lifetime/multi-hit parity, visual parity, or full Helper combat parity.
- `synthetic-imported-helper-target.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes into state `1200`, activates `HitDef id = 8877`, damages P2 for 31, records target link `p1-helper-0 -> p2 / 8877`, emits helper-side `S5,1` plus FightFX `F7007`, and branches to `1223` / anim `953` through `NumTarget(8877)` plus `Target(8877), Life`. Current checksum: `68f95b67`. This proves bounded helper-owned explicit-id target memory only, not default/undefined target ids, helper `Target*` mutation controllers, helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper `HitDef` lifetime/multi-hit parity, visual parity, or full Helper target parity.
- `synthetic-imported-helper-default-target.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes into state `1200`, activates `HitDef` without `id`, damages P2 for 33, records target link `p1-helper-0 -> p2 / 0`, emits helper-side `S5,4` plus FightFX `F7010`, and branches to `1229` / anim `961` through `NumTarget(0)` plus `Target(0), Life`. Current checksum: `e1bcced0`. This proves bounded helper-owned default target id `0` memory only, not helper `Target*` mutation controllers, helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper `HitDef` lifetime/multi-hit parity, visual parity, or full Helper target parity.
- `synthetic-imported-helper-bare-target.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes into state `1200`, activates `HitDef id = 8878`, damages P2 for 35, records target link `p1-helper-0 -> p2 / 8878`, emits helper-side `S5,5` plus FightFX `F7011`, and branches to `1230` / anim `962` through `NumTarget(8878)` plus bare `Target, Life`. Current checksum: `15f3c1db`. This proves bounded helper-owned bare Target trigger reads after explicit-id target memory only, not helper `Target*` mutation controllers, helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper `HitDef` lifetime/multi-hit parity, visual parity, or full Helper target parity.
- `synthetic-imported-helper-target-controllers.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes into state `1200`, activates `HitDef id = 8879`, damages P2 for 37, records target link `p1-helper-0 -> p2 / 8879`, emits helper-side `S5,6` plus FightFX `F7012`, then branches to `1231` / anim `963` and applies helper-owned `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop`. The gate requires binding offset `36,-12`, final P2 `life = 944` / `power = 40`, and helper payload `targetCount = 0` after drop. Current checksum: `61f4c61e`. This proves bounded helper-owned Target side effects from direct helper `HitDef` target memory only, not helper `TargetState`, helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, helper/projectile target redirects beyond the gated subset, exact helper hitpause/tick order, visual parity, or full Helper target parity.
- `synthetic-imported-helper-targetstate.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, routes into state `1200`, activates `HitDef id = 8880`, damages P2 for 31, records target link `p1-helper-0 -> p2 / 8880`, then branches to `1232` / anim `964` and executes helper-local `TargetState value = 888`. The gate requires P2 custom-owner actor frames for owner-backed states `888` and `889`, final P2 state `0`/control after `SelfState`, helper payload `targetCount = 1`, and helper target-link evidence. Current checksum: `011633b8`. This proves bounded helper-local TargetState into owner-backed state data only, not helper-owned custom state tables, throws, teams, multi-target/helper-owned opponent selection, helper/projectile target redirects beyond the gated subset, exact helper hitpause/tick order, visual parity, or full Helper TargetState parity.
- `synthetic-imported-helper-projectile-target.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile id `8860` with `parentId = p1-helper-0`, the Projectile hits P2 for 18 damage, MatchWorld exposes owner target link `p1 -> p2 / 8860` plus helper target link `p1-helper-0 -> p2 / 8860`, and the helper branches from `1224` to `1225` / anim `955` through `NumTarget(8860)` plus `Target(8860), Life`. Current checksum: `49261b53`. This proves bounded helper-parented Projectile explicit-id target memory only, not helper `Target*` mutation controllers, helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, visual parity, or full Helper/Projectile target parity.
- `synthetic-imported-helper-projectile-target-controllers.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile id `8861` with `parentId = p1-helper-0`, the Projectile hits P2, MatchWorld exposes owner target link `p1 -> p2 / 8861` plus helper target link `p1-helper-0 -> p2 / 8861`, and the helper branches from `1233` to `1234` / anim `966` through `NumTarget(8861)` plus `Target(8861), Life` before applying helper-owned `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop`. The gate requires binding offset `36,-12`, final P2 `life = 958` / `power = 40`, helper payload `targetCount = 0`, projectile payload `parentId = p1-helper-0`, and shared `S5,7` / FightFX `F7013` contact package evidence. Current checksum: `58688be8`. This proves bounded helper-owned Target side effects from explicit-id helper-parented Projectile target memory only, not helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, visual parity, or full Helper/Projectile Target parity.
- `synthetic-imported-helper-projectile-targetstate.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile id `8862` with `parentId = p1-helper-0`, the Projectile hits P2, MatchWorld exposes owner target link `p1 -> p2 / 8862` plus helper target link `p1-helper-0 -> p2 / 8862`, and the helper branches from `1237` to `1238` / anim `972` through `NumTarget(8862)` plus `Target(8862), Life` before executing helper-owned `TargetState value = 888`. The gate requires P2 custom-owner actor frames for owner-backed states `888` and `889`, final P2 state `0`/control after `SelfState`, helper payload `targetCount = 1`, projectile payload `parentId = p1-helper-0`, and shared `S5,9` / FightFX `F7015` contact package evidence. Current checksum: `b12e1cb3`. This proves bounded helper-owned TargetState from helper-parented Projectile explicit target memory only, not helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, visual parity, or full Helper/Projectile TargetState parity.
- `synthetic-imported-helper-projectile-default-targetstate.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile with omitted `projid` / `id` and `parentId = p1-helper-0`, the Projectile hits P2, MatchWorld exposes owner target link `p1 -> p2 / 0` plus helper target link `p1-helper-0 -> p2 / 0`, and the helper branches from `1239` to `1240` / anim `975` through `NumTarget(0)` plus `Target(0), Life` before executing helper-owned `TargetState value = 888`. The gate requires P2 custom-owner actor frames for owner-backed states `888` and `889`, final P2 state `0`/control after `SelfState`, helper payload `targetCount = 1`, projectile payload `parentId = p1-helper-0` / `effectId = 0`, and shared `S5,10` / FightFX `F7016` contact package evidence. Current checksum: `918c42a1`. This proves bounded helper-owned TargetState from helper-parented Projectile default target memory only, not helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, visual parity, or full Helper/Projectile TargetState parity.
- `synthetic-imported-helper-projectile-bare-target.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile id `8863` with `parentId = p1-helper-0`, the Projectile hits P2, MatchWorld exposes owner target link `p1 -> p2 / 8863` plus helper target link `p1-helper-0 -> p2 / 8863`, and the helper branches from `1241/977` to `1242/978` through `NumTarget(8863)` plus bare `Target, Life`. The gate requires helper payload `targetCount = 1`, projectile payload `effectId = 8863`, final P2 `life = 982`, and shared `S5,11` / FightFX `F7017` contact package evidence. Current checksum: `8c9129c1`. This proves bounded helper-local bare Target reads from helper-parented Projectile target memory only, not helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, visual parity, or full Helper/Projectile Target parity.
- `synthetic-imported-helper-projectile-default-target-controllers.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile with omitted `projid` / `id` and `parentId = p1-helper-0`, the Projectile hits P2, MatchWorld exposes owner target link `p1 -> p2 / 0` plus helper target link `p1-helper-0 -> p2 / 0`, and the helper branches from `1235` to `1236` / anim `969` through `NumTarget(0)` plus `Target(0), Life` before applying helper-owned `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop`. The gate requires binding offset `36,-12`, final P2 `life = 958` / `power = 40`, helper payload `targetCount = 0`, projectile payload `parentId = p1-helper-0`, projectile effect id `0`, and shared `S5,8` / FightFX `F7014` contact package evidence. Current checksum: `0c4c69ae`. This proves bounded helper-owned Target side effects from helper-parented Projectile default target memory only, not helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, visual parity, or full Helper/Projectile Target parity.
- `synthetic-imported-helper-projectile-default-target.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile without `projid`/`id` with `parentId = p1-helper-0`, the Projectile hits P2 for 18 damage, MatchWorld exposes owner target link `p1 -> p2 / 0` plus helper target link `p1-helper-0 -> p2 / 0`, and the helper branches from `1226` to `1227` / anim `958` through `NumTarget(0)` plus `Target(0), Life`. Current checksum: `b0daddf6`. This proves bounded helper-parented Projectile default target id `0` memory only, not helper `Target*` mutation controllers, helper-owned custom state tables, teams, multi-target/helper-owned opponent selection, exact helper hitpause/tick order, exact helper Projectile lifetime parity, visual parity, or full Helper/Projectile target parity.
- `synthetic-imported-helper-numexplod.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Explod anim `942`, evaluates helper-local `NumExplod(8830) > 0`, and routes from `1200` to `1210` / anim `930` with helper-parented Explod lifecycle and payload evidence. Current checksum: `4328278a`. This proves helper-local `NumExplod(id)` branch evidence only, not exact helper effect-count parity, helper-owned effect namespaces, exact parent/root/team scopes, or full Helper trigger parity.
- `synthetic-imported-helper-numhelper.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, evaluates helper-local `NumHelper(42) > 0`, and routes from `1200` to `1211` / anim `931` with helper lifecycle and payload evidence. Current checksum: `4e32e951`. This proves helper-local `NumHelper(id)` branch evidence only, not exact helper ownership scopes, broader indexed/team/helper-owned redirects beyond caller-provided `EnemyNear(index)` lists, full helper combat/contact parity, or full Helper trigger parity.
- `synthetic-imported-helper-numproj.json`: required imported CMD/CNS route where a spawned visual `Helper` runs its bounded helper-local micro-VM, spawns owner-side Projectile anim `944` with `parentId = p1-helper-0`, evaluates helper-local `NumProjID(8851) > 0`, and routes from `1200` to `1213` / anim `933` with helper-parented Projectile lifecycle and payload evidence. Current checksum: `3312a554`. This proves helper-local `NumProjID(id)` branch evidence only, not helper-owned Projectile combat/contact presentation, exact projectile namespaces/scopes, `ProjContact` timing, dynamic ids, or full Helper/Projectile parity.
- `synthetic-imported-numproj.json`: required imported CMD/CNS State -1 `x -> 200` route where a colliding `Projectile` spawn makes `NumProjID(77) > 0` true and branches back in the owner state to `273`. This proves bounded projectile-count trigger plumbing only, not exact projectile lifetime/count parity, helper-owned projectiles, redirects, multi-projectile teams, or full Projectile parity.
- `synthetic-imported-numexplod.json`: required imported CMD/CNS State -1 `x -> 200` route where a visual `Explod` spawn makes `NumExplod(9000) > 0` true and branches back in the owner state to `274`. This proves bounded explod-count trigger plumbing only, not Explod binding, remove triggers, FightFX/common animation routing, exact scaling parity, ownpal/remappal, or exact lifetime parity.
- `synthetic-imported-removeexplod.json`: required imported CMD/CNS State -1 `x -> 200` route where a visual `Explod` spawns, `RemoveExplod id = 9000` executes, MatchWorld emits an explod `remove` lifecycle event, and the final frame has no P1 Explod actors. This proves bounded id removal only, not advanced remove triggers, binding, FightFX/common animation routing, or exact lifetime parity.
- `synthetic-imported-explod-velocity.json`: required imported CMD/CNS State -1 `x -> 200` route where a visual `Explod` consumes bounded `vel`/`accel` params and actor-frame evidence records observed position/velocity movement. This proves bounded visual movement only, not exact bind/tick-order parity, exact lifetime/physics parity, exact scaling parity, ownpal/remappal, or FightFX/common animation routing.
- `synthetic-imported-explod-bind.json`: required imported CMD/CNS State -1 `x -> 200` route where a visual `Explod` consumes bounded `bindtime`, the source state executes `PosAdd`, and actor-frame evidence records the Explod following owner movement. This proves bounded owner-side `p1`/`front`/`back` bind plumbing only, not `p2` binding, exact tick order, exact lifetime/physics parity, exact scaling parity, ownpal/remappal, or FightFX/common animation routing.
- `synthetic-imported-explod-scale.json`: required imported CMD/CNS State -1 `x -> 200` route where a visual `Explod` consumes bounded `scale = 2,0.5`, renderer snapshots expose `renderScale`, projection scales the sprite/collision-space boxes around the sprite axis, and actor-frame evidence records observed scale. This proves bounded static scale only, not exact MUGEN/IKEMEN scaling parity, palette ownership, FightFX/common animation routing, shadow/blend edge cases, or tick-order interactions.
- `synthetic-imported-targetbind-pause.json`: required imported CMD/CNS route where a real `HitDef` creates target memory, `TargetBind` executes from that target during source `SuperPause` movetime, `PosAdd` moves the source actor, and the bound target remains offset through `MatchWorld.targetLinks` plus `matchPauseAdvances` evidence. This proves only the bounded two-actor TargetBind + SuperPause movetime path, not throws, helper-owned targets, redirect stacks, or exact MUGEN/IKEMEN target persistence.
- `synthetic-imported-superpause.json`: required imported CMD/CNS State -1 `x -> 200` route where `SuperPause` compiles into typed `pause:superpause` operation evidence, emits a pause runtime event, preserves a `matchPause` snapshot for actor/state/darken/remaining/movetime evidence, and proves the opponent actor remains frozen across the pause window.
- `synthetic-imported-superpause-pausebg.json`: required imported CMD/CNS State -1 `x -> 200` route where `SuperPause pausebg = 0` preserves bounded `pauseBg = false` match-pause metadata. Current checksum: `49bcfe16`; final checksum: `397a8fae`. This proves bounded pause-background snapshot metadata only, not actual renderer/background update parity, exact stage/BGCtrl pause timing, renderer visual suppression/playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, super backgrounds, or full super presentation parity.
- `synthetic-imported-superpause-anim-disabled.json`: required imported CMD/CNS State -1 `x -> 200` route where `SuperPause anim = -1` suppresses optional `superAnim` snapshot metadata and is gated through match-pause evidence as `superAnimAbsent = true`. Current checksum: `fc7a2ca4`; final checksum: `5be3ca6c`. This proves bounded no-animation snapshot metadata only, not renderer visual suppression/playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, super backgrounds, or full super presentation parity.
- `synthetic-imported-superpause-default-anim.json`: required imported CMD/CNS State -1 `x -> 200` route where omitted `SuperPause anim` defaults to FightFX action `30`, survives the pause-world snapshot, and is gated through match-pause evidence as `superAnim` raw/source/action/offset. Current checksum: `318c5e9f`; final checksum: `747e7619`. This proves bounded default SuperPause presentation metadata only, not renderer visual playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, super backgrounds, or full super presentation parity.
- `synthetic-imported-superpause-dynamic-anim-pos.json`: required imported CMD/CNS State -1 `x -> 200` route where dynamic `SuperPause anim = var(6)` plus `pos = var(7),var(8)` resolves through active expression fallback, survives the pause-world snapshot, and is gated through match-pause evidence as `superAnim` raw/source/action/offset. Current checksum: `e6bfbf75`. This proves bounded dynamic SuperPause presentation metadata only, not renderer visual playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, super backgrounds, or full super presentation parity.
- `synthetic-imported-superpause-anim-pos.json`: required imported CMD/CNS State -1 `x -> 200` route where explicit `SuperPause anim = S200` plus `pos = 24,-48` compiles into typed pause metadata, survives the pause-world snapshot, and is gated through match-pause evidence as `superAnim` raw/source/action/offset. Current checksum: `f7dcdc9d`. This proves bounded explicit SuperPause presentation metadata only, not renderer visual playback parity, FightFX/common asset lookup/rendering, super backgrounds, or full super presentation parity.
- `synthetic-imported-superpause-projectile-freeze.json`: required imported CMD/CNS State -1 `x -> 200` route where `Projectile` and `SuperPause` execute together, the projectile effect actor spawns through `MatchWorld` lifecycle/store evidence, `requiredMatchPauseAdvances` proves the `p1` projectile effect advances while the source `movetime` is active, and `requiredMatchPauseFreezes` proves it remains frozen afterward during the bounded pause window.
- `synthetic-imported-superpause-effect-freeze.json`: required imported CMD/CNS State -1 `x -> 200` route where `Helper`, `Explod`, and `SuperPause` execute together, helper/explod actors spawn through `MatchWorld` lifecycle/store evidence, `requiredMatchPauseAdvances` proves source-owned helper/explod actors advance while source `movetime` is active, and `requiredMatchPauseFreezes` proves both freeze afterward during the bounded pause window. This proves visual effect actor pause evidence only, not Helper VM, Explod binding/removal parity, exact pause layering, or IKEMEN/MUGEN effect parity.
- `synthetic-imported-explod-ignorehitpause.json`: required imported CMD/CNS State -1 `x -> 200` route where two visual Explods spawn before a direct hit, one Explod freezes during hitpause, the second Explod with `ignorehitpause = 1` advances through hitpause, and MatchWorld store/lifecycle evidence proves both actors were produced by the P1 effect store. Current checksum: `a351168e`. This proves bounded Explod `ignorehitpause` only, not exact MUGEN/IKEMEN hitpause layering, helper-bound Explod timing, FightFX/common animation routing, or full pause parity.
- `synthetic-imported-explod-supermovetime.json`: required imported CMD/CNS State -1 `x -> 200` route where two visual Explods and `SuperPause` execute together, one Explod freezes after source `movetime`, the second Explod with `supermovetime = 4` advances after source `movetime`, and MatchWorld store/lifecycle evidence proves both actors were produced by the P1 effect store. Current checksum: `1d99f66f`. This proves bounded Explod `supermovetime` only, not regular `Pause` behavior, helper-bound Explod timing, FightFX/common animation routing, or full MUGEN/IKEMEN pause layering.
- `synthetic-imported-explod-pausemovetime.json`: required imported CMD/CNS State -1 `x -> 200` route where two visual Explods and regular `Pause` execute together, one Explod freezes after source `movetime`, the second Explod with `pausemovetime = 4` advances after source `movetime`, and MatchWorld store/lifecycle evidence proves both actors were produced by the P1 effect store. Current checksum: `467c2030`. This proves bounded Explod `pausemovetime` only, not `SuperPause` layering, helper-bound Explod timing, FightFX/common animation routing, or full MUGEN/IKEMEN pause layering.
- `synthetic-imported-projectile.json`: required imported CMD/CNS State -1 `x -> 200` route where `Projectile` compiles into typed `projectile` operation evidence, spawns and removes a colliding projectile effect actor through required `MatchWorld` lifecycle events, proves the `p1` effect store produced a projectile, produces hit event/combat reason evidence, leaves world-visible projectile target-memory evidence through `targetLinks`, and evaluates a bounded `ProjHit(77)` branch back in the owner state. This proves the bounded projectile actor lifecycle plus target-memory/contact-trigger path only, not exact priority classes, exact trigger timing, visible remove/cancel animation playback, or full projectile parity.
- `synthetic-imported-projectile-target-redirect.json`: required imported CMD/CNS State -1 `x -> 200` route where a player-owned Projectile-only hit records owner target link `p1 -> p2 / 77`, removes through MatchWorld lifecycle events, preserves terminal hit payload evidence, and routes P1 from state `200` to `277` through `NumTarget(77)` plus `Target(77), Life <= 969`. Current checksum: `cd099094`. This proves bounded owner-local Projectile target redirect reads only, not target mutation through redirects, helper-owned projectile targets, teams, multi-target selection, exact target lifetime/tick order, or full Projectile target parity.
- `synthetic-imported-projectile-motion.json`: required imported CMD/CNS State -1 `x -> 200` route where `Projectile` consumes bounded `accel` plus `projscale`, spawns through `MatchWorld` lifecycle/store evidence, actor-frame evidence proves velocity changes from acceleration, and renderer-independent `renderScale` evidence proves static projectile scale reaches snapshots. Current checksum: `4e5c5114`. This proves bounded visual projectile motion/scale only, not exact `velmul`, hitbox scaling parity, contact timing, or full MUGEN/IKEMEN projectile parity.
- `synthetic-imported-projectile-velmul.json`: required imported CMD/CNS State -1 `x -> 200` route where `Projectile` consumes bounded `velmul = 0.5,1`, spawns through `MatchWorld` lifecycle/store evidence, and actor-frame evidence proves observed X velocity decay. Current checksum: `749b8552`. This proves bounded visual velocity multiplier support only, not exact tick-order parity, hitbox scaling parity, contact timing, or full MUGEN/IKEMEN projectile parity.
- `synthetic-imported-modifyprojectile.json`: required imported CMD/CNS State -1 `x -> 200` route where `ModifyProjectile` mutates a live owner-side projectile and proves typed `modifyprojectile` operation evidence plus observed velocity/scale/effect-payload changes. Current checksum: `63a87da1`. This proves bounded static owner-projectile mutation only, not exact tick-order, redirects, helper-owned projectiles, multi-projectile selection parity, or full MUGEN/IKEMEN projectile parity.
- `synthetic-imported-modifyexplod.json`: required imported CMD/CNS State -1 `x -> 200` route where `ModifyExplod` mutates a live owner-side visual Explod and proves typed `modifyexplod` operation evidence plus observed velocity, scale, effect-payload priority, remove-time, remove-on-get-hit, and pause-budget telemetry. Current checksum: `bca75991`. This proves bounded static owner-Explod mutation only, not dynamic params, position rebinding, helper-owned effect namespaces, FightFX/common routing, remove-trigger parity, exact tick order, or full Explod lifecycle parity.
- `synthetic-imported-projectile-contact.json`: required imported CMD/CNS State -1 `x -> 200` route where `Projectile` contact evaluates a bounded `ProjContact(77)` branch back in the owner state while still proving projectile lifecycle, hit evidence, target-link evidence, producer-store evidence, and a shared contact package for attacker-side `hitsound = S5,0` plus FightFX `sparkno = F7002` metadata. Current checksum: `ba22ed74`. This proves projectile contact-trigger and presentation telemetry plumbing only, not exact trigger timing, multi-target lifetime, helper ownership, SND playback, exact spark lookup/binding/layering/timing, or full projectile parity.
- `synthetic-imported-projectile-guard.json`: required imported CMD/CNS State -1 `x -> 200` route where the same bounded `Projectile` actor carries typed guard params, a held-back defender blocks it through the shared partial hit/guard combat resolver, guard event/combat reason evidence plus projectile target-link evidence are produced, a bounded `ProjGuarded(77)` branch evaluates back in the owner state, and a shared contact package ties attacker-side `guardsound = S6,0` to FightFX `guard.sparkno = F7004` metadata. Current checksum: `4bcc5650`. This proves bounded projectile guard/contact-trigger and presentation telemetry only, not exact priority classes, exact trigger timing, exact guard-state timing, SND playback, exact spark lookup/binding/layering/timing, visible remove/cancel animation playback, or full projectile parity.
- `synthetic-imported-projectile-hittime-any.json`: required imported CMD/CNS route where a player-owned Projectile hits P2 and P1 branches through `ProjHitTime(0) >= 0` into state/action `282`. Current checksum: `47c1cf7f`. This proves bounded any-projectile hit-time owner-state reads for player-owned Projectiles with hit event/reason, lifecycle, effect-store, and target-link evidence, not exact hit timing/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams, or full Projectile hit parity.
- `synthetic-imported-projectile-contacttime-any.json`: required imported CMD/CNS route where a player-owned Projectile hits P2 and P1 branches through `ProjContactTime(0) >= 0` into state/action `281`. Current checksum: `f1751155`. This proves bounded any-projectile contact-time owner-state reads for player-owned Projectiles with hit event/reason, lifecycle, effect-store, and target-link evidence, not exact contact timing/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams, or full Projectile contact parity.
- `synthetic-imported-projectile-guardedtime-any.json`: required imported CMD/CNS route where P2 guards a player-owned Projectile and P1 branches through `ProjGuardedTime(0) >= 0` into state/action `279`. Current checksum: `c8473340`. This proves bounded any-projectile guarded-time owner-state reads for player-owned Projectiles with guard event/reason, lifecycle, effect-store, and target-link evidence, not exact guard timing/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams, or full Projectile guard parity.
- `synthetic-imported-projectile-multihit.json`: required imported CMD/CNS State -1 `x -> 200` route where one bounded `Projectile` actor uses `projhits = 2` and `projmisstime = 3`, hits the same defender twice with a cooldown between contacts, remains active after the first hit, and removes after the second hit. This proves only the current single-target multi-hit/cooldown path, not exact MUGEN/IKEMEN multi-target projectile parity, hitpause layering, terminal playback for this no-terminal-action route, helper-owned projectiles, or full projectile lifecycle parity.
- `synthetic-imported-projectile-clash.json`: required imported CMD/CNS State -1 route where both imported players fire equal-`projpriority` projectiles with `projcancelanim`, both projectiles spawn through `MatchWorld` lifecycle/store evidence, required runtime event substrings prove bounded projectile clash trade/removal plus preserved cancel-animation metadata, and required actor-frame evidence proves bounded visible cancel terminal playback for both removed projectiles. This proves only the current equal-priority projectile-vs-projectile subset, not exact MUGEN/IKEMEN priority classes, exact cancel timing, helper-owned projectiles, or full projectile parity.
- `synthetic-imported-projectile-priority-cancel.json`: required imported CMD/CNS State -1 route where both imported players fire projectiles with different `projpriority` values, both projectiles spawn through `MatchWorld` lifecycle/store evidence, the lower-priority projectile is removed with preserved `projcancelanim` metadata plus bounded visible loser cancel playback, and the higher-priority projectile remains in its producer effect store after decrementing remaining priority by 1. This proves only the current higher-priority cancel/decrement subset, not exact MUGEN/IKEMEN priority classes, exact cancel timing, helper-owned projectiles, or full projectile parity.
- `synthetic-imported-helper.json`: required imported CMD/CNS State -1 `x -> 200` route where `Helper` compiles into typed `helper` operation evidence and produces required `MatchWorld` spawn/active lifecycle plus `p1` helper-store evidence for a bounded visual helper effect actor. This proves visual helper actor plumbing only, not helper VM, redirects, key control, helper combat, parent/root binding beyond current metadata, real `DestroySelf` removal/lifecycle semantics, or pause parity.
- `synthetic-imported-explod.json`: required imported CMD/CNS State -1 `x -> 200` route where `Explod` compiles into typed `explod` operation evidence and produces required `MatchWorld` spawn/active lifecycle plus `p1` explod-store evidence for a bounded visual explod effect actor. This proves visual explod actor plumbing only, not exact binding, exact scaling parity, ownpal/remappal, FightFX/common animation routing, or remove-trigger parity; separate gates cover bounded owner-side `bindtime`, static `scale`, and `vel`/`accel`.
- `kfm-official-x.json`: optional official KFM `x -> 200` route when `.scratch/fixtures/kfm-official.zip` exists.
- `kfm-official-basic-movement.json`: optional official KFM movement route where scripted `F`, `D`, and `U` input enters real KFM walk `20`, crouch prep `11`, jump `41`, and final idle/control `0`. Current checksum: `ef30066c`. This proves private-fixture movement confidence only, not public bundled KFM support, exact Common1 movement tables, CMD priority/raw-buffer parity, landing/collision/platform parity, or full movement parity.
- `kfm-official-x-hit-sound.json`: optional official KFM `x -> 200` route that requires bounded attacker-side `PlaySnd` telemetry for authored HitDef `hitsound = S5,0` after contact when `.scratch/fixtures/kfm-official.zip` exists. Current checksum: `bd153db9`. This proves private-fixture event telemetry only, not SND decode/playback, channel priority, mixing, timing, or full audio parity.
- `kfm-official-x-hit-spark.json`: optional official KFM `x -> 200` route that requires bounded attacker-side `HitSpark` telemetry for authored `sparkno = 0` after contact when `.scratch/fixtures/kfm-official.zip` exists. Current checksum: `bd153db9`. This proves private-fixture event telemetry only, not exact FightFX/common lookup, layering, scale, palette, render timing, or full hit-effect parity.
- `kfm-official-qcf-x.json`: optional official KFM `QCF_x -> 1000` route when `.scratch/fixtures/kfm-official.zip` exists. Current checksum: `349cd995`. The gate requires command buffering, ordered actor-frame `11 -> 20 -> 1000`, ordered `ChangeState` / `PosAdd` / `HitDef`, typed `kinematic:posadd` / `hitdef`, authored move sound `S0,3`, contact hitsound `S5,4`, target link `p1 -> p2 / 0`, final P1 `power = 35` / `targetCount = 1`, and final P2 `life = 910` with hitfall telemetry. This proves one bounded private-fixture Kung Fu Palm input-to-contact route, not exact timing/pushback, spark rendering, broad specials, public asset support, or full parity.
- `kfm-official-x-guard.json`: optional official KFM `x -> 200` route with held-back guard event/combat reason evidence when `.scratch/fixtures/kfm-official.zip` exists.
- `kfm-official-default-guard-state.json`: optional trace where a synthetic imported HitDef without `p2stateno` is guarded by official KFM as defender and proves real Common1 stand guard-hit states `150 -> 151` execute with ordered controller/typed-operation evidence through `ChangeAnim` -> `ChangeState` -> `HitVelSet` -> `kinematic:hitvelset` -> `CtrlSet` -> `resource:ctrlset` -> `ChangeState`, plus actor-frame stand state/physics/body evidence and final guarded chip life/control. Current checksum: `885bb1da`. This proves the current guarded stand state/controller/frame route only, not exact guard distance, guard-start/end, crouch/air guard transitions, or guard effect parity.
- `kfm-official-default-guard-slide-stop.json`: optional trace using the private official KFM fixture where direct guarded contact reaches real Common1 stand guard-hit state `151` and requires stricter ordered slide-stop/control evidence: `HitVelSet` -> `kinematic:hitvelset` -> `VelSet` -> `kinematic:velset` -> `CtrlSet` -> `resource:ctrlset` -> final `ChangeState`. Current checksum: `885bb1da`. This is private fixture evidence only and proves bounded real KFM stand guard slide-stop/control order, not public bundled KFM support, exact guard timing, proximity guard, guard effects, crouch/air parity, or full MUGEN/IKEMEN guard parity.
- `kfm-official-default-guard-hold-return.json`: optional trace using the private official KFM fixture where the same real Common1 stand guard-hit route returns through observable guard-hold state `130` after `150 -> 151`, while preserving the stricter slide-stop controller/typed-operation order. Current checksum: `885bb1da`. This is private fixture evidence only and proves bounded real KFM stand guard-hold return presence, not public bundled KFM support, exact guard-hold duration, exact guard timing, proximity guard, guard effects, crouch/air guard-hold parity, visual/audio parity, or full MUGEN/IKEMEN guard parity.
- `kfm-official-default-guard-hold-walk-return.json`: optional trace using the private official KFM fixture where the same real Common1 stand guard-hit route returns through observable guard-hold state `130` after `150 -> 151`, then resumes held-back walking state/action `20` with control while preserving slide-stop order. Current checksum: `885bb1da`. This is private fixture evidence only and proves bounded real KFM stand guard-hold-to-walk return, not public bundled KFM support, exact guard-hold duration, exact guard timing, proximity guard, guard effects, crouch/air guard-hold parity, visual/audio parity, or full MUGEN/IKEMEN guard parity.
- `kfm-official-default-crouch-guard-state.json`: optional trace where official KFM blocks while holding down-back and proves the real Common1 crouch guard-hit route reaches `153` through the same composite `command = "holddown"` expression shape, with ordered controller/typed-operation evidence through `152 -> 153` plus actor-frame crouch state/physics/body evidence and final guarded chip life/control. Current checksum: `d11153d0`. This proves bounded KFM crouch branch/controller/frame evidence only, not exact guard distance, guard-start/end, sparks, sounds, air guard, or complete guard parity.
- `kfm-official-default-crouch-guard-hold-crouch-return.json`: optional trace using the private official KFM fixture where real Common1 crouch guard-hit route `152 -> 153` returns through observable crouch guard-hold state `131`, then resumes held-down crouch state/action `11` with control while preserving the `153` slide-stop controller/typed-operation order. Current checksum: `d11153d0`. This is private fixture evidence only and proves bounded real KFM crouch guard-hold-to-crouch return, not public bundled KFM support, exact guard-hold duration, exact guard timing, proximity guard, guard effects, air guard-hold parity, visual/audio parity, or full MUGEN/IKEMEN guard parity.
- `kfm-official-default-air-guard-state.json`: optional trace where official KFM jumps, blocks while holding back, proves real Common1 air guard-hit states `154 -> 155` execute after an `A`-guardable HitDef, executes Common1 `VarSet` system-variable logic, then lands through real Common1 state `52` and resumes held-back walking state/action `20` with control, requiring ordered actor-frame `154 -> 155 -> 52 -> 20` plus `HitVelSet`, `kinematic:hitvelset`, `VelAdd`, `CtrlSet`, `resource:ctrlset`, `VelSet`, `kinematic:velset`, `PosSet`, `kinematic:posset`, and `ChangeState` order evidence plus actor-frame air velocity/body/landing telemetry. Current checksum: `62367dac`. This proves bounded KFM air guard-hit landing/controller/frame evidence only, not exact air guard physics, guard-start/end, sparks, sounds, proximity guard, or IKEMEN parity.
- `kfm-official-auto-guard-start.json`: optional trace where official KFM as defender enters its real Common1 guard-start route from held-back input plus bounded `InGuardDist` before contact, with required ordered `120` `ChangeAnim` -> `StateTypeSet` -> `metadata:statetypeset` -> `ChangeState` -> `130` `ChangeAnim` evidence. Current checksum: `ad493cde`. This proves only the bounded automatic guard-start controller route against the local official fixture, not exact proximity guard, guard end, sparks, sounds, air guard, or IKEMEN parity.
- `kfm-official-auto-guard-end.json`: optional trace where official KFM as defender leaves its real Common1 guard route through state `140` and returns to idle/control after bounded `InGuardDist` is no longer true, with required ordered `120 -> 130` guard-start evidence, `130` stop-guarding `ChangeState`, and return-to-idle `VelSet` / `kinematic:velset`. Current checksum: `ee962d04`. This proves only the bounded automatic guard-end controller route against the local official fixture, not exact guard-end timing, proximity guard, sparks, sounds, air guard, or IKEMEN parity.
- `kfm-official-x-hitstun.json`: optional official KFM `x -> 200` hit route that proves the target reaches the current partial hitstun/get-hit snapshot when `.scratch/fixtures/kfm-official.zip` exists.
- `kfm-official-default-gethit.json`: optional trace where a synthetic imported HitDef without `p2stateno` hits official KFM as defender and proves KFM's real Common1 state `5000` executes. The separate `kfm-official-x.json` gate covers KFM's own state `200` HitDef route.
- `kfm-official-default-gethit-progression.json`: optional trace where official KFM as defender executes real Common1 `5000 -> 5001 -> 0` through `HitShakeOver` and `HitOver` after a HitDef without `p2stateno`, with a fixture-specific actor-frame profile for KFM's `Clsn2 = 2`, state `5001` animation split, body width, and bounded slide velocity. This is private fixture evidence only, not public bundled KFM support or exact timing parity.
- `kfm-official-default-crouch-gethit-progression.json`: optional trace where official KFM as defender holds crouch, executes prep state `11`, then runs real Common1 `5010 -> 5011 -> 0` through `HitShakeOver` and `HitOver` after a HitDef without `p2stateno`, with a fixture-specific actor-frame profile for `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width, bounded slide velocity, and ordered KFM crouch slide controller/typed-operation evidence through `HitVelSet`, `VelMul`, `VelSet`, and `DefenceMulSet`. Current checksum: `3d197fae`. This is private fixture evidence only, not public bundled KFM support or exact timing/table parity.
- `kfm-official-default-fall-gethit.json`: optional trace where official KFM as defender executes real Common1 `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110` after a fall HitDef without `p2stateno`. Checksum `0b3ece0c` now requires ordered actor-frame evidence for the full branch plus bounded controller/typed-operation order through real KFM `ChangeState`, `HitVelSet` / `kinematic:hitvelset`, ground-impact `VelSet` / `kinematic:velset`, bounce `HitFallVel` / `hitfall:hitfallvel`, and lie-down `HitFallDamage` / `hitfall:hitfalldamage` / `VelSet` / `kinematic:velset`. This proves the current ground-impact, first bounce-state entry, and lie-down entry only, not get-up/recovery input, state `5120` completion, or guard-state parity.
- `kfm-official-default-fall-recovery.json`: optional trace where official KFM as defender continues from real Common1 lie-down `5110` into get-up state `5120` and returns to `0` with control after a fall HitDef without `p2stateno`. Current checksum: `b1c6456a`. The local fixture artifact executes `0, 200, 5000, 5030, 5035, 5050, 5100, 5101, 5110, 5120`, requires actor-frame `5110 -> 5120` order, and requires bounded official controller/typed-operation order through `5110` `HitFallDamage` / `hitfall:hitfalldamage` / `VelSet` / `kinematic:velset` before `5120` `VelSet` / `kinematic:velset` / `HitFallSet` / `hitfall:hitfallset` / `ChangeState`. This proves the bounded get-up completion route only, not exact tick-order parity, recovery-input branching, or guard-state parity.
- `kfm-official-default-fall-recovery-input.json`: optional trace where official KFM as defender receives a recoverable fall HitDef, `CanRecover` becomes true, defender input `x+y` activates `command = "recovery"`, and real Common1 states execute `5050 -> 5210 -> 52 -> 0` before final idle/control. Checksum `5154d760` now requires ordered actor-frame evidence for `5050 -> 5210 -> 52` plus bounded controller/typed-operation order through real KFM `5050` `VelAdd` / `ChangeState`, `5210` `PalFX` / `sprite-effect:palfx`, `PosFreeze` / `bounds:posfreeze`, `Turn` / `orientation:turn`, `NotHitBy` / `eligibility:nothitby`, `VelMul`, `VelAdd`, `ChangeState`, `52` `VarSet`, and state `0` `VelSet` / `kinematic:velset`. This proves the bounded private-fixture official air recovery-input route only, not public asset support, exact recovery thresholds/velocities, full tick-order parity, or guard-state parity.
- `kfm-official-default-air-fall-recovery-input.json`: optional trace where official KFM starts airborne, receives a recoverable fall HitDef without `p2stateno`, routes through real Common1 `5020 -> 5030 -> 5035 -> 5050`, accepts `command = "recovery"` after KFM's `5030` countdown reaches `0`, executes `5210 -> 52 -> 0`, and returns to idle/control. Current checksum: `a431028a`. This proves the bounded private-fixture air-entry recovery-input route only, not public asset support, exact recovery thresholds/velocities, full tick-order parity, or guard-state parity.
- `kfm-official-default-air-fall-recovery-too-early.json`: optional trace where official KFM starts airborne, receives a recoverable fall HitDef without `p2stateno`, routes through real Common1 `5020 -> 5030 -> 5035 -> 5050`, and rejects early `command = "recovery"` while `fall.recovertime` remains positive. Current checksum: `a1db1589`. States `5210`, `5200`, `5201`, `52`, `5100`, `5101`, `5110`, and `5120` are forbidden. This proves the bounded private-fixture air-entry early-input reject window only, not exact recovery thresholds/velocities, tick-order parity, or guard-state parity.
- `kfm-official-default-fall-recovery-threshold.json`: optional trace where official KFM as defender reaches state `5050` while `hitFall.recoverTime` is still positive, then accepts `command = "recovery"` near the ground and executes `5200 -> 5201 -> 52 -> 0`. Current checksum: `19ec5148`. The gate requires `requiredActorFrameSequences` so the positive `5050` state-frame evidence must precede `5200` with `recoverTime = 0`. This proves a bounded official recovery-threshold-to-ground branch oracle only, not exact threshold tables, velocity math, air-recovery selection, controller-loop tick order, or broad fixture compatibility.
- `kfm-official-default-fall-recovery-too-early.json`: optional trace where official KFM as defender receives a recoverable fall HitDef, defender input activates `command = "recovery"` before the bounded recovery timer permits it, real Common1 state `5050` actor-frame evidence keeps minimum observed `recoverTime >= 1` while the countdown drops, and states `5210`, `5200`, and `5201` are forbidden by the gate. Current checksum: `0ad7ae02`. This proves the bounded official early-input reject window only, not exact recovery thresholds/velocities, tick-order parity, or guard-state parity.
- `kfm-official-default-fall-ground-recovery.json`: optional trace where official KFM as defender receives a recoverable fall HitDef, `CanRecover` becomes true near the ground, defender input `x+y` activates `command = "recovery"`, and real Common1 states execute `5050 -> 5200 -> 5201 -> 52 -> 0` before final idle/control. The current local fixture artifact executes `0, 52, 200, 5000, 5030, 5050, 5200, 5201` with `SelfState`, `VelSet`, and `PosSet` controller evidence plus active commands `x`, `y`, and `recovery`, checksum `6079c8c9`. It now also requires bounded official KFM actor-frame order for the observable `5050 -> 5200 -> 52` buckets plus controller/typed-operation order through `5050` `VelAdd`/`ChangeState`, `5200` `VelAdd`/`SelfState`, and `52` landing `VelSet`/`PosSet`/`kinematic:*`/`ChangeState` evidence. `5201` remains required as an executed state, but the gate does not claim a summarized `5201` actor-frame bucket. This proves the bounded official ground recovery-input route only, not exact recovery thresholds/velocities, full tick-order parity, or guard-state parity.
- `kfm-official-default-fall-ground-recovery-priority.json`: optional trace where official KFM uses the same real Common1 ground-recovery route as `kfm-official-default-fall-ground-recovery.json`, but the gate also forbids generic air recovery state `5210` and lie-down states `5100`/`5101`/`5110`/`5120`, and requires typed hit/landing kinematic operations. Current checksum is `6079c8c9`. This proves private-fixture ground-over-air priority confidence only, not public KFM support, exact recovery thresholds, velocity math, controller-loop timing, or full recovery parity.
- `kfm-official-x-state-exit.json`: optional official KFM `x -> 200` hit route that runs enough recovery frames to prove final P1 returns to idle/control when `.scratch/fixtures/kfm-official.zip` exists.
- `diagnostics.json`: summary of passed artifacts and skipped optional fixtures.

## Current Explod RemoveOnGetHit Gate

`pnpm qa:trace` includes required `synthetic-imported-explod-removeongethit.json`, `synthetic-imported-explod-removeonprojectilehit.json`, and `synthetic-imported-explod-removeonprojectileguard.json`.

These gates prove:

- imported P2 creates a passive visual `Explod` with `removeongethit = 1`;
- generated P1 hits P2 through the current direct-hit route;
- imported P1 hits imported P2 through the current Projectile route;
- imported P1 projectile is blocked by imported P2 through the current Projectile guard route;
- MatchWorld emits P2 Explod spawn/remove lifecycle evidence;
- the P1 projectile store and P2 Explod store record produced serials.

This proves bounded owner-side direct/projectile hit-or-guard pruning only. It does not prove exact MUGEN/IKEMEN tick order, helper-bound Explod cleanup timing, or custom-state edge cases.

## Current Explod Pause-Budget Gate

`pnpm qa:trace` includes required `synthetic-imported-hitpausetime-ignorehitpause.json`, `synthetic-imported-explod-ignorehitpause.json`, `synthetic-imported-explod-pausemovetime.json`, and `synthetic-imported-explod-supermovetime.json`.

These gates prove:

- imported P1 can route from active state `200` to `220` during direct-hit hitpause through `HitPauseTime > 0` on a `ChangeState` marked `ignorehitpause = 1`;
- the `HitPauseTime` route records P1 `HitPause:player` advance and P2 `HitPause:player` freeze evidence;
- imported P1 creates two source-owned visual `Explod` actors during regular `Pause` and `SuperPause` routes;
- imported P1 creates two source-owned visual `Explod` actors before a direct-hit hitpause route;
- one Explod without `ignorehitpause` freezes during hitpause;
- one Explod with `ignorehitpause = 1` advances during hitpause;
- one Explod without `pausemovetime`/`supermovetime` freezes after source `movetime`;
- one Explod with `pausemovetime = 4` advances after regular `Pause` source `movetime`;
- one Explod with `supermovetime = 4` advances after `SuperPause` source `movetime`;
- MatchWorld emits Explod spawn/store evidence with P1 serial progression;
- trace evidence records both match-pause freeze and advance rows by exact Explod actor id.

This proves bounded player active-state `HitPauseTime`/`ignorehitpause` routing and bounded Explod `ignorehitpause`/`pausemovetime`/`supermovetime` actor advance only. Exact MUGEN/IKEMEN pause layering and broad controller tick order remain unsupported.

## Playable MVP Acceptance

The Playable MVP requires:

- Default Runtime Mode playable without imported assets.
- At least three selectable local fighters with visible atlas QA.
- At least one native stage and one imported stage path.
- At least one official KFM imported runtime route.
- Inspector Mode with animation playback and collision overlays.
- Compatibility export covering character, stage, runtime roster, and runtime session.
- No crash on unsupported features from the fixture matrix.

## Creator Studio Acceptance

The later studio MVP requires:

- A project manifest can be created, loaded, and saved locally.
- Asset Library shows imported and generated assets with validation state.
- Character Studio can preview actions/collision boxes for at least one imported and one generated character.
- Stage Studio can preview one native and one imported stage.
- Playtest launcher starts Runtime Mode from project selections.
- Studio state is editor/project data, not hidden renderer state.
- Existing MUGEN compatibility reports remain available from the studio surfaces.

## Regression Checklist

Before closing a broad compatibility task, confirm:

- File loading still accepts ZIP and folder input.
- Runtime URL params still select mode, fighters, and stage.
- Collision overlays still align with rendered sprites.
- Debug panels do not overflow or hide critical values.
- Compatibility JSON still includes unsupported feature counts.
- External fixture assets remain under `.scratch/` or another ignored path.
