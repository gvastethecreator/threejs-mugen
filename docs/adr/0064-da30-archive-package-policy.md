# ADR 0064 — Archive and package profile policy (DA30-064)

ZIP path rules: reject `..`, absolute, UNC. DEF roots required. Encoding UTF-8 preferred. Case collision quarantine. Size/count/depth caps. Nested archives blocked by default. Profile detection from DEF/system. URLs not followed offline.
