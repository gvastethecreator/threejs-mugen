# ADR 0068 — Module packaging and permissions (DA30-096)

Manifest: identity, version, deps, capabilities, digests. Trust: default deny network/fs. Updates: pin digest. Isolation: worker boundary. Budgets: size/CPU. Disable/uninstall cleans permissions.
