# IKEMEN Helper Tag redirect research checkpoint

## Outcome

Helper-targeted TagIn/TagOut semantics are source-pinned, locally audited, and split into executable compatibility gates.

## Evidence

- The official wiki explicitly says TagIn affects Helpers; RedirectID accepts any eligible character PlayerID.
- Pinned compiler/runtime source proves redirect-first lookup, original-caller expression ownership, fixed parameter order, and incremental mutation.
- Helper-local axes are state, TagIn control, and standby. Partner/member/leader axes cross into root team systems.
- Negative, absent, destroyed, and disabled targets abort before later Tag evaluation; standby remains lookup-eligible and ancestry is not traversed.
- Pinned action scheduling proves standby Helpers continue CNS execution while other systems suppress gameplay interaction.
- Local audit found state/control storage ready, but complete Helper standby gameplay filtering, presentation-policy evidence, and aggregate partner/order ownership are not ready.
- [Research note](../research/2026-07-11-ikemen-helper-tag-redirect-semantics.md) records the full matrix, source links, inference boundary, and staged implementation sequence.
- `git diff --check`: required gate for this research-only checkpoint.
- Runtime tests/traces/smoke: N/A; no executable or visible behavior changed.

## Global Port Status

- Runtime/IKEMEN: root-created Helper identity is executable; Helper Tag semantics are mapped and the state/control-only gate is next.
- Match/gameplay: no new participation was enabled; Helper standby still lacks complete gameplay enforcement and presentation-policy evidence.
- Compatibility: no runtime or trace artifact changed in this research checkpoint.
- Three.js renderer: unchanged; no visual claim was added.
- Studio editor: unchanged; no authoring claim was added.
- Toolchain/quality: TypeScript 7 and the prior 1700-test/538-trace baseline remain the latest executable evidence.

## Audit

The critical risk was misclassifying every Helper Tag axis as local, or enabling standby as a cosmetic bit while Helpers could still hit and render normally. The chosen sequence isolates source-backed local state/control first and keeps cross-system effects blocked. Independent review was unavailable in this host; this is an internal adversarial audit.

## Blocked

Helper standby gameplay parity, Helper-relative partner mutation, member/leader order quirks, Helper-originated Tag, exact incremental mutation, custom state ownership, score movement, and full IKEMEN parity.
