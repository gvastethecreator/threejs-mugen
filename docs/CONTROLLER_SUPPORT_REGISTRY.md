# Controller Support Registry

This is the registry contract for CNS controller work. It prevents the runtime from growing controller behavior through invisible ad hoc paths.

Every controller family should have:

- parser recognition
- compiled representation when possible
- typed `ControllerOp` when it mutates runtime state
- runtime execution level
- ignored params
- unsupported params
- fixture or synthetic trace evidence
- UI/report wording

## Support States

## End-to-end legal fixture

`mugen-lite-journey.json` checksum `a372a02c` / final `24709fb2` is the required loader-to-runtime package gate. Its repository-authored `CC0-1.0` DEF/CMD/CNS/AIR/SFF surfaces prove ordered movement, a two-element action-`200` attack, guard, get-hit, fall, and recovery through the same imported character definition. Required trace evidence records both action-`200` element indices and their individual observed-frame counts, while unqualified `minFrames` keeps the historical aggregate behavior. `JourneyUnknownController` is intentional negative evidence and must remain visible in compatibility diagnostics. This gate does not claim ZIP transport, exact Common1 behavior, third-party character compatibility, visual/audio parity, AIR loop semantics, or controller parity beyond the observed route.

Entry 458 extends that same gate through deterministic ZIP generation and production `ZipCharacterSource` extraction. ZIP signature, repeat-byte identity, extracted VFS parity, and loader success are focused-test requirements. Hostile/corrupt/password/multivolume archives and broad third-party layout compatibility remain unsupported.

| State | Meaning |
| --- | --- |
| `recognized` | The controller name is known and can be counted/reported. |
| `compiled` | Controller data is converted into typed runtime IR or typed operation data. |
| `executed-partial` | The runtime performs a documented subset. |
| `executed-parity` | The tested behavior is expected to match MUGEN/IKEMEN for the covered case. |
| `noop` | The controller is intentionally accepted but does nothing in runtime. |
| `unsupported` | The controller is known but not executed. |
| `unknown` | The controller or param was encountered but not classified. |

## Current Trigger Notes

- `AssertSpecial NoKOSnd` is `executed-partial`: a tick-active global flag suppresses automatic common `f:11,0` emission when the round first enters KO; normal and double KO emit per defeated player, while time-over emits none. `NoKOSlow` is now `executed-partial`: KO-frame capture suppresses the bounded default 60-tick slowdown while retaining timeline length. Required legal-fixture trace `mugen-lite-journey-nokoslow` checksum `ceac9f37` and desktop/mobile browser smoke now prove a repository-owned ZIP command/state `210` can assert `nokoslow` on a lethal KO and retain playback `1`; this does not broaden the existing round-flow claim. Post-KO echo suppression, teams, motif ownership, configurable round timing, and full round/audio parity remain blocked.
- Contextual SND-bank selection is `executed-partial`: unprefixed `PlaySnd` stays player-owned; unprefixed `HitDef`/`SuperPause` use common prefix `f`; explicit `S`/`F` remain stable; missing common archives fail closed. Broader ownership and perceptual parity remain blocked.
- `PlaySnd channel = 0` browser playback is `executed-partial` with bounded official hit cancellation: shared accepted-hit application increments defender `receivedHitSequence`, and Web Audio stops only that actor's active or pending voice once per increment. Guard, other actors, and later voices during unchanged sequence remain unaffected. Broader defender kinds, exact multi-hit ordering, common/system/BGM ownership, KO policy, and perceptual parity remain blocked.
- `PlaySnd`, `SndPan`, and `StopSnd` remain `executed-partial` with improved browser playback ownership: numbered channels are keyed by runtime actor plus channel, matching actor channel numbers coexist, and actor-local replacement/pan/stop no longer affect another actor. `StopSnd -1` remains global per Elecbyte. Exact free-channel allocation, channel `0` cancellation on hit, broader priority/mix semantics, common/system/BGM ownership, and perceptual parity remain blocked.
- Helper-local direct `HitDef` execution is `executed-partial` for bounded first-generation visual helpers. The helper micro-VM resolves expression-backed `hitsound` / `guardsound` into active move metadata, helper contact presentation emits sound/FightFX events, and the bridge records owner-attributed typed `audio:playsnd` only after accepted hit/guard contact. Required direct/persistence traces are `99b55e47`, `61b3ffbf`, `ba2a19f4`, `1e37fd5c`, and `4b48e97d`; fail-closed HitBy rejection has focused coverage. Exact SND playback, nested helper ancestry, redirects, teams, and full helper parity remain blocked.
- Helper-authored `TagIn` / `TagOut` is `executed-partial` only for unredirected self standby under explicit `ikemen-go`. Omitted/static/deferred self resolves in live Helper context; aggregate, state/control, member/leader, and RedirectID payloads fail closed. Required `synthetic-imported-ikemen-helper-self-tag.json` checksum `08014285` gates concrete controller/operation telemetry, standby/effective-control order, continued CNS, and preserved parented Projectile. Active-root gameplay, Helper-authored aggregate routes, exact incremental failure, and full Tag parity remain blocked.

- `EnvColor` is `executed-partial` for bounded stage-flash telemetry. Static `value` / `time` / `under` params lower into typed `envcolor` operation evidence; required `synthetic-imported-envcolor.json` checksum `956b0f4b` and `synthetic-imported-envcolor-under.json` checksum `0a7b5c96` cover `under = 0` and `under = 1`. Required `synthetic-imported-envcolor-dynamic.json` checksum `845c3d5e` / final checksum `282fc77f` proves dynamic `value = var(0),var(1),var(2)`, `time = var(3)`, and `under = var(4)` resolve through active controller expression fallback, record typed `envcolor` telemetry after resolution, and reach stage-frame evidence `32,128,240` / `under = true`. Official Elecbyte docs define `EnvColor value/time/under` and expression-capable controller params. Exact blend math, layer/window behavior, pause timing, renderer parity, helper/redirect ownership, and full presentation parity remain blocked.
- `LifeAdd`, `LifeSet`, `PowerAdd`, and `PowerSet` are `executed-partial` for bounded owner-local static resource ops and current active-state dynamic expression-backed routes. Static resource ops remain covered by `synthetic-imported-resource.json` checksum `7bbcb2e4`; required `synthetic-imported-lifeadd-dynamic.json` checksum `8b0493f8` / final checksum `cbe4ab51` proves dynamic `LifeAdd value = IfElse(var(8), -2000, 0), kill = var(9)` can resolve through runtime dispatch after `VarSet` seeds, emit typed `resource:lifeadd` telemetry, and clamp owner life to `1` through the current nonlethal route. Required `synthetic-imported-resourceset-dynamic.json` checksum `1bd04945` / final checksum `35db4dcd` proves dynamic `LifeSet value = IfElse(var(10), 750, 0)`, `PowerAdd value = IfElse(var(11), 350, 0)`, and `PowerSet value = IfElse(var(12), 900, 0)` can resolve through runtime dispatch, emit typed `resource:lifeset`, `resource:poweradd`, and `resource:powerset` telemetry, and preserve owner life `750` / power `900` in state/action `291`. Official Elecbyte State Controller Reference defines these controllers and treats numeric controller params as expression-capable unless otherwise specified. Broader dynamic resource-family lowering beyond current owner-local routes, exact KO/round/lifebar flow, helper/team/redirect ownership, exact resource scaling, and full resource parity remain blocked.
- `Const240p`, `Const480p`, and `Const720p` are `executed-partial` as bounded player-local coordinate conversion functions. They evaluate numeric expression args, convert from official 240p/480p/720p widths (`320`, `640`, `1280`) into the current expression context player `localCoord` width, and fall back to 320px when no player localcoord metadata is available. Required `synthetic-imported-const-coordinate.json` checksum `ea879c1b` proves imported State -1 routing through `Const240p(3) = 6`, `Const480p(6) = 6`, and `Const720p(12) = 6` for a 640x480 player localcoord. Required `synthetic-imported-const-controller-param.json` checksum `2dad3a50` proves bounded active-state `VelSet` numeric params can evaluate `Const240p(3) + Const480p(6)` to velocity `12` and `0 - Const720p(12)` to velocity `-6` in that same localcoord context, with static seed and dynamic resolved `VelSet` both recorded as `kinematic:velset`. Focused coverage also proves `Const720p(var(0))` argument evaluation. Official Elecbyte Trigger Reference defines these functions as coordinate conversion into the player's coordinate space by width ratio, and the Elecbyte State Controller Reference defines numeric controller params as expression-capable unless otherwise specified. Broad coordinate translation across all controller params, dynamic typed-operation lowering beyond active-state `VelSet`, exact renderer/screenpack viewport ownership, camera animation parity, helper/team/simul namespace breadth, score movement, and full viewport parity remain blocked.
- `VelAdd` is `executed-partial` for bounded active-state static kinematic operations, one dynamic expression-backed active route, and one bounded first-generation helper-local dynamic route. Static `VelAdd` remains covered by `synthetic-imported-kinematic.json` checksum `92804390`; required `synthetic-imported-dynamic-veladd.json` checksum `daf99fb4` proves a dynamic active-state `VelAdd x = var(0) + 1, y = var(1) - 1` can resolve through runtime dispatch after `VarSet` seeds and a static `VelSet` seed, then emit typed `kinematic:veladd` telemetry. Required `synthetic-imported-helper-dynamic-veladd.json` checksum `fbb8bcae` proves a helper-local `VelAdd x = Parent,Life - 996, y = Root,StateNo - 205` can resolve through the helper dispatcher after a static `VelSet` seed, emit typed `kinematic:veladd`, reach helper velocity `5,-3`, and route to state/action `1402` / anim `942`. Official Elecbyte State Controller Reference defines `VelAdd` as adding optional `x` / `y` values to player velocity and treats numeric controller params as expression-capable unless otherwise specified. Dynamic typed lowering for every kinematic controller, helper-local dynamic typed telemetry beyond current helper `VelSet` / `VelAdd` / `VelMul`, exact physics/tick order, floor snapping, teams/simul/helper breadth, and full movement parity remain blocked.
- `VelMul` is `executed-partial` for bounded active-state static kinematic operations, one dynamic expression-backed active route, and one bounded first-generation helper-local dynamic route. Static `VelMul` remains covered by `synthetic-imported-kinematic.json` checksum `92804390`; required `synthetic-imported-dynamic-velmul.json` checksum `4d241401` proves a dynamic active-state `VelMul x = var(0) * 0.5, y = 0 - var(1)` can resolve through runtime dispatch after `VarSet` seeds and a static `VelSet` seed, then emit typed `kinematic:velmul` telemetry. Required `synthetic-imported-helper-dynamic-velmul.json` checksum `08220a98` proves a helper-local `VelMul x = Parent,Life - 998, y = Root,StateNo - 202` can resolve through the helper dispatcher after a static `VelSet` seed, emit typed `kinematic:velmul`, reach helper velocity `4,-6`, and route to state/action `1403` / anim `943`. Official Elecbyte State Controller Reference defines `VelMul` as multiplying optional `x` / `y` factors into player velocity and treats numeric controller params as expression-capable unless otherwise specified. Dynamic typed lowering for every kinematic controller, helper-local dynamic typed telemetry beyond current helper `VelSet` / `VelAdd` / `VelMul` / `PosSet` / `PosAdd`, exact physics/tick order, floor snapping, teams/simul/helper breadth, and full movement parity remain blocked.
- `PosSet` is `executed-partial` for bounded active-state static kinematic operations, one dynamic expression-backed active route, and one bounded first-generation helper-local dynamic route. Static `PosSet` remains covered by `synthetic-imported-kinematic.json` checksum `92804390`; required `synthetic-imported-dynamic-posset.json` checksum `aeb730fb` proves a dynamic active-state `PosSet x = var(0) + 5, y = var(1) - 2` can resolve through runtime dispatch after `VarSet` seeds and a static `PosSet` seed, then emit typed `kinematic:posset` telemetry with actor-frame position evidence `maxPos.x = 16` / `minPos.y = -20`. Required `synthetic-imported-helper-dynamic-posset.json` checksum `50596bc2` proves a helper-local `PosSet x = Parent,Life - 984, y = Root,StateNo - 220` can resolve through the helper dispatcher after a static `PosSet` seed, emit typed `kinematic:posset`, reach helper position `16,-20`, and route to state/action `1404` / anim `944`. Official Elecbyte State Controller Reference defines `PosSet` as setting optional `x` / `y` player coordinates and treats numeric controller params as expression-capable unless otherwise specified. Dynamic typed lowering for every kinematic controller, helper-local dynamic typed telemetry beyond current helper `VelSet` / `VelAdd` / `VelMul` / `PosSet` / `PosAdd`, exact physics/tick order, floor snapping, teams/simul/helper breadth, and full movement parity remain blocked.
- `PosAdd` is `executed-partial` for bounded active-state static kinematic operations, one dynamic expression-backed active route, and one bounded first-generation helper-local dynamic route. Static `PosAdd` remains covered by `synthetic-imported-kinematic.json` checksum `92804390`; required `synthetic-imported-dynamic-posadd.json` checksum `8ac604b1` proves a dynamic active-state `PosAdd x = var(0) + 5, y = var(1) - 2` can resolve through runtime dispatch after `VarSet` seeds and a static `PosSet` seed, then emit typed `kinematic:posadd` telemetry with actor-frame position evidence `maxPos.x = 18` / `minPos.y = -24`. Required `synthetic-imported-helper-dynamic-posadd.json` checksum `97ec15d0` proves a helper-local `PosAdd x = Parent,Life - 984, y = Root,StateNo - 220` can resolve through the helper dispatcher after a static `PosSet` seed, emit typed `kinematic:posadd`, reach helper position `18,-17`, and route to state/action `1405` / anim `945`. Official Elecbyte State Controller Reference defines `PosAdd` as offsetting optional `x` / `y` player position amounts and treats numeric controller params as expression-capable unless otherwise specified. Dynamic typed lowering for every kinematic controller, helper-local dynamic typed telemetry beyond current helper `VelSet` / `VelAdd` / `VelMul` / `PosSet` / `PosAdd`, exact physics/tick order, floor snapping, facing ownership, teams/simul/helper breadth, and full movement parity remain blocked.
- `PosAdd` is `executed-partial` for bounded active-state static kinematic operations and one dynamic expression-backed active route. Static `PosAdd` remains covered by `synthetic-imported-kinematic.json` checksum `92804390`; required `synthetic-imported-dynamic-posadd.json` checksum `8ac604b1` proves a dynamic active-state `PosAdd x = var(0) + 5, y = var(1) - 2` can resolve through runtime dispatch after `VarSet` seeds and a static `PosSet` seed, then emit typed `kinematic:posadd` telemetry with actor-frame position evidence `maxPos.x = 18` / `minPos.y = -24` in a facing-right owner route. Official Elecbyte State Controller Reference defines `PosAdd` as adding optional `x` / `y` position offsets and treats numeric controller params as expression-capable unless otherwise specified. Dynamic typed lowering for every kinematic controller, helper-local dynamic typed telemetry beyond current helper `VelSet` / `VelAdd`, exact coordinate/facing ownership, exact physics/tick order, floor snapping, teams/simul/helper breadth, and full movement parity remain blocked.
- `GameWidth`, `GameHeight`, `ScreenWidth`, and `ScreenHeight` are `executed-partial` as bounded runtime trigger identifiers through `ExpressionContext.gameSpace`. INI-style `mugen.cfg` / `config.ini` `[Config] GameWidth` / `GameHeight` can feed imported stage/system metadata and override stage `[StageInfo] localcoord` for the current runtime game-space context; when no config dimensions exist, stage localcoord remains the fallback. Camera zoom is applied as inverse game-space scaling for `GameWidth` / `GameHeight`, and `ScreenWidth` / `ScreenHeight` read the unscaled screen-space dimensions from the same context. Active player, paused/hitpause, passive assert-special, helper-local effect, and controller-param fallback contexts can receive the shared viewport read model. Required `synthetic-imported-config-gamespace.json` checksum `2f3c0a63` proves imported State -1 routing through parsed-config-equivalent `ScreenWidth = 1280`, `ScreenHeight = 720`, `GameWidth = 2560`, and `GameHeight = 1440` at camera zoom `0.5`; previous `synthetic-imported-screenspace.json` checksum `5330bacd` and `synthetic-imported-gamespace.json` checksum `b6f248ab` remain required. Official Elecbyte docs define `mugen.cfg` `[Config] GameWidth` / `GameHeight` as game coordinate dimensions, define `GameWidth` / `GameHeight` as game-space dimensions in player local coordinates that scale inversely with camera zoom, and define `ScreenWidth` / `ScreenHeight` as equivalent non-zooming screen-space triggers. Exact player-local coordinate translation, renderer/screenpack viewport ownership, camera animation parity, IKEMEN `config.json` execution, helper/team/simul namespace breadth, score movement, and full viewport parity remain blocked.
- `ReversalDef` is `executed-partial` for bounded direct, no-contact `guard.dist` rejection, authored walk-back/stand/crouch/air guard-input direct, direct custom-state, player-owned Projectile, and helper-parented/root-owned Projectile reversal routes. Static `ReversalDef` setup lowers into typed `reversaldef` operation evidence, direct `synthetic-imported-reversal.json` remains required for defender-side Clsn1 reversal of a matching incoming HitDef plus bounded attacker `MoveReversed` routing, required `synthetic-imported-guarddist-reversal-no-contact.json` checksum `ca20c823` / final checksum `2bc9b86d` proves active ReversalDef does not reverse from `guard.dist` proximity alone when Clsn1 boxes do not contact, required `synthetic-imported-walkback-guard-reversal.json` checksum `70c83b8c` / final checksum `b7a8cb9d` proves an authored state-20 walk-back defender can hold back, move away without `AssertSpecial nowalk`, and counter a guardable direct `HitDef` before default get-hit `5000` or stand guard states `150` / `151` execute, required `synthetic-imported-air-guard-reversal.json` checksum `966b17b8` / final checksum `2fa19142` proves a no-walk-stabilized airborne defender in state `40` can counter an air-guardable direct `HitDef` before default get-hit `5000`, stand guard states `150` / `151`, crouch guard states `152` / `153`, or air guard states `154` / `155` execute, required `synthetic-imported-crouch-guard-reversal.json` checksum `405f475e` / final checksum `d1f39c08` proves the down-back crouch-state counterpart, required `synthetic-imported-guard-reversal.json` checksum `6f8df3a4` / final checksum `e0771a15` proves the held-back stand guard-input counterpart, required `synthetic-imported-custom-state-reversal.json` checksum `18065db0` / final checksum `ac8d0073` proves active ReversalDef can counter a direct owner-backed `HitDef p2stateno = 889` route before state `889`, default get-hit `5000`, or guard states `150` / `151` execute and route the attacker into defender-owned custom state/action `888`, required `synthetic-imported-projectile-reversal.json` checksum `5c4ddf48` / final checksum `bb0bbb99` proves a player-owned Projectile id `77` can trigger the same reversal world, and required `synthetic-imported-helper-projectile-reversal.json` checksum `a1d82380` / final checksum `ca66a49a` proves a visual Helper-spawned Projectile id `8878` with `ownerId = p1`, `rootId = p1`, and `parentId = p1-helper-0` can trigger it at the initial contact before hit/guard/HitOverride/SuperPause-style reject paths consume it. Exact guard-distance boxes, positive proximity-only `guard.dist` ReversalDef contact, custom-state breadth beyond direct routes, projectile reflection/removal after reversal, helper-owned custom-state tables, exact attr grammar, hitpause/tick order, multi-projectile/multi-target/team breadth, and full ReversalDef/guard parity remain blocked.
- `AngleSet`, `AngleAdd`, `AngleMul`, and `AngleDraw` are `executed-partial` for bounded active-state sprite rotation/scale telemetry. Static and resolved dynamic `AngleSet value`, `AngleAdd value`, `AngleMul value`, and `AngleDraw value/scale` lower into typed `sprite-effect:angleset`, `sprite-effect:angleadd`, `sprite-effect:anglemul`, and `sprite-effect:angledraw` operation data when params resolve. Required `synthetic-imported-anglemul.json` checksum `e0dae072` proves static `AngleSet value = 30`, `AngleMul value = 1.5`, and `AngleDraw` can resolve through `RuntimeSpriteEffectControllerWorld` / `RuntimeSpriteEffectWorld.applyAngle` into imported actor-frame `renderAngle = 45` with typed `sprite-effect:anglemul` evidence. Required `synthetic-imported-anglemul-dynamic.json` checksum `0bb54a1c` / final checksum `c9f2b557` proves dynamic `AngleSet value = var(0)` plus `AngleMul value = fvar(0)` can resolve into typed `sprite-effect:angleset` / `sprite-effect:anglemul` evidence and imported actor-frame `renderAngle = 45`. Required `synthetic-imported-angle-dynamic.json` checksum `13560dcd` / final checksum `4d7c4726` proves dynamic `AngleSet value = var(0)`, `AngleAdd value = var(1)`, and `AngleDraw value = var(2), scale = var(3),fvar(0)` resolve into typed `sprite-effect:angleset` / `sprite-effect:angleadd` / `sprite-effect:angledraw` evidence plus imported actor-frame/final `renderAngle = 35` and `renderScale = 2,0.5`. Official Elecbyte docs define `AngleMul value` as an `angle_multiplier` float that multiplies the drawing angle used by `AngleDraw`, and define adjacent Angle params as floats while stating rotation/scaling does not affect collision boxes. Exact axis pivot, collision rotation/scale, draw-order/palette interaction, renderer parity, helper/redirect ownership, and full presentation parity remain blocked.
- `AfterImage` and `AfterImageTime` are `executed-partial` for bounded active-state ghost-trail telemetry. Static `AfterImage` params and `AfterImageTime time/value` lower into typed sprite-effect operation data. Required `synthetic-imported-afterimage-dynamic.json` trace checksum `e7299ac5` / final checksum `b946d805` proves dynamic `AfterImage time/length/timegap/framegap/paladd/palmul` can resolve through `RuntimeSpriteEffectControllerWorld` / `RuntimeSpriteEffectWorld.applyAfterImage`, record typed `sprite-effect:afterimage` telemetry after resolution, and preserve imported actor-frame/final `afterImageTime = 18`, `afterImageLength = 5`, `afterImageTimeGap = 2`, `afterImageFrameGap = 3`, at least one sample, and opacity `0.34`. Required `synthetic-imported-afterimagetime-dynamic.json` checksum `c5ef6fff` / final checksum `661a233d` proves dynamic `AfterImageTime value = var(0)` can record typed `sprite-effect:afterimagetime` telemetry after resolution. Official Elecbyte docs define `AfterImage`, `AfterImageTime`, and numeric controller params as expressions. Exact no-active-afterimage behavior, trail blending, palette math, sampling cadence, renderer parity, helper/redirect ownership, and full presentation parity remain blocked.
- `PlaySnd`, `SndPan`, `StopSnd`, `SuperPause sound`, and contact sound refs are `executed-partial` for bounded sound-event telemetry. Static audio numeric params lower into typed `audio:*` or typed pause/contact operation data where possible; bounded active-state dynamic `PlaySnd`, `SndPan`, and `StopSnd` params now resolve through the active expression context and record typed `audio:*` telemetry while preserving authored raw sound refs for debug events. Bounded dynamic `SuperPause sound` refs now resolve at pause start and record typed `audio:playsnd` telemetry while preserving authored raw sound refs for pause-start debug events. Dynamic direct `HitDef hitsound` and `guardsound` group/index params resolve when the HitDef activates, preserve the raw sound ref plus resolved group/index metadata, and now record typed contact `audio:playsnd` telemetry for the bounded direct routes. Bounded player-owned `Projectile hitsound` / `guardsound` refs, first-generation helper-parented/root-owned `Projectile guardsound` guard-contact refs, the three player-owned and three first-generation helper-parented/root-owned Projectile normal-hit GetHitVar `hitsound` refs, the player-owned Projectile attacker-side HitCount `hitsound` ref, and the first-generation helper-parented/root-owned Projectile attacker-side HitCount `hitsound` ref now preserve sound-event/FightFX package metadata and record typed contact `audio:playsnd` telemetry for required routes. Required `synthetic-imported-sound.json` checksum `cc9c8c49` remains the static audio operation/event gate. Required `synthetic-imported-sound-dynamic-pan.json` checksum `879afcf4` / final checksum `b780e5e9` proves `PlaySnd channel = var(1), pan = var(0)`, `SndPan channel = var(1), abspan = var(2)`, and `StopSnd channel = var(1)` resolve into sound-event telemetry plus typed `audio:playsnd`, `audio:sndpan`, and `audio:stopsnd` evidence. Required `synthetic-imported-sound-dynamic-value.json` checksum `bcdafe32` / final checksum `31b8a7b3` proves `PlaySnd value = Fvar(0),var(1)` resolves into group `5`, index `3`, channel `4`, `soundPrefix = kfm`, raw `Fvar(0),var(1)`, and typed `audio:playsnd` evidence. Required `synthetic-imported-superpause-sound.json` checksum `3e19cb86` / final checksum `c5fb9428` proves `SuperPause sound = Svar(0),var(1)` resolves at pause start into `PlaySnd` group `10`, index `0`, raw `Svar(0),var(1)` telemetry plus typed `audio:playsnd` evidence for `S10,0`. Required `synthetic-imported-hitdef-dynamic-hitsound.json` checksum `fe3c0f3d` / final checksum `855df386` proves `HitDef hitsound = Fvar(0),var(1)` resolves into contact `PlaySnd` group `5`, index `4`, raw `Fvar(0),var(1)`, `soundPrefix = kfm`, and typed `audio:playsnd` evidence for `F5,4`. Required `synthetic-imported-hitdef-dynamic-guardsound.json` checksum `bb38362a` / final checksum `3e0ddeb0` proves guarded `HitDef guardsound = Fvar(0),var(1)` resolves into contact `PlaySnd` group `6`, index `4`, raw `Fvar(0),var(1)`, `contactKind = guard`, `soundPrefix = kfm`, and typed `audio:playsnd` evidence for `F6,4`. Required `synthetic-imported-helper-projectile-guard-ko.json` checksum `05dbcded` / final checksum `98b8bf17`, `synthetic-imported-helper-projectile-guard-kill.json` checksum `33930a00` / final checksum `8412e638`, and `synthetic-imported-helper-projectile-guard-terminal.json` checksum `c6937f42` / final checksum `e0835e33` prove helper Projectile guard-contact `guardsound = S6,0` emits typed `audio:playsnd` evidence. Required `synthetic-imported-projectile-gethitvar-hit-metadata.json` checksum `8e5df79b` / final checksum `4d078c5d`, `synthetic-imported-projectile-gethitvar-hitid-chainid.json` checksum `4356b5cb` / final checksum `4b270d45`, and `synthetic-imported-projectile-gethitvar-hitcount.json` checksum `df2619f9` / final checksum `5469bc69` prove player Projectile normal-hit GetHitVar `hitsound = S5,45/46/47` emits typed `audio:playsnd` evidence with FightFX `F7002` packages. Required `synthetic-imported-helper-projectile-gethitvar-hit-metadata.json` checksum `28afbcea` / final checksum `c960b1cf`, `synthetic-imported-helper-projectile-gethitvar-hitid-chainid.json` checksum `616e0b2c` / final checksum `0aebcc73`, and `synthetic-imported-helper-projectile-gethitvar-hitcount.json` checksum `40ec4f4b` / final checksum `6f15ff30` prove helper Projectile normal-hit GetHitVar `hitsound = S5,40/41/42` emits typed `audio:playsnd` evidence with FightFX `F7002` packages. Required `synthetic-imported-projectile-hitcount.json` trace checksum `ee8f4e19` / final checksum `0fd4adf8` proves player Projectile attacker-side HitCount `hitsound = S5,44` emits typed `audio:playsnd` evidence with a FightFX `F7002` package. Required `synthetic-imported-helper-projectile-hitcount.json` trace checksum `c8f5dc55` / final checksum `e1569fab` proves helper Projectile attacker-side HitCount `hitsound = S5,43` emits the corresponding typed package. Official Elecbyte docs define `PlaySnd value = group_no, sound_no`, F-prefixed common/fight sound refs, `SndPan channel/pan/abspan`, `StopSnd channel`, `HitDef hitsound/guardsound`, `SuperPause sound = snd_grp, snd_no`, PlaySnd panning linkage, helper-created Projectiles as root-owned, and numeric controller params as expression-capable unless otherwise specified. Exact Web Audio panning, channel priority/timing/mixing, broader helper/redirect ownership, broader helper Projectile normal-hit sound breadth, super-background audio, and full audio parity remain blocked.
- `Pause` / `SuperPause` are `executed-partial` for bounded match-pause telemetry, source `movetime`, SuperPause `darken`, `poweradd`, dynamic numeric params, explicit `anim/pos` metadata, omitted default `anim = 30` metadata, `anim = -1` metadata suppression, bounded dynamic numeric `anim/pos` metadata, and bounded `pausebg = 0` metadata. Static pause params lower into typed pause operation data where possible; dynamic raw `time`, `movetime`, `darken`, `poweradd`, `anim`, `pos`, and `pausebg` can resolve through the active expression fallback before pause application. Required `synthetic-imported-superpause-pausebg.json` checksum `49bcfe16` / final checksum `397a8fae` proves `pausebg = 0` reaches match-pause/trace evidence as `pauseBg = false`. Required `synthetic-imported-superpause-anim-disabled.json` checksum `fc7a2ca4` / final checksum `5be3ca6c` proves `anim = -1` leaves no optional `superAnim` snapshot metadata through `superAnimAbsent = true`. Required `synthetic-imported-superpause-default-anim.json` checksum `318c5e9f` / final checksum `747e7619` proves omitted `SuperPause anim` now snapshots default FightFX `superAnim.raw = 30`, `source = fightfx`, `actionNo = 30`, and `offset = 0,0`. Required `synthetic-imported-superpause-dynamic-anim-pos.json` checksum `e6bfbf75` / final checksum `eb49d9db` proves `anim = var(6)` and `pos = var(7),var(8)` resolve as trace-gated `superAnim` raw/source/action/offset metadata. Required `synthetic-imported-superpause-anim-pos.json` checksum `f7dcdc9d` / final checksum `7bd4afe8` proves `anim = S200` and `pos = 24,-48` survive as trace-gated player-AIR metadata. Required `synthetic-imported-superpause-dynamic-params.json` checksum `052bb481` / final checksum `1847a3f3` proves `time = var(2)`, `movetime = var(3)`, `darken = var(4)`, and `poweradd = var(5)` resolve to max remaining `9`, max move time `2`, `darken = false`, P1 source-movetime advance, P2 freeze, and final P1 power `75`; the checksum includes default `superAnim` metadata from omitted `anim`. Official Elecbyte docs define numeric controller params as expression-capable unless otherwise specified, `Pause movetime`, `pausebg = 0` background-continuation semantics, `SuperPause time/darken/poweradd`, default `SuperPause anim = 30`, `S` player-AIR prefix behavior, `anim = -1`, and `pos` offset. Typed-operation lowering for dynamic pause params, bottom-to-zero exactness, Pause-over-Pause/SuperPause preemption/delay, actual renderer/background update parity, exact stage/BGCtrl pause timing, renderer visual suppression/playback parity, actual FightFX/common asset lookup/rendering, dynamic `S` player-AIR prefix breadth, `unhittable`, super backgrounds, helper/team/redirect ownership, and full pause/super presentation parity remain blocked.
- `SuperPause p2defmul` is `executed-partial` for bounded positive current-target damage scaling during the active SuperPause. Static `p2defmul` is preserved on `PauseControllerOp`; dynamic raw params resolve through the active expression fallback; `PlayableMatchRuntime` maps positive defence multiplier to a temporary target damage multiplier and restores the previous target `defenseMultiplier` when the active SuperPause expires. Required `synthetic-imported-superpause-p2defmul.json` checksum `ec1ba95e` / final checksum `6d009665` proves `p2defmul = 2` maps to runtime multiplier `0.5`, so `TargetLifeAdd -20` applies as `-10` after direct target memory exists and final P2 life reaches `953`; the checksum includes default `superAnim` metadata from omitted `anim`. Official Elecbyte docs define numeric controller params as expression-capable unless otherwise specified, `SuperPause p2defmul`, and `TargetLifeAdd` defense scaling. `p2defmul = 0` / `Super.TargetDefenceMul`, exact recovery lifetime, stacking with other damage-scale systems, helper/redirect/multi-target ownership, exact rounding, and full super damage-scaling parity remain blocked.
- `synthetic-imported-helper-projtime-same-id-last-contact.json` checksum `4e74aec3` is the current required helper-local same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` guard-then-hit last-contact-kind gate. It proves a visual Helper can spawn two root-owned, helper-parented Projectiles with id `8918`, guard first, hit later, route helper `1200 -> 1306 -> 1307` through fixed-id and ID `0` hit/contact time reads, and keep fixed-id plus ID `0` guarded time reads inactive; forbidden helper state `1308` proves stale helper-local same-id guarded time does not survive. It requires two helper-parented Projectile lifecycle rows, owner/root `p1`, parent `p1-helper-0`, root owner target-link id `8918`, guard package `S6,34` / `F7038` / `sparkxy = 35,-77`, and hit package `S5,35` / `F7038` / `sparkxy = 36,-78`. Together with `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b`, both helper-local same-ID two-contact orders are now required. Exact `Proj*Time` tick order/lifetime, helper custom-state breadth beyond these owner-side routes, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, and full Projectile parity remain blocked.
- `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b` remains the required helper-local same-ID hit-then-guard counterpart.
- `synthetic-imported-projectile-projtime-same-id-hit-then-guard.json` checksum `d49ee334` remains the required player Projectile same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` hit-then-guard last-contact-kind gate. It proves two Projectiles with id `8916` can hit first, guard later, route owner `200 -> 383 -> 384` through fixed-id and ID `0` guard/contact time reads, and keep fixed-id plus ID `0` hit time reads inactive; forbidden state `385` proves stale same-id hit time does not survive. It requires two Projectile payloads/lifecycle rows, owner target-link id `8916`, hit package `S5,30` / `F7036` / `sparkxy = 31,-73`, and guard package `S6,31` / `F7036` / `sparkxy = 32,-74`. Together with the previous guard-then-hit gate, this covers the bounded two-contact same-ID player `Proj*Time` order pair; exact `Proj*Time` tick order/lifetime, helper custom-state breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, and full Projectile parity remain blocked.
- `synthetic-imported-projectile-projtime-same-id-last-contact.json` checksum `fb4c2450` remains the required player Projectile same-ID guard-then-hit `Proj*Time` counterpart.
- `synthetic-imported-projectile-projhittime-multi-id.json` checksum `5d897825`, `synthetic-imported-projectile-projcontacttime-multi-id.json` checksum `d9b3cecf`, and `synthetic-imported-projectile-projguardedtime-multi-id.json` checksum `e52d0d01` are the current required player Projectile `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` multi-id arbitration gates. They prove wrong-id/non-contact Projectile ids `8909` / `8911` / `8913` do not route forbidden states `373` / `376` / `379`, while valid hit/contact/guard Projectile ids `8910` / `8912` / `8914` route owner `200 -> 371 -> 372`, `200 -> 374 -> 375`, and `200 -> 377 -> 378` through fixed-id plus ID `0` time reads, with two Projectile payloads, owner target-link ids `8910` / `8912` / `8914`, hit/guard sounds, and FightFX spark package evidence. This is bounded player Projectile two-id timing arbitration evidence only; exact `Proj*Time` tick order/lifetime, same-ID selection priority, helper Projectile/custom-state persistence breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, and full Projectile parity remain blocked.
- `synthetic-imported-projectile-projhit-multi-id.json` checksum `ab0f3fb3` and `synthetic-imported-projectile-projguarded-multi-id.json` checksum `023921e3` remain the required player Projectile `ProjHit` / `ProjGuarded` multi-id arbitration gates.
- `synthetic-imported-projectile-projcontact-multi-id.json` checksum `e790ec3e` remains the required player Projectile `ProjContact` multi-id arbitration gate.
- `synthetic-imported-projectile-projcontact-suffix-any.json` checksum `2fb80418` remains the required player Projectile `ProjContact` any-id suffix gate.
- `synthetic-imported-projectile-projhit-suffix-any.json` checksum `35ffd57d` and `synthetic-imported-projectile-projguarded-suffix-any.json` checksum `4000bc4f` remain the required player Projectile `ProjHit` / `ProjGuarded` any-id suffix gates.
- `synthetic-imported-projectile-projhit-suffix.json` checksum `dd3db5ee` and `synthetic-imported-projectile-projguarded-suffix.json` checksum `80bbe439` remain the required fixed-id player Projectile `ProjHit` / `ProjGuarded` suffix gates.
- `synthetic-imported-projectile-projcontact-suffix.json` checksum `c904ded7` remains the required player Projectile `ProjContact` suffix gate. It proves a player-owned Projectile id `8897` can be guarded, route owner `200 -> 350 -> 351` through Elecbyte legacy `ProjContact8897 = 1, >= 1` second-form syntax plus fixed-id first-form suffix syntax, and preserve active post-contact projectile payload, owner target-link, guard sound, and FightFX spark package evidence.
- `synthetic-imported-projectile-projcontactpersist.json` checksum `8e678b1b` remains the required player Projectile `ProjContact` state-transition gate. It proves a player-owned Projectile id `8896` can be guarded, expose owner `ProjContact(8896)` / `ProjContactTime(8896)` in owner state `200`, transition into owner state `348`, and branch from that later owner `StateDef` into owner state `349` with active post-contact projectile payload, owner target-link, guard sound, and FightFX spark package evidence.
- `synthetic-imported-helper-projcontactpersist.json` checksum `65639428` remains the required helper Projectile counterpart. It proves helper-local `ProjContact(8895)` / `ProjContactTime(8895)` through helper `1300 -> 1302 -> 1301` after a guarded helper-parented owner-side Projectile contact.
- `synthetic-imported-helper-movereversedpersist.json` checksum `ef8ffdf5` remains the previous required helper-owned reversed `StateDef movehitpersist` gate. It proves a helper-local direct `HitDef` can be countered by defender-side `ReversalDef`, carry helper-local `MoveReversed` into helper state `1232` declaring `movehitpersist = 1`, while `MoveContact = 0`, `MoveHit = 0`, `MoveGuarded = 0`, `HitCount = 0`, and `UniqHitCount = 0`, and route helper `1200 -> 1232 -> 1233` with P2 reversal state `779` evidence. This is bounded helper reversed Move* state-transition evidence only; helper Projectile/custom-state movehitpersist breadth, exact reversal priority/target-state semantics, multi-hit/multi-target/team counting, and full Move* lifetime parity remain blocked.
- `synthetic-imported-helper-moveguardedpersist.json` checksum `d5ce7897` remains the previous required helper-owned guarded `StateDef movehitpersist` gate. It proves a helper-local guarded `HitDef` contact can carry `MoveContact` / `MoveGuarded` into helper state `1230` declaring `movehitpersist = 1`, while `MoveHit = 0`, `HitCount = 0`, and `UniqHitCount = 0`, and route helper `1200 -> 1230 -> 1231` with helper-owned guard sound/FightFX contact evidence.
- `synthetic-imported-helper-movehitpersist.json` checksum `2354ef95` remains the previous required helper-owned hit `StateDef movehitpersist` gate. It proves a helper-local `HitDef` contact can carry `MoveContact` / `MoveHit` into helper state `1228` declaring `movehitpersist = 1`, while `HitCount = 0` and `UniqHitCount = 0`, and route helper `1200 -> 1228 -> 1229` with helper-owned sound/FightFX contact evidence.
- `synthetic-imported-helper-hitcountpersist.json` checksum `fc9588d8` remains the previous required helper-owned `StateDef hitcountpersist` gate. It proves a helper-local `HitDef` contact can carry `HitCount` / `UniqHitCount` into helper state `1226` declaring `hitcountpersist = 1`, while `MoveHit = 0`, and route helper `1200 -> 1226 -> 1227` with helper-owned sound/FightFX contact evidence.
- `synthetic-imported-helper-hitdefpersist.json` checksum `9d5c64c4` remains the previous required helper-owned `StateDef hitdefpersist` gate. It proves a helper-local `HitDef` can activate in helper state `1200`, persist into helper state `1224` declaring `hitdefpersist = 1`, hit from that destination state with helper-owned sound/FightFX evidence, and route helper `1200 -> 1224 -> 1225`. Previous direct `synthetic-imported-hitdefpersist.json` checksum `4bb3e86c` remains required. This is bounded direct/helper active-HitDef state-transition evidence only; multi-HitDef stacking, player/helper Projectile hitdefpersist breadth, custom-state breadth, ReversalDef interactions, exact combo UI accumulation, multi-hit/multi-target/team counting, and full HitDef lifetime parity remain blocked.
- `synthetic-imported-movehitpersist.json` checksum `5c1ef583` remains the previous required direct `StateDef movehitpersist` gate. It proves direct `HitDef` attacker-side `MoveContact` / `MoveHit` can persist into a destination state declaring `movehitpersist = 1`, while `HitCount` / `UniqHitCount` remain reset after state entry, routing P1 `200 -> 344 -> 345`.
- `synthetic-imported-hitcountpersist.json` checksum `6f032088` remains the previous required direct `StateDef hitcountpersist` gate. It proves direct `HitDef` attacker-side `HitCount` / `UniqHitCount` can persist into a destination state declaring `hitcountpersist = 1`, while `MoveHit` remains reset after state entry, routing P1 `200 -> 342 -> 343`.
- `synthetic-imported-projectile-hitcount.json` trace checksum `ee8f4e19` / final checksum `0fd4adf8` and `synthetic-imported-helper-projectile-hitcount.json` trace checksum `c8f5dc55` / final checksum `e1569fab` remain required Projectile/helper normal-hit attacker-side `HitCount` / `UniqHitCount` gates. They prove player-owned Projectile contact can feed owner-state hit counts and helper-parented/root-owned Projectile contact can mirror helper-local hit-count memory. Both routes require typed `audio:playsnd` and FightFX `F7002` package telemetry, with player `S5,44` and helper-local `S5,43`, but they do not prove Projectile/helper `hitcountpersist` breadth.
- `synthetic-imported-projectile-gethitvar-hitcount.json` trace checksum `df2619f9` / final checksum `5469bc69` and `synthetic-imported-helper-projectile-gethitvar-hitcount.json` trace checksum `40ec4f4b` / final checksum `6f15ff30` are the current required Projectile normal-hit `GetHitVar(hitcount)` metadata gates. They prove player-owned Projectile effect id `77` and helper-parented/root-owned Projectile effect id `8892` can route P2 through defender-owned Common1-style `5000 -> 339/340`, preserve target/lifecycle evidence, and expose HitDef `numhits = 3/4` while `guarded` remains false. Both routes require typed `audio:playsnd` and FightFX `F7002` package telemetry, with player `S5,47` and helper-local `S5,42`. This is bounded Projectile/helper normal-hit hitcount metadata evidence only; exact combo accumulation, chain-hit eligibility arbitration, multi-hit timing, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, and full parity remain blocked.
- `synthetic-imported-projectile-gethitvar-hitid-chainid.json` trace checksum `4356b5cb` / final checksum `4b270d45` and `synthetic-imported-helper-projectile-gethitvar-hitid-chainid.json` trace checksum `616e0b2c` / final checksum `0aebcc73` are the current required Projectile normal-hit `GetHitVar(hitid/chainid)` metadata gates. They prove player-owned Projectile effect id `77` and helper-parented/root-owned Projectile effect id `8891` can route P2 through defender-owned Common1-style `5000 -> 337/338`, preserve target/lifecycle evidence, and expose HitDef `id = 78` plus `chainID = 44` to defender CNS while `guarded` remains false. Both routes require typed `audio:playsnd` and FightFX `F7002` package telemetry, with player `S5,46` and helper-local `S5,41`. This is bounded Projectile/helper normal-hit id metadata evidence only; exact chain-hit eligibility arbitration, combo accumulation, multi-hit timing, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, and full parity remain blocked.
- `synthetic-imported-projectile-gethitvar-hit-metadata.json` trace checksum `8e5df79b` / final checksum `4d078c5d` and `synthetic-imported-helper-projectile-gethitvar-hit-metadata.json` trace checksum `28afbcea` / final checksum `c960b1cf` remain the current required Projectile normal-hit `GetHitVar(damage/hittime/xvel/yvel)` metadata gates. They prove player-owned Projectile id `77` and helper-parented/root-owned Projectile id `8890` can route P2 through defender-owned Common1-style `5000 -> 335/336`, preserve target/lifecycle evidence, and expose `damage`, `hittime`, `xvel`, and `yvel` to defender CNS while `guarded` remains false. Both routes require typed `audio:playsnd` and FightFX `F7002` package telemetry, with player `S5,45` and helper-local `S5,40`. This is bounded Projectile/helper normal-hit metadata evidence only; exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, multi-hit arbitration, broader combo/chain/id breadth, custom-state inheritance, and full parity remain blocked.
- `synthetic-imported-projectile-guard-slide-stop.json` trace checksum `965c2d12` / final checksum `0973a73c` and `synthetic-imported-helper-projectile-guard-slide-stop.json` trace checksum `6c42a378` / final checksum `df8b7a42` are the current required Projectile guard slide-stop/control gates. They extend the existing direct `synthetic-imported-default-guard-slide-stop.json` proof to player-owned Projectile and helper-parented Projectile stand-guard routes, requiring Common1-style `150 -> 151 -> 130`, ordered `HitVelSet`, `VelSet`, `CtrlSet`, final `ChangeState`, typed `kinematic:hitvelset`, `kinematic:velset`, and `resource:ctrlset` evidence, plus target/lifecycle evidence. This is bounded Projectile/helper guard-control evidence only; exact guard control tick order, HitOver vs CtrlSet parity, guard velocity decay/friction, guard effects, helper-owned custom states, and full parity remain blocked.
- `synthetic-imported-guard-timing-default.json` trace checksum `859cb873` / final checksum `bae55cbc`, `synthetic-imported-projectile-guard-timing-default.json` trace checksum `21dc44c4` / final checksum `1c3d9c20`, and `synthetic-imported-helper-projectile-guard-timing-default.json` trace checksum `d421498c` / final checksum `1733494a` remain the previous required HitDef/Projectile guard timing default gates. They prove omitted `guard.hittime`, `guard.slidetime`, and `guard.ctrltime` derive through `ground.hittime -> guard.hittime -> guard.slidetime -> guard.ctrltime` for stand-guard routes on direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile`, with `GetHitVar` evidence and helper Projectile owner/helper target links plus lifecycle payloads. Previous default `guard.velocity`, default/explicit `guard.cornerpush.veloff`, and default/explicit `down.cornerpush.veloff` gates remain required. This is bounded HitDef/Projectile parameter default evidence, not a new controller family; exact guard control tick order, HitOver vs CtrlSet parity, guard effects, guard velocity decay/friction, and full parity remain blocked.
- `synthetic-imported-hitoverride-guardflag-forceair-forceguard-keepstate.json` trace checksum `35fa8224`, `synthetic-imported-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` trace checksum `1fd6c321`, and `synthetic-imported-helper-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` trace checksum `7efa40bb` remain the previous required HitOverride guardflag-plus-force-flags gates: direct `HitDef`, player-owned Projectile id `77`, and helper-parented Projectile id `8884` carry incoming `guardflag = H`, skip slot `1 -> 776` with `guardflag.not = HA` and slot `2 -> 778` with `guardflag = A`, select slot `5 -> 779`, then apply `forceair = 1`, `forceguard = 1`, and `keepstate = 1`. The gates require executed `ChangeState`, `HitOverride`, direct `HitDef` or `Projectile` plus `Helper` for the helper route, typed `hitdef` / `projectile` / `helper` / `hitoverride` operations, active `x`, override telemetry, target-link evidence, lifecycle-payload evidence where applicable, P2 actor-frame evidence with state/action `0`, `stateType = A`, `physics = A`, `guardingFrames >= 1`, final state/action `0`, and forbidden states `776`, `778`, `779`, `5000`, `150`, and `151` plus Projectile custom state `889` where applicable. This is bounded direct/player/helper guardflag-filter-plus-force-flag priority evidence only; exact guard timing/guarded get-hit variables, forceguard chip/damage semantics, final-frame forced aerial persistence, and full HitOverride parity remain blocked.
- `synthetic-imported-hitoverride-missonoverride-default-guardflag-filter.json` trace checksum `05725ecb`, `synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter.json` trace checksum `c1402d31`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter.json` trace checksum `889d77c1` remain the previous required default `missonoverride = -1` custom-state HitOverride guardflag-filter gates: direct `HitDef` with `p2stateno = 888` omits `missonoverride`, carries `guardflag = H`, and rejects before target memory, override state entry, owner-backed custom state `888`, default get-hit, or guard states. Player-owned Projectile id `77` and helper-parented Projectile id `8882` omit `missonoverride`, use `p2stateno = 889` / `p2getp1state = 1` / `guardflag = H`, skip slot `1 -> 776` with `guardflag.not = HA` and slot `2 -> 778` with `guardflag = A`, select slot `5 -> 779`, suppress Projectile custom state `889`, and end P2 in state/action `779`; the helper route also proves owner/helper target links, helper payload `targetCount = 1`, projectile payload `hasHit = true` / `hitsRemaining = 0`, and helper branch `1293` suppression. The gates require executed `ChangeState`, `HitOverride`, direct `HitDef` or `Projectile` plus `Helper` for the helper route, typed `hitdef` / `projectile` / `helper` / `hitoverride` operations, active `x`, reject/override telemetry, target-link or no-target evidence as appropriate, lifecycle-payload evidence, final state evidence, and forbidden states `776`, `778`, the custom state, `5000`, `150`, and `151`. This is bounded direct/player/helper default custom-state guardflag-filter evidence only; exact guard timing/guarded contact semantics and full HitOverride/custom-state parity remain blocked.
- `synthetic-imported-hitoverride-missonoverride-zero-guardflag-filter.json` trace checksum `058b335f`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` trace checksum `af29f125`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` trace checksum `9edbf3d0` remain the previous required explicit `missonoverride = 0` custom-state HitOverride guardflag-filter gates: they prove direct `HitDef`, player-owned Projectile id `77`, and helper-parented Projectile id `8881` can use `guardflag = H`, skip attr-matching slot `1 -> 776` with `guardflag.not = HA`, skip slot `2 -> 778` with `guardflag = A`, select slot `5 -> 779`, suppress the custom state, and end P2 in state/action `779`; the helper route also proves owner/helper target links, helper payload `targetCount = 1`, projectile payload `hasHit = true` / `hitsRemaining = 0`, and helper branch `1291` suppression. The gates require executed `ChangeState`, `HitOverride`, and direct `HitDef` or `Projectile` plus `Helper` for the helper route, typed `hitdef` / `projectile` / `helper` / `hitoverride` operations, active `x`, override telemetry, target-link evidence, lifecycle-payload evidence, final redirected-state evidence, and forbidden states `776`, `778`, the custom state, `5000`, `150`, and `151`. This is bounded direct/player/helper explicit custom-state guardflag-filter evidence only; exact guard timing/guarded contact semantics, forceair/forceguard priority combinations, and full HitOverride/custom-state parity remain blocked.
- `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority.json` trace checksum `9a5a149f` and `synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority.json` checksum `96b6b7de` remain required Projectile explicit `missonoverride = 0` custom-state HitOverride slot-priority gates: they prove player-owned Projectile id `77` and helper-parented Projectile id `8878` can declare `p2stateno = 889` / `p2getp1state = 1`, select active slot `2 -> 778` over slot `5 -> 779`, consume/remove as a hit, suppress projectile custom state `889`, and end P2 in state/action `778`; the helper route also proves owner/helper target links, helper payload `targetCount = 1`, projectile payload `hasHit = true` / `hitsRemaining = 0`, and helper `ProjHit` branch `1288` suppression. The gates require executed `ChangeState`, `Projectile`, `HitOverride`, and for helper route `Helper`, typed `projectile` / `hitoverride` / `helper` operations, active `x`, override telemetry, target-link evidence, lifecycle-payload evidence, final redirected-state evidence, and forbidden states `779`, `889`, `5000`, `150`, `151`, plus `1288` for helper. This is bounded player-owned/helper-parented Projectile explicit custom-state slot-priority evidence only; custom-state guardflag breadth beyond the latest explicit route and full Projectile HitOverride/custom-state parity remain blocked.
- `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` trace checksum `fb964bfb` remains required helper-parented Projectile default custom-state HitOverride `missonoverride = -1` / `forceair` / `forceguard` / `keepstate` evidence.
- `synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` trace checksum `4ce42cf3` remains the previous required player-owned Projectile default custom-state HitOverride gate. It proves a Projectile with `p2stateno = 889`, `p2getp1state = 1`, and omitted `missonoverride` can select P2 active slot `3 -> 780`, record target link `p1 -> p2 / 77`, consume/remove as a hit, expose aerial/guarding actor-frame evidence, avoid override state `780`, suppress projectile custom state `889`, and end P2 in state/action `0` / life `1000` / moveType `I`.
- `synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` trace checksum `20e40425` remains the previous required direct-HitDef default custom-state HitOverride `missonoverride = -1` / `forceair` / `forceguard` / `keepstate` miss gate: it proves a direct `HitDef` with `p2stateno = 888`, `p2getp1state = 1`, and omitted `missonoverride` rejects before target memory, damage, guard, forceair/forceguard actor frames, keepstate redirect handling, override state `780`, owner-backed custom state `888`, default get-hit state `5000`, or guard states `150`/`151` even while P2 has active slot `3 -> 780` with `forceair = 1`, `forceguard = 1`, and `keepstate = 1`. The gate requires executed `ChangeState`, `HitDef`, and `HitOverride`, typed `hitdef` and `hitoverride` operations, active `x`, reject event/combat telemetry, no target links, final P2 state/action `0` / life `1000` / ctrl true / moveType `I`, and forbidden states `780`, `888`, `5000`, and `150`/`151`. This is bounded default direct custom-state miss evidence only; custom-state guardflag inheritance/timing and full HitOverride/custom-state parity remain blocked.

- `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` trace checksum `e23a33af` remains the previous required helper-parented Projectile custom-state HitOverride `missonoverride = 0` / `forceair` / `forceguard` / `keepstate` gate: it proves a visual Helper can spawn Projectile id `8876` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and explicit `missonoverride = 0`, select defender slot `3 -> 780`, record target links `p1 -> p2 / 8876` and `p1-helper-0 -> p2 / 8876`, consume/remove the projectile, expose helper payload `targetCount = 1`, expose projectile payload `hasHit = true` / `hitsRemaining = 0`, expose aerial/guarding actor-frame evidence (`stateType = A`, `physics = A`, `guardingFrames >= 1`), avoid entering state `780` because `keepstate` is set, and suppress projectile custom-state `889` plus helper `ProjHit` branch `1284` because the override matched. The gate requires executed `ChangeState`, `Helper`, `Projectile`, and `HitOverride`, typed `helper`, `projectile`, and `hitoverride` operations, active `x`, override event/combat telemetry containing `HitOverride slot 3`, helper/projectile lifecycle-payload evidence, final P2 state/action `0` / life `1000` / moveType `I`, and forbidden states `780`, `889`, `1284`, `5000`, `150`, and `151`. Previous player-owned custom-state gate `synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `15bc955b` and previous direct explicit custom-state gate `synthetic-imported-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `4d9043a5` remain required. This is bounded explicit `missonoverride = 0` helper-parented Projectile custom-state forceair/forceguard/keepstate evidence only; helper-parented Projectile default `missonoverride` breadth, custom-state guardflag inheritance/timing, and full HitOverride/custom-state parity remain blocked.

- `synthetic-imported-helper-projectile-hitoverride-forceair-forceguard-keepstate.json` trace checksum `84dc3969` is the current required helper-parented Projectile HitOverride `forceair` / `forceguard` / `keepstate` gate: it proves a defender that installs active slot `3 -> 780` with `forceair = 1`, `forceguard = 1`, and `keepstate = 1` can select that slot on an incoming helper-spawned Projectile, record target links `p1 -> p2 / 8875` and `p1-helper-0 -> p2 / 8875`, expose projectile payload evidence with `hasHit = true` / `hitsRemaining = 0`, expose aerial/guarding actor-frame evidence (`stateType = A`, `physics = A`, `guardingFrames >= 1`), and avoid entering state `780` because `keepstate` is set. The gate requires executed `ChangeState`, `Helper`, `Projectile`, and `HitOverride`, typed `helper`, `projectile`, and `hitoverride` operations, active `x`, target-link evidence, helper/projectile lifecycle/payload evidence, override event/combat telemetry containing `HitOverride slot 3`, final P2 state/action `0` / life `1000` / moveType `I`, and forbidden states `780`, `889`, `1282`, `5000`, `150`, and `151`. Previous direct-HitDef, player-owned Projectile, and helper-parented Projectile HitOverride gates remain required as listed below. This is bounded helper-parented Projectile forceair/forceguard/keepstate evidence only; final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state guardflag inheritance/timing, and full helper Projectile HitOverride parity remain blocked.
- `synthetic-imported-projectile-hitoverride-forceair-forceguard-keepstate.json` trace checksum `3806a769` remains the previous required player-owned Projectile HitOverride `forceair` / `forceguard` / `keepstate` gate: it proves a defender that installs active slot `3 -> 780` with `forceair = 1`, `forceguard = 1`, and `keepstate = 1` can select that slot on an incoming Projectile, record target link `p1 -> p2 / 77`, expose projectile hit/removal lifecycle evidence, expose aerial/guarding actor-frame evidence (`stateType = A`, `physics = A`, `guardingFrames >= 1`), and avoid entering state `780` because `keepstate` is set. This is bounded player-owned Projectile forceair/forceguard/keepstate evidence only; final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state guardflag inheritance/timing, and full Projectile HitOverride parity remain blocked.
- `synthetic-imported-hitoverride-forceair-forceguard-keepstate.json` trace checksum `19787fb2` remains the previous required direct-HitDef HitOverride `forceair` / `forceguard` / `keepstate` gate: it proves a defender that installs active slot `3 -> 780` with `forceair = 1`, `forceguard = 1`, and `keepstate = 1` can select that slot on an incoming direct `HitDef`, record target link `p1 -> p2 / 77`, expose aerial/guarding actor-frame evidence (`stateType = A`, `physics = A`, `guardingFrames >= 1`), and avoid entering state `780` because `keepstate` is set. This is bounded direct-HitDef forceair/forceguard/keepstate evidence only; final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state forceair/forceguard/keepstate breadth, custom-state guardflag inheritance/timing, and full HitOverride parity remain blocked.
- `synthetic-imported-helper-projectile-hitoverride-guardflag-filter.json` trace checksum `41a87267` remains the previous required helper-parented Projectile HitOverride `guardflag` / `guardflag.not` filter gate: it proves a defender that installs attr-matching slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H` skips the first two slots for incoming helper-parented Projectile `guardflag = H`, then redirects through slot `5`, state `779`, before Projectile custom-state/default routes or helper `ProjHit` branch `1280`. The gate requires executed `ChangeState`, `Helper`, `Projectile`, and at least three `HitOverride` controllers, typed `helper`, `projectile`, plus at least three `hitoverride` operations, active `x`, helper/projectile lifecycle/payload evidence, owner/helper target-link evidence, override event/combat telemetry containing `HitOverride slot 5`, final P2 state `779` / life `1000`, and forbidden states `776`, `778`, `889`, `1280`, `5000`, `150`, and `151`. Previous direct-HitDef, player-owned Projectile, and helper-parented Projectile HitOverride gates remain required as listed below. This is bounded helper-parented Projectile guardflag filtering only; custom-state guardflag inheritance/timing, custom-state forceair/forceguard/keepstate breadth, and full helper Projectile/HitOverride parity remain blocked.
- `synthetic-imported-hitoverride-guardflag-filter.json` trace checksum `b88a2da3` remains the previous required direct-HitDef HitOverride `guardflag` / `guardflag.not` filter gate: it proves a defender that installs attr-matching slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H` skips the first two slots for incoming `HitDef.guardflag = H`, then redirects through slot `5`, state `779`. The gate requires executed `ChangeState`, `HitDef`, and at least three `HitOverride` controllers, typed `hitdef` plus at least three `hitoverride` operations, active `x`, target-link evidence, override event/combat telemetry containing `HitOverride slot 5`, final P2 state `779` / life `1000`, and forbidden states `776`, `778`, `5000`, `150`, and `151`. This is bounded direct-HitDef guardflag filtering only; custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, and full HitOverride parity remain blocked.
- `synthetic-imported-hitoverride-missonoverride-zero-slot-priority.json` trace checksum `92fefd6a` is the previous required direct-HitDef `missonoverride = 0` custom-state HitOverride slot-priority gate: it proves a defender that installs matching slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order redirects through the lowest matching active slot, state `778`, before owner-backed custom state `888` or default Common1 states. The gate requires executed `ChangeState`, `HitDef`, and at least two `HitOverride` controllers, typed `hitdef` plus at least two `hitoverride` operations, active `x`, target-link evidence, override event/combat telemetry containing `HitOverride slot 2`, final P2 state `778` / life `1000`, and forbidden states `779`, `888`, `5000`, `150`, and `151`. Player-owned/helper-parented Projectile explicit custom-state slot priority is covered by the newer `missonoverride = 0` gates. This is bounded direct-HitDef `missonoverride = 0` slot priority only; broader `missonoverride` custom-state breadth, custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, and full HitOverride parity remain blocked.
- `synthetic-imported-helper-projectile-hitoverride-slot-priority.json` trace checksum `1d058518` remains the required helper-parented Projectile HitOverride slot-priority gate: it proves a defender that installs matching slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order still redirects through the lowest matching active slot, state `778`, before helper `ProjHit`, projectile custom state `889`, or default Common1 states. The gate requires executed `ChangeState`, `Helper`, `Projectile`, and at least two `HitOverride` controllers, typed `helper`, `projectile`, plus at least two `hitoverride` operations, active `x`, helper/projectile lifecycle/payload and owner/helper target-link evidence, override event/combat telemetry containing `HitOverride slot 2`, final P2 state `778` / life `1000`, and forbidden states `779`, `889`, `1278`, `5000`, `150`, and `151`. Previous `synthetic-imported-projectile-hitoverride-slot-priority.json` checksum `378d9ce8` remains required for bounded player-owned Projectile slot priority; previous `synthetic-imported-hitoverride-slot-priority.json` checksum `8de62354` remains required for bounded direct-HitDef slot priority. Explicit `missonoverride = 0` custom-state slot priority is covered by newer player/helper Projectile gates. This is bounded helper-parented Projectile lowest-slot priority only; broader `missonoverride` custom-state breadth, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, and full Projectile/HitOverride parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-guard-kill.json` trace checksum `c889a534` is the current required owner-backed custom-state guard-hit `GetHitVar(kill)` gate: it proves guarded direct `HitDef guard.kill = 0` can record target memory, feed owner-local `TargetState`, branch P2 `888 -> 904` through `GetHitVar(kill) = 0` plus `GetHitVar(guarded)`, and return through `SelfState`. This is bounded direct-HitDef custom-state guard kill metadata only; `p2stateno`-on-guard behavior, exact guard KO/no-KO round flow, helper/projectile custom-state inheritance, and full custom-state/guard parity remain blocked.
- `synthetic-imported-helper-projectile-gethitvar-guard-kill.json` trace checksum `7f9aa699` remains required for helper-parented Projectile guard-hit `GetHitVar(kill)`: it proves helper-spawned Projectile `guard.kill = 0` can route P2 through defender-owned states `150 -> 151`, branch through `GetHitVar(kill) = 0` plus `GetHitVar(guarded)`, reach state/action `334`, and preserve guard event/reason, owner/helper target-link evidence, and helper-local `Projectile` controller/op telemetry.
- `synthetic-imported-projectile-gethitvar-guard-kill.json` trace checksum `3feae5a7` remains required for the matching bounded player-owned Projectile guard kill metadata route through `150 -> 151 -> 333`.
- `synthetic-imported-gethitvar-air-guard-kill.json` trace checksum `4382207e` remains required for the matching bounded direct-HitDef air guard kill metadata route through `154 -> 155 -> 332`.
- `synthetic-imported-gethitvar-crouch-guard-kill.json` trace checksum `2976fb8c` remains required for the matching bounded direct-HitDef crouch guard kill metadata route through `152 -> 153 -> 331`.
- `synthetic-imported-gethitvar-guard-kill.json` trace checksum `abb4e468` remains required for the matching bounded direct-HitDef stand guard kill metadata route through `150 -> 151 -> 330`.
- `synthetic-imported-gethitvar-kill.json` trace checksum `ef5ffabf` remains the required defender-owned normal-hit `GetHitVar(kill)` gate: it proves direct normal `HitDef kill = 0` can route P2 into defender-owned state `5000`, branch through `GetHitVar(kill) = 0` plus `!GetHitVar(guarded)`, reach state/action `329`, and preserve hit event/reason plus target-link evidence. This is bounded direct-HitDef normal kill metadata only; KO/round-flow behavior, helper/projectile/custom-state inheritance, exact target lifetime/tick order, and full get-hit parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-fall-metadata.json` trace checksum `4a3a1c6b` is the current required owner-backed custom-state `GetHitVar` fall metadata gate: it proves direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` can route P2 into P1-owned state data, branch through `GetHitVar(fall)`, `GetHitVar(fall.damage) = 70`, `GetHitVar(fall.kill) = 0`, `GetHitVar(fall.xvel) = 3`, and `GetHitVar(fall.yvel) = -6`, preserve actor-frame `customOwnerId = p1`, keep target-link evidence for target id `77`, reach state/action `903`, and return through `SelfState`. This is bounded direct-HitDef fall damage/kill/velocity metadata inheritance only; exact metadata lifetime/stacking, helper/projectile inheritance, throws, teams/simul, exact bind/tick order, and full custom-state fall parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-fall-envshake.json` trace checksum `5c9d1653` remains the required owner-backed custom-state `GetHitVar` fall envshake gate: it proves direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` can route P2 into P1-owned state data, branch through `GetHitVar(fall)`, `GetHitVar(fall.envshake.time) = 15`, `GetHitVar(fall.envshake.freq) = 178`, `GetHitVar(fall.envshake.ampl) = 6`, and `GetHitVar(fall.envshake.phase) = 0`, preserve actor-frame `customOwnerId = p1`, keep target-link evidence for target id `77`, reach state/action `902`, and return through `SelfState`. This is bounded direct-HitDef fall envshake metadata inheritance only; exact camera waveform, pause/stage/layer interaction, metadata lifetime/stacking, helper/projectile inheritance, throws, teams/simul, exact bind/tick order, and full custom-state fall presentation parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-guard-timing.json` trace checksum `ba77beec` remains the previous required owner-backed custom-state `GetHitVar` guard timing gate: it proves guarded direct `HitDef` target memory can feed owner-local `TargetState`, route P2 into P1-owned state data, branch through `GetHitVar(guarded) = 1`, `GetHitVar(slidetime) = 5`, `GetHitVar(ctrltime) = 7`, and positive `GetHitVar(hitshaketime)`, preserve actor-frame `customOwnerId = p1`, keep target-link evidence for target id `77`, reach state/action `901`, and return through `SelfState`. This is bounded target-memory-to-custom-state guard timing inheritance only; `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, and full custom-state/get-hit parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid.json` trace checksum `250f77c2` remains required for owner-backed custom-state `GetHitVar` hitcount/id evidence: it proves direct `HitDef p2stateno = 888, id = 77, chainID = 43, numhits = 3` stores bounded numhits/id/chainID metadata and branches P2 `888 -> 900` through `GetHitVar(hitcount/hitid/chainid)` before `SelfState`. This is bounded p2stateno custom-state metadata inheritance only; exact combo accumulation, chain-hit eligibility arbitration, helper/projectile inheritance breadth, teams/simul, exact target lifetime/tick order, and full custom-state/get-hit parity remain blocked.
- `synthetic-imported-gethitvar-fallcount.json` trace checksum `c391d938` remains the previous required imported CNS `GetHitVar(fallcount)` gate: it proves a fall `HitDef` can route P2 into owner-backed state `5100`, run `HitFallDamage`, record one bounded Common1-style ground-impact count, consume stored `fall.damage`, and branch through `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0` into state/action `328`. Exact multi-ground-hit combo accumulation, lifetime/reset parity, non-Common1 impact detection, helper/projectile/custom-state inheritance, and full fall/get-hit parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-snap.json` trace checksum `ce4680b9` remains the previous required owner-backed custom-state `GetHitVar` snap gate: it proves direct `HitDef p2stateno = 888, snap = 16,-24` stores bounded `xoff/yoff/zoff` metadata, routes P2 into P1-owned state `888`, branches through `GetHitVar(xoff) = 16`, `GetHitVar(yoff) = -24`, and `GetHitVar(zoff) = 0`, preserves actor-frame `customOwnerId = p1`, reaches state/action `899`, and returns through `SelfState`. This is bounded p2stateno custom-state snap metadata inheritance only; exact throw positioning, z-axis support, guard snap behavior, helper/projectile inheritance breadth, teams/simul, visual/audio parity, and full throw/get-hit parity remain blocked.
- `synthetic-imported-gethitvar-snap.json` trace checksum `312a53fc` remains the previous required direct get-hit `GetHitVar` snap gate: it proves direct `HitDef snap = 16,-24` stores bounded `xoff/yoff/zoff` metadata, applies simple attacker-relative direct-contact positioning, routes P2 into owner-backed state `5100`, branches through `GetHitVar(xoff) = 16`, `GetHitVar(yoff) = -24`, and `GetHitVar(zoff) = 0`, and reaches state/action `288`. This is bounded direct-HitDef snap metadata and simple positioning only; exact throw positioning, z-axis support, guard snap behavior, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, and full throw/get-hit parity remain blocked.
- `synthetic-imported-gethitvar-hitid-chainid.json` trace checksum `18df99ed` remains the previous required normal get-hit `GetHitVar` gate: it proves direct `HitDef id = 77, chainID = 43` records target memory, stores bounded hit id / chain id metadata, routes P2 into defender-owned state `5000`, branches through `GetHitVar(hitid) = 77`, `GetHitVar(chainid) = 43`, and `!GetHitVar(guarded)`, and reaches state/action `326`. This is bounded direct-HitDef id/chainID metadata only; exact chain-hit eligibility arbitration, helper/projectile/custom-state inheritance breadth, teams/simul, exact target lifetime/tick order, and full get-hit parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-yaccel.json` trace checksum `549fe48d` remains the previous required custom-state `GetHitVar` gate: it proves direct `HitDef p2stateno = 888` stores `yaccel = 0.62`, routes P2 into P1-owned state data, branches through `GetHitVar(yaccel) = .62`, `GetHitVar(hittime) > 0`, and `!GetHitVar(guarded)`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState`. This is bounded direct-hit yaccel metadata inheritance only; exact physics integration, fall acceleration arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind tick-order, and full custom-state parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-isbound.json` trace checksum `d25307e9` remains the previous required custom-state `GetHitVar` gate: it proves direct `HitDef` target memory can feed `TargetBind` before typed owner-local `TargetState`, route P2 into P1-owned state data, branch through `GetHitVar(isbound) = 1`, positive `GetHitVar(hittime)`, and `!GetHitVar(guarded)`, preserve actor-frame `customOwnerId = p1`, and return through `SelfState`. This is bounded TargetBind subject metadata inheritance only; throws, exact bind tick-order/lifetime, visual bind parity, helper/root/parent redirects, teams/simul, `p2stateno`-on-guard behavior, and full custom-state parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-guarded.json` trace checksum `54f62821` remains the previous required custom-state guarded `GetHitVar` gate: it proves guarded direct `HitDef` target memory can feed typed owner-local `TargetState`, route P2 into P1-owned state data, branch through `GetHitVar(guarded) = 1`, `GetHitVar(hitshaketime) > 0`, and `GetHitVar(hittime) > 0`, preserve actor-frame `customOwnerId = p1`, and return through `SelfState`. This is bounded guarded target-memory-to-custom-state metadata inheritance only; `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, and full custom-state parity remain blocked.
- `synthetic-imported-custom-state-gethitvar-down-recover.json` trace checksum `5bd94568` remains the previous required custom-state down-recovery `GetHitVar` gate: it proves direct fall `HitDef p2stateno = 888` with `p2getp1state = 1` can route P2 into P1-owned state data, branch through `GetHitVar(down.recover) = 1`, `GetHitVar(down.recovertime) = 45`, and alias `GetHitVar(recovertime) = 45`, preserve actor-frame `customOwnerId = p1`, and return through `SelfState`. This is bounded owner-backed custom-state down-recovery metadata inheritance only; exact lie-down tables, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, exact recovery threshold behavior, and full custom-state parity remain blocked.
- `synthetic-imported-assertspecial-round-flow-telemetry.json` trace checksum `10f95bdb` is the current required `AssertSpecial` round-flow telemetry gate: it proves static official-style `Intro` and `NoKOSlow` flags lower into typed `assertspecial` operation evidence, runtime execution exposes `intro` and `noKoSlow`, and final imported actor evidence contains normalized `assertSpecialGlobalFlags` `intro` and `nokoslow`. This is parser/typed-op/runtime/final-actor telemetry only; exact intro ownership, KO slow-motion suppression, winpose/round transitions, helper/team/global ownership, pause layering, and full round-flow parity remain blocked.
- `synthetic-imported-helper-dynamic-posadd.json` trace checksum `97ec15d0` is the current required helper dynamic controller-param gate: it proves a first-generation visual Helper can evaluate dynamic `PosAdd` params through `Parent,Life` and `Root,StateNo`, emit typed `kinematic:posadd` telemetry after a static `PosSet` seed, reach actor-frame position `18,-17`, and route to state/action `1405` / anim `945`. Previous `synthetic-imported-helper-dynamic-posset.json` checksum `50596bc2` remains required for helper-local dynamic `PosSet`; previous `synthetic-imported-helper-dynamic-velmul.json` checksum `08220a98` remains required for helper-local dynamic `VelMul`; previous `synthetic-imported-helper-dynamic-veladd.json` checksum `fbb8bcae` remains required for helper-local dynamic `VelAdd`; previous `synthetic-imported-helper-controller-param-parentroot.json` checksum `94919326` remains required for helper-local dynamic `VelSet`. This is bounded helper-local dynamic `VelSet` / `VelAdd` / `VelMul` / `PosSet` / `PosAdd` typed telemetry only; nested helper ancestry where root differs from parent, helper-spawned helpers, player `Parent` controller-param redirects, helper-local dynamic typed lowering beyond these routes, recursive redirection, debug warning text, teams/simul, helper-owned controller breadth, and full controller/expression parity remain blocked.
- `synthetic-imported-assertspecial-helper-explod-shadow.json` trace checksum `83f61b48` remains the previous required renderer/global gate: it proves bounded `AssertSpecial GlobalNoShadow` helper/explod shadow actor-frame evidence, with required `AssertSpecial` controller evidence, typed `assertspecial` operation evidence, `Helper` controller evidence, helper/explod lifecycle evidence, final-actor `assertSpecialGlobalFlags` evidence, and actor-frame `shadowVisible=false` evidence for P1, P2, Helper, and Explod. Previous required `synthetic-imported-assertspecial-shadow-telemetry.json` trace checksum `2b9c8fac` remains the bounded player shadow renderer oracle for normalized `noshadow` and `globalnoshadow`. Exact shadow skew/stage parameters, projectile shadow semantics, exact ownership beyond the current spawned helper/explod route, pause layering, and full renderer/global flag parity remain blocked. Previous `synthetic-imported-assertspecial-global-telemetry.json` checksum `fc793d29` remains required and proves bounded global-style telemetry for `nobardisplay`, `nobg`, `nofg`, `nokosnd`, and `nomusic`. Previous `synthetic-imported-default-liedown-fast-recovery.json` checksum `74bdac97` remains required and proves bounded positive fast lie-down recovery while fresh `recovery` command input is active in Common1-style state `5110` with positive `down.recovertime`. Previous `synthetic-imported-assertspecial-nofastrecoverfromliedown.json` checksum `74bf5d85` remains required and proves bounded IKEMEN `AssertSpecial NoFastRecoverFromLieDown` suppresses that shortcut until `down.recovertime` reaches `0`. Previous controller-param Root redirect gate remains `synthetic-imported-controller-param-root-redirect.json` checksum `6cf929ee`: it proves a dynamic `VelSet` parameter expression can resolve current player `Root, Life` / `Root, StateNo`, moving seeded P1 velocity `4,-2` to `5,-3` in current player controller execution. The gate requires named controller order, static plus dynamic resolved `kinematic:velset` evidence, and actor-frame velocity telemetry. This is bounded current player `VelSet` controller-param Root redirect evidence only; player Parent controller-param redirects, nested helper ancestry beyond the helper-local gate above, helper-spawned helpers, dynamic-parameter typed lowering beyond active-state `VelSet`, recursive redirect evaluation, debug warning text, broad bottom/redirect parity, helper/team ownership, and full controller/expression parity remain blocked.
- `synthetic-imported-controller-param-target-redirect.json` checksum `dee522ae` remains the previous required controller-param Target redirect gate: it proves a dynamic `VelSet` parameter expression can resolve `Target(77), Life` after direct `HitDef` target memory exists, moving seeded P1 velocity `4,-2` to `3,-3` in current controller execution. The gate requires target-link evidence, named controller order, `HitDef`, static plus dynamic resolved `kinematic:velset` evidence, and actor-frame velocity telemetry. This is bounded current `VelSet` controller-param Target redirect evidence only; broad controller-param redirect parity remains blocked.
- `synthetic-imported-controller-param-bottom.json` checksum `8e154a39` remains the previous required controller-param bottom gate: it proves a dynamic `VelSet` parameter expression that evaluates through a missing parent redirect falls back to `0` in current controller execution. The gate seeds velocity through a static typed `kinematic:velset` operation, then observes the dynamic `Parent,Vel X` / `Parent,Vel Y` fallback through named controller order, dynamic resolved `kinematic:velset` evidence, and actor-frame velocity telemetry. This is bounded controller-param bottom evidence only; player Parent controller-param redirect support, nested helper ancestry beyond the helper-local gate above, helper-spawned helpers, dynamic-parameter typed lowering beyond active-state `VelSet`, recursive redirect evaluation, debug warning text, broad bottom parity, helper/team ownership, and full controller/expression parity remain blocked.
- `synthetic-imported-target-ifelse-bottom.json` checksum `be7554d4` remains the previous required IfElse/bottom gate: it proves direct `HitDef` target memory can coexist with a missing `Target(999)` destination in an unused `IfElse(...)` branch, where the missing branch is still eagerly evaluated but isolated from the selected return value, forbidden state `400` does not execute, and fallback state/action `401` executes. Focused CNS coverage also proves warning-style unsupported diagnostics still fire for unused missing `IfElse(...)` branches. This is bounded IfElse/bottom result-isolation evidence only; Cond-style lazy evaluation, recursive redirection, debug warning text, broad bottom parity, teams/simul, target mutation through redirects, and full redirect parity remain blocked.
- `synthetic-imported-target-cond-bottom.json` checksum `e882a2bb` remains the previous required Cond/bottom gate: it proves direct `HitDef` target memory can coexist with a missing `Target(999)` destination in an unused `Cond(...)` branch, where skipped branches are not evaluated, forbidden state `398` does not execute, and fallback state/action `399` executes. Focused CNS coverage also proves skipped `Cond(...)` branches do not call the unsupported-redirect reporter while selected missing redirects fail closed. This is bounded Cond/bottom evidence only; recursive redirection, debug warning text, broad bottom parity, teams/simul, target mutation through redirects, and full redirect parity remain blocked.
- `synthetic-imported-target-redirect-bottom.json` checksum `5e50a90a` remains the previous required invalid-redirect bottom gate: it proves direct `HitDef` target memory can coexist with a missing `Target(999)` destination inside `(Target(999), Life = 0) || 1`, where the missing redirect propagates bottom, forbidden state `396` does not execute, and fallback state/action `397` executes. Focused CNS coverage also proves missing `Parent` redirects propagate through composite expressions while unused `IfElse(...)` branches stay isolated. This is bounded parser-expression evidence only; recursive redirection, debug warning text, broad bottom parity, teams/simul, target mutation through redirects, and full redirect parity remain blocked.
- `synthetic-imported-default-fall-ground-recovery-priority.json` checksum `e83b2db7` is the current required Common1 ground-recovery priority gate: it proves a fall `HitDef` with `fall.recover = 1` and without `p2stateno` can route defender-owned `5000 -> 5030 -> 5050 -> 5200 -> 5201 -> 52 -> 0` through active `command = "recovery"` near ground, with positive-to-zero `fall.recovertime`, ordered ground-recovery controller/typed-operation evidence, final idle/control, and forbidden generic air-recovery/liedown states `5210`/`5100`/`5101`/`5110`/`5120`. This is bounded ground-over-air recovery selection evidence only; exact recovery threshold tables, ground/air recovery arbitration constants, controller-loop tick parity, velocity math, and full fall/recovery parity remain blocked.
- `kfm-official-default-fall-ground-recovery-priority.json` checksum `6079c8c9` is the optional private-fixture mirror for that priority gate when `.scratch/fixtures/kfm-official.zip` exists: it reuses the real KFM ground-recovery route while keeping generic air recovery `5210` and lie-down states forbidden, with bounded actor-frame and typed hit/landing kinematic evidence. This is private fixture confidence only; public KFM support, exact threshold tables, velocity math, visual/audio parity, and full fall/recovery parity remain blocked.
- `synthetic-imported-default-fall-recovery-input-priority.json` checksum `f5e72e07` is the previous required Common1 recovery-input priority gate: it proves a fall `HitDef` with `fall.recover = 1` and without `p2stateno` can route defender-owned `5000 -> 5030 -> 5050 -> 5210 -> 0` through active `command = "recovery"` while a competing `HitFall && CanRecover` probe exists, with named `Recovery Input` `ChangeState`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` / `VelSet` / `kinematic:velset` / `HitFallSet` / `hitfall:hitfallset` evidence, final idle/control, and forbidden probe/ground-recovery states `5250`/`5200`. This is bounded input-priority evidence only; exact recovery threshold tables, ground/air recovery arbitration, controller-loop tick parity, velocity math, and full fall/recovery parity remain blocked.
- `synthetic-imported-hitfall-canrecover-ready.json` checksum `c0097d7f` is the previous required Common1 CanRecover-ready fall trigger gate: it proves a fall `HitDef` with `fall.recover = 1` and without `p2stateno` can route defender-owned `5000 -> 5030 -> 5050 -> 5250 -> 0` through `HitFall && CanRecover` after `fall.recovertime` drops from positive to zero, without active `recovery` command, with named `HitFall CanRecover Ready Probe` `ChangeState`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` evidence, final idle/control, and forbidden recovery states `5210`/`5200`. This is bounded readiness-trigger evidence only; exact recovery threshold tables, controller-loop tick parity, velocity math, and full fall/recovery parity remain blocked.
- `synthetic-imported-hitfall-recover-true.json` checksum `f1e3424a` is the previous required Common1 recover-enabled fall trigger gate: it proves a fall `HitDef` with `fall.recover = 1` and without `p2stateno` can route defender-owned `5000 -> 5030 -> 5050 -> 5240 -> 0` through `HitFall && GetHitVar(fall.recover) && !CanRecover`, with named `HitFall Recover Enabled Probe` `ChangeState`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` evidence, positive `fall.recovertime` metadata in `5050` and `5240`, final idle/control, and forbidden recovery states `5210`/`5200`. This is bounded recover-enabled-but-not-yet-recoverable trigger evidence only; exact recovery threshold tables, recovery arbitration, controller-loop tick parity, fall/CanRecover precedence, and full fall/recovery parity remain blocked.
- `synthetic-imported-hitfall-recover-false.json` checksum `236df0a8` is the previous required Common1 recover-disabled fall trigger gate: it proves a fall `HitDef` with `fall.recover = 0` and without `p2stateno` can route defender-owned `5000 -> 5030 -> 5050 -> 5230 -> 0` through `HitFall && !GetHitVar(fall.recover) && !CanRecover`, with named `HitFall Recover Disabled Probe` `ChangeState`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` evidence, positive `fall.recovertime` metadata in `5050` and `5230`, final idle/control, and forbidden recovery states `5210`/`5200`. This is bounded recover-disabled trigger evidence only; exact recovery threshold tables, recovery arbitration, controller-loop tick parity, fall/CanRecover precedence, and full fall/recovery parity remain blocked.
- `synthetic-imported-hitfall-false.json` checksum `1d538e43` is the previous required Common1 normal get-hit trigger gate: it proves a direct non-fall `HitDef` without `p2stateno` can enter defender-owned `5000` and branch into state/action `325` through `!HitFall` plus `!GetHitVar(fall)` / `!GetHitVar(guarded)`, with named `Normal HitFall False Probe` `ChangeState`, ordered actor-frame evidence, final P2 state `325` / moveType `H`, and forbidden fall/recovery states `5001`, `5030`, `5050`, `5210`, and `5200`. This is bounded HitFall-false trigger evidence only; exact normal get-hit timing, fall arbitration, custom-state/helper/team inheritance, controller-loop tick parity, and full get-hit parity remain blocked.
- `synthetic-imported-hitfall-canrecover.json` checksum `7cf7ab46` is the previous required Common1 fall trigger gate: it proves a fall `HitDef` without `p2stateno` can route defender-owned `5000 -> 5030 -> 5050 -> 5220 -> 0` through `HitFall` while `CanRecover` is still false, with positive `fall.recovertime` actor-frame evidence in `5050`/`5220`, ordered `HitVelSet` / `kinematic:hitvelset`, `VelAdd`, named probe `ChangeState`, forbidden `5210`/`5200`, and final idle/control. This is bounded `HitFall`/`CanRecover` trigger evidence only; exact recovery threshold tables, recovery arbitration, controller-loop tick parity, and full fall/recovery parity remain blocked.
- `synthetic-imported-air-guard-landing.json` checksum `d6986d7f` remains a required Common1 air guard landing walk-control gate: it proves an airborne held-back defender can block an A-guardable direct `HitDef`, route `154 -> 155 -> 52 -> 20`, execute `155` `HitVelSet`, `VelAdd`, `CtrlSet`, landing `VelSet`, landing `PosSet`, and final `ChangeState` with typed `kinematic:*` / `resource:ctrlset` evidence, record a state-`52` y = 0 actor frame, and end in imported walk state/action `20` with control. This is bounded synthetic air guard landing walk-control evidence only; exact air guard physics/timing, state-`52` internals, proximity, effects, controller-loop tick parity, and full guard parity remain blocked.
- `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699` is the current required Common1 crouch guard-hold crouch-control gate: it proves defender-owned `152 -> 153 -> 130 -> 10` actor-frame ordering with `153` `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, final `ChangeState`, active `holdback` / `holddown` / `x` command evidence, final imported crouch state/action `10`, and control restored. Optional private-fixture `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` proves the real KFM/Common1 `152 -> 153 -> 131 -> 11` route shape when `.scratch/fixtures/kfm-official.zip` exists. This is bounded synthetic plus optional private-fixture crouch guard return evidence only; exact guard timing, proximity, effects, controller-loop tick parity, public KFM support, and full guard parity remain blocked.
- `synthetic-imported-crouch-guard-slide-stop.json` checksum `2bea7311` is the previous required Common1 crouch guard slide-stop/control gate: it proves defender-owned `152 -> 153 -> 130` ordering with `153` `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, final `ChangeState`, active `holddown` / `x` command evidence, and actor-frame velocity-before-stop evidence. This is bounded crouch guard evidence only; exact guard timing, proximity, effects, controller-loop tick parity, and full guard parity remain blocked.
- Optional private-fixture `kfm-official-basic-movement.json` checksum `ef30066c` confirms real KFM can follow the current scripted movement input route through authored states/actions `20 -> 11 -> 41` and final `0`/control. This is movement-route confidence only; public KFM support, exact Common1 movement tables, CMD priority/raw-buffer parity, landing/collision/platform parity, controller-loop parity, and full movement parity remain blocked.
- `RuntimeActiveControllerRunWorld` owns bounded active-controller scan-to-dispatch orchestration after state lookup and trigger filtering: imported-state controller scans, owner-backed state data, `ignorehitpause` filtering, compiled controller dispatch creation, dispatch route execution, and stop-on-state-change now route through one named bridge before concrete state/runtime/side-effect hooks run. This is ownership cleanup only; it does not add exact CNS VM timing, persistent-controller semantics, helper/team/redirect scopes, side-effect ordering parity, unsupported-feature breadth, or full active-controller parity.
- `RuntimeActiveControllerDispatchWorld` owns bounded active-controller route orchestration after scan/trigger pass: it tries active state/animation dispatch first, routes shared runtime-controller execution through the existing runtime-controller boundary, routes side effects through `RuntimeActiveSideEffectDispatchWorld`, and keeps unsupported dispatches fail-soft/reportable. This is ownership cleanup only; it does not add exact CNS VM timing, persistent-controller semantics, helper/team/redirect scopes, side-effect ordering parity, missing-action fallback parity, or full active-controller parity.
- `RuntimeAfterImageSampleWorld` owns bounded `AfterImage` sample projection for the current ghost-trail path: cloned actor position, facing, self/state-owner sprite metadata, and current AIR frame group/index/offset now route through a named sample factory boundary. This is ownership cleanup only; it does not add exact trail cadence, material/blend parity, renderer parity, or helper/team presentation ownership.
- `RuntimeFrameWorld` owns bounded current AIR frame lookup plus cloned `Clsn1` / `Clsn2` projection for current runtime/snapshot consumers, including the current-frame hitbox handoff used by `HitDef` and `ReversalDef` dispatch routes. This is ownership cleanup only; it does not add collision priority, rotated/scaled box parity, guard-distance threshold parity, renderer parity, or helper/team collision ownership.
- `RuntimeControllerEvaluationContextWorld` owns bounded `StateControllerExecutor` context creation for active runtime-controller dispatch: owner const reads, actor hitpause reads, actor random callbacks, and stage-time forwarding route through a named factory before passive controller execution. This is ownership cleanup only; it does not add passive-controller breadth, exact controller-loop timing, helper/team context scopes, or exact random stream parity.
- `RuntimeDispatchEvaluationWorld` owns bounded dynamic active-controller dispatch-param fallback: compiled numeric/Boolean dispatch values win, dynamic fallback expressions request a concrete read context from `RuntimeExpressionContextWorld`, numeric params require finite truncated results, and Boolean params use numeric truthiness. This is ownership cleanup only; it does not add dynamic-param breadth, exact controller-loop timing, helper/team parameter scopes, or expression parity.
- `RuntimeActiveStateDispatchWorld` owns bounded active-state `ChangeState` / `SelfState` and `ChangeAnim` / `ChangeAnim2` dispatch mutation from `PlayableMatchRuntime`: dynamic numeric/Boolean param resolution, controller telemetry, state-entry handoff, optional ctrl mutation, state-owner animation source selection, and `elem` / `elemtime` handoff route through a named world. This is ownership cleanup only; it does not add exact controller-loop timing, persistent-controller semantics, helper/team/redirect scopes, missing-action fallback parity, or full ChangeState/ChangeAnim VM parity.
- `RuntimeActiveSideEffectDispatchWorld` owns bounded active-state side-effect dispatch routing from `PlayableMatchRuntime`: singleton routes for `HitDef`, `ReversalDef`, `Width`, `FallEnvShake`, `Pause` / `SuperPause`, sound, `EnvColor`, `EnvShake`, and contact controllers plus grouped sprite-effect, effect-spawn, and `Target*` / `BindToTarget` routes. This is ownership cleanup only; existing controller worlds still own concrete semantics and this does not add exact side-effect ordering, persistent-controller semantics, helper/team/redirect scopes, visual parity, or full active-controller VM parity.
- `RuntimeTriggerEvaluationWorld` owns the bounded single-trigger evaluation bridge for imported active/state-entry controller filtering: it receives actor/opponent/owner/tick plus a context factory, evaluates the compiled normalized `TriggerIr` expression, and returns the raw value plus Boolean pass/fail. `RuntimeExpressionContextWorld` still owns the read model and `RuntimeTriggerGateWorld` still owns `triggerall` / numbered group order. This is ownership cleanup only; it does not add expression breadth, exact VM timing, or helper/team redirect parity.
- `RuntimeExpressionContextWorld` owns the shared active runtime read model for imported trigger evaluation and dynamic controller-param fallback. Focused `RuntimeExpressionContextSystem` coverage proves `Target` redirects, `MoveHit` / `HitCount` / `ProjHit` / `ReceivedDamage` reads, `NumHelper` / `NumExplod` / `NumProj` / `NumTarget` counts, command checks, const/state/anim reads, `HitVar` reads, and target metadata through one context. This is trigger/read-model ownership only; full expression parity, helper/team redirect mutation, exact VM timing, and composite `HitDefAttr = ...` parity remain blocked.
- Default get-hit state selection, bounded normal HitFall-false branch, bounded recover-enabled/recover-disabled/CanRecover-ready fall branches, bounded recovery-input priority, bounded ground-over-air recovery priority, bounded air fall order, bounded positive lie-down fast recovery, and bounded air-entry recovery input are partial for imported defenders when `HitDef` omits `p2stateno`: required `synthetic-imported-default-gethit.json` gates standing state `5000`, required `synthetic-imported-hitfall-false.json` checksum `1d538e43` gates a direct non-fall route `5000 -> 325` through `!HitFall` / `!GetHitVar(fall)` while fall/recovery states are forbidden, required `synthetic-imported-default-liedown-fast-recovery.json` checksum `74bdac97` gates the bounded runtime fast-recovery shortcut from `5110` to `5120` while `down.recovertime` is still positive and active `command = "recovery"` evidence exists, required `synthetic-imported-default-fall-ground-recovery-priority.json` checksum `e83b2db7` gates a near-ground recoverable route `5000 -> 5030 -> 5050 -> 5200 -> 5201 -> 52 -> 0` through active `command = "recovery"` while generic air-recovery state `5210` is forbidden, required `synthetic-imported-default-fall-recovery-input-priority.json` checksum `f5e72e07` gates a recoverable input route `5000 -> 5030 -> 5050 -> 5210 -> 0` through active `command = "recovery"` while a competing `HitFall && CanRecover` probe state `5250` is forbidden, required `synthetic-imported-hitfall-canrecover-ready.json` checksum `c0097d7f` gates a recoverable fall route `5000 -> 5030 -> 5050 -> 5250 -> 0` through `HitFall && CanRecover` after `fall.recovertime` drops to zero and without active recovery input, required `synthetic-imported-hitfall-recover-true.json` checksum `f1e3424a` gates a fall recover-enabled-but-not-yet-recoverable route `5000 -> 5030 -> 5050 -> 5240 -> 0` through `HitFall && GetHitVar(fall.recover) && !CanRecover`, required `synthetic-imported-hitfall-recover-false.json` checksum `236df0a8` gates a fall recover-disabled route `5000 -> 5030 -> 5050 -> 5230 -> 0` through `HitFall && !GetHitVar(fall.recover) && !CanRecover`, required `synthetic-imported-default-crouch-gethit.json` checksum `7ec18c61` gates held-crouch state `5010`, required `synthetic-imported-default-air-gethit.json` checksum `dc4fb7c9` gates airborne state `5020`, required `synthetic-imported-default-air-fall-gethit.json` checksum `1230a2f3` gates fall routing through `5020 -> 5030 -> 5050`, required `synthetic-imported-default-air-ground-impact.json` checksum `0ba3c80f` gates `5020 -> 5030 -> 5050 -> 5100` with `5100` `HitFallDamage` / `hitfall:hitfalldamage` evidence, required `synthetic-imported-default-air-liedown-recovery.json` checksum `56a8f236` gates `5020 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0` with `5101` `HitFallVel`, `5110` `HitFallDamage`, typed `hitfall:hitfallvel` / `hitfall:hitfalldamage`, bounded `5110` `downRecoverTime` countdown, and final idle/control evidence, and required `synthetic-imported-default-air-fall-recovery-input.json` checksum `334a419e` gates `5020 -> 5030 -> 5050 -> 5210 -> 52 -> 0` after `command = "recovery"` with positive-to-zero `fall.recovertime`, air-recovery velocity, landing, and final idle/control evidence. This is state-selection/order plus bounded normal trigger, recover-enabled/disabled/readiness/input-priority and near-ground priority triggers, ground-impact, bounce, lie-down countdown, fast lie-down shortcut, get-up, idle-return, and recovery-input controller support only; exact Common1 animation choice, slide/fall/landing routing, exact normal get-hit timing, exact recovery threshold tables, exact velocity math, exact ground-impact timing/position, exact bounce physics, exact lie-down duration tables, `HitShakeOver`/`HitOver` progression, controller-loop timing, ground/air recovery arbitration, and full get-hit/recovery parity remain blocked.
- Default air-entry early recovery rejection is now separately gated by required `synthetic-imported-default-air-fall-recovery-too-early.json` checksum `48a2e708`, proving `5020 -> 5030 -> 5050` while early `command = "recovery"` is active, `fall.recovertime` remains positive, and recovery/landing states `5210`, `5200`, `5201`, `52`, `5100`, `5101`, `5110`, and `5120` stay forbidden. This strengthens bounded Common1 recovery controller support only; exact threshold tables, velocity math, controller-loop timing, recovery arbitration, KFM/public fixture parity, and full recovery parity remain blocked.
- `ProjCancelTime(projid)` is supported for bounded owner-state Projectile cancel timing and bounded helper-local helper-parented Projectile cancel timing after projectile-vs-projectile clash cancellation. Required `synthetic-imported-projectile-canceltime.json` checksum `64e8dec4` proves owner-state `ProjCancelTime(77) >= 0` can route the owner of a canceled player-owned Projectile into state/action `283`; required `synthetic-imported-projectile-canceltime-any.json` checksum `5bff1961` proves owner-state `ProjCancelTime(0) >= 0` can route that owner into state/action `320`; required `synthetic-imported-projectile-canceltime-dynamic.json` checksum `0da26c87` proves owner-state expression-derived `ProjCancelTime(77 + var(0)) >= 0` can route that owner into state/action `318`; required `synthetic-imported-projectile-canceltime-var.json` checksum `e057e102` proves owner-state `ProjCancelTime(var(0)) >= 0` can route that owner into state/action `319` after owner-local `VarSet` seeds Projectile id `77`; required `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01` proves helper-local `ProjCancelTime(0) >= 0` can route a Helper from state/action `1266` into `1267`; required `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176` proves helper-local `ProjCancelTime(8868) >= 0` can route the Helper from state/action `1268` into `1269` after the matching helper-parented owner-side Projectile is canceled; required `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2` proves helper-local expression-derived `ProjCancelTime(8869 + var(0)) >= 0` can route the Helper from state/action `1270` into `1271` after the matching helper-parented owner-side Projectile is canceled. Focused helper-local runtime coverage separately proves nonzero `ProjCancelTime(var(n))` id selection. These traces require clash/cancel event evidence, loser `projcancelanim` terminal playback, lifecycle, effect-store, and payload data. Exact cancel tick-order/lifetime, broad dynamic expression parity, exact priority classes, multi-projectile id selection semantics beyond the gated routes, redirects, teams/simul, and full MUGEN/IKEMEN projectile cancel parity remain blocked.
- `GetHitVar(xvel)`, `GetHitVar(yvel)`, `GetHitVar(damage)`, `GetHitVar(kill)`, `GetHitVar(hittime)`, and `GetHitVar(hitshaketime)` are supported for bounded direct-contact normal hit metadata, and `GetHitVar(kill)` is also supported for bounded direct stand/crouch/air guard plus player-owned Projectile `guard.kill` metadata. Required `synthetic-imported-gethitvar-velocity.json` checksum `878a03f7` proves defender-owned state `5000` can branch through `GetHitVar(xvel) = 4 && GetHitVar(yvel) = -2 && !GetHitVar(fall) && !GetHitVar(guarded)` into state/action `324` after a direct `HitDef` hit with authored `ground.velocity = 4,-2`; required `synthetic-imported-gethitvar-damage.json` checksum `2c726114` proves the same route through `GetHitVar(damage) = 37 && !GetHitVar(guarded)` into state/action `323`; required `synthetic-imported-gethitvar-kill.json` checksum `ef5ffabf` proves direct `HitDef kill = 0` is readable through `GetHitVar(kill) = 0 && !GetHitVar(guarded)` into state/action `329`; required `synthetic-imported-gethitvar-guard-kill.json` checksum `abb4e468` proves direct stand guard `HitDef guard.kill = 0` is readable through `GetHitVar(kill) = 0 && GetHitVar(guarded)` into state/action `330`; required `synthetic-imported-gethitvar-crouch-guard-kill.json` checksum `2976fb8c` proves the matching direct crouch guard route into state/action `331`; required `synthetic-imported-gethitvar-air-guard-kill.json` checksum `4382207e` proves the matching direct air guard route into state/action `332`; required `synthetic-imported-projectile-gethitvar-guard-kill.json` checksum `3feae5a7` proves the matching player-owned Projectile guard route into state/action `333`; required `synthetic-imported-gethitvar-hitshaketime.json` checksum `655107b9` proves the same normal-hit route through `GetHitVar(hitshaketime) > 0 && !GetHitVar(guarded)` into state/action `310`; required `synthetic-imported-gethitvar-hittime.json` checksum `a11beef0` proves the same normal-hit route through `GetHitVar(hittime) > 0 && !GetHitVar(guarded)` into state/action `309`. Direct and projectile combat paths now store bounded applied damage plus hit timing in shared runtime hit vars, direct normal-hit contact stores bounded impact velocity and normal kill metadata for get-hit CNS, and direct stand/crouch/air guard plus player-owned Projectile guard store bounded guard kill metadata for guard-hit CNS. Exact hitstun tick order, hitpause tick lifetime, hitshake lifetime, velocity lifetime after later physics/controllers, damage lifetime/rounding, guard-chip semantics, exact guard KO/no-KO round flow, helper-parented Projectile/custom-state inheritance breadth, tick order, and full Common1 get-hit/guard parity remain blocked.
- `GetHitVar(guarded)` is supported for bounded direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` guard metadata in defender-owned guard-hit CNS. Required `synthetic-imported-gethitvar-guarded.json` checksum `7c36defb` proves direct guard metadata can branch P2 from `150 -> 151` into state/action `302`; required `synthetic-imported-projectile-gethitvar-guarded.json` checksum `a0104472` proves player-owned Projectile guard metadata can branch P2 from `150 -> 151` into state/action `303`; required `synthetic-imported-helper-projectile-gethitvar-guarded.json` checksum `2b413bd7` proves helper-parented Projectile guard metadata can branch P2 from `150 -> 151` into state/action `304` with helper/projectile lifecycle, payload, target-link evidence, and helper-local `Projectile` controller/op telemetry. These routes require `HitVelSet` / `CtrlSet` typed evidence before the branch and return to idle/control. Custom-state guarded metadata, exact guard timing/effects, and full Common1 guard parity remain blocked.
- `GetHitVar(hittime)`, `GetHitVar(hitshaketime)`, `GetHitVar(slidetime)`, and `GetHitVar(ctrltime)` are supported for bounded guard timing metadata in defender-owned guard-hit CNS. Required `synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3c3f2e25` proves air state `155` can branch through helper-parented Projectile air guard `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `317`; required `synthetic-imported-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3fcf1421` proves the same air hitshake read for player-owned Projectile into state/action `316`; required `synthetic-imported-gethitvar-air-guard-hitshaketime.json` checksum `703e9328` proves the same air hitshake read for direct `HitDef` into state/action `315`; required `synthetic-imported-gethitvar-crouch-guard-hitshaketime.json` checksum `b31d1dac` proves crouch state `153` can branch through direct-`HitDef` crouch guard into state/action `314`; required `synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime.json` checksum `64a1a8bd` proves standing state `151` can branch through helper-parented Projectile `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `313`; required `synthetic-imported-projectile-gethitvar-guard-hitshaketime.json` checksum `724f66d6` proves the same hitshake read for player-owned Projectile into state/action `312`; required `synthetic-imported-gethitvar-guard-hitshaketime.json` checksum `31d76de9` proves the same hitshake read for direct `HitDef` standing guard into state/action `311`; required `synthetic-imported-gethitvar-guard-timing.json` checksum `cf92c669` proves state `151` can branch through `GetHitVar(hittime)`, `GetHitVar(slidetime)`, and `GetHitVar(ctrltime)` into state/action `308` after `150 -> 151` and typed `HitVelSet` operation evidence. Exact hitpause lifetime, exact crouch/air guard timing and landing breadth, guard timing/proximity, guard end, guard effects, helper-owned custom states, custom-state inheritance, tick order, and full Common1 guard parity remain blocked.
- Stand guard slide-stop/control has required Common1-style traces: `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641` proves direct guarded contact routes through `150 -> 151 -> 130`, then requires ordered `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, and final `ChangeState` evidence in state `151`, with actor-frame velocity evidence before stop/control; `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c` extends the portable route through `150 -> 151 -> 130 -> 20` and final walking control under held-back input. This is controller/order evidence for the bounded stand route only; exact guard-hold duration, guard timing, proximity guard, guard effects, crouch/air parity, and full guard VM parity remain blocked.
- Player-owned and helper-parented Projectile stand guard slide-stop/control now have required traces: `synthetic-imported-projectile-guard-slide-stop.json` checksum `965c2d12` proves player-owned Projectile guarded contact routes through `150 -> 151 -> 130` with the same ordered `HitVelSet` / `VelSet` / `CtrlSet` / final `ChangeState` and typed operation evidence, plus projectile target/lifecycle evidence; `synthetic-imported-helper-projectile-guard-slide-stop.json` checksum `6c42a378` proves the helper-parented Projectile route with owner/helper target links and helper/projectile lifecycle payloads for Projectile id `8859`. This is Projectile/helper guard-control evidence only; exact guard tick order, HitOver vs CtrlSet parity, velocity decay/friction, helper-owned custom states, and full guard/projectile parity remain blocked.
- Optional private KFM/Common1 guard slide-stop/control has stricter fixture gates for stand and crouch routes: `kfm-official-default-guard-slide-stop.json` checksum `885bb1da` requires real KFM state `151` `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, and final `ChangeState` order after direct guarded contact; `kfm-official-default-guard-hold-return.json` checksum `885bb1da` extends the stand route with actor-frame sequence evidence that `150 -> 151` returns through observable guard-hold `130`; `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da` further proves the route resumes held-back walking state/action `20` with control; `kfm-official-default-crouch-guard-slide-stop.json` checksum `d11153d0` requires the matching real KFM state `153` crouch order while holding down-back; `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` extends the crouch route with actor-frame sequence evidence that `152 -> 153` returns through observable crouch guard-hold `131` and resumes held-down crouch state/action `11` with control; `kfm-official-default-air-guard-state.json` checksum `62367dac` now requires real KFM air guard-hit `154 -> 155 -> 52 -> 20` actor-frame sequence and final state/action `20` control. This is private-fixture confidence only; public KFM support, exact guard-hold duration, exact guard timing, proximity guard, guard effects, exact air guard physics/landing timing/state-`52` internals, and full guard VM parity remain blocked.
- `GetHitVar(fall.damage)`, `GetHitVar(fall.kill)`, `GetHitVar(fall.xvel)`, and `GetHitVar(fall.yvel)` are supported for bounded stored direct-`HitDef` fall metadata in owner-backed get-hit CNS before `HitFallDamage` resolves and in owner-backed custom state data after `p2stateno = 888` / `p2getp1state = 1`. Required `synthetic-imported-gethitvar-fall-metadata.json` checksum `474fa734` proves state `5100` can branch through those reads into state/action `305`; required `synthetic-imported-custom-state-gethitvar-fall-metadata.json` checksum `4a3a1c6b` proves attacker-owned state data can branch `888 -> 903` through the same stored `fall.damage = 70`, `fall.kill = 0`, `fall.xvel = 3`, and `fall.yvel = -6` values. Exact metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance breadth, tick order, and full fall/get-hit parity remain blocked.
- `GetHitVar(fallcount)` is supported for one bounded stored Common1-style ground-impact count in owner-backed get-hit CNS after `HitFallDamage` executes in state `5100`. Required `synthetic-imported-gethitvar-fallcount.json` checksum `c391d938` proves state `5100` can branch through `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0` into state/action `328`. Exact multi-ground-hit combo accumulation, lifetime/reset parity, non-Common1 impact detection, redirects, helper/projectile/custom-state inheritance, tick order, and full fall/get-hit parity remain blocked.
- `GetHitVar(fall.envshake.time)`, `GetHitVar(fall.envshake.freq)`, `GetHitVar(fall.envshake.ampl)`, and `GetHitVar(fall.envshake.phase)` are supported for bounded stored direct-`HitDef` fall env-shake metadata in owner-backed get-hit CNS before `FallEnvShake` presentation consumes it. Required `synthetic-imported-gethitvar-fall-envshake.json` checksum `6364632a` proves state `5100` can branch through those reads into state/action `306` with `time = 15`, `freq = 178`, `ampl = 6`, and `phase = 0`; required `synthetic-imported-custom-state-gethitvar-fall-envshake.json` checksum `5c9d1653` proves the same stored values can be read while P2 executes P1-owned custom state data and branches `888 -> 902` before `SelfState`. Exact camera waveform, pause/stage/layer interaction, metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance breadth, tick order, and full fall presentation parity remain blocked.
- `GetHitVar(down.recover)`, `GetHitVar(down.recovertime)`, and alias `GetHitVar(recovertime)` are supported for bounded stored direct-`HitDef` down-recovery metadata in owner-backed get-hit CNS before lie-down recovery consumes the timer. Required `synthetic-imported-gethitvar-down-recover.json` checksum `b8a7aef0` proves state `5100` can branch through those reads into state/action `307` with `down.recover = 1` and `down.recovertime = 45`. Exact liedown tables, 5110/5120 tick order, metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, tick order, and full Common1 recovery parity remain blocked.
- `StateNo`, `Anim`, `Time` / `StateTime`, `Life`, `Power`, `Pos X/Y`, and `Vel X/Y` are supported in a bounded current-owner form through the compiler/evaluator and runtime expression context. Required `synthetic-imported-owner-metrics.json` checksum `1a61aaeb` gates State -1 routing through those reads into state/action `298`; `RuntimeExpressionContextWorld` normalizes the internal post-transition `stateElapsed = -1` sentinel to observable CNS time zero. This proves current owner metric trigger plumbing only; exact VM tick ordering, helper/team/redirect state namespaces, localcoord scaling, persistent-controller parity, and full trigger parity remain blocked.
- `TeamSide` is supported in a bounded one-on-one sandbox form through the compiler/evaluator and runtime expression context: `p1` and first-generation `p1-*` actors resolve to side `1`, `p2` and `p2-*` actors resolve to side `2`, and unknown actor ids resolve to `0`. Required `synthetic-imported-teamside.json` checksum `f55695b7` gates State -1 routing through `TeamSide = 1` plus `EnemyNear, TeamSide = 2` into state/action `299` without combat side effects. This proves current two-actor side-context trigger plumbing only; teams/simul/turns, indexed opponent selection, helper-owned opponent lists, dynamic side ownership, and full MUGEN/IKEMEN `TeamSide` semantics remain blocked.
- `FrontEdgeDist`, `BackEdgeDist`, `FrontEdgeBodyDist`, and `BackEdgeBodyDist` now read current stage left/right bounds through active runtime expression contexts, active/passive controller expression fallback, and first-generation visual Helper micro-VM contexts. Required `synthetic-imported-edge-distance.json` checksum `785de452` gates a bounded imported State -1 route through those reads into state/action `293`; missing stage bounds still fail soft to `999` for partial fixtures. This is bounded edge-distance trigger support only; exact screen/camera edge parity, team/simul/helper namespace breadth, localcoord scaling, push/corner behavior, and full MUGEN/IKEMEN edge behavior remain blocked.
- `Name`, `P1Name`, `P2Name`, and `AuthorName` are supported expression identifiers in the compiler/evaluator for the current bounded two-actor runtime context. Required `synthetic-imported-identity.json` checksum `c9be5cf1` gates State -1 routing through current owner display name, opponent display name, owner author, and redirected current-opponent author via `EnemyNear, AuthorName`. This is trigger subset support, not a controller family, and not team/simul/helper/player-indexed identity parity.
- `AnimTime` is supported in a bounded current-action form through the compiler/evaluator and runtime expression context. Required `synthetic-imported-animtime.json` checksum `9e42b546` gates an imported active-state route where state `200` waits for its authored AIR action duration and exits through `AnimTime = 0` into state `291` without combat side effects. This proves current actor animation-end trigger plumbing only; exact AIR negative-duration semantics, looped-action semantics, state-owner/helper namespaces, persistent-controller timing, and full animation VM parity remain blocked.
- `AnimElemTime(n)` is supported in a bounded current-action form through the compiler/evaluator and runtime expression context. Required `synthetic-imported-animelemtime.json` checksum `2036557d` gates an imported active-state route where state `200` advances through authored AIR frame durations `[2,4,4]` and exits through `AnimElemTime(2) = 2` into state `292` without combat side effects. This proves current actor animation-element elapsed-time trigger plumbing only; exact AIR loop semantics, invalid-element bottom values, negative-duration semantics, state-owner/helper namespaces, persistent-controller timing, and full animation VM parity remain blocked.
- `SelfAnimExist(n)` is supported in a bounded own-character form through the compiler/evaluator and runtime expression context. Required `synthetic-imported-selfanimexist.json` checksum `99930032` gates State -1 routing through an existing own AIR action `200` and rejects missing action `9999` before entering state `290`. This proves current own-animation lookup only; redirected animation owners, helper/parent/root lookup, common/FightFX namespaces, and exact MUGEN/IKEMEN lookup parity remain blocked.
- `Ctrl`, `StateType`, `MoveType`, and `Physics` are supported in a bounded owner-context form through the compiler/evaluator and runtime expression context. Required `synthetic-imported-state-context.json` checksum `cb9c3d1e` gates State -1 routing through `ctrl`, `StateType = S`, `MoveType = I`, and `Physics = S` into state/action `295`. This proves current owner-context trigger plumbing only; helper/team/redirect metadata ownership, exact controller-loop ordering, persistent-controller parity, and full trigger parity remain blocked.
- `P2StateType` and `P2MoveType` are supported in a bounded two-actor current-opponent form through the compiler/evaluator and runtime expression context. Required `synthetic-imported-p2-state-context.json` checksum `caf32557` gates State -1 routing through `P2StateType = S` and `P2MoveType = I` into state/action `296`. This proves current opponent metadata trigger plumbing only; teams/simul, helpers, custom-state opponent ownership, exact opponent selection, persistent-controller parity, and full trigger parity remain blocked.
- `P2Dist` and `P2BodyDist` are supported in a bounded current-opponent form through the compiler/evaluator and runtime expression context. `P2BodyDist X` uses current S/C/A/L X edges, facing-aware opponent front/back selection, cross-localcoord scaling, IKEMEN-only Width, and OverrideClsn Size. IKEMEN `P2BodyDist Y` uses signed vertical edge gaps with overlap zero, cross-localcoord scaling, Height, then OverrideClsn Size; MUGEN output contexts retain center-axis `P2Dist Y` behavior. EnemyNear/Target redirects preserve original output policy/localcoord. Required `synthetic-imported-p2-distance.json` remains the State -1 routing gate. This proves root/current-opponent spacing reads only; helpers, full teams/simul selection, MUGEN bind quirks/old rounding, exact push/corner and late-controller interaction, persistent-controller parity, and full spacing parity remain blocked.
- `StageTime` and `GameTime` are supported in a bounded match-tick form through the compiler/evaluator and runtime expression context. Required `synthetic-imported-gametime.json` checksum `bab573f3` gates State -1 routing through `GameTime >= 4` into state/action `294`, while `synthetic-imported-stagetime.json` remains the StageTime proof. This proves current global tick trigger plumbing only; exact pause accounting, replay/rollback timing, multi-round timer ownership, IKEMEN round-system behavior, and full timing parity remain blocked.
- `EnemyNear` player redirects support omitted index / `0` against the current one-on-one opponent and still fail closed for positive indexes unless the caller supplies an explicit enemy-near redirect list. Required `synthetic-imported-enemynear-index.json` checksum `b97e2eda` gates default-context `EnemyNear(0), StateNo` routing to state `284` and forbids the `EnemyNear(1), StateNo` trap state `285`; focused evaluator/helper coverage proves caller-provided `EnemyNear(1)` / `EnemyNear(var(n))` can read explicit opponent states when `opponentStates` exists. This is a bounded trigger subset only; negative or dynamic-invalid indexes are still reported unsupported, and team/simul nth-nearest selection, helper-owned opponent lists, mutation through redirects, and exact IKEMEN/MUGEN opponent selection parity remain blocked.
- `HitPauseTime` is now a supported expression identifier in the compiler/evaluator and can read the current actor hitpause counter through runtime expression contexts. Imported active-state controllers with `ignorehitpause = 1` can run in a bounded controller-only pass during global hitpause, and required `synthetic-imported-hitpausetime-ignorehitpause.json` checksum `a3a78bb8` gates P1 player advance into state `220` while P2 player freeze evidence remains present. This is trigger/controller-loop subset support, not a new controller family, not persistent parity, and not exact hitpause-loop parity.
- `Target, ...`, static `Target(id), ...`, and bounded dynamic executable target-id expressions such as `Target(var(0)), ...` are supported for the current two-player target memory. Required `synthetic-imported-bare-target-redirect.json` checksum `f9c90aa8` gates a direct player `HitDef` route where bare `Target, Life < 1000` branches P1 from state `200` to `270`; required `synthetic-imported-projectile-target-redirect.json` checksum `cd099094` gates a player-owned Projectile-only route where `NumTarget(77)` plus `Target(77), Life <= 969` branches P1 from state `200` to `277`; required `synthetic-imported-hitdef-projectile-target-mix.json` checksum `e98d4857` gates mixed owner-local target-memory reads where direct `HitDef` id `77` and player-owned `Projectile` id `78` both hit P2 before P1 branches into `278`; required `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2` gates delayed owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` after player-owned Projectile target memory for target id `77`; required `synthetic-imported-projectile-default-target-controllers.json` checksum `1c1a3e77` gates the same player-owned Projectile Target side-effect subset after omitted `projid` / `id` defaults target memory to id `0`; required `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962` gates delayed owner-local `TargetState value = 888` after player-owned Projectile target memory for target id `77`, routing P2 through attacker-owned state data `888 -> 889` before `SelfState` returns P2 to state `0`/control; required `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa` gates the same player-owned Projectile TargetState route after omitted `projid` / `id` defaults target memory to id `0`; required `synthetic-imported-helper-bare-target.json` checksum `15f3c1db` gates a helper-owned direct `HitDef` route where bare `Target, Life <= 965` branches the Helper from state `1200` to `1230/962`; required `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1` gates a helper-parented Projectile route where bare `Target, Life <= 982` branches the Helper from state `1241/977` to `1242/978`; required `synthetic-imported-helper-target-controllers.json` checksum `61f4c61e` gates the bounded helper-owned direct-HitDef Target side-effect route for `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop`; required `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8` gates the matching bounded helper-owned Target side-effect route after helper-parented Projectile target memory for target id `8861`; required `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae` gates the same route after omitted `projid` / `id` defaults to target id `0`; required `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3` gates helper-owned `TargetState value = 888` from helper-parented Projectile target memory for target id `8862` into owner-backed custom state data; required `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1` gates the same TargetState route after omitted `projid` / `id` defaults to target id `0`; required `synthetic-imported-helper-targetstate.json` checksum `011633b8` gates helper-local `TargetState value = 888` from helper-owned direct-HitDef target memory into owner-backed custom state data; required `synthetic-imported-default-numtarget.json` checksum `5869ebbd` gates a direct `HitDef` without `id` route where `NumTarget(0)` branches P1 from state `200` to `268`; required `synthetic-imported-default-target-redirect.json` checksum `d43caabf` gates the matching default target redirect route where `Target(0), Life < 1000` branches P1 from state `200` to `269`; required `synthetic-imported-target-redirect.json` checksum `89580963` gates a direct `HitDef` route where `Target(77), Life < 1000` branches P1 from state `200` to `286`; required `synthetic-imported-target-dynamic-redirect.json` checksum `9985b62a` gates owner-local `var(0) = 77` feeding `Target(var(0)), Life < 1000` and routing P1 to state `287`. The default-id gates keep target-link evidence for P2 target id `0`; the player bare/static/dynamic explicit-id gates keep target-link evidence for P2 target id `77`; the mixed target-memory gate keeps target-link evidence for both ids `77` and `78`; the player Projectile explicit Target-controller gate keeps target-link/binding evidence for P2 target id `77` plus final P1 `targetCount = 0`, the player Projectile default Target-controller gate keeps the same binding/final evidence for target id `0`, the explicit TargetState gate keeps custom-owner P2 actor-frame evidence plus final P2 state `0`/control, and the default TargetState gate keeps the same custom-owner evidence for target id `0`; the helper bare gate keeps target-link evidence for P2 target id `8878` owned by `p1-helper-0`; the helper Projectile bare gate keeps owner/helper target-link evidence for P2 target id `8863`; the helper direct-HitDef Target-controller gate keeps target-link/binding evidence for P2 target id `8879`; the helper Projectile Target-controller gates keep owner/helper target-link and binding evidence for P2 target ids `8861` and `0`; the helper direct-HitDef TargetState gate keeps target-link evidence for P2 target id `8880`, and the helper Projectile TargetState gates keep owner/helper target-link evidence for P2 target ids `8862` and `0`. This is trigger subset plus bounded player Projectile target redirect, mixed direct/projectile target-memory reads, explicit/default Target side effects, and explicit/default TargetState into owner-backed state data, helper direct-HitDef, explicit/default helper-parented Projectile Target side-effect, and explicit/default TargetState-into-owner-data subsets; Target* mutation mixing, unsupported or negative target-id expressions, helper-owned custom state tables, teams, multi-target selection, exact target lifetime/tick order, exact final-animation parity, and full target redirect/target-controller/TargetState parity remain blocked.
- Helper-local `EnemyNear, ...` is supported as a read-only redirect against the current two-player opponent runtime state when `RuntimeEffectLifecycleWorld` supplies `opponentState` to `HelperSystem`; caller-provided `EnemyNear(index)` / `EnemyNear(var(n))` and `NumEnemy` are supported when `opponentStates` supplies an explicit opponent-state list. Required `synthetic-imported-helper-enemynear.json` checksum `35498955` gates a visual Helper route from state `1200` to `1202` / anim `922` through `EnemyNear, StateNo` plus `EnemyNear, Life`, and focused `RuntimeCnsSubset` / `EffectActorSystem` coverage proves indexed list reads and explicit opponent-count reads. This is helper-local trigger subset support only; caller-independent indexed enemy selection, teams/simul/turns, helper-owned opponents, helper combat, and exact opponent selection parity remain blocked.
- Helper-local `NumExplod(id)` is supported for current first-generation visual Helpers against helper-parented owner-side Explods. Required `synthetic-imported-helper-numexplod.json` checksum `4328278a` gates a visual Helper route from state `1200` to `1210` / anim `930` after spawning Explod anim `942` with `parentId = p1-helper-0`. This is helper-local count-trigger support only; helper-owned effect namespaces, exact parent/root/team scopes, dynamic effect params, and full helper trigger parity remain blocked.
- Helper-local `NumHelper(id)` is supported for current first-generation visual Helpers against owner-side visual Helper actors in the same effect store. Required `synthetic-imported-helper-numhelper.json` checksum `4e32e951` gates a visual Helper route from state `1200` to `1211` / anim `931` through `NumHelper(42) > 0`. This is helper-local count-trigger support only; exact helper ownership scopes, broader indexed/team/helper-owned redirects beyond caller-provided `EnemyNear(index)` lists, helper combat, and full helper trigger parity remain blocked.
- Helper-local `NumProj` / `NumProjID(id)` is supported for current first-generation visual Helpers against helper-parented owner-side Projectile actors in the same effect store. Required `synthetic-imported-helper-numproj.json` checksum `3312a554` gates a visual Helper route from state `1200` to `1213` / anim `933` through `NumProjID(8851) > 0` after spawning Projectile anim `944` with `parentId = p1-helper-0`; focused coverage proves same-id player-owned and removed helper-parented projectiles do not count. This is helper-local count-trigger support only; exact projectile namespaces/scopes, dynamic ids, teams, and full helper Projectile parity remain blocked.
- Helper-local static `Projectile` is supported for current first-generation visual Helpers as owner-side projectile spawn only. Required `synthetic-imported-helper-projectile.json` checksum `b6269136` gates a visual Helper route from state `1200` to `1212` / anim `932` while spawning projectile anim `943` with `parentId = p1-helper-0`; required `synthetic-imported-helper-projectile-target.json` checksum `49261b53` gates bounded helper-owned explicit-id target memory after a helper-parented Projectile contact, required `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1` gates helper-local bare `Target, Life` after helper-parented Projectile target memory, required `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8` gates bounded helper-owned Target controller side effects after helper-parented Projectile explicit-id target memory, required `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3` gates helper-owned TargetState into owner-backed state data after helper-parented Projectile explicit-id target memory, required `synthetic-imported-helper-projectile-default-target.json` checksum `b0daddf6` gates the same target-memory path for omitted `projid`/`id` defaulting to target id `0`, required `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae` gates bounded helper-owned Target controller side effects for that default id `0` path, and required `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1` gates helper-owned TargetState into owner-backed state data for that default id `0` path. Helper-owned projectile combat/contact presentation, exact projectile namespaces, dynamic ids beyond gated static/default ids, and full helper projectile parity remain blocked.
- Helper-local `ModifyProjectile` is supported for current first-generation visual Helpers against helper-parented owner-side Projectile actors by static id and bounded dynamic resolver coverage. Required `synthetic-imported-helper-modifyprojectile.json` checksum `09d3f7e4` gates a visual Helper route through `1200 -> 1214 -> 1215` / anims `934` and `935`, after spawning projectile anim `945` with `parentId = p1-helper-0`; its required payload includes explicit bounds `projedgebound = 28`, `projstagebound = 24`, and `projheightbound = -120,60`. Required `synthetic-imported-helper-modifyprojectile-dynamic-bounds.json` checksum `f582153e` / final checksum `adc63407` gates helper-local dynamic `projedgebound` / `projstagebound` / `projheightbound` resolution through `Parent` / `Root` redirects into bounds `52` / `36` / `-144,72`. Required `synthetic-imported-helper-modifyprojectile-dynamic-params.json` checksum `2d88a550` / final checksum `edb6d2d2` gates helper-local `Parent` / `Root` dynamic non-bound params mutating helper-parented motion/payload fields while focused coverage keeps same-id player-owned Projectiles unchanged. Required `synthetic-imported-helper-modifyprojectile-omitted-bounds.json` checksum `9db04bbc` / final checksum `555d744b` gates preservation of explicit helper-parented Projectile bounds when later helper-local mutation omits all bound params. This is helper-local visual Projectile mutation support only; exact namespace scopes, teams, tick order, team/simul helper selection, and full helper Projectile parity remain blocked.
- Helper-local `ProjHit(id)` / bounded `ProjContact(id)` markers are supported for current first-generation visual Helpers against helper-parented owner-side Projectile contact state. Required `synthetic-imported-helper-projhit.json` checksum `2d9a281e` gates a visual Helper route through `1200 -> 1216 -> 1217` / anims `936` and `937`, after spawning projectile anim `946` with `parentId = p1-helper-0`; focused coverage proves same-id player-owned Projectile contact stays ignored while helper-parented Projectile contact triggers the branch. Required `synthetic-imported-helper-projectile-hitcount.json` trace checksum `c8f5dc55` / final checksum `e1569fab` now proves a helper-parented Projectile normal hit can also mirror attacker-side hit-count memory into the visual Helper and branch helper `1257 -> 1258` through helper-local `HitCount >= 1 && UniqHitCount >= 1`, with typed `audio:playsnd`, helper-local `S5,43`, and FightFX `F7002` package telemetry. Required `synthetic-imported-helper-projectile-target.json` checksum `49261b53` proves a separate helper-parented Projectile contact can mirror target memory into the spawning Helper and branch through `NumTarget(id)` plus `Target(id), Life`; required `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1` proves the same mirrored target memory can feed helper-local bare `Target, Life`; required `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8` and `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae` prove that mirrored explicit/default target memory can also feed bounded helper-owned `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop`; required `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3` and `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1` prove mirrored explicit/default target memory can feed bounded helper-owned `TargetState` into owner-backed custom state data. Exact `ProjContact` / `ProjHit` tick order and lifetime, exact `HitCount` lifetime/persist semantics, dynamic projectile params, exact namespace scopes, teams, and full helper Projectile parity remain blocked.
- Helper-local `ProjHitTime(0)`, `ProjGuardedTime(0)`, `ProjContactTime(0)`, `ProjCancelTime(0)`, fixed-id `ProjCancelTime(id)`, and bounded expression-derived `ProjCancelTime(expr)` reads are supported for current first-generation visual Helpers against helper-parented owner-side Projectile hit/guard/contact/cancel state. Required `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b` gates a Helper route through `1200 -> 1260 -> 1261` / anims `986` and `987`, after spawning Projectile anim `988` with `parentId = p1-helper-0`; required `synthetic-imported-helper-projguardedtime-any.json` checksum `1f1a38e4` gates `1200 -> 1262 -> 1263` / anims `989` and `990` after Projectile anim `991`; required `synthetic-imported-helper-projcontacttime-any.json` checksum `0d9f7829` gates `1200 -> 1264 -> 1265` / anims `992` and `993` after Projectile anim `994`; required `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01` gates `1200 -> 1266 -> 1267` / anims `995` and `996` after helper-parented Projectile anim `997` is canceled by an opposing Projectile and plays cancel anim `998`; required `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176` gates `1200 -> 1268 -> 1269` / anims `1005` and `1006` after helper-parented Projectile id `8868` plays cancel anim `1008`; required `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2` gates `1200 -> 1270 -> 1271` / anims `1015` and `1016` after helper-parented Projectile id `8869` plays cancel anim `1018` and the Helper branches through `ProjCancelTime(8869 + var(0))`. Focused coverage proves same-owner player Projectile contact/cancel stays ignored, nonmatching fixed ids stay `-1`, nonzero `ProjCancelTime(var(n))` can select the matching helper-parented canceled Projectile id, and helper-parented Projectile markers trigger after marker age advances. Exact hit/contact/guard/cancel tick-order/lifetime, multi-projectile id selection, broad dynamic expression parity, exact namespace scopes, teams, and full helper Projectile parity remain blocked.
- Helper-local `ProjGuarded(id)` / bounded `ProjGuardedTime(id)` markers are supported for current first-generation visual Helpers against helper-parented owner-side Projectile guard state. Required `synthetic-imported-helper-projguard.json` checksum `d2e2f20d` gates a visual Helper route through `1200 -> 1218 -> 1219` / anims `938` and `947`, after spawning projectile anim `948` with `parentId = p1-helper-0`; focused coverage proves same-id player-owned Projectile guard contact stays ignored while helper-parented Projectile guard contact triggers the branch after contact age advances. The gate also requires owner-side target-link evidence plus shared `guardsound = S6,0` / FightFX `guard.sparkno = F7004` package telemetry. Exact guard target lifetime, `ProjContact` / `ProjHit` / `ProjGuarded` tick order, dynamic projectile params, exact namespace scopes, teams, and full helper Projectile parity remain blocked.
- Helper-local `ProjContact(id)` / bounded `ProjContactTime(id)` markers are supported for current first-generation visual Helpers against helper-parented owner-side Projectile generic contact state. Required `synthetic-imported-helper-projcontact.json` checksum `4dcbdd25` gates a visual Helper route through `1200 -> 1220 -> 1221` / anims `949` and `950`, after spawning projectile anim `951` with `parentId = p1-helper-0`; focused coverage proves same-id player-owned Projectile contact stays ignored while helper-parented Projectile generic contact triggers the branch after contact age advances. The gate also requires owner-side target-link evidence plus shared `guardsound = S6,0` / FightFX `guard.sparkno = F7004` package telemetry. Exact generic target lifetime, `ProjContact` / `ProjHit` / `ProjGuarded` tick order, dynamic projectile params, exact namespace scopes, teams, and full helper Projectile parity remain blocked.
- Player-owned Projectile `ProjHitTime(0)` any-id hit-time reads are supported only for the current owner state. Required `synthetic-imported-projectile-hittime-any.json` checksum `47c1cf7f` gates a player-owned Projectile hit route where P1 branches through `ProjHitTime(0) >= 0` into state/action `282` with hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. Exact hit tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams, and full Projectile hit parity remain blocked.
- Player-owned Projectile `ProjContactTime(77)` fixed-id and `ProjContactTime(0)` any-id contact-time reads are supported only for the current owner state. Required `synthetic-imported-projectile-contacttime-id.json` checksum `e9ebf36a` gates a player-owned Projectile contact route where P1 branches through `ProjContactTime(77) >= 1` into state/action `321`; required `synthetic-imported-projectile-contacttime-any.json` checksum `f1751155` gates `ProjContactTime(0) >= 0` into state/action `281`. Both require hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. Exact contact tick-order/lifetime, multi-projectile same-id/id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams, and full Projectile contact parity remain blocked.
- Player-owned Projectile `ProjGuardedTime(77)` fixed-id and `ProjGuardedTime(0)` any-id guarded contact-time reads are supported only for the current owner state. Required `synthetic-imported-projectile-guardedtime-id.json` checksum `dfd08f28` gates a player-owned Projectile guard route where P1 branches through `ProjGuardedTime(77) >= 1` into state/action `322`; required `synthetic-imported-projectile-guardedtime-any.json` checksum `c8473340` gates `ProjGuardedTime(0) >= 0` into state/action `279`. Both require guard event/reason, Projectile lifecycle, effect-store, and target-link evidence. Exact guard tick-order/lifetime, multi-projectile same-id/id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams, and full Projectile guard parity remain blocked.

- Current IKEMEN override for the earlier `SuperPause p2defmul` entry: explicit `ikemen-go` resolves omitted/non-positive values through game-level `1.5`, accepts a match override, and applies a separate temporary scale to the current opposing root plus existing helpers. Required team-defense checksum `76873f0d` / final `b4425c66` proves P2 and `p2-helper-0` at `0.6667` and final P2 life `950`. Current temporary-field trace checksums are positive target `cb237a35` / final `361c48ef`, IKEMEN stack `f3c5f15e` / final `6d3f8447`, and helper-owned SuperPause `472ac081` / final `c038233c`; these supersede earlier base-defense projection checksums. The earlier zero-value blocker is closed for this bounded profile/topology; simul/tag teams, global config loading, helper defender combat, nested ancestry, and exact hitpause timing remain blocked.

## Current Guard Notes

- Required synthetic auto guard-start/end gates now include minimum controller-event and actor-frame state-order evidence: `synthetic-imported-auto-guard-start.json` checksum `0c734290` requires P2 state `120` `ChangeState` `Guard Start Done` plus actor-frame `120` before `130`, and `synthetic-imported-auto-guard-end.json` checksum `d1dc0aa3` requires P2 state `120` `Guard Start Done` before state `130` `ChangeState` `Stop Guarding`, actor-frame `120 -> 130 -> 140`, and final idle/control evidence. This strengthens bounded guard-start/end trace evidence without claiming exact proximity guard, guard-end timing, full Common1 controller loop, or full MUGEN/IKEMEN guard VM parity.
- Required synthetic guard-hit gates now also include actor-frame state/physics/body/push telemetry: `synthetic-imported-default-guard-state.json` checksum `016938a1` gates stand `150 -> 151`, `synthetic-imported-crouch-guard-state.json` checksum `6c4321af` and `synthetic-imported-diagonal-crouch-guard-state.json` checksum `1dd33fb5` gate crouch `152 -> 153`, `synthetic-imported-air-guard-state.json` checksum `ce9cc9ba` gates air `154 -> 155` with bounded air velocity telemetry, and `synthetic-imported-air-guard-landing.json` checksum `d6986d7f` gates the bounded synthetic air landing walk-control route `154 -> 155 -> 52 -> 20` with `155` landing `VelSet` / `PosSet` operations and final walk/control. This strengthens synthetic Common1-style guard-hit evidence without claiming exact proximity guard, guard effects, exact air physics/timing, state-`52` internals, controller-loop parity, or full guard VM parity.

## Current Common1 Recovery Notes

- Optional private-fixture `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae` now requires real KFM held-crouch prep state `11`, Common1 `5010 -> 5011 -> 0` through `HitShakeOver` / `HitOver`, KFM-specific actor-frame evidence for `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width `39/39`, bounded slide velocity, ordered `5010` `ChangeAnim` / `ChangeState`, ordered `5011` `HitVelSet`, `VelMul`, `VelSet`, `DefenceMulSet`, their typed operations, and final `0`/control. This is private fixture confidence only; public KFM support, exact tick timing, exact crouch get-hit animation/slide tables beyond this bounded frame/controller profile, fall routing, helper/team/custom-state breadth, and full Common1 get-hit parity remain blocked.
- Optional private-fixture `kfm-official-default-fall-recovery-input.json` checksum `5154d760` now requires ordered real KFM air-recovery evidence for `5050 -> 5210 -> 52 -> 0`: actor-frame order for `5050 -> 5210 -> 52`, controller/typed-operation order through `5050` `VelAdd` / `ChangeState`, `5210` `PalFX` / `sprite-effect:palfx`, `PosFreeze` / `bounds:posfreeze`, `Turn` / `orientation:turn`, `NotHitBy` / `eligibility:nothitby`, `VelMul`, `VelAdd`, `ChangeState`, `52` `VarSet`, and state `0` `VelSet` / `kinematic:velset`. This strengthens fixture confidence only; public KFM support, exact thresholds/velocity math, exact tick-order parity, and full recovery parity remain blocked.
- Optional private-fixture `kfm-official-default-air-fall-recovery-input.json` checksum `a431028a` and `kfm-official-default-air-fall-recovery-too-early.json` checksum `a1db1589` now require real KFM air-entry evidence through `5020 -> 5030 -> 5035 -> 5050`: the input route accepts recovery through `5210 -> 52 -> 0` after KFM's `5030` countdown reaches `0`, while the negative route stays in `5050` with positive `fall.recovertime` and forbids recovery/landing/ground-impact/lie-down branches. This is private fixture confidence only; public KFM support, exact thresholds/velocity math, exact tick-order parity, and full Common1 recovery parity remain blocked.
- Optional private-fixture `kfm-official-qcf-x.json` requires real KFM `QCF_x` into state `1000` with ordered `ChangeState` / `PosAdd` / `HitDef`, typed `kinematic:posadd` / `hitdef`, authored `PlaySnd` telemetry for `S0,3` and contact hitsound `S5,4`, inherited `[Data] sparkno = 2` hit-spark telemetry with `sparkxy = -10,-60`, shared contact package metadata between `S5,4` and spark, target link `p1 -> p2 / 0`, final P1 `power = 35` / `targetCount = 1`, and final P2 `life = 915` plus bounded hitfall evidence. The 85-damage branch now follows source-backed size-box `P2BodyDist X`; this remains one private Kung Fu Palm route, not exact timing, pushback, renderer/audio playback, broad special parity, or public KFM support.
- Optional private-fixture `kfm-official-default-fall-ground-recovery.json` checksum `6079c8c9` now requires ordered real KFM ground-recovery actor-frame evidence for observable `5050 -> 5200 -> 52` buckets plus bounded controller/typed-operation order through `5050` `VelAdd` / `ChangeState`, `5200` `VelAdd` / `SelfState`, and `52` landing `VelSet` / `PosSet` / `kinematic:*` / `ChangeState`. Optional private-fixture `kfm-official-default-fall-ground-recovery-priority.json` checksum `6079c8c9` mirrors the same route while forbidding generic air recovery `5210` and lie-down states. `5201` remains required as an executed state; no summarized `5201` actor-frame bucket, exact threshold table, velocity math, full controller-loop tick order, public KFM support, or score movement is claimed.
- Required `synthetic-imported-default-fall-official-ground-recovery.json` checksum `74b72495` now gates a bounded official-style synthetic ground-recovery sequence through `5050 -> 5200 -> 5201 -> 52 -> 0`. It requires actor-frame order for positive-to-zero `fall.recovertime`, bounded `5201` recovery velocity/position telemetry, `52` landing y = 0 evidence, final idle/control, and controller/order evidence for `VelAdd`, `ChangeState`, `SelfState`, `VelSet`, `PosSet`, `NotHitBy`, `CtrlSet`, plus typed `kinematic:*`, `eligibility:nothitby`, and `resource:ctrlset` operations. This strengthens Common1 recovery evidence only; exact `fall.recovertime` tables, velocity math, controller-loop timing, public KFM support, and full recovery parity remain blocked.

## Current HitDef Presentation Notes

- `RuntimeHitDefControllerDispatchWorld` owns active-state HitDef activation dispatch from compiled CNS classification into the current attack payload: controller telemetry, typed `hitdef` operation selection, raw fallback attack params, fired-HitDef dedupe by state/line/frame, current-frame `Clsn1` hitbox handoff, currentMove mutation, attack movetype/control writes, and operation telemetry. Focused `HitDefSystem` coverage proves activation payloads and duplicate suppression. This is dispatch ownership only; trigger filtering, contact resolution, Common1/custom-state routing, target/reversal consequences, helper/projectile ownership, exact multi-hit lifetime, broad attr grammar, and full HitDef VM parity remain partial.
- Required common/default and FightFX spark gates now require selected-frame and multi-frame AIR metadata in addition to first resolved frame metadata: `synthetic-imported-hitdef-common-spark.json` checksum `5ea054d7`, `synthetic-imported-hitdef-fightfx-spark.json` checksum `11537b56`, `synthetic-imported-hitdef-common-guard-spark.json` checksum `7650a09c`, and `synthetic-imported-hitdef-fightfx-guard-spark.json` checksum `32f3e92d` require selected first-frame offset `3,-4`, first-frame duration `5`, at least two asset frames, frame indices `[0, 1]`, and total authored duration `11` before renderer handoff. Required `synthetic-imported-hitdef-data-spark.json` checksum `d950796c` and `synthetic-imported-hitdef-data-guard-spark.json` checksum `7bd9d162` prove omitted direct `HitDef` `sparkno` / `guard.sparkno` inherit parsed CNS `[Data]` constants before hit-effect emission. Required `synthetic-imported-hitdef-guard-effect-package.json` checksum `1c3167b7` proves one guarded direct contact can emit bounded `guardsound = S6,0` telemetry and FightFX `guard.sparkno = F7004` selected-frame/multi-frame metadata with shared contact package metadata; required `synthetic-imported-hitdef-hit-effect-package.json` checksum `46aa5ce1` proves one direct hit contact can emit bounded `hitsound = F5,0` telemetry with `soundPrefix = kfm` and FightFX `sparkno = F7002` selected-frame/multi-frame metadata with shared contact package metadata. Focused loader/runtime tests also prove IKEMEN-style character `[Files] fx` FightFX packages can be selected by matching `fightfx.prefix` for runtime `F` spark frames and prefixed SND lookup. This keeps `HitDef` presentation at `executed-partial`: exact intra-tick audio/spark ordering, channel priority/mixing/timing, full `sys.ffx` lifetime/refcount/cache semantics, exact FightFX/common sprite lookup, render timing, binding, layering, scale, palette, motif/screenpack ownership, hit/guard-effect parity, and full spark/audio parity remain partial.

## Current Projectile Presentation Notes

- Required `synthetic-imported-projectile-contact.json` trace checksum `57b3b556` / final checksum `e0f3e41c` now requires a bounded Projectile hit contact package: attacker-side `hitsound = S5,0` `PlaySnd` telemetry plus FightFX `sparkno = F7002` multi-frame metadata sharing the same non-empty projectile `contactId`, `contactTick`, and `contactKind = hit`. Required `synthetic-imported-projectile-guard.json` trace checksum `eb9c2e58` / final checksum `b1c74e5e` applies the same package shape to held-back projectile guard contact with `guardsound = S6,0`, FightFX `guard.sparkno = F7004`, and `contactKind = guard`. This keeps `Projectile` presentation at `executed-partial`: exact projectile effect timing, SND playback, channel/mixing parity, renderer lookup, binding, layering, scale, palette, helper-owned projectile effects, multi-target presentation, and full Projectile parity remain partial.

## Current Effect Spawn Dispatch Notes

- `RuntimeEffectSpawnControllerDispatchWorld` owns active-state Explod / RemoveExplod / ModifyExplod / Helper / Projectile / ModifyProjectile dispatch from compiled CNS classification into `RuntimeEffectSpawnWorld`: controller telemetry, typed effect operation selection, spawn/count mutation handoff, and success-gated operation telemetry. Focused `EffectSpawnSystem` coverage proves successful Explod telemetry/mutation and failed ModifyExplod no-operation gating. This is dispatch ownership only; exact effect spawn tick order, helper-owned effect namespaces, dynamic effect params, helper-owned Projectile combat/contact beyond bounded target memory, helper `Target*` mutation/multi-target parity, and full effect/helper/projectile VM parity remain partial.

## Current Helper Lifecycle Context Notes

- `RuntimeEffectLifecycleWorld` forwards `stageBounds`, `stageTime`, and `runtimeTick` into helper-local controller execution for active and paused lifecycle passes, `RuntimeMatchOpponentContextWorld` owns the concrete 1v1 active/pause/hitpause direct-opponent plus singleton lifecycle-list source, and `RuntimeEffectHelperContextWorld` owns the parent/root/opponent roster, target-candidate, and helper hook context construction consumed by those passes. Focused `EffectLifecycleSystem` coverage proves a visual Helper can reject `GameTime` before the supplied stage time, then execute `PlaySnd`, `VelSet x = FrontEdgeDist`, and `ChangeState` using supplied stage bounds and runtime tick; focused `RuntimeMatchOpponentContextSystem` and `RuntimeEffectHelperContextSystem` coverage prove the context seams directly. This is helper lifecycle context ownership only; exact helper clock parity, pause/combat ordering, redirects beyond the bounded owner/root/opponent/target routes, and full helper VM parity remain partial.
- Helper-local `Parent` / `Root` trigger redirects are trace-gated by required `synthetic-imported-helper-parentroot.json` checksum `5154220c`. The gate proves a current first-generation visual Helper can read cloned owner/root runtime state through `Parent,StateNo`, `Parent,Vel X`, and `Root,Anim`, then route `1200 -> 1400` / anim `940`. Helper-local controller-param redirects are separately trace-gated by required `synthetic-imported-helper-controller-param-parentroot.json` checksum `94919326`, proving dynamic `VelSet x = Parent,Life - 995` / `y = Root,StateNo - 203` emits typed `kinematic:velset`, reaches velocity `5,-3`, and routes `1200 -> 1401` / anim `941`; required `synthetic-imported-helper-dynamic-veladd.json` checksum `fbb8bcae` proves the adjacent dynamic `VelAdd x = Parent,Life - 996` / `y = Root,StateNo - 205` route emits typed `kinematic:veladd` and routes `1200 -> 1402` / anim `942`; required `synthetic-imported-helper-dynamic-velmul.json` checksum `08220a98` proves the adjacent dynamic `VelMul x = Parent,Life - 998` / `y = Root,StateNo - 202` route emits typed `kinematic:velmul` and routes `1200 -> 1403` / anim `943`; required `synthetic-imported-helper-dynamic-posset.json` checksum `50596bc2` proves the adjacent dynamic `PosSet x = Parent,Life - 984` / `y = Root,StateNo - 220` route emits typed `kinematic:posset` and routes `1200 -> 1404` / anim `944`; required `synthetic-imported-helper-dynamic-posadd.json` checksum `97ec15d0` proves the adjacent dynamic `PosAdd x = Parent,Life - 984` / `y = Root,StateNo - 220` route emits typed `kinematic:posadd` and routes `1200 -> 1405` / anim `945`. This is first-generation helper-local redirect evidence only; helper/broader invalid-destination `bottom`, nested helper ancestry where root differs from parent, helper-spawned helpers, player-state parent/root ownership, teams/simul, redirect mutation, keyctrl, and full helper redirect parity remain blocked.
- `ExpressionEvaluator` can resolve `EnemyNear(index)` through an explicit enemy-near redirect callback and read `NumEnemy` through an explicit provider. `RuntimeExpressionContextWorld` can expose a caller-provided opponent roster through the shared runtime read model and delegates nearest ordering to `RuntimeOpponentSelectionWorld`; that world owns bounded horizontal body-distance scoring, raw runtime-state list ordering, stable ties, finite-before-nonfinite ordering, metadata-preserving roster construction, and metadata-preserving roster ordering. `RuntimeMatchOpponentContextWorld` owns the concrete 1v1 active/pause/hitpause source that maps P1/P2 into direct opponent plus explicit singleton lifecycle list with unknown-actor fail-closed behavior. `RuntimeEffectHelperContextWorld` accepts that explicit lifecycle `opponents` list and turns it into an id-bearing helper-local `opponentRoster` through `RuntimeOpponentSelectionWorld` for active/paused visual Helper lifecycle contexts, while preserving current opponent id/state fallback and explicit roster overrides. `HelperSystem` supplies helper-local `opponentStates` through the same selection boundary and can accept richer `opponentRoster` entries with id plus runtime state, so non-current helper-local indexed `TeamSide` can resolve after sorting when a broader caller supplies more than the current opponent. Focused `RuntimeMatchOpponentContextSystem`, `MatchInteractionSystem`, `PauseSystem`, `RuntimeHitPauseSystem`, `EffectLifecycleSystem`, `RuntimeEffectHelperContextSystem`, `RuntimeOpponentSelectionSystem`, `RuntimeExpressionContextSystem`, `RuntimeCnsSubset`, and `EffectActorSystem` coverage proves concrete 1v1 lifecycle roster source forwarding, explicit lifecycle opponent-list routing, id-bearing roster construction, lifecycle roster forwarding, direct evaluator support, dynamic index expressions through `var(n)`, runtime-context indexed routing, nearest roster ordering, helper-local nearest indexed routing, helper-local id-bearing roster `TeamSide`, explicit opponent-count reads, and missing-index fail-closed behavior. This is caller-provided/current-opponent roster support only; real teams/simul roster ownership, automatic multi-opponent match roster discovery, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, and full helper/team VM parity remain partial.

## Current Active Controller Telemetry Notes

- `RuntimeActiveControllerTelemetryWorld` owns the bounded active-controller telemetry hook adapter used by active state hooks, side-effect dispatchers, and fallback runtime-controller dispatch before they forward records into `RuntimeCompatibilityTelemetryWorld`. Focused `RuntimeActiveControllerTelemetrySystem` coverage proves controller and operation forwarding through the seam. This is active-controller telemetry ownership only; exact event semantics, imported-only filtering, event retention limits, helper/team/redirect telemetry breadth, visual/debug UI parity, and full CNS VM parity remain partial.

## Current Combat State Hook Notes

- `RuntimeMatchCombatStateHooksWorld` owns the bounded adapter that builds direct/projectile combat state hooks and helper-combat state hooks before `RuntimeMatchCombatBridgeWorld` routes direct, projectile, and helper combat. Direct/projectile hooks preserve state-owner availability and entry options; helper hooks keep self-owned availability checks and still forward entry options. Focused `RuntimeMatchCombatStateHooksSystem` coverage proves both contracts. This is combat/helper ownership only; helper-owned custom-state table breadth, throws, teams/simul actor registries, multi-target helper ownership, exact combat/helper tick order, and full combat/helper VM parity remain partial.

## Current State Transition Notes

- `RuntimeStateTransitionControllerWorld` is the current named boundary for the basic `StateControllerExecutor` `ChangeState` / `SelfState` path. Focused `StateTransitionControllerSystem` coverage proves raw `value` / `stateno` expression fallback, previous-state metadata writes, animation timing reset, optional `ctrl`, missing-value reporting, unchanged-state timing reset behavior, and executor routing. This does not prove exact active-state entry tick order, persistent controller semantics, redirects, helper/team ownership, custom-state breadth, or full MUGEN/IKEMEN state-entry parity.

## Current Registry

Latest registry addendum: `synthetic-imported-afterimage-dynamic.json` trace checksum `e7299ac5` / final checksum `b946d805` upgrades required bounded active imported dynamic AfterImage params into typed sprite-effect telemetry. The route presses `x`, seeds owner-local vars, executes dynamic `AfterImage time/length/timegap/framegap/paladd/palmul` params, and requires typed `sprite-effect:afterimage` evidence plus actor-frame/final ghost-trail evidence; `pnpm qa:trace` passes 523/523 artifacts, 492 required and 31 optional. This is current dynamic owner-local AfterImage telemetry only; exact trail blending, palette math, sampling cadence, renderer parity, helper/redirect ownership, score movement, and full presentation parity remain partial.

Previous registry addendum: `synthetic-imported-angle-dynamic.json` checksum `13560dcd` / final checksum `4d7c4726` and `synthetic-imported-anglemul-dynamic.json` checksum `0bb54a1c` / final checksum `c9f2b557` remain required bounded active imported dynamic Angle typed sprite-effect telemetry.

Previous registry addendum: `synthetic-imported-remappal-dynamic.json` checksum `5f04f2d4` / final checksum `71ad06f0` remains required bounded active imported dynamic `RemapPal source/dest` typed sprite-effect telemetry.

Previous registry addendum: `synthetic-imported-trans-dynamic.json` checksum `4bffcd82` / final checksum `5beea0f0` upgrades required bounded active imported dynamic `Trans alpha` into typed sprite-effect telemetry. The route presses `x`, seeds `var(0)=96` and `var(1)=160`, executes `Trans trans = addalpha` with `alpha = var(0),var(1)`, and requires `Trans`, `variable:varset`, `hitdef`, and `sprite-effect:trans` evidence plus actor-frame/final `renderOpacity = 0.375`; `pnpm qa:trace` passes 523/523 artifacts, 492 required and 31 optional. This is current dynamic owner-local Trans alpha telemetry only; exact add/sub alpha math, palette/remap interaction, draw-order parity, renderer parity, helper/redirect ownership, score movement, and full presentation parity remain partial.

Previous registry addendum: `synthetic-imported-resourceset-dynamic.json` checksum `1bd04945` / final checksum `35db4dcd` remains required bounded active imported dynamic `LifeSet`, `PowerAdd`, and `PowerSet` typed resource telemetry.

Previous registry addendum: `synthetic-imported-screenbound-dynamic.json` checksum `9797bdfe` / final checksum `d76b641a` remains required bounded active imported dynamic `ScreenBound` typed bounds telemetry. The route presses `x`, seeds `var(0)=0`, `var(1)=0`, and `var(2)=1`, executes `ScreenBound value = var(0), movecamera = var(1),var(2)` plus an offstage `PosAdd` probe, and requires `ScreenBound` controller evidence, `variable:varset`, `bounds:screenbound`, and `kinematic:posadd` operation evidence, plus actor-frame `screenBound = false`, `moveCameraX = false`, `moveCameraY = true` and stage-frame bound/camera evidence.

Previous registry addendum: `synthetic-imported-posfreeze-dynamic.json` checksum `8de0c2e9` / final checksum `6c40bb79` remains required bounded active imported dynamic `PosFreeze` typed bounds telemetry. The route presses `x`, seeds `var(0)=1` and `var(1)=0`, executes `PosFreeze x = var(0), y = var(1)`, and requires `PosFreeze` controller evidence, `variable:varset`, `bounds:posfreeze`, and `hitdef` operation evidence, plus actor-frame/final `posFreezeX = true` and `posFreezeY = false`.

Previous registry addendum: `synthetic-imported-helper-dynamic-posadd.json` checksum `97ec15d0` remains required bounded helper-local dynamic `PosAdd` typed telemetry. Previous registry addendum `synthetic-imported-helper-dynamic-posset.json` checksum `50596bc2` remains required for bounded helper-local dynamic `PosSet` telemetry, previous `synthetic-imported-helper-dynamic-velmul.json` checksum `08220a98` remains required for bounded helper-local dynamic `VelMul` telemetry, previous `synthetic-imported-helper-dynamic-veladd.json` checksum `fbb8bcae` remains required for bounded helper-local dynamic `VelAdd` telemetry, previous `synthetic-imported-helper-controller-param-parentroot.json` checksum `94919326` remains required for bounded helper-local dynamic `VelSet` telemetry, previous `synthetic-imported-dynamic-posadd.json` checksum `8ac604b1` remains required for bounded active-state dynamic `PosAdd` telemetry, previous `synthetic-imported-dynamic-posset.json` checksum `aeb730fb` remains required for bounded active-state dynamic `PosSet` telemetry, previous `synthetic-imported-dynamic-velmul.json` checksum `4d241401` remains required for bounded active-state dynamic `VelMul` telemetry, previous `synthetic-imported-dynamic-veladd.json` checksum `daf99fb4` remains required for bounded active-state dynamic `VelAdd` telemetry, and previous `synthetic-imported-const-controller-param.json` checksum `2dad3a50` remains required for bounded active-state dynamic `VelSet` plus player-local `Const*` controller-param evidence.

Previous registry addendum: `synthetic-imported-const-controller-param.json` checksum `2dad3a50` adds required bounded `Const240p` / `Const480p` / `Const720p` player-local coordinate conversion evidence inside active-state `VelSet` controller params. The route presses `x`, uses player `localCoord = 640,480`, executes `VelSet x = Const240p(3) + Const480p(6)` and `y = 0 - Const720p(12)`, requires controller-order evidence, proves velocity telemetry `x = 12` / `y = -6`, and requires static seed plus dynamic resolved `VelSet` operation telemetry; that checkpoint passed 509/509 artifacts, 478 required and 31 optional. This is current `VelSet` param-context coordinate conversion plus active-state dynamic `VelSet` typed-telemetry evidence only; broad coordinate translation across all controller params, dynamic typed-operation lowering beyond active-state `VelSet`, exact player-local coordinate translation, renderer/screenpack viewport ownership, camera animation parity, IKEMEN `config.json` execution, helper/team/simul namespace breadth, score movement, and full viewport parity remain partial. Previous registry addendum `synthetic-imported-const-coordinate.json` checksum `ea879c1b` remains required for bounded State -1 route conversion evidence.

Previous registry addendum: `synthetic-imported-const-coordinate.json` checksum `ea879c1b` adds required bounded `Const240p` / `Const480p` / `Const720p` player-local coordinate conversion evidence. The route presses `x`, uses player `localCoord = 640,480`, evaluates `Const240p(3) = 6`, `Const480p(6) = 6`, and `Const720p(12) = 6`, and routes imported State -1 into state/action `9304`; that checkpoint passed 508/508 artifacts, 477 required and 31 optional. This is expression-context coordinate conversion evidence only. Previous registry addendum `synthetic-imported-config-gamespace.json` checksum `2f3c0a63` remains required for bounded INI config game-space evidence.

Previous registry addendum: `synthetic-imported-config-gamespace.json` checksum `2f3c0a63` adds required bounded INI config game-space evidence. The route presses `x`, uses parsed-config-equivalent `1280x720` dimensions over a `640x480` stage localcoord, evaluates `ScreenWidth = 1280`, `ScreenHeight = 720`, `GameWidth = 2560`, and `GameHeight = 1440` at camera zoom `0.5`, and routes imported State -1 into state/action `9303`; that checkpoint passed 507/507 artifacts, 476 required and 31 optional. This is config parsing plus trigger expression precedence evidence only; exact player-local coordinate translation, renderer/screenpack viewport ownership, camera animation parity, IKEMEN `config.json` execution, helper/team/simul namespace breadth, score movement, and full viewport parity remain partial. Previous registry addendum `synthetic-imported-screenspace.json` checksum `5330bacd` remains required for bounded stage-local `ScreenWidth` / `ScreenHeight` evidence.

Previous registry addendum: `synthetic-imported-modifyprojectile-omitted-bounds.json` checksum `24cbb1dc` / final checksum `e94d1480` and `synthetic-imported-helper-modifyprojectile-omitted-bounds.json` checksum `9db04bbc` / final checksum `555d744b` add required owner-side/helper-local `ModifyProjectile` omitted-bound preservation evidence. The routes mutate non-bound live Projectile fields while omitting `projedgebound`, `projstagebound`, and `projheightbound`, and require the existing explicit bounds to survive in payload evidence. Previous required owner/helper dynamic params and dynamic bounds gates remain: owner params `synthetic-imported-modifyprojectile-dynamic-params.json` checksum `6ffbef92` / final checksum `5665a98e`, helper params `synthetic-imported-helper-modifyprojectile-dynamic-params.json` checksum `2d88a550` / final checksum `edb6d2d2`, helper bounds `synthetic-imported-helper-modifyprojectile-dynamic-bounds.json` checksum `f582153e` / final checksum `adc63407`, and owner bounds `synthetic-imported-modifyprojectile-dynamic-bounds.json` checksum `e2f7a077` / final checksum `aa78704a`; static owner/helper bounds gates `synthetic-imported-modifyprojectile.json` checksum `63a87da1` / `synthetic-imported-helper-modifyprojectile.json` checksum `09d3f7e4` remain required. Exact selection/tick order, helper/team namespace breadth, team/simul helper selection, and full Projectile parity remain partial.

Superseded registry addendum: `synthetic-imported-afterimagetime-dynamic.json` previously checksumed `16edc106` as required active imported dynamic `AfterImageTime value/time` expression fallback evidence without typed operation evidence. Current checksum `c5ef6fff` / final checksum `661a233d` now requires `sprite-effect:afterimagetime` after runtime expression resolution while retaining imported actor-frame/final ghost-trail duration telemetry. This is bounded dynamic AfterImageTime telemetry only; exact no-active-afterimage behavior, trail blending, palette math, sampling cadence, renderer parity, helper/redirect ownership, and full presentation parity remain partial.

Current registry addendum: `synthetic-imported-afterimage-dynamic.json` trace checksum `e7299ac5` / final checksum `b946d805` upgrades required active imported dynamic `AfterImage` expression evidence into typed `sprite-effect:afterimage` telemetry. The route proves owner-local vars can seed dynamic `time`, `length`, `timegap`, `framegap`, `paladd`, and `palmul` params, then route through `RuntimeSpriteEffectControllerWorld` / `RuntimeSpriteEffectWorld.applyAfterImage` into imported actor-frame/final ghost-trail telemetry. It requires `VarSet`, `AfterImage` controller evidence, `sprite-effect:afterimage`, and `hitdef`; `pnpm qa:trace` passed 523/523 artifacts, 492 required and 31 optional. This is bounded dynamic AfterImage telemetry only; exact trail blending, palette math, sampling cadence, renderer parity, helper/redirect ownership, and full presentation parity remain partial.

Superseded registry addendum: `synthetic-imported-trans-dynamic.json` previously checksumed `91a7baf9` as required active imported dynamic `Trans alpha` expression fallback evidence without typed operation evidence. Current checksum `4bffcd82` / final checksum `5beea0f0` now requires `sprite-effect:trans` after runtime expression resolution while retaining imported actor-frame/final `renderOpacity = 0.375` telemetry. This is bounded dynamic Trans opacity telemetry only; dynamic typed lowering for other sprite-effect params, exact add/sub alpha math, palette/remap interaction, draw-order parity, renderer parity, helper/redirect ownership, and full presentation parity remain partial.

Superseded registry addendum: `synthetic-imported-palfx-dynamic.json` previously checksumed `c56e955a` as required active imported dynamic `PalFX time/add/mul/color/invertall` expression fallback evidence without typed operation evidence. Current checksum `36cdca15` / final checksum `7a1a4525` now requires `sprite-effect:palfx` after runtime expression resolution while retaining imported actor-frame/final `paletteFx` telemetry: time `12`, add `[64,-16,255]`, mul `[224,144,256]`, color `200`, and invert `true`. This is bounded dynamic PalFX material telemetry only; `sinadd`, exact palette math/blend/remap order, ACT/SFF pixel parity beyond existing bounded handoff, renderer parity, helper/redirect ownership, and full presentation parity remain partial.

Previous registry addendum: `synthetic-imported-helper-projtime-same-id-last-contact.json` checksum `4e74aec3` adds required helper-local same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` guard-then-hit last-contact-kind evidence. The route proves a visual Helper can spawn two same-id root-owned Projectiles id `8918` with `parentId = p1-helper-0`, guard first, hit later, then route helper `1200 -> 1306 -> 1307` through `ProjHitTime(8918) >= 1`, `ProjHitTime(0) >= 1`, `ProjContactTime(8918) >= 1`, `ProjContactTime(0) >= 1`, `ProjGuardedTime(8918) < 0`, and `ProjGuardedTime(0) < 0`, with forbidden helper trap state `1308`, helper/projectile lifecycle rows, owner target-link id `8918`, guard package `S6,34` / `F7038` / `sparkxy = 35,-77`, hit package `S5,35` / `F7038` / `sparkxy = 36,-78`, and `pnpm qa:trace` 439/439 artifacts, 409 required and 30 optional. This pairs with `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b`; bounded helper-local owner-side same-ID two-contact timing evidence now covers both orderings. Exact tick/lifetime, helper custom-state breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, and full Projectile parity remain partial.

Previous registry addendum: `synthetic-imported-projectile-projtime-same-id-hit-then-guard.json` checksum `d49ee334` adds required player Projectile same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` hit-then-guard last-contact-kind evidence. The route proves two same-id Projectiles id `8916` can first hit and later guard, then route owner `200 -> 383 -> 384` through fixed-id plus ID `0` guarded/contact time reads while hit time reads are inactive, with forbidden trap state `385`, hit package `S5,30` / `F7036` / `sparkxy = 31,-73`, guard package `S6,31` / `F7036` / `sparkxy = 32,-74`, and `pnpm qa:trace` 437/437 artifacts, 407 required and 30 optional. This completes the bounded two-contact player same-ID order pair with previous `synthetic-imported-projectile-projtime-same-id-last-contact.json` checksum `fb4c2450`; exact tick/lifetime, helper custom-state breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, and full Projectile parity remain partial. Previous multi-id `synthetic-imported-projectile-projhittime-multi-id.json` checksum `5d897825`, `synthetic-imported-projectile-projcontacttime-multi-id.json` checksum `d9b3cecf`, and `synthetic-imported-projectile-projguardedtime-multi-id.json` checksum `e52d0d01` remain required counterparts.

Previous registry addendum: `synthetic-imported-projectile-projtime-same-id-last-contact.json` checksum `fb4c2450` adds required player Projectile same-ID guard-then-hit `Proj*Time` evidence and remains required.

Latest registry addendum: `synthetic-imported-projectile-projhittime-multi-id.json` checksum `5d897825`, `synthetic-imported-projectile-projcontacttime-multi-id.json` checksum `d9b3cecf`, and `synthetic-imported-projectile-projguardedtime-multi-id.json` checksum `e52d0d01` add required player Projectile fixed-id plus ID `0` `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` arbitration evidence. The routes prove wrong-id/non-contact Projectile ids `8909` / `8911` / `8913` do not route forbidden states `373` / `376` / `379`, while valid hit/contact/guard Projectile ids `8910` / `8912` / `8914` route owner `200 -> 371 -> 372`, `200 -> 374 -> 375`, and `200 -> 377 -> 378` through fixed-id and ID `0` forms, with two Projectile payloads, owner target-link ids `8910` / `8912` / `8914`, hit/guard sounds, and FightFX spark package evidence. `pnpm qa:trace` passes 435/435 artifacts, 405 required and 30 optional. This is bounded player Projectile two-id timing arbitration evidence only; exact `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` tick order/lifetime, same-ID selection priority, helper Projectile/custom-state persistence breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, visual/audio parity beyond bounded packages, and full Projectile parity remain partial. Previous multi-id `synthetic-imported-projectile-projhit-multi-id.json` checksum `ab0f3fb3`, `synthetic-imported-projectile-projguarded-multi-id.json` checksum `023921e3`, `synthetic-imported-projectile-projcontact-multi-id.json` checksum `e790ec3e`, any-id `synthetic-imported-projectile-projcontact-suffix-any.json` checksum `2fb80418`, any-id `synthetic-imported-projectile-projhit-suffix-any.json` checksum `35ffd57d`, any-id `synthetic-imported-projectile-projguarded-suffix-any.json` checksum `4000bc4f`, fixed-id `synthetic-imported-projectile-projhit-suffix.json` checksum `dd3db5ee`, fixed-id `synthetic-imported-projectile-projguarded-suffix.json` checksum `80bbe439`, `synthetic-imported-projectile-projcontact-suffix.json` checksum `c904ded7`, `synthetic-imported-projectile-projcontactpersist.json` checksum `8e678b1b`, and `synthetic-imported-helper-projcontactpersist.json` checksum `65639428` remain required counterparts.

Previous registry addendum: `synthetic-imported-projectile-projhit-multi-id.json` checksum `ab0f3fb3` and `synthetic-imported-projectile-projguarded-multi-id.json` checksum `023921e3` add required player Projectile fixed-id plus any-id `ProjHit` / `ProjGuarded` arbitration evidence and remain required.

Previous registry addendum: `synthetic-imported-helper-movereversedpersist.json` checksum `ef8ffdf5` adds required helper-owned reversed StateDef `movehitpersist = 1` evidence. The route proves helper-local MoveReversed can persist into helper state `1232`, while MoveContact / MoveHit / MoveGuarded / HitCount / UniqHitCount reset, and branch to helper state `1233` after P2 defender-side ReversalDef state `779`. That checkpoint passed 421/421 artifacts, 391 required and 30 optional, and remains required. This is bounded helper StateDef reversed Move* lifetime evidence only; helper Projectile/custom-state movehitpersist breadth, exact reversal priority/target-state semantics, exact combo accumulation, chain-hit eligibility arbitration, multi-hit/multi-target/team counting, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, and full Move* parity remain partial.

Previous registry addendum: `synthetic-imported-helper-moveguardedpersist.json` checksum `d5ce7897` adds required helper-owned guarded StateDef `movehitpersist = 1` evidence. The route proves helper-local MoveContact / MoveGuarded can persist into helper state `1230`, while MoveHit / HitCount / UniqHitCount reset, and branch to helper state `1231` with helper-owned guard contact presentation. That checkpoint passed 420/420 artifacts, 390 required and 30 optional, and remains required.

Previous registry addendum: `synthetic-imported-helper-movehitpersist.json` checksum `2354ef95` adds required helper-owned StateDef `movehitpersist = 1` evidence. The route proves helper-local MoveContact / MoveHit can persist into helper state `1228`, while HitCount / UniqHitCount reset, and branch to helper state `1229` with helper-owned contact presentation. That checkpoint passed 419/419 artifacts, 389 required and 30 optional, and remains required.

Previous registry addendum: `synthetic-imported-helper-hitcountpersist.json` checksum `fc9588d8` adds required helper-owned StateDef `hitcountpersist = 1` evidence. The route proves helper-local HitCount / UniqHitCount can persist into helper state `1226`, while MoveHit resets, and branch to helper state `1227` with helper-owned contact presentation. That checkpoint passed 418/418 artifacts, 388 required and 30 optional, and remains required.

Previous registry addendum: `synthetic-imported-helper-hitdefpersist.json` checksum `9d5c64c4` adds required helper-owned StateDef `hitdefpersist = 1` evidence. The route proves a helper-local active HitDef can persist into helper state `1224`, hit from that destination state with helper-owned contact presentation, and branch to helper state `1225`. That checkpoint passed 417/417 artifacts, 387 required and 30 optional, and remains required. This is bounded direct/helper StateDef HitDef-lifetime evidence only; multi-HitDef stacking, player/helper Projectile hitdefpersist breadth, custom-state hitdefpersist breadth, ReversalDef interactions, exact combo accumulation, chain-hit eligibility arbitration, multi-hit/multi-target/team counting, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, and full HitDef parity remain partial.

Previous registry addendum: `synthetic-imported-movehitpersist.json` checksum `5c1ef583` adds required direct StateDef `movehitpersist = 1` evidence. The route proves direct HitDef `MoveContact` / `MoveHit` can persist into state `344`, while `HitCount = 0 && UniqHitCount = 0` confirms hit counters reset before branching to state `345`. That checkpoint passed 415/415 artifacts, 385 required and 30 optional, and remains required.

Previous registry addendum: `synthetic-imported-hitcountpersist.json` checksum `6f032088` adds required direct StateDef `hitcountpersist = 1` evidence. The route proves direct HitDef `HitCount` / `UniqHitCount` can persist into state `342`, while `MoveHit = 0` confirms move-contact memory resets before branching to state `343`. That checkpoint passed 414/414 artifacts, 384 required and 30 optional, and remains required. This is bounded direct StateDef hit-count persistence evidence only; Projectile/helper hitcountpersist breadth, exact combo accumulation, chain-hit eligibility arbitration, multi-hit/multi-target/team counting, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, and full HitCount parity remain partial.

Previous registry addendum, upgraded by typed-audio cut: `synthetic-imported-projectile-hitcount.json` trace checksum `ee8f4e19` / final checksum `0fd4adf8` and `synthetic-imported-helper-projectile-hitcount.json` trace checksum `c8f5dc55` / final checksum `e1569fab` add required player-owned/helper-parented Projectile normal-hit attacker-side `HitCount` / `UniqHitCount` evidence. The player route feeds owner `MoveContact` / `MoveHit` / `HitCount`, branches P1 to state `341`, preserves target/lifecycle evidence, and requires typed `audio:playsnd`, `S5,44`, and FightFX `F7002` package telemetry. The helper route mirrors hit-count memory into the visual helper, exposes helper-local `HitCount` / `UniqHitCount`, branches helper `1257 -> 1258`, preserves owner/helper target links plus helper/projectile lifecycle payload evidence, and now requires typed `audio:playsnd`, helper-local `S5,43`, and FightFX `F7002` package telemetry. Current `pnpm qa:trace` passes 524/524 artifacts, 493 required and 31 optional, and the gates remain required. This is bounded Projectile/helper attacker-side hit-count evidence only; exact combo accumulation, Projectile/helper hitcountpersist breadth, chain-hit eligibility arbitration, multi-hit/multi-target/team counting, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, and full Projectile/HitCount parity remain partial.

Previous registry addendum: `synthetic-imported-projectile-gethitvar-hitcount.json` trace checksum `df2619f9` / final checksum `5469bc69` and upgraded `synthetic-imported-helper-projectile-gethitvar-hitcount.json` trace checksum `40ec4f4b` / final checksum `6f15ff30` add required player-owned/helper-parented Projectile normal-hit `GetHitVar(hitcount)` evidence. The routes preserve HitDef `numhits` separately from Projectile `projhits`, route P2 through defender-owned Common1-style `5000 -> 339/340`, and prove non-guarded hitcount reads. Both routes require typed `audio:playsnd` and FightFX `F7002`, with player `S5,47` and helper-local `S5,42`. This is bounded Projectile/helper normal-hit hitcount metadata evidence only; exact combo accumulation, chain-hit eligibility arbitration, multi-hit timing, exact hitpause lifetime, exact target lifetime/tick order, helper-owned custom states, custom-state inheritance, broader helper Projectile normal-hit sound breadth, and full Projectile/GetHitVar parity remain partial.

Previous registry addendum: `synthetic-imported-projectile-gethitvar-hitid-chainid.json` trace checksum `4356b5cb` / final checksum `4b270d45` and upgraded `synthetic-imported-helper-projectile-gethitvar-hitid-chainid.json` trace checksum `616e0b2c` / final checksum `0aebcc73` add required player-owned/helper-parented Projectile normal-hit `GetHitVar(hitid/chainid)` evidence. Both routes require typed `audio:playsnd` and FightFX `F7002`, with player `S5,46` and helper-local `S5,41`. That checkpoint remains required.

Previous registry addendum: `synthetic-imported-projectile-gethitvar-hit-metadata.json` trace checksum `8e5df79b` / final checksum `4d078c5d` and upgraded `synthetic-imported-helper-projectile-gethitvar-hit-metadata.json` trace checksum `28afbcea` / final checksum `c960b1cf` add required player-owned/helper-parented Projectile normal-hit `GetHitVar(damage/hittime/xvel/yvel)` evidence. Both routes require typed `audio:playsnd` and FightFX `F7002`, with player `S5,45` and helper-local `S5,40`. That checkpoint remains required.

Previous registry addendum: `synthetic-imported-projectile-guard-slide-stop.json` trace checksum `965c2d12` / final checksum `0973a73c` and `synthetic-imported-helper-projectile-guard-slide-stop.json` trace checksum `6c42a378` / final checksum `df8b7a42` add required player-owned Projectile and helper-parented Projectile guard slide-stop/control evidence. That checkpoint remains required.

Previous registry addendum: `synthetic-imported-guard-velocity-default.json` checksum `e6bd9b40`, `synthetic-imported-projectile-guard-velocity-default.json` checksum `b72451a4`, and `synthetic-imported-helper-projectile-guard-velocity-default.json` checksum `2067ba99` add required direct HitDef, player-owned Projectile, and helper-parented Projectile default `guard.velocity` derivation evidence. That checkpoint remains required.

Previous registry addendum: `synthetic-imported-guard-cornerpush-default.json` checksum `95293bc4`, `synthetic-imported-projectile-guard-cornerpush-default.json` checksum `58798e7a`, and `synthetic-imported-helper-projectile-guard-cornerpush-default.json` checksum `292b2015` add required direct HitDef, player-owned Projectile, and helper-parented Projectile default `guard.cornerpush.veloff` derivation evidence. That checkpoint remains required.

Previous registry addendum: `synthetic-imported-air-hit-cornerpush-default.json` checksum `73129a04`, `synthetic-imported-projectile-air-hit-cornerpush-default.json` checksum `9bfae4d6`, and `synthetic-imported-helper-projectile-air-hit-cornerpush-default.json` checksum `9c81047d` add required direct HitDef, player-owned Projectile, and helper-parented Projectile default `air.cornerpush.veloff` derivation evidence. That checkpoint passed 388/388 artifacts, 358 required and 30 optional, and remains required.

Previous registry addendum: `synthetic-imported-air-guard-cornerpush-default.json` checksum `c32781ad`, `synthetic-imported-projectile-air-guard-cornerpush-default.json` checksum `90f5e385`, and `synthetic-imported-helper-projectile-air-guard-cornerpush-default.json` checksum `0271a2b9` add required direct HitDef, player-owned Projectile, and helper-parented Projectile default `airguard.cornerpush.veloff` evidence. That checkpoint remains required.

Previous registry addendum: `synthetic-imported-air-guard-cornerpush.json` checksum `9fdb8a81`, `synthetic-imported-projectile-air-guard-cornerpush.json` checksum `15f26082`, and `synthetic-imported-helper-projectile-air-guard-cornerpush.json` checksum `35d7148b` add required direct HitDef, player-owned Projectile, and helper-parented Projectile explicit `airguard.cornerpush.veloff` evidence. That checkpoint remains required.

Previous registry addendum: `synthetic-imported-air-guard-velocity-default.json` checksum `b1710269`, `synthetic-imported-projectile-air-guard-velocity-default.json` checksum `bd1a774e`, and `synthetic-imported-helper-projectile-air-guard-velocity-default.json` checksum `3351e770` add required direct HitDef, player-owned Projectile, and helper-parented Projectile default `airguard.velocity` evidence. The routes prove omitted `airguard.velocity` derives from authored `air.velocity = -6,-8` as X `* 1.5` and Y `/ 2`, applies in Common1-style state `155`, and is observed through actor-frame velocity evidence; helper Projectile also preserves owner/helper target-link and lifecycle-payload evidence. That `pnpm qa:trace` pass was 379/379 artifacts, 349 required and 30 optional, and remains required.

Previous registry addendum: `synthetic-imported-air-guard-velocity.json` checksum `5ebc1e7b`, `synthetic-imported-projectile-air-guard-velocity.json` checksum `0094c369`, and `synthetic-imported-helper-projectile-air-guard-velocity.json` checksum `b547dfb3` add required direct HitDef, player-owned Projectile, and helper-parented Projectile explicit `airguard.velocity` evidence. That `pnpm qa:trace` pass was 376/376 artifacts, 346 required and 30 optional, and remains required.

Previous registry addendum: `synthetic-imported-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `35fa8224`, `synthetic-imported-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `1fd6c321`, and `synthetic-imported-helper-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `7efa40bb` add required direct/player/helper HitOverride guardflag-filter-plus-force-flag evidence. The routes prove incoming `guardflag = H` skips guardflag-mismatched slots `1 -> 776` and `2 -> 778`, selects slot `5 -> 779`, applies `forceair` / `forceguard`, keeps P2 in state/action `0` through `keepstate`, and preserves target-link plus lifecycle-payload evidence where applicable. That `pnpm qa:trace` pass was 373/373 artifacts, 343 required and 30 optional. This is bounded guardflag-filter-plus-force-flag priority evidence only; exact guard timing/guarded get-hit variables, forceguard chip/damage semantics, final-frame forced aerial persistence, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, and full HitOverride parity remain partial.

Previous registry addendum: `synthetic-imported-hitoverride-missonoverride-default-guardflag-filter.json` checksum `05725ecb`, `synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter.json` checksum `c1402d31`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter.json` checksum `889d77c1` add required direct/player/helper default `missonoverride = -1` custom-state HitOverride guardflag-filter evidence. The direct route proves omitted `missonoverride` rejects a direct custom-state `HitDef` before target memory, owner-backed state `888`, default get-hit, or guard states. The player Projectile and helper Projectile routes prove omitted `missonoverride` keeps Projectile custom-state override eligible for matching HitOverride slots, skips guardflag-mismatched slots `1 -> 776` and `2 -> 778`, selects slot `5 -> 779`, suppresses Projectile custom state `889`, and preserves owner/helper target-link plus lifecycle-payload evidence on the helper route. That `pnpm qa:trace` pass was 370/370 artifacts, 340 required and 30 optional. This is bounded default custom-state guardflag-filter evidence only; exact guard timing/guarded contact semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, and full HitOverride/custom-state parity remain partial.

Previous registry addendum: `synthetic-imported-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `058b335f`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `af29f125`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `9edbf3d0` now add required direct/player/helper explicit `missonoverride = 0` custom-state HitOverride guardflag-filter evidence: direct `HitDef`, player Projectile id `77`, and helper Projectile id `8881` all skip the `guardflag.not = HA` and `guardflag = A` slots, select slot `5 -> 779`, suppress custom state entry, and forbid default get-hit/guard states; helper route also records owner/helper target links and suppresses helper branch `1291`. Previous `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority.json` checksum `9a5a149f`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority.json` checksum `96b6b7de`, `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `fb964bfb`, `synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `4ce42cf3`, `synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `20e40425`, `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `e23a33af`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `15bc955b`, `synthetic-imported-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `4d9043a5`, `synthetic-imported-helper-projectile-hitoverride-forceair-forceguard-keepstate.json` checksum `84dc3969`, `synthetic-imported-projectile-hitoverride-forceair-forceguard-keepstate.json` checksum `3806a769`, `synthetic-imported-hitoverride-forceair-forceguard-keepstate.json` checksum `19787fb2`, `synthetic-imported-helper-projectile-hitoverride-guardflag-filter.json` checksum `41a87267`, `synthetic-imported-projectile-hitoverride-guardflag-filter.json` checksum `a51e82ec`, `synthetic-imported-hitoverride-guardflag-filter.json` checksum `b88a2da3`, `synthetic-imported-hitoverride-missonoverride-zero-slot-priority.json` checksum `92fefd6a`, `synthetic-imported-helper-projectile-hitoverride-slot-priority.json` checksum `1d058518`, `synthetic-imported-projectile-hitoverride-slot-priority.json` checksum `378d9ce8`, and `synthetic-imported-hitoverride-slot-priority.json` checksum `8de62354` remain required for adjacent direct/helper/player custom-state, forceair, guardflag, and slot-priority evidence. Previous Projectile, direct-HitDef, guard, and GetHitVar gates remain required as listed below. This is bounded explicit custom-state guardflag-filter evidence only; exact guard timing/guarded contact semantics, score movement, and full HitOverride/custom-state parity remain partial.

Previous registry addendum: `synthetic-imported-p2stateno-guard-ignored.json` checksum `76d1becd` added required direct-HitDef successful-guard `p2stateno` behavior evidence: `p2stateno = 888` / `p2getp1state = 1` is present on the guarded HitDef, target id `77` is recorded, P2 stays in defender-owned guard states `150 -> 151 -> 130 -> 20`, and state `888` is forbidden. This is bounded direct-HitDef successful-guard `p2stateno` evidence only; exact guard KO/no-KO round flow, helper/projectile `p2stateno` behavior, helper/projectile custom-state inheritance, score movement, and full guard parity remain partial.

HitDef/Projectile default `missonoverride` addendum: current required evidence includes `synthetic-imported-hitoverride-missonoverride-default-guardflag-filter.json`, `synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter.json`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter.json` for bounded default custom-state guardflag filtering across direct `HitDef`, player-owned Projectile, and helper-parented Projectile routes.

Projectile localcoord default bounds addendum: required `synthetic-imported-projectile-localcoord-default-bounds-terminal.json` checksum `af7ee80e` / final checksum `3fcb4661` and `synthetic-imported-helper-projectile-localcoord-default-bounds-terminal.json` checksum `46b0164c` / final checksum `b1531c44` add bounded 640x480 character `localcoord` default-bound evidence beside the 240p default, explicit bounds, timeout, fallback, cancel, and guard terminal gates. Parsed `[Info] localcoord` now reaches imported fighter definitions, player-owned Projectile spawns, and helper-parented/root-owned Projectile spawns; omitted defaults derive `projedgebound = 80`, `projstagebound = 80`, and `projheightbound = -480,2`, while explicit bounds remain authored. Official Elecbyte docs define character `localcoord`, width-ratio coordinate translation, 480p omitted Projectile defaults, and Helper-created Projectiles as root-owned. This is bounded localcoord-derived default-bound evidence only; exact GameWidth/GameHeight negotiation, exact camera/screen/stage split, full localcoord scaling across all Projectile params/controllers, exact terminal timing, sprite/layer/palette parity, team/simul breadth, score movement, and full Projectile bounds parity remain blocked.

Projectile default bounds terminal addendum: required `synthetic-imported-projectile-default-bounds-terminal.json` checksum `52879717` / final checksum `df3e2061` adds bounded player-owned Projectile omitted-bound evidence beside the explicit bounds, timeout, fallback, cancel, and guard terminal gates. `ProjectileSystem` derives omitted `projedgebound = 40`, `projstagebound = 40`, and `projheightbound = -240,1` for 320x240-localcoord-compatible routes; the trace uses authored `projremanim = 927`, omitted bound params, and visible bounds terminal playback. Official Elecbyte docs define those 240p defaults. This is bounded player-owned 240p default-bound evidence only; exact camera/screen/stage split, full localcoord scaling beyond the bounded 640x480 default-bound gates, exact terminal timing, sprite/layer/palette parity, team/simul breadth, score movement, and full Projectile bounds parity remain blocked.

Projectile helper default bounds terminal addendum: required `synthetic-imported-helper-projectile-default-bounds-terminal.json` checksum `e85d7bbf` / final checksum `bea653fa` adds bounded helper-parented/root-owned Projectile omitted-bound evidence beside the player default, explicit bounds, timeout, fallback, cancel, and guard terminal gates. The trace spawns helper `p1-helper-0`, then root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, authored `projremanim = 1134`, omitted bound params, and visible bounds terminal playback. Official Elecbyte docs define Helper-created Projectiles as root-owned and define those 240p defaults. This is bounded helper Projectile 240p default-bound evidence only; exact camera/screen/stage split, full localcoord scaling beyond the bounded 640x480 default-bound gates, exact terminal timing, sprite/layer/palette parity, team/simul breadth, score movement, and full helper Projectile bounds parity remain blocked.
Projectile/helper projstagebound terminal addendum: required `synthetic-imported-helper-projectile-stagebound-terminal.json` checksum `488ce550` / final checksum `dd0d956d` remains required as bounded helper-parented/root-owned Projectile explicit `projstagebound = 24` terminal playback evidence.

Projectile/helper projedgebound terminal addendum: required `synthetic-imported-helper-projectile-edgebound-terminal.json` checksum `8482f4f3` / final checksum `1ab7d718` remains required as bounded helper-parented/root-owned Projectile explicit `projedgebound = 24` terminal playback evidence.

Projectile/helper projheightbound terminal addendum: required `synthetic-imported-helper-projectile-heightbound-terminal.json` checksum `debb08b1` / final checksum `0821ec70` adds bounded helper-parented/root-owned Projectile explicit `projheightbound = -120,60` terminal playback evidence beside the player-owned height-bound, edge-bound, stage-bound, generic bounds, timeout, fallback, cancel, and guard terminal gates. It proves helper `p1-helper-0` can spawn root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, author `projremanim = 1125`, start at offset `80,-45`, travel at velocity `0,-120`, set `projheightbound = -120,60`, avoid contact, exit vertical y bounds before long `projremovetime = 90`, and play visible terminal anim `1125`; the trace records typed Helper and Projectile operation evidence, active `x` command evidence, helper/projectile spawn/remove lifecycle evidence, visible helper and projectile actor-frame evidence, and terminal payload evidence with `heightBound = { low: -120, high: 60 }`, `hasHit = false`, `hitsRemaining = 1`, `removalReason = bounds`, `terminalReason = bounds`, and terminal duration `2`. Official Elecbyte docs define `projheightbound` as the least and greatest y values a projectile may reach before automatic removal, define `projremanim` as timeout/bounds removal animation, and state Helper-created Projectiles become root-owned. This is bounded helper Projectile explicit `projheightbound` evidence only; exact default `projheightbound` values, exact coordinate/camera/height scaling parity, exact stage-vs-screen/edge split, exact terminal timing, exact sprite/layer/palette parity, team/simul breadth, score movement, and full helper Projectile bounds parity remain blocked.

Projectile/projheightbound terminal addendum: required `synthetic-imported-projectile-heightbound-terminal.json` checksum `1164a584` / final checksum `363aced8` adds bounded player-owned Projectile explicit `projheightbound = -120,60` terminal playback evidence beside the edge-bound, stage-bound, generic bounds, timeout, fallback, cancel, and guard terminal gates. It proves imported Projectile id `77` can author `projremanim = 926`, start at offset `80,-45`, travel at velocity `0,-120`, set `projheightbound = -120,60`, avoid contact, exit vertical y bounds before long `projremovetime = 90`, and play visible terminal anim `926`; the trace records typed Projectile operation evidence, active `x` command evidence, Projectile spawn/remove lifecycle evidence, visible projectile actor-frame evidence for terminal anim `926`, and terminal payload evidence with `heightBound = { low: -120, high: 60 }`, `hasHit = false`, `hitsRemaining = 1`, `removalReason = bounds`, `terminalReason = bounds`, and terminal duration `2`. Official Elecbyte docs define `projheightbound` as the least and greatest y values a projectile may reach before automatic removal and define `projremanim` as timeout/bounds removal animation. This is bounded explicit `projheightbound` evidence only; exact default `projheightbound` values, exact coordinate/camera/height scaling parity, exact stage-vs-screen/edge split, exact terminal timing, exact sprite/layer/palette parity, team/simul breadth, score movement, and full Projectile bounds parity remain blocked.

Projectile/projedgebound terminal addendum: required `synthetic-imported-projectile-edgebound-terminal.json` checksum `e4361063` / final checksum `6dcae566` adds bounded player-owned Projectile explicit `projedgebound = 24` terminal playback evidence beside the stage-bound, generic bounds, timeout, fallback, cancel, and guard terminal gates. It proves imported Projectile id `77` can author `projremanim = 925`, start at offset `80,-45`, travel at velocity `160,0`, set `projedgebound = 24`, avoid contact, exit the current horizontal screen-edge proxy before long `projremovetime = 90`, and play visible terminal anim `925`; the trace records typed Projectile operation evidence, active `x` command evidence, Projectile spawn/remove lifecycle evidence, visible projectile actor-frame evidence for terminal anim `925`, and terminal payload evidence with `edgeBound = 24`, `hasHit = false`, `hitsRemaining = 1`, `removalReason = bounds`, `terminalReason = bounds`, and terminal duration `2`. Official Elecbyte docs define `projedgebound` as the distance off screen edge before automatic projectile removal and define `projremanim` as timeout/bounds removal animation. This is bounded explicit `projedgebound` evidence only; exact default `projedgebound` values, exact camera/screen-edge geometry, exact `projstagebound` vs `projedgebound` split, `projheightbound` parity, exact terminal timing, helper-owned bounds-removal parity, exact sprite/layer/palette parity, team/simul breadth, score movement, and full Projectile bounds parity remain blocked.

Projectile/projstagebound terminal addendum: required `synthetic-imported-projectile-stagebound-terminal.json` checksum `fe3df8e7` / final checksum `b467573f` remains required as bounded player-owned Projectile explicit `projstagebound = 24` terminal playback evidence.

Projectile/bounds remove terminal addendum: required `synthetic-imported-projectile-bounds-remove-terminal.json` checksum `1d7479d3` / final checksum `39b81931` remains required as bounded player-owned Projectile bounds-removal terminal playback evidence.

Projectile/helper remove fallback terminal addendum: required `synthetic-imported-helper-projectile-remove-hit-fallback-terminal.json` checksum `0ed9e229` / final checksum `9cd6d27b` adds bounded helper-parented/root-owned Projectile timeout-removal fallback playback evidence beside the helper/player cancel fallback, player remove fallback, explicit remove, and guard terminal gates. It proves helper `p1-helper-0` can spawn root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, author `projhitanim = 1122`, omit `projremanim`, stay static with no contact until `projremovetime = 24`, and fall back to `projhitanim` for visible timeout terminal playback; the trace records typed helper/projectile operation evidence, helper state/action evidence `1320` / anim `1120`, Projectile spawn/remove lifecycle evidence, visible projectile actor-frame evidence for terminal anim `1122`, and terminal payload evidence with `hasHit = false`, `hitsRemaining = 1`, `hitAnimNo = 1122`, `removalReason = timeout`, `terminalReason = timeout`, and terminal duration `2`. Official Elecbyte docs define helper-created Projectiles as root-owned, `projremanim` as timeout/bounds removal animation, omitted `projremanim` as falling back to `projhitanim`, and `projremovetime` timeout removal. This is bounded helper-parented/root-owned Projectile timeout fallback evidence only; exact terminal timing, bounds-removal parity, broader fallback breadth, exact sprite/layer/palette parity, team/simul breadth, score movement, and full helper Projectile terminal parity remain blocked.

Projectile/helper cancel fallback terminal addendum: required `synthetic-imported-helper-projectile-cancel-remove-fallback-terminal.json` checksum `cf33c924` / final checksum `bd3a1279` adds bounded helper-parented/root-owned Projectile cancel-removal fallback playback evidence beside the player cancel fallback, explicit cancel, remove fallback, remove terminal, and guard terminal gates. It proves helper `p1-helper-0` can spawn root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, author `projremanim = 1028`, omit `projcancelanim`, lose a Projectile priority clash against P2 priority `3 > 1`, and fall back to `projremanim` for visible cancel terminal playback; the trace records typed helper/projectile operation evidence, helper branch evidence `1272 -> 1273`, P1 helper Projectile remove lifecycle evidence, P2 active Projectile evidence, visible projectile actor-frame evidence for terminal anim `1028`, and terminal payload evidence with `hasHit = true`, `removeAnimNo = 1028`, `removalReason = cancel`, `terminalReason = cancel`, and terminal duration `2`. Official Elecbyte docs define helper-created Projectiles as root-owned, omitted `projcancelanim` as falling back to `projremanim`, and `projpriority` cancellation behavior. This is bounded helper-parented/root-owned Projectile cancel fallback evidence only; exact cancel tick-order/lifetime, exact terminal timing, bounds-removal parity, broader fallback breadth, exact sprite/layer/palette parity, team/simul breadth, score movement, and full helper Projectile terminal parity remain blocked.

Projectile/cancel fallback terminal addendum: required `synthetic-imported-projectile-cancel-remove-fallback-terminal.json` checksum `4966ed30` / final checksum `98170d08` adds bounded player-owned Projectile cancel-removal fallback playback evidence beside the explicit cancel, remove fallback, remove terminal, and guard terminal gates. It proves P2 can author `projremanim = 921`, omit `projcancelanim`, lose a Projectile priority clash against P1 priority `3 > 1`, and fall back to `projremanim` for visible cancel terminal playback; the trace records typed projectile operation evidence, P2 Projectile remove lifecycle evidence, P1 active Projectile evidence, visible projectile actor-frame evidence for terminal anim `921`, and terminal payload evidence with `hasHit = true`, `removeAnimNo = 921`, `removalReason = cancel`, `terminalReason = cancel`, and terminal duration `2`. Official Elecbyte docs define omitted `projcancelanim` as falling back to `projremanim` and `projpriority` cancellation behavior. This is bounded player-owned Projectile cancel fallback evidence only; exact cancel tick-order/lifetime, exact terminal timing, bounds-removal parity, broader fallback breadth, exact sprite/layer/palette parity, team/simul breadth, score movement, and full Projectile terminal parity remain blocked.

Projectile/remove fallback terminal addendum: required `synthetic-imported-projectile-remove-hit-fallback-terminal.json` checksum `3bbdfbfc` / final checksum `76ca3f77` adds bounded player-owned Projectile timeout-removal fallback playback evidence beside the explicit remove, guard terminal, and cancel terminal gates. It proves imported Projectile id `77` can omit `projremanim` and fall back to authored `projhitanim = 920`; a no-contact timeout records typed projectile operation evidence, Projectile spawn/remove lifecycle evidence, visible projectile actor-frame evidence for terminal anim `920`, and terminal payload evidence with `hasHit = false`, `hitsRemaining = 1`, `hitAnimNo = 920`, `removalReason = timeout`, `terminalReason = timeout`, `removeTime = 24`, and terminal duration `2`. Official Elecbyte docs define omitted `projremanim` as falling back to `projhitanim` and `projremovetime` as timeout removal. This is bounded player-owned Projectile timeout fallback evidence only; exact terminal timing, bounds-removal parity, exact sprite/layer/palette parity, helper-owned remove fallback parity, team/simul breadth, score movement, and full Projectile terminal parity remain blocked.

Projectile/remove terminal addendum: required `synthetic-imported-projectile-remove-terminal.json` checksum `8a65629c` / final checksum `7ba3479b` adds bounded player-owned Projectile timeout-removal terminal playback evidence beside the guard terminal and cancel terminal gates. It proves imported Projectile id `77` can use authored `projremanim = 919`; a no-contact timeout records typed projectile operation evidence, Projectile spawn/remove lifecycle evidence, visible projectile actor-frame evidence for terminal anim `919`, and terminal payload evidence with `hasHit = false`, `hitsRemaining = 1`, `removalReason = timeout`, `terminalReason = timeout`, `removeTime = 24`, and terminal duration `2`. Official Elecbyte docs define `projremanim` as the removal animation for expired time or bounds and `projremovetime` as timeout removal. This is bounded player-owned Projectile timeout terminal evidence only; exact terminal timing, bounds-removal parity, broader fallback breadth, exact sprite/layer/palette parity, helper-owned remove terminal parity, team/simul breadth, score movement, and full Projectile terminal parity remain blocked.

Helper Projectile/guard terminal addendum: required `synthetic-imported-helper-projectile-guard-terminal.json` checksum `c6937f42` / final checksum `e0835e33` adds bounded helper-parented/root-owned Projectile guarded-contact terminal playback plus typed guard sound evidence beside the player-owned Projectile terminal, player-owned Projectile no-KO/KO, helper Projectile no-KO/KO, and direct `HitDef` guard-chip gates. It proves helper `p1-helper-0` can spawn root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, id `8922`, and authored `projhitanim = 1109`; a guarded contact records typed helper/projectile operation evidence, typed `audio:playsnd` for `guardsound = S6,0`, helper state evidence `1314 -> 1315`, Projectile spawn/remove lifecycle evidence, owner and helper target-link evidence, guard event/reason evidence, event text `hit removal anim 1109`, visible projectile actor-frame evidence for terminal anim `1109`, and terminal payload evidence with `hasHit = true`, `removalReason = hit`, `terminalReason = hit`, and terminal duration at least `2`. Official Elecbyte docs define Projectile `projhitanim`, `projremanim`, `projcancelanim`, and `projremove` removal-animation behavior, and helper-created Projectiles as root-owned. This is bounded helper-parented/root-owned Projectile guard terminal evidence only; exact terminal timing, exact sprite/layer/palette parity, helper-owned custom-state terminal parity, helper Projectile normal-hit sound telemetry, team/simul breadth, score movement, and full helper Projectile guard round-flow parity remain blocked.

Projectile/guard terminal addendum: required `synthetic-imported-projectile-guard-terminal.json` checksum `26f1e7f9` / final checksum `f9df24d0` remains bounded player-owned Projectile guarded-contact terminal playback evidence. It proves imported Projectile id `77` can use authored `projhitanim = 912`; a guarded contact records target-link evidence, event text `hit removal anim 912`, visible projectile actor-frame evidence for terminal anim `912`, and terminal payload evidence with `terminalReason = hit`.

Projectile/guard.kill no-KO addendum: required `synthetic-imported-projectile-guard-kill.json` checksum `905eb8e3` / final checksum `c6cc7787` adds bounded player-owned Projectile `guard.kill = 0` nonlethal guard-chip clamp evidence beside the player-owned Projectile KO, helper Projectile no-KO/KO, direct `HitDef` KO, and direct nonlethal `guard.kill = 0` clamp gates. It proves imported Projectile id `77` can use `damage = 31,2000` and explicit `guard.kill = 0`; a guarded P2 clamps to life `1` without round KO evidence while Projectile target-link evidence remains present. Official Elecbyte docs define Projectile as taking HitDef parameters, including `guard.kill`, and `guard.kill = 0` as the nonlethal chip clamp. This is bounded player-owned Projectile guard-chip no-KO only; exact KO slowdown/lifebar/guard-finish timing, exact no-KO guard recovery timing, team/simul breadth, exact effect terminal/presentation timing, score movement, and full Projectile guard round-flow parity remain blocked.

Helper Projectile/guard.kill no-KO addendum: required `synthetic-imported-helper-projectile-guard-kill.json` checksum `33930a00` / final checksum `8412e638` adds bounded helper-parented/root-owned Projectile `guard.kill = 0` nonlethal guard-chip clamp plus typed guard sound evidence beside the helper Projectile KO, player-owned Projectile KO, direct `HitDef` KO, and direct nonlethal `guard.kill = 0` clamp gates. It proves helper `p1-helper-0` can spawn root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, `damage = 18,2000`, and explicit `guard.kill = 0`; a guarded P2 clamps to life `1` without round KO evidence while owner/helper target links and typed `audio:playsnd` for `guardsound = S6,0` remain present. Official Elecbyte docs define Projectile as taking HitDef parameters, helper-created Projectiles as root-owned, and `guard.kill = 0` as the nonlethal chip clamp. This is bounded helper-parented/root-owned Projectile guard-chip no-KO only; exact KO slowdown/lifebar/guard-finish timing, exact no-KO guard recovery timing, helper-owned custom states, team/simul breadth, exact effect terminal/presentation timing, score movement, and full helper Projectile guard round-flow parity remain blocked.

Helper Projectile/guard KO addendum: required `synthetic-imported-helper-projectile-guard-ko.json` checksum `05dbcded` / final checksum `98b8bf17` adds bounded helper-parented/root-owned Projectile default lethal guard-chip KO plus typed guard sound evidence beside the player-owned Projectile, direct `HitDef` KO, and nonlethal `guard.kill = 0` clamp gates. It proves helper `p1-helper-0` can spawn root-owned Projectile `p1-projectile-0` with parent `p1-helper-0`, `damage = 18,2000`, and default `guard.kill`; a guarded P2 can reach life `0` and emit round KO winner/message evidence while owner/helper target links and typed `audio:playsnd` for `guardsound = S6,0` remain present. Official Elecbyte docs define Projectile as taking HitDef parameters, helper-created Projectiles as root-owned, and `guard.kill = 0` as the nonlethal chip clamp. This is bounded helper-parented/root-owned Projectile guard-chip KO only; exact KO slowdown, lifebar behavior, guard-finish timing, helper-owned custom states, team/simul breadth, exact effect terminal/presentation timing, score movement, and full helper Projectile guard round-flow parity remain blocked.

Projectile/guard KO addendum: required `synthetic-imported-projectile-guard-ko.json` checksum `2285474a` / final checksum `c968c723` remains the bounded player-owned Projectile default lethal guard-chip KO evidence beside the direct `HitDef` KO and nonlethal `guard.kill = 0` clamp gates. It proves imported Projectile `damage = 31,2000` with default `guard.kill` can reduce P2 to life `0` and emit round KO winner/message evidence. Official Elecbyte docs define Projectile as taking HitDef parameters, including `guard.kill`. This is bounded player-owned Projectile guard-chip KO only; exact KO slowdown, lifebar behavior, guard-finish timing, team/simul breadth, exact effect terminal/presentation timing, score movement, and full projectile guard round-flow parity remain blocked.

HitDef/guard KO addendum: required `synthetic-imported-hitdef-guard-ko.json` checksum `b7db75f4` / final checksum `0f9afa50` adds bounded default lethal direct guard-chip KO evidence beside the existing nonlethal `synthetic-imported-hitdef-guard-kill.json` clamp. It proves guarded `guard.damage = 2000` with default `guard.kill` can reduce P2 to life `0` and emit round KO winner/message evidence. This is bounded direct-HitDef guard-chip KO only; exact KO slowdown, lifebar behavior, guard-finish timing, helper/projectile guard KO, team/simul breadth, score movement, and full guard round-flow parity remain blocked.

HitDef/Projectile table addendum: the evidence now includes required `synthetic-imported-air-hit-cornerpush-default.json` checksum `73129a04`, `synthetic-imported-projectile-air-hit-cornerpush-default.json` checksum `9bfae4d6`, and `synthetic-imported-helper-projectile-air-hit-cornerpush-default.json` checksum `9c81047d` for bounded default `air.cornerpush.veloff` derivation on direct HitDef, player-owned Projectile, and helper-parented Projectile air-hit routes; required default/explicit `airguard.cornerpush.veloff` and default/explicit `airguard.velocity` gates remain active. The evidence also includes required `synthetic-imported-projectile-hitoverride-missonoverride-zero.json` checksum `5c12f3cc` for bounded player-owned Projectile `missonoverride = 0` HitOverride redirect behavior, required `synthetic-imported-helper-projectile-hitoverride-missonoverride-one.json` checksum `a99979bb` for bounded helper-parented Projectile `missonoverride = 1` HitOverride miss behavior, required `synthetic-imported-projectile-hitoverride-missonoverride-one.json` checksum `2dc86467` for bounded player-owned Projectile `missonoverride = 1` HitOverride miss behavior, required `synthetic-imported-helper-projectile-hitoverride-p2stateno.json` checksum `ce4c1d9a` for bounded helper-parented Projectile `p2stateno` HitOverride redirect behavior, required `synthetic-imported-projectile-hitoverride-p2stateno.json` checksum `2ec0725a` for bounded player-owned Projectile `p2stateno` HitOverride redirect behavior, required `synthetic-imported-custom-state-gethitvar-guard-kill.json` checksum `c889a534`, `synthetic-imported-helper-projectile-gethitvar-guard-kill.json` checksum `7f9aa699`, `synthetic-imported-projectile-gethitvar-guard-kill.json` checksum `3feae5a7`, `synthetic-imported-gethitvar-air-guard-kill.json` checksum `4382207e`, `synthetic-imported-gethitvar-crouch-guard-kill.json` checksum `2976fb8c`, and `synthetic-imported-gethitvar-guard-kill.json` checksum `abb4e468` for bounded guard-hit `GetHitVar(kill)` routes, required `synthetic-imported-custom-state-gethitvar-guard-timing.json` checksum `ba77beec` for one bounded guarded timing metadata route through owner-local `TargetState`, required `synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid.json` checksum `250f77c2` for one bounded owner-backed custom-state `GetHitVar(hitcount/hitid/chainid)` route, and required `synthetic-imported-gethitvar-fallcount.json` checksum `c391d938` for one bounded post-impact `GetHitVar(fallcount)` route. The `HitDef` row's snap evidence also includes required `synthetic-imported-custom-state-gethitvar-snap.json` checksum `ce4680b9` beside `synthetic-imported-gethitvar-snap.json` checksum `312a53fc`; support covers bounded direct-HitDef custom-state, helper-parented and player-owned Projectile, plus direct stand/crouch/air guard kill metadata, custom-state guarded timing, numhits/id/chainID, fallcount, snap metadata, player-owned Projectile `missonoverride = 0`, helper-parented and player-owned Projectile `missonoverride = 1`, helper-parented plus player-owned Projectile `p2stateno` HitOverride routes, and bounded air-hit/air-guard corner-push routes only.

| Controller Family | Current Level | Evidence | Notes |
| --- | --- | --- | --- |
| `ChangeState`, `SelfState` | executed-partial | unit tests, imported traces, `RuntimeActiveStateDispatchSystem` tests, `RuntimeStateMetadataSystem` tests, `synthetic-imported-default-gethit-progression.json` checksum `ef2a67f8`, `synthetic-imported-default-crouch-gethit-progression.json` checksum `fd986a9e`, optional `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae`, `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641`, `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c`, `synthetic-imported-air-guard-landing.json` checksum `d6986d7f`, `synthetic-imported-default-fall-gethit.json` checksum `6af73a91`, `synthetic-imported-default-fall-recovery.json` checksum `d83797d9`, `synthetic-imported-default-fall-official-ground-recovery.json` checksum `74b72495`, optional `kfm-official-default-fall-gethit.json` checksum `517bc059`, optional `kfm-official-default-fall-recovery.json` checksum `2743c2c9`, `synthetic-imported-custom-state.json`, `synthetic-imported-target-owned-custom-state.json` checksum `410fb8c0` | Supports selected trigger/state routing, owner transitions, bounded active-state dispatch through `RuntimeActiveStateDispatchWorld`, bounded previous-state metadata writes through `RuntimeStateMetadataWorld` for `PrevStateNo`, `PrevAnim`, `PrevStateType`, and `PrevMoveType`, bounded Common1-style stand get-hit progression order through `5000` `ChangeState` before `5001` `ChangeState`, bounded Common1-style crouch get-hit progression order through `5010` `ChangeState` before `5011` `ChangeState` including optional real KFM held-crouch prep `11` plus `5010 -> 5011 -> 0` fixture evidence and KFM-specific `HitVelSet` / `VelMul` / `VelSet` / `DefenceMulSet` typed-operation order, bounded stand guard slide-stop routing through `150 -> 151 -> 130` with `151` final `ChangeState` after `HitVelSet` / `VelSet` / `CtrlSet`, bounded stand guard-hold walk-control routing through `150 -> 151 -> 130 -> 20`, bounded synthetic air guard landing walk-control routing through `154 -> 155 -> 52 -> 20` with `155` final `ChangeState` after `HitVelSet` / `VelAdd` / `CtrlSet` / `VelSet` / `PosSet`, optional KFM air guard route through `154 -> 155 -> 52 -> 20`, bounded synthetic fall-entry order through `5000 -> 5030 -> 5050`, bounded synthetic full-chain recovery routing through `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120` where `5110` `ChangeState` `Get Up` precedes `5120` recovery settlement and final stand/control with required down-recovery countdown-range and first-to-last-drop evidence, bounded official-style synthetic ground-recovery routing through `5050 -> 5200 -> 5201 -> 52 -> 0` with controller/operation order evidence, optional official KFM `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110` and `5110 -> 5120 -> 0` actor-frame/controller-order evidence where the private fixture exists, a bounded attacker-owned custom-state chain returning through `SelfState`, and a bounded target-owned `p2getp1state = 0` route returning through the target's own `SelfState`; state-entry tick-order, redirect/helper/team previous-state ownership, exact guard/recovery thresholds/velocity/bounce math, exact air guard physics/timing, and full parity still partial. |
| `ChangeAnim`, `ChangeAnim2` | executed-partial | `synthetic-imported-animation.json` checksum `ee651cbc`, `synthetic-imported-changeanim2-elem.json` checksum `b0b46d33`, focused `AnimationControllerSystem` tests, focused `RuntimeActiveStateDispatchSystem` tests, runtime tests/traces | `ChangeAnim` can retarget the active AIR action and partial `ChangeAnim2` can retarget to the state-owner action table with actor-frame/final-animation evidence. The active-state dispatch route is owned by `RuntimeActiveStateDispatchWorld`, including dynamic action/elem/elemtime resolution handoff, state-owner action-owner selection, controller telemetry, and `elem` / `elemtime` handoff. The active-state `ChangeAnim2` path now has a required known-owner AIR trace for `elem = 3` / `elemtime = 1` against authored frame durations `[4,5,6]`, ending at `frameIndex = 2` / `animTime = 20`. `RuntimeAnimationControllerWorld` owns the basic `StateControllerExecutor` mutation path, including raw expression fallback, self/state-owner source marking, reset, and bounded `elem` / `elemtime` seeding when an AIR action resolver is supplied. Missing-action fallback, broader active-state edge cases, redirects, helper/custom-state ownership, multi-import SFF namespaces, exact controller tick order, and exact animation-source behavior remain partial. |
| `HitDef` | compiled, executed-partial | `synthetic-imported-x.json`, `synthetic-imported-custom-state.json`, `synthetic-imported-target-owned-custom-state.json` checksum `410fb8c0`, `synthetic-imported-hitdef-priority.json`, `synthetic-imported-hitdef-kill.json`, `synthetic-imported-hitdef-guard-kill.json`, `synthetic-imported-gethitvar-kill.json` checksum `ef5ffabf`, `synthetic-imported-gethitvar-guard-kill.json` checksum `abb4e468`, `synthetic-imported-gethitvar-crouch-guard-kill.json` checksum `2976fb8c`, `synthetic-imported-gethitvar-air-guard-kill.json` checksum `4382207e`, `synthetic-imported-hitdef-guard-sound.json`, `synthetic-imported-hitdef-hit-sound.json`, `synthetic-imported-hitdef-hit-spark.json`, `synthetic-imported-hitdef-guard-spark.json`, `synthetic-imported-hitdef-data-spark.json` checksum `d950796c`, `synthetic-imported-hitdef-data-guard-spark.json` checksum `7bd9d162`, `synthetic-imported-hitdef-common-spark.json` checksum `5ea054d7`, `synthetic-imported-hitdef-fightfx-spark.json` checksum `11537b56`, `synthetic-imported-hitdef-common-guard-spark.json` checksum `7650a09c`, `synthetic-imported-hitdef-fightfx-guard-spark.json` checksum `32f3e92d`, `synthetic-imported-hitdef-guard-effect-package.json` checksum `1c3167b7`, `synthetic-imported-hitdef-hit-effect-package.json` checksum `46aa5ce1`, `synthetic-imported-fall-defence-up.json`, `synthetic-imported-gethitvar-fall-recover.json` checksum `259b300f`, `synthetic-imported-gethitvar-fall-metadata.json` checksum `474fa734`, `synthetic-imported-gethitvar-fall-envshake.json` checksum `6364632a`, `synthetic-imported-gethitvar-down-recover.json` checksum `b8a7aef0`, `synthetic-imported-gethitvar-guarded.json` checksum `7c36defb`, `synthetic-imported-gethitvar-animtype.json`, `synthetic-imported-gethitvar-snap.json` checksum `312a53fc`, `synthetic-imported-default-guard-state.json`, `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641`, `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c`, `synthetic-imported-crouch-guard-state.json`, `synthetic-imported-diagonal-crouch-guard-state.json`, `synthetic-imported-air-guard-state.json`, `synthetic-imported-air-guard-landing.json` checksum `d6986d7f`, `kfm-official-auto-guard-start.json`, `kfm-official-auto-guard-end.json`, `kfm-official-default-guard-state.json`, `kfm-official-default-crouch-guard-state.json`, `kfm-official-default-air-guard-state.json`, `synthetic-imported-assertspecial-noko.json` checksum `f2f60521`, `synthetic-imported-assertspecial-guarddeny.json` checksum `fd2eb239`, `synthetic-imported-assertspecial-crouch-guarddeny.json` checksum `757cb87a`, `synthetic-imported-assertspecial-air-guarddeny.json` checksum `810a1679`, `synthetic-imported-assertspecial-lifetime.json`, focused `PlayableMatchRuntime` / `HitSparkRenderer` / `RuntimeCompiler` tests, KFM optional traces | Basic attr, guardflag, damage, pause, hit velocity, bounded normal-hit `kill` plus stand/crouch/air guard `guard.kill` metadata exposed through `GetHitVar(kill)`, bounded `snap` metadata exposed through `GetHitVar(xoff/yoff/zoff)` plus simple direct-contact attacker-relative positioning, fall metadata including bounded `fall.defence_up`, `fall.recover` / `fall.recovertime`, `fall.damage`, `fall.kill`, `fall.xvelocity`, `fall.yvelocity`, `fall.envshake.*`, and `down.recover` / `down.recovertime`, bounded direct guarded metadata exposed through `GetHitVar(guarded)`, get-hit classification vars (`animtype`, `fall.animtype`, `ground.type`, `air.type`, `isbound`), p1/p2 state routing, bounded owner-backed `p2stateno` custom-state chain/return, bounded target-owned `p2getp1state = 0` custom-state route through defender-owned state/action `888` with no attacker `customOwnerId`, bounded direct `priority` clash where a higher numeric priority suppresses a lower-priority direct attack, bounded `hitsound` / `guardsound` refs with required hit/guard `PlaySnd` telemetry for `hitsound = S5,0` and `guardsound = S6,0`, bounded `sparkno` / `guard.sparkno` / `sparkxy` refs with required hit/guard `HitSpark` telemetry for `sparkno = S7001` and `guard.sparkno = S7000`, and typed `HitDef` `ControllerOp` fallback from parsed CNS `[Data] sparkno` / `guard.sparkno` constants when a state `HitDef` omits explicit spark refs, with required fallback trace evidence for hit `[Data] sparkno = 2` and guard `[Data] guard.sparkno = 40`. Required common/default plus FightFX asset-frame metadata for unprefixed hit `7001`, FightFX hit `F7002`, unprefixed guard `7003`, and FightFX guard `F7004` via `hitSparkLibraries`, plus combined package routes requiring `hitsound = F5,0` with `soundPrefix = kfm` and FightFX `sparkno = F7002` on one direct hit contact and `guardsound = S6,0` with FightFX `guard.sparkno = F7004` on one guarded contact, each with shared non-empty contact id/tick/kind metadata. Bounded 180-frame `HitSparkRenderer` player AIR first-frame, common/FightFX system-namespace fallback probes, and runtime-provided package-frame preservation from snapshot events have smoke/player diagnostics and unit coverage. `DemoFighterDefinition.hitSparkLibraries` can provide common/default or FightFX AIR action frames for hit/guard spark refs, and the renderer preserves those frames before synthetic fallback. Bounded automatic guard-start/end and stand/crouch/atomic-`DB`/air guard-hit state routing where defender-owned Common1-style states exist, bounded stand guard slide-stop/control order through `150 -> 151 -> 130`, bounded stand guard-hold walk-control through `150 -> 151 -> 130 -> 20`, bounded crouch guard slide-stop/control order through `152 -> 153 -> 130`, bounded synthetic air guard landing walk-control through `154 -> 155 -> 52 -> 20` with `155` `HitVelSet` / `VelAdd` / `CtrlSet` / landing `VelSet` / landing `PosSet` / final `ChangeState` order, bounded named controller/typed-operation order for those synthetic guard-hit routes, a required direct guard metadata branch through `GetHitVar(guarded) = 1` after `150 -> 151`, a required direct stand guard kill metadata branch through `GetHitVar(kill) = 0 && GetHitVar(guarded)` after `150 -> 151`, a required direct crouch guard kill metadata branch through the same expression after `152 -> 153`, a required direct air guard kill metadata branch through the same expression after `154 -> 155`, required fall metadata branches through `GetHitVar(fall.damage/fall.kill/fall.xvel/fall.yvel)`, `GetHitVar(fall.envshake.time/freq/ampl/phase)`, and `GetHitVar(down.recover/down.recovertime/recovertime)` after owner-backed state `5100`, optional official KFM/Common1 controller/operation order plus actor-frame state/physics/body telemetry for auto guard-start/end and stand/crouch/air guard-hit routes when the private fixture exists, bounded defender-side `NoStandGuard`, `NoCrouchGuard`, and `NoAirGuard` denial for matching state-type guards with ordered flag-before-HitDef evidence, bounded one-frame `NoStandGuard` expiry into later guard evidence, and bounded nonlethal life clamps for direct `kill = 0`, `guard.kill = 0`, stored `fall.kill = 0` fall damage, and defender-side `AssertSpecial NoKO` with ordered flag-before-HitDef evidence. `RuntimeDirectCombatWorld` owns the current bounded same-tick direct priority win/trade mutation and direct hit/guard result mutation after collision/eligibility resolution. Exact MUGEN/IKEMEN priority classes, guard KO/round/no-KO interactions beyond this clamp and bounded stand/crouch/air guard kill metadata, exact get-hit animation selection, exact fall metadata lifetime/stacking, exact fall-defense lifetime/stacking, exact recovery threshold tables, exact guard physics/timing/effects beyond guarded metadata and bounded slide-stop/landing handoff, state-`52` guard landing internals, exact throw positioning, z-axis and guard snap parity, sound telemetry, prefixed `soundPrefix` FightFX lookup, and bounded spark rendering/source classification, exact intra-tick sound/spark ordering, exact channel priority/mixing/timing, exact FightFX/common sprite lookup, render timing, binding, layering, scale, palette, motif/screenpack ownership, reversal/projectile/helper/custom-state priority, throws, and multi-hit priority remain partial. |
| `AssertSpecial` | compiled, executed-partial | `synthetic-imported-assertspecial-control.json` checksum `144ac7bc`, `synthetic-imported-assertspecial-global-telemetry.json` checksum `fc793d29`, `synthetic-imported-assertspecial-round-flow-telemetry.json` checksum `10f95bdb`, `synthetic-imported-assertspecial-shadow-telemetry.json` trace checksum `2b9c8fac`, `synthetic-imported-assertspecial-helper-explod-shadow.json` trace checksum `83f61b48`, `synthetic-imported-assertspecial-timerfreeze.json` checksum `408528f1`, `synthetic-imported-assertspecial-roundnotover.json` checksum `342d49f0`, `synthetic-imported-assertspecial-guarddeny.json` checksum `fd2eb239`, `synthetic-imported-assertspecial-crouch-guarddeny.json` checksum `757cb87a`, `synthetic-imported-assertspecial-air-guarddeny.json` checksum `810a1679`, `synthetic-imported-assertspecial-lifetime.json`, `synthetic-imported-assertspecial-unguardable.json` checksum `e84aa12d`, `synthetic-imported-assertspecial-noko.json` checksum `f2f60521`, `synthetic-imported-target-noko.json` checksum `321a1eba`, `synthetic-imported-assertspecial-nogetupfromliedown.json` checksum `4c3b6281`, `synthetic-imported-assertspecial-nofastrecoverfromliedown.json` checksum `74bf5d85`, focused `RuntimeRecoverySystem` / compiler / runtime tests, focused compiler/runtime/trace tests | Static flags lower into typed `assertspecial` operation evidence before the existing runtime flag application path. Runtime normalizes flags, exposes debug telemetry, applies bounded `NoAutoTurn`, `NoWalk`, `Invisible`, bounded global-style telemetry for `nobardisplay`, `nobg`, `nofg`, `nokosnd`, and `nomusic`, bounded round-flow telemetry for official `intro` and `nokoslow` plus runtime `intro` / `noKoSlow` booleans, bounded local/global shadow presentation for `noshadow` and `globalnoshadow`, actor-local `TimerFreeze` for current active P1/P2 round-timer freeze, actor-local `RoundNotOver` for current active P1/P2 round-finish hold after timer zero, bounded IKEMEN `NoGetUpFromLieDown` suppression for the current Common1 `5110 -> 5120` lie-down get-up handoff, bounded IKEMEN `NoFastRecoverFromLieDown` suppression for the current fast lie-down recovery shortcut, attacker-side `Unguardable`, defender-side `NoKO`, stand/crouch/air guard-denial flags, and a one-frame trigger-expiry route where later held-back contact guards after `NoStandGuard` stops evaluating. The global telemetry gates prove grouped official-style `AssertSpecial` controllers can normalize global-style and round-flow flags into final-actor `assertSpecialGlobalFlags` evidence; they do not prove lifebar hiding, stage BG/FG suppression, KO sound suppression, music pause, intro flow, or KO slow-motion behavior. The shadow gates prove grouped official-style `AssertSpecial` controllers can normalize `noshadow` into final-actor `assertSpecialFlags`, normalize `globalnoshadow` into final-actor `assertSpecialGlobalFlags`, suppress P1/P2 actor-frame shadows, and suppress Helper plus Explod actor-frame shadows in the current spawned `GlobalNoShadow` route through `shadowVisible=false`; renderer ellipse removal remains unit-covered. The TimerFreeze gate proves a 61-frame fight timer stays at displayed timer `2` for 70 active frames while the flag is asserted; the RoundNotOver gate proves a 61-frame timer can reach displayed timer `0` while the round remains `fight` / `Fight`. The NoGetUpFromLieDown trace proves the typed flag reaches runtime state, executes as typed `assertspecial` evidence in `5110`, counts `down.recovertime` to `0`, blocks `RuntimeRecoverySystem.advanceCommon1LieDownRecovery` from entering `5120`, and leaves final P2 in `5110`. The NoFastRecoverFromLieDown trace proves fresh `recovery` command input can stay active in `5110` while the flag suppresses the bounded fast recovery shortcut until `down.recovertime` reaches `0`, then `5110 -> 5120 -> 0` executes. The control trace now requires typed operation evidence; stand/crouch/air guard-deny and NoKO traces still require ordered flag-before-HitDef evidence, target-controller NoKO has bounded flag-before-TargetLifeAdd evidence, and the `Unguardable` oracle is now required by the trace coverage-summary contract. Dynamic/unparseable flags, exact input-mashing thresholds, exact global/team/helper ownership beyond the current spawned helper/explod route, pause-layer interaction, exact winpose/KO/lifebar/round-transition behavior, broader persistence layering, priority, public KFM/Common1 fixture confirmation, round/no-KO semantics, exact shadow skew/stage parameters/layers, projectile shadow semantics, and full MUGEN/IKEMEN parity remain partial. |
| `HitFallVel`, `HitFallDamage`, `HitFallSet` | compiled, executed-partial | focused `HitFallControllerSystem` tests, `synthetic-imported-common-gethit.json` checksum `713e49b7`, `synthetic-imported-fall-defence-up.json`, `synthetic-imported-gethitvar-fall-defence-up.json`, `synthetic-imported-gethitvar-fall-recover.json` checksum `259b300f`, `synthetic-imported-gethitvar-fall-metadata.json` checksum `474fa734`, `synthetic-imported-gethitvar-fall-envshake.json` checksum `6364632a`, `synthetic-imported-gethitvar-down-recover.json` checksum `b8a7aef0`, `synthetic-imported-default-fall-ground-recovery-priority.json` checksum `e83b2db7`, `synthetic-imported-default-fall-recovery-input-priority.json` checksum `f5e72e07`, `synthetic-imported-hitfall-canrecover-ready.json` checksum `c0097d7f`, `synthetic-imported-default-fall-recovery.json` checksum `d83797d9`, `synthetic-imported-default-liedown-fast-recovery.json` checksum `74bdac97`, `synthetic-imported-default-fall-recovery-threshold.json` checksum `7bb15a5f`, `synthetic-imported-default-fall-official-recovery-threshold.json` checksum `86804271`, `synthetic-imported-default-fall-recovery-tick-order.json` checksum `e2691aab`, `synthetic-imported-default-fall-recovery-too-early.json` checksum `050e7e3c`, `synthetic-imported-default-fall-official-recovery-too-early.json` checksum `ef945ff5`, `synthetic-imported-default-fall-ground-recovery.json`, optional `kfm-official-default-fall-gethit.json` checksum `517bc059`, optional `kfm-official-default-fall-recovery.json` checksum `2743c2c9`, optional `kfm-official-default-fall-recovery-too-early.json` checksum `0ad7ae02`, optional `kfm-official-default-fall-ground-recovery.json`, optional `kfm-official-default-fall-ground-recovery-priority.json` checksum `6079c8c9` | `RuntimeHitFallControllerWorld` owns bounded `HitFallVel`, `HitFallDamage`, and `HitFallSet` mutations from typed `hitfall:*` ops or raw params. Partial Common1/get-hit plumbing includes bounded `fall.defence_up` scaling for deferred `HitFallDamage`, nonlethal stored fall damage, bounded `GetHitVar(fall.defence_up)` branching, bounded `GetHitVar(fall.recover)` branching while `fall.recovertime` is positive and `CanRecover` is false, bounded `HitFall && CanRecover` branching after `fall.recovertime` reaches zero without active recovery input, bounded `command = "recovery"` priority over a competing `HitFall && CanRecover` probe, bounded near-ground `command = "recovery"` priority into `5200/5201/52` while generic `5210` air recovery is forbidden, bounded stored fall metadata reads for `fall.damage`, `fall.kill`, `fall.xvel`, and `fall.yvel` before `HitFallDamage`, bounded stored fall env-shake metadata reads before `FallEnvShake`, bounded stored down-recovery metadata reads for `down.recover`, `down.recovertime`, and `recovertime`, bounded named custom get-hit controller order through `HitFallVel -> HitFallDamage -> HitFallSet -> FallEnvShake` in state `5100`, optional official KFM fall-entry evidence through `5101` `HitFallVel` / `hitfall:hitfallvel` and `5110` `HitFallDamage` / `hitfall:hitfalldamage`, bounded synthetic full-chain fall recovery order through `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120` with down-counting `hitFall.downRecoverTime` actor-frame evidence in `5110` and `5120` `HitFallSet` / `hitfall:hitfallset`, bounded positive lie-down fast recovery from `5110` to `5120` while `down.recovertime` is still positive and fresh `command = "recovery"` is active, bounded air-recovery settle ordering through `HitFallSet` plus first-to-last `hitFall.recoverTime` drop evidence inside `5050` before `5210`, official-style synthetic threshold evidence where `5050` drops to `recoverTime = 0` before `5200`, bounded early-recovery rejection where `5050` actor-frame evidence keeps minimum observed `hitFall.recoverTime >= 1` while the countdown drops and recovery states stay forbidden, official-style synthetic early-recovery rejection with the same positive-minimum window, bounded synthetic ground-recovery controller-order evidence through `5050 -> 5200 -> 5201 -> 52 -> 0`, optional official KFM lie-down/get-up controller/typed-operation evidence, optional official KFM ground-recovery controller-order evidence through `5050 -> 5200 -> 52` landing operations, and optional private-fixture ground-over-air priority evidence where `5210` plus lie-down states stay forbidden on the real KFM route. Exact lifetime, stacking, helper/team/redirect ownership, threshold tables, velocity/bounce math, ground/air recovery-input arbitration, and full VM tick-order parity remain partial. |
| `FallEnvShake` | compiled, executed-partial | `synthetic-imported-common-gethit.json` checksum `713e49b7`, focused `EnvShakeSystem` coverage | `RuntimeFallEnvShakeControllerDispatchWorld` owns active-state controller telemetry, typed `fallenvshake` operation selection, bounded fall-shake event handoff into `RuntimeEnvShakeWorld`, consumed `hitFall.envShake` cleanup, and operation telemetry after a real event. Required trace evidence still covers P2 state `5100` with `time = 15`, `freq = 178`, `ampl = 6`, and `phase = 0`. Exact pause/stage/layer interaction, waveform parity, helper/redirect ownership, and broader fall presentation parity remain partial. |
| `Target*`, `BindToTarget` | compiled, executed-partial | `synthetic-imported-target.json` checksum `f5a16dc9`, `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2`, `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962`, `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa`, `synthetic-imported-helper-target-controllers.json` checksum `61f4c61e`, `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1`, `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8`, `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae`, `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3`, `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1`, `synthetic-imported-helper-targetstate.json` checksum `011633b8`, `synthetic-imported-default-numtarget.json` checksum `5869ebbd`, `synthetic-imported-default-target-redirect.json` checksum `d43caabf`, `synthetic-imported-bare-target-redirect.json` checksum `f9c90aa8`, `synthetic-imported-projectile-target-redirect.json` checksum `cd099094`, `synthetic-imported-hitdef-projectile-target-mix.json` checksum `e98d4857`, `synthetic-imported-target-redirect.json` checksum `89580963`, `synthetic-imported-target-dynamic-redirect.json` checksum `9985b62a`, `synthetic-imported-target-noko.json` checksum `321a1eba`, `synthetic-imported-targetstate-custom.json`, `synthetic-imported-bindtotarget-head.json`, `synthetic-imported-bindtotarget-mid.json`, `synthetic-imported-targetbind-pause.json` | Target memory exists; TargetDrop keeps the official omitted-`keepone` default of `1` in typed IR and raw fallback; `synthetic-imported-target.json` now gates typed TargetLifeAdd/TargetPowerAdd/TargetVel*/TargetFacing/TargetBind/BindToTarget/TargetDrop operation evidence, world-visible target-link/binding evidence, final P1 `targetCount = 0`, final P2 `life = 943` / `power = 40`, and actor-frame P2 facing/velocity evidence after Target* side effects. `synthetic-imported-projectile-target-controllers.json` gates the matching bounded player-owned Projectile Target side-effect subset without direct HitDef controller evidence: projectile target memory id `77`, TargetBind offset `36,-12`, final P1 `targetCount = 0`, and final P2 `life = 949` / `power = 40`. `synthetic-imported-projectile-targetstate.json` gates the bounded player-owned Projectile TargetState subset without direct HitDef controller evidence: projectile target memory id `77`, custom-owner P2 frames for states `888` and `889`, `SelfState` return, final P2 state `0`/control, and final P2 `life = 969`. `synthetic-imported-projectile-default-targetstate.json` gates the same player-owned Projectile TargetState subset when omitted `projid` / `id` defaults target memory to id `0`. `synthetic-imported-helper-target-controllers.json` gates the matching bounded helper-owned direct-HitDef subset for `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` with helper target-link/binding evidence, helper payload `targetCount = 0`, and final P2 `life = 944` / `power = 40`; `synthetic-imported-helper-projectile-bare-target.json` gates helper-local bare `Target, Life` after helper-parented Projectile target memory with owner/helper target links for id `8863` and helper payload `targetCount = 1`; `synthetic-imported-helper-projectile-target-controllers.json` gates the same bounded helper-owned Target side-effect subset after helper-parented Projectile target memory with owner/helper target links for id `8861`, binding evidence, helper payload `targetCount = 0`, and final P2 `life = 958` / `power = 40`; `synthetic-imported-helper-projectile-default-target-controllers.json` gates the same helper-parented Projectile side-effect subset when omitted `projid` / `id` defaults to target id `0`; `synthetic-imported-helper-projectile-targetstate.json` gates bounded helper-owned `TargetState value = 888` after helper-parented Projectile target memory with owner/helper target links for id `8862`, custom-owner actor-frame evidence, and `SelfState` return; `synthetic-imported-helper-projectile-default-targetstate.json` gates the same TargetState route after omitted `projid` / `id` defaults to target id `0`; `synthetic-imported-helper-targetstate.json` gates bounded helper-local `TargetState value = 888` into helper-owner state data with custom-owner actor-frame evidence and `SelfState` return. `synthetic-imported-default-numtarget.json` gates omitted-id direct `HitDef` target memory through target id `0` and an owner-local `NumTarget(0)` branch, `synthetic-imported-default-target-redirect.json` gates the same default-id memory through `Target(0), Life` trigger reads, `synthetic-imported-bare-target-redirect.json` gates bare `Target, Life` trigger reads from current two-player target memory after direct `HitDef` contact, `synthetic-imported-projectile-target-redirect.json` gates bounded static `Target(77), Life` trigger reads from current two-player target memory after player Projectile contact, `synthetic-imported-hitdef-projectile-target-mix.json` gates bounded owner-local mixed target reads for direct `HitDef` id `77` and player-owned `Projectile` id `78`, `synthetic-imported-target-redirect.json` gates bounded static `Target(77), Life` trigger reads from current two-player target memory after direct `HitDef` contact, and `synthetic-imported-target-dynamic-redirect.json` gates bounded dynamic `Target(var(0)), Life` reads after owner-local `VarSet` seeds the target id. `synthetic-imported-target-noko.json` gates bounded `TargetLifeAdd` lethal damage clamping to final P2 life `1` when defender-side `AssertSpecial NoKO` has executed before the target damage route. TargetState can enter a bounded controller-owner custom-state chain from target memory and return through `SelfState`; helper TargetState can enter only owner-backed state data, not helper-owned custom state tables. TargetBind can keep a bound target offset while the source actor advances during bounded `SuperPause` movetime. BindToTarget can bind the controller owner to a recent target with static `pos = x,y,Foot|Mid|Head` and bounded `time`; `Head` and `Mid` have required trace evidence through parsed target `[Size]` anchors. Target-memory advance now prunes TargetBind records whose bound actor id/target id no longer survives expiry, and active TargetBind / BindToTarget position application requires matching live target memory before it moves either actor, so stale binding records fail closed. Complex target binding, complete custom-state parity beyond the current `p2stateno` and TargetState owner-backed chains, teams, helper/root/parent targets beyond the direct helper-HitDef and helper-parented Projectile gated subsets, Target* mutation mixing, unsupported or negative target-id expressions, multi-target behavior, exact target lifetime, exact final-animation parity, exact NoKO lifetime/round flow, and exact tick-order remain partial. |
| `Pause`, `SuperPause` | compiled, executed-partial | runtime tests, `synthetic-imported-superpause.json`, `synthetic-imported-superpause-pausebg.json` checksum `49bcfe16`, `synthetic-imported-superpause-anim-disabled.json` checksum `fc7a2ca4`, `synthetic-imported-superpause-default-anim.json` checksum `318c5e9f`, `synthetic-imported-superpause-dynamic-anim-pos.json` checksum `e6bfbf75`, `synthetic-imported-superpause-anim-pos.json` checksum `f7dcdc9d`, `synthetic-imported-superpause-dynamic-params.json` checksum `052bb481`, `synthetic-imported-targetbind-pause.json`, `synthetic-imported-superpause-projectile-freeze.json`, `synthetic-imported-superpause-effect-freeze.json`, `synthetic-imported-explod-pausemovetime.json`, `synthetic-imported-explod-supermovetime.json` | Runtime tests cover source `movetime` with opponent freeze; typed pause operations now require match-freeze snapshot plus actor/effect advance and freeze trace evidence for actor/state/darken/remaining/movetime, including bounded `pausebg = 0` metadata, omitted default, disabled, explicit, and dynamic `SuperPause anim/pos` metadata, bounded dynamic time/movetime/darken/poweradd fallback, bounded TargetBind, projectile, visual Helper/Explod source-movetime routes, bounded Explod-owned `pausemovetime`, and bounded Explod-owned `supermovetime`. Full pause layering/superpause effects, actual renderer/background update parity, exact stage/BGCtrl pause timing, renderer visual suppression/playback parity, FightFX/common asset lookup/rendering, dynamic `S` prefix breadth, and unhittable parity are partial. |
| `Projectile` | compiled, executed-partial | `synthetic-imported-projectile.json`, `synthetic-imported-projectile-motion.json`, `synthetic-imported-projectile-velmul.json`, `synthetic-imported-projectile-contact.json` trace checksum `57b3b556` / final checksum `e0f3e41c`, `synthetic-imported-projectile-guard.json` trace checksum `eb9c2e58` / final checksum `b1c74e5e`, `synthetic-imported-projectile-localcoord-default-bounds-terminal.json` checksum `af7ee80e`, `synthetic-imported-helper-projectile-localcoord-default-bounds-terminal.json` checksum `46b0164c`, `synthetic-imported-helper-projectile-default-bounds-terminal.json` checksum `e85d7bbf`, `synthetic-imported-projectile-default-bounds-terminal.json` checksum `52879717`, `synthetic-imported-helper-projectile-stagebound-terminal.json` checksum `488ce550`, `synthetic-imported-helper-projectile-edgebound-terminal.json` checksum `8482f4f3`, `synthetic-imported-helper-projectile-heightbound-terminal.json` checksum `debb08b1`, `synthetic-imported-projectile-heightbound-terminal.json` checksum `1164a584`, `synthetic-imported-projectile-edgebound-terminal.json` checksum `e4361063`, `synthetic-imported-projectile-stagebound-terminal.json` checksum `fe3df8e7`, `synthetic-imported-projectile-bounds-remove-terminal.json` checksum `1d7479d3`, terminal fallback/cancel/timing traces, `EffectSpawnSystem` tests, `EffectLifecycleSystem` tests, `ProjectileCombatSystem` tests | Bounded projectile actor, spawn/remove lifecycle evidence, producer-store evidence, target-memory path, bounded motion/scale/combat/contact timing, official 240p omitted bound defaults (`projedgebound = 40`, `projstagebound = 40`, `projheightbound = -240,1`) plus bounded 640x480 character localcoord-derived omitted defaults (`80`, `80`, `-480,2`) for player-owned and helper-parented/root-owned Projectiles, explicit player/helper height/edge/stage bound removal, terminal playback for resolved `projhitanim`/`projremanim`/`projcancelanim`, no-contact timeout/fallback/cancel routes, multi-hit cooldown, priority clash/cancel, and guarded/hit target-memory routes through the shared effect actor world. Exact priority classes, exact trigger timing/lifetime, exact cancel tick-order/lifetime, exact terminal timing, full localcoord scaling beyond bounded 640x480 default-bound gates, exact camera/screen/stage/height semantics, exact stage-vs-screen/edge split, exact `velmul` tick-order parity, scaled hitbox parity, SND playback, exact spark lookup/binding/layering/timing/scale/palette, helper-owned custom states, multi-projectile selection beyond gated routes, custom-state guarded metadata, multi-target behavior, and IKEMEN parity remain blocked. |
| `ModifyProjectile` | compiled, executed-partial | `synthetic-imported-modifyprojectile-omitted-bounds.json` checksum `24cbb1dc` / final checksum `e94d1480`, `synthetic-imported-helper-modifyprojectile-omitted-bounds.json` checksum `9db04bbc` / final checksum `555d744b`, `synthetic-imported-helper-modifyprojectile-dynamic-params.json` checksum `2d88a550` / final checksum `edb6d2d2`, `synthetic-imported-modifyprojectile-dynamic-params.json` checksum `6ffbef92` / final checksum `5665a98e`, `synthetic-imported-helper-modifyprojectile-dynamic-bounds.json` checksum `f582153e` / final checksum `adc63407`, `synthetic-imported-modifyprojectile-dynamic-bounds.json` checksum `e2f7a077` / final checksum `aa78704a`, `synthetic-imported-modifyprojectile.json` checksum `63a87da1`, `synthetic-imported-helper-modifyprojectile.json` checksum `09d3f7e4`, `EffectSpawnSystem` tests, `ProjectileSystem` tests, focused `EffectActorSystem` helper-local tests | Bounded owner-side/helper-local mutation of live Projectile actors by static or dynamic `projid`, covering dynamic owner-side and helper-local `velocity`/`vel`, `accel`, `velmul`, `projscale`/`scale`, `projremovetime`/`removetime`, `sprpriority`, `projpriority`/`priority`, `projhits`, `projmisstime`, `projremove`, and owner-side/helper-local `projedgebound`, `projstagebound`, and `projheightbound` expressions through `RuntimeEffectSpawnWorld` and `RuntimeEffectActorWorld`; omitted bound params preserve existing explicit Projectile bounds in the current required owner/helper routes. The helper-local micro-VM can mutate only helper-parented owner-side Projectiles after helper-local spawn, required traces prove helper-local dynamic bounds/non-bound params through `Parent` / `Root`, and focused tests prove same-id player-owned exclusion. Redirects, helper-owned projectile combat/contact presentation, exact selection/tick order, remove triggers, helper/team namespace breadth, team/simul helper selection, and full MUGEN/IKEMEN projectile parity remain blocked. |
| `Helper`, helper-local `HitDef` / `NumTarget(id)` / `Target(id)` / bare `Target` / bounded direct-HitDef and Projectile `Target*` / `TargetState` / `BindToParent` / `BindToRoot` / static `Explod` / static `RemoveExplod` / static `ModifyExplod` / static `Projectile` / static `ModifyProjectile` / `NumExplod(id)` / `NumHelper(id)` / `NumProjID(id)` / `ProjHit(id)` / `ProjGuarded(id)` / `ProjContactTime(id)` / helper-local `HitCount` | compiled, executed-partial | `synthetic-imported-helper.json`, `synthetic-imported-helper-hitdef.json` checksum `89f9e876`, `synthetic-imported-helper-target.json` checksum `68f95b67`, `synthetic-imported-helper-default-target.json` checksum `e1bcced0`, `synthetic-imported-helper-bare-target.json` checksum `15f3c1db`, `synthetic-imported-helper-target-controllers.json` checksum `61f4c61e`, `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8`, `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae`, `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3`, `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1`, `synthetic-imported-helper-targetstate.json` checksum `011633b8`, `synthetic-imported-helper-projectile-target.json` checksum `49261b53`, `synthetic-imported-helper-projectile-default-target.json` checksum `b0daddf6`, `synthetic-imported-helper-ishelper.json` checksum `37877602`, `synthetic-imported-helper-enemynear.json` checksum `35498955`, `synthetic-imported-helper-bindtoparent.json` checksum `f9922c0e`, `synthetic-imported-helper-bindtoroot.json` checksum `bf72306c`, `synthetic-imported-helper-explod.json` checksum `87ae363f`, `synthetic-imported-helper-projectile.json` checksum `b6269136`, `synthetic-imported-helper-modifyprojectile.json` checksum `09d3f7e4`, `synthetic-imported-helper-projhit.json` checksum `2d9a281e`, `synthetic-imported-helper-projectile-hitcount.json` trace checksum `c8f5dc55` / final checksum `e1569fab`, `synthetic-imported-helper-projguard.json` checksum `d2e2f20d`, `synthetic-imported-helper-projcontact.json` checksum `4dcbdd25`, `synthetic-imported-helper-numproj.json` checksum `3312a554`, `synthetic-imported-helper-removeexplod.json` checksum `ff8658a2`, `synthetic-imported-helper-modifyexplod.json` checksum `0749041c`, `synthetic-imported-helper-numexplod.json` checksum `4328278a`, `synthetic-imported-helper-numhelper.json` checksum `4e32e951`, `synthetic-imported-superpause-effect-freeze.json`, `EffectSpawnSystem` tests, `EffectLifecycleSystem` tests, focused `EffectActorSystem` helper-local VM tests | Visual/helper actor plumbing with spawn/active lifecycle, producer-store evidence, bounded state-owner action resolution through `RuntimeEffectSpawnWorld`, owner runtime-program/animation handoff, lifecycle orchestration through `RuntimeEffectLifecycleWorld`, bounded source-movetime advance/freeze evidence during `SuperPause`, and a bounded helper-local micro-VM for `Time` triggers, `ChangeState`, `ChangeAnim`, helper-local `HitDef` activation into direct helper combat, helper-owned explicit/default/bare HitDef target memory with `NumTarget(id)` / `Target(id), Life` / bare `Target, Life` reads, bounded helper-owned direct-HitDef `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` side effects, bounded helper-owned Target side effects after explicit/default helper-parented Projectile target memory, bounded helper-local direct-HitDef and explicit/default helper-parented Projectile `TargetState` into owner-backed state data with `SelfState` return evidence, helper-owned explicit/default helper-parented Projectile target memory with owner and helper target-link evidence, helper-local kinematic controllers, `CtrlSet`, `StateTypeSet`, `LifeAdd`, `LifeSet`, `PowerAdd`, `PowerSet`, `VarSet`, `VarAdd`, `VarRandom`, `VarRangeSet`, helper-local `PlaySnd`/`StopSnd` sound-event telemetry, helper identity triggers (`IsHelper`, `IsHelper(id)`), helper-local `NumExplod(id)` counts for helper-parented owner-side Explods, helper-local `NumHelper(id)` counts for owner-side visual Helpers in the same effect store, helper-local `NumProjID(id)` counts for helper-parented owner-side Projectiles with removed-projectile exclusion, helper-local `ProjHit(id)`, `ProjGuarded(id)` / `ProjGuardedTime(id)`, bounded `ProjContact(id)` / `ProjContactTime(id)` reads, and bounded helper-local `HitCount` / `UniqHitCount` after mirrored helper-parented Projectile contact memory, read-only `Parent, ...` / `Root, ...` trigger/value redirects against owner runtime state as leading expressions and composite operands, read-only helper-local `EnemyNear, ...` redirects against the current two-player opponent runtime state plus caller-provided `EnemyNear(index)` / `EnemyNear(var(n))` lists when explicit opponent states are supplied, static helper-local `BindToParent` / `BindToRoot` owner binding against supplied owner/root runtime state, static helper-local `Explod` visual spawn into the owner-side Explod store with helper `parentId`, static helper-local `RemoveExplod` cleanup by id from the owner-side Explod store, static helper-local `ModifyExplod` mutation by id of helper-parented owner-side Explods, static helper-local `Projectile` spawn into the owner-side Projectile store with helper `parentId`, static helper-local `ModifyProjectile` mutation by id of helper-parented owner-side Projectiles, required ownerBind target/offset payload evidence for both binding trace gates, and `DestroySelf`. Broader indexed/team/helper-owned redirects beyond caller-provided `EnemyNear(index)` lists, teams/simul/turns, keyctrl ownership, player-state binding parity, nested helper ancestry, helper-owned custom state tables, helper Target/multi-target parity beyond the direct-HitDef and helper-parented Projectile gated subsets, throws, exact helper hitpause/tick order, exact helper HitDef/Projectile lifetime parity, helper-owned projectile combat/contact presentation, exact helper-local `ProjContact` / `ProjHit` / `ProjGuarded` timing and lifetime, exact helper-local `HitCount` lifetime/persist semantics, helper-owned effect namespaces, dynamic helper-local effect params/projectile ids, helper-bound Explod/Projectile timing/mutation beyond bounded static spawn/remove/modify/count-id/count-projectile/contact-trigger routes, exact helper effect-count parity, exact helper ownership scopes, helper fvar/sysvar `VarRandom`, exact helper resource scopes, exact helper-local sound timing/channel/redirect ownership, exact random stream parity, exact pause/tick order, exact opponent/parent/root selection, and full helper parity remain blocked. |
| `Helper` helper-local `ProjHitTime(0)` / `ProjGuardedTime(0)` / `ProjContactTime(0)` / `ProjCancelTime(...)` | compiled, executed-partial | `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b`, `synthetic-imported-helper-projguardedtime-any.json` checksum `1f1a38e4`, `synthetic-imported-helper-projcontacttime-any.json` checksum `0d9f7829`, `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01`, `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176`, `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2`, focused `EffectActorSystem` helper-local VM tests | Bounded helper-local any-projectile hit/guard/contact/cancel-time reads plus fixed/expression-derived cancel-time ids from helper-parented owner-side Projectiles only; same-owner player Projectile contact/cancel is ignored, helper-parented markers become visible after marker age advances, and the required traces route `1200 -> 1260 -> 1261`, `1200 -> 1262 -> 1263`, `1200 -> 1264 -> 1265`, `1200 -> 1266 -> 1267`, `1200 -> 1268 -> 1269`, and `1200 -> 1270 -> 1271`. Exact hit/contact/guard/cancel tick-order/lifetime, multi-projectile id selection, broad dynamic expression parity, teams, exact namespaces, and full helper Projectile parity remain blocked. |
| `Helper` helper-local `ProjGuarded(id)` / `ProjGuardedTime(id)` | compiled, executed-partial | `synthetic-imported-helper-projguard.json` checksum `d2e2f20d`, focused `EffectActorSystem` helper-local VM tests | Bounded helper-local guard/contact reads from helper-parented owner-side Projectiles only; same-id player-owned Projectile guard contact is ignored, helper-parented guard contact becomes visible after contact age advances, and the required trace routes `1200 -> 1218 -> 1219` / anims `938` and `947` after Projectile anim `948`. Helper-owned Projectile combat/contact presentation, exact `ProjContact` / `ProjHit` / `ProjGuarded` timing and lifetime, dynamic ids/params, teams, exact namespaces, and full helper Projectile parity remain blocked. |
| `Helper` helper-local `ProjContact(id)` / `ProjContactTime(id)` | compiled, executed-partial | `synthetic-imported-helper-projcontact.json` checksum `4dcbdd25`, focused `EffectActorSystem` helper-local VM tests | Bounded helper-local generic contact reads from helper-parented owner-side Projectiles only; same-id player-owned Projectile contact is ignored, helper-parented guard contact becomes visible as generic contact after contact age advances, and the required trace routes `1200 -> 1220 -> 1221` / anims `949` and `950` after Projectile anim `951`. Helper-owned Projectile combat/contact presentation, exact `ProjContact` / `ProjHit` / `ProjGuarded` timing and lifetime, dynamic ids/params, teams, exact namespaces, and full helper Projectile parity remain blocked. |
| `Explod`, `RemoveExplod` | compiled, executed-partial | `synthetic-imported-explod.json`, `synthetic-imported-explod-bind.json`, `synthetic-imported-explod-velocity.json`, `synthetic-imported-explod-scale.json`, `synthetic-imported-helper-explod.json` checksum `87ae363f`, `synthetic-imported-helper-removeexplod.json` checksum `ff8658a2`, `synthetic-imported-helper-modifyexplod.json` checksum `0749041c`, `synthetic-imported-removeexplod.json`, `synthetic-imported-superpause-effect-freeze.json`, `synthetic-imported-explod-ignorehitpause.json`, `synthetic-imported-explod-pausemovetime.json`, `synthetic-imported-explod-supermovetime.json`, `EffectSpawnSystem` tests, `EffectLifecycleSystem` tests | Bounded visual explod path with spawn/active/remove lifecycle, producer-store evidence, spawn position/bind resolution through `RuntimeEffectSpawnWorld`, helper-local static visual spawn/removal/mutation handoff through `RuntimeEffectActorWorld`, lifecycle orchestration through `RuntimeEffectLifecycleWorld`, owner-side `bindtime`, `vel`/`accel` motion, bounded static `scale`, bounded hit-pause `ignorehitpause` evidence, bounded `pausemovetime`/`supermovetime` advance budgets, bounded regular `Pause` actor-budget evidence, and bounded source-movetime advance/freeze evidence during `SuperPause`; exact binding/tick order, helper-owned effect namespaces, dynamic helper-local effect params, helper-bound Explod timing beyond the static spawn/remove/modify-id route, ownpal, exact scaling parity, FightFX routing, remove-trigger semantics, and full pause parity remain partial. |
| `ModifyExplod` | compiled, executed-partial | `synthetic-imported-modifyexplod.json` checksum `bca75991`, `synthetic-imported-helper-modifyexplod.json` checksum `0749041c`, `ExplodSystem` tests, `EffectSpawnSystem` tests, focused `EffectActorSystem` helper-local tests | Bounded owner-side mutation of live visual Explod actors by `id`, covering static `vel`/`velocity`, `accel`, `scale`, `removetime`, `sprpriority`, `removeongethit`, `ignorehitpause`, `pausemovetime`, `supermovetime`, `facing`, `trans`, and existing-bind `bindtime` through `RuntimeEffectSpawnWorld` and `RuntimeEffectActorWorld`; the helper-local micro-VM can mutate only helper-parented owner-side Explods by static id after helper-local spawn. Dynamic expressions, position rebinding, helper-owned effect namespaces, FightFX/common routing, remove-trigger parity, exact tick order, and full MUGEN/IKEMEN Explod lifecycle parity remain blocked. |
| `HitBy`, `NotHitBy` | compiled, executed-partial | `synthetic-imported-hitby-allow.json` checksum `c75d5c7d`, `synthetic-imported-hitby-reject.json` checksum `65185fd1`, `synthetic-imported-reject.json` checksum `5aca7dc0`, `synthetic-imported-default-fall-ground-recovery-priority.json` checksum `e83b2db7`, `synthetic-imported-default-fall-ground-recovery.json`, focused `HitDefenseSystem` tests | Static attr allow/deny setup has typed `eligibility:*` evidence; `RuntimeHitDefenseWorld` owns bounded setup/removal from typed ops or raw params; the required HitBy allow trace proves a matching `S,NA` allow-list still resolves as a hit, the required HitBy reject trace proves a mismatched `S,NT` allow-list blocks `S,NA` without damage, the required reject trace proves NotHitBy can reject matching contact, and bounded ground-recovery requires `NotHitBy` / `eligibility:nothitby` safety telemetry before landing. Exact attr grammar, slot priority, edge timing, helper/custom-state ownership, and full invulnerability timing parity remain partial. |
| `HitOverride` | compiled, executed-partial | `synthetic-imported-hitoverride.json`, `synthetic-imported-hitoverride-slot-priority.json`, `synthetic-imported-hitoverride-guardflag-filter.json`, `synthetic-imported-hitoverride-missonoverride-zero-slot-priority.json`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority.json`, `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority.json`, `synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json`, `synthetic-imported-hitoverride-missonoverride-default-guardflag-filter.json`, `synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter.json`, `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter.json`, `synthetic-imported-projectile-hitoverride-slot-priority.json`, `synthetic-imported-helper-projectile-hitoverride-slot-priority.json`, `synthetic-imported-ikemen-active-root-hitoverride.json` checksum `dd6bc943` / final `5a093268`, `synthetic-imported-ikemen-active-root-hitoverride-expiry.json` checksum `eae92580` / final `9314ef50`, runtime tests, focused `CombatResolver` / `HitDefenseSystem` / `HitOverrideSystem` tests | Static slot/attr/time/state setup has typed `hitoverride` evidence; typed and raw setup preserve bounded `guardflag` / `guardflag.not` params; `RuntimeHitDefenseWorld` owns bounded slot setup/removal from typed ops or raw params; required traces prove a matching incoming direct `HitDef`, direct `HitDef guardflag` filtered route, direct/player/helper `missonoverride = 0` custom-state slot-priority routes, direct default `missonoverride = -1` custom-state miss route, direct/player/helper default `missonoverride = -1` custom-state guardflag-filter routes, player-owned `Projectile`, or helper-parented `Projectile` can choose/filter active slots, redirect, or reject before target memory as documented. Explicit IKEMEN active-motion roots may now author one ordinary direct-HitDef override route and age their actor-local slots before controller execution; the adversarial expiry trace proves a one-tick slot no longer redirects a delayed root HitDef. `RuntimeHitOverrideWorld` owns bounded slot ticking and redirect mutation for direct and projectile combat paths. Attr grammar, exact guard timing/guarded contact semantics, broad active-root variants, broader helper/projectile/custom-state redirects, `forceguard`/`forceair` edge timing, and IKEMEN parity remain partial. |
| `MoveHitReset` | compiled, executed-partial | `synthetic-imported-movehitreset.json`, runtime tests | Static reset compiles into typed `contact:movehitreset` evidence; the required trace proves a direct `HitDef` contact can be cleared before a later `MoveHit >= 1` branch fires. Projectile contact memory is intentionally not cleared by this bridge. Exact helper/redirect/team ownership and tick-order parity remain future work. |
| `HitAdd` | compiled, executed-partial | `synthetic-imported-hitadd.json` | Static `value` lowers into typed `contact:hitadd` operation evidence and adds to bounded current-state direct `HitCount` telemetry. `UniqHitCount` remains target-uniqueness evidence and is not increased by `HitAdd`. Guard/projectile counts, helpers, combo lifetime, redirects, teams, and exact tick-order parity remain future work. |
| `ReversalDef` | compiled, executed-partial | `synthetic-imported-reversal.json`, `synthetic-imported-guarddist-reversal-no-contact.json`, `synthetic-imported-walkback-guard-reversal.json`, `synthetic-imported-air-guard-reversal.json`, `synthetic-imported-crouch-guard-reversal.json`, `synthetic-imported-guard-reversal.json`, `synthetic-imported-custom-state-reversal.json`, `synthetic-imported-projectile-reversal.json`, `synthetic-imported-helper-projectile-reversal.json`, focused `ReversalSystem` tests, runtime tests | Static reversal attr/pause/state setup has typed `reversaldef` evidence; `RuntimeReversalControllerDispatchWorld` owns active-state controller telemetry, typed operation selection, raw fallback activation payload, operation telemetry, and activation handoff into `RuntimeReversalWorld`; `RuntimeReversalWorld` owns bounded active counter detection, no-contact guard-distance rejection, and direct/authored walk-back guard-input/stand guard-input/crouch guard-input/air guard-input/direct-custom-state/player-Projectile/helper-parented Projectile counter-result mutation for matching incoming contact. Exact priority beyond these bounded routes, exact guard-distance boxes, positive proximity-only `guard.dist` ReversalDef contact, custom-state breadth beyond direct routes, projectile reflection/removal after reversal, helper-owned custom-state tables, attr grammar, trigger lifetime, hitpause/tick order, and IKEMEN parity remain partial. |
| `AttackMulSet`, `DefenceMulSet` | compiled, executed-partial | `synthetic-imported-damage-scale.json`, `synthetic-imported-damage-scale-dynamic.json` trace checksum `3433b369` / final checksum `e3db6dd9`, focused `DamageScaleSystem` and `RuntimeControllerDispatchSystem` tests | Static numeric multipliers and resolved dynamic `value` params compile or record into typed `damage-scale:*` operations; `RuntimeDamageScaleWorld` owns bounded multiplier setup from typed ops or raw params; the resulting values feed the partial combat resolver's outgoing/incoming direct HitDef damage math. Exact helper/projectile/custom-state/guard/target scaling order, redirect ownership, rounding, controller-loop timing, and full damage-scaling parity remain partial. |
| `VelSet`, `VelAdd`, `VelMul`, `HitVelSet`, `PosSet`, `PosAdd` | compiled, executed-partial | `synthetic-imported-kinematic.json` checksum `92804390`, `synthetic-imported-dynamic-posadd.json` checksum `8ac604b1`, `synthetic-imported-dynamic-posset.json` checksum `aeb730fb`, `synthetic-imported-dynamic-velmul.json` checksum `4d241401`, `synthetic-imported-dynamic-veladd.json` checksum `daf99fb4`, `synthetic-imported-helper-controller-param-parentroot.json` checksum `94919326`, `synthetic-imported-helper-dynamic-veladd.json` checksum `fbb8bcae`, `synthetic-imported-helper-dynamic-velmul.json` checksum `08220a98`, `synthetic-imported-helper-dynamic-posset.json` checksum `50596bc2`, `synthetic-imported-helper-dynamic-posadd.json` checksum `97ec15d0`, `synthetic-imported-const-controller-param.json` checksum `2dad3a50`, `synthetic-imported-default-fall-gethit.json` checksum `6af73a91`, `synthetic-imported-default-guard-state.json`, `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641`, `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c`, `synthetic-imported-crouch-guard-state.json`, `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699`, `synthetic-imported-diagonal-crouch-guard-state.json`, `synthetic-imported-air-guard-state.json`, `synthetic-imported-air-guard-landing.json` checksum `d6986d7f`, optional `kfm-official-default-guard-slide-stop.json` / `kfm-official-default-guard-hold-return.json` / `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da`, optional `kfm-official-default-crouch-guard-slide-stop.json` / `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0`, optional `kfm-official-default-fall-gethit.json` checksum `517bc059`, optional `kfm-official-default-air-guard-state.json`, optional `kfm-official-auto-guard-end.json`, focused `KinematicControllerSystem` tests | Static movement/position params and static `HitVelSet x/y` flags lower into typed `kinematic:*` operation evidence, including `kinematic:velset`, `kinematic:veladd`, `kinematic:velmul`, `kinematic:posset`, `kinematic:posadd`, and `kinematic:hitvelset` for bounded guard-hit and fall-entry routes. `RuntimeKinematicControllerWorld` owns bounded passive movement/position setup from typed operations or raw params before per-frame integration remains in `RuntimeKinematicsWorld`. The synthetic fall-entry gate requires `5030` `HitVelSet` / `kinematic:hitvelset`; optional KFM fall-entry evidence also requires ground-impact `VelSet` / `kinematic:velset` and lie-down `VelSet` / `kinematic:velset`. The stand and crouch guard slide-stop gates require `151`/`153` `HitVelSet` before `VelSet` with `kinematic:hitvelset` and `kinematic:velset` evidence plus synthetic actor-frame velocity before stop/control; the required synthetic stand guard-hold walk-return gate proves the portable route resumes state/action `20` with control under held-back input, the required synthetic crouch guard-hold crouch-return gate proves the portable route resumes state/action `10` with control under held down-back input, and optional private KFM fixture routes now mirror both stand `150 -> 151 -> 130 -> 20` and crouch `152 -> 153 -> 131 -> 11` return shapes while preserving controller order. The air guard-hit gates require named `VelAdd` gravity order, the required synthetic air guard landing gate requires `155` `HitVelSet`, `VelAdd`, landing `VelSet` / `kinematic:velset`, landing `PosSet` / `kinematic:posset`, state-`52` y = 0 actor-frame evidence, and final state/action `20` walk-control, the optional KFM air guard route requires `VelSet`/`PosSet` landing operations plus bounded actor-frame velocity telemetry and final state/action `20` walk-control, and the optional KFM auto guard-end route requires return-to-idle `VelSet` / `kinematic:velset` evidence. Bounded active-state dynamic `VelSet`, `VelAdd`, `VelMul`, `PosSet`, and `PosAdd` now emit typed telemetry after expression resolution; bounded first-generation helper-local dynamic `VelSet`, `VelAdd`, `VelMul`, `PosSet`, and `PosAdd` now also emit typed telemetry through required Parent/Root routes. Remaining dynamic movement params still fall back to runtime expression evaluation, and physics semantics are partial and must stay trace-gated. |
| `PosFreeze`, `ScreenBound` | compiled, executed-partial | unit tests, focused `BoundsControllerSystem` tests, `synthetic-imported-bounds.json`, `synthetic-imported-posfreeze-dynamic.json` checksum `8de0c2e9` / final checksum `6c40bb79`, `synthetic-imported-screenbound-camera.json`, `synthetic-imported-screenbound-dynamic.json` checksum `9797bdfe` / final checksum `d76b641a` | Static one-frame position-freeze and screen/camera-bound params lower into typed `bounds:*` operation evidence. Dynamic active-state `PosFreeze x = var(0), y = var(1)` resolves owner-local vars through the runtime expression context, records typed `bounds:posfreeze` telemetry after resolution, and reaches actor-frame/final `posFreezeX = true`, `posFreezeY = false` telemetry. Dynamic active-state `ScreenBound value = var(0), movecamera = var(1),var(2)` now resolves owner-local vars through the same runtime fallback, records typed `bounds:screenbound` telemetry after resolution, and reaches actor-frame plus stage-frame clamp/camera evidence. `RuntimeBoundsControllerWorld` owns bounded passive controller setup from typed ops or raw params, and `RuntimeActorConstraintWorld` owns current one-frame reset/projection, stage clamping, and body-push interaction. Exact camera behavior, exact tick order, pause/hitpause/helper/team ownership, and exact screen-edge parity remain future work. |
| `Gravity` | compiled, executed-partial | `synthetic-imported-gravity.json` checksum `92161764`, Common1 fall traces, focused `KinematicControllerSystem` tests | Static `Gravity` lowers into typed `kinematic:gravity` operation evidence and updates bounded vertical velocity telemetry in an airborne imported state through `RuntimeKinematicControllerWorld`. Exact `yaccel` constants, floor snap, pause/tick order, and full air-physics parity remain future work. |
| `Width` | compiled, executed-partial | unit tests, `synthetic-imported-width.json`, `synthetic-imported-width-dynamic.json` checksum `51554c91` / final checksum `84a85277` | Static `player` / `value` params lower into typed `collision:width` operation evidence; `RuntimeActorConstraintControllerDispatchWorld` owns active-state controller telemetry, typed operation selection, operation telemetry, and body-width mutation handoff into `RuntimeActorConstraintWorld`. Dynamic active-state `player` / `value` expression fallback now resolves owner-local vars through the same active expression context, records typed `collision:width` telemetry after resolution, and reaches body-width telemetry `front = 21`, `back = 43`. Edge widths, exact player/edge semantics, team/helper push rules, and tick-order parity remain future work. |
| `Height` | compiled, executed-partial | focused compiler, actor-constraint, active-dispatch, active-motion, match-runtime, and root-body-push tests | Static `value = top[,bottom]` lowers into typed `collision:height`; omitted bottom defaults to zero and dynamic pairs resolve in caller context. Root `RedirectID` mutation scales the caller-owned result into the target localcoord. `RuntimeActorConstraintWorld` owns the one-frame delta and `RuntimeRootBodyPushWorld` composes it over current S/C/A/L size-box Y before bounded OverrideClsn Size projection for PlayerPush admission. Exact hitpause persistence, helpers/broad redirects, `P2BodyDist Y`, other Height consumers, and full parity remain future work. |
| `OverrideClsn` | compiled, executed-partial | focused compiler, collision-override, frame, active-dispatch, match-runtime, and root-body-push tests | Static and dynamic groups None/Clsn1/Clsn2/Size support ordered clear/replace/append/delete modifiers, caller-owned expression evaluation, root RedirectID localcoord scaling, and reset even during hitpause. Clsn2 and size project into current contact/PlayerPush geometry; Clsn1 projects over the complete current AIR box set into snapshots and controllers that read it after the override. Exact Clsn1-after-HitDef/ReversalDef ordering, helpers, proxies, transforms, projectiles, broad redirects, collision triggers, and full parity remain future work. |
| `StateTypeSet` | compiled, executed-partial | unit tests, focused `StateTypeSystem` tests, `synthetic-imported-statetypeset.json`, `synthetic-imported-statetypeset-dynamic.json` checksum `577404e4` / final checksum `083a76de`, optional `kfm-official-auto-guard-start.json`, optional `kfm-official-auto-guard-end.json` | Static `statetype` / `movetype` / `physics` params lower into typed `metadata:statetypeset` operation evidence; `RuntimeStateTypeWorld` owns bounded metadata setup from typed ops or raw params and updates actor-frame state metadata. Dynamic active-state enum expressions such as `IfElse(var(0), C, S)` now resolve through runtime expression fallback, record typed `metadata:statetypeset` telemetry after resolution, and reach actor-frame/final `C/A/N` metadata in the required trace. Optional official KFM auto guard-start/end gates now require `StateTypeSet` plus typed metadata in real Common1 state `120`. Broad string-param parity, helper/team/redirect ownership, exact physics/tick-order interactions, and full engine parity remain future work. |
| `PlayerPush` | compiled, executed-partial | unit tests, focused `BoundsControllerSystem` / `RuntimeRootBodyPushSystem` tests, `synthetic-imported-playerpush.json`, `synthetic-imported-playerpush-dynamic.json` checksum `b7775652` / final checksum `92aca1cd`, `synthetic-imported-ikemen-playerpush-policy.json` | Static/dynamic value, priority, AffectTeam, and optional root RedirectID are preserved. `RuntimeRootBodyPushWorld` selects state size-box X/Y, composes bounded one-frame Width/Height and OverrideClsn Size geometry, requires current projected Clsn2 unless SizePushOnly, applies team/priority/weight/pushfactor policy to X/Z, clamps legacy MUGEN widths to five world units, and resolves exact X-center ties by priority, hit/attack state, scaled Y, and facing. Required policy trace proves size-box-weighted displacement `-2.75/43`. Duplicate run-order visitation, Z ties/corners, interpolation, proxies, negative/zero Width, helpers, exact pause/reset timing, rendering, and full parity remain future work. |
| `Turn` | compiled, executed-partial | unit tests, `synthetic-imported-turn.json` | Static `Turn` lowers into typed `orientation:turn` operation evidence and flips the bounded current runtime facing. Exact auto-facing, tick order, team/helper/target-facing parity, and broader orientation semantics remain future work. |
| `SprPriority` | compiled, executed-partial | unit tests, `synthetic-imported-sprpriority.json`, `synthetic-imported-sprpriority-dynamic.json` checksum `a9e0862d` / final checksum `4919326d` | Static `value` / `priority` params lower into typed `sprite-effect:sprpriority` operation evidence and update bounded renderer ordering telemetry through `RuntimeSpriteEffectWorld`; dynamic active-state `value` expression fallback now resolves owner-local vars through the sprite-effect boundary, records typed `sprite-effect:sprpriority` telemetry after resolution, and reaches actor-frame/final `spritePriority = 4`. Dynamic typed lowering for other sprite-effect params plus exact layer/shadow/helper/Explod draw-order parity remain future work. |
| `PalFX` | compiled, executed-partial | unit tests, `synthetic-imported-palfx.json`, `synthetic-imported-palfx-dynamic.json` checksum `36cdca15` / final checksum `7a1a4525`, `synthetic-imported-palfx-remappal.json` checksum `ba5fc1e6` | Static `time` / `add` / `mul` / `color` / `invert` params lower into typed `sprite-effect:palfx` operation evidence and update bounded material telemetry through `RuntimeSpriteEffectWorld`; dynamic active-state `time` / `add` / `mul` / `color` / `invertall` expressions now resolve owner-local vars through the sprite-effect boundary, record typed `sprite-effect:palfx` telemetry after resolution, and reach bounded `paletteFx` telemetry. The combined gate proves same-actor coexistence with `RemapPal` telemetry. `sinadd`, exact palette remap/blend ordering, exact interaction with indexed ACT/SFF remap beyond existing bounded handoff, and engine-perfect color math remain future work. |
| `Trans` | compiled, executed-partial | unit tests, `synthetic-imported-trans.json`, `synthetic-imported-trans-dynamic.json` checksum `4bffcd82` / final checksum `5beea0f0` | Static `trans` / `value` params and static `alpha` lower into typed `sprite-effect:trans` operation evidence and update bounded render-opacity telemetry; dynamic active-state `alpha` expression fallback can resolve owner-local vars through the sprite-effect boundary, record typed `sprite-effect:trans` telemetry after resolution, and reach `renderOpacity = 0.375` evidence. Exact add/sub alpha math, palette/remap interaction, draw-order interaction, and engine-perfect blending remain future work. |
| `AngleSet`, `AngleAdd`, `AngleMul`, `AngleDraw` | compiled, executed-partial | unit tests, `synthetic-imported-angle.json`, `synthetic-imported-anglemul.json` checksum `e0dae072`, `synthetic-imported-anglemul-dynamic.json` checksum `0bb54a1c` / final checksum `c9f2b557`, `synthetic-imported-angle-dynamic.json` checksum `13560dcd` / final checksum `4d7c4726` | Static and resolved dynamic `value` params for `AngleSet`/`AngleAdd`/`AngleMul` and `AngleDraw value/scale` lower into typed `sprite-effect:angleset` / `sprite-effect:angleadd` / `sprite-effect:anglemul` / `sprite-effect:angledraw` operation evidence, and `RuntimeSpriteEffectWorld` exposes bounded render-angle/render-scale telemetry consumed by Three.js. The static `AngleMul` gate proves `30 * 1.5 -> renderAngle 45` with typed op evidence; the dynamic `AngleMul` gate proves `AngleSet value = var(0)` plus `AngleMul value = fvar(0)` records typed evidence and reaches the same `renderAngle = 45`. Dynamic active-state `AngleSet value`, `AngleAdd value`, and `AngleDraw value/scale` also record typed evidence and reach `renderAngle = 35` / `renderScale = 2,0.5`. Exact axis pivot, collision-box rotation/scale, palette/draw-order interaction, AfterImage trail blending/cadence, and engine-perfect timing remain future work. |
| `EnvColor` | compiled, executed-partial | unit tests, `synthetic-imported-envcolor.json`, `synthetic-imported-envcolor-under.json`, `synthetic-imported-envcolor-dynamic.json` checksum `845c3d5e` / final checksum `282fc77f` | Static `value` / `time` / `under` params lower into typed `envcolor` operation evidence and update bounded stage-flash telemetry/render overlay through `RuntimeEnvColorWorld`; required traces cover both `under = 0` and `under = 1`. Dynamic `value` / `time` / `under` params resolve through active controller expression fallback, record typed `envcolor` operation evidence after resolution, and emit bounded stage-frame telemetry `32,128,240` / `under = true`. Exact layer/window/blend behavior, pause timing, renderer parity, helper/redirect ownership, and engine-perfect presentation parity remain future work. |
| `RemapPal` | compiled, executed-partial | unit tests, `synthetic-imported-remappal.json`, `synthetic-imported-remappal-dynamic.json` checksum `5f04f2d4` / final checksum `71ad06f0`, `synthetic-imported-palfx-remappal.json` checksum `ba5fc1e6`, focused `ActParser`, `SffParser`, `SffSpriteProvider`, `CharacterRenderer`, `MugenCharacterLoader`, and `CompatibilityReport` tests | Static `source` / `dest` params lower into typed `sprite-effect:remappal` operation evidence and update bounded palette-remap telemetry through `RuntimeSpriteEffectControllerWorld` / `RuntimeSpriteEffectWorld`; dynamic active-state source/dest expression fallback can resolve owner-local vars through the sprite-effect boundary, record typed `sprite-effect:remappal` telemetry after resolution, and reach bounded palette-remap telemetry. The combined gate proves same-actor coexistence with `PalFX` telemetry. The loader now parses DEF `pal1..pal12` ACT refs, indexed SFF decoders preserve palette-index pixels, and the renderer/provider can rebuild decoded indexed SFF sprite canvases with a loaded ACT destination palette. Exact source-bank semantics, truecolor/PNG remap, helper/team/redirect ownership, exact PalFX interaction math/order, and full palette parity remain future work. |
| `CtrlSet` | compiled, executed-partial | `synthetic-imported-control.json` checksum `80c4c446`, `synthetic-imported-control-dynamic.json` checksum `885cc464` / final checksum `ecf2bec6`, guard-state traces, `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641`, `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c`, `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699`, `synthetic-imported-air-guard-landing.json` checksum `d6986d7f`, optional `kfm-official-default-guard-slide-stop.json` / `kfm-official-default-guard-hold-return.json` / `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da`, optional `kfm-official-default-crouch-guard-slide-stop.json` / `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` | Static numeric values lower into typed `resource:ctrlset` operation evidence and update the bounded owner control flag. Bounded dynamic active-state `value = IfElse(var(0), 1, 0)` now resolves owner-local expressions through runtime fallback, records typed `resource:ctrlset` telemetry after resolution, keeps actor-frame state/action evidence in the required trace, and leaves final owner control true. The stand and crouch guard slide-stop gates require `CtrlSet` after `VelSet` and before the final `ChangeState` in states `151` and `153`, including optional private KFM fixture routes; the required synthetic stand guard-hold walk-return gate preserves that ordered control handoff before proving final controlled walk state/action `20`, the required synthetic crouch guard-hold crouch-return gate preserves the same ordered control handoff before proving final controlled crouch state/action `10`, optional KFM walk-return mirrors the stand route, and optional KFM crouch-return proves final real KFM crouch state/action `11` control after `131`. The required air guard landing gate requires `155` `CtrlSet` before landing `VelSet`/`PosSet`, state-`52` handoff, and final controlled walk state/action `20`. Exact state-entry/control timing, persistent-controller timing, helper/team/redirect ownership, broad dynamic resource lowering, and full control semantics remain future work. |
| `LifeAdd`, `LifeSet`, `PowerAdd`, `PowerSet` | compiled, executed-partial | `synthetic-imported-resource.json` checksum `7bbcb2e4`, unit tests/runtime traces | Static numeric resource controllers lower into typed `resource:*` operation evidence, can mutate owner life/power, expose life `750` and power `900` through required actor-frame evidence, and can drive later `Life` / `Power` branches. Negative `LifeAdd` respects bounded `kill = 0` and defender-side `NoKO` clamps; target-controller negative damage has separate bounded evidence through `synthetic-imported-target-noko.json`. Scaling, redirects, round-flow, KO semantics, helper/team ownership, and edge rules remain partial. |
| `VarSet`, `VarAdd`, `VarRandom`, `VarRangeSet` | compiled, executed-partial | `synthetic-imported-variable.json` checksum `3b33f7a8`, unit tests/runtime traces, `RuntimeRandomSystem` coverage | Static `VarSet`, `VarAdd`, `VarRandom`, and `VarRangeSet` lower into typed `variable:*` operation evidence and can drive local `var(...)` branches; `VarRandom` writes owner-local int vars from bounded inclusive ranges using deterministic sandbox RNG now owned by `RuntimeRandomSystem`, while typed/raw `VarSet` and `VarAdd` support `sysvar(n)` for bounded Common1 system-variable branches such as air guard landing. Exact variable scopes, helper/parent/root redirects, exact MUGEN random stream parity, IKEMEN map vars, and full VM parity remain partial. |
| `PlaySnd`, `SndPan`, `StopSnd` | compiled, executed-partial | `synthetic-imported-sound.json` checksum `cc9c8c49`, `synthetic-imported-hitdef-hit-sound.json` checksum `6fc00d8a`, `synthetic-imported-hitdef-guard-sound.json` checksum `fdf1f7f6`, `synthetic-imported-hitdef-dynamic-hitsound.json` checksum `fe3c0f3d` / final checksum `855df386`, `synthetic-imported-hitdef-dynamic-guardsound.json` checksum `bb38362a` / final checksum `3e0ddeb0`, `synthetic-imported-projectile-contact.json` trace checksum `57b3b556` / final checksum `e0f3e41c`, `synthetic-imported-projectile-guard.json` trace checksum `eb9c2e58` / final checksum `b1c74e5e`, `synthetic-imported-helper-projectile-guard-ko.json` checksum `05dbcded` / final checksum `98b8bf17`, `synthetic-imported-helper-projectile-guard-kill.json` checksum `33930a00` / final checksum `8412e638`, `synthetic-imported-helper-projectile-guard-terminal.json` checksum `c6937f42` / final checksum `e0835e33`, `synthetic-imported-projectile-gethitvar-hit-metadata.json` checksum `8e5df79b` / final checksum `4d078c5d`, `synthetic-imported-projectile-gethitvar-hitid-chainid.json` checksum `4356b5cb` / final checksum `4b270d45`, `synthetic-imported-projectile-gethitvar-hitcount.json` checksum `df2619f9` / final checksum `5469bc69`, `synthetic-imported-helper-projectile-gethitvar-hit-metadata.json` checksum `28afbcea` / final checksum `c960b1cf`, `synthetic-imported-helper-projectile-gethitvar-hitid-chainid.json` checksum `616e0b2c` / final checksum `0aebcc73`, `synthetic-imported-helper-projectile-gethitvar-hitcount.json` checksum `40ec4f4b` / final checksum `6f15ff30`, focused `AudioEventSystem`, `RuntimeCompiler`, `RuntimeContactPresentationSystem`, `MugenAudioSystem`, `PlayableMatchRuntime`, and `EffectActorSystem` tests, runtime events | Static `PlaySnd value = Sgroup,index`, static `PlaySnd lowpriority`, static `PlaySnd volumescale`, legacy static `PlaySnd volume`, static `PlaySnd freqmul`, static `PlaySnd loop`, static `PlaySnd pan`, static `PlaySnd abspan`, static `SndPan channel` plus `pan` / `abspan`, and static `StopSnd channel` lower into typed `audio:playsnd` / `audio:sndpan` / `audio:stopsnd` operation evidence while preserving raw controller fallback for unsupported/dynamic refs. Emits bounded `RuntimeSoundEvent` evidence with parsed `Sgroup,index`, channel, `lowPriority`, `volumeScale`, `legacyVolume`, `freqMul`, `loop`, `pan`, `absPan`, state, and tick data through `RuntimeAudioWorld` for players and through `HelperSystem` for current visual Helper actors running the helper-local micro-VM; the required imported sound trace now gates `PlaySnd abspan = -64` as `absPan: -64`; direct `HitDef hitsound` / `guardsound` can now record bounded contact `audio:playsnd` telemetry after hit/guard contact, bounded player-owned `Projectile hitsound` / `guardsound` can record typed contact `audio:playsnd` telemetry, bounded first-generation helper-parented/root-owned `Projectile guardsound` can record typed guard-contact `audio:playsnd` telemetry, and the three bounded player-owned and helper Projectile normal-hit GetHitVar `hitsound` refs can record typed hit-contact `audio:playsnd` telemetry while preserving package metadata after contact. `MugenAudioSystem` now scans player and effect actor snapshots so helper-local sound telemetry can reach the browser audio adapter, replaces active explicit channels for normal `PlaySnd`, skips active explicit channels for `lowpriority = 1`, applies `SndPan` to active explicit channel panners, treats `StopSnd channel = -1` / omitted channel as stop-all for tracked channelled and unchannelled sources, applies `volumescale` as bounded gain scaling, ignores legacy `volume` during modern playback while keeping diagnostics, applies `freqmul` as bounded playback-rate scaling, maps `loop = 1` to source looping, and applies bounded actor/camera-aware `pan` plus absolute `abspan` stereo panning in the tracked Web Audio path. Exact Web Audio/SND channel parity, FightFX/system channel fallback, broad dynamic sound expressions, pre-RC8 `volume` gain semantics, priority classes, broader helper/redirect ownership, broader helper Projectile normal-hit sound breadth, pause/superpause audio behavior, and exact timing/mixing remain partial. |
| `AfterImage`, `AfterImageTime` | compiled, executed-partial | unit tests, `synthetic-imported-afterimage.json`, `synthetic-imported-afterimage-dynamic.json` trace checksum `e7299ac5` / final checksum `b946d805`, `synthetic-imported-afterimagetime-dynamic.json` checksum `c5ef6fff` / final checksum `661a233d` | Static `AfterImage time/length/timegap/framegap/paladd/palmul/trans` and static `AfterImageTime time/value` params lower into typed `sprite-effect:afterimage` / `sprite-effect:afterimagetime` operation evidence and update bounded ghost-trail telemetry through `RuntimeSpriteEffectWorld`; dynamic active-state `AfterImage time/length/timegap/framegap/paladd/palmul` and `AfterImageTime value/time` expression fallback can resolve owner-local vars through the sprite-effect boundary and record typed sprite-effect telemetry after resolution. `RuntimeAfterImageSampleWorld` owns actor/frame sample projection. Exact blend modes, palette/remap interaction, sampling cadence, persistence edge cases, and engine-perfect timing remain future work. |
| `EnvShake` | compiled, executed-partial | `synthetic-imported-envshake.json` checksum `061f17d5`, `synthetic-imported-envshake-dynamic.json` checksum `e1bf593f` / final checksum `8f52f1f4`, focused compiler/runtime/trace tests, runtime events | Static numeric `time`/`freq`/`ampl`/`phase` params lower into typed `envshake` operation evidence before emitting bounded `RuntimeEnvShakeEvent` telemetry; dynamic active-state params can resolve through the active controller expression fallback, record typed `envshake` operation evidence after resolution, and emit bounded runtime env-shake telemetry. `RuntimeEnvShakeWorld` owns event insertion plus deterministic camera-shake projection. `mul`, exact pause/stage/layer interaction, helper/redirect ownership, screenpack ownership, and engine-perfect waveform parity remain future work. |
| `Null`, `ForceFeedback`, `DisplayToClipboard`, `AppendToClipboard`, `ClearClipboard`, `MakeDust`, `DestroySelf` | compiled noop / helper-local partial | `synthetic-imported-noop.json` checksum `2877b222`, compatibility reports, focused `EffectActorSystem` helper-local VM tests | Accepted imported active-state CNS no-op executions lower into typed `noop:*` operation evidence: `Null` has no side effects, `ForceFeedback` is a browser no-op, debug clipboard controllers are accepted/countable without output mutation, deprecated `MakeDust` is accepted/countable as presentation no-op, and `DestroySelf` is accepted/countable as helper-lifecycle no-op only for player active states. Inside the bounded helper-local micro-VM, `DestroySelf` removes the current visual Helper actor. Device force feedback, debug text rendering, clipboard output, dust rendering, helper redirects/parent-root ownership, helper combat side effects, persistent/no-op timing edge cases, and exact VM ordering remain unsupported. |

## Current Explod Param Notes

`removeongethit` is `executed-partial` for owner-side visual Explods. It compiles into `ExplodControllerOp.removeOnGetHit`, falls back to raw params when needed, and is trace-gated by `synthetic-imported-explod-removeongethit.json`, `synthetic-imported-explod-removeonprojectilehit.json`, and `synthetic-imported-explod-removeonprojectileguard.json`.

`ignorehitpause`, `pausemovetime`, and `supermovetime` are `executed-partial` for visual Explod actor advancement. They compile into `ExplodControllerOp`, fall back to raw params when needed, `synthetic-imported-explod-ignorehitpause.json` trace-gates the first required hitpause route, `synthetic-imported-explod-pausemovetime.json` trace-gates the first required regular `Pause` budget route, and `synthetic-imported-explod-supermovetime.json` trace-gates the first required `SuperPause` budget route.

`ModifyExplod` is `executed-partial` for owner-side visual Explods. It compiles into `ModifyExplodControllerOp`, falls back to raw params when needed, and is trace-gated by `synthetic-imported-modifyexplod.json` checksum `bca75991` for bounded player-state live mutation of velocity, acceleration, scale, priority, remove time, pause budgets, and remove-on-get-hit telemetry. `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` adds the bounded helper-local route where a current visual Helper mutates only its helper-parented owner-side Explod by static id.

Do not describe this as full remove-trigger or pause parity. Unsupported scope remains helper-bound Explod timing beyond the bounded static helper-local spawn/remove/modify-id route, helper-owned effect namespaces, dynamic helper-local effect params, position rebinding, custom-state edge cases, exact tick order, exact pause layering, and broader FightFX/common animation routing.

## Adding A Controller

Use this path:

```txt
CNS parser
  -> model/controller params
  -> compiler support
  -> typed ControllerOp when mutating runtime
  -> small runtime system or executor path
  -> compatibility report counts
  -> trace artifact evidence
  -> docs/support table update
```

## Controller Entry Template

When adding or upgrading a controller, record:

```md
### ControllerName

- Support level:
- Parser/model fields:
- Compiled IR:
- Typed operation:
- Runtime system:
- Supported params:
- Ignored params:
- Unsupported params:
- Trace artifacts:
- Fixtures:
- UI wording:
- Remaining parity gaps:
```

## Hard Rules

- A recognized controller is not executable support.
- A compiled controller is not runtime support until a trace proves execution.
- A runtime path with ignored params is `executed-partial`.
- New broad controller behavior should not go directly into `PlayableMatchRuntime` unless it is a bounded bridge with a follow-up extraction note.
- If a typed operation exists, trace gates should require `executedOperations`.

Depth controller addendum: `Depth` is now `compiled, executed-partial` for self-owned imported playable and explicit active-motion Tag roots. Static and dynamic `player`, `edge`, and fallback `value` pairs emit typed `collision:depth` operations; player values add to base contact depth, edge values narrow stage Z bounds, value applies both, omitted second values default to zero, and frame reset restores base player depth plus zero edge depth. Required `synthetic-imported-ikemen-depth-controller.json` checksum `bd050230` / final checksum `0a6abca8` proves the combined route after depth player-push promotion; trace QA passes 561/561 artifacts, 530 required. `redirectid`, helpers, exact hitpause persistence, rendering, and full parity remain unsupported.

ScreenBound stagebound addendum: optional static/dynamic `stagebound` now composes independently with existing `value` and `movecamera`, preserves earlier same-frame state when omitted, resets enabled per frame, and controls logical-Z stage clamp for playable and explicit active-motion Tag roots. Required `synthetic-imported-ikemen-screen-stagebound.json` checksum `ea905cc9` / final checksum `0efa02b0` proves P3 can opt out of stage Z clamp and remain outside P4 contact depth after depth player-push promotion; trace QA passes 561/561 artifacts, 530 required. Exact X stage/screen separation, `redirectid`, helpers, hitpause timing, rendering, and full parity remain unsupported.

Depth player-push addendum: `RuntimeRootBodyPushWorld` now uses current body widths plus `combatDepth.size`, character localcoord scaling, and IKEMEN's normalized `0.75` axis-selection threshold to resolve bounded symmetric X, Z, or combined push for eligible roots, followed by X/Z stage reclamp. Required `synthetic-imported-ikemen-depth-player-push.json` checksum `5870f7de` / final checksum `cf226647` proves depth-dominant P3/P4 overlap moves only Z to `-5/5` without combat; trace QA passes 561/561 artifacts, 530 required. Clsn/Y admission, priority, weight, pushfactor, AffectTeam, helpers, interpolation, exact ties/tick order, and full parity remain unsupported.

Depth/ScreenBound RedirectID addendum: optional static/dynamic RedirectID expressions now resolve through the IKEMEN PlayerID registry and route `Depth` or `ScreenBound` from playable and active Tag callers to live roots across normal, paused, and hitpause-ignored controller paths. Invalid, negative, missing, helper, and non-IKEMEN destinations fail closed. Required `synthetic-imported-ikemen-depth-bounds-redirect.json` checksum `ca32f0ae` / final checksum `37b3126b` proves P4 operations are attributed to P3 while `redirectid = 999` emits no controller/operation; trace QA passes 562/562 artifacts, 531 required. Helper targets, exact dynamic payload context, recursive redirects, broader controller coverage, exact pause/reset order, and full parity remain unsupported.
