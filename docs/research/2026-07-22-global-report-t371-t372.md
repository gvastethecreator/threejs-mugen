# Runtime implementation report: T371-T372

Date: 2026-07-22

Feature commits: `54c1e980`, `3f5f4a5`

## Status

T371 and T372 close a bounded PlayerPush RedirectID batch. This report records
focused runtime evidence only. It is not a global quality checkpoint.

## Delivered

- Current first-generation Helpers materialize static or dynamic PlayerPush
  policy in their caller before verified RedirectID writeback.
- A later Helper target retains an incoming enabled, priority, and team policy
  through its one-frame reset.
- Root PlayerPush RedirectID materializes dynamic policy in the caller before
  dispatch and defers a later-root write until after the destination reset.
- Required imported Helper and root trace artifacts exercise both redirect
  routes and assert typed `collision:playerpush` telemetry with destination
  push disablement.

## Evidence

- Focused runtime suite: 5 files / 1034 tests pass.
- Diff hygiene: pass before each runtime feature commit.
- Required trace artifacts: pass inside the focused trace preset suite.

## Deferred checkpoint gates

The current batch policy defers TypeScript, complete Vitest, the full trace
aggregate, build, and boundary checks until several more runtime slices
accumulate. No global green claim follows from this report. Browser smoke is
N/A because this batch changes no renderer or Studio surface.

## Global state

Compatibility scores remain 65 / 36 / 20 / 10-12 / 6-8 / 25. The next
constraint audit is root ScreenBound RedirectID caller evaluation and target
reset order.

## Claim ceiling

Allowed: bounded current Helper and root PlayerPush RedirectID policy with
caller-owned dynamic values under the current actor order.

Blocked: exact CharList scheduling, hitpause/reset parity, collision/corner
geometry, nested ownership, renderer/upstream differentials, rollback/netplay,
score movement, and full MUGEN/IKEMEN parity.
