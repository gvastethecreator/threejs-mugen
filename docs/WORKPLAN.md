# Workplan

This is the operating ledger for building the whole approved direction without losing the playable prototype. It is intentionally narrower than the vision docs: if a task is not here, it is either already done, blocked by a gate, or belongs in a later planning update.

Authoritative horizon docs:

- `APPROVED_HORIZON_PLAN.md`: scope contract and milestone order.
- `HORIZON_IMPLEMENTATION_BLUEPRINT.md`: cross-cutting orchestration blueprint for building every accepted idea through one evidence chain.
- `MASTER_CONSTRUCTION_PLAN.md`: broad release train.
- `CONSTRUCTION_WAVES.md`: practical wave plan with build packages, dependencies, acceptance gates, anti-claims, and next rounds.
- `ROADMAP_PROGRESS_SYSTEM.md`: source-of-truth stack, package lifecycle, horizon ladder, update matrix, and closeout template.
- `ROADMAP_PACKAGE_MILESTONES.md`: compact package ladder, milestone exits, next recommended slice, and package closeout ownership.
- `ROADMAP_RELEASE_TARGETS.md`: release-train targets, usable milestone gates, and score-movement rules.
- `ROADMAP_EXECUTION_BOARD.md`: current queue, package acceptance, and handoff checklist.
- `WORKPLAN.md`: current execution ledger.
- `ARCHITECTURE_DECISIONS.md`: decisions that constrain implementation.
- `COMPATIBILITY_PROFILES.md`: normative profile split for native runtime, MUGEN 1.0/1.1, IKEMEN scan-only, and future modules.
- `CONTROLLER_SUPPORT_REGISTRY.md`: controller support levels, typed-operation expectations, evidence, and partial-support wording.
- `TICK_ORDER_CONTRACT.md`: frame/tick order contract and trace requirements for behavior changes.
- `FIXTURE_GOLDENS.md`: required/optional fixture artifacts, golden update rules, and claim map.
- `STAGE_COMPATIBILITY_MATRIX.md`: imported stage feature levels and Stage Studio gates.
- `GENERATED_ASSET_QA_CONTRACT.md`: generated asset provenance, motion/scale/baseline QA, and regeneration rules.
- `MODULE_BOUNDARY_CONTRACT.md`: shared-core, fighting-module, and future-module boundaries.

## Current Build Rule

Do not add more product surface area unless it carries real runtime, asset, build, or evidence data. The spine stays:

```txt
RuntimeTrace
  -> ControllerOp / Runtime IR
  -> MatchWorld ownership
  -> KFM/Common1 gates
  -> stage/audio/presentation IR
  -> evidence-first Creator Studio
  -> generated authoring
  -> IKEMEN scanner
  -> shared module contracts
  -> first platformer slice
```

Construction lock for the expanded horizon:

- Build all approved ideas, but only call a slice real when it has executable evidence.
- Near-term wording is **partial MUGEN compatibility with fixture gates** plus **IKEMEN profile scanning**, not full IKEMEN execution.
- The normative compatibility sentence is **partial MUGEN 1.1 fixture-backed runtime, native generated roster, IKEMEN scanner only**.
- Studio work is allowed when it reveals real assets, traces, reports, build status, or runtime state.
- Generated assets are native/authored pipeline proof and must never inflate imported MUGEN compatibility.
- Modular-engine work remains planned until fighting runtime contracts are clean enough to extract.

Operational queue lock:

- Use `docs/ROADMAP_EXECUTION_BOARD.md` to pick the next focused package.
- Use `docs/ROADMAP_PACKAGE_MILESTONES.md` to confirm the active package ladder and default next code slice after docs/setup work.
- Use `docs/ROADMAP_RELEASE_TARGETS.md` to keep "usable MVP" wording tied to gates instead of vibes.
- Use `docs/ROADMAP_PROGRESS_SYSTEM.md` when deciding which docs must change and what closeout evidence is required.
- Do not start a package unless its linked `.scratch/roadmap/issues/` file has acceptance and blocked claims.
- Docs-only roadmap passes may improve handoff clarity, but they do not raise progress scores.

## 2026-07-02 Runtime Evidence Note

FightFX/audio prefix support is now bounded but no longer metadata-only: imported DEF `[Info] fightfx.prefix` flows into runtime `fightFxPrefix`, F-prefixed FightFX asset frames, `HitSpark` events, F-prefixed sound refs, `soundPrefix` audio/trace telemetry, and `RuntimeTrace.requiredHitEffectEvents`; character `[Files] fx = ...` entries can load IKEMEN-style FightFX DEF `[Info] prefix` packages with AIR/SFF/SND assets; and the matching prefixed package is preferred for runtime `F` spark frames and `F` sound refs before global `data/fightfx.*` fallback. Static `PlaySnd lowpriority = 1`, `volumescale = 50`, `freqmul = 0.5`, `loop = 1`, `pan = 32`, and static `SndPan channel = 2, pan = -48` also survive compiler/runtime/trace handoff through `RuntimeSoundEvent.lowPriority` / `volumeScale` / `freqMul` / `loop` / `pan`; focused tests prove static `abspan` lowering/event projection. The partial Web Audio route skips replacing an already active explicit channel for low-priority sounds, preserves `StopSnd channel = -1` / omitted-channel stop-all behavior for tracked channelled and unchannelled sources, applies bounded `volumescale` gain scaling, applies bounded `freqmul` playback-rate scaling, enables source looping, updates active channel panners through `SndPan`, and resolves `pan` / `abspan` through a bounded stereo-pan boundary. `synthetic-imported-hitdef-fightfx-spark.json` remains checksum `11537b56`, `synthetic-imported-hitdef-hit-effect-package.json` remains checksum `46aa5ce1`, and `synthetic-imported-sound.json` now has checksum `91574367` while requiring `lowPriority: true`, `volumeScale: 50`, `freqMul: 0.5`, `loop: true`, `pan: 32`, and `SndPan pan: -48`. This is bounded package-selection, prefixed-SND lookup, explicit-channel arbitration, `volumescale`, `freqmul`, `loop`, `pan`, `abspan`, and `SndPan` support only; exact `sys.ffx` lifetime/refcount/cache semantics, legacy `volume`, dynamic pan params, global channel fallback, screenpack/motif ownership, renderer/audio timing/layering/scale/palette/mixing parity, score movement, and full hit-effect/audio parity remain blocked.

The latest required Common1 trace is now `synthetic-imported-hitfall-recover-true.json` checksum `f1e3424a`: it runs in the current `pnpm qa:trace` aggregate of 280/280 artifacts, 255 required and 25 optional, and proves bounded synthetic recover-enabled-but-not-yet-recoverable fall routing through `5000 -> 5030 -> 5050 -> 5240 -> 0` after a fall `HitDef` with `fall.recover = 1` and no `p2stateno`, with named `HitFall Recover Enabled Probe`, ordered `HitVelSet` / `kinematic:hitvelset` / `VelAdd` / `ChangeState` evidence, positive `fall.recovertime` metadata in `5050` and `5240`, and forbidden recovery states `5210`/`5200`. This is bounded recover-enabled trigger/order evidence only; exact recovery threshold tables, exact Common1 controller-loop timing, recovery arbitration, fall/CanRecover precedence, score movement, and full fall/recovery parity remain blocked. The previous recover-disabled trace remains `synthetic-imported-hitfall-recover-false.json` checksum `236df0a8`; the previous required normal trigger trace remains `synthetic-imported-hitfall-false.json` checksum `1d538e43`; the previous required fall-trigger trace remains `synthetic-imported-hitfall-canrecover.json` checksum `7cf7ab46`; the previous required Common1 guard trace remains `synthetic-imported-air-guard-landing.json` checksum `d6986d7f`.

The required imported `EnvShake` trace gate is restored in `pnpm qa:trace`: `synthetic-imported-envshake.json` checksum `061f17d5` now runs in the current aggregate 280/280 artifacts, 255 required and 25 optional after later Common1 gates. This gate proves bounded imported `EnvShake` controller telemetry, typed `envshake` operation evidence, and `RuntimeEnvShakeEvent` output for P1 state `200`. It is evidence-pipeline restoration only; exact waveform, pause/stage/layer interaction, helper/redirect ownership, visual/audio parity, score movement, and full MUGEN/IKEMEN presentation parity remain blocked.

## 2026-06-25 Construction Consensus

The latest architecture, product, and modularity review accepts the full horizon, but only as an ordered construction program:

| Track | Build Now | Parallel Work Allowed | Blocked Until | Acceptance |
| --- | --- | --- | --- | --- |
| Runtime spine | `RuntimeTrace -> ControllerOp -> MatchWorld` ownership, with `PlayableMatchRuntime` shrinking over time. | Narrow controller/system cuts that add trace evidence and do not hide raw-controller debt. | Broad CNS semantics that bypass typed operations or world systems. | Stable trace checksums or documented intentional drift; missing evidence fails loudly. |
| Fixture compatibility | Official KFM/Common1 gates for command routing, hit/guard, get-hit, fall/recovery, state exit, and guard rules. | Synthetic regression traces and optional fixture packaging. | Claims like "KFM works" or "MUGEN compatible" without an actual fixture artifact. | Each claim names fixture, state chain, controllers, support level, checksum, and remaining gaps. |
| Creator Studio | Actionable status contract, Evidence persistence, Build authority, Asset repair flow, Character/Stage previews. | UI planning and diagnostic surfaces that bind to current project/runtime/evidence data. | Advanced editors that cannot save, compile, playtest, export, or explain blocked state. | Every `ok`, `partial`, `warn`, `blocked`, and `unsupported` state has affected item, impact, evidence, and next action. |
| Generated assets | Prompt/source provenance, regenerated source sheets when motion/scale is bad, atlas QA, collisions, playtest evidence. | New original fighters/stages as native generated assets. | Treating generated assets as imported MUGEN compatibility or fixing bad source motion by cropping. | Prompt -> source -> atlas -> QA -> collisions -> runtime trace is visible in Studio. |
| IKEMEN profile | Scanner/reporting for IKEMEN-only files, ZSS, Lua, config, stage/system features. | Source research and report schema work. | ZSS/Lua execution, rollback, netplay, or IKEMEN-specific runtime behavior. | IKEMEN-only content is recognized as scanner/report evidence, not runtime support. |
| Shared core | Identify candidate contracts only after the fighting seams prove them: project, assets, input, tick, snapshots, render/audio/debug/build/QA. | Read-only Module Studio status and import-boundary notes. | A platformer runtime or generic SDK. | Shared code contains no CNS, CMD, HitDef, Common1, rounds, helpers, targets, or MUGEN command routing. |
| Platformer slice | Planning only. | Tiny design notes tied to future shared contracts. | `MatchWorld`, Build, Evidence, and import-boundary gates pass. | A later scene runs from project/build data while fighting smoke/trace still pass. |

This means the immediate construction order is:

```txt
1. Runtime spine and fixture evidence
2. Studio actionable truth and persisted evidence
3. Character/Stage diagnostic previews
4. Generated authoring records and QA ingestion
5. IKEMEN scanner/profile reporting
6. Shared module contract extraction
7. Tiny platformer slice
```

The Studio object model for this plan is:

```txt
Project
  -> assets/sourcePackages/modules/entry
  -> compatibility/evidence/buildOutputs
```

`Build` is the authority for runnable/partial/blocked/exportable truth. `Assets`, `Evidence`, `Character`, `Stage`, and `Debug` link back to that truth rather than inventing separate status language.

Latest runtime evidence cut:

- Latest R1 required recover-enabled fall trigger cut: `synthetic-imported-hitfall-recover-true.json` checksum `f1e3424a` proves bounded Common1-style `HitFall && GetHitVar(fall.recover) && !CanRecover` routing. A fall `HitDef` with `fall.recover = 1` and no `p2stateno` routes defender-owned `5000 -> 5030 -> 5050 -> 5240 -> 0`, executes named `HitFall Recover Enabled Probe` `ChangeState`, records ordered actor-frame evidence, preserves positive `fall.recovertime` metadata in `5050` and `5240`, and forbids recovery states `5210`/`5200`. Current `pnpm qa:trace` passes 280/280 artifacts, 255 required and 25 optional. This is synthetic recover-enabled trigger/order evidence only; exact recovery threshold tables, controller-loop tick parity, recovery arbitration, fall/CanRecover precedence, full fall/recovery parity, and score movement remain blocked.
- Previous R1 required recover-disabled fall trigger cut: `synthetic-imported-hitfall-recover-false.json` checksum `236df0a8` proves bounded Common1-style `HitFall && !GetHitVar(fall.recover) && !CanRecover` routing through `5000 -> 5030 -> 5050 -> 5230 -> 0`. It remains required but is no longer the latest runtime checkpoint.
- Previous R1 required normal get-hit trigger cut: `synthetic-imported-hitfall-false.json` checksum `1d538e43` proves bounded Common1-style `!HitFall` / `!GetHitVar(fall)` routing. A direct non-fall `HitDef` without `p2stateno` routes defender-owned `5000 -> 325`, executes named `Normal HitFall False Probe` `ChangeState`, records ordered actor-frame evidence, final P2 state `325` / moveType `H`, and forbids fall/recovery states `5001`, `5030`, `5050`, `5210`, and `5200`. This is synthetic normal get-hit trigger evidence only; exact normal get-hit timing, fall arbitration, custom-state/helper/team inheritance, full Common1 get-hit parity, and score movement remain blocked.
- Previous R1 required fall-trigger cut: `synthetic-imported-hitfall-canrecover.json` checksum `7cf7ab46` proves bounded Common1-style `HitFall` true / `CanRecover` false routing through `5000 -> 5030 -> 5050 -> 5220 -> 0`. This is synthetic fall-trigger evidence only; exact recovery threshold tables, recovery arbitration, controller-loop tick parity, full fall/recovery parity, and score movement remain blocked.
- Latest R2 ownership cut: `RuntimeEffectHelperContextWorld` owns bounded visual Helper lifecycle context construction outside `RuntimeEffectLifecycleWorld`: complete-owner validation, parent/root runtime-state projection, current opponent id/state fallback, explicit lifecycle opponent-list to nearest-order helper `opponentRoster` projection, explicit roster override, target-candidate forwarding, and helper `TargetState` / telemetry hook forwarding now route through one named seam. Focused coverage proves nearest roster ordering, explicit roster preservation, target/hook forwarding, and incomplete-owner fail-closed behavior; existing lifecycle coverage proves active/paused callers still forward the same context. This is helper/effect lifecycle ownership cleanup only; real teams/simul lifecycle roster ownership, automatic multi-opponent match discovery, helper-owned opponent roster discovery, richer identity beyond ids/runtime state, exact helper lifecycle/pause/combat ordering, visual/audio parity, full Helper VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchHelperProjectileTargetWorld` owns bounded match-level helper-parented Projectile target-memory bridge wiring outside `PlayableMatchRuntime`: normal post-fighter combat forwards owner, defender, projectile, and `RuntimeTargetWorld` through one named seam before lower helper target-memory logic runs. Focused coverage proves forwarding and owner-projectile fail-closed behavior. This is helper/projectile ownership cleanup only; helper-owned Projectile contact timing, helper-owned custom-state tables, teams/simul actor registries, multi-target helper ownership, exact target lifetime, visual/audio parity, full Helper/Projectile VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchHelperBindingWorld` owns bounded match-level helper callback wiring outside `PlayableMatchRuntime`: helper-owned `TargetState` owner handlers and helper-local Projectile telemetry handlers attach through one named seam, while target entry still delegates to `RuntimeMatchHelperTargetStateWorld` and telemetry filtering still delegates to `RuntimeHelperTelemetryWorld`. Focused coverage proves owner-specific target-state route forwarding, stale handler replacement, helper-state/owner-state telemetry attribution, and non-Projectile telemetry ignore behavior. This is helper/custom-state ownership cleanup only; helper-owned custom-state tables, throws, teams/simul actor registries, multi-target helper ownership, exact helper TargetState/projectile timing, broad helper telemetry semantics, visual/audio parity, full Helper VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchHelperTargetStateWorld` owns bounded match-roster target resolution for helper-owned `TargetState` entry outside `PlayableMatchRuntime`: helper owner validation still routes through `RuntimeHelperTargetStateWorld`, while roster lookup, state availability, and state entry now sit behind one match-level seam. This is helper/custom-state ownership cleanup only; helper-owned custom-state tables, throws, teams/simul actor registries, multi-target helper ownership, exact helper TargetState timing, visual/audio parity, full Helper VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeSnapshotWorld.match()` owns bounded full match snapshot envelope assembly outside `PlayableMatchRuntime`: selected P1 action, playback/speed/toggles, match pause handoff, stage/camera snapshot, round snapshot, player actor snapshots, effect snapshots, compatibility-session handoff, and 80-line log trimming route through one named boundary. This is snapshot/debug ownership cleanup only; snapshot schema changes, compatibility telemetry semantics, exact camera/effect/renderer parity, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchTickBranchWorld` owns bounded per-tick hitpause / pause / active branch arbitration outside `PlayableMatchRuntime`: hitpause consumes the tick before pause or active, match pause consumes the tick before active when hitpause is clear, and active match runs only when neither pause layer owns the tick. This is match-loop ownership cleanup only; exact pause layering/arbitration, hitpause/pause tick semantics, helper/team scheduling, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchStepWorld` owns bounded public match-step cadence outside `PlayableMatchRuntime`: stopped playback snapshots without ticking frame clock, forced stepping advances exactly one tick, sub-1x speed samples on frame-clock divisors, multi-step speed loops advance in order, and round-over checks stop remaining iterations. This is match-step ownership cleanup only; exact pause/round arbitration, frame pacing, replay/rollback timing, helper/team scheduling, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R1 required trace restoration cut: `synthetic-imported-envshake.json` checksum `061f17d5` is back in `pnpm qa:trace` and proves bounded imported `EnvShake` controller telemetry, typed `envshake` operation evidence, and `RuntimeEnvShakeEvent` output for P1 state `200`. Current `pnpm qa:trace` passes 280/280 artifacts, 255 required and 25 optional after later Common1 gates. This is evidence-pipeline restoration only; exact waveform, pause/stage/layer interaction, helper/redirect ownership, visual/audio parity, full presentation parity, and score movement remain blocked.
- Previous R1 optional fixture trace cut: `kfm-official-default-crouch-guard-slide-stop.json` checksum `d11153d0` proves real KFM/Common1 crouch guard-hit state `153` executes ordered `HitVelSet -> VelSet -> CtrlSet -> ChangeState` with typed kinematic/resource operation evidence after direct guarded contact while holding down-back. The previous required crouch trace remains `synthetic-imported-crouch-guard-slide-stop.json` checksum `2bea7311`, proving bounded defender-owned crouch guard-hit routing through `152 -> 153 -> 130` with actor-frame velocity-before-stop evidence. Previous stand mirror `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641` covers `150 -> 151 -> 130`; optional private KFM stand fixture cut `kfm-official-default-guard-slide-stop.json` checksum `885bb1da` mirrors stand slide-stop/control order against real KFM/Common1 state `151` when `.scratch/fixtures/kfm-official.zip` exists. Previous `synthetic-imported-gethitvar-velocity.json` checksum `878a03f7` and `synthetic-imported-gethitvar-damage.json` checksum `2c726114` remain required for normal-hit `GetHitVar(xvel/yvel)` and `GetHitVar(damage)`. This is stand/crouch guard slide-stop/control plus velocity/damage metadata evidence only; exact guard timing/proximity/effects, public KFM `130` guard-hold return, exact MUGEN/IKEMEN velocity lifetime after later physics/controllers, damage lifetime/rounding, guard-chip semantics, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, full get-hit/guard parity, and score movement remain blocked.
- Latest R2 ownership cut: `RuntimeMatchActiveWorld` now owns bounded normal active-match orchestration outside `PlayableMatchRuntime` after hitpause/pause gates: round timer tick, normal command-buffer writes, input control, fighter advance, post-fighter interaction/combat, and round-finish handling route through one named boundary. Focused `RuntimeMatchActiveSystem` coverage proves order and sub-world result preservation. This is match-loop ownership cleanup only; exact MUGEN/IKEMEN active tick order, pause-start arbitration, helper/team/redirect actor ownership, combat priority parity, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchHitPauseWorld` now owns bounded normal active-match hitpause handoff outside `PlayableMatchRuntime`: `RuntimeHitPauseWorld.advanceRuntime` delegation, P1/P2 inputs, stage/runtime ticks, paused presentation lifecycle, and ignorehitpause controller execution route through one named boundary before the normal pause branch. Focused `RuntimeMatchHitPauseSystem` coverage proves delegate payload forwarding and paused/not-paused result preservation. This is match-loop ownership cleanup only; exact MUGEN/IKEMEN hitpause tick order, pause arbitration/layering, helper/team/redirect hitpause ownership, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchFrameStartWorld` now owns bounded normal active-match frame-start ordering outside `PlayableMatchRuntime`: per-frame hit/assert flag reset, pre-facing `AssertSpecial`, and auto-facing route through one named boundary before hitpause/pause handling. Focused `RuntimeMatchFrameStartSystem` coverage proves reset-before-assert and assert-before-facing order for both players. This is match-loop ownership cleanup only; exact MUGEN/IKEMEN frame-start tick order, pause/hitpause arbitration, helper/team/redirect frame-start ownership, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchPostFighterWorld` now owns bounded normal active-match post-fighter bridge wiring outside `PlayableMatchRuntime`: combat resolver construction and `RuntimeMatchInteractionWorld.advanceRuntime` handoff route through one named boundary. Focused `RuntimeMatchPostFighterSystem` coverage proves target memory, effect lifecycle, projectile clash, actor constraints, target bindings, direct/projectile/helper combat, clamps, and presentation effect forwarding after resolver construction. This is match-loop ownership cleanup only; exact MUGEN/IKEMEN post-fighter tick order, combat priority parity, projectile/helper contact timing, helper/team/redirect ownership, target lifetime parity, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchInputControlWorld` now owns bounded normal active-match P1/P2-controlled/simple-AI input dispatch outside `PlayableMatchRuntime`: P1 player input, controlled P2 player input, and uncontrolled P2 simple-AI fallback route through one named boundary after normal command-buffer writes. Focused `RuntimeMatchInputControlSystem` coverage proves P1-before-controlled-P2 ordering, mirrored opponent arguments, and P1-before-AI fallback ordering. This is match-loop ownership cleanup only; exact MUGEN/IKEMEN input priority, command timing, input-conflict resolution, pause/hitpause command parity, helper/team/redirect command ownership, AI parity, visual/audio parity, full input VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchRoundWorld` now owns bounded active-match round timer delegation plus finish side effects outside `PlayableMatchRuntime`: timer tick routes through the boundary, and KO/time-over finish routes match stop plus log emission through one named world. Focused `RuntimeMatchRoundSystem` coverage proves timer delegation, finish stop/log mutation, and no-finish no-op behavior. This is match-loop ownership cleanup only; exact MUGEN/IKEMEN round-flow timing, intros/winposes, KO slowdown, continue flow, teams/simul/turns, lifebar/screenpack behavior, visual/audio parity, full round VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchFighterAdvanceWorld` now owns bounded active 1v1 fighter-advance orchestration outside `PlayableMatchRuntime`: P1 advance, P2 auto-guard start, pause-gated P2 advance, and P1 auto-guard start route through one named world. Focused `RuntimeMatchFighterAdvanceSystem` coverage proves normal P1/P2 ordering and pause-after-P1 skip behavior. This is match-loop ownership cleanup only; exact MUGEN/IKEMEN player tick order, pause-start arbitration, teams/simul roster advance, helper/team/redirect actor advance semantics, guard-start parity, visual/audio parity, full match VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchPauseControllerWorld` owns bounded Pause/SuperPause controller result side effects outside `PlayableMatchRuntime`: pause application routes through `RuntimePauseWorld`, SuperPause power delta routes through an injected resource hook, and the existing match log line emits through one named world. Focused `PauseSystem` coverage proves tick/controller/op forwarding, power-delta handoff, log emission, and zero-length no-side-effect behavior. This is pause-result ownership cleanup only; exact MUGEN/IKEMEN pause layering, SuperPause background/effects/sound timing, helper/team/redirect pause ownership, pause/hitpause command parity, visual/audio parity, full pause VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchCombatBridgeWorld` owns bounded priority/direct/projectile/helper combat resolver construction outside `PlayableMatchRuntime`. `RuntimeMatchInteractionWorld` receives priority-clash, direct-combat, projectile-combat, and helper-combat callbacks from one named bridge instead of inline match-loop closures. Focused `RuntimeMatchCombatBridgeSystem` coverage proves route wiring, hurtbox forwarding, projectile target-memory callback forwarding, and log forwarding. This is combat-callback ownership cleanup only; exact MUGEN/IKEMEN direct-combat priority, helper-owned combat/contact timing, projectile hit/cancel timing, teams/simul/multi-target breadth, visual/audio parity, full combat VM parity, and score movement remain blocked.
- Latest R2 ownership cut: `RuntimeFighterAdvanceWorld` now owns bounded per-fighter advance order from `PlayableMatchRuntime`: sprite-effect tick, hit eligibility slots, HitOverride slots, contact timers, render-angle reset, state clock, frame constraints, recovery-window tick, preserve-moveType read, stun, move lifecycle, kinematics, animation, active controllers, ground-recovery landing, lie-down recovery, and frozen-position preservation. Focused `RuntimeFighterAdvanceSystem` coverage proves order, render-angle cleanup before state-clock handoff, preserve flag forwarding, and tick-start position capture after the recovery-window tick but before kinematics. `PlayableMatchRuntime` still supplies concrete worlds, state/action callbacks, active-controller execution, and stage/tick context. This is advance-order ownership cleanup only; exact MUGEN/IKEMEN player tick order, persistent-controller timing, helper/team/redirect actor advance semantics, recovery/stun/physics arbitration, visual parity, full VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeActiveControllerDispatchWorld` owns bounded active-controller route orchestration after scan/trigger pass: active state/animation mutation dispatch, shared runtime-controller execution handoff, active side-effect dispatch, and unsupported fail-soft dispatch are selected through one named boundary. Focused `RuntimeActiveControllerDispatchSystem` coverage proves state-first stop, runtime-controller handoff, side-effect handoff, and unsupported pass-through. `PlayableMatchRuntime` still supplies concrete hooks, world instances, frame lookup, target hooks, telemetry callbacks, stage/tick context, and active-loop order. This is dispatch ownership cleanup only; exact CNS VM timing, persistent-controller semantics, helper/team/redirect scopes, side-effect ordering parity, missing-action fallback parity, target/combat/presentation semantic parity, visual parity, full active-controller parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeMatchInteractionWorld`, `RuntimePausedMatchWorld`, and `RuntimeHitPauseWorld` pass explicit one-opponent lifecycle `opponents` lists into `RuntimeEffectLifecycleWorld` from concrete 1v1 match/pause/hitpause routes while preserving the legacy direct opponent argument. Focused `MatchInteractionSystem`, `PauseSystem`, and `RuntimeHitPauseSystem` coverage proves active and paused lifecycle options carry `[current opponent]` plus stage/runtime ticks. This is concrete bridge cleanup only; real teams/simul roster ownership, automatic multi-opponent match roster discovery, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, broader indexed/team redirects, visual parity, full Helper VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeEffectLifecycleWorld` now accepts an explicit lifecycle `opponents` list, builds an id-bearing nearest-order `opponentRoster` through `RuntimeOpponentSelectionWorld`, and keeps the legacy current `opponentId` / `opponentState` separate for one-opponent fallback routes. Focused `EffectLifecycleSystem` coverage proves an unsorted `[far, near, tied]` list becomes a nearest-order helper roster and the `opponents` control field does not leak into helper options. This is lifecycle/source cleanup only; real teams/simul roster ownership, automatic match-level multi-opponent lifecycle wiring, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, broader indexed/team redirects, visual parity, full Helper VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeOpponentSelectionWorld` now builds id-bearing `opponentRoster` entries in nearest order without cloning runtime states, and `RuntimeEffectLifecycleWorld` delegates current-opponent lifecycle roster construction through that boundary. Focused `RuntimeOpponentSelectionSystem` coverage proves ids and state refs survive nearest ordering, while `EffectLifecycleSystem` coverage keeps active/paused lifecycle forwarding green. This is selection/lifecycle cleanup only; real teams/simul roster ownership, multi-opponent lifecycle roster sources, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, broader indexed/team redirects, visual parity, full Helper VM parity, and score movement remain blocked.
- Previous R2 ownership cut: `RuntimeEffectLifecycleWorld` now forwards the current opponent as an id-bearing `opponentRoster` into active and paused visual Helper lifecycle contexts while preserving legacy `opponentId` / `opponentState`. Focused `EffectLifecycleSystem` coverage proves both lifecycle paths carry the current opponent id plus runtime state. This is lifecycle/read-context cleanup only; real teams/simul roster ownership, multi-opponent lifecycle roster construction, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, broader indexed/team redirects, visual parity, full Helper VM parity, and score movement remain blocked.
- Optional private `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae` confirms real KFM held-crouch prep state `11`, then Common1 `5010 -> 5011 -> 0` through `HitShakeOver` and `HitOver` after a direct imported `HitDef` omits `p2stateno`, with final checksum `f469a942`, `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width `39/39`, ordered KFM crouch slide `HitVelSet` / `VelMul` / `VelSet` / `DefenceMulSet` typed-operation evidence, and final idle/control. Required `synthetic-imported-default-crouch-gethit-progression.json` checksum `fd986a9e` remains the portable crouch progression gate, now superseded as latest required R1 by the `GetHitVar(xvel/yvel)` gate. This is private-fixture confidence plus required synthetic evidence only; public KFM support, exact MUGEN/IKEMEN tick timing, exact crouch get-hit animation/slide tables beyond the KFM frame/controller profile, fall routing, custom-state/helper/team breadth, visual/audio parity, full Common1 get-hit parity, and score movement remain blocked.
- Required `synthetic-imported-default-air-fall-recovery-too-early.json` checksum `48a2e708` now gates bounded defender-owned Common1-style air-entry early recovery rejection. When P2 is airborne and a fall imported `HitDef` omits `p2stateno`, P2 routes through `5020 -> 5030 -> 5050`, keeps `command = "recovery"` active too early, remains in `5050` while `fall.recovertime` is positive from `11` to `6`, and forbids recovery/landing states `5210`, `5200`, `5201`, `52`, `5100`, `5101`, `5110`, and `5120`; current `pnpm qa:trace` aggregate is 264/264 artifacts, 244 required and 20 optional. This is air-entry early recovery-input rejection evidence only; exact MUGEN/IKEMEN recovery threshold tables, exact air get-hit animation, exact velocity math, exact controller-loop timing, recovery arbitration between air/ground branches, visual/audio parity, full Common1 recovery parity, and score movement remain blocked.
- Previous required `synthetic-imported-default-air-fall-recovery-input.json` checksum `334a419e` remains required and gates bounded defender-owned Common1-style air-entry recovery input through `5020 -> 5030 -> 5050 -> 5210 -> 52 -> 0`.
- Previous required `synthetic-imported-default-air-liedown-recovery.json` checksum `56a8f236` remains the bounded `5020 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0` route proof with bounce, lie-down countdown, get-up, and idle-return evidence.
- Previous required `synthetic-imported-default-air-ground-impact.json` checksum `0ba3c80f` remains the narrower bounded `5020 -> 5030 -> 5050 -> 5100` route proof with `5100` `HitFallDamage` / `hitfall:hitfalldamage` evidence.
- Previous required `synthetic-imported-default-air-fall-gethit.json` checksum `1230a2f3` remains the narrower bounded `5020 -> 5030 -> 5050` route proof.
- Previous required `synthetic-imported-default-air-gethit.json` checksum `dc4fb7c9` gates bounded defender-owned Common1-style air default get-hit state selection. When P2 is airborne and a direct imported `HitDef` omits `p2stateno`, P2 enters state `5020`; that checkpoint passed 259/259 artifacts, 239 required and 20 optional. This is state-selection evidence only; exact air get-hit animation, fall route, landing route, air recovery, `HitShakeOver`/`HitOver` progression, controller-loop timing, visual/audio parity, full Common1 get-hit parity, and score movement remain blocked.
- Previous required `synthetic-imported-default-crouch-gethit.json` checksum `7ec18c61` gates bounded defender-owned Common1-style crouch default get-hit state selection. When P2 holds crouch and a direct imported `HitDef` omits `p2stateno`, P2 enters state `5010`; that checkpoint passed 258/258 artifacts, 238 required and 20 optional. This is state-selection evidence only; exact crouch get-hit animation, slide timing, fall routing, `HitShakeOver`/`HitOver` progression, controller-loop timing, visual/audio parity, full Common1 get-hit parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-contacttime-id.json` checksum `e9ebf36a` and `synthetic-imported-projectile-guardedtime-id.json` checksum `dfd08f28` gate bounded owner-state `ProjContactTime(77)` / `ProjGuardedTime(77)` routing after player-owned Projectile contact/guard markers. P1 routes from state/action `200` into `321` and `322` with hit/guard event/reason, Projectile lifecycle, effect-store, and target-link evidence. That checkpoint passed 257/257 artifacts, 237 required and 20 optional. This is fixed-id contact/guard-time trigger evidence only; exact contact/guard tick-order/lifetime, multi-projectile same-id selection, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, full Projectile timing parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-canceltime-any.json` checksum `5bff1961` gates bounded owner-state `ProjCancelTime(0)` routing after that owner's player-owned Projectile id `77` is canceled by an opposing Projectile clash. P2 routes from state/action `200` into `320` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `918`, projectile lifecycle, effect-store, and payload evidence. This is owner-state any-id cancel-time trigger evidence for one canceled Projectile route only; exact cancel tick-order/lifetime, broad dynamic expression parity, multi-projectile any-id arbitration beyond this route, exact priority classes, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, full Projectile cancel parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-canceltime-var.json` checksum `e057e102` gates bounded owner-state `ProjCancelTime(var(0))` routing after owner-local `VarSet` seeds canceled player-owned Projectile id `77`. P2 routes from state/action `200` into `319` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `917`, projectile lifecycle, effect-store, and payload evidence. This is owner-state var-backed cancel-time trigger evidence only; exact cancel tick-order/lifetime, broad dynamic expression parity, multi-projectile any-id arbitration, exact priority classes, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, full Projectile cancel parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-canceltime-dynamic.json` checksum `0da26c87` gates bounded owner-state expression-derived `ProjCancelTime(77 + var(0))` routing after that owner's player-owned Projectile is canceled by an opposing Projectile clash. P2 routes from state/action `200` into `318` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `916`, projectile lifecycle, effect-store, and payload evidence. This is owner-state dynamic-id cancel-time trigger evidence only; exact cancel tick-order/lifetime, broad dynamic expression parity, multi-projectile id `0` selection, exact priority classes, helper-owned projectile routing, redirects, teams/simul, visual/audio parity, full Projectile cancel parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2` gates bounded helper-local expression-derived `ProjCancelTime(8869 + var(0))` routing after that helper-parented owner-side Projectile is canceled by an opposing Projectile clash. The Helper routes from state/action `1270` into `1271` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `1018`, helper/projectile lifecycle, effect-store, and payload evidence. Focused helper-local runtime coverage separately proves nonzero `ProjCancelTime(var(n))` id selection against helper-parented canceled Projectiles. This is helper-local dynamic-id cancel-time trigger evidence only; exact cancel tick-order/lifetime, broad dynamic expression parity, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, visual/audio parity, full Helper/Projectile cancel parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176` gates bounded helper-local `ProjCancelTime(8868)` routing after that helper-parented owner-side Projectile is canceled by an opposing Projectile clash. The Helper routes from state/action `1268` into `1269` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `1008`, helper/projectile lifecycle, effect-store, and payload evidence. This is helper-local fixed-id cancel-time trigger evidence only; exact cancel tick-order/lifetime, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, visual/audio parity, full Helper/Projectile cancel parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01` gates bounded helper-local `ProjCancelTime(0)` routing after a helper-parented owner-side Projectile is canceled by an opposing Projectile clash. The Helper routes from state/action `1266` into `1267` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `998`, helper/projectile lifecycle, effect-store, and payload evidence. This is helper-local cancel-time trigger evidence only; exact cancel tick-order/lifetime, exact priority classes, multi-projectile any-id selection semantics beyond this route, helper-owned custom states, redirects, teams/simul, visual/audio parity, full Helper/Projectile cancel parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-canceltime.json` checksum `64e8dec4` gates bounded owner-state `ProjCancelTime(77)` routing after that owner's player-owned Projectile is canceled by an opposing Projectile clash. P2 routes into state/action `283` with clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback, projectile lifecycle, effect-store, and payload evidence. This is owner-state cancel-time trigger evidence only; exact cancel tick-order/lifetime, exact priority classes, multi-projectile id `0` selection, redirects, teams/simul, visual/audio parity, full Projectile cancel parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projguardedtime-any.json` checksum `bd64e9db` and `synthetic-imported-helper-projcontacttime-any.json` checksum `5c6a4e11` gate bounded helper-local `ProjGuardedTime(0)` / `ProjContactTime(0)` any-projectile routing after helper-parented Projectile guard/contact markers. The Helper routes into state/actions `1263` and `1265` with guard event/reason, helper/projectile lifecycle, effect-store, target-link, and sound/FightFX package evidence. This is helper-local guard/contact-time trigger evidence only; exact contact/guard tick-order/lifetime, multi-projectile any-id selection, helper-owned custom-state targets, redirects, teams/simul, visual/audio parity, full Helper/Projectile parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b` gates bounded helper-local `ProjHitTime(0)` any-projectile hit-time routing after a helper-parented Projectile hits P2. The Helper routes into state/action `1261` with hit event/reason, helper/projectile lifecycle, effect-store, target-link, and sound/FightFX package evidence. This is helper-local hit-time trigger evidence only; exact hit tick-order/lifetime, multi-projectile any-id selection, helper-owned custom-state targets, redirects, teams/simul, visual/audio parity, full Helper/Projectile parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-hittime-any.json` checksum `47c1cf7f` gates bounded owner-state `ProjHitTime(0)` any-projectile hit-time routing after a player-owned Projectile hits P2. P1 routes into state/action `282` with hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. This is player-owned hit-time trigger evidence only; exact hit tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, visual/audio parity, full Projectile hit parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-contacttime-any.json` checksum `f1751155` gates bounded owner-state `ProjContactTime(0)` any-projectile contact-time routing after a player-owned Projectile hits P2. P1 routes into state/action `281` with hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. This is contact-time trigger evidence only; exact contact tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, visual/audio parity, full Projectile contact parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-guardedtime-any.json` checksum `c8473340` gates bounded owner-state `ProjGuardedTime(0)` any-projectile guard-time routing after P2 guards a player-owned Projectile. P1 routes into state/action `279` with guard event/reason, Projectile lifecycle, effect-store, and target-link evidence. This is guarded contact-time trigger evidence only; exact guard tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, visual/audio parity, full Projectile guard parity, and score movement remain blocked.
- Previous required `synthetic-imported-hitdef-projectile-target-mix.json` checksum `e98d4857` gates bounded owner-local target-memory mixing: direct `HitDef` id `77` and player-owned `Projectile` id `78` both hit P2 in one active state, owner target links expose both ids, and P1 routes through `NumTarget(77)`, `Target(77), Life`, `NumTarget(78)`, and `Target(78), Life` into state/action `278`. This is mixed target-memory trigger evidence only; Target* mutation mixing, helper-owned projectile targets, helper-owned custom state tables, throws, teams/simul, multi-target/helper-owned opponent selection, exact target lifetime/tick order, visual parity, full target/combat parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3c3f2e25` gates bounded defender-owned air guard-hit routing where `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` routes P2 from state `155` into state/action `317` after a helper-parented Projectile air guard, with helper-local Projectile spawn telemetry plus helper/projectile lifecycle and owner/helper target-link evidence.
- Previous `synthetic-imported-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3fcf1421` gates bounded defender-owned player Projectile air guard-hit routing into state/action `316`.
- Previous required `synthetic-imported-gethitvar-air-guard-hitshaketime.json` checksum `703e9328` gates bounded defender-owned air guard-hit routing where `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` routes P2 from state `155` into state/action `315` after a direct `HitDef` air guard. That cut passed `pnpm qa:trace` at 239/239 artifacts, 219 required and 20 optional before the Projectile air guard hitshaketime gate was added. This is direct-`HitDef` air guard hitshake timing metadata trigger evidence only; exact air guard timing/landing/proximity, guard end/effects, helper/projectile air-guard variants, custom-state inheritance breadth, exact VM tick order, visual parity, full Common1 guard parity, and score movement remain blocked.
- Previous required `synthetic-imported-gethitvar-crouch-guard-hitshaketime.json` checksum `b31d1dac` gates bounded defender-owned crouch guard-hit routing where `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` routes P2 from state `153` into state/action `314` after a direct `HitDef` crouch guard. That cut passed `pnpm qa:trace` at 238/238 artifacts, 218 required and 20 optional before the air guard hitshaketime gate was added. This is crouch guard hitshake timing metadata trigger evidence only; exact crouch guard timing/proximity, guard end/effects, helper/projectile crouch-guard variants, custom-state inheritance breadth, exact VM tick order, visual parity, full Common1 guard parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime.json` checksum `64a1a8bd` gates bounded defender-owned guard-hit routing where `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` routes P2 from state `151` into state/action `313` after a helper-parented Projectile guard. That cut passed `pnpm qa:trace` at 237/237 artifacts, 217 required and 20 optional before the crouch guard hitshaketime gate was added. This is helper-parented Projectile guard hitshake timing metadata trigger evidence only; helper-owned custom states, exact helper Projectile guard timing/effects, projectile visual/audio parity, custom-state inheritance breadth, exact VM tick order, visual parity, full Common1 guard parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-gethitvar-guard-hitshaketime.json` checksum `724f66d6` gates bounded defender-owned guard-hit routing where `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` routes P2 from state `151` into state/action `312` after a player-owned Projectile guard. That cut passed `pnpm qa:trace` at 236/236 artifacts, 216 required and 20 optional before the Helper Projectile guard hitshaketime gate was added. This is player-owned Projectile guard hitshake timing metadata trigger evidence only; exact hitpause lifetime, guard timing/effects, projectile visual/audio parity, custom-state inheritance breadth, exact VM tick order, visual parity, full Common1 guard parity, and score movement remain blocked.
- Previous required `synthetic-imported-gethitvar-guard-hitshaketime.json` checksum `31d76de9` gates bounded defender-owned guard-hit routing where `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` routes P2 from state `151` into state/action `311` after a direct `HitDef` guard. That cut passed `pnpm qa:trace` at 235/235 artifacts, 215 required and 20 optional before the Projectile guard hitshaketime gate was added. This is direct-HitDef guard hitshake timing metadata trigger evidence only; exact hitpause lifetime, guard timing/proximity, guard end/effects, custom-state/helper/projectile inheritance breadth, exact VM tick order, visual parity, full Common1 guard parity, and score movement remain blocked.
- Previous required `synthetic-imported-gethitvar-hitshaketime.json` checksum `655107b9` gates bounded defender-owned normal get-hit routing where `GetHitVar(hitshaketime) > 0 && !GetHitVar(guarded)` routes P2 from state `5000` into state/action `310` after a direct `HitDef` hit. That cut passed `pnpm qa:trace` at 234/234 artifacts, 214 required and 20 optional before the guard hitshaketime gate was added. This is direct-contact normal hitshake timing metadata trigger evidence only; exact hitpause tick lifetime, hitshake lifetime, custom-state/helper/projectile inheritance breadth, exact VM tick order, visual parity, full Common1 get-hit parity, and score movement remain blocked.
- Previous required `synthetic-imported-gethitvar-hittime.json` checksum `a11beef0` gates bounded defender-owned normal get-hit routing where `GetHitVar(hittime) > 0 && !GetHitVar(guarded)` routes P2 from state `5000` into state/action `309` after a direct `HitDef` hit. That cut passed `pnpm qa:trace` at 233/233 artifacts, 213 required and 20 optional before the hitshaketime gate was added. This is direct-contact normal hit timing metadata trigger evidence only; exact hitstun tick order, custom-state/helper/projectile inheritance breadth, exact VM tick order, visual parity, full Common1 get-hit parity, and score movement remain blocked.
- Previous required `synthetic-imported-gethitvar-guard-timing.json` checksum `cf92c669` gates bounded defender-owned guard-hit routing where `GetHitVar(hittime) > 0`, `GetHitVar(slidetime) = 5`, and `GetHitVar(ctrltime) = 7` route P2 from state `151` into state/action `308` after a direct `HitDef` guard and typed `HitVelSet` evidence. That cut passed `pnpm qa:trace` at 232/232 artifacts, 212 required and 20 optional before the hittime gate was added. This is direct-`HitDef` guard timing metadata trigger evidence only; exact guard timing/proximity, guard end, guard effects, projectile/helper/custom-state inheritance, exact VM tick order, visual parity, full Common1 guard parity, and score movement remain blocked.
- Previous required `synthetic-imported-gethitvar-down-recover.json` checksum `b8a7aef0` gates bounded owner-backed get-hit routing where `GetHitVar(down.recover) = 1`, `GetHitVar(down.recovertime) = 45`, and alias `GetHitVar(recovertime) = 45` route P2 from state `5100` into state/action `307` before lie-down recovery consumes the timer. That cut passed `pnpm qa:trace` at 231/231 artifacts, 211 required and 20 optional before the guard timing gate was added. This is stored direct-`HitDef` down-recovery metadata trigger evidence only; exact liedown tables, 5110/5120 tick order, metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, exact VM tick order, visual parity, full Common1 recovery parity, and score movement remain blocked.
- Required `synthetic-imported-gethitvar-fall-envshake.json` checksum `6364632a` remains the bounded owner-backed get-hit route for `GetHitVar(fall.envshake.time)`, `GetHitVar(fall.envshake.freq)`, `GetHitVar(fall.envshake.ampl)`, and `GetHitVar(fall.envshake.phase)` into state/action `306` before `FallEnvShake` presentation executes.
- Required `synthetic-imported-gethitvar-fall-metadata.json` checksum `474fa734` remains the bounded owner-backed get-hit route for `GetHitVar(fall.damage)`, `GetHitVar(fall.kill)`, `GetHitVar(fall.xvel)`, and `GetHitVar(fall.yvel)` into state/action `305` before `HitFallDamage` resolves.
- Required `synthetic-imported-helper-projectile-gethitvar-guarded.json` checksum `2b413bd7` gates bounded helper-parented `Projectile` guard metadata through defender-owned guard-hit CNS: P2 blocks a helper-spawned Projectile, runs `150 -> 151`, executes `HitVelSet` / `CtrlSet`, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `304` before returning to idle/control. The gate also requires helper-local `Projectile` controller/op telemetry from helper state `1200`. That checkpoint passed `pnpm qa:trace` at 226/226 artifacts, 206 required and 20 optional before TeamSide and later GetHitVar timing gates were added. This is helper-parented Projectile guard metadata trigger evidence only; custom-state guarded metadata, exact guard timing/effects, visual parity, full Common1/guard parity, and score movement remain blocked. Previous player-owned Projectile guarded evidence remains `synthetic-imported-projectile-gethitvar-guarded.json` checksum `a0104472`; previous direct-`HitDef` guarded evidence remains `synthetic-imported-gethitvar-guarded.json` checksum `7c36defb`.
- Previous required `synthetic-imported-gethitvar-fall-recover.json` checksum `259b300f` gates bounded owner-backed get-hit routing where `GetHitVar(fall.recover) = 1`, `GetHitVar(fall.recovertime) > 0`, and `!CanRecover` route P2 from state `5100` into state/action `301`. This is get-hit metadata trigger evidence only; exact recovery threshold tables, custom-state lifetime, helper/projectile inheritance, controller-loop timing, visual parity, full Common1/get-hit parity, and score movement remain blocked.
- Previous required `synthetic-imported-animelem-offset.json` checksum `4484031d` still gates bounded imported active-state legacy `AnimElem` elapsed-time routing: P1 enters state `200`, advances through authored AIR frame durations `[2,6,4]`, then exits through `AnimElem = 2, = 4` into state/action `300` without `HitDef` / combat side effects. Previous required `synthetic-imported-animelem.json` checksum `683d9a10` still gates `AnimElem = 2, = 0` into state/action `299`. This is current-actor animation-element trigger evidence only; AIR loop semantics, negative-duration semantics, helper/state-owner namespaces, persistent-controller timing, visual parity, full animation VM parity, and score movement remain blocked.
- Required `synthetic-imported-owner-metrics.json` checksum `1a61aaeb` remains the bounded imported State -1 current-owner metric routing gate: P1 presses `x`, then `StateNo = 0`, `Anim = 0`, `Time >= 0`, `Life = 1000`, `Power = 0`, `Pos X < 0`, `Pos Y = 0`, `Vel X = 0`, and `Vel Y = 0` route into state/action `298` without `HitDef` / combat side effects. `RuntimeExpressionContextWorld` normalizes the post-transition `stateElapsed = -1` sentinel to observable CNS `Time` / `StateTime` zero. This is owner metric trigger evidence only; exact VM tick ordering, helper/team/redirect state namespaces, localcoord scaling, visual parity, full trigger parity, and score movement remain blocked.
- Previous required `synthetic-imported-p2-distance.json` checksum `2c584be0` gates bounded imported State -1 current-opponent spacing routing: P1 presses `x`, then `P2Dist X = 55`, `P2Dist Y = 0`, `P2BodyDist X = 7`, and `P2BodyDist Y = 0` route into state/action `297` without `HitDef` / combat side effects. Teams/simul, helpers, exact opponent selection, localcoord scaling, push/corner adjustment, visual parity, full spacing parity, and score movement remain blocked.
- Previous required `synthetic-imported-state-context.json` checksum `cb9c3d1e` gates bounded imported State -1 owner-context routing: P1 presses `x`, then `ctrl`, `StateType = S`, `MoveType = I`, and `Physics = S` route into state/action `295` without `HitDef` / combat side effects. Helper/team/redirect metadata ownership, exact controller-loop ordering, visual parity, full trigger parity, and score movement remain blocked.
- Previous required `synthetic-imported-gametime.json` checksum `bab573f3` gates bounded imported State -1 global-tick routing: P1 delays the `x` command until `GameTime >= 4`, then enters state/action `294` without `HitDef` / combat side effects. Exact pause accounting, replay/rollback timing, multi-round timer ownership, IKEMEN round-system behavior, visual parity, full global timing parity, and score movement remain blocked.
- `RuntimeFighterStateWorld` now owns bounded fighter runtime-state construction outside `PlayableMatchRuntime`: resource maxima, damage multipliers from constants, initial action/control/resource state, command buffers, contact memory, telemetry buckets, injected world references, deterministic RNG seed, and lazy runtime-program compilation. Focused `RuntimeFighterStateSystem` coverage proves injected-world preservation, bounded constants, initial runtime state, p2 priority, native no-program behavior, and command/contact setup. This is R2 ownership cleanup only; exact player lifecycle parity, helper/custom-state clone breadth, team/simul roster ownership, intro/round lifecycle, visual parity, full actor registry parity, and score movement remain blocked.
- `RuntimeMatchResetWorld` now owns bounded match reset orchestration outside `PlayableMatchRuntime`: round timer reset, pause reset, EnvColor reset, effect actor store reset, in-place P1/P2 recreation, helper TargetState handler reattachment, and reset logging. Focused `RuntimeMatchResetSystem` coverage proves reset order, actor identity preservation, handler reattachment, and log handoff, while existing `PlayableMatchRuntime` coverage preserves externally owned effect actor store reset behavior. This is R2 ownership cleanup only; exact round-flow parity, continue/round intro semantics, helper/custom-state reset breadth, screenpack/lifebar reset behavior, visual parity, full match lifecycle parity, and score movement remain blocked.
- `RuntimeHelperTargetStateWorld` now owns bounded helper TargetState handler attach/re-attach wiring outside `PlayableMatchRuntime` constructor/reset. The same world now covers callback binding plus helper owner validation, target lookup, unavailable-state fail-closed behavior, and owner-backed target state entry. Focused `RuntimeHelperTargetStateSystem` coverage proves owner-specific handlers replace stale handlers and forward owner/helper/target/state id context. This is R2 ownership cleanup only; helper-owned custom state table parity, throws, teams/simul, multi-target/helper-owned opponent selection, exact helper TargetState timing, visual parity, full Helper VM parity, and score movement remain blocked.
- `RuntimeControllerEvaluationContextWorld` now owns bounded `StateControllerExecutor` context creation for active runtime-controller dispatch outside `PlayableMatchRuntime`: owner const reads, actor hitpause reads, actor random callbacks, and stage-time forwarding route through a named context factory. This is R2 ownership cleanup only; full passive-controller parity, exact CNS controller-loop timing, helper/team/redirect context scopes, exact random stream parity, visual parity, full controller VM parity, and score movement remain blocked.
- `FrontEdgeDist`, `BackEdgeDist`, `FrontEdgeBodyDist`, and `BackEdgeBodyDist` now use supplied stage bounds in active trigger contexts, dynamic controller-param fallback, extracted passive controller systems, and first-generation visual Helper micro-VM contexts. Required `synthetic-imported-edge-distance.json` checksum `785de452` proves bounded imported State -1 routing through those reads into state/action `293`, while focused evaluator/context/helper tests prove bounded edge values plus no-stage fallback. This is expression plumbing only; exact camera/screen edge behavior, localcoord scaling, teams/simul/helper namespace breadth, visual parity, full edge-distance parity, and score movement remain blocked.
- `RuntimeDispatchEvaluationWorld` now owns bounded dynamic active-controller dispatch-param fallback outside `PlayableMatchRuntime`: compiled numeric/Boolean values short-circuit, dynamic fallback expressions request a context from `RuntimeExpressionContextWorld`, numeric results are finite/truncated, Boolean params use numeric truthiness, and actor/opponent/owner/tick forwarding is covered by focused tests. This is R2 ownership cleanup only; full dynamic-param parity, persistent-controller timing, exact CNS controller tick order, helper/team/redirect parameter scopes, visual parity, full controller VM parity, and score movement remain blocked.
- `RuntimeTriggerEvaluationWorld` now owns bounded normalized `TriggerIr` expression evaluation outside `PlayableMatchRuntime`: actor/opponent/owner/tick forwarding, context-factory handoff, raw expression result capture, and Boolean pass/fail projection. `RuntimeExpressionContextWorld` still owns the read model and `RuntimeTriggerGateWorld` still owns `triggerall` / numbered-group ordering. `RuntimeDispatchEvaluationWorld` and `RuntimeTriggerEvaluationWorld` now also forward explicit opponent rosters into their context factories, and `PlayableMatchRuntime` routes the current one-opponent list through that seam. Focused adapter tests prove `NumEnemy` reads can flow through both dynamic param fallback and trigger evaluation, and focused context tests prove explicit `EnemyNear(index)` rosters sort by bounded nearest body-distance with stable ties before redirect resolution. This is R2 ownership cleanup only; full expression language parity, persistent-controller timing, exact CNS controller/trigger tick order, helper/team redirect parameter scopes, real team/simul roster ownership, visual parity, full trigger VM parity, and score movement remain blocked.
- `RuntimeTriggerGateWorld` now owns bounded CNS trigger grouping/order outside `PlayableMatchRuntime`: `triggerall` AND preconditions, numbered `triggerN` OR groups, no-numbered-trigger pass-through, and short-circuit ordering. Focused `RuntimeTriggerGateSystem` coverage proves `triggerall` failure skips numbered groups, first passing numbered group wins, no-numbered groups pass after `triggerall`, all-failing numbered groups fail, and callback actor/opponent/owner/tick context is forwarded. This is R2 ownership cleanup only; full expression language parity, persistent-controller timing, exact CNS trigger tick order, helper/team/redirect trigger scopes, visual parity, full trigger VM parity, and score movement remain blocked.
- `RuntimeAutoGuardStartWorld` now owns bounded imported auto guard-start orchestration outside `PlayableMatchRuntime`: imported-defender filtering, current input/current move/hitpause/hitstun eligibility handoff to `RuntimeGuardWorld`, `InGuardDist` gate, guard-start state selection/availability, clear-state-owner entry, and guard-start runtime mutation. Focused `RuntimeAutoGuardStartSystem` coverage proves successful imported guard start plus non-imported, ineligible, out-of-distance, and unavailable-state fail-closed paths. This is R2 ownership cleanup only; exact proximity-guard timing, guard-end/effects/audio, helper/team/redirect guard ownership, visual parity, full guard VM parity, and score movement remain blocked.
- `RuntimeActiveControllerScanWorld` now owns bounded active-state controller scanning outside `PlayableMatchRuntime`: current owner selection, imported/owner-backed guard, current state lookup, `ignorehitpause` filtering, trigger gating, controller iteration, and stop/continue flow after state-changing controllers. Focused `RuntimeActiveControllerScanSystem` coverage proves owner-backed scanning, hitpause-only filtering, stop-after-ChangeState flow, missing-state skip, non-imported skip, and failed-trigger no-op. This is R2 ownership cleanup only; exact CNS VM tick order, persistent controller semantics, helper/team/redirect controller scopes, full controller-loop parity, visual parity, and score movement remain blocked.
- `RuntimeActiveSideEffectDispatchWorld` now owns bounded active-state side-effect dispatch routing outside `PlayableMatchRuntime`: singleton routes for `HitDef`, `ReversalDef`, `Width`, `FallEnvShake`, `Pause` / `SuperPause`, sound, `EnvColor`, `EnvShake`, and contact controllers plus grouped sprite-effect, effect-spawn, and `Target*` / `BindToTarget` routes. Focused `RuntimeActiveSideEffectDispatchSystem` coverage proves every current side-effect route maps to the expected handler, missing hooks fail soft, and non-side-effect dispatches pass through. This is R2 ownership cleanup only; exact CNS VM tick order, persistent controller semantics, helper/team/redirect controller scopes, side-effect ordering parity, target/combat/presentation semantic parity, visual parity, and score movement remain blocked.
- `RuntimeTargetStateEntryWorld` now owns the bounded active-state `TargetState` state-entry adapter that was still inline in `PlayableMatchRuntime`: existing `stateOwner` is preserved, the controller actor becomes the owner otherwise, unavailable target states fail closed, and successful entries route the target into owner-backed state data through explicit hooks. Focused `RuntimeTargetStateEntrySystem` coverage proves controller-owned entry, owner-backed custom-state preservation, and unavailable-state no-op behavior. This is R2 ownership cleanup only; exact TargetState tick order, throws, helper/team/multi-target target state selection, full custom-state parity, visual parity, and score movement remain blocked.
- `RuntimeHelperTargetStateWorld` now owns the bounded helper-owned `TargetState` state-entry adapter that was still inline in `PlayableMatchRuntime`: helper/owner identity is checked before target lookup, missing targets fail closed, unavailable target states fail closed, and successful entries route the target into owner-backed state data through explicit state-entry hooks. Focused `RuntimeHelperTargetStateSystem` coverage proves owner-backed entry, owner-mismatch skip, missing-target no-op, and unavailable-state no-op. This is R2 ownership cleanup only; helper-owned custom state table parity, throws, teams/simul, multi-target selection, exact helper TargetState timing, and score movement remain blocked.
- `RuntimeHelperProjectileTargetWorld` now owns the bounded helper-parented Projectile target-memory mirror that was still inline in `PlayableMatchRuntime`: owner-parented Projectile contacts are ignored, helper-parented Projectile contacts resolve the parent Helper by runtime serial, missing helpers fail closed, and successful contacts write the Projectile target id into helper-local target memory through `TargetSystem`. Focused `RuntimeHelperProjectileTargetSystem` coverage proves mirror, owner-projectile skip, and missing-helper no-op behavior. This is R2 ownership cleanup only; exact helper Projectile target lifetime, helper-owned custom-state tables, teams/simul, multi-target selection, exact combat/effect tick order, and score movement remain blocked.
- `RuntimeStateEntryRouteWorld` now owns the bounded State -1 `ChangeState` routing scan that was still inline in `PlayableMatchRuntime`: state-entry iteration, non-`ChangeState` filtering, trigger gating, dynamic state-id resolver handoff, route telemetry, authored state-move selection, and raw state-entry fallback. Focused `RuntimeStateEntryRouteSystem` coverage proves route-to-move, route-to-state, failed trigger / unresolved state-id no-op scans, dynamic expression handoff, and empty-list skip behavior. This is R2 ownership cleanup only; exact State -1 VM ordering, persistent controller semantics, helper/team/redirect command ownership, full CNS VM parity, and score movement remain blocked.
- `RuntimeMoveStartWorld` now owns bounded native/imported state-move startup outside `PlayableMatchRuntime`: selected current move/label, move tick reset, hit/reversal reset, attack `moveType`, control handoff, and authored action state-entry handoff. Focused `RuntimeMoveStartSystem` coverage proves selected move metadata/reset behavior and control-before-state-entry hook order. This is R2 ownership cleanup only; exact MUGEN/IKEMEN command timing, cancel windows, combo/input priority, helper/team/redirect move startup, persistent-controller timing, full move VM parity, and score movement remain blocked.
- `RuntimeHelperCombatWorld` now owns the bounded helper-owned direct `HitDef` contact loop that was still inline in `PlayableMatchRuntime`: helper iteration from the owner effect store, helper direct-combat actor projection, active-window and contact checks, `HitBy`/`NotHitBy` reject logging, direct hit/guard handoff, imported default get-hit/guard-state hooks, helper target-memory remembering, contact presentation, and helper runtime-state sync. Focused `RuntimeHelperCombatSystem` coverage proves hit, guard, target-memory, presentation telemetry, default state hooks, and reject no-op behavior. This is R2 ownership cleanup only; exact helper hitpause/tick order, multi-target/team helper combat, helper-owned custom-state tables, helper-owned Projectile combat/contact presentation, full Helper VM parity, and score movement remain blocked.
- Required `synthetic-imported-hitdef-data-spark.json` checksum `d950796c` and `synthetic-imported-hitdef-data-guard-spark.json` checksum `7bd9d162` now gate portable parsed CNS `[Data] sparkno` / `guard.sparkno` fallback through typed `HitDef` `ControllerOp` data and runtime hit/guard `HitSpark` telemetry when explicit refs are omitted. The direct-hit route inherits `[Data] sparkno = 2` with `sparkxy = -8,-54`; the held-back guard route inherits `[Data] guard.sparkno = 40` with `sparkxy = 13,-57`. Current `pnpm qa:trace` aggregate is 210/210 artifacts, 190 required and 20 optional. This is required fallback telemetry only; exact FightFX/common lookup, renderer/audio playback parity, broad HitDef semantics, public KFM support, full MUGEN/IKEMEN parity, and score movement remain blocked.
- Previous optional private-fixture `kfm-official-qcf-x.json` checksum `349cd995` gates one real KFM Kung Fu Palm route: `QCF_x` command input, state `1000`, ordered actor-frame `11 -> 20 -> 1000`, ordered `ChangeState` / `PosAdd` / `HitDef`, typed `kinematic:posadd` / `hitdef`, authored `PlaySnd` telemetry for `S0,3` and contact hitsound `S5,4`, inherited `[Data] sparkno = 2` hit-spark telemetry with `sparkxy = -10,-60`, shared sound+spark contact package metadata, target link `p1 -> p2 / 0`, final P1 `power = 35` / `targetCount = 1`, and final P2 `life = 910` / hitfall evidence. This is private fixture confidence only; public KFM support, exact special timing/pushback, renderer/audio playback parity, broad specials, full MUGEN/IKEMEN parity, and score movement remain blocked.
- Required `synthetic-imported-projectile-default-target-controllers.json` checksum `1c1a3e77` gates bounded player-owned Projectile default Target side effects: state `200` spawns a Projectile with omitted `projid` / `id`, the Projectile-only route hits P2 for 31, owner target link `p1 -> p2 / 0` is recorded, and delayed owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` execute from target memory. Evidence includes Projectile anim `915`, lifecycle spawn/remove, effect payload `ownerId = p1` / `effectId = 0` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, TargetBind offset `36,-12`, final P1 `targetCount = 0`, and final P2 `life = 949` / `power = 40`. Current `pnpm qa:trace` aggregate is 208/208 artifacts, 188 required and 20 optional. This is player Projectile default Target side effects only; Target* mutation mixing, helper-owned custom state tables, throws, teams/simul, multi-target selection, exact target lifetime/tick order, visual parity, full Projectile target parity, and score movement remain blocked.
- Previous required `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa` gates bounded player-owned Projectile default TargetState routing through target id `0` and attacker-owned state data `888 -> 889 -> 0`.
- Previous required `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962` gates bounded player-owned Projectile explicit TargetState routing through target id `77` and attacker-owned state data `888 -> 889 -> 0`.
- Previous required `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2` gates bounded player-owned Projectile Target side effects: state `200` spawns Projectile id `77`, P2 is hit for 31, owner target link `p1 -> p2 / 77` is recorded, and delayed owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` execute from target memory. That checkpoint passed `pnpm qa:trace` at 205/205 artifacts, 185 required and 20 optional.
- Previous required `synthetic-imported-projectile-target-redirect.json` checksum `cd099094` gates bounded player-owned Projectile target redirect routing: state `200` spawns Projectile id `77`, the Projectile-only route hits P2 for 31, owner target link `p1 -> p2 / 77` is recorded, and P1 branches through `NumTarget(77)` plus `Target(77), Life <= 969` into state/action `277`. That checkpoint passed `pnpm qa:trace` at 204/204 artifacts, 184 required and 20 optional. This is player Projectile target-memory trigger evidence only; Target* mutation mixing, helper-owned projectile targets, teams/simul, multi-target selection, exact target lifetime/tick order, visual parity, full Projectile target parity, and score movement remain blocked.
- Previous required `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1` gates bounded helper-parented Projectile bare Target routing: helper state `1200` spawns owner-side Projectile id `8863`, the Projectile hits P2, owner and helper target links both record target id `8863`, and the helper branches through `NumTarget(8863)` plus bare `Target, Life` into `1242/978`. That checkpoint passed `pnpm qa:trace` at 203/203 artifacts, 183 required and 20 optional. Previous helper Projectile default TargetState proof remains required as `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1`; previous helper Projectile explicit TargetState proof remains required as `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3`; previous helper Projectile default Target-controller proof remains required as `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae`; previous helper Projectile explicit Target-controller proof remains required as `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8`; previous helper TargetState proof remains required as `synthetic-imported-helper-targetstate.json` checksum `011633b8`; previous helper Target-controller proof remains required as `synthetic-imported-helper-target-controllers.json` checksum `61f4c61e`; previous helper direct bare `Target, Life` proof remains required as `synthetic-imported-helper-bare-target.json` checksum `15f3c1db`; previous player bare `Target, Life` proof remains required as `synthetic-imported-bare-target-redirect.json` checksum `f9c90aa8`; previous default `Target(0), Life` proof remains required as `synthetic-imported-default-target-redirect.json` checksum `d43caabf`; previous default `NumTarget(0)` proof remains required as `synthetic-imported-default-numtarget.json` checksum `5869ebbd`; previous helper direct default-target proof remains required as `synthetic-imported-helper-default-target.json` checksum `e1bcced0`; previous helper Projectile default-target proof remains required as `synthetic-imported-helper-projectile-default-target.json` checksum `b0daddf6`. This is direct player bare/default/static/dynamic target trigger evidence, explicit/default/bare helper HitDef plus helper-parented Projectile target evidence, bounded helper-owned direct-HitDef and explicit/default helper-parented Projectile Target mutation evidence, and bounded helper-local direct-HitDef plus explicit/default helper-parented Projectile TargetState into owner-backed state data only; helper-owned custom state tables, throws, teams/simul, multi-target/helper-owned opponent selection, exact target lifetime/tick order, exact helper hitpause/tick order, exact helper `HitDef`/Projectile lifetime parity, visual parity, full target/combat/projectile parity, and score movement remain blocked.
- Required `synthetic-imported-default-fall-official-air-recovery.json` checksum `b0363be9` gates a bounded official-style synthetic Common1 recovery route through `5050 -> 5210 -> 52 -> 0` after `command = "recovery"` while airborne. Actor-frame sequence evidence proves positive-to-zero `fall.recovertime` in `5050`, `5210` recovery velocity telemetry, `52` landing y = 0 evidence, and final idle/control. Controller/order evidence proves `VelAdd`, `ChangeState`, `VelSet`, `HitFallSet`, `CtrlSet`, and typed `kinematic:*`, `hitfall:hitfallset`, and `resource:ctrlset` operations. That checkpoint passed 187/187 artifacts, 167 required and 20 optional. This is R1 Common1 recovery precision only; exact `fall.recovertime` tables, velocity math, exact controller-loop timing, public bundled KFM support, visual parity, full Common1 recovery parity, and score movement remain blocked.
- Optional private-fixture `kfm-official-default-fall-recovery-input.json` checksum `151486e1` now gates stricter real KFM/Common1 air-recovery evidence without changing trace behavior: ordered actor-frame `5050 -> 5210 -> 52`, ordered `5050` `VelAdd` / `ChangeState`, `5210` `PalFX` / `sprite-effect:palfx`, `PosFreeze` / `bounds:posfreeze`, `Turn` / `orientation:turn`, `NotHitBy` / `eligibility:nothitby`, `VelMul`, `VelAdd`, `ChangeState`, `52` `VarSet`, and state `0` `VelSet` / `kinematic:velset`. This is optional local fixture confidence only; no public bundled KFM, score movement, exact threshold/velocity/tick-order parity, or full recovery parity claim.
- Optional private-fixture `kfm-official-default-fall-ground-recovery.json` checksum `6d361534` now gates stricter real KFM/Common1 ground-recovery evidence without changing trace behavior: ordered actor-frame `5050 -> 5200 -> 52`, ordered `5050` `VelAdd` / `ChangeState`, `5200` `VelAdd` / `SelfState`, and `52` `VelSet` / `PosSet` / `kinematic:*` / `ChangeState`. `5201` remains required as an executed state, but no summarized `5201` actor-frame bucket is claimed. This is optional local fixture confidence only; no public bundled KFM, score movement, exact threshold/velocity/tick-order parity, guard-state parity, or full recovery parity claim.
- Previous required `synthetic-imported-default-fall-official-ground-recovery.json` checksum `74b72495` remains the bounded official-style synthetic ground route through `5050 -> 5200 -> 5201 -> 52 -> 0`.
- `HelperSystem` / `RuntimeEffectActorWorld` support bounded helper-local `ProjContact(id)` / `ProjContactTime(id)` marker reads from helper-parented owner-side Projectile generic contact state. Required `synthetic-imported-helper-projcontact.json` checksum `67ed6c2d` proves a visual Helper routes through `1200 -> 1220 -> 1221` / anims `949` and `950` after spawning owner-side Projectile anim `951` with `parentId = p1-helper-0`; the gate now also requires owner-side target-link evidence plus shared `guardsound = S6,0` / FightFX `guard.sparkno = F7004` package telemetry with selected AIR-frame offset/duration metadata. Previous `synthetic-imported-helper-projguard.json` checksum `1c2c18a5` now requires the same guard package contract for `ProjGuarded(id)`, `synthetic-imported-helper-projhit.json` checksum `2d9a281e` requires shared `hitsound = S5,0` / FightFX `sparkno = F7002` package telemetry for `ProjHit(id)`, and `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b` proves helper-local `ProjHitTime(0)` can branch after a helper-parented Projectile hit while same-owner player Projectiles stay ignored. New `synthetic-imported-helper-projguardedtime-any.json` checksum `bd64e9db` and `synthetic-imported-helper-projcontacttime-any.json` checksum `5c6a4e11` prove matching helper-local any-id guard/contact-time branches after helper-parented Projectile guard/contact markers while same-owner player Projectiles stay ignored. New `synthetic-imported-helper-projectile-target.json` checksum `49261b53` proves helper-parented Projectile contact can also mirror explicit-id target memory into the spawning Helper and branch through helper-local `Target(id)`. Focused `EffectActorSystem` coverage proves same-id player-owned Projectile contact stays ignored while helper-parented Projectile contact triggers the helper branch after contact age advances. This is helper-local projectile-contact trigger plus bounded owner-side package telemetry, any-id timing counters, and explicit-id target memory only; exact `ProjContact` / `ProjHit` / `ProjGuarded` timing and lifetime, multi-projectile any-id selection beyond the bounded helper `Proj*Time(0)` routes, exact projectile namespaces/scopes, dynamic ids/params, teams, exact tick order and score movement remain blocked.
- `CompatibilityReport.sounds` now derives SND package telemetry from parsed `soundArchive`: total declared sounds, decoded WAV count, unsupported/raw count, format counts, sample-rate counts, and channel counts. This improves asset compatibility inspection and export data only; exact SND playback, channel priority, mixing, timing, and IKEMEN/MUGEN audio parity remain blocked.
- Required `synthetic-imported-default-fall-official-recovery-threshold.json` checksum `86804271` and `synthetic-imported-default-fall-official-recovery-too-early.json` checksum `ef945ff5` promote official-style synthetic Common1 recovery threshold / too-early rejection routes into `pnpm qa:trace`. The threshold gate proves an official-style `5050 -> 5200 -> 5201 -> 52 -> 0` ground-recovery branch after `hitFall.recoverTime` drops to `0`; the negative gate proves early `command = "recovery"` stays in `5050` while minimum observed `recoverTime >= 1`. This is R1 trace precision only; exact `fall.recovertime` tables, velocity math, exact controller-loop tick order, public bundled KFM support, full Common1 recovery parity, and score movement remain blocked.
- `RuntimeCombatResolutionWorld` now owns the bounded active direct/projectile contact orchestration that was still inline in `PlayableMatchRuntime`: direct move eligibility, reversal checks, HitBy/NotHitBy reject logging, HitOverride redirect hooks, target-memory remembering, direct hit/guard result handoff, projectile-combat callbacks, received-damage/contact memory, and contact presentation emission. `PlayableMatchRuntime` still supplies runtime tick, frame hurtboxes, concrete state-entry hooks, trigger/controller order, active effect stores, and actor roster. Focused `RuntimeCombatResolutionSystem` coverage proves direct target/contact/presentation ordering and projectile callback routing through target/contact/presentation/damage hooks. This is R2 ownership cleanup only; helper-owned combat, projectile target ownership, exact direct/projectile tick order, multi-target/team behavior, exact ReversalDef/HitOverride priority, full combat VM parity, and score movement remain blocked.
- `RuntimeTargetWorld.resolveCandidates` now owns bounded target-candidate filtering from live target memory before current Target* / BindToTarget controller application and active target-binding position application. The match runtime still supplies the currently materialized concrete actor list and owns trigger order, state validation, helper/projectile actor materialization, and combat context. Focused `TargetSystem` coverage proves candidate filtering by actor id and target id plus mutation only against remembered targets. This is R2 ownership cleanup only; helper/projectile target ownership, exact multi-target/team selection, exact target lifetime/bind tick order, full target VM parity, and score movement remain blocked.
- `RuntimeExpressionContextWorld` now owns the bounded active runtime expression/trigger context used by imported state triggers and dynamic controller-param fallback. `PlayableMatchRuntime` delegates target redirects, contact/projectile/effect count reads, command/const/state/anim/hitvar reads, `HitDefAttr`, hitpause/hitover reads, `EnemyNear(index)` / `NumEnemy` read-model wiring, and `InGuardDist` wiring to that world while still owning next-random callbacks, animation timing callbacks, concrete controller dispatch, and VM timing. `RuntimeOpponentSelectionWorld` now owns bounded horizontal body-distance scoring, raw runtime-state ordering, metadata-preserving roster construction, metadata-preserving roster ordering, and stable nearest-roster ordering for explicit runtime opponent lists before `EnemyNear(index)` resolves. `RuntimeEffectLifecycleWorld` can build helper-local `opponentRoster` from explicit lifecycle `opponents` lists through `RuntimeOpponentSelectionWorld`, and `HelperSystem` reuses the same boundary for caller-supplied helper-local `opponentStates` plus richer `opponentRoster` entries with id plus runtime state, so helper-local indexed `EnemyNear` no longer preserves caller order separately from active runtime contexts and can read non-current `TeamSide` when ids are supplied. Focused `RuntimeExpressionContextSystem` coverage proves numeric reads, target redirects, compiled trigger evaluation, shared const/state/HitVar helpers, explicit opponent roster reads through `EnemyNear(1)` / `EnemyNear(var(n))`, bounded nearest-roster sorting for `EnemyNear(0..3)`, `NumEnemy = 2`, default one-opponent fallback, and missing-index fail-closed behavior; focused `RuntimeOpponentSelectionSystem` coverage proves the selection boundary directly plus id-bearing roster construction, focused `EffectActorSystem` coverage proves helper-local nearest-roster reads plus id-bearing non-current `TeamSide`, and focused `EffectLifecycleSystem` coverage proves active/paused lifecycle roster forwarding plus explicit opponent-list routing. This is R2 ownership cleanup only; full expression language parity, composite `HitDefAttr` parity, real teams/simul roster ownership, automatic match-level multi-opponent lifecycle wiring, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, helper/team redirect mutation, exact VM timing, and score movement remain blocked.
- `RuntimeStateTransitionControllerWorld` now owns bounded passive `ChangeState` / `SelfState` setup from raw controller params in the basic `StateControllerExecutor` path. `StateControllerExecutor` still owns controller routing, expression context creation, and broad runtime-controller execution; `RuntimeStateEntryWorld` and `PlayableMatchRuntime` still own active-state entry, state/action lookup, custom-state owner selection, and tick order. Focused `StateTransitionControllerSystem` coverage proves expression fallback, previous-state metadata writes, frame/time reset, optional `ctrl`, missing-value reporting, unchanged-state timing reset, and executor routing. This is R2 ownership cleanup only; exact ChangeState/SelfState tick order, persistent controller semantics, redirects/helper/team ownership, full custom-state breadth, full state-entry VM parity, and score movement remain blocked.
- `RuntimeAnimationControllerWorld` now owns bounded passive `ChangeAnim` / `ChangeAnim2` setup from raw controller params in the basic `StateControllerExecutor` path. `StateControllerExecutor` still owns controller routing, expression context creation, and broad runtime-controller execution; `PlayableMatchRuntime` still owns active-state action lookup, state-owner selection, and tick order. Focused `AnimationControllerSystem` coverage proves expression fallback, self/state-owner source marking, reset, bounded `elem` / `elemtime` seeding against known AIR frame durations, no-value no-op behavior, and executor routing. Required `synthetic-imported-changeanim2-elem.json` checksum `b0b46d33` now also gates one active-state state-owner AIR route for `ChangeAnim2` with `elem = 3` / `elemtime = 1`. This is partial evidence only; missing-action fallback, broader active-state edge cases, redirects/helper/team ownership, full state-owner namespace behavior, exact animation-source parity, and score movement remain blocked.
- `RuntimeKinematicControllerWorld` now owns bounded passive `VelSet`, `VelAdd`, `VelMul`, `HitVelSet`, `PosSet`, `PosAdd`, and `Gravity` setup from typed `kinematic:*` operations or raw controller params. `StateControllerExecutor` still owns controller routing, expression context creation, and broad runtime-controller execution; `RuntimeKinematicsWorld` still owns per-frame actor integration, sandbox gravity, ground snap, and landing hooks. Focused `KinematicControllerSystem` coverage proves typed setup, raw expression fallback, default-axis behavior, hit-velocity flags, gravity defaults, and executor routing. This is R2 ownership cleanup only; exact MUGEN/IKEMEN physics, velocity tick order, `yaccel` constants, helper/team/redirect ownership, full kinematic VM parity, and score movement remain blocked.
- `RuntimeBoundsControllerWorld` now owns bounded passive `PlayerPush`, `PosFreeze`, and `ScreenBound` setup from typed `collision:playerpush` / `bounds:*` operations or raw controller params. `StateControllerExecutor` still owns controller routing, expression context creation, and broad runtime-controller execution; `RuntimeActorConstraintWorld` still owns per-frame reset/projection, stage clamp, and body-push separation. Focused `BoundsControllerSystem` coverage proves typed and raw fallback semantics plus executor routing. This is R2 ownership cleanup only; exact player/edge collision, team/helper push behavior, screen-edge/camera parity, PosFreeze tick order, full constraint VM parity, and score movement remain blocked.
- `RuntimeHitFallControllerWorld` now owns bounded passive `HitFallVel`, `HitFallDamage`, and `HitFallSet` mutations from typed `hitfall:*` operations or raw controller params. `StateControllerExecutor` still owns controller routing, expression context creation, and broad runtime-controller execution. Focused `HitFallControllerSystem` coverage proves typed `HitFallSet`, raw expression fallback, stored fall velocity application, bounded `fall.defence_up` scaling, and nonlethal deferred fall damage. This is R2 ownership cleanup only; exact Common1 controller-loop order, helper/team/redirect ownership, exact recovery thresholds/velocity math, full fall/get-hit parity, and score movement remain blocked.
- `RuntimeStateTypeWorld` now owns bounded passive `StateTypeSet` `stateType` / `moveType` / `physics` setup from typed `metadata:statetypeset` operations or raw controller params. `StateControllerExecutor` still owns controller routing and broad runtime-controller execution. Focused `StateTypeSystem` coverage proves typed setup, raw case-normalized fallback, and invalid raw no-op behavior. This is R2 ownership cleanup only; dynamic metadata expressions, helper/team/redirect ownership, exact physics/tick-order interactions, full StateTypeSet parity, and score movement remain blocked.
- `RuntimeDamageScaleWorld` now owns bounded passive `AttackMulSet` and `DefenceMulSet` multiplier setup from typed `damage-scale:*` operations or raw controller params. `StateControllerExecutor` still owns controller routing, expression context creation, and broad runtime-controller execution. Focused `DamageScaleSystem` coverage proves typed setup, raw expression fallback, clamp behavior, and no-value no-op behavior. This is R2 ownership cleanup only; exact MUGEN/IKEMEN scaling stack/order, helper/projectile/custom-state/guard edge cases, redirect ownership, controller-loop timing, full damage-scale parity, and score movement remain blocked.
- `RuntimeHitDefenseWorld` now owns bounded passive `HitBy`, `NotHitBy`, and `HitOverride` slot setup/removal from typed `eligibility:*` / `hitoverride` operations or raw controller params. `StateControllerExecutor` still owns controller routing, expression context creation, and broad runtime-controller execution. Focused `HitDefenseSystem` coverage proves typed and raw setup/removal semantics. This is R2 ownership cleanup only; exact attr grammar, slot priority, helper/custom-state redirect breadth, forceair/forceguard edge order, controller-loop timing, full defensive-slot parity, and score movement remain blocked.
- `RuntimeHitDefControllerDispatchWorld` now owns bounded active-state HitDef activation dispatch: controller telemetry, typed `hitdef` operation selection, raw fallback attack params, fired-HitDef dedupe, current-frame `Clsn1` hitbox handoff, currentMove mutation, attack movetype/control writes, and operation telemetry. `PlayableMatchRuntime` still owns trigger filtering, active-state order, current-frame lookup, direct/projectile contact resolution, Common1/custom-state routing, and target/reversal consequences. Focused `HitDefSystem` coverage proves activation payloads and duplicate suppression through this boundary. This is R2 ownership cleanup only; exact HitDef trigger lifetime, contact ordering, multi-hit windows, helper/projectile/custom-state ownership, broad attr grammar, hitpause/tick order, full HitDef VM parity, and score movement remain blocked.
- `RuntimeReversalControllerDispatchWorld` now owns bounded active-state ReversalDef side-effect dispatch: controller telemetry, typed `reversaldef` operation selection, raw fallback activation payload, activation handoff into `RuntimeReversalWorld`, and operation telemetry. `PlayableMatchRuntime` still owns trigger filtering, active-state order, current-frame hitbox lookup, and later counter-result state routing. Focused `ReversalSystem` coverage proves controller/op telemetry plus activation through this boundary. This is R2 ownership cleanup only; exact ReversalDef priority, guard/projectile/helper/custom-state counter breadth, attr grammar, trigger lifetime, hitpause/tick order, full ReversalDef VM parity, and score movement remain blocked.
- `RuntimeEffectSpawnControllerDispatchWorld` now owns bounded active-state Explod / RemoveExplod / ModifyExplod / Helper / Projectile / ModifyProjectile side-effect dispatch: controller telemetry, typed effect operation selection, spawn/count mutation handoff into `RuntimeEffectSpawnWorld`, and success-gated operation telemetry. `PlayableMatchRuntime` still owns trigger filtering, active-state order, actor/opponent context, effect actor world ownership, and exact spawn/combat ordering. Focused `EffectSpawnSystem` coverage proves successful Explod telemetry/mutation and failed ModifyExplod no-operation gating through this boundary. This is R2 ownership cleanup only; exact effect spawn tick order, helper-owned effect namespaces, dynamic effect params, helper-owned projectile combat/contact/target memory, full effect/helper/projectile VM parity, and score movement remain blocked.
- `RuntimeFallEnvShakeControllerDispatchWorld` now owns bounded active-state FallEnvShake side-effect dispatch: controller telemetry, typed `fallenvshake` operation selection, fall-shake event handoff, `hitFall.envShake` cleanup, and operation telemetry after a real event. `PlayableMatchRuntime` still owns trigger filtering, active-state order, actor/world ownership, and upstream HitDef fall metadata. Focused `EnvShakeSystem` coverage proves `FallEnvShake` telemetry/mutation through this boundary. This is R2 ownership cleanup only; exact waveform, pause/stage/layer interaction, helper/redirect ownership, full presentation parity, and score movement remain blocked.
- `RuntimeActorConstraintControllerDispatchWorld` now owns bounded active-state `Width` side-effect dispatch: controller telemetry, typed `collision:width` operation selection, operation telemetry, and body-width mutation handoff into `RuntimeActorConstraintWorld`. `PlayableMatchRuntime` still owns trigger filtering, active-state order, per-frame constraint reset, stage clamp, and body-push ordering. Focused `ActorConstraintSystem` coverage proves `Width` telemetry/mutation through this boundary. This is R2 ownership cleanup only; exact player/edge collision, team/helper push behavior, screen-edge/camera parity, Width edge semantics, full constraint VM parity, and score movement remain blocked.
- `RuntimePauseControllerDispatchWorld` now owns bounded active-state Pause/SuperPause side-effect dispatch: controller telemetry, typed `pause` operation selection, apply-controller callback handoff, and operation telemetry after a real pause result. `RuntimeMatchPauseControllerWorld` now owns result side effects after that handoff, while `PlayableMatchRuntime` still owns trigger filtering, active-state order, paused-match progression, and hitpause ignored routing. Focused `PauseSystem` coverage proves `SuperPause` telemetry/application through this boundary. This is R2 ownership cleanup only; exact pause layering, super background/sound/spark timing, helper/redirect ownership, full pause VM parity, and score movement remain blocked.
- `RuntimeEnvShakeControllerDispatchWorld` now owns bounded active-state EnvShake side-effect dispatch: controller telemetry, typed `envshake` operation selection, operation telemetry, and `EnvShake` event handoff into `RuntimeEnvShakeWorld`. `PlayableMatchRuntime` still owns trigger filtering, active-state order, actor/world ownership, and FallEnvShake routing. Focused `EnvShakeSystem` coverage proves `EnvShake` telemetry/mutation through this boundary. This is R2 ownership cleanup only; exact waveform, pause/stage/layer interaction, helper/redirect ownership, full presentation parity, and score movement remain blocked.
- `RuntimeEnvColorControllerDispatchWorld` now owns bounded active-state EnvColor side-effect dispatch: controller telemetry, typed `envcolor` operation selection, operation telemetry, and `EnvColor` event handoff into `RuntimeEnvColorWorld`. `PlayableMatchRuntime` still owns trigger filtering, active-state order, stage-world ownership, and pause/hitpause callback routing. Focused `EnvColorSystem` coverage proves `EnvColor` telemetry/mutation through this boundary. This is R2 ownership cleanup only; exact blend math, layer/window ordering, pause timing, renderer parity, full presentation parity, and score movement remain blocked.
- `RuntimeAudioControllerDispatchWorld` now owns bounded active-state audio side-effect dispatch: controller telemetry, typed `audio:*` operation selection, operation telemetry, and `PlaySnd` / `StopSnd` event handoff into `RuntimeAudioWorld`. `PlayableMatchRuntime` still owns trigger filtering, active-state order, hit/contact timing, and actor context. Focused `AudioEventSystem` coverage proves `PlaySnd` telemetry/mutation through this boundary. This is R2 ownership cleanup only; exact SND playback, channel priority, mixing, FightFX/common fallback, full audio parity, and score movement remain blocked.
- `RuntimeContactControllerDispatchWorld` now owns bounded active-state contact-memory side-effect dispatch: controller telemetry, typed `contact:*` operation selection, operation telemetry, `HitAdd` mutation, and `MoveHitReset` direct contact reset through `RuntimeContactMemoryWorld`. `PlayableMatchRuntime` still owns trigger filtering, active-state order, and direct/projectile contact creation. Focused `ContactMemorySystem` coverage proves `HitAdd` telemetry/mutation and `MoveHitReset` reset telemetry through this boundary. This is R2 ownership cleanup only; exact combo lifetime, helper/projectile contact ownership, guard-count parity, full CNS VM parity, and score movement remain blocked.
- `RuntimeTargetControllerDispatchWorld` now owns bounded active-state Target / BindToTarget side-effect dispatch: controller telemetry, typed `target:*` / `bindtotarget` operation selection, operation telemetry, and mutation handoff into `RuntimeTargetWorld` with match-owned callbacks for damage scaling, TargetState entry, and target constants. `RuntimeTargetWorld` owns live target-memory candidate filtering before those mutations; `PlayableMatchRuntime` still owns trigger filtering, active-state order, concrete state validation, and the concrete actor roster. Focused `TargetSystem` coverage proves `TargetLifeAdd` telemetry/mutation, `BindToTarget` anchor/position telemetry, and remembered-target candidate filtering through this boundary. This is R2 ownership cleanup only; helper/projectile target ownership, exact multi-target semantics, throw binding, full CNS VM parity, and score movement remain blocked.
- `RuntimeAfterImageSampleWorld` now owns bounded `AfterImage` sample projection: cloned actor position, facing, self/state-owner sprite owner metadata, and current AIR frame group/index/offset are produced outside `PlayableMatchRuntime` before `RuntimeSpriteEffectWorld` captures ghost-trail samples. This is R2 ownership cleanup only; exact ghost-trail sampling cadence, material/blend parity, helper/redirect presentation ownership, renderer parity, full CNS VM parity, and score movement remain blocked.
- `RuntimeFrameWorld` now owns bounded current AIR frame lookup plus collision-box projection for runtime and snapshot consumers: active move hitboxes, frame hitboxes, frame hurtboxes, cloned boxes, and missing-frame default hurtboxes route through one renderer-independent contract. This is R2 ownership cleanup only; exact collision priority, frame timing, guard-distance thresholds, rotated/scaled box semantics, helper/redirect collision ownership, renderer parity, full CNS VM parity, and score movement remain blocked.
- `RuntimeSpriteEffectControllerWorld` now owns bounded active-state sprite-effect side-effect dispatch: controller telemetry, typed `sprite-effect:*` operation extraction, operation telemetry, and mutation handoff into `RuntimeSpriteEffectWorld` for `SprPriority`, `PalFX`, `AfterImage`, `AfterImageTime`, and `AngleSet` / `AngleAdd` / `AngleDraw`. `PlayableMatchRuntime` still owns trigger filtering, active-state order, hitpause selection, and render projection. Focused `SpriteEffectSystem` coverage proves PalFX telemetry/mutation and AfterImage sampling through this boundary. This is R2 ownership cleanup only; exact visual tick order, helper/redirect ownership, renderer parity, full CNS VM parity, and score movement remain blocked.
- `RuntimeStateEntrySetupWorld` now owns bounded imported State -1 setup-controller selection before command routing: imported-only guard, `ChangeState` bypass, trigger gating, setup-controller classification, and execution handoff. `PlayableMatchRuntime` still supplies trigger evaluation, random/stage-time context, and concrete execution through `RuntimeControllerDispatchWorld`. Focused `RuntimeStateEntrySetupSystem` coverage proves imported setup execution, non-imported skip, trigger failure, and non-setup filtering. This is R2 ownership cleanup only; exact State -1 ordering, persistent semantics, redirect/helper/team scopes, full CNS VM parity, and score movement remain blocked.
- `RuntimeStateEntryRouteWorld` now owns bounded State -1 `ChangeState` route selection before local fallback input handling: entry scan order, `ChangeState` filtering, trigger-gated route selection, dynamic `value` resolver handoff, route telemetry, authored `stateMoves` handoff, and raw state-entry fallback. `PlayableMatchRuntime` still supplies trigger evaluation, expression context, `startMoveWithSpec`, concrete `enterState`, and tick order. Focused `RuntimeStateEntryRouteSystem` coverage proves the boundary without changing trace semantics. This is R2 ownership cleanup only; exact State -1 VM order, helper/team/redirect command ownership, persistent controller semantics, and full CNS VM parity remain blocked.
- `RuntimeControllerDispatchWorld` now gives active imported runtime controllers, State -1 setup controllers, and pre-facing `AssertSpecial` controller application a shared execution boundary. `PlayableMatchRuntime` delegates runtime-state replacement, context handoff, controller telemetry, typed-operation telemetry, and unsupported-controller reporting through that world while still owning trigger filtering, `ChangeState` / `ChangeAnim`, side-effect dispatch, and tick order. This is R2 ownership cleanup only; exact CNS controller-loop order, persistent semantics, side-effect VM parity, helper/team/redirect execution, and score movement remain blocked.
- `RuntimeResourceWorld` now gives `RuntimeResourceSystem` a named resource boundary for authored max-resource resolution, life/power clamping, control writes, resource controllers, and variable/range assignments while keeping the existing helper functions as delegating compatibility shims. Focused `RuntimeResourceSystem` coverage proves direct world mutation for life, power, ctrl, and vars. This is R2 ownership cleanup only; exact CNS resource timing, helper/team/redirect scopes, round/KO flow, and score movement remain blocked.
- `RuntimeTraceGate.requiredActorFrames` can now require observed `hitFall.recoverTime` minimum-positive windows through `observedHitFallRecoverTimeMinAtLeast`. Required `synthetic-imported-default-fall-recovery-too-early.json` checksum `050e7e3c` and optional `kfm-official-default-fall-recovery-too-early.json` checksum `d2edbde4` now require P2 state `5050` actor-frame evidence with minimum observed `recoverTime >= 1`, first-to-last drop `>= 1`, and at least 2 summarized frames while early `command = "recovery"` is active and recovery states stay forbidden. This tightens bounded early recovery-input rejection without claiming exact `fall.recovertime` tables, controller-loop tick order, velocity math, public bundled KFM support, or full MUGEN/IKEMEN recovery parity.
- `kfm-official-default-fall-recovery-threshold.json` checksum `bf7b058a` now requires the optional private official KFM fixture to prove real P2 state `5050` actor-frame evidence with positive `hitFall.recoverTime`, a first-to-last `recoverTime` drop inside the summarized `5050` bucket, and at least 2 summarized frames before `5200` with `recoverTime = 0`. This tightens the bounded Common1 ground-recovery threshold oracle without claiming exact `fall.recovertime` tables, controller-loop tick order, velocity math, public bundled KFM support, or full MUGEN/IKEMEN recovery parity.
- `RuntimeStateEntryWorld` now owns bounded state-entry mutation that was still inline in `PlayableMatchRuntime`: state availability lookup, state-number transition metadata, changed-state elapsed reset, owner-backed custom-state assignment/clearing, stale current-move cancellation, `firedHitDefs` clearing, contact reset callback, StateDef `type` / `movetype` / `physics` / `ctrl` / `velset` application, and self/state-owner animation handoff. Focused `RuntimeStateEntrySystem` coverage proves normal state entry, owner-backed custom states, owner-derived previous-state metadata, and metadata normalization. This is R2 ownership cleanup only; exact CNS `ChangeState` / `SelfState` tick order, persistent-controller timing, helper/team/root redirects, and score movement remain blocked.
- `RuntimeTraceGate.requiredActorFrameSequences` and `requiredControllerEventSequences` now require `synthetic-imported-default-fall-recovery.json` checksum `d83797d9` to prove imported P2 full-chain order `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120`, while `RuntimeTraceGate.requiredActorFrames` still requires `5110` bounded `hitFall.downRecoverTime` countdown range (`max >= 58`, `min <= 54`, first-to-last drop `>= 1`, at least 2 summarized frames). This tightens bounded Common1 fall recovery evidence without changing score bands; exact `down.recovertime` / `fall.recovertime` tables, exact controller-loop tick order, animation timing, bounce/velocity math, recovery-input breadth, public bundled KFM support, and full MUGEN/IKEMEN fall recovery parity remain blocked.
- `synthetic-imported-helper-projectile-target.json` checksum `49261b53` gates bounded helper-parented Projectile explicit-id target memory from the helper micro-VM. The required trace proves helper state `1200` spawns owner-side Projectile id `8860`, damages P2 for 18, records owner target link `p1 -> p2 / 8860`, mirrors helper target link `p1-helper-0 -> p2 / 8860`, and branches to `1225/955` through `NumTarget(8860)` plus `Target(8860), Life`.
- `synthetic-imported-helper-target.json` checksum `68f95b67` gates bounded helper-owned explicit-id target memory from the helper micro-VM. The required trace proves helper state `1200` activates `HitDef id = 8877`, damages P2 for 31, records target link `p1-helper-0 -> p2 / 8877`, emits helper-side `S5,1` plus FightFX `F7007`, and branches to `1223/953` through `NumTarget(8877)` plus `Target(8877), Life`. `synthetic-imported-helper-bare-target.json` checksum `15f3c1db` extends that explicit-id helper HitDef memory to a helper-local bare `Target, Life` branch: helper `HitDef id = 8878` damages P2 for 35, records target link `p1-helper-0 -> p2 / 8878`, emits helper-side `S5,5` plus FightFX `F7011`, and branches to `1230/962`. `synthetic-imported-helper-targetstate.json` checksum `011633b8` extends the direct helper HitDef memory path to helper-local `TargetState value = 888`, routing P2 through helper-owner state data `888 -> 889` and returning through `SelfState`. `synthetic-imported-helper-hitdef.json` checksum `89f9e876` remains the bounded helper-owned direct `HitDef` combat proof: helper state `1200` activates `HitDef`, damages P2 for 29, emits helper-side `S5,0` plus FightFX `F7006` contact-package telemetry, and branches to `1222/952` after `EnemyNear, Life` observes the hit. `RuntimeMatchInteractionWorld` routes bounded helper combat after normal direct/projectile combat, and helper `ChangeState` applies authored `Statedef` type/movetype/physics/ctrl metadata. `synthetic-imported-helper-projhit.json` checksum `2d9a281e` gates bounded helper-local `ProjHit(8853)` dispatch from the helper micro-VM. The required trace proves a spawned visual Helper routes through `1200 -> 1216 -> 1217`, spawns owner-side Projectile anim `946` with `parentId = p1-helper-0`, branches after that helper-parented Projectile contacts P2, exposes owner-side target-link evidence, and emits a shared hit sound/FightFX spark package. `synthetic-imported-helper-projguard.json` checksum `1c2c18a5` and `synthetic-imported-helper-projcontact.json` checksum `67ed6c2d` carry the matching guard package contract. `synthetic-imported-helper-modifyprojectile.json` checksum `09d3f7e4` keeps bounded helper-local `ModifyProjectile` mutation gated; `synthetic-imported-helper-projectile.json` checksum `b6269136` keeps bounded helper-local `Projectile` spawn gated; and `synthetic-imported-helper-numproj.json` checksum `3312a554` keeps helper-local `NumProjID(8851) > 0` count evidence gated. Previous `synthetic-imported-helper-numhelper.json` checksum `4e32e951` keeps helper-local `NumHelper(id)` gated; previous `synthetic-imported-helper-numexplod.json` checksum `4328278a` keeps helper-local `NumExplod(id)` gated; previous `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` keeps bounded helper-local static `ModifyExplod` mutation gated; previous `synthetic-imported-helper-removeexplod.json` checksum `ff8658a2` keeps bounded helper-local static `RemoveExplod` cleanup gated; previous `synthetic-imported-helper-explod.json` checksum `87ae363f` keeps bounded helper-local static `Explod` spawn gated; previous `synthetic-imported-helper-bindtoroot.json` checksum `bf72306c` keeps bounded helper-local static `BindToRoot` root binding gated with root `ownerBind` payload evidence; previous `synthetic-imported-helper-bindtoparent.json` checksum `f9922c0e` keeps bounded helper-local static `BindToParent` gated with parent `ownerBind` payload evidence; previous `synthetic-imported-helper-enemynear.json` checksum `35498955` keeps bounded helper-local `EnemyNear, ...` trigger redirect reads gated; previous `synthetic-imported-target-dynamic-redirect.json` checksum `9985b62a` keeps bounded dynamic executable `Target(expr), ...` trigger redirect reads gated. The current code also supports caller-provided `EnemyNear(index)` / `EnemyNear(var(n))` lists in evaluator/helper tests when an explicit `opponentStates` roster is supplied. This is bounded helper-local direct HitDef, explicit/default/bare HitDef plus Projectile target memory, bounded direct-HitDef Target side effects/owner-backed TargetState, visual Projectile spawn/count/mutation/contact-trigger, and contact-package evidence only; helper-owned custom state tables, throws, teams, exact helper hitpause/tick order, exact helper HitDef/Projectile lifetime parity, player-state `BindToParent` / `BindToRoot`, exact helper effect-count/ownership parity, dynamic bind/effect/projectile params, position rebinding, nested helper ancestry where root differs from parent, broader indexed/team/helper-owned redirect ownership beyond caller-provided `EnemyNear(index)` lists, helper-owned opponents, exact `ProjContact` / `ProjHit` timing and lifetime, helper-owned effect namespaces, helper-bound Explod/Projectile timing/mutation/contact beyond bounded static routes, unsupported or negative target-id expressions, mutation through redirects, multi-target selection, exact target/helper lifetime or tick order, and full helper/effect/redirect parity remain blocked.
- `RuntimeStateClockWorld` now owns bounded `Time` / state elapsed clock mutation for active-frame advance and changed-state elapsed reset. `PlayableMatchRuntime` delegates both `stateElapsed += 1` and transition reset through that boundary while preserving the current `-1 -> 0` first-frame convention. Focused `RuntimeStateClockSystem` tests prove advance, reset, and no-op transition behavior. This is R2 state-clock ownership cleanup only; exact CNS `Time` tick-order parity, persistent-controller timing parity, pause/hitpause timing changes, helper/team/redirect state clocks, and score movement remain blocked.
- `RuntimeStateMetadataWorld` now owns bounded previous-state transition metadata writes for `prevStateNo`, `prevAnimNo`, `prevStateType`, and `prevMoveType`. `PlayableMatchRuntime` delegates state-number changes through this boundary, and the basic `StateControllerExecutor` `ChangeState` / `SelfState` path uses the same helper for runtime-only executor output. Focused `RuntimeStateMetadataSystem`, `RuntimeCnsSubset`, and `PlayableMatchRuntime` Prev-trigger tests prove changed-state capture and unchanged-state no-op behavior. This is R2 state-metadata ownership cleanup only; exact state-entry tick order, redirects/helper/team previous-state ownership, persistent controller semantics, full `ChangeState` / `SelfState` parity, and score movement remain blocked.
- `TargetSystem` now prunes `TargetBind` binding records during target-memory advance when the bound actor id / target id no longer survives expiry, and active `TargetBind` / `BindToTarget` position application still fails closed without live target memory. Focused `TargetSystem` coverage proves expiry pruning, active binding success, and stale-binding no-mutation behavior. This is R2 target-lifetime cleanup only; helper/team/multi-target ownership, exact bind/drop tick order, throws/custom-state binding parity, and full target parity remain blocked.
- `TargetSystem` now gates active `TargetBind` and `BindToTarget` position application against live target memory, so stale binding records no longer move actors after target memory disappears. Focused `TargetSystem` coverage proves active binding success and stale-binding no-mutation behavior. This is R2 target-lifetime cleanup only; helper/team/multi-target ownership, exact bind/drop tick order, throws/custom-state binding parity, and full target parity remain blocked.
- `TargetSystem` now owns bounded `BindToTarget` `postype` anchor math that was still inline in `PlayableMatchRuntime`: match runtime supplies only character constants, while `resolveRuntimeTargetAnchor` resolves `Foot`, `Mid`, and `Head` against MUGEN `size.*.pos` constants. Focused `TargetSystem` coverage proves constant-backed anchor math and preserves current `BindToTarget` placement. This is R2 ownership cleanup only; exact bind tick order, multi-target/team/helper ownership, throws/custom-state binding, and full target parity remain blocked.
- `synthetic-imported-default-fall-recovery.json` checksum `d83797d9` now gates bounded Common1-style full fall recovery order: imported P2 must show actor-frame sequence `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120`, state `5110` with `moveType = H` and a down-counting `hitFall.downRecoverTime` range plus first-to-last drop before state `5120` with `moveType = I`, and controller/typed-operation evidence through `5000` / `5030` / `5050` plus `5110` `ChangeState` `Get Up` before `5120` `VelSet` / `kinematic:velset` / `HitFallSet` / `hitfall:hitfallset` / `ChangeState` `Stand`. Optional local `kfm-official-default-fall-recovery.json` checksum `b1c6456a` still requires matching `5110 -> 5120` actor-frame evidence plus bounded official KFM `5110`/`5120` controller/typed-operation order. Exact Common1 controller-loop tick order, animation timing, threshold tables, exact `down.recovertime` / `fall.recovertime` tables, velocity/bounce math, recovery-input breadth, and full fall-recovery parity remain blocked.
- Direct `HitDef` priority is now bounded evidence, not parity: static `priority` params compile into typed `hitdef` operation data, same-tick direct attack clashes can consume the lower-priority direct attack, and `synthetic-imported-hitdef-priority.json` proves the higher-priority attacker alone deals damage. Exact MUGEN/IKEMEN priority classes, reversal/projectile/helper/custom-state priority, multi-hit priority, and full tick-order parity remain blocked.
- Static `SprPriority` is now typed/evidence-gated: `value` / `priority` params lower into `sprite-effect:sprpriority`, the imported runtime records the operation, and `synthetic-imported-sprpriority.json` requires actor-frame `spritePriority = 5`. Exact layer/shadow/helper/Explod draw-order parity remains blocked.
- Static `PalFX` is now typed/evidence-gated: `time/add/mul/color/invert` params lower into `sprite-effect:palfx`, the imported runtime records the operation, and `synthetic-imported-palfx.json` requires clamped material telemetry. Exact palette math, blending, `sinadd`, remap interaction, and timing parity remain blocked.
- Static `RemapPal` is now typed/evidence-gated: `source/dest` params lower into `sprite-effect:remappal`, the imported runtime records the operation, and `synthetic-imported-remappal.json` requires palette-remap telemetry. ACT/SFF pixel remapping, palette application, PalFX interaction, and timing parity remain blocked.
- Static `AfterImage` / `AfterImageTime` are now typed/evidence-gated: numeric trail params lower into `sprite-effect:afterimage` / `sprite-effect:afterimagetime`, the imported runtime records both operations, and `synthetic-imported-afterimage.json` requires bounded actor-frame ghost-trail telemetry. Exact blend modes, palette/remap interaction, sampling cadence, persistence edge cases, and engine-perfect timing remain blocked.
- `HitDefAttr` is now runtime-backed for the bounded imported path: expression/trigger evaluation calls the combat attr matcher against the current active `HitDef` move attr, so KFM-style `HitDefAttr = SC, NA, SA, HA` plus `MoveContact` branches can route instead of hard-returning false. The function-style `HitDefAttr(...)` form is also supported. `synthetic-imported-hitdefattr.json` is now a required trace gate. Full cancel windows, priority classes, redirects, helper/projectile attrs, and engine-perfect attr parity remain blocked.
- Required synthetic auto guard-start/end now have controller-event plus actor-frame state-order evidence: `synthetic-imported-auto-guard-start.json` checksum `0c734290` requires P2 state `120` `ChangeState` `Guard Start Done` plus actor-frame `120` before `130`, and `synthetic-imported-auto-guard-end.json` checksum `d1dc0aa3` requires P2 state `120` `Guard Start Done` before state `130` `ChangeState` `Stop Guarding`, actor-frame `120 -> 130 -> 140`, and final idle/control evidence. Exact proximity guard, exact guard-end timing, full Common1 controller-loop parity, guard effects, and full MUGEN/IKEMEN guard VM parity remain blocked.
- Required synthetic guard-hit gates now have actor-frame state/physics/body/push telemetry: `synthetic-imported-default-guard-state.json` checksum `016938a1` gates stand `150 -> 151`, `synthetic-imported-crouch-guard-state.json` checksum `6c4321af` and `synthetic-imported-diagonal-crouch-guard-state.json` checksum `1dd33fb5` gate crouch `152 -> 153`, `synthetic-imported-air-guard-state.json` checksum `ce9cc9ba` gates air `154 -> 155` with bounded air velocity evidence, and `synthetic-imported-air-guard-landing.json` checksum `d6986d7f` gates the bounded synthetic air landing handoff `154 -> 155 -> 52`. This strengthens existing controller/typed-operation order gates without claiming exact proximity guard, guard effects, exact air physics/timing, state-`52` internals, controller-loop parity, or full guard VM parity.
- Direct-contact triggers now expose bounded age counters: `MoveContact`, `MoveHit`, and `MoveGuarded` return frame counts after matching direct `HitDef` contact in the current owner state, and partial `MoveHitReset` clears that direct-contact memory without touching projectile contact memory. `synthetic-imported-movehit-counter.json` gates `MoveHit >= 1`, and `synthetic-imported-movehitreset.json` checksum `719c2cc7` gates typed `contact:movehitreset` preventing the later `MoveHit >= 1` branch into state `263`. Exact first-tick timing, hitpause accounting, helpers/redirects/teams, multi-target lifetime, and full MUGEN/IKEMEN contact-trigger parity remain blocked.
- Direct hit-count triggers now exist for the bounded two-actor path: `HitCount` and `UniqHitCount` increment after direct `HitDef` hit contact in the current owner state, reset with state entry / partial `MoveHitReset`, and are trace-gated by `synthetic-imported-hitcount.json`. Guard/projectile counts, helpers, multi-target uniqueness, combo lifetime, hitpause accounting, and exact parity remain blocked.
- Variable controllers now have dedicated required trace evidence: `synthetic-imported-variable.json` checksum `3b33f7a8` gates typed `variable:varset`, `variable:varadd`, `variable:varrandom`, and `variable:varrangeset` in an imported owner state, then proves the written vars can route into state `288` through `var(...)` triggers. Exact MUGEN random stream, variable scopes, helper/parent/root redirects, sysvar/fvar lifetime parity, IKEMEN map vars, dynamic lowering, and full controller VM parity remain blocked.
- Resource controllers now have dedicated required trace evidence: `synthetic-imported-resource.json` checksum `7bbcb2e4` gates typed `resource:lifeadd`, `resource:lifeset`, `resource:poweradd`, and `resource:powerset` in an imported owner state, then proves written life/power values can route into state `289` through `Life` / `Power` triggers with actor-frame and final P1 evidence for life `750` and power `900`. Exact scaling, redirects, helper/team ownership, round/KO flow, dynamic lowering, and full controller VM parity remain blocked.
- Control flag writes now have dedicated required trace evidence: `synthetic-imported-control.json` checksum `80c4c446` gates typed `resource:ctrlset` in an imported owner state and proves final P1 control restored in state `200`. Dynamic expressions, helper/redirect ownership, exact state-entry control timing, and full control semantics remain blocked.
- Animation controllers now have dedicated required trace evidence and an executor-side system boundary: `synthetic-imported-animation.json` checksum `ee651cbc` gates bounded `ChangeAnim` retargeting to action `205` and partial `ChangeAnim2` state-owner action retargeting to `206` through actor-frame/final-animation evidence in an imported owner state; `synthetic-imported-changeanim2-elem.json` checksum `b0b46d33` gates bounded active-state `ChangeAnim2` `elem = 3` / `elemtime = 1` positioning against a known state-owner AIR action with authored frame durations `[4,5,6]`. Focused `AnimationControllerSystem` coverage still proves basic executor retargeting and bounded `elem` / `elemtime` seeding. Missing-action fallback, broader active-state edge cases, redirects, helper/custom-state ownership, multi-import SFF namespaces, exact controller tick order, and exact animation-source parity remain blocked.
- Gravity now has dedicated required trace evidence: `synthetic-imported-gravity.json` checksum `92161764` gates typed `kinematic:gravity` operation evidence in an airborne imported owner state and requires actor-frame vertical velocity telemetry. Exact `yaccel` constants, floor snap, pause/tick order, and full MUGEN/IKEMEN air-physics parity remain blocked.
- Static movement/position controllers now have dedicated required trace evidence: `synthetic-imported-kinematic.json` checksum `92804390` gates typed `kinematic:velset`, `kinematic:veladd`, `kinematic:velmul`, `kinematic:posset`, and `kinematic:posadd` operation evidence plus bounded actor-frame position/velocity telemetry. Dynamic expressions, exact physics/tick order, floor snapping, helper/custom-state ownership, and full movement parity remain blocked.
- Sound controllers now have dedicated required trace evidence and a named runtime boundary: `synthetic-imported-sound.json` checksum `91574367` gates partial `PlaySnd`, `SndPan`, and `StopSnd` runtime sound events in an imported owner state, including parsed `S5,0`, channel `2`, static `lowpriority = 1`, static `volumescale = 50`, static `freqmul = 0.5`, static `loop = 1`, static `PlaySnd pan = 32`, and static `SndPan pan = -48` telemetry, while static params lower into typed `audio:playsnd` / `audio:sndpan` / `audio:stopsnd` operations before `RuntimeAudioWorld` owns bounded player event insertion for Web Audio/debug/trace consumption. Focused coverage also proves static `abspan` lowering/event projection. `HelperSystem` now emits the same bounded `RuntimeSoundEvent` shape for helper-local audio controllers inside current visual Helper actors, helper snapshots clone that sound history, and `MugenAudioSystem` scans effect snapshots as well as player snapshots. The Web Audio route now has bounded explicit-channel replace / low-priority skip / pan / stop-all behavior plus bounded `volumescale` gain scaling, `freqmul` playback-rate scaling, loop handling, and actor/camera-aware stereo panning for decodable SND playback. Dynamic sound refs, SND decode breadth, Web Audio timing/mixing, legacy `volume`, exact priority semantics, dynamic pan params, exact helper/redirect ownership, and exact MUGEN/IKEMEN audio parity remain blocked.
- No-op controllers now have dedicated required trace evidence: `synthetic-imported-noop.json` checksum `2877b222` gates imported `Null`, browser no-op `ForceFeedback`, debug clipboard no-ops `DisplayToClipboard` / `AppendToClipboard` / `ClearClipboard`, deprecated dust presentation no-op `MakeDust`, and helper-lifecycle no-op `DestroySelf` typed `noop:*` operation visibility in an owner state while the route continues into `HitDef`. Device force feedback, debug text rendering, clipboard output, dust rendering, helper removal/lifecycle side effects, no-op side effects, helper/redirect ownership, exact timing, and full CNS VM parity remain blocked.
- Imported compatibility telemetry now has a named runtime boundary: `RuntimeCompatibilityTelemetryWorld` owns imported/owner-backed actor filtering, executed-state and routed-entry bookkeeping, controller/operation count keys, bounded controller-event history, active-command projection, command-history handoff, and `compatibilitySession` construction before debug/trace/Studio consumption. This is ownership cleanup only; new controller semantics, exact CNS VM timing, and full imported-runtime parity remain blocked.
- Runtime snapshots now have a named runtime boundary: `RuntimeSnapshotWorld` owns full match snapshot envelope assembly, stage/camera projection, player actor projection, and bounded final effect snapshot aggregation before debug/trace/render consumption. This includes selected P1 action, playback/speed/toggles, match pause/round/compatibility-session handoff, actor identity/source, sprite-owner metadata, cloned runtime state, target refs/bindings, active move or AIR-frame collision boxes, fallback hurtbox, cloned sound/hit-effect/env-shake event histories, stable cloned effect snapshot ordering, and log trimming. This is ownership cleanup only; target semantics, effect actor VM semantics, compatibility telemetry semantics, renderer parity, motif/screenpack snapshot rules, and full imported-runtime parity remain blocked.
- Visual-helper lifecycle now has explicit owner-side removal and bounded helper-local VM coverage: `HelperSystem` exposes bounded helper removal by helper id or runtime serial, `RuntimeEffectActorWorld` owns the store mutation without crossing p1/p2 owner stores, `RuntimeEffectSpawnWorld` passes owner runtime programs and animation maps into Helpers, and `RuntimeEffectLifecycleWorld` passes owner/root runtime state for bounded read-only `Parent, ...` / `Root, ...` helper redirects plus current opponent runtime state/id for bounded helper-local `EnemyNear, ...`, optional explicit `opponentStates` for caller-provided `EnemyNear(index)` and `NumEnemy`, explicit-id `Target(id)` reads, `stageBounds`, `stageTime`, and `runtimeTick`. Focused `EffectLifecycleSystem`, `EffectActorSystem`, `RuntimeCnsSubset`, and `EffectSpawnSystem` tests prove the boundary, including helper-local `Time` / `GameTime` trigger evaluation, stage-bound `FrontEdgeDist` param evaluation, runtime-tick helper-local sound telemetry, helper-local state/action/kinematic/control/metadata/resource/variable/sound branches, bounded helper identity triggers (`IsHelper`, `IsHelper(id)`), leading parent/root/opponent redirect branches/values, parent/root operands inside composite arithmetic/boolean/`IfElse(...)` expressions, direct and explicit-index opponent redirect branches/values plus explicit opponent-count reads when a caller supplies opponent states, explicit-id helper target memory, static helper-local `BindToParent` / `BindToRoot` owner binding, static helper-local `Explod` visual side-effect routing, static helper-local `Projectile` visual side-effect routing, static helper-local `RemoveExplod` cleanup by id, static helper-local `ModifyExplod` mutation by id of helper-parented owner-side Explods, helper-local `NumExplod(id)` counts for helper-parented owner-side Explods, helper-local `NumHelper(id)` counts for owner-side visual Helpers, and `DestroySelf` removal. Required `synthetic-imported-helper-target.json` checksum `68f95b67` proves explicit-id helper target memory and `NumTarget(8877)` / `Target(8877), Life`; required `synthetic-imported-helper-projectile.json` checksum `b6269136` proves a spawned Helper can route through `Projectile` and spawn owner-side Projectile anim `943` with helper parent metadata; previous `synthetic-imported-helper-numhelper.json` checksum `4e32e951` proves a spawned Helper can branch through `NumHelper(42) > 0`; previous `synthetic-imported-helper-numexplod.json` checksum `4328278a` proves a spawned Helper can branch through `NumExplod(8830) > 0` after spawning owner-side Explod anim `942`; previous `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` proves a spawned Helper can route through `1200 -> 1208 -> 1209`, spawn owner-side Explod anim `941`, and mutate it with helper `parentId` evidence; previous `synthetic-imported-helper-removeexplod.json` checksum `ff8658a2` keeps static helper-local RemoveExplod cleanup gated; previous `synthetic-imported-helper-explod.json` checksum `87ae363f` keeps static helper-local Explod spawn gated; previous `synthetic-imported-helper-bindtoroot.json` checksum `bf72306c` keeps root binding gated with root `ownerBind` payload evidence; previous `synthetic-imported-helper-bindtoparent.json` checksum `f9922c0e` keeps parent binding gated with parent `ownerBind` payload evidence, while `synthetic-imported-helper-enemynear.json` checksum `35498955` keeps opponent routing gated. This is ownership cleanup plus bounded helper-local trigger/bind/effect/target evidence only; broader indexed/team/helper-owned redirect ownership beyond caller-provided `EnemyNear(index)` lists, player-state `BindToParent` / `BindToRoot`, exact helper effect-count/ownership parity, dynamic bind/effect params, position rebinding, team/keyctrl ownership, nested helper ancestry where root differs from parent, helper-owned opponents/HitDefs beyond the explicit-id direct cut, helper `Target*` mutation/multi-target parity, helper-owned Projectile combat/contact beyond bounded target memory, helper-owned effect namespaces, helper-bound Explod timing/mutation beyond the static spawn/remove/modify/count-id route, helper combat, exact helper resource scopes, exact helper-local sound timing/channel/redirect ownership, exact helper-local clock parity, and full MUGEN/IKEMEN helper parity remain blocked.
- Match interaction now has a concrete runtime-system bridge: `RuntimeMatchInteractionWorld.advanceRuntime(...)` owns the normal-loop wiring for target-memory aging, active-effect advance, explicit one-opponent lifecycle roster source forwarding, projectile clash, body separation, active target binding, stage clamp, and presentation-effect advance after fighters run. Focused `MatchInteractionSystem` tests prove actor-local `targetWorld`, `effectLifecycleWorld`, lifecycle `[current opponent]` options, `effectActorWorld.resolveProjectileClashes(...)`, and `RuntimeActorConstraintWorld` wiring. This is ownership cleanup only; helper VM execution, new target/projectile/effect semantics, exact post-fighter tick-order parity, parent/root/team redirects, real teams/simul roster ownership, and full interaction parity remain blocked.
- Paused-match interaction now has a concrete runtime-system bridge: `RuntimePausedMatchWorld.advanceRuntime(...)` owns source-movetime target-memory aging, active-effect advance, presentation-effect advance, active target binding, stage clamp, explicit one-opponent lifecycle roster source forwarding, and frozen-actor paused presentation wiring. Focused `PauseSystem` tests prove actor-local `targetWorld`, `effectLifecycleWorld`, lifecycle `[current opponent]` options, and `RuntimeActorConstraintWorld` wiring. This is ownership cleanup only; new pause semantics, helper VM during pause, exact pause layering, exact paused effect tick order, parent/root/team redirects, real teams/simul roster ownership, and full pause parity remain blocked.
- Hitpause now has a concrete runtime-system bridge: `RuntimeHitPauseWorld.advanceRuntime(...)` owns current tick/input command buffering with `hitPause: true` plus paused-presentation wiring through `RuntimeEffectLifecycleWorld` using pause kind `hitpause` and explicit one-opponent lifecycle roster source forwarding. Focused `RuntimeHitPauseSystem` tests prove the runtime boundary and lifecycle `[current opponent]` options. This is ownership cleanup only; new hitpause semantics, helper-owned hitpause execution, broad side-effect ordering during hitpause, exact first-frame decrement order, real teams/simul roster ownership, and full hitpause parity remain blocked.
- Actor kinematics now has a named runtime boundary: `RuntimeKinematicsWorld` wraps bounded position integration, sandbox airborne gravity, ground snap, imported hit-state ground-snap preservation, and landing idle-action requests outside inline `PlayableMatchRuntime` branching. Focused `RuntimeKinematicsSystem` tests prove grounded movement, airborne gravity, landing snap/idle, active-move landing, and imported hit-state ground-snap preservation. This is ownership cleanup only; exact MUGEN physics, `yaccel` constants, landing timing, air recovery parity, helper physics ownership, and score movement remain blocked.
- Contact presentation now has a named runtime boundary: `RuntimeContactPresentationWorld` wraps bounded direct `HitDef` and `Projectile` contact package emission outside inline `PlayableMatchRuntime` branching. Focused tests prove direct hit and projectile guard packages share `contactId` / `contactTick` / `contactKind` metadata across attacker-side sound and spark telemetry while preserving hit-spark asset-frame handoff, including selected frame offset/duration trace evidence. This is ownership cleanup only; exact intra-tick audio/spark ordering, SND playback/mixing/channel priority, exact FightFX/common lookup/binding/layering/timing/scale/palette, helper-owned contact presentation, multi-target presentation, and score movement remain blocked.
- Guard-distance proximity now has a named runtime boundary: `RuntimeGuardDistanceWorld` wraps bounded `InGuardDist`/auto-guard proximity checks outside inline `PlayableMatchRuntime` branching. Focused `RuntimeGuardDistanceSystem` tests prove current move presence, spent-hit rejection, the pre-active guard-distance window, guardflag/AssertSpecial/unguardable filtering, and authored/default `guard.dist` thresholds. This is ownership cleanup only; exact proximity guard parity, guard-end timing, guard effects, air-guard landing, broad Common1 controller-loop parity, and score movement remain blocked.
- Actor animation now has a named runtime boundary: `RuntimeAnimationWorld` wraps bounded `animTime`, `frameIndex`, `frameElapsed`, completion, final-frame hold, `loopStart`, duration clamping, shared timing helpers, active action lookup/reset, and bounded `elem` / `elemtime` seeding for known AIR actions outside inline `PlayableMatchRuntime` branching. Focused `RuntimeAnimationSystem` tests prove empty actions, authored durations, frame changes, loop completion, final-frame hold, invalid-duration clamping, `AnimTime` / `AnimElemTime` helper math, missing-action no-mutation behavior, same-action element retargeting, and authored-action retargeting. This is ownership cleanup only; exact AIR negative-duration semantics, missing-action fallback parity, full `elem` / `elemtime` parity, helper/team redirect namespaces, controller tick order, and score movement remain blocked.
- EnvShake now has dedicated required trace evidence and a named runtime boundary: `synthetic-imported-envshake.json` checksum `061f17d5` gates static `EnvShake` lowering into typed `envshake` operation evidence plus partial runtime shake events in an imported owner state, including `time = 16`, `freq = 30`, `ampl = -7`, and `phase = 0.5` telemetry, while `RuntimeEnvShakeWorld` owns bounded event insertion and deterministic camera-shake projection. Dynamic params, `mul`, exact pause/stage/layer behavior, helper/redirect ownership, and MUGEN/IKEMEN waveform parity remain blocked.
- EnvColor now has dedicated required trace evidence and a named runtime boundary: `synthetic-imported-envcolor.json` checksum `956b0f4b` gates partial `EnvColor` stage-flash telemetry in an imported owner state, including `value = 16,96,255`, `time = 12`, and `under = 0`, while `RuntimeEnvColorWorld` owns bounded event history, stage-flash projection, and reset. Dynamic params, exact blend math, layer/window ordering, pause timing, and full presentation parity remain blocked.
- Sprite effects now have a named runtime boundary: `RuntimeSpriteEffectWorld` owns current match-runtime `SprPriority`, `PalFX`, `AfterImage`, `AfterImageTime`, and `Angle*` mutation/ticking before Three.js/debug/trace consumption. This preserves existing typed trace evidence while exact draw order, palette math, trail cadence, helper/redirect ownership, and full presentation parity remain blocked.
- Actor constraints now have a named runtime boundary: `RuntimeActorConstraintWorld` owns bounded `Width`, one-frame `PlayerPush`/`PosFreeze`/`ScreenBound` constraint reset/projection, stage clamping, and body-push separation before snapshot/debug/trace consumption. This preserves existing body/bounds trace evidence while exact player/edge collision, camera/screen-edge parity, team/helper push, and tick-order parity remain blocked.
- Direct combat now has a named runtime boundary: `RuntimeDirectCombatWorld` owns bounded same-tick direct `HitDef` priority win/trade mutation and direct hit/guard result mutation for life, pause, stun, velocity, hit vars, hit fall metadata, power gain, contact memory, received-damage memory, and get-hit cleanup before snapshot/debug/trace consumption. Collision selection, target routing, and Common1/custom-state transitions remain integrated by `PlayableMatchRuntime`; exact priority classes, throws, multi-hit/combo lifetime, helper/team/redirect combat ownership, and tick-order parity remain blocked.
- Helper direct combat now has a named runtime boundary: `RuntimeHelperCombatWorld` owns bounded helper-owned direct `HitDef` contact resolution, reject logging, default get-hit/guard state hooks, helper target memory, contact presentation, and helper state sync before snapshot/debug/trace consumption. `PlayableMatchRuntime` still supplies current hurtboxes, tick, state validation, effect stores, and broader ordering; exact helper hitpause/tick order, helper custom-state breadth, helper-owned projectile combat/contact presentation, teams, and full helper combat parity remain blocked.
- Default get-hit state selection now has a named runtime boundary: `RuntimeGetHitStateWorld` owns bounded stand/crouch/air `5000`/`5010`/`5020` fallback selection for imported direct and projectile hit routes before `PlayableMatchRuntime` validates and enters states. Exact Common1 get-hit animation selection, custom-state ownership breadth, helper/team/redirect get-hit routing, projectile-specific get-hit parity, and full get-hit VM parity remain blocked.
- Hit-state transition ownership now has a named runtime boundary: `RuntimeHitStateTransitionWorld` owns bounded direct-hit and ReversalDef `p1stateno` / `p2stateno` / `p2getp1state` routing before `PlayableMatchRuntime` validates and enters states. Attacker-owned custom states, target-owned `p2getp1state = 0`, and unavailable-state no-op behavior have focused system coverage; exact throws, helper/root/parent redirects, team ownership, bind/tick-order parity, and full custom-state VM semantics remain blocked.
- State availability now has a named runtime boundary: `RuntimeStateAvailabilityWorld` owns bounded compiled-state, parsed-state, animation-fallback, and owner-backed lookup before state-entry validation. Exact StateDef lookup edge cases, redirects beyond the current owner hook, helper/root/parent/team lookup, state-entry mutation, and full CNS VM state ownership remain blocked.
- ReversalDef now has a named runtime boundary: `RuntimeReversalWorld` owns bounded activation, active counter detection, and direct counter-result mutation before snapshot/debug/trace consumption. State-entry and target-state routing remain explicit hooks in `PlayableMatchRuntime`; exact priority, guard/projectile/helper/custom-state reversal behavior, attr grammar, trigger lifetime, hitpause/tick-order parity, and full MUGEN/IKEMEN behavior remain blocked.
- HitOverride now has a named runtime boundary: `RuntimeHitOverrideWorld` owns bounded slot countdown and redirect mutation for direct and projectile combat paths before snapshot/debug/trace consumption. State-entry validation remains an integration hook; exact slot priority, attr grammar, helper/custom-state redirects, projectile/custom-state edge timing, and IKEMEN parity remain blocked.
- Defender-local received-damage triggers now exist for the bounded two-actor direct-hit path: `ReceivedDamage` stores the latest direct `HitDef` damage for the defender's current state, `ReceivedHits` increments for that direct hit, and `synthetic-imported-receiveddamage.json` gates a `5000 -> 280` branch. Guard chip, projectile damage, target-controller damage, helpers, teams, exact lifetime, hitpause accounting, and exact parity remain blocked.
- Projectile-hit received-damage triggers now exist for the bounded two-actor projectile path: `synthetic-imported-projectile-receiveddamage.json` proves a Projectile hit can route the defender into `5000`, set `ReceivedDamage`/`ReceivedHits`, and branch into `280`. Projectile guard, target-controller damage, helpers, teams, exact lifetime, hitpause accounting, and exact parity remain blocked.
- Projectile contact presentation now has bounded package telemetry: `synthetic-imported-projectile-contact.json` checksum `ba22ed74` requires one Projectile hit contact to share attacker-side `hitsound = S5,0` and FightFX `sparkno = F7002` metadata through one contact id/tick/kind, including selected AIR-frame offset/duration evidence, and `synthetic-imported-projectile-guard.json` checksum `4bcc5650` requires the matching held-back guard package for `guardsound = S6,0` plus FightFX `guard.sparkno = F7004`. Exact projectile effect timing, SND playback/mixing, renderer lookup/binding/layering/scale/palette, helper-owned projectile effects, multi-target presentation, and full Projectile parity remain blocked.
- Helper visual motion now has a bounded `velset` cut: `synthetic-imported-helper-velocity.json` proves a typed/raw Helper can spawn with `velset = 3,-1` and expose moving actor-frame position/velocity evidence, checksum `dc0fb203`. Helper VM redirects, helper combat, helper-owned projectiles/HitDefs, exact helper physics, and full parity remain blocked.
- Helper visual scale now has a bounded cut: `synthetic-imported-helper-scale.json` proves a typed/raw Helper can spawn with `scale = 2,0.5`, expose helper effect payload scale, and expose actor-frame render-scale evidence, checksum `a161d896`. Helper collision-scale parity, ownpal/palette ownership, redirects, helper combat, and full parity remain blocked.
- Helper visual pause budgets now have a bounded `supermovetime` cut: `synthetic-imported-helper-supermovetime.json` proves a typed/raw Helper can spawn with `supermovetime = 4` and advance during `SuperPause` after source movetime expires, checksum `857f927e`. Exact pause layering/tick order, redirects, helper combat, and full parity remain blocked.
- Helper visual pause-budget coverage now includes the full current bounded trio: `synthetic-imported-helper-pausemovetime.json` proves regular `Pause` budget advance with checksum `bfe83f64`, and `synthetic-imported-helper-ignorehitpause.json` proves hitpause advance with checksum `7d008a66`. Exact hitpause/pause layering, redirects, helper combat, and full parity remain blocked.
- ModifyExplod now has bounded required trace cuts: `synthetic-imported-modifyexplod.json` checksum `bca75991` gates typed `modifyexplod` operation evidence and proves a live owner-side visual Explod can mutate static velocity, acceleration, scale, remove time, sprite priority, remove-on-get-hit, and pause-budget telemetry through `RuntimeEffectSpawnWorld` / `RuntimeEffectActorWorld`; `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` gates the helper-local static route for mutating only helper-parented owner-side Explods. Dynamic params, position rebinding, helper-owned effect namespaces, exact tick order, FightFX/common routing, remove-trigger semantics, and full Explod lifecycle parity remain blocked.
- Helper-local `NumExplod(id)` now has bounded required trace evidence: `synthetic-imported-helper-numexplod.json` checksum `4328278a` gates a helper-local branch after static helper-local Explod spawn, counting only helper-parented owner-side Explods. Exact helper effect-count parity, helper-owned effect namespaces, parent/root/team scopes, dynamic effect params, and full helper trigger parity remain blocked.
- Helper-local `NumHelper(id)` now has bounded required trace evidence: `synthetic-imported-helper-numhelper.json` checksum `4e32e951` gates a helper-local branch against owner-side visual Helper counts in the same effect store. Exact helper ownership scopes, broader indexed/team/helper-owned redirects beyond caller-provided `EnemyNear(index)` lists, helper combat, and full helper trigger parity remain blocked.
- Helper-local `Projectile` now has bounded required trace evidence: `synthetic-imported-helper-projectile.json` checksum `b6269136` gates a helper-local static Projectile spawn into the owner-side Projectile store with helper `parentId` metadata, `synthetic-imported-helper-projectile-target.json` checksum `49261b53` gates explicit-id target memory after helper-parented Projectile contact, `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1` gates bare `Target, Life` reads after helper-parented Projectile contact, `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8` gates explicit-id target side effects, `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3` gates explicit-id target memory into helper-owned TargetState / owner-backed custom-state return, `synthetic-imported-helper-projectile-default-target.json` checksum `b0daddf6` gates default target id `0` after helper-parented Projectile contact, `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae` gates default target id `0` target side effects, and `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1` gates default target id `0` into helper-owned TargetState / owner-backed custom-state return. Helper-owned Projectile combat/contact presentation, exact projectile namespaces, dynamic params, and full helper Projectile parity remain blocked.
- `HitPauseTime` is now a bounded runtime expression trigger with a required `ignorehitpause` controller-loop trace: `synthetic-imported-hitpausetime-ignorehitpause.json` checksum `a3a78bb8` proves the compiler/evaluator/runtime expression context can read the current actor hitpause counter and an imported active-state `ChangeState` marked `ignorehitpause = 1` can route through `HitPauseTime > 0` into state `220` during the current global hitpause branch, without advancing full physics/AI/animation. Persistent controller semantics, ordinary non-ignore controllers, broad side-effect ordering, pause layering, helper ownership, and full MUGEN/IKEMEN hitpause tick-order parity remain blocked.
- Reversal contact triggers now have a bounded first cut: `MoveReversed` marks the attacker state after a defender-side partial `ReversalDef` counters a matching direct `HitDef`, then can branch after hitpause through `MoveReversed >= 1`. `synthetic-imported-reversal.json` now gates defender state `777` plus attacker state `778`. Exact reversal priority, projectile/helper/custom-state reversal behavior, redirect ownership, trigger lifetime, hitpause/tick-order parity, and full MUGEN/IKEMEN behavior remain blocked.
- Previous-state metadata now includes bounded `PrevMoveType`, `PrevAnim`, and `PrevStateType`: imported routes can record parsed current statedef move/state type and current animation during `ChangeState`, then branch from the next state through `PrevMoveType = A`, `PrevAnim = 205`, or `PrevStateType = A`. `synthetic-imported-prevmovetype.json` gates `200(A) -> 269(I) -> 270`, `synthetic-imported-prevanim.json` gates `ChangeAnim 205 -> 275 -> 276`, and `synthetic-imported-prevstatetype.json` gates airborne state type metadata through `200(A) -> 271(S) -> 272`. Exact tick-order parity, redirects/custom-state ownership, helpers/teams, and full previous-state/animation parity remain blocked.
- Own-state lookup now has a bounded `SelfStateNoExist(n)` cut: `synthetic-imported-selfstatenoexist.json` gates a State -1 route where existing state `277` is accepted and missing state `9999` is rejected. Redirected state owners, helpers, parent/root lookup, and exact MUGEN/IKEMEN lookup parity remain blocked.
- Active-state animation-end routing now has a bounded `AnimTime` cut: `synthetic-imported-animtime.json` checksum `9e42b546` gates a state `200` route that waits for the authored AIR duration and exits through `AnimTime = 0` into state `291` without combat side effects. Exact AIR negative-duration semantics, looped-action semantics, state-owner/helper namespaces, persistent-controller timing, and full animation VM parity remain blocked.
- Active-state animation-element timing now has a bounded `AnimElemTime` cut: `synthetic-imported-animelemtime.json` checksum `2036557d` gates a state `200` route that advances through authored AIR frame durations `[2,4,4]` and exits through `AnimElemTime(2) = 2` into state `292` without combat side effects. Exact AIR loop semantics, invalid-element bottom values, negative-duration semantics, state-owner/helper namespaces, persistent-controller timing, and full animation VM parity remain blocked.
- Own-animation lookup has a bounded `SelfAnimExist(n)` cut: `synthetic-imported-selfanimexist.json` checksum `99930032` gates a State -1 route where existing AIR action `200` is accepted and missing action `9999` is rejected before state `290`. Redirected animation owners, helpers, parent/root lookup, common/FightFX namespaces, and exact MUGEN/IKEMEN lookup parity remain blocked.
- Identity lookup now has a bounded `Name` / `P1Name` / `P2Name` / `AuthorName` cut: `synthetic-imported-identity.json` checksum `c9be5cf1` gates a State -1 route where current owner display name, current opponent display name, owner author, and `EnemyNear, AuthorName` all match runtime metadata before entering state `276`. Team/simul/turns identity selection, helper-owned identities, player-indexed semantics, parent/root/target identity redirects, and exact MUGEN/IKEMEN string parity remain blocked.
- Team side lookup now has a bounded one-on-one `TeamSide` cut: `synthetic-imported-teamside.json` checksum `f55695b7` gates a State -1 route where `TeamSide = 1` and `EnemyNear, TeamSide = 2` route into state/action `299` without combat side effects. Current resolution maps `p1` / `p1-*` actors to side `1`, `p2` / `p2-*` actors to side `2`, and unknown ids to `0`. Teams/simul/turns, indexed opponent selection, helper-owned opponent lists, dynamic side ownership, and full MUGEN/IKEMEN `TeamSide` semantics remain blocked.
- Own-command lookup now has a bounded `SelfCommand` cut: `synthetic-imported-selfcommand.json` gates a State -1 route where the current owner command buffer routes `SelfCommand = "x"` into state `278`. Helper/team/redirect command ownership and exact IKEMEN/MUGEN lookup parity remain blocked.
- Global tick lookup now has a bounded `StageTime` cut: `synthetic-imported-stagetime.json` gates a State -1 route where `StageTime >= 3` routes into state `279`. Stage scripting, pause/replay/rollback accounting, round-system edges, and exact IKEMEN/MUGEN timing parity remain blocked.
- Owner context triggers now have a bounded `StateContext` cut: `synthetic-imported-state-context.json` checksum `cb9c3d1e` gates a State -1 route where `ctrl`, `StateType = S`, `MoveType = I`, and `Physics = S` route into state `295`. Helper/team/redirect metadata ownership, exact controller-loop ordering, persistent-controller parity, and full trigger parity remain blocked.
- Opponent spacing triggers now have a bounded `P2Distance` cut: `synthetic-imported-p2-distance.json` checksum `2c584be0` gates a State -1 route where `P2Dist X/Y` and `P2BodyDist X/Y` route into state `297`. Teams/simul, helpers, exact opponent selection, localcoord scaling, push/corner adjustment, persistent-controller parity, and full spacing parity remain blocked.
- Opponent context triggers retain the bounded `P2StateContext` cut: `synthetic-imported-p2-state-context.json` checksum `caf32557` gates a State -1 route where `P2StateType = S` and `P2MoveType = I` route into state `296`. Teams/simul, helpers, custom-state opponent ownership, exact opponent selection, persistent-controller parity, and full trigger parity remain blocked.
- Fall-defense hit-var lookup now has a bounded `GetHitVar(fall.defence_up)` cut: `synthetic-imported-gethitvar-fall-defence-up.json` gates an owner-backed get-hit route where stored `fall.defence_up = 150` branches into state `286`. Exact lifetime/stacking, redirects, helper/projectile/custom-state inheritance, and tick-order parity remain blocked.
- Fall-recovery hit-var lookup now has bounded `GetHitVar(fall.recover)` and down-recovery cuts: `synthetic-imported-gethitvar-fall-recover.json` gates an owner-backed get-hit route where the HitDef recovery flag is true while `fall.recovertime` is still positive and `CanRecover` is false; `synthetic-imported-gethitvar-down-recover.json` checksum `b8a7aef0` gates `GetHitVar(down.recover)`, `GetHitVar(down.recovertime)`, and alias `GetHitVar(recovertime)` into state/action `307`. Exact recovery threshold tables, liedown tables, 5110/5120 tick order, custom-state lifetime, helper/projectile inheritance, and tick-order parity remain blocked.
- Fall metadata lookup now has bounded `GetHitVar(fall.damage/fall.kill/fall.xvel/fall.yvel)` and `GetHitVar(fall.envshake.time/freq/ampl/phase)` cuts: `synthetic-imported-gethitvar-fall-metadata.json` checksum `474fa734` routes owner-backed get-hit CNS from state `5100` into state/action `305` before `HitFallDamage`, and `synthetic-imported-gethitvar-fall-envshake.json` checksum `6364632a` routes into state/action `306` before `FallEnvShake` presentation. Exact metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, camera waveform, pause/stage/layer interaction, and tick-order parity remain blocked.
- Guarded/timing hit-var lookup now has bounded helper-parented Projectile air guard, player-owned Projectile air guard, direct air guard, crouch guard, helper-parented Projectile, player Projectile, and direct standing guard `GetHitVar(hitshaketime)` evidence, normal `GetHitVar(hitshaketime)` and `GetHitVar(hittime)` evidence, bounded `GetHitVar(guarded)` cuts for direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile`, plus direct-guard timing metadata: `synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3c3f2e25` gates helper-parented Projectile air guard hitshake timing into state/action `317`, `synthetic-imported-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3fcf1421` gates player-owned Projectile air guard hitshake timing into state/action `316`, `synthetic-imported-gethitvar-air-guard-hitshaketime.json` checksum `703e9328` gates direct air guard hitshake timing into state/action `315`, `synthetic-imported-gethitvar-crouch-guard-hitshaketime.json` checksum `b31d1dac` gates direct crouch guard hitshake timing into state/action `314`, `synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime.json` checksum `64a1a8bd` gates helper-parented Projectile guard hitshake timing into state/action `313`, `synthetic-imported-projectile-gethitvar-guard-hitshaketime.json` checksum `724f66d6` gates player Projectile guard hitshake timing into state/action `312`, `synthetic-imported-gethitvar-guard-hitshaketime.json` checksum `31d76de9` gates direct standing guard hitshake timing into state/action `311`, `synthetic-imported-gethitvar-hitshaketime.json` checksum `655107b9` gates normal hitshake timing into state/action `310`, `synthetic-imported-gethitvar-hittime.json` checksum `a11beef0` gates normal hit timing into state/action `309`, `synthetic-imported-gethitvar-guarded.json` checksum `7c36defb` gates direct guard metadata into state/action `302`, `synthetic-imported-projectile-gethitvar-guarded.json` checksum `a0104472` gates player-owned Projectile guard metadata into state/action `303`, `synthetic-imported-helper-projectile-gethitvar-guarded.json` checksum `2b413bd7` gates helper-parented Projectile guard metadata into state/action `304` plus helper-local `Projectile` controller/op telemetry from helper state `1200`, and `synthetic-imported-gethitvar-guard-timing.json` checksum `cf92c669` gates guard `GetHitVar(hittime/slidetime/ctrltime)` into state/action `308`. Custom-state guarded metadata, exact hitstun/hitpause/guard timing/effects, exact air guard landing/timing, helper-owned custom states, and full Common1 hit/guard parity remain blocked.
- Get-hit classification lookup now has a bounded cut: `synthetic-imported-gethitvar-animtype.json` gates an owner-backed get-hit route where `fall.animtype = Up`, `ground.type = Low`, `air.type = Trip`, and `!GetHitVar(isbound)` branch into state `287`. Exact Common1 animation selection, binding lifetime, redirects, helper/projectile inheritance, and tick-order parity remain blocked.
- Hit eligibility now has HitBy accept, HitBy mismatch-reject, and NotHitBy deny required trace evidence: `synthetic-imported-hitby-allow.json` checksum `c75d5c7d` gates static `HitBy value = S,NA` through typed `eligibility:hitby`, accepted direct-hit contact, and final P2 life `963`; `synthetic-imported-hitby-reject.json` checksum `65185fd1` gates static `HitBy value = S,NT` rejecting `HitDef attr = S,NA` through typed `eligibility:hitby`, reject telemetry, no accepted hit, and final P2 life `1000`; `synthetic-imported-reject.json` checksum `5aca7dc0` gates `NotHitBy` rejection through typed `eligibility:nothitby`. Exact attr grammar, slot priority, helper/custom-state ownership, edge timing, and full MUGEN/IKEMEN hit-eligibility parity remain blocked.
- HitOverride redirect now has dedicated trace evidence: `synthetic-imported-hitoverride.json` checksum `dff6a468` gates a defender-side `HitOverride` slot redirecting a matching direct `HitDef` into state `777` with no normal damage. Exact slot priority, attr grammar, helper/projectile/custom-state redirects, and IKEMEN parity remain blocked.

## Construction Review Addendum

The architecture/product/modularity review for the expanded approach agrees on this build sequence:

```txt
MatchWorld ownership real
  -> KFM/Common1 tick-order and guard gates
  -> Studio Build/Evidence/Assets as the trust chain
  -> Character/Stage previews as diagnostics
  -> generated authoring provenance and QA ingestion
  -> IKEMEN scanner/profile reports
  -> shared Studio/Build/Evidence contracts
  -> snapshot/render/audio/debug interfaces
  -> module descriptor catalog and import-boundary gate
  -> tiny platformer proof slice
```

Rules added by the review:

- Every runtime/CNS/CMD task must close with `claim allowed` and `claim blocked` language, plus the artifact or fixture that can falsify it.
- Official fixtures may stay optional for local/CI availability, but skipped official fixtures block official compatibility claims.
- Negative fixtures are first-class: missing `common1.cns`, malformed DEF paths, unknown controllers/triggers, unsupported SFF/SND data, and IKEMEN-only ZSS/Lua/config must report `unsupported` or `unknown` without crashing or creating green status.
- Studio surfaces must be designed as a trust workflow, not equal tabs: `Project / Workbench -> Assets -> Evidence -> Build`, with `Character`, `Stage`, and `Debug` as contextual tools. `Build` owns runnable/partial/blocked/exportable truth.
- Generated assets must preserve prompt/source/atlas/QA/collision/trace provenance. Bad source walk, crouch, jump, or scale means regenerate source art, not crop or slice around the issue.
- Shared-core extraction starts with stable Studio/Build/Evidence contracts and later generic snapshot/render/audio/debug interfaces. Do not promote `src/game`, `MugenSnapshot`, or fighting renderer/audio types to shared core until import-boundary checks pass.
- The first platformer is a contract proof only: level/tile model, player, simple platform collision, camera, collectible, hazard, one enemy, snapshot, Studio playtest route, and QA smoke. It remains blocked until fighting smoke/trace still pass.

## Current Execution Authority

This table is the tie-breaker when older roadmap docs repeat similar queues. Build from top to bottom unless a newer round explicitly documents why it is taking a parallel evidence-only slice. A row is not "done" because code exists; it is done when the acceptance artifact, closeout, allowed claim, and blocked claim language all line up.

Progress control now also has a compact status board in `docs/PROGRESS_TRACKER.md` and a compact package ladder in `docs/ROADMAP_PACKAGE_MILESTONES.md`. Keep the tracker short, use the package ladder for slice selection, and keep detailed evidence plus historical claims here and in `docs/BUILD_EXECUTION_BACKLOG.md`.

| Order | Build | Code Anchors | Acceptance Artifact | Required Closeout | Claim Allowed | Still Blocked |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | Deepen `MatchWorld` lifecycle ownership for helpers/projectiles/explods/targets/effects. | `src/mugen/runtime/MatchWorld.ts`, `PlayableMatchRuntime`, `ProjectileSystem`, `HelperSystem`, `ExplodSystem`, `TargetSystem`, `RuntimeTrace`. | Trace artifacts and Debug Studio show actor/effect `ownerId`, `rootId`, `parentId`, lifecycle, and stable final snapshots. | Focused tests, `pnpm qa:trace`, docs if checksum drift is intentional. | `MatchWorld` owns a named lifecycle slice. | Generic ECS, broad SDK, full helper VM, or hidden raw-runtime side effects. |
| 2 | Tighten KFM/Common1 route evidence beyond the bounded threshold, synthetic air/ground velocity telemetry, optional official ordered-threshold oracle, and optional official air/ground recovery-input routes: exact controller-loop order, threshold oracle breadth, and broader official fixture gaps. | CMD/CNS compiler, expression/trigger evaluator, state controller executor, common get-hit/fall tests, trace gates. | Required synthetic plus optional official KFM artifacts name executed states/controllers/typed ops, checksums, support level, and remaining gaps. | Focused tests, `pnpm qa:trace`, artifact inspection. | `mugen-1.1 executed partial for this fixture route`. | `KFM works`, full MUGEN parity, or optional fixture skipped as pass. |
| 3 | Tighten exact guard-rule evidence. | `CombatResolver`, guard-state routing, `InGuardDist`, `CommandBuffer`, Common1 guard traces. | Trace proves guard start/end/proximity/air/support where implemented, including far-reject evidence for bounded `guard.dist`, plus damage, stun, push, state route, and unsupported guard effects. | Focused tests, `pnpm qa:trace`, support docs. | `guard route executed partial with named gaps`. | Exact guard parity without oracle/comparison evidence. |
| 4 | Promote Studio Evidence into replay-style trace review. | `RuntimeTraceArtifact`, Studio Evidence UI, evidence storage, QA bridge, smoke script. | Persisted/current trace diff shows frame, event, gate, actor/effect, and checksum deltas with affected asset/controller links. | `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm qa:smoke`, visual QA. | Evidence Browser compares trace artifacts and links next actions. | Rollback/netplay/replay VM claims. |
| 5 | Add source relink/regenerate repair affordances. | Studio Assets, Build Center, project manifest, source-package metadata, export bundle. | Missing/stale source packages show affected assets, impact, relink/regenerate action, build invalidation, and recompile state. | `pnpm qa:smoke`, visual QA, docs. | Studio can explain and start repair for project assets. | Advanced editing without save -> compile -> playtest -> export. |
| 6 | Build Character Studio Preview and retire Inspector only after parity of visible function. | DEF/AIR/SFF/CMD/CNS models, `CharacterRenderer`, collision overlays, Studio Character surface. | Selected character shows files, action timeline, frame/sprite/axis, Clsn1/Clsn2, commands, states, related evidence, and warnings. | Browser visual QA desktop/narrow, smoke coverage, parser tests if model behavior changes. | Character Preview is diagnostic and project-aware. | Full character editor or generated authoring UI with unproven edits. |
| 7 | Build Stage Studio Preview and stage/audio/presentation IR. | Stage parser/model/render, camera/floor/bounds, SND/audio diagnostics, Studio Stage surface. | Stage preview/report classifies layers/audio/controllers as rendered, animated, bounded, fallback, unsupported, missing, or decoded. | Focused tests where available, `pnpm qa:smoke`, visual QA. | Stage Preview diagnoses imported/native stage behavior. | Exact BGCtrl parity, motif, screenpack, model-stage parity. |
| 8 | Add generated asset authoring records and QA ingestion. | Studio Assets, generated fighter manifests, imagegen provenance records, `sprite-atlas-builder` reports. | Prompt/source -> atlas -> contact sheet/GIF -> motion/scale QA -> collisions -> runtime trace is visible in Studio. | Asset QA scripts, browser visual QA, trace when playable. | Generated/native asset pipeline proof. | Generated asset counted as imported MUGEN compatibility, or cropped bad motion marked fixed. |
| 9 | Add IKEMEN profile scanner/reporting. | Compatibility profiles, loader/report scanners, unsupported feature tracker. | ZSS, Lua, IKEMEN config, extended controllers/triggers/stage/system features are counted as recognized/unsupported/unknown. | Scanner tests, report fixture, docs. | `ikemen-go-scan recognized unsupported`. | ZSS/Lua execution, IKEMEN runtime behavior, rollback/netplay. |
| 10 | Extract shared module contracts, then build the tiny platformer slice. | Project, asset, input, tick, snapshot, render, audio, debug, build, QA contracts. | First contract draft exists in `src/engine/ModuleContracts.ts`; shared core forbids CNS/CMD/HitDef/round/helper/projectile/explod/target/MUGEN command routing/IKEMEN ZSS, and planned `platformer-module` is described without MUGEN controller semantics. A platformer scene later runs from project/build data. | Import-boundary check, fighting regressions, platformer smoke when implemented. | Modular engine extraction candidate, then one proven non-fighting slice. | Production multi-genre tooling before the fighting contracts hold; promoting `src/game` or `MugenSnapshot` to shared core. |

## Four-To-Six Week Execution Program

This is the practical construction slice for the approved horizon. It keeps the match playable every week while building toward the full MUGEN/IKEMEN-inspired Three.js engine, Creator Studio, generated asset pipeline, and later modular engine.

The three review lenses agree on the same priority:

- Runtime strictness: `MatchWorld` must own real lifecycle behavior, not only derive readable evidence from `PlayableMatchRuntime`.
- Product/UX: Creator Studio must behave as a trust workflow, not a flat tab list. `Build` is the authority and `Evidence` is the proof.
- Pragmatic delivery: every week must improve runtime evidence, Studio evidence, Build truth, or playable match behavior.

| Week | Build | Acceptance Gate | Claim Allowed | Claim Blocked |
| --- | --- | --- | --- | --- |
| 1 | Deepen one real `MatchWorld` ownership slice: projectile lifecycle, effect pause ordering, or target semantics. | Trace checksums stay stable or intentional drift is documented; Debug Studio and trace artifacts show owner/root/parent facts for the affected slice. | `MatchWorld` owns this named lifecycle slice. | Generic ECS, broad SDK, full helper VM, or hidden raw side effects. |
| 2 | Tighten KFM/Common1 recovery precision on existing get-hit/fall chains. | Required synthetic and optional official KFM artifacts prove executed states/controllers/typed operations and name remaining gaps. | `mugen-1.1 executed partial for this fixture route`. | `KFM works`, full MUGEN parity, or skipped optional fixture counted as pass. |
| 3 | Tighten guard-rule evidence for start/end/proximity and stand/crouch guard-hit. | Trace proves guard reason, damage/stun/push, state/anim route where supported, plus far-reject bounded `guard.dist` evidence and unsupported guard effects. | `guard route executed partial with named gaps`. | Exact guard parity, air guard, sparks/sounds, or IKEMEN guard parity without oracle evidence. |
| 4 | Promote Studio Evidence into the main truth board. | Persisted/current trace diff shows frame, event, gate, actor/effect, checksum, affected asset/controller, stale/missing-source state, and next action. | Evidence Browser can compare trace artifacts and point to the next action. | Rollback, netplay, replay VM, or green evidence without links. |
| 5 | Add source relink/regenerate and build invalidation affordances. | Missing/stale source packages show affected assets, impact, relink/regenerate action, invalidated build state, and recompile path. | Studio can explain and start repair for project assets. | Advanced editing without save -> compile -> playtest -> evidence -> export. |
| 6 | Build Character Studio Preview as a diagnostic surface. | Selected character shows DEF files, AIR timeline, sprite source, axis, Clsn1/Clsn2, CMD routes, CNS states, related evidence, and warnings with desktop/narrow visual QA. | Character Preview is project-aware and diagnostic. | Full character editor, generated authoring editor, or Inspector retirement before parity of visible function. |

If a week cannot close one of these gates, reduce the slice instead of expanding scope. A week that only adds UI or docs without improving a falsifiable runtime/studio/build/evidence fact does not advance the approved horizon.

Stage Studio Preview follows the Character Preview unless the runtime/stage workstream becomes the higher risk first. Generated authoring begins after Studio Evidence/Assets/Build can show prompt/source -> atlas -> QA -> collision/action -> runtime trace without hiding warnings. IKEMEN scanner work can run in parallel as report-only, but ZSS/Lua/rollback/netplay remain blocked. The shared module contract draft may move forward as documentation and manifest metadata; the first platformer slice stays blocked until fighting smoke/trace and import-boundary checks pass.

## Status Ledger

| Workstream | Status | Current Proof | Next Gate |
| --- | --- | --- | --- |
| ControllerOp / IR | Active, partial | `HitDef`, `Target*`, `Pause`/`SuperPause`, `Projectile`, `Helper`, `Explod`, `HitFall*`, `FallEnvShake`, static `EnvShake`, static `AssertSpecial`, static numeric `VelSet`/`VelAdd`/`VelMul`/`HitVelSet`/`PosSet`/`PosAdd`/bounded `Gravity`, static `PosFreeze`/`ScreenBound`, static `Width`, static `PlayerPush`, static `StateTypeSet`, static `Turn`, static numeric `CtrlSet`/`LifeAdd`/`LifeSet`/`PowerAdd`/`PowerSet`/`VarSet`/`VarAdd`/`VarRandom`/`VarRangeSet`, typed `VarSet`/`VarAdd sysvar(n)` assignments, static `HitBy`/`NotHitBy`/`HitOverride` setup, static `ReversalDef` setup, static `AttackMulSet`/`DefenceMulSet` damage-scale setup, static `HitDef hitsound`/`guardsound` refs, and static `SprPriority`/`PalFX`/`RemapPal`/`AfterImage`/`AfterImageTime` sprite-effect setup have typed operation evidence; `RuntimeControllerDispatchWorld` owns the current runtime-controller execution bridge after dispatch, `RuntimeExpressionContextWorld` owns the shared active runtime expression/trigger read context used by imported trigger evaluation and dynamic param fallback, `RuntimeAnimationControllerWorld` owns passive `ChangeAnim`/`ChangeAnim2` setup in the basic executor path, `RuntimeKinematicControllerWorld` owns passive `VelSet`/`VelAdd`/`VelMul`/`HitVelSet`/`PosSet`/`PosAdd`/`Gravity` setup, `RuntimeBoundsControllerWorld` owns passive `PlayerPush`/`PosFreeze`/`ScreenBound` setup, `RuntimeHitFallControllerWorld` owns passive `HitFallVel`/`HitFallDamage`/`HitFallSet` mutations, `RuntimeStateTypeWorld` owns passive `StateTypeSet` metadata setup, `RuntimeDamageScaleWorld` owns passive `AttackMulSet`/`DefenceMulSet` multiplier setup, `RuntimeHitDefenseWorld` owns passive `HitBy`/`NotHitBy`/`HitOverride` slot setup/removal, `RuntimeHitDefControllerDispatchWorld` owns active-state HitDef activation into the current attack payload, `RuntimeReversalControllerDispatchWorld` owns active-state ReversalDef dispatch into `RuntimeReversalWorld`, and `RuntimeResourceWorld` inside `RuntimeResourceSystem` owns resource/variable mutation semantics; dynamic movement/bounds/collision/metadata/resource/variable/eligibility/reversal/damage-scale/sprite-effect/audio/presentation params still fall back to runtime expression evaluation, and many controllers still execute from raw params. | Next controller family or system boundary must shrink raw controller paths without overstating parity. |
| Latest private KFM fixture addendum | Active, optional | Current `pnpm qa:trace` passes 280/280 artifacts, 255 required and 25 optional after the required HitFall recover-true gate. `kfm-official-default-crouch-guard-slide-stop.json` checksum `d11153d0` still confirms real KFM/Common1 crouch guard-hit state `153` executes ordered `HitVelSet -> VelSet -> CtrlSet -> ChangeState` slide-stop/control evidence with typed `kinematic:*` and `resource:ctrlset` operations. `kfm-official-default-guard-slide-stop.json` checksum `885bb1da` remains the stand mirror; optional `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae` remains the crouch progression evidence, while optional `kfm-official-default-air-fall-recovery-input.json` checksum `3bce8aba` and `kfm-official-default-air-fall-recovery-too-early.json` checksum `b199382a` remain the air-entry recovery evidence. | Private fixture confidence only: no public KFM, no score movement, no exact tick timing, thresholds, velocity math, public `130` guard-hold return, VM tick-order, visual/audio, or full Common1 parity claim. |
| Runtime trace evidence | Active, usable first cut | `pnpm qa:trace` now passes 280/280 artifacts, 255 required and 25 optional. Latest required Common1 evidence is `synthetic-imported-hitfall-recover-true` checksum `f1e3424a`, proving bounded recover-enabled-but-not-yet-recoverable fall routing `5000 -> 5030 -> 5050 -> 5240 -> 0` after a fall `HitDef` with `fall.recover = 1`, with named probe `ChangeState`, actor-frame order, positive `fall.recovertime` metadata, and forbidden recovery states. Previous recover-disabled evidence is `synthetic-imported-hitfall-recover-false` checksum `236df0a8`; previous required normal trigger evidence is `synthetic-imported-hitfall-false` checksum `1d538e43`; previous required fall trigger evidence is `synthetic-imported-hitfall-canrecover` checksum `7cf7ab46`; previous required air guard landing evidence is `synthetic-imported-air-guard-landing` checksum `d6986d7f`; latest optional private KFM mirror is `kfm-official-default-crouch-guard-slide-stop` checksum `d11153d0`; previous required crouch guard evidence is `synthetic-imported-crouch-guard-slide-stop` checksum `2bea7311`. Previous required `synthetic-imported-envshake` checksum `061f17d5`, stand guard `synthetic-imported-default-guard-slide-stop` checksum `a9663641`, optional KFM stand `kfm-official-default-guard-slide-stop` checksum `885bb1da`, get-hit metadata, Projectile, Helper, target-memory, Common1 recovery, FightFX/audio, AssertSpecial, ReversalDef, HitOverride, and resource/variable traces remain active. | Keep adding small falsifiable runtime oracles; full replay scrubber, richer reason payloads, exact normal get-hit timing, fall arbitration, exact guard timing/proximity/effects, public KFM `130` guard-hold return, exact air physics/landing timing/state-52 internals, exact fall/recovery thresholds, exact crouch get-hit animation/slide/fall/progression timing, exact tick order, exact helper/projectile lifetime, teams/simul, and full VM parity remain future work. |
| MatchWorld ownership | Active, early | `MatchWorld` facade plus actor registry/lifecycle metadata, per-tick lifecycle events, `targetLinks` from `RuntimeTargetWorld` target-memory/binding snapshots, a `RuntimeEffectActorWorld` boundary for `Explod`/`Helper`/`Projectile` stores, a `RuntimeTargetWorld` boundary for target remember/advance/snapshot/runtime-state snapshot cloning/link/count/find/controller application, bounded `BindToTarget` lookup/postype binding/position application, active `TargetBind`/`BindToTarget` position application, and `RuntimeTargetControllerDispatchWorld` active-state Target / BindToTarget dispatch/telemetry, a `RuntimeHelperTelemetryWorld` boundary for helper-local Projectile controller/op telemetry binding, a `RuntimeMatchHelperTargetStateWorld` boundary for helper-owned `TargetState` match-roster actor resolution, a `RuntimeHitEligibilityWorld` boundary for bounded `HitBy`/`NotHitBy` slot ticking plus per-frame `AssertSpecial`/render-opacity reset, a `RuntimeAssertSpecialWorld` boundary for bounded imported pre-facing `AssertSpecial` lookup/filter/trigger/application before auto-facing, a `RuntimeGuardWorld` boundary for bounded guard-hit state selection and auto guard-start eligibility/mutation, a `RuntimeGetHitStateWorld` boundary for bounded default stand/crouch/air get-hit state selection, a `RuntimeHitStateTransitionWorld` boundary for bounded direct-hit/ReversalDef `p1stateno` / `p2stateno` / `p2getp1state` routing, a `RuntimeStateAvailabilityWorld` boundary for bounded state/action availability lookup, a `MatchWorldLifecycleSystem` boundary for actor/effect lifecycle records/events, a `RuntimeContactMemoryWorld` boundary for bounded direct/projectile contact-memory create/reset/mutation/reads consumed by `PlayableMatchRuntime`, `RuntimeDirectCombatWorld`, and `RuntimeReversalWorld`, a `RuntimeContactControllerDispatchWorld` boundary for active-state HitAdd / MoveHitReset dispatch into contact memory, a `RuntimeRandomSystem` boundary for deterministic sandbox-side seed/advance/clamp/fallback random units consumed by `PlayableMatchRuntime` and `StateControllerExecutor`, a `RuntimeStunSystem` boundary for bounded hitstun/guardstun input-lock and timer/friction updates, a `RuntimeRecoverySystem` boundary for bounded fall-recovery countdown, Common1 liedown recovery, and imported ground-recovery landing hooks, a `RuntimePauseWorld` boundary for current match-pause state/snapshot/tick/reset/controller application, a `RuntimeRoundSystem` boundary for bounded timer/KO/time-over/winner/message/reset state, a `RuntimeEnvShakeWorld` boundary for bounded EnvShake/FallEnvShake event insertion plus camera-shake projection, a `RuntimeAudioWorld` boundary for bounded PlaySnd/StopSnd event insertion, a `RuntimeAudioControllerDispatchWorld` boundary for active-state sound controller dispatch/telemetry into that world, a `RuntimeHitEffectWorld` boundary for bounded direct HitDef spark telemetry, a `HitSparkAssetSystem` boundary for bounded player/common/FightFX spark frame lookup, a `RuntimeEnvColorWorld` boundary for bounded EnvColor event history/stage-flash projection, a `RuntimeSpriteEffectWorld` boundary for bounded SprPriority/PalFX/AfterImage/AfterImageTime/Angle mutation/ticking, a `RuntimeSpriteEffectControllerWorld` boundary for active-state sprite-effect dispatch/telemetry into that world, a `RuntimeActorConstraintWorld` boundary for bounded Width/PlayerPush/PosFreeze/ScreenBound/stage-clamp/body-push behavior, a `RuntimeDirectCombatWorld` boundary for bounded direct priority and hit/guard result mutation, a `RuntimeHitOverrideWorld` boundary for bounded HitOverride ticking/redirect mutation, a `RuntimeReversalWorld` boundary for bounded ReversalDef activation/counter mutation, a `RuntimeReversalControllerDispatchWorld` boundary for active-state ReversalDef dispatch/telemetry into that world, a `RuntimeProjectileCombatWorld` boundary for bounded projectile contact/reject/HitOverride/hit-or-guard/cleanup plus projectile clash trade/cancel/decrement, a `RuntimeMatchCombatBridgeWorld` boundary for constructing the priority/direct/projectile/helper combat callbacks passed into `RuntimeMatchInteractionWorld`, a `RuntimeMatchStepWorld` boundary for stopped/forced/speed-sampled public match-step cadence, a `RuntimeMatchTickBranchWorld` boundary for per-tick hitpause/pause/active branch arbitration, and a `RuntimeSnapshotWorld.match()` boundary for full match snapshot envelope assembly; `RuntimeTraceGate.requiredRoundFrames` now gates bounded KO and time-over `RoundSnapshot` evidence through required `synthetic-imported-round-ko.json` checksum `bfd5f073` and `synthetic-imported-round-timeover.json` checksum `7d9f7907`; Target* side-effect application, bounded `BindToTarget` binding mutation, active target binding position application, and bounded `NumTarget(id)` reads use `TargetSystem`; `ProjectileSystem`/`HelperSystem`/`ExplodSystem` consume typed operations; `EffectActorSystem` owns mutable per-fighter stores and bounded `NumExplod(id)` / `NumHelper(id)` / `NumProjID(id)` count reads behind active/presentation world passes; `RuntimeProjectileCombatWorld` owns the bounded projectile contact/reject/override/hit-or-guard/removal loop, bounded `projhits`/`projmisstime` multi-hit cooldown, bounded equal-priority trade and higher-priority cancel/decrement, plus bounded projectile terminal playback through `RuntimeEffectActorWorld`. | Move exact random-stream parity, frame pacing/replay/rollback timing, exact pause layering/arbitration, snapshot schema evolution, compatibility telemetry semantics, projectile priority classes, exact terminal timing/rem-trigger parity, multi-target/after-hit semantics, effect pause/combat ordering, exact EnvShake pause/stage/layer/waveform behavior, exact audio timing/mixing/channel semantics, exact FightFX/common spark lookup/binding/layering/timing/scale semantics, exact EnvColor blend/layer/window/pause semantics, exact sprite draw-order/material/trail/palette semantics, exact actor collision/camera/screen-edge semantics, exact direct-combat priority/throw/multi-hit semantics, exact default get-hit animation/route semantics, exact StateDef lookup/entry semantics, exact custom-state ownership/VM semantics, exact helper TargetState actor registry/timing/custom-state semantics beyond bounded match roster lookup, exact helper Projectile telemetry/timing semantics beyond bounded Projectile controller/op forwarding, exact HitOverride slot/attr/redirect semantics, exact ReversalDef priority/guard/projectile/helper/custom-state semantics, exact contact/combo lifetime, exact guard proximity/end/effects/landing/tick order, exact hitstun/recovery tick order, exact Common1 recovery thresholds, exact round/lifebar/team flow, exact hit-eligibility and AssertSpecial lifetime/priority/persistence, and exact target/pause semantics behind `MatchWorld` with no behavior checksum drift. |
| KFM/Common1 compatibility | Active, partial | Optional KFM `x`, `QCF_x`, guard, auto guard-start/end, hitstun, default defender-owned Common1 `5000`, defender-owned stand `5000 -> 5001 -> 0` with required synthetic controller-event and actor-frame order, defender-owned auto guard-start/end `120 -> 130 -> 140 -> 0` with optional KFM controller/operation order, defender-owned stand/crouch/air guard-hit `150 -> 151` / `152 -> 153` / `154 -> 155 -> 52 -> grounded control`, required stand guard slide-stop/control `150 -> 151 -> 130` with `HitVelSet -> VelSet -> CtrlSet -> ChangeState` typed-operation order and actor-frame velocity-before-stop evidence, optional KFM guard-hit controller/operation order plus actor-frame state/physics/body telemetry for stand/crouch/air routes, defender-owned fall/ground-impact/bounce/lie-down entry `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110`, required bounded synthetic full-chain fall recovery `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0` with controller/actor-frame order plus `hitFall.downRecoverTime` countdown-range and first-to-last-drop evidence in `5110`, defender-owned air recovery-input `5050 -> 5210 -> 52 -> 0`, defender-owned ground recovery-input `5050 -> 5200 -> 5201 -> 52 -> 0`, optional ordered KFM threshold evidence from positive `5050` recover time into the ground branch, and state-exit gates; synthetic custom get-hit controller-flow, bounded stand/crouch/air guard-hit `150 -> 151` / `152 -> 153` / `154 -> 155` with runtime-backed `GetHitVar(slidetime)` / `GetHitVar(ctrltime)` and `sysvar(0)` landing flags when guard params/Common1 states exist, bounded named controller/typed-operation order for stand/crouch/atomic-`DB`/air guard-hit routes, bounded InGuardDist near/far evidence, bounded synthetic auto guard `120 -> 130 -> 140 -> 0` with controller-event and actor-frame order evidence, bounded synthetic recovery-input route `5050 -> 5210 -> 0`, bounded synthetic recovery-threshold handoff evidence with first-to-last `hitFall.recoverTime` drop in `5050`, bounded actor-frame recovery tick-order evidence with the same drop before `5210`, bounded named controller/typed-operation recovery order evidence, bounded synthetic air-recovery velocity evidence in `5210`, and bounded synthetic ground-recovery selection/velocity/controller-order evidence through `5050 -> 5200 -> 5201 -> 52 -> 0`. | Exact controller/VM tick-order parity beyond the bounded named event/operation sequences, exact threshold/down-recovery tables/velocity/bounce math beyond bounded routes, broader recovery parity, and exact guard proximity/effect/timing work. |
| Stage/audio/presentation IR | Active, early | Stage DEF/SFF parsing, static/action BG rendering, bounded recognized-type BGCtrl execution, partial SND/Web Audio event path, per-layer Stage BG IR in `StageCompatibilityReport`, bounded/unsupported BGCtrl reporting, HitSpark player AIR/system-frame/package-frame handoff tests, first-pass `fight.def`/FightFX AIR/SFF/SND loading plus decoded system-SFF provider registration, bounded character `[Files] fx` FightFX prefix package loading/selection, prefixed `soundPrefix` audio archive lookup, and a URL-addressable Studio `Stage` surface for floor/bounds/zoffset/starts/camera/layer/controller diagnostics. | Richer camera/layer visual QA, exact BGCtrl timing/parity, exact parallax/window/mask/trans support tiers, exact FightFX/common layering/timing/scale/palette/audio mixing, `sys.ffx` lifetime/refcount/cache semantics, channel fallback, and motif/screenpack ownership. |
| Creator Studio | Active, evidence-first | Workbench, Assets, Inspector, Debug, Evidence, Modules, Build surfaces read current runtime/project data. Asset/gate records now have first actionable status fields: severity, impact, evidence ids, blockers, exportability, and next action; selected asset panels, Build readiness, Evidence summary, `project.json`, `.studioAssets`, `.studioEvidence`, and `pnpm qa:smoke` expose/check them. Trace artifacts exported from Studio are persisted into bounded browser-local evidence history with project entry/source-package metadata, stale/current status, current trace comparison, a first visible metric/gate diff review, and a frame scrubber with per-frame actor/effect/input/event deltas, effect-payload deltas, and world deltas. Current UI shell polish adds live workspace status, clearer mode selection, viewport skip link, resilient badges, collapsible heavy debug panels, a Studio focus hero/rail for current decision, gates, assets, traces, and build state, Debug-to-Inspector exact CNS state/controller deep-links, Debug-to-Inspector command-buffer/CMD Browser deep-links, URL-backed Runtime Debug lenses for targets/effects/pause/audio, explicit Pause / HitPause snapshot rows, typed helper/projectile/explod effect-payload drilldowns, first trace-world evidence rows that jump back to the Evidence scrubber, and manual source-package relink from Build Center so Runtime/Studio remain usable on desktop and narrow screens. | Full replay-style trace scrubber, persistent source handles, regenerate actions, Character/Stage previews tied to selected assets and evidence, multi-artifact frame diff review, and true editing workflows beyond shell/readability polish. |
| Generated authoring | Planned, external first cut | Local generated roster and atlas QA exist; provenance shown as generated/native, not MUGEN compatibility. | Studio asset records for prompts, atlas QA, collision/action authoring, replacement flow. |
| IKEMEN profile | Active, report-only | `IkemenFeatureScanner` adds `CompatibilityReport.profiles.ikemen`, exported JSON, DebugPanel, Studio Evidence, and unsupported-feature entries for ZSS files/references/fallback `.cns.zss`, ZSS language blocks/controller syntax, Lua files/hooks including `hook.*`, IKEMEN config JSON, screenpack/select/menu/movelist signals, `IkemenVersion`, selected IKEMEN-only controllers/params/triggers/`AssertSpecial` flags, IKEMEN text-system signals such as `Text`, `ModifyText`, `RemoveText`, and `NumText`, model-stage assets, and named 3D/Z stage params. | Broader fixture corpus for IKEMEN-only package/system/stage files, more source-mapped trigger/controller classification, and no execution until runtime gates exist. |
| Shared module contracts | Active, guarded first draft | `src/engine/ModuleContracts.ts` defines shared contract ids, active/planned module records, and forbidden legacy concepts for shared core/platformer; `ProjectCompiler` exports the same contract report through `runtime-manifest/v0`; Studio Modules renders consume/provide/blocked claims and smoke QA checks the visible surface plus exported package manifest; `ArchitectureBoundaries.test.ts` prevents shared core from importing MUGEN/App/Game/Three/package-loader code and prevents MUGEN parser/runtime code from importing App/Game/Three renderer code; `pnpm check:boundaries` adds a command-level guard for future `src/core/**`, future platformer module paths, and `src/engine/**` fighting-term leakage outside `ModuleContracts.ts`; Studio Build/Evidence expose the same boundary as `architecture-boundaries` / `test:architecture-boundaries` acceptance evidence. | Keep the boundary test and `pnpm check:boundaries` green while extracting small shared services, then implement only a tiny platformer smoke after fighting gates stay green. |
| Modular engine | Blocked by fighting contracts | Module Studio is read-only/planned; `projectType` is currently `mugen-port`. | Shared contracts extracted without CNS/CMD/HitDef/Common1/round/helper/projectile/explod/target/MUGEN/IKEMEN leakage and verified by an import-boundary gate. |
| Platformer slice | Blocked | Documented horizon only. | Tiny platformer runs from `runtime-manifest/v0` after shared contracts hold. |

Latest R2 ownership addendum: `RuntimeMatchRoundWorld` now owns bounded active-match round timer delegation plus finish side effects that used to live inline in `PlayableMatchRuntime`: timer tick, KO/time-over stop, and round-finish log emission route through one boundary. This does not prove exact round-flow timing, intros/winposes, KO slowdown, continue flow, teams/simul/turns, lifebar/screenpack behavior, visual/audio parity, score movement, or full MUGEN/IKEMEN round VM parity.
Previous R2 ownership addendum: `RuntimeMatchFighterAdvanceWorld` owns bounded active 1v1 fighter-advance orchestration that used to live inline in `PlayableMatchRuntime`: P1 advance, P2 auto-guard start, pause-gated P2 advance, and P1 auto-guard start route through one boundary. This does not prove exact player tick order, pause-start arbitration, teams/simul roster advance, helper/team/redirect actor advance semantics, guard-start parity, visual/audio parity, score movement, or full MUGEN/IKEMEN match VM parity.
Previous R2 ownership addendum: `RuntimeMatchPauseControllerWorld` owns bounded Pause/SuperPause controller result side effects that used to live inline in `PlayableMatchRuntime.applyMatchPauseController`: pause application routes through `RuntimePauseWorld`, SuperPause power delta routes through an injected resource hook, and the existing match log line emits through one named world. This does not prove exact pause layering, SuperPause background/effects/sound timing, helper/team/redirect pause ownership, pause/hitpause command parity, visual/audio parity, score movement, or full MUGEN/IKEMEN pause VM parity.
Previous R2 ownership addendum: `RuntimeMatchCombatBridgeWorld` owns bounded combat resolver construction that used to live inline in `PlayableMatchRuntime`: priority clash, direct combat, projectile combat, and helper combat callbacks route through one bridge before `RuntimeMatchInteractionWorld.advanceRuntime`. This does not prove exact direct-combat priority, helper-owned contact timing, projectile hit/cancel timing, teams/simul/multi-target combat breadth, visual/audio parity, score movement, or full MUGEN/IKEMEN combat VM parity.
Previous R2 ownership addendum: `RuntimeMatchTickInputWorld` now owns bounded normal-match input/tick stamping that used to live inline in `PlayableMatchRuntime`: actor `compatibilityTick`, cloned `currentInput`, and normal non-hitpause command-buffer writes. Pause and hitpause command buffering remain owned by `RuntimePausedMatchWorld` and `RuntimeHitPauseWorld`; this does not prove exact command timing, input conflict priority, helper/team/redirect command ownership, pause/hitpause command parity, visual parity, score movement, or full MUGEN/IKEMEN input VM parity.
Previous R2 ownership addendum: `RuntimeStateClockWorld` owns bounded `Time` / state elapsed clock mutation that used to live inline in `PlayableMatchRuntime`: active-frame advance and changed-state elapsed reset. This complements `RuntimeStateMetadataWorld`, `RuntimeStateAvailabilityWorld`, `RuntimeHitStateTransitionWorld`, and `RuntimeAnimationWorld`, and does not prove exact CNS `Time` tick-order parity, persistent-controller timing parity, pause/hitpause timing changes, helper/team/redirect state clocks, or full MUGEN/IKEMEN timing parity.
Previous R2 ownership addendum: `RuntimeStateMetadataWorld` now owns bounded previous-state transition metadata writes that used to live inline in `PlayableMatchRuntime`: `prevStateNo`, `prevAnimNo`, `prevStateType`, and `prevMoveType`. The basic `StateControllerExecutor` `ChangeState` / `SelfState` output path uses the same helper, so current `PrevStateNo`, `PrevAnim`, `PrevStateType`, and `PrevMoveType` support has one named runtime-system boundary. This complements `RuntimeStateAvailabilityWorld`, `RuntimeHitStateTransitionWorld`, and `RuntimeAnimationWorld`, and does not prove exact state-entry tick order, redirects/helper/team previous-state ownership, persistent controller semantics, full `ChangeState` / `SelfState` parity, or full MUGEN/IKEMEN previous-state parity.
Previous R2 ownership addendum: `RuntimeContactPresentationWorld` now owns bounded direct `HitDef` and `Projectile` contact presentation package emission that used to live inline in `PlayableMatchRuntime`: shared contact metadata creation, attacker-side `PlaySnd` telemetry, attacker-side HitSpark telemetry, and hit-spark asset-frame handoff for current direct/projectile contact routes. This complements `RuntimeAudioWorld`, `RuntimeHitEffectWorld`, `HitSparkAssetSystem`, and `RuntimeProjectileCombatWorld`, and does not prove exact audio/spark ordering, SND playback/mixing/channel priority, exact FightFX/common lookup/binding/layering/timing/scale/palette, helper-owned contact presentation, multi-target presentation, or full MUGEN/IKEMEN presentation parity.
Previous R2 ownership addendum: `RuntimeGuardDistanceWorld` owns the bounded `InGuardDist`/auto-guard proximity checks that used to live inline in `PlayableMatchRuntime`: current move presence, spent-hit rejection, pre-active guard-distance window, guardflag/AssertSpecial/unguardable filtering, caller-supplied hurtboxes, and authored/default `guard.dist` thresholds. This complements `RuntimeGuardWorld`, `RuntimeAnimationWorld`, `RuntimeKinematicsWorld`, `RuntimeInputControlWorld`, and `RuntimeMoveLifecycleWorld`, and does not prove exact proximity guard parity, guard-end timing, guard effects, air-guard landing, broad Common1 controller-loop parity, or full MUGEN/IKEMEN guard VM parity.

## Next Five Required Cuts

These are the current mandatory cuts before the plan expands into larger Studio editing or non-fighting module work:

| Order | Cut | Why It Comes Next | Gate |
| --- | --- | --- | --- |
| 1 | `MatchWorld` lifecycle ownership | `MatchWorld` must become more than a derived facade before helpers/projectiles/explods/targets can be trusted by Studio or future modules. | Existing trace checksums stay stable, or intentional drift is documented with a new artifact. |
| 2 | KFM/Common1 recovery precision and exact tick order | The current gates now prove stand get-hit, fall/ground-impact, a synthetic full recovery chain `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0` with bounded controller/actor-frame order plus `hitFall.downRecoverTime` countdown-range and first-to-last-drop evidence observed in `5110`, a synthetic recovery-input branch through `5050 -> 5210 -> 0`, a synthetic recovery-threshold actor-frame handoff with first-to-last `hitFall.recoverTime` drop in `5050`, a summarized actor-frame tick-order gate from dropping `5050` countdown evidence before `5210`, a bounded named controller/typed-operation order route from `5050` `VelAdd` `Gravity` before `ChangeState` `Recovery Input` into `5210` `VelSet` / `kinematic:velset` / `HitFallSet` / `hitfall:hitfallset` / `ChangeState`, synthetic air-recovery velocity in `5210`, a synthetic ground-recovery selection/velocity/controller-order route through `5050 -> 5200 -> 5201 -> 52 -> 0`, optional official KFM lie-down/get-up completion through `5110 -> 5120 -> 0`, optional official KFM air recovery-input completion through `5050 -> 5210 -> 52 -> 0`, optional official KFM ground recovery-input completion through `5050 -> 5200 -> 5201 -> 52 -> 0` with bounded `5050`/`5200`/`52` controller-order evidence, and optional official KFM ordered threshold evidence where `5050` keeps positive `hitFall.recoverTime` before `5200` appears with `recoverTime = 0` and the ground branch accepts recovery, but not full controller/VM tick order, exact threshold/down-recovery tables, or exact velocity/bounce math. | Required synthetic and optional official KFM artifacts continue to prove the executed state/controller chain while new cuts reduce residual gaps. |
| 3 | Exact guard-rule parity cut | Current guard behavior now includes held-back guard, bounded `InGuardDist` trigger plumbing, bounded automatic guard-start/end through Common1-style `120 -> 130 -> 140 -> 0` with required synthetic controller-event plus actor-frame order evidence, optional KFM controller/operation order for auto guard-start/end, bounded authored stand/crouch/air guard-hit routing, parsed `guard.slidetime` / `guard.ctrltime` flowing through `GetHitVar` when present, named controller/typed-operation routes for synthetic stand `150 -> 151`, crouch/atomic-`DB` `152 -> 153`, and air `154 -> 155`, required stand `150 -> 151 -> 130`, crouch `152 -> 153 -> 130`, and bounded synthetic air `154 -> 155 -> 52` landing handoff gates, plus optional KFM controller/operation order and actor-frame state/physics/body evidence for stand/crouch/air guard-hit routes, but still lacks exact proximity, exact air physics/timing/state-52 internals, spark/sound, effect, and guard-end timing parity. | Trace proves guard event, damage/stun/push, state/anim route where supported, actor-frame telemetry where supported, and unsupported guard details. |
| 4 | Studio actionable status contract | First cut done: generated/imported asset records, gates, evidence records, Build readiness, selected asset UI, `project.json`, and QA bridge now carry/display severity, impact, evidence ids, blockers, exportability, and next action. Source-package relink now has a first explicit Build Center affordance. | Next cut should persist those records across sessions, compare evidence, add persistent source handles, and turn regenerate actions into explicit affordances. |
| 5 | Evidence persistence and comparison | Second persistence/comparison/review/scrubber cut done: exported trace artifacts are stored in browser-local evidence history with project id, entry, source packages, checksum, stale/current status, QA bridge exposure, current-vs-persisted checksum/frame/event/gate deltas, a visible Trace Comparison Review, and a Trace Frame Scrubber with per-frame actor/effect/input/event deltas plus World Delta rows. | Next cut should add replay-style multi-frame diff views across multiple artifacts, persist more record kinds, and add regenerate plus persistent-source actions from stale/missing-source states. |

## Current Active Cuts

1. **Stabilize custom get-hit evidence**
   - Owner: Runtime / QA.
   - Build: keep `synthetic-imported-common-gethit` required in `pnpm qa:trace`.
   - Done: trace proves `p2stateno`, attacker-owned state execution, ordered typed `HitFallVel`, `HitFallDamage`, `HitFallSet`, `FallEnvShake` evidence, bounded P2 FallEnvShake runtime event telemetry, final `hitFall`, and no false full-parity claim.

2. **Move one side-effect family behind a runtime system**
   - Owner: Runtime architecture.
   - Current cut: Target* controller side effects now apply through `TargetSystem` while state-entry validation remains a match-runtime callback.
   - Current cut: Projectile, Helper, and Explod now compile into typed operation data and their systems consume it while preserving bounded effect-actor lifecycle.
   - Current cut: `EffectActorSystem` now owns per-fighter effect actor serials, bounded stores, advance/removal mutation, hit-removal pruning, snapshot handoff, and read-only store summaries for Explod/Helper/Projectile actors, so `PlayableMatchRuntime` no longer mutates three independent effect arrays directly.
   - Current cut: `RuntimeEffectActorWorld` now wraps those stores behind a world-style contract for spawn, active-effect advance, presentation-effect advance, removal, snapshots, summaries, and reset while preserving external store identity for tests and future Studio/QA inspection.
   - Current cut: `RuntimeEffectActorWorld.countActors(...)` now wraps bounded `NumExplod`, `NumHelper`, and `NumProj`/`NumProjID` trigger counts behind one world query consumed by `PlayableMatchRuntime`, including id filters and removed-projectile exclusion.
   - Current cut: `HelperSystem` / `RuntimeEffectActorWorld` now own explicit visual-helper removal by helper id or runtime serial, including all-owner helper clear for bounded lifecycle cleanup and p1/p2 store isolation; `RuntimeEffectSpawnWorld` exposes the same handoff for future controller dispatch.
   - Current cut: `HelperSystem` now owns a bounded helper-local micro-VM for current visual Helper actors spawned with owner runtime-program data. `RuntimeEffectSpawnWorld` passes owner runtime programs and animation maps into Helpers, and `RuntimeEffectLifecycleWorld` passes owner runtime state for bounded read-only `Parent, ...` / `Root, ...` helper redirects plus current opponent runtime state/id for bounded helper-local `EnemyNear, ...`, optional explicit `opponentStates` for caller-provided `EnemyNear(index)`, explicit-id `Target(id)` reads, owner-backed helper `TargetState` entry, stage bounds, stage time, and runtime tick for helper-local `GameTime`, edge-distance, and sound-event telemetry. Focused tests prove helper-local `Time` / `GameTime` trigger evaluation, stage-bound `FrontEdgeDist` param evaluation, runtime-tick helper-local `PlaySnd` telemetry, `VelSet`, `ChangeAnim`, `ChangeState`, `DestroySelf` removal, `CtrlSet`, `StateTypeSet`, `LifeAdd`/`LifeSet`/`PowerAdd`/`PowerSet`, `VarSet`/`VarAdd`/`VarRandom`/`VarRangeSet` trigger branches, helper-local `PlaySnd`/`StopSnd` sound-event telemetry, leading parent/root/opponent redirect branches/values, parent/root operands inside composite arithmetic/boolean/`IfElse(...)` expressions, direct opponent and caller-provided indexed opponent `StateNo`/`Life`/`Pos X`/`Var(...)` reads, explicit-id helper target memory, bounded direct-HitDef Target side effects, bounded owner-backed helper `TargetState`, static helper-local `Explod` visual side effects, static helper-local `Projectile` visual side effects, static helper-local `RemoveExplod` cleanup, and static helper-local `ModifyExplod` mutation by id of helper-parented owner-side Explods. Broader indexed/team/helper-owned redirects beyond caller-provided `EnemyNear(index)` lists, team/keyctrl, helper-owned opponents, helper custom-state table ownership, helper fvar/sysvar `VarRandom`, exact helper-local sound timing/channel/redirect ownership, exact helper HitDef lifetime/multi-hit parity, helper-owned Projectile combat/contact beyond bounded target memory, helper-bound Explod timing/mutation beyond static spawn/remove/modify/count-id routes, full helper combat parity, exact helper resource scopes, exact random stream parity, exact tick-order/pause parity, and full helper parity remain future work.
   - Current cut: `FighterMatchState` no longer retains the raw effect actor store; the match integration runtime reaches helpers/projectiles/explods through `RuntimeEffectActorWorld`.
   - Current cut: `MatchWorld` now creates and owns `RuntimeEffectActorWorld`, injects it into `PlayableMatchRuntime`, consumes its summaries as actor-registry `effectStores`, and `RuntimeTraceArtifact` preserves `world.effectStores` without changing behavior checksums.
   - Current cut: `RuntimeTrace` and `RuntimeTraceArtifact` now preserve typed helper/projectile/explod effect payload summaries and per-frame effect field deltas for Evidence review while omitting that inspection payload from behavior checksums; `requiredEffectPayloads` can make concrete Helper/Projectile/Explod payload facts gate-required.
   - Current cut: `MatchWorld` now tracks lightweight spawned/active/removed lifecycle metadata and per-tick lifecycle events for player/effect actors, exposes them through Debug Studio and the QA bridge, and `RuntimeTraceArtifact` frame summaries preserve `world.lifecycle` plus `world.eventsThisTick` without changing behavior checksums.
   - Current cut: `RuntimeProjectileCombatWorld` now owns the bounded projectile contact/reject/override/hit-or-guard/removal loop and is reached through `RuntimeEffectActorWorld`; Projectile controllers now carry typed guard params, bounded `projhits`/`projmisstime`, bounded `projpriority`, terminal-animation metadata for `projhitanim`/`projremanim`/`projcancelanim`, bounded `accel`, bounded `velmul`, bounded static `projscale`, and bounded contact-presentation refs for `hitsound`, `guardsound`, `sparkno`, `guard.sparkno`, and `sparkxy`. The required `synthetic-imported-projectile-motion` trace proves projectile acceleration and render-scale evidence, `synthetic-imported-projectile-velmul` proves velocity multiplier evidence, `synthetic-imported-projectile-time` proves a bounded `ProjHitTime(77)` branch after owner-side projectile contact memory, `synthetic-imported-projectile-hittime-any` checksum `47c1cf7f` proves a bounded `ProjHitTime(0)` any-projectile hit-time branch into state/action `282` after player-owned Projectile hit, `synthetic-imported-projectile-contacttime-id` checksum `e9ebf36a` proves a bounded `ProjContactTime(77)` fixed-id contact-time branch into state/action `321`, `synthetic-imported-projectile-contacttime-any` checksum `f1751155` proves a bounded `ProjContactTime(0)` any-projectile contact-time branch into state/action `281` after player-owned Projectile contact, `synthetic-imported-projectile-guardedtime-id` checksum `dfd08f28` proves a bounded `ProjGuardedTime(77)` fixed-id guard-time branch into state/action `322`, `synthetic-imported-projectile-guardedtime-any` checksum `c8473340` proves a bounded `ProjGuardedTime(0)` any-projectile guard-time branch into state/action `279` after player-owned Projectile guard, `synthetic-imported-projectile-canceltime` checksum `64e8dec4` proves a bounded owner-state `ProjCancelTime(77)` branch into state/action `283` after a player-owned Projectile is canceled by an opposing Projectile clash, `synthetic-imported-helper-projcanceltime-any` checksum `f7e7fa01` proves a bounded helper-local `ProjCancelTime(0)` branch into state/action `1267` after a helper-parented Projectile is canceled by an opposing Projectile clash, `synthetic-imported-projectile-target-redirect` checksum `cd099094` proves owner-local target trigger reads after a player-owned Projectile-only contact, `synthetic-imported-projectile-target-controllers` checksum `8c7bd6c2` proves delayed owner-local Target* side effects after explicit-id player-owned Projectile target memory, `synthetic-imported-projectile-default-target-controllers` checksum `1c1a3e77` proves the same default target id `0` side-effect route for omitted `projid` / `id`, `synthetic-imported-projectile-targetstate` checksum `dd1c7962` proves delayed owner-local TargetState into attacker-owned state data after explicit-id player-owned Projectile target memory, `synthetic-imported-projectile-default-targetstate` checksum `8f35f1fa` proves the same default target id `0` TargetState route, `synthetic-imported-projectile-contact` checksum `ba22ed74` proves hit contact package telemetry for `hitsound = S5,0` plus FightFX `sparkno = F7002`, `synthetic-imported-projectile-guard` checksum `4bcc5650` proves a held-back projectile block plus guard contact package telemetry for `guardsound = S6,0` plus FightFX `guard.sparkno = F7004`, `synthetic-imported-projectile-multihit` proves a single projectile can hit twice with a cooldown before removal, `synthetic-imported-projectile-clash` proves equal-priority projectile trade/removal plus cancel-animation metadata and actor-frame terminal playback evidence, and `synthetic-imported-projectile-priority-cancel` proves higher-priority cancellation removes the losing projectile with cancel-animation metadata plus bounded visible terminal playback while the winning projectile remains in its effect store after bounded priority decrement, while exact priority classes/exact terminal timing/exact guard effects and projectile effect timing/SND playback/exact `Proj*Time` hitpause/tick order/exact `ProjCancelTime` cancel tick-order/lifetime/exact `velmul` tick order/scaled collision/multi-target rules/helper-owned custom states/pause/custom-state parity remains unsupported.
   - Current cut update: `synthetic-imported-projectile-contacttime-id` checksum `e9ebf36a` and `synthetic-imported-projectile-guardedtime-id` checksum `dfd08f28` prove bounded owner-state `ProjContactTime(77)` and `ProjGuardedTime(77)` after player-owned Projectile contact/guard markers. Previous `synthetic-imported-projectile-canceltime-any` checksum `5bff1961` proves bounded owner-state `ProjCancelTime(0)` after that owner's player-owned Projectile id `77` is canceled. Previous `synthetic-imported-projectile-canceltime-var` checksum `e057e102` proves bounded owner-state `ProjCancelTime(var(0))` after owner-local `VarSet` seeds canceled player-owned Projectile id `77`. Previous `synthetic-imported-projectile-canceltime-dynamic` checksum `0da26c87` proves bounded owner-state expression-derived `ProjCancelTime(77 + var(0))` after that owner's player-owned Projectile is canceled by an opposing Projectile clash. Previous `synthetic-imported-helper-projcanceltime-dynamic` checksum `cc78dde2` proves bounded helper-local expression-derived `ProjCancelTime(8869 + var(0))` after a matching helper-parented Projectile is canceled by an opposing Projectile clash, while focused helper-local runtime coverage proves nonzero helper-local `ProjCancelTime(var(n))` id selection. Previous `synthetic-imported-helper-projcanceltime-id` checksum `fc412176` proves bounded helper-local `ProjCancelTime(8868)` after a matching helper-parented Projectile cancel; previous `synthetic-imported-helper-projcanceltime-any` checksum `f7e7fa01` proves bounded helper-local `ProjCancelTime(0)` after a helper-parented Projectile cancel, and previous `synthetic-imported-projectile-canceltime` checksum `64e8dec4` proves bounded owner-state `ProjCancelTime(77)` after a player-owned Projectile cancel; previous `synthetic-imported-helper-projguardedtime-any` checksum `bd64e9db` and `synthetic-imported-helper-projcontacttime-any` checksum `5c6a4e11` prove bounded helper-local `ProjGuardedTime(0)` and `ProjContactTime(0)` any-projectile routes after helper-parented Projectile guard/contact markers, and previous `synthetic-imported-helper-projhittime-any` checksum `bca9f47b` proves the hit-time route. Remaining unsupported `Proj*Time` scope is exact hitpause/contact/guard/cancel tick order, broad dynamic expression parity, multi-projectile id selection semantics beyond gated routes, redirects, teams/simul, and full helper/Projectile parity.
   - Current cut: `ActorSnapshot.runtime.targetRefs`/`targetBindings` and `MatchWorld.targetLinks` now expose recent target memory and active target binds to Debug Studio and trace `world` evidence without changing behavior checksums.
   - Current cut: `RuntimeTargetWorld` now wraps target-memory mutation/read paths behind a world-style contract for remember, advance, snapshot, runtime-state snapshot cloning, link derivation, count, find, and simplified Target* controller application. `MatchWorld` creates and owns that world, injects it into `PlayableMatchRuntime`, and now builds actor-registry target snapshots plus `targetLinks` through `RuntimeTargetWorld` instead of duplicating binding/link cloning logic inline, keeping existing target-link trace evidence behavior-stable while exact target semantics remain future work.
   - Current cut: `RuntimePauseWorld` now wraps current match-pause mutation/read paths behind a world-style contract for controller application, current-state reads, snapshot projection, source-movetime checks, countdown ticks, and reset. `PlayableMatchRuntime` delegates current `Pause`/`SuperPause` state mutation to that boundary while keeping existing trace behavior stable; exact pause layering and presentation parity remain future work.
   - Current cut: `RuntimePausedMatchWorld` now wraps the regular paused-match tick order behind a named boundary: hitpause-style command buffering during pause, source-actor `movetime` player/AI advancement, active and presentation effect advancement for the moving source, target binding application, stage clamp, frozen-actor paused presentation, pause replacement interruption, and pause countdown ticking. `advanceRuntime(...)` now also owns the concrete source-movetime wiring for actor-local target/effect/constraint systems, while `PlayableMatchRuntime` keeps fighting-specific input/fighter callbacks explicit. Exact pause layering, super backgrounds, helper VM pause behavior, rollback timing, and full MUGEN/IKEMEN pause parity remain future work.
   - Current cut: `RuntimeHitPauseWorld` now wraps the global hitpause tick order behind a named boundary: command buffering during hitpause, `ignorehitpause` controller dispatch for both actors, paused presentation advancement, and actor hitpause countdown. `advanceRuntime(...)` now also owns the concrete current-tick command-buffer and paused-presentation wiring through `RuntimeEffectLifecycleWorld`, while `PlayableMatchRuntime` keeps `ignorehitpause` controller execution explicit. This is ownership cleanup, not persistent-controller semantics, helper-owned hitpause, broad side-effect ordering, or exact hitpause tick-order parity.
   - Current cut: `RuntimeMoveLifecycleWorld` now wraps bounded active-move lifecycle mutation outside inline `PlayableMatchRuntime`: move ticking, non-reversal attack lock, completed move cleanup, reversal cleanup, and idle/control restoration callbacks. Focused `RuntimeMoveLifecycleSystem` tests prove no-op, active attack, completed attack, and completed reversal routes without adding new move semantics, cancel timing, input timing, or score movement.
   - Current cut: `RuntimeInputControlWorld` now wraps bounded local player and simple AI control intent outside inline `PlayableMatchRuntime`: input blocking, State -1 setup/entry precedence, local punch/kick intent, crouch/jump/walk/idle mutation, air drift, `NoWalk`, AI chase, and AI attack cooldown. Focused `RuntimeInputControlSystem` tests prove the boundary without adding new input semantics, exact command timing, exact AI behavior parity, or score movement.
   - Current cut: `RuntimeAssertSpecialWorld` now wraps the imported pre-facing `AssertSpecial` pass behind a named boundary: owner-backed active-state lookup, controller filtering, trigger gating, and runtime flag application before auto-facing. `PlayableMatchRuntime` still supplies trigger/evaluation callbacks and decides pass placement, so this is ownership cleanup, not exact persistence/global/helper/team/pause parity.
   - Current cut: `RuntimeEnvShakeWorld` now wraps bounded EnvShake/FallEnvShake event insertion and camera-shake projection behind a named runtime boundary. `PlayableMatchRuntime` delegates controller/fall event history mutation and stage camera-shake projection to that boundary while preserving existing actor event telemetry for renderer/debug/trace consumers; static `EnvShake` now lowers into typed `envshake` operation evidence before event insertion. Exact pause/stage/layer/waveform parity remains future work.
   - Current cut: `RuntimeAudioWorld` wraps bounded `PlaySnd`/`SndPan`/`StopSnd` event insertion behind a named runtime boundary, with static controller params now lowered into typed `audio:playsnd` / `audio:sndpan` / `audio:stopsnd` operation evidence before raw fallback. `PlayableMatchRuntime` delegates controller sound-event history mutation to that boundary while preserving existing actor sound telemetry for Web Audio/debug/trace consumers; direct `HitDef hitsound` and `guardsound` now also emit bounded attacker-side `PlaySnd` telemetry through required `synthetic-imported-hitdef-hit-sound.json` checksum `6fc00d8a` and `synthetic-imported-hitdef-guard-sound.json` checksum `fdf1f7f6`; static `lowpriority`, `volumescale`, `freqmul`, `loop`, `pan`, focused `abspan`, and static `SndPan` now survive compiler/runtime/trace or focused-test handoff for bounded Web Audio channel/gain/rate/loop/stereo-pan behavior. Dynamic refs, exact timing, mixing, broad channel priority, FightFX/system sound fallback, dynamic pan params, legacy volume, helper/redirect ownership, and full audio parity remain future work.
   - Current cut: `RuntimeHitEffectWorld` wraps bounded direct `HitDef` spark telemetry behind a named runtime boundary. Static `HitDef sparkno`/`guard.sparkno`/`sparkxy` refs compile into typed HitDef operation data, `PlayableMatchRuntime` delegates guard/hit spark event insertion to that boundary, required `synthetic-imported-hitdef-hit-spark.json` checksum `b6554124` proves bounded attacker-side hit `HitSpark` telemetry for `sparkno = S7001` plus `sparkxy = 10,-72`, required `synthetic-imported-hitdef-guard-spark.json` checksum `72c8fa3a` proves bounded attacker-side guard `HitSpark` telemetry for `guard.sparkno = S7000` plus `sparkxy = 12,-64`, required `synthetic-imported-hitdef-common-spark.json` checksum `5ea054d7` proves bounded common/default asset-frame metadata for unprefixed `sparkno = 7001`, required `synthetic-imported-hitdef-fightfx-spark.json` checksum `11537b56` proves bounded FightFX asset-frame metadata for `sparkno = F7002`, required `synthetic-imported-hitdef-common-guard-spark.json` checksum `7650a09c` proves bounded common/default guard asset-frame metadata for unprefixed `guard.sparkno = 7003`, and required `synthetic-imported-hitdef-fightfx-guard-spark.json` checksum `32f3e92d` proves bounded FightFX guard asset-frame metadata for `guard.sparkno = F7004`. `HitSparkRenderer` now consumes snapshot hit-effect events, treats `S` refs as player AIR action ids, classifies unprefixed refs as common/default and `F` refs as FightFX, resolves the first local player AIR frame into a sprite texture when available, preserves runtime-provided common/FightFX package frames from `DemoFighterDefinition.hitSparkLibraries` before synthetic fallback, synthesizes bounded common/FightFX system lookup frames through the global sprite namespace when no package frame exists, and keeps a bounded 180-frame additive fallback overlay at facing-adjusted `sparkxy`; native demo moves include local spark actions and visual smoke warms the renderer, resets the round, drives native contact, and gates active desktop/mobile spark plus player-source resolved-sprite diagnostics. `MugenCharacterLoader` now discovers optional package `fight.def`, resolves and parses `fightfx.air`, decodes `fightfx.sff` when possible, exposes those assets as current common/FightFX hit-spark libraries, `createImportedFighterDefinition` carries them into `hitSparkLibraries`, and `App` registers decoded system SFF sprites through a global hit-spark provider route. Exact visual binding, render timing, layering, scale, palette, motif/screenpack ownership, and full hit/guard-effect parity remain future work.
   - Current cut addendum: required common/default and FightFX hit/guard spark traces now require authored `sparkxy` offsets plus selected-frame and multi-frame AIR metadata before renderer handoff. `synthetic-imported-hitdef-common-spark.json` checksum `5ea054d7`, `synthetic-imported-hitdef-fightfx-spark.json` checksum `11537b56`, `synthetic-imported-hitdef-common-guard-spark.json` checksum `7650a09c`, and `synthetic-imported-hitdef-fightfx-guard-spark.json` checksum `32f3e92d` require selected first-frame offset `3,-4`, first-frame duration `5`, at least two asset frames, frame indices `[0, 1]`, and total authored duration `11` alongside source/action/first-frame/sprite metadata and spark offset requirements. Required `synthetic-imported-hitdef-hit-effect-package.json` checksum `46aa5ce1` proves one direct hit contact can emit bounded `hitsound = F5,0` telemetry with `soundPrefix = kfm` and FightFX `sparkno = F7002` selected-frame/multi-frame metadata with shared non-empty `contactId` / `contactTick` / `contactKind = hit` plus authored `sparkxy = 18,-68`; required `synthetic-imported-hitdef-guard-effect-package.json` checksum `1c3167b7` proves the same package evidence shape for guarded `guardsound = S6,0` plus FightFX `guard.sparkno = F7004`, `contactKind = guard`, and authored `sparkxy = 15,-63`. This is trace proof plus bounded prefixed-SND lookup only; exact intra-tick sound/spark ordering, channel priority/mixing/timing, visual frame timing, lookup, binding, layering, scale, palette, motif ownership, hit/guard-effect parity, and full spark/audio parity remain future work.
   - Current cut: `RuntimeEnvColorWorld` now wraps bounded `EnvColor` event history, stage-flash projection, and reset behind a named runtime boundary. `PlayableMatchRuntime` delegates controller stage-flash mutation/projection to that boundary while preserving existing stage-frame color/opacity telemetry; exact blend math, layer/window ordering, pause timing, and full presentation parity remain future work.
   - Current cut: `RuntimeSpriteEffectWorld` now wraps bounded `SprPriority`, `PalFX`, `AfterImage`, `AfterImageTime`, and `Angle*` mutation/ticking behind a named runtime boundary. `PlayableMatchRuntime` delegates those actor presentation state updates while preserving existing snapshot/trace telemetry; exact draw order, material math, trail cadence, palette/remap interaction, and full presentation parity remain future work.
   - Current cut: `RuntimeActorConstraintWorld` now wraps bounded `Width`, one-frame `PlayerPush`/`PosFreeze`/`ScreenBound` reset/projection, stage clamping, and body-push separation behind a named runtime boundary. `PlayableMatchRuntime` delegates those actor constraint updates while preserving existing snapshot/trace telemetry; exact player/edge collision, camera/screen-edge behavior, team/helper push, and tick-order parity remain future work.
   - Current cut: `RuntimeDirectCombatWorld` now wraps bounded same-tick direct `HitDef` priority win/trade mutation and bounded direct hit/guard result mutation behind a named runtime boundary. `PlayableMatchRuntime` delegates priority result consumption plus life/pause/stun/velocity/hitVars/hitFall/power/contact/received-damage/get-hit cleanup while keeping collision selection, target routing, and Common1/custom-state transitions as explicit integration points; exact direct-combat priority classes, throws, multi-hit/combo lifetime, helper/team/redirect ownership, and tick-order parity remain future work.
   - Current cut: `RuntimeHitOverrideWorld` now wraps bounded HitOverride slot countdown and redirect mutation behind a named runtime boundary. `PlayableMatchRuntime` delegates direct/projectile redirect state mutation to that boundary while keeping state-entry validation explicit; exact slot priority, attr grammar, helper/custom-state redirect breadth, projectile/custom-state edge timing, and IKEMEN parity remain future work.
   - Current cut: `RuntimeReversalWorld` now wraps bounded ReversalDef activation, active counter detection, and direct counter-result mutation behind a named runtime boundary. `PlayableMatchRuntime` keeps state-entry and target-state routing explicit; exact priority, guard/projectile/helper/custom-state counter breadth, attr grammar, trigger lifetime, hitpause/tick-order parity, and IKEMEN parity remain future work.
   - Current cut: `RuntimeProjectileCombatWorld` now wraps bounded projectile contact/reject/HitOverride/hit-or-guard/removal mutation plus projectile clash trade/cancel/decrement mutation behind a named runtime boundary. `RuntimeEffectActorWorld` delegates projectile combat and projectile-vs-projectile clash handling to that boundary while preserving existing effect-store cleanup and snapshot telemetry; exact projectile priority classes, multi-target/helper-owned projectile behavior, exact contact-trigger lifetime, terminal timing, guard effects, pause layering, and IKEMEN parity remain future work.
   - Current cut: `RuntimeEffectSpawnWorld` now wraps bounded Explod/Helper/Projectile spawn resolution, RemoveExplod dispatch, ModifyExplod dispatch, visual-helper removal handoff, and ModifyProjectile dispatch behind a named runtime boundary. `PlayableMatchRuntime` delegates those controller side effects while keeping operation recording and match orchestration explicit; exact spawn timing, helper VM ownership, real `DestroySelf`, helper-owned projectiles/effect namespaces, parent/root redirects, FightFX/common animation routing, advanced remove triggers, and full lifecycle parity remain future work.
   - Current cut: `RuntimeMatchInteractionWorld` now wraps bounded post-fighter interaction order behind a named runtime boundary: target-memory aging, active-effect advance, projectile clash, body separation, target bindings, priority/direct combat, projectile combat, stage clamp, and presentation-effect advance. `advanceRuntime(...)` now also owns the concrete normal-loop wiring for actor-local target/effect/constraint/projectile-clash systems, while `PlayableMatchRuntime` keeps fighting-specific combat callbacks explicit. Exact MUGEN/IKEMEN tick-order parity, pause-specific bridge ownership, helper VM lifecycle, parent/root redirects, and full interaction lifecycle parity remain future work.
   - Current cut: `RuntimeEffectLifecycleWorld` now wraps bounded active-effect tick, presentation tick, paused presentation tick, effect snapshot grouping, shared get-hit cleanup orchestration, and helper advance context wiring behind a named runtime boundary. `RuntimeMatchInteractionWorld`, `RuntimePausedMatchWorld`, and `RuntimeHitPauseWorld` pass `stageBounds`, `stageTime`, and `runtimeTick` through that boundary so helper-local `GameTime` / edge-distance reads and helper-local sound telemetry use match tick context where available. `PlayableMatchRuntime` delegates those lifecycle passes while direct combat, HitOverride, Reversal, and projectile get-hit routes share one get-hit cleanup helper; exact helper VM lifecycle, exact pause/combat ordering, advanced remove triggers, parent/root redirects, exact helper-local clock parity, and full lifecycle parity remain future work.
   - Current cut: `RuntimeContactMemoryWorld` now wraps bounded direct/projectile contact-memory creation, reset, mutation, and reads for `MoveContact`/`MoveHit`/`MoveGuarded`, `HitCount`/`UniqHitCount`, `HitAdd`, `MoveReversed`, `ReceivedDamage`/`ReceivedHits`, and projectile contact/time markers. `PlayableMatchRuntime` injects the world into fighter state and remains the actor/state-number integration point; `RuntimeDirectCombatWorld` and `RuntimeReversalWorld` consume the same boundary for direct hit/guard, received-damage, and reversal marker writes. This is ownership cleanup with behavior-stable trace evidence, not exact contact/combo lifetime parity.
   - Current cut: `RuntimeActiveControllerDispatchWorld` now wraps bounded active-controller route orchestration after active scan/trigger pass. It selects `RuntimeActiveStateDispatchWorld` for state/animation mutation first, shared `RuntimeControllerDispatchWorld` runtime-controller execution second, `RuntimeActiveSideEffectDispatchWorld` side-effect dispatch third, and unsupported fail-soft dispatch last. `PlayableMatchRuntime` still owns trigger filtering, concrete world instances, concrete state/action callbacks, frame/target/tick context, and active-loop order. This is ownership cleanup for dispatch plumbing, not exact CNS VM order, persistent controller semantics, helper/team/redirect execution, side-effect ordering parity, missing-action fallback parity, or full parity.
   - Current cut: `RuntimeResourceWorld` now wraps bounded resource and variable writes for `CtrlSet`, `LifeAdd`, `LifeSet`, `PowerAdd`, `PowerSet`, `VarSet`, `VarAdd`, partial `VarRandom`, `VarRangeSet`, and sysvar assignments inside `RuntimeResourceSystem`. It also owns shared authored life/power max resolution, runtime power-delta clamping, bounded life deltas, and control writes used by the playable match loop, direct combat, projectile combat, target controllers, and reversals. `RuntimeRandomSystem` owns deterministic sandbox-side seed creation, LCG advance, controller-safe clamping, and fallback random-unit salt consumed by `VarRandom` and expression contexts. `synthetic-imported-variable.json` checksum `3b33f7a8` now gates typed `variable:varrandom` evidence, and `synthetic-imported-control.json` checksum `80c4c446` still gates typed `resource:ctrlset` control-restore evidence. `StateControllerExecutor` remains the parameter/expression fallback layer, so this is ownership cleanup for the current controller subset, not exact CNS VM, random stream parity, helper/team/redirect resource ownership, target/projectile resource parity, or variable-scope parity.
   - Current cut: `RuntimeStunWorld` now wraps bounded hitstun/guardstun input-lock, timer decay, guarding flag maintenance, existing horizontal friction, hitstun action requests, imported hit-state moveType preservation, current-move guardrails, and non-imported idle moveType restoration. `PlayableMatchRuntime` still supplies concrete action-change and imported-state predicates, so this is ownership cleanup, not exact guard recovery, helper/custom-state stun ownership, or hitpause tick-order parity.
   - Current cut: `RuntimeHitEligibilityWorld` now wraps bounded `HitBy`/`NotHitBy` slot ticking and per-frame `AssertSpecial`/render-opacity reset. `StateControllerExecutor` still applies controller writes and combat systems still consume the runtime flags, so this is ownership cleanup, not exact attr grammar, slot priority, helper/team/global ownership, persistence layering, pause interaction, or full hit-eligibility parity.
   - Current cut: `RuntimeAssertSpecialWorld` now wraps bounded imported pre-facing `AssertSpecial` application before orientation. Static `AssertSpecial` flags now lower into typed `assertspecial` operation evidence before `StateControllerExecutor` applies the same runtime writes, and exact flag lifetime/layering/global ownership remains blocked.
   - Current cut: `RuntimeSnapshotWorld` now wraps bounded stage/camera, player actor, and final effect snapshot aggregation: camera center honors `ScreenBound moveCameraX = 0`, falls back when all actors disable camera follow, carries EnvShake/EnvColor presentation data into `StageSnapshot`, projects player actor identity/source/sprite-owner metadata, cloned runtime state, target refs/bindings, collision boxes, fallback hurtbox, and event histories, then aggregates cloned Explod/Helper/Projectile snapshots in stable p1/p2 order. Compatibility sessions, target semantics, exact stage/motif camera rules, effect VM semantics, and renderer parity remain blocked.
   - Current cut: `RuntimeOrientationWorld` now wraps bounded automatic facing and `Turn` facing flips. `PlayableMatchRuntime` still owns the pass order around imported pre-facing `AssertSpecial`, and `StateControllerExecutor` still validates operation shape before delegation, so this is ownership cleanup, not exact auto-facing order, target-facing/team/helper semantics, redirect ownership, or full orientation parity.
   - Current cut: `RuntimeRecoverySystem` now wraps bounded `fall.recovertime` countdown, Common1 liedown `data.liedown.time` default/decrement, and imported `5201 -> 52` ground-recovery landing hooks. `PlayableMatchRuntime` still validates and enters states through callbacks, so this is ownership cleanup, not exact Common1 recovery threshold or controller-loop parity.
   - Current cut: `RuntimeTargetWorld.applyBindToTargetController` now wraps bounded `BindToTarget` target lookup, raw/typed postype binding, duration, facing-aware position application, and operation reporting. `PlayableMatchRuntime` still supplies target `[Size]` anchors and evidence callbacks, so this is ownership cleanup, not exact bind tick order, team/helper/redirect behavior, or full target parity.
   - Current cut: `RuntimeTargetWorld.resolveCandidates` now filters concrete candidate actors through live target memory before Target* / BindToTarget controller application and active binding position application. `PlayableMatchRuntime` still supplies the current materialized opponent actor and owns post-fighter ordering, so this is ownership cleanup, not helper/projectile target ownership, team/multi-target selection, exact bind tick order, redirects, or full target parity.
   - Current cut: `RuntimeRoundSystem` now wraps bounded timer decrement, KO/time-over finish state, winner/message snapshot projection, and reset semantics; `RuntimeMatchRoundWorld` now wraps active-match timer delegation plus finish side effects that stop playback and emit the round log. `PlayableMatchRuntime` still owns life mutation and broader match/round orchestration, so this is ownership cleanup, not round/lifebar/team/screenpack parity.
   - Current cut: `RuntimeTraceGate.requiredRoundFrames` now gates bounded `RoundSnapshot` evidence by state, winner, message, frame count, and observed timer range. Required `synthetic-imported-round-ko.json` checksum `bfd5f073` proves KO snapshot evidence and final P2 life `0` after a lethal imported `HitDef`, without claiming exact MUGEN/IKEMEN round flow.
   - Current cut: `PlayableMatchRuntimeOptions.roundTimerFrames` / `MatchWorldOptions.roundTimerFrames` now provide short deterministic timer fixtures, and required `synthetic-imported-round-timeover.json` checksum `7d9f7907` proves bounded `timeover`/draw snapshot evidence without waiting for the default 99-second timer.
   - Current cut: `MatchWorldLifecycleSystem` now wraps actor/effect lifecycle records, live/removed lists, spawn/active/remove events, and recent event history for `MatchWorld` and trace evidence. `MatchWorld` still derives registry data from snapshots/stores, so this is evidence ownership, not full actor simulation ownership.
   - Current cut: `RuntimeTraceGate.requiredTargetLinks` now makes world-visible target ownership/binding evidence a required gate condition for the synthetic imported Target* route and target-memory evidence a required gate condition for the synthetic imported Projectile route. Requirements can also check observed link frame counts, target age, binding remaining ranges, and exact TargetBind offsets; `synthetic-imported-target.json` checksum `f5a16dc9` now also requires P2 actor-frame facing/velocity evidence plus final P2 `life = 943` / `power = 40` after bounded TargetLifeAdd/TargetPowerAdd/TargetVel*/TargetFacing side effects. `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2` now requires the same TargetLifeAdd/TargetPowerAdd/TargetVel*/TargetFacing/TargetBind/TargetDrop operation family after explicit-id player-owned Projectile target memory, with final P1 `targetCount = 0` and final P2 `life = 949` / `power = 40` and no direct HitDef controller evidence in that fixture. `synthetic-imported-projectile-default-target-controllers.json` checksum `1c1a3e77` requires the same side-effect family after omitted `projid` / `id` defaults target memory to id `0`. `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962` requires player-owned Projectile target memory to feed `TargetState value = 888`, P2 custom-owner actor frames in states `888` and `889`, `SelfState` return, final P2 state `0`/control, and final P2 `life = 969` without direct HitDef controller evidence. `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa` requires the same TargetState route after omitted `projid` / `id` defaults target memory to id `0`. `synthetic-imported-bare-target-redirect.json` checksum `f9c90aa8` proves bounded bare `Target, Life` trigger reads from direct `HitDef` target memory and routes P1 `200 -> 270`; `synthetic-imported-projectile-target-redirect.json` checksum `cd099094` proves bounded `Target(77), Life` trigger reads from player-owned Projectile target memory and routes P1 `200 -> 277`; `synthetic-imported-default-target-redirect.json` checksum `d43caabf` proves bounded `Target(0), Life` trigger reads from omitted-id direct `HitDef` target memory and routes P1 `200 -> 269`; `synthetic-imported-target-dynamic-redirect.json` checksum `9985b62a` proves bounded `Target(var(0)), Life` trigger reads from current target memory and routes P1 `200 -> 287` after direct contact plus owner-local `var(0) = 77`; `synthetic-imported-target-redirect.json` checksum `89580963` keeps static `Target(77), Life` routing gated. `synthetic-imported-target-noko.json` checksum `321a1eba` requires ordered defender `AssertSpecial NoKO` before `HitDef` and lethal `TargetLifeAdd`, target-link id `77`, and final P2 life `1`, proving bounded target-controller no-KO clamping without exact NoKO lifetime or target parity.
   - Current cut: static imported `Width player/value` now compiles into typed `collision:width`, `PlayableMatchRuntime` consumes the operation for bounded body-width/push separation, and `synthetic-imported-width.json` requires state `200`, controller `Width`, operation `collision:width`, and actor-frame body-width evidence. Exact edge/player semantics, exact push overlap resolution, and tick-order parity remain future work.
   - Current cut: static imported `StateTypeSet statetype/movetype/physics` now compiles into typed `metadata:statetypeset`, `StateControllerExecutor` consumes the operation before raw fallback, and `synthetic-imported-statetypeset.json` requires state `200`, controller `StateTypeSet`, operation `metadata:statetypeset`, and actor-frame `C/A/N` metadata evidence. Exact physics/tick-order interactions remain future work.
   - Current cut: static imported `PlayerPush value` now compiles into typed `collision:playerpush`, `StateControllerExecutor` consumes the operation before raw fallback, and `synthetic-imported-playerpush.json` requires state `200`, controller `PlayerPush`, operation `collision:playerpush`, and actor-frame `playerPush = false` evidence. Exact overlap resolution, team/helper push rules, and tick-order parity remain future work.
   - Current cut: static imported `Turn` now compiles into typed `orientation:turn`, `StateControllerExecutor` consumes the operation before raw fallback, and `synthetic-imported-turn.json` requires state `200`, controller `Turn`, operation `orientation:turn`, and actor-frame `facing = -1` evidence. Exact auto-facing, tick-order, team/helper/target-facing parity, and broader orientation semantics remain future work.
   - Current cut: static imported `SprPriority value/priority` now compiles into typed `sprite-effect:sprpriority`, the imported runtime consumes the operation before raw fallback, and `synthetic-imported-sprpriority.json` requires state `200`, controller `SprPriority`, operation `sprite-effect:sprpriority`, and actor-frame `spritePriority = 5` evidence. Exact layer/shadow/helper/Explod draw-order parity remains future work.
   - Current cut: static imported `PalFX time/add/mul/color/invert` now compiles into typed `sprite-effect:palfx`, the imported runtime consumes the operation before raw fallback, and `synthetic-imported-palfx.json` requires state `200`, controller `PalFX`, operation `sprite-effect:palfx`, and actor-frame material telemetry evidence. Exact palette math, blending, `sinadd`, remap interaction, and timing parity remain future work.
   - Current cut: static imported `RemapPal source/dest` now compiles into typed `sprite-effect:remappal`, `StateControllerExecutor` consumes the operation before raw fallback, and `synthetic-imported-remappal.json` requires state `200`, controller `RemapPal`, operation `sprite-effect:remappal`, and actor-frame palette-remap telemetry evidence. ACT/SFF pixel remapping, palette application, PalFX interaction, and timing parity remain future work.
   - Current cut: static imported `AfterImage` / `AfterImageTime` now compile into typed `sprite-effect:afterimage` / `sprite-effect:afterimagetime`, the imported runtime consumes the operation before raw fallback, and `synthetic-imported-afterimage.json` requires state `200`, controllers `AfterImage` / `AfterImageTime`, both typed operations, and actor-frame ghost-trail telemetry. Exact blend modes, palette/remap interaction, sampling cadence, persistence edge cases, and engine-perfect timing remain future work.
   - Current cut: `TargetDrop` typed IR and raw runtime fallback now match the official omitted-`keepone` default of `1`, with unit coverage for preserving one `excludeID` target and pruning bindings; `RuntimeTrace` final-actor evidence exposes `targetCount`, and `synthetic-imported-target.json` requires P1 `targetCount = 0` after TargetDrop plus final P2 resource evidence after TargetLifeAdd/TargetPowerAdd. Multi-target runtime semantics remain future work.
   - Current cut: `BindToTarget` now compiles into typed `bindtotarget` operation data and can keep the controller owner bound to a recent target with static `pos = x,y,Foot|Mid|Head`; `Mid`/`Head` use parsed target `[Size]` anchors when available, active owner-to-target binds are exposed in runtime snapshots and `MatchWorld.targetLinks`, and `synthetic-imported-bindtotarget-head.json` plus `synthetic-imported-bindtotarget-mid.json` are now required trace evidence for those anchor paths.
   - Current cut: `RuntimeTraceGate` can require actor-frame/final-actor `customOwnerId`; `synthetic-imported-custom-state.json` now proves bounded owner-backed `HitDef p2stateno = 888`, P1-owned `ChangeState` chain to `889`, and `SelfState` return to P2 state `0`/control with checksum `bf632df3`. `synthetic-imported-target-owned-custom-state.json` checksum `410fb8c0` proves the complementary `p2getp1state = 0` route: P2 enters defender-owned state/action `888` without attacker `customOwnerId`, then returns through `SelfState`.
   - Current cut: `synthetic-imported-targetstate-custom.json` now proves bounded target-memory-driven owner-backed `TargetState value = 888`, P1-owned `ChangeState` chain to `889`, and `SelfState` return to P2 state `0`/control with checksum `fedaf0a4`.
   - Current cut: `synthetic-imported-enemynear-index.json` checksum `b97e2eda` proves bounded one-on-one `EnemyNear(0), StateNo` routing from State -1 into state `284` while a positive-index `EnemyNear(1), StateNo` trap state `285` stays forbidden. Positive indexes beyond the current opponent fail closed in the one-on-one sandbox unless a caller supplies an explicit roster; `RuntimeOpponentSelectionWorld` now owns the bounded nearest-body-distance ordering used by those supplied rosters before `EnemyNear(index)` resolves in both active runtime expression contexts and helper-local `opponentStates` / `opponentRoster` contexts. Helper-local `opponentRoster` can preserve ids for bounded non-current `TeamSide` reads after sorting. Negative or dynamic-invalid indexes remain reported unsupported. Real team/simul roster ownership, helper-owned opponent roster discovery, redirect mutation, exact opponent lifetime, y-axis/priority selection parity, and full IKEMEN/MUGEN EnemyNear parity remain blocked.
   - Current cut: `synthetic-imported-numtarget.json` proves bounded `NumTarget(77)` trigger reads from direct `HitDef` target memory and branches the owner from state `200` to `263`; `synthetic-imported-default-numtarget.json` checksum `5869ebbd` proves the omitted-id `NumTarget(0)` route; `synthetic-imported-bare-target-redirect.json` checksum `f9c90aa8` proves bounded bare `Target, Life` trigger redirect reads from direct `HitDef` target memory and branches P1 from state `200` to `270`; `synthetic-imported-default-target-redirect.json` checksum `d43caabf` proves bounded `Target(0), Life` trigger redirect reads from omitted-id direct `HitDef` target memory and branches P1 from state `200` to `269`; `synthetic-imported-target-dynamic-redirect.json` checksum `9985b62a` proves bounded `Target(var(0)), Life` trigger redirect reads from current target memory and branches P1 from state `200` to `287`; `synthetic-imported-target-redirect.json` checksum `89580963` keeps static `Target(77), Life` routing gated. Unsupported or negative target-id expressions, helper/projectile targets, mutation through redirects, teams, multi-target selection, and exact target lifetime/tick order remain blocked.
   - Current cut: `synthetic-imported-numhelper.json` and `synthetic-imported-numproj.json` now prove bounded `NumHelper(42)` / `NumProjID(77)` trigger reads from the effect-actor stores and branch the owner from state `200` to `264` / `273`.
   - Current cut: `synthetic-imported-helper-projectile.json` checksum `b6269136` proves bounded helper-local `Projectile` side-effect dispatch in `HelperSystem`, routing a visual Helper from `1200` to `1212` / anim `932` while spawning owner-side Projectile anim `943` with `parentId = p1-helper-0`. Previous `synthetic-imported-helper-numhelper.json` checksum `4e32e951` still proves bounded helper-local `NumHelper(id)`, previous `synthetic-imported-helper-numexplod.json` checksum `4328278a` still proves bounded helper-local `NumExplod(id)`, previous `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` still proves bounded helper-local static `ModifyExplod` mutation, previous `synthetic-imported-helper-removeexplod.json` checksum `ff8658a2` still proves bounded helper-local static `RemoveExplod` cleanup, previous `synthetic-imported-helper-explod.json` checksum `87ae363f` still proves bounded helper-local static `Explod` spawn, previous `synthetic-imported-helper-bindtoroot.json` checksum `bf72306c` still proves bounded root binding with root `ownerBind` payload evidence, previous `synthetic-imported-helper-bindtoparent.json` checksum `f9922c0e` still proves bounded parent binding with parent `ownerBind` payload evidence, previous `synthetic-imported-helper-enemynear.json` checksum `35498955` still proves bounded helper-local opponent routing, and previous `synthetic-imported-helper-ishelper.json` checksum `37877602` still proves bounded helper identity routing.
   - Current cut: `synthetic-imported-numexplod.json` now proves bounded `NumExplod(9000)` trigger reads from the Explod effect-actor store and branches the owner from state `200` to `274`.
   - Current cut: `synthetic-imported-removeexplod.json` now proves bounded `RemoveExplod id = 9000` removes a visual Explod and emits MatchWorld lifecycle remove evidence; checksum `2387da19`.
   - Current cut: `synthetic-imported-explod-velocity.json` now proves bounded Explod `vel`/`accel` movement with observed actor-frame position/velocity evidence; checksum `5adcfc81`.
   - Current cut: `synthetic-imported-explod-bind.json` now proves bounded owner-side Explod `bindtime` follows P1 while the owner moves through `PosAdd`; checksum `6e71298b`.
   - Current cut: `synthetic-imported-explod-scale.json` now proves bounded static Explod `scale = 2,0.5` reaches render snapshots/projection and actor-frame scale evidence; checksum `52bc1513`.
   - Current cut: `synthetic-imported-explod-removeongethit.json` now proves bounded owner-side Explod `removeongethit = 1` pruning when the owner enters the current get-hit route; checksum `0c394dc9`.
   - Current cut: `synthetic-imported-explod-removeonprojectilehit.json` proves the same owner-side Explod pruning through the current Projectile hit route; checksum `c37db36c`.
   - Current cut: `synthetic-imported-explod-removeonprojectileguard.json` proves the same owner-side Explod pruning through the current Projectile guard route; checksum `1faf8796`.
   - Current cut: `synthetic-imported-explod-ignorehitpause.json` proves bounded visual Explod `ignorehitpause = 1` advances during hitpause while a sibling Explod remains frozen; checksum `a351168e`.
   - Current cut: `synthetic-imported-explod-pausemovetime.json` proves bounded visual Explod `pausemovetime = 4` advances after regular `Pause` source `movetime` expires while a sibling Explod remains frozen; checksum `467c2030`.
   - Current cut: `synthetic-imported-explod-supermovetime.json` proves bounded visual Explod `supermovetime = 4` advances after source `SuperPause` `movetime` expires while a sibling Explod remains frozen; checksum `1d99f66f`.
   - Current cut: `synthetic-imported-targetbind-pause.json` now proves a bounded TargetBind + SuperPause movetime route: a real `HitDef` creates target memory, typed `TargetBind` records a binding with observed `36,-12` offset and remaining-frame evidence, typed `SuperPause` creates a match-pause snapshot, a source `PosAdd` executes during movetime, and `MatchWorld.targetLinks` keeps the bound target visible with binding metadata.
   - Current cut: `RuntimeTraceGate.requiredWorldLifecycleEvents` now makes `MatchWorld` lifecycle evidence a required gate condition; the synthetic imported Projectile route requires both projectile spawn and remove events with owner/root/parent metadata, while Helper and Explod routes require spawn/active events for their bounded visual effect actors.
   - Current cut: `RuntimeTraceGate.requiredEffectStores` now makes `RuntimeEffectActorWorld` producer-store evidence a required gate condition for the synthetic imported Projectile, Helper, and Explod routes.
   - Current cut: `RuntimeTraceGate.requiredMatchPauses`, `requiredMatchPauseAdvances`, and `requiredMatchPauseFreezes` now make match-freeze snapshot, source-movetime advance, and frozen actor/effect evidence required gate conditions for synthetic imported SuperPause routes, including source actor/state, darken, remaining frames, source `movetime`, P2 freeze, a bounded `Projectile + SuperPause` route where the `p1` projectile effect actor advances during source `movetime` and then remains frozen afterward, and a bounded `Helper + Explod + SuperPause` route where visual helper/explod actors advance during source `movetime` and freeze afterward; runtime tests also assert source animation advances during `movetime` while the opponent remains frozen.
   - Claim allowed: `synthetic-imported-targetbind-pause.json` proves bounded TargetBind offset preservation during source `SuperPause` movetime; `synthetic-imported-projectile-contact.json` checksum `ba22ed74` proves bounded Projectile hit contact with target-link/contact-trigger evidence plus shared `hitsound = S5,0`, FightFX `sparkno = F7002`, and authored `sparkxy = 18,-68` package telemetry; `synthetic-imported-projectile-guard.json` checksum `4bcc5650` proves bounded Projectile guard resolution with typed guard params, target-link evidence, guard event/combat-reason evidence, a bounded `ProjGuarded(77)` branch, and shared `guardsound = S6,0` plus FightFX `guard.sparkno = F7004` with authored `sparkxy = 15,-63` package telemetry; `synthetic-imported-helper-projhit.json` checksum `2d9a281e`, `synthetic-imported-helper-projguard.json` checksum `1c2c18a5`, and `synthetic-imported-helper-projcontact.json` checksum `67ed6c2d` prove matching owner-side target-link plus sound/FightFX spark package telemetry with authored spark offsets plus selected AIR-frame offset/duration for helper-parented Projectile hit/guard/generic contact routes; `synthetic-imported-projectile-multihit.json` proves bounded single-target `projhits`/`projmisstime` re-contact and final removal; `synthetic-imported-projectile-clash.json` proves bounded equal-priority projectile-vs-projectile trade/removal with runtime event evidence, preserved `projcancelanim` metadata, and actor-frame terminal playback evidence; `synthetic-imported-projectile-priority-cancel.json` proves bounded higher-priority projectile cancel/removal with winner-store, priority-decrement evidence, loser `projcancelanim` metadata, and visible terminal playback; `synthetic-imported-superpause-projectile-freeze.json` proves bounded projectile effect actor source-movetime advance plus freeze evidence during the current partial SuperPause path; `synthetic-imported-superpause-effect-freeze.json` proves bounded visual Helper/Explod source-movetime advance plus freeze evidence; `synthetic-imported-explod-ignorehitpause.json` proves bounded Explod-owned `ignorehitpause` advance during hitpause; `synthetic-imported-explod-pausemovetime.json` proves bounded Explod-owned `pausemovetime` advance after regular Pause source movetime ends; `synthetic-imported-explod-supermovetime.json` proves bounded Explod-owned `supermovetime` advance after SuperPause source movetime ends.
   - Still blocked: exact projectile/helper/explod pause parity, helper VM pause behavior beyond visual actor evidence, exact Explod scaling/bind/lifetime parity beyond current bounded static cases, exact projectile priority classes, exact projectile terminal timing/rem-trigger parity, exact helper-owned target ownership, full guard effects, exact sound/spark/super-background timing, and full MUGEN/IKEMEN pause layering.
   - Next candidate: after the helper-local Projectile cut, exact projectile parity beyond the bounded clash subset, richer effect pause ordering, exact target semantics behind `MatchWorld`, deeper helper-owned combat/contact, helper-bound effect tick-order/namespace work, exact helper effect-count/ownership parity, or real KFM/Common1 get-hit/fall gating, whichever can move without broad combat drift.
   - Done: old and new traces keep matching checksums unless the behavior change is intentional and documented; the next cut should promote exact effect combat/pause decisions, not only store access, through `MatchWorld`.

3. **Typed ControllerOp expansion**
   - Owner: Compiler/runtime.
   - Current cut: `Pause`/`SuperPause` now compile into typed `pause:*` operations and the required synthetic `SuperPause` trace gate also requires `matchPause` snapshot evidence; `Projectile`, `Helper`, `Explod`, `HitFall*`, `FallEnvShake`, static `EnvShake`, static numeric movement/position controllers including `VelSet`, `VelAdd`, `VelMul`, `PosSet`, `PosAdd`, `Gravity`, and `HitVelSet`, static `PosFreeze`/`ScreenBound`, static `Width`, static `PlayerPush`, static `StateTypeSet`, static `Turn`, static numeric resource/control/variable controllers, typed `VarSet`/`VarAdd sysvar(n)` assignments, static hit-eligibility setup controllers, static `ReversalDef`, and static `AttackMulSet`/`DefenceMulSet` now compile into typed operation evidence, with required synthetic trace coverage now including `kinematic:velset`, `kinematic:veladd`, `kinematic:velmul`, `kinematic:posset`, `kinematic:posadd`, `kinematic:gravity`, `kinematic:hitvelset`, `bounds:posfreeze`, `bounds:screenbound`, `collision:width`, `collision:playerpush`, `metadata:statetypeset`, `orientation:turn`, `resource:ctrlset`, `resource:lifeadd`, `resource:lifeset`, `resource:poweradd`, `resource:powerset`, `eligibility:hitby`, `eligibility:nothitby`, `reversaldef`, `damage-scale:attackmulset`, `damage-scale:defencemulset`, and `envshake`. Static `HitDef` operation data now also preserves bounded `kill`, `guard.kill`, and `fall.kill` params for nonlethal direct/chip/fall-damage clamps; static `LifeAdd kill` lowers into resource ops for bounded negative-life clamps. `RuntimeTrace` stage-frame evidence now gates the bounded `ScreenBound` X clamp bypass and X camera-follow exclusion route without adding stage summaries to behavior checksums; actor-frame position, velocity, life, power, body-width, body-push, facing, control, resource values, and metadata evidence gate the bounded static kinematic, `CtrlSet`, `Gravity`, `Width`, `PlayerPush`, `Turn`, `LifeAdd`/`LifeSet`/`PowerAdd`/`PowerSet`, and `StateTypeSet` routes without changing unrelated behavior checksums.
   - Later candidate families: deeper effect lifecycle ownership, richer get-hit/fall state flow, or the next high-value raw controller family.
   - Done: reports distinguish parsed, recognized, compiled, executed partial, unsupported, and unknown; trace gates require `executedOperations`.

4. **KFM/Common1 get-hit and fall gate**
   - Owner: Compatibility runtime.
   - Current cut: optional `kfm-official-default-gethit` proves official KFM as defender enters real Common1 state `5000` from a HitDef without `p2stateno`; required `synthetic-imported-default-gethit-progression` checksum `ef2a67f8` now proves bounded stand get-hit progression `5000 -> 5001 -> 0` with ordered `5000`/`5001` `ChangeState` controller-event evidence, actor-frame `5000 -> 5001` evidence, and required stand shake/slide state/physics/Clsn/velocity telemetry; optional `kfm-official-default-gethit-progression` proves real KFM stand get-hit progression `5000 -> 5001 -> 0` through `HitShakeOver` and `HitOver`; optional `kfm-official-auto-guard-start` and `kfm-official-auto-guard-end` now require ordered real KFM Common1 auto guard-start/end controller/operation evidence through `120 -> 130 -> 140 -> 0`, including `StateTypeSet`/`metadata:statetypeset` and return-to-idle `VelSet`/`kinematic:velset`; required `synthetic-imported-default-guard-state`, `synthetic-imported-crouch-guard-state`, `synthetic-imported-diagonal-crouch-guard-state`, and `synthetic-imported-air-guard-state` prove bounded guard-hit `150 -> 151`, crouch guard-hit `152 -> 153`, atomic `DB` input normalization for held-back/held-down guard checks, air guard-hit `154 -> 155`, and a final grounded/control snapshot; those required guard-state gates now also require named controller/typed-operation order through `ChangeAnim`, `ChangeState`, `HitVelSet`, `kinematic:hitvelset`, `CtrlSet`, `resource:ctrlset`, and final `ChangeState`, plus actor-frame state/physics/body/push telemetry, with air also pinning `VelAdd` gravity and bounded velocity evidence; required `synthetic-imported-hitdef-hit-sound` checksum `6fc00d8a` proves bounded direct `HitDef hitsound = S5,0` telemetry after hit, required `synthetic-imported-hitdef-guard-sound` checksum `fdf1f7f6` proves bounded direct `HitDef guardsound = S6,0` telemetry after guard, required `synthetic-imported-hitdef-hit-spark` checksum `b6554124` proves bounded direct `HitDef sparkno = S7001` plus `sparkxy = 10,-72` telemetry after hit, required `synthetic-imported-hitdef-guard-spark` checksum `72c8fa3a` proves bounded direct `HitDef guard.sparkno = S7000` plus `sparkxy = 12,-64` telemetry after guard, required `synthetic-imported-hitdef-common-spark` checksum `5ea054d7` proves bounded common/default source-frame metadata for unprefixed `sparkno = 7001`, required `synthetic-imported-hitdef-fightfx-spark` checksum `11537b56` proves bounded FightFX source-frame metadata for `sparkno = F7002`, required `synthetic-imported-hitdef-common-guard-spark` checksum `7650a09c` proves bounded common/default guard source-frame metadata for unprefixed `guard.sparkno = 7003`, required `synthetic-imported-hitdef-fightfx-guard-spark` checksum `32f3e92d` proves bounded FightFX guard source-frame metadata for `guard.sparkno = F7004`, required `synthetic-imported-hitdef-hit-effect-package` checksum `46aa5ce1` proves one direct hit contact emits both `hitsound = F5,0` with `soundPrefix = kfm` and FightFX `sparkno = F7002` metadata together, and required `synthetic-imported-hitdef-guard-effect-package` checksum `1c3167b7` proves one guarded direct contact emits both `guardsound = S6,0` and FightFX `guard.sparkno = F7004` metadata together; optional `kfm-official-x-hit-sound` and `kfm-official-x-hit-spark` both pass with checksum `bd153db9`, proving real KFM `x -> 200` emits bounded `S5,0` hit sound and `sparkno 0` hit spark telemetry after contact when the private fixture exists; optional `kfm-official-default-guard-state`, `kfm-official-default-crouch-guard-state`, and `kfm-official-default-air-guard-state` prove real KFM Common1 reaches those same stand/crouch/air guard-hit routes when the private fixture exists, including `155 -> 52 -> grounded control` for the air route, and now require ordered controller/typed-operation evidence plus actor-frame state/physics/body telemetry for those official routes; required `synthetic-imported-default-fall-gethit` checksum `6af73a91` proves a portable default fall branch into `5000 -> 5030 -> 5050` with ordered actor-frame evidence plus controller/typed-operation order through `ChangeState`, `VelAdd`, `HitVelSet`, and `kinematic:hitvelset`; required `synthetic-imported-default-fall-recovery` proves a bounded recovery chain through `5100 -> 5101 -> 5110 -> 5120 -> 0`; required `synthetic-imported-default-fall-recovery-input` proves a bounded `CanRecover` plus `command = "recovery"` route through `5050 -> 5210 -> 0`; required `synthetic-imported-default-fall-recovery-threshold` checksum `7bb15a5f` proves actor-frame recovery countdown handoff from positive `hitFall.recoverTime` in `5050` to `recoverTime = 0` in `5210`, with first-to-last drop evidence inside `5050`; required `synthetic-imported-default-fall-recovery-tick-order` checksum `e2691aab` proves that dropping summarized actor-frame countdown evidence must precede `5210` recovery evidence and now also requires the bounded named controller/typed-operation route through `VelAdd` `Gravity`, recovery `ChangeState`, `VelSet`, `kinematic:velset`, `HitFallSet`, `hitfall:hitfallset`, and final `ChangeState`; required `synthetic-imported-default-fall-air-recovery-velocity` checksum `560f6308` proves bounded `5210` air-recovery velocity telemetry; required `synthetic-imported-default-fall-ground-recovery` checksum `7945fd93` proves bounded near-ground selection through `5050 -> 5200 -> 5201 -> 52 -> 0` with `SelfState`, `VelSet`, `PosSet`, and actor-frame velocity evidence; required `synthetic-imported-default-fall-recovery-too-early` checksum `050e7e3c` proves the same command is rejected while `fall.recovertime` is still positive and the defender remains in `5050`; optional `kfm-official-default-fall-gethit` checksum `0b3ece0c` proves real KFM reaches ground-impact `5100`, bounce-entry `5101`, and lie-down `5110` with ordered `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110` actor-frame and controller/typed-operation evidence; optional `kfm-official-default-fall-recovery` proves real KFM continues through `5110 -> 5120 -> 0` with `HitFallSet` and final control; optional `kfm-official-default-fall-recovery-input` proves real KFM can route `5050 -> 5210 -> 52 -> 0` through `CanRecover` plus `command = "recovery"`; optional `kfm-official-default-fall-ground-recovery` proves real KFM can route `5050 -> 5200 -> 5201 -> 52 -> 0` through the same recovery command near ground; optional `kfm-official-default-fall-recovery-threshold` checksum `bf7b058a` now also requires ordered actor-frame evidence where positive-recoverTime state `5050` precedes state `5200` with `recoverTime = 0`.
   - Current required HitFall recover-true cut: `synthetic-imported-hitfall-recover-true` checksum `f1e3424a` proves bounded Common1-style `HitFall && GetHitVar(fall.recover) && !CanRecover` trigger routing through `5000 -> 5030 -> 5050 -> 5240 -> 0`, with named `HitFall Recover Enabled Probe` `ChangeState`, ordered actor frames, positive `fall.recovertime` metadata, and recovery states `5210`/`5200` forbidden. Previous required recover-disabled cut `synthetic-imported-hitfall-recover-false` checksum `236df0a8` proves bounded `HitFall && !GetHitVar(fall.recover) && !CanRecover` routing through `5000 -> 5030 -> 5050 -> 5230 -> 0`. Previous required HitFall-false cut `synthetic-imported-hitfall-false` checksum `1d538e43` proves bounded normal get-hit `!HitFall` / `!GetHitVar(fall)` trigger routing through `5000 -> 325`. Previous required HitFall/CanRecover cut `synthetic-imported-hitfall-canrecover` checksum `7cf7ab46` proves bounded `5000 -> 5030 -> 5050 -> 5220 -> 0` fall-trigger routing. Previous required air guard landing cut `synthetic-imported-air-guard-landing` checksum `d6986d7f` still proves bounded Common1-style air guard landing handoff `154 -> 155 -> 52`. Previous optional KFM crouch guard slide-stop cut `kfm-official-default-crouch-guard-slide-stop` checksum `d11153d0` proves real KFM/Common1 crouch guard-hit state `153` slide-stop/control order. The previous required `synthetic-imported-crouch-guard-slide-stop` checksum `2bea7311` still proves bounded Common1-style crouch guard-hit slide-stop/control route `152 -> 153 -> 130`. Current trace aggregate is 280/280 artifacts, 255 required and 25 optional.
   - Current crouch official fixture cut: optional `kfm-official-default-crouch-gethit-progression` checksum `3d197fae` proves real KFM/Common1 held-crouch prep `11` and `5010 -> 5011 -> 0` through `HitShakeOver` / `HitOver`, including KFM-specific frame evidence for `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width `39/39`, ordered `HitVelSet` / `VelMul` / `VelSet` / `DefenceMulSet` typed-operation evidence, and final `0`/control.
   - Current air-entry official fixture cut: optional `kfm-official-default-air-fall-recovery-input` checksum `3bce8aba` and `kfm-official-default-air-fall-recovery-too-early` checksum `b199382a` prove real KFM/Common1 airborne recovery routes through `5020 -> 5030 -> 5035 -> 5050`, including valid `5210 -> 52 -> 0` recovery after `5030` countdown reaches `0` and too-early rejection in `5050` with recovery/landing/ground-impact states forbidden.
   - Current official negative cut: optional `kfm-official-default-fall-recovery-too-early` checksum `d2edbde4` proves real KFM/Common1 also rejects early `command = "recovery"` in this bounded window, keeps the defender in `5050` with minimum observed `recoverTime >= 1` while the countdown drops, and forbids `5210`, `5200`, and `5201`.
   - Next: exact guard-rule parity items beyond bounded synthetic and optional-official auto guard-start/end controller order and official guard-hit frame telemetry, exact recovery threshold tables/velocity math beyond the current synthetic threshold, actor-frame tick-order, air/ground velocity, official ordered-threshold, and official too-early reject windows, broader recovery parity beyond the bounded air/ground routes, and exact controller/VM tick-order parity cuts.

5. **Guard-rule improvement**
   - Owner: Combat/runtime.
   - Build: extend current held-back/down-back guard result, bounded `InGuardDist`, bounded automatic `120 -> 130 -> 140 -> 0` guard-start/end, bounded stand/crouch/air guard-hit routing, parsed guard slide/control timing, bounded hit/guard sound plus hit/guard spark telemetry, bounded player/system-namespace spark renderer, runtime package-frame handoff, and first-pass `fight.def`/FightFX AIR/SFF provider registration into exact proximity/landing behavior, effect binding/layering/timing/scale/palette, and broader guard parity.
   - Current cut: `AssertSpecial` guard/control-affecting flags now participate in bounded runtime evidence: static flags lower into typed `assertspecial` operation evidence, and `synthetic-imported-assertspecial-control.json` checksum `144ac7bc` requires that op while proving `NoAutoTurn`, `NoWalk`, and `Invisible` telemetry; defender-side `NoStandGuard`/`NoCrouchGuard`/`NoAirGuard` can deny guard by state type and now require flag-before-HitDef order evidence in `synthetic-imported-assertspecial-guarddeny.json` checksum `fd2eb239`, `synthetic-imported-assertspecial-crouch-guarddeny.json` checksum `757cb87a`, and `synthetic-imported-assertspecial-air-guarddeny.json` checksum `810a1679`; attacker-side `Unguardable` forces hit evidence in the partial resolver; defender-side `NoKO` clamps lethal direct damage to life `1` with `synthetic-imported-assertspecial-noko.json` checksum `f2f60521` requiring P2 passive `AssertSpecial` before P1 lethal `HitDef`, and clamps lethal target-controller damage with `synthetic-imported-target-noko.json` checksum `321a1eba` requiring the same defender flag before P1 `HitDef` and later `TargetLifeAdd`; `NoAutoTurn` can hold facing through a pre-facing imported `AssertSpecial` pass, `NoWalk` suppresses directional walking without collapsing a controlled imported state into walk/idle, `Invisible` exposes render-opacity evidence, `DB`/`UB` now count as held-back for bounded guard detection, airborne back/forward input no longer collapses the defender into standing walk state, `guard.kill = 0` now clamps lethal chip damage to life `1`, one-frame `Time < 1` `NoStandGuard` expiry allows a later held-back guard route, and `synthetic-imported-assertspecial-control`, `synthetic-imported-assertspecial-guarddeny`, `synthetic-imported-assertspecial-crouch-guarddeny`, `synthetic-imported-assertspecial-air-guarddeny`, `synthetic-imported-assertspecial-lifetime`, `synthetic-imported-assertspecial-unguardable`, `synthetic-imported-assertspecial-noko`, `synthetic-imported-target-noko`, `synthetic-imported-diagonal-crouch-guard-state`, `synthetic-imported-air-guard-state`, and `synthetic-imported-hitdef-guard-kill` are required trace gates. Exact global/team/helper AssertSpecial timing, broader persistence layering, priority, pause interaction, target/no-KO round semantics, and KFM/Common1 confirmation remain future work.
   - Done: trace proves guard event, guard damage/stun/push, state/anim route where supported, simple `guard.slidetime` / `guard.ctrltime` evidence when present, synthetic auto guard-start/end controller-event plus actor-frame order evidence, named controller/typed-operation order plus synthetic actor-frame state/physics/body/push telemetry for bounded stand `150 -> 151`, crouch/atomic-`DB` `152 -> 153`, and air `154 -> 155` guard-hit routes, optional KFM/Common1 controller/operation order for auto guard-start/end plus stand/crouch/air guard-hit routes, optional KFM stand and crouch slide-stop/control order, optional KFM actor-frame state/physics/body/air-velocity/landing telemetry for guard-hit routes, attacker-side `Unguardable` hit-through-guard evidence, bounded `InGuardDist` near trigger plus far-reject route, bounded air guard-hit route `154 -> 155` with KFM `VarSet`/`sysvar(0)` landing/control evidence, and explicit unsupported gaps.

6. **Evidence Browser persistence**
   - Owner: Studio / QA.
   - Build: save recent trace artifacts and evidence records to browser-local project state, then compare persisted trace artifacts with the current exported trace.
   - Done: reopening Studio shows prior artifacts with checksums, stale/missing-source warnings, and a first same/different/missing-current comparison row.

7. **Character Studio preview**
   - Owner: Studio / compatibility.
   - Build: absorb the transitional Inspector into Studio selection flow.
   - Done: selected character shows DEF files, AIR timeline, sprite source, axis, Clsn1/Clsn2, CMD routes, CNS states, and related trace/report evidence.

8. **Stage Studio preview**
   - Owner: Studio / renderer.
   - Build: stage report, bounds, floor, zoffset, starts, camera, BG layers, parallax/tiling, unsupported features.
   - Done: imported and native stages can be inspected visually and through report records; imported BG layers now have per-layer rendered/animated/fallback/missing/unsupported evidence, bounded/unsupported BGCtrl rows, and Studio has a dedicated `Stage` surface for selected stage facts, available stage switching, BG layer status, BG controller diagnostics, file coverage, layer fallbacks, and unsupported stage features.

9. **Generated asset authoring records**
   - Owner: Assets / Studio.
   - Build: prompt/source records, atlas QA records, contact sheet/GIF paths, collision/action authoring placeholders.
   - Done: generated fighter validity is traceable from source prompt to runtime manifest and QA report.

10. **Studio visual planning pass**
    - Owner: Product / Studio.
    - Build: generate or sketch three distinct Creator Studio layout directions before a large UI rewrite; selected direction must preserve central playtest/preview, dense evidence tables, warnings, and contextual inspector.
    - Done: chosen direction is implemented only after binding to real project/runtime/evidence data and passing browser visual QA.

11. **IKEMEN profile scanner**
   - Owner: Compatibility.
   - Current cut: scanner/reporting detects ZSS files/references/fallback `.cns.zss`, ZSS `[Statedef ...]` / `[State ...]` code blocks, ZSS syntax blocks and controller calls including text lifecycle `ModifyText`, Lua files/hooks including `hook.*`, IKEMEN config JSON, screenpack/select/menu/movelist signals, `IkemenVersion`, selected IKEMEN-only controllers, `RedirectID`, fightfx `F` action prefixes, selected extended triggers not covered by the current bounded runtime subset, selected `AssertSpecial` flags, model-stage assets, and named 3D/Z stage params. Results flow into `CompatibilityReport.profiles.ikemen`, unsupported features, exported compatibility JSON, DebugPanel, and Studio Evidence. `PrevMoveType`, `PrevAnim`, `PrevStateType`, `SelfCommand`, `StageTime`, `ReceivedDamage`, and `ReceivedHits` are now omitted from unsupported scanner findings because they have bounded runtime evidence.
   - Done: reports separate MUGEN 1.0, MUGEN 1.1, and IKEMEN-only scanner claims without attempting IKEMEN execution.
   - Next: add more package-level fixtures from real screenpacks/stages, keep mapping the official source trigger/controller tables, and keep ZSS/Lua execution blocked.

12. **App and runtime extraction plan**
    - Owner: Architecture.
    - Build: extract `App.ts` Studio surface renderers and `PlayableMatchRuntime.ts` systems without broad rewrites.
    - Done: each extraction has no behavior drift, or drift is captured by trace/smoke evidence.

13. **Shared module contract draft**
    - Owner: Architecture / Studio.
    - Build: define input, asset, snapshot, render, audio, debug, build, and QA contracts after fighting seams are proven.
    - Done: contract draft explicitly forbids CNS, HitDef, round, helper, and MUGEN command routing in shared core.

## Definition Of Done By Task Type

| Task Type | Required Closeout |
| --- | --- |
| Parser/compiler | Focused tests plus `pnpm test`; docs update if support level changes. |
| Runtime/controller/combat/tick order | Focused tests, `pnpm qa:trace`, final artifact inspection, support-level docs. |
| Type/API/build package | `pnpm typecheck`, `pnpm build`, package/schema docs. |
| UI/renderer/stage/sprite/debug panel | Browser visual QA, preferably `pnpm qa:smoke`, plus diagnostics/screenshots. |
| Studio data surface | QA bridge fields, evidence links, local save/reopen behavior when persistent. |
| Generated assets | Source prompt/provenance, atlas QA, contact sheets/GIFs, browser screenshot, trace where playable. |

Every closeout should add the short claim pair:

```txt
Claim allowed: ...
Claim blocked: ...
```

For compatibility work, the claim pair must name the fixture or trace artifact. For UI/Studio work, it must name the browser evidence or QA bridge field. For generated assets, it must name the source/atlas/QA/trace provenance.

## Parallel Work Policy

Allowed in parallel:

- Asset Library, Evidence Browser, Build Center, and Debug Studio improvements that read existing runtime/project data.
- Generated asset experiments that remain labeled generated/native and do not count as imported MUGEN compatibility.
- Stage Studio preview/reporting before BGCtrl editing.
- IKEMEN scanning/reporting before IKEMEN execution.

Blocked until gates pass:

- Executing ZSS/Lua.
- Rollback/netplay.
- Full screenpack/motif parity.
- Production platformer/beat-em-up module work.
- Advanced Studio editing that cannot save, compile, playtest, and report evidence.

## Fixture Matrix

| Fixture | Role | Required Next Evidence |
| --- | --- | --- |
| Native generated roster | Always-playable baseline. | Runtime smoke, atlas QA badges, hit/whiff trace. |
| Official KFM | Main compatibility gate. | Required when fixture exists: normal, special, guard, hitstun, state exit, Common1 get-hit/fall. |
| KFM720 | Localcoord/scale stress. | Inspector/runtime scale sanity and compatibility report. |
| CodeFuMan | SFF v1/PCX stress. | Imported sprite decode plus one routed attack. |
| SF3 Ryu demo | Parser/report stress. | Parse/report without crash; runtime playability not required yet. |
| Imported stage | Presentation gate. | Stage report plus visual rendered/fallback/unsupported layer evidence. |

## Current Non-Goals

- Full MUGEN/IKEMEN parity claim.
- IKEMEN execution.
- Generic engine SDK implementation.
- New game genre runtime.
- Sprite fixes by cropping bad generated motion.
- Green status badges without linked evidence.
