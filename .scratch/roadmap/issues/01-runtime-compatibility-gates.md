# 01 - Runtime Compatibility Gates

Status: ready-for-agent
Labels: runtime-trace, mugen-compat, ready-for-agent

## Objective

Keep converting partial CNS/CMD/runtime behavior into typed operations, named runtime systems, deterministic trace artifacts, and honest compatibility docs.

## Next Useful Cuts

- Current queue labels in `docs/ROADMAP_EXECUTION_BOARD.md`: R1 KFM/Common1 get-hit/recovery precision, R1 `AssertSpecial` precision, R2 MatchWorld ownership deepening.
- Latest focused R2 runtime ownership checkpoint: `RuntimeSpriteEffectControllerWorld` now owns active-state `RemapPal` sprite-effect dispatch. `StateProgramExecutor` classifies `RemapPal` as side-effect `remappal`, `RuntimeActiveSideEffectDispatchWorld` routes it through `spriteEffect`, and `RuntimeSpriteEffectWorld.applyRemapPal` applies bounded `paletteRemap` mutation through typed `sprite-effect:remappal` operations or raw fallback params. `StateControllerExecutor` keeps dynamic raw fallback through a resolver. Focused `SpriteEffectSystem`, `RuntimeActiveSideEffectDispatchSystem`, `StateProgramExecutor`, and `RuntimeCnsSubset` coverage proves classification, route, telemetry, mutation, and fallback preservation. Official source checked: Elecbyte 1.1 State Controller Reference for `RemapPal source/dest`. Claim allowed: current active-state `RemapPal` dispatch has a named, testable sprite-effect boundary. Claim blocked: exact source-bank/default/removal semantics, truecolor/PNG remap, helper/redirect ownership, exact PalFX order, renderer parity, score movement, and full MUGEN/IKEMEN palette parity.
- Latest focused R1 runtime checkpoint: required `synthetic-imported-helper-projtime-same-id-last-contact.json` checksum `4e74aec3` proves bounded helper-local same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` guard-then-hit last-contact-kind arbitration. A visual Helper spawns two helper-parented owner-side Projectiles with id `8918` (`ownerId/rootId = p1`, `parentId = p1-helper-0`): the first is guarded, the second hits later, and helper route `1200 -> 1306 -> 1307` requires `ProjHitTime(8918) >= 1`, `ProjHitTime(0) >= 1`, `ProjContactTime(8918) >= 1`, `ProjContactTime(0) >= 1`, `ProjGuardedTime(8918) < 0`, and `ProjGuardedTime(0) < 0`. Forbidden helper state `1308` proves stale helper-local same-id guarded time does not remain readable after the later same-id hit. Evidence includes two Projectile controller/op executions, helper/projectile lifecycle rows, root owner target-link id `8918`, guard package `S6,34` / `F7038` / `sparkxy = 35,-77`, and hit package `S5,35` / `F7038` / `sparkxy = 36,-78`. `pnpm qa:trace` passes 439/439 artifacts, 409 required and 30 optional. Official source checked: Elecbyte Trigger Reference defines `ProjContactTime`, `ProjGuardedTime`, and `ProjHitTime` as last-projectile-contact reads with required nonnegative ID and ID `0` skipping ID filtering; Elecbyte Projectile docs say helper-created projectiles become root-owned. Claim allowed: bounded helper-local owner-side same-ID guard-then-hit `Proj*Time` last-contact arbitration; together with `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b`, both helper-local same-ID two-contact orders are now required. Claim blocked: exact Proj*Time tick order/lifetime, helper custom-state breadth beyond these routes, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity. Previous player same-ID order pair, multi-id Proj*Time, multi-id ProjHit/ProjGuarded, multi-id ProjContact, any-id ProjContact, any-id ProjHit/ProjGuarded, fixed-id ProjHit/ProjGuarded, player/helper Projectile `ProjContact` state-transition, Projectile/helper `HitCount`, Projectile `GetHitVar`, guard slide-stop/control, default guard timing/velocity/cornerpush, and down/air cornerpush gates remain required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projtime-same-id-hit-then-guard.json` checksum `f4c1da3b` proves bounded helper-local same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` hit-then-guard last-contact-kind arbitration. A visual Helper spawns two helper-parented owner-side Projectiles with id `8917` (`ownerId/rootId = p1`, `parentId = p1-helper-0`): the first hits, the second is guarded later, and helper route `1200 -> 1303 -> 1304` requires `ProjGuardedTime(8917) >= 1`, `ProjGuardedTime(0) >= 1`, `ProjContactTime(8917) >= 1`, `ProjContactTime(0) >= 1`, `ProjHitTime(8917) < 0`, and `ProjHitTime(0) < 0`. Forbidden helper state `1305` proves stale helper-local same-id hit time does not remain readable after the later same-id guard. Evidence includes two Projectile controller/op executions, helper/projectile lifecycle rows, root owner target-link id `8917`, hit package `S5,32` / `F7037` / `sparkxy = 33,-75`, and guard package `S6,33` / `F7037` / `sparkxy = 34,-76`. That checkpoint passed 438/438 artifacts, 408 required and 30 optional. Claim allowed: bounded helper-local owner-side same-ID hit-then-guard `Proj*Time` last-contact arbitration. Claim blocked: exact Proj*Time tick order/lifetime, helper custom-state breadth beyond this route, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projtime-same-id-hit-then-guard.json` checksum `d49ee334` proves bounded player-owned same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` hit-then-guard last-contact-kind arbitration. Two Projectiles share id `8916`: the first hits, the second is guarded later, and owner state `200 -> 383 -> 384` requires `ProjGuardedTime(8916) >= 1`, `ProjGuardedTime(0) >= 1`, `ProjContactTime(8916) >= 1`, `ProjContactTime(0) >= 1`, `ProjHitTime(8916) < 0`, and `ProjHitTime(0) < 0`. Forbidden trap state `385` proves stale same-id hit time does not remain readable after the later same-id guard. That checkpoint passed 437/437 artifacts, 407 required and 30 optional. Claim allowed: bounded hit-then-guard plus prior guard-then-hit same-id player Projectile `Proj*Time` last-contact arbitration. Claim blocked: exact Proj*Time tick order/lifetime, helper custom-state breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projtime-same-id-last-contact.json` checksum `fb4c2450` proves bounded player-owned same-ID `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` guard-then-hit last-contact-kind arbitration. Two player Projectiles share id `8915`: the first is guarded, the second hits later, and owner state `200 -> 380 -> 381` requires `ProjHitTime(8915) >= 1`, `ProjHitTime(0) >= 1`, `ProjContactTime(8915) >= 1`, `ProjContactTime(0) >= 1`, `ProjGuardedTime(8915) < 0`, and `ProjGuardedTime(0) < 0`. Forbidden trap state `382` proves stale same-id guarded time does not remain readable after the later same-id hit. Evidence includes two Projectile controller/op executions, two Projectile payloads/lifecycle rows, target-link id `8915`, guard package `S6,28` / `F7035` / `sparkxy = 29,-71`, and hit package `S5,29` / `F7035` / `sparkxy = 30,-72`. That checkpoint passed 436/436 artifacts, 406 required and 30 optional. Official source checked: Elecbyte Trigger Reference defines `ProjContactTime`, `ProjGuardedTime`, and `ProjHitTime` as last-projectile-contact reads with required nonnegative ID and ID `0` skipping ID filtering; Elecbyte CNS docs say legacy `ProjHit`, `ProjContact`, and `ProjGuarded` are superseded by `Proj*Time`. Claim allowed: bounded guard-then-hit same-id player Projectile `Proj*Time` last-contact arbitration. Claim blocked: exact Proj*Time tick order/lifetime, helper Projectile/custom-state persistence breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, broader same-id/multi-target arbitration, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity. Previous multi-id Proj*Time, multi-id ProjHit/ProjGuarded, multi-id ProjContact, any-id ProjContact, any-id ProjHit/ProjGuarded, fixed-id ProjHit/ProjGuarded, player Projectile `ProjContact` fixed-id suffix/state-transition, helper Projectile `ProjContact` state-transition, helper reversed/guarded/hit-route `movehitpersist`, helper `hitcountpersist`, helper `hitdefpersist`, direct `hitdefpersist`, direct `movehitpersist`, direct `hitcountpersist`, Projectile/helper `HitCount`, Projectile `GetHitVar(hitcount)`, `hitid/chainid`, `damage/hittime/xvel/yvel`, guard slide-stop/control, default guard timing/velocity/cornerpush, and down/air cornerpush gates remain required.
- Latest focused R1 runtime checkpoint: required `synthetic-imported-projectile-projhittime-multi-id.json` checksum `5d897825`, `synthetic-imported-projectile-projcontacttime-multi-id.json` checksum `d9b3cecf`, and `synthetic-imported-projectile-projguardedtime-multi-id.json` checksum `e52d0d01` prove bounded player-owned Projectile `ProjHitTime` / `ProjContactTime` / `ProjGuardedTime` multi-id arbitration. P1 spawns wrong-id/non-contact Projectile ids `8909` / `8911` / `8913` and valid hit/contact/guard Projectile ids `8910` / `8912` / `8914`; owner state `200` routes `200 -> 371 -> 372`, `200 -> 374 -> 375`, and `200 -> 377 -> 378` only through fixed-id `ProjHitTime(8910) >= 1` / `ProjContactTime(8912) >= 1` / `ProjGuardedTime(8914) >= 1` plus ID `0` any-projectile reads, while forbidden states `373` / `376` / `379` prove wrong-id time reads do not route. Evidence includes two Projectile controller/op executions per route, two Projectile payloads, owner target-link ids `8910` / `8912` / `8914`, hit sound `S5,25`, guard sounds `S6,26` / `S6,27`, FightFX `F7032` / `F7033` / `F7034`, and authored `sparkxy = 26,-68` / `27,-69` / `28,-70`. `pnpm qa:trace` passes 435/435 artifacts, 405 required and 30 optional. Official source checked: Elecbyte Trigger Reference defines `ProjHitTime`, `ProjContactTime`, and `ProjGuardedTime` as required-ID trigger reads where ID `0` skips projectile-ID filtering and no matching last contact returns `-1`; Elecbyte CNS docs say old-style `ProjHit`, `ProjContact`, and `ProjGuarded` are superseded by `ProjHitTime`, `ProjContactTime`, and `ProjGuardedTime`. Claim allowed: bounded player-owned Projectile fixed-id plus ID `0` any-id `Proj*Time` arbitration across two Projectiles. Claim blocked: exact Proj*Time tick order/lifetime, same-ID selection priority, helper Projectile/custom-state persistence breadth, Move* interaction breadth, redirects, teams, helper-owned custom-state targets, visual/audio parity beyond bounded packages, score movement, and full MUGEN/IKEMEN Projectile parity. Previous multi-id ProjHit/ProjGuarded, multi-id ProjContact, any-id ProjContact, any-id ProjHit/ProjGuarded, fixed-id ProjHit/ProjGuarded, player Projectile `ProjContact` fixed-id suffix/state-transition, helper Projectile `ProjContact` state-transition, helper reversed/guarded/hit-route `movehitpersist`, helper `hitcountpersist`, helper `hitdefpersist`, direct `hitdefpersist`, direct `movehitpersist`, direct `hitcountpersist`, Projectile/helper `HitCount`, Projectile `GetHitVar(hitcount)`, `hitid/chainid`, `damage/hittime/xvel/yvel`, guard slide-stop/control, default `guard.hittime` / `guard.slidetime` / `guard.ctrltime`, default `guard.velocity`, default/explicit `guard.cornerpush.veloff`, and default/explicit `down.cornerpush.veloff` gates remain required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projhit-suffix.json` checksum `dd3db5ee` and `synthetic-imported-projectile-projguarded-suffix.json` checksum `80bbe439` prove bounded fixed-id player-owned Projectile legacy `ProjHit[ID]` / `ProjGuarded[ID]` syntax. That checkpoint passed 426/426 artifacts, 396 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projcontact-suffix.json` checksum `c904ded7` proves bounded player-owned Projectile legacy `ProjContact[ID] = value, [oper] value2` syntax. That checkpoint passed 424/424 artifacts, 394 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-projcontactpersist.json` checksum `8e678b1b` proves bounded player-owned Projectile `ProjContact` state-transition support. That checkpoint passed 423/423 artifacts, 393 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projcontactpersist.json` checksum `65639428` proves bounded helper Projectile `ProjContact` state-transition support. A visual Helper spawns helper-parented owner-side Projectile id `8895`, P2 guards it, helper state `1300` observes `ProjContact(8895)` / `ProjContactTime(8895)`, transitions into helper state `1302`, and branches to helper state `1301` from that later StateDef with helper/projectile lifecycle and owner target-link evidence. That checkpoint passed 422/422 artifacts, 392 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-movereversedpersist.json` checksum `ef8ffdf5` proves bounded helper-owned reversed `StateDef movehitpersist` support. That checkpoint passed 421/421 artifacts, 391 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-moveguardedpersist.json` checksum `d5ce7897` proves bounded helper-owned guarded `StateDef movehitpersist` support. That checkpoint passed 420/420 artifacts, 390 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-movehitpersist.json` checksum `2354ef95` proves bounded helper-owned hit `StateDef movehitpersist` support. That checkpoint passed 419/419 artifacts, 389 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-hitcountpersist.json` checksum `fc9588d8` proves bounded helper-owned `StateDef hitcountpersist` support. That checkpoint passed 418/418 artifacts, 388 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-hitdefpersist.json` checksum `9d5c64c4` proves bounded helper-owned `StateDef hitdefpersist` support. That checkpoint passed 417/417 artifacts, 387 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-movehitpersist.json` checksum `5c1ef583` proves bounded direct `StateDef movehitpersist` support. That checkpoint passed 415/415 artifacts, 385 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitcountpersist.json` checksum `6f032088` proves bounded direct `StateDef hitcountpersist` support. That checkpoint passed 414/414 artifacts, 384 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitcount.json` checksum `97a1b671` and `synthetic-imported-helper-projectile-hitcount.json` checksum `19d1c22c` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit attacker-side `HitCount` / `UniqHitCount` contact memory. That checkpoint passed 413/413 artifacts, 383 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-gethitvar-hitcount.json` trace checksum `fa445b05` / final checksum `0c2197f7` and `synthetic-imported-helper-projectile-gethitvar-hitcount.json` trace checksum `ded0c9b3` / final checksum `bba71972` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit `GetHitVar(hitcount)` metadata from HitDef `numhits`. That checkpoint passed 411/411 artifacts, 381 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-gethitvar-hitid-chainid.json` trace checksum `80392a85` / final checksum `514a6803` and `synthetic-imported-helper-projectile-gethitvar-hitid-chainid.json` trace checksum `24df4416` / final checksum `f84c36ad` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit `GetHitVar(hitid/chainid)` metadata. That checkpoint passed 409/409 artifacts, 379 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-gethitvar-hit-metadata.json` trace checksum `6b7ad6e5` / final checksum `36365083` and `synthetic-imported-helper-projectile-gethitvar-hit-metadata.json` trace checksum `4a65158a` / final checksum `dfbc9fe9` prove bounded player-owned and helper-parented/root-owned Projectile normal-hit `GetHitVar(damage/hittime/xvel/yvel)` metadata. That checkpoint passed 407/407 artifacts, 377 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-guard-slide-stop.json` trace checksum `965c2d12` / final checksum `0973a73c` and `synthetic-imported-helper-projectile-guard-slide-stop.json` trace checksum `6c42a378` / final checksum `df8b7a42` gate Projectile/helper guard slide-stop/control. That checkpoint passed 405/405 artifacts, 375 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-guard-timing-default.json` trace checksum `859cb873` / final checksum `bae55cbc`, `synthetic-imported-projectile-guard-timing-default.json` trace checksum `21dc44c4` / final checksum `1c3d9c20`, and `synthetic-imported-helper-projectile-guard-timing-default.json` trace checksum `d421498c` / final checksum `1733494a` gate official default `guard.hittime`, `guard.slidetime`, and `guard.ctrltime` derivation. That checkpoint passed 403/403 artifacts, 373 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-guard-velocity-default.json` checksum `e6bd9b40`, `synthetic-imported-projectile-guard-velocity-default.json` checksum `b72451a4`, and `synthetic-imported-helper-projectile-guard-velocity-default.json` checksum `2067ba99` gate official default `guard.velocity` derivation. That checkpoint passed 400/400 artifacts, 370 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-guard-cornerpush-default.json` checksum `95293bc4`, `synthetic-imported-projectile-guard-cornerpush-default.json` checksum `58798e7a`, and `synthetic-imported-helper-projectile-guard-cornerpush-default.json` checksum `292b2015` gate bounded default `guard.cornerpush.veloff` derivation. That checkpoint passed 397/397 artifacts, 367 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-hit-cornerpush-default.json` checksum `73129a04`, `synthetic-imported-projectile-air-hit-cornerpush-default.json` checksum `9bfae4d6`, and `synthetic-imported-helper-projectile-air-hit-cornerpush-default.json` checksum `9c81047d` gate bounded default `air.cornerpush.veloff` derivation for direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` air-hit routes. All three routes omit corner-push params, hit an airborne defender at the stage edge, derive air-hit corner push through `air.cornerpush.veloff` -> `ground.cornerpush.veloff` plus the existing guard/ground velocity fallback path, keep the defender in Common1-style airborne hit state `5020`, and require attacker/owner X velocity evidence from derived corner pushback; helper Projectile also records owner/helper target links plus helper/projectile lifecycle payload evidence. That checkpoint passed 388/388 artifacts, 358 required and 30 optional, and remains required. Official source checked: Elecbyte `HitDef` docs define corner-push params/defaults, Elecbyte `Projectile` docs say `Projectile` takes `HitDef` parameters, and helper-created Projectiles are root-owned. Claim allowed: bounded default direct/player/helper Projectile air-hit corner-push derivation with target/lifecycle proof. Claim blocked: exact corner-push timing/decay, wall friction, exact air get-hit physics/timing, exact hit effects, throws, teams/simul, score movement, and full Common1/projectile parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-guard-cornerpush-default.json` checksum `c32781ad`, `synthetic-imported-projectile-air-guard-cornerpush-default.json` checksum `90f5e385`, and `synthetic-imported-helper-projectile-air-guard-cornerpush-default.json` checksum `0271a2b9` gate bounded default `airguard.cornerpush.veloff` support for direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` guard routes. That checkpoint passed 385/385 artifacts, 355 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-guard-cornerpush.json` checksum `9fdb8a81`, `synthetic-imported-projectile-air-guard-cornerpush.json` checksum `15f26082`, and `synthetic-imported-helper-projectile-air-guard-cornerpush.json` checksum `35d7148b` gate bounded explicit `airguard.cornerpush.veloff` support for direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` guard routes. That checkpoint passed 382/382 artifacts, 352 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-guard-velocity-default.json` checksum `b1710269`, `synthetic-imported-projectile-air-guard-velocity-default.json` checksum `bd1a774e`, and `synthetic-imported-helper-projectile-air-guard-velocity-default.json` checksum `3351e770` gate bounded official default `airguard.velocity` derivation for direct `HitDef`, player-owned `Projectile`, and helper-parented `Projectile` guard routes. That checkpoint passed 379/379 artifacts, 349 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-air-guard-velocity.json` checksum `5ebc1e7b`, `synthetic-imported-projectile-air-guard-velocity.json` checksum `0094c369`, and `synthetic-imported-helper-projectile-air-guard-velocity.json` checksum `b547dfb3` gate bounded explicit `airguard.velocity = 8,-4` support. That checkpoint passed 376/376 artifacts, 346 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `35fa8224`, `synthetic-imported-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `1fd6c321`, and `synthetic-imported-helper-projectile-hitoverride-guardflag-forceair-forceguard-keepstate.json` checksum `7efa40bb` gate bounded direct/player/helper `HitOverride guardflag` filtering before selected-slot `forceair` / `forceguard` / `keepstate` application. That checkpoint passed 373/373 artifacts, 343 required and 30 optional, and remains required.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-default-guardflag-filter.json` checksum `05725ecb`, `synthetic-imported-projectile-hitoverride-missonoverride-default-guardflag-filter.json` checksum `c1402d31`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-guardflag-filter.json` checksum `889d77c1` gate bounded direct/player/helper default `missonoverride = -1` custom-state `HitOverride guardflag` / `guardflag.not` filtering. Direct `HitDef` with `p2stateno = 888`, omitted `missonoverride`, and `guardflag = H` rejects before target memory, override state entry, owner-backed custom-state `888`, default get-hit, or guard states; P2 remains idle/control with life `1000`. Player-owned Projectile id `77` and helper-parented Projectile id `8882` omit `missonoverride`, use `p2stateno = 889` / `p2getp1state = 1` / `guardflag = H`, skip slots `1 -> 776` with `guardflag.not = HA` and `2 -> 778` with `guardflag = A`, select slot `5 -> 779`, suppress projectile custom-state `889`, and end P2 in state/action `779`, life `1000`, moveType `I`. The helper route also records target links `p1 -> p2 / 8882` and `p1-helper-0 -> p2 / 8882`, helper payload `targetCount = 1`, projectile payload `hasHit = true` / `hitsRemaining = 0`, and suppresses helper branch `1293`. That `pnpm qa:trace` pass was 370/370 artifacts, 340 required and 30 optional. Official source checked: Elecbyte `HitDef`, `Projectile`, and `HitOverride` docs define p2 custom-state params, Projectile HitDef-param inheritance/root ownership, and override slot semantics; local Ikemen-GO source parses `missonoverride`, `guardflag`, and `guardflag.not`, scans slots low-to-high, applies guardflag filters before selection, and uses default custom-state miss only for non-projectile contacts when `missonoverride = -1`. Claim allowed: bounded direct default custom-state reject plus player/helper Projectile default custom-state guardflag filtering with owner/helper target memory and lifecycle-payload evidence. Claim blocked: exact guard timing/guarded contact semantics, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `058b335f`, `synthetic-imported-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `af29f125`, and `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-guardflag-filter.json` checksum `9edbf3d0` gate bounded direct/player/helper explicit `missonoverride = 0` custom-state `HitOverride guardflag` / `guardflag.not` filtering. Direct `HitDef`, player-owned Projectile id `77`, and helper-parented Projectile id `8881` all use `guardflag = H`; P2 installs slots `1 -> 776` with `guardflag.not = HA`, `2 -> 778` with `guardflag = A`, and `5 -> 779` with `guardflag = H`; all three routes skip slots `1/2`, select slot `5`, suppress custom state entry, and end P2 in state/action `779`, life `1000`, moveType `I`. The helper route also records target links `p1 -> p2 / 8881` and `p1-helper-0 -> p2 / 8881`, helper payload `targetCount = 1`, projectile payload `hasHit = true` / `hitsRemaining = 0`, and suppresses helper branch `1291`. That `pnpm qa:trace` pass was 367/367 artifacts, 337 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; local Ikemen-GO source parses guardflag filters and scans `HitOverride` slots low-to-high. Claim allowed: bounded direct/player/helper explicit `missonoverride = 0` custom-state guardflag filtering with owner/helper target memory and lifecycle-payload evidence. Claim blocked: exact guard timing/guarded contact semantics, forceair/forceguard priority combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-slot-priority.json` checksum `9a5a149f` plus `synthetic-imported-projectile-hitoverride-missonoverride-zero-slot-priority.json` checksum `96b6b7de` gate bounded Projectile explicit `missonoverride = 0` custom-state `HitOverride` slot priority. Player-owned Projectile id `77` and helper-parented Projectile id `8878` both declare `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`; P2 installs matching slots `5 -> 779` and `2 -> 778`; both routes select slot `2`, consume/remove the projectile, emit override telemetry, suppress projectile custom state `889`, and end P2 in state/action `778`, life `1000`, moveType `I`. The helper route also records target links `p1 -> p2 / 8878` and `p1-helper-0 -> p2 / 8878`, helper payload `targetCount = 1`, projectile payload `hasHit = true` / `hitsRemaining = 0`, and suppresses helper `ProjHit` branch `1288`. `pnpm qa:trace` passed 364/364 artifacts, 334 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; local Ikemen-GO source scans `HitOverride` slots low-to-high and does not force projectile custom-state HitOverride miss when `missonoverride = 0`. Claim allowed: bounded player-owned/helper-parented Projectile explicit `missonoverride = 0` custom-state lowest-slot priority with owner/helper target memory and lifecycle-payload evidence. Claim blocked: custom-state guardflag breadth beyond the latest explicit route, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full Projectile HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `fb964bfb` gates bounded helper-parented Projectile default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` behavior. A visual Helper spawns owner-side Projectile id `8877` with `parentId = p1-helper-0`, `p2stateno = 889` / `p2getp1state = 1`, and omitted `missonoverride`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records owner target link `p1 -> p2 / 8877` plus helper target link `p1-helper-0 -> p2 / 8877`, consumes/removes the projectile, selects slot `3`, emits override telemetry, records helper payload `targetCount = 1`, records projectile payload `hasHit = true` / `hitsRemaining = 0`, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889` and helper `ProjHit` branch `1286`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `889`, `1286`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. `pnpm qa:trace` passes 362/362 artifacts, 332 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned; Elecbyte HitOverride docs define `forceair` and custom-state miss behavior; local Ikemen-GO source applies default custom-state HitOverride miss only for non-projectile contacts when `missonoverride = -1`, then applies `forceair` / `forceguard`, `keepstate`, and custom-state suppression after slot selection. Claim allowed: bounded helper-parented Projectile default custom-state forceair/forceguard/keepstate route with owner/helper target memory, helper/projectile lifecycle payloads, aerial/guarding frame evidence, keepstate no-override-state behavior, projectile custom-state suppression, and helper `ProjHit` suppression. Claim blocked: custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full helper Projectile HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `4ce42cf3` gates bounded player-owned Projectile default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` behavior. P1 Projectile id `77` declares `p2stateno = 889` / `p2getp1state = 1` and omits `missonoverride`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile, selects slot `3`, emits override telemetry, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `889`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. That `pnpm qa:trace` pass was 361/361 artifacts, 331 required and 30 optional. Claim allowed: bounded player-owned Projectile default custom-state forceair/forceguard/keepstate route with target memory, projectile lifecycle payloads, aerial/guarding frame evidence, keepstate no-override-state behavior, and projectile custom-state suppression. Claim blocked: custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full Projectile HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-default-forceair-forceguard-keepstate.json` checksum `20e40425` gates bounded direct-HitDef default `missonoverride = -1` custom-state `HitOverride forceair` / `forceguard` / `keepstate` miss behavior. P1 declares `p2stateno = 888` / `p2getp1state = 1` and omits `missonoverride`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact rejects before target memory, damage, guard, forceair/forceguard actor frames, keepstate redirect handling, override state `780`, owner-backed custom-state `888`, default get-hit state `5000`, or guard states `150` / `151`. Final P2 remains state/action `0`, life `1000`, control true, moveType `I`, and no target links are recorded. That `pnpm qa:trace` pass was 360/360 artifacts, 330 required and 30 optional. Official source checked: Elecbyte HitDef docs define `p2stateno` / `p2getp1state`; Elecbyte HitOverride docs define `forceair` and custom-state miss behavior; local Ikemen-GO source returns miss for default `missonoverride = -1` on direct non-projectile `p1stateno` / `p2stateno` before applying `forceair` / `forceguard` / `keepstate`. Claim allowed: bounded direct default custom-state miss force-flag route, proving force flags do not apply after the default custom-state reject. Claim blocked: custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `e23a33af` gates bounded helper-parented Projectile explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` behavior. A visual Helper spawns owner-side Projectile id `8876` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records owner target link `p1 -> p2 / 8876` plus helper target link `p1-helper-0 -> p2 / 8876`, consumes/removes the projectile, selects slot `3`, emits override telemetry, records helper payload `targetCount = 1` plus projectile payload `hasHit = true` / `hitsRemaining = 0`, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889` and helper `ProjHit` branch `1284`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `889`, `1284`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. That `pnpm qa:trace` pass was 359/359 artifacts, 329 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes HitDef parameters and helper-spawned projectiles are root-owned, Elecbyte HitOverride docs define `forceair` and custom-state miss behavior, and local Ikemen-GO source applies default custom-state HitOverride miss only for non-projectile contacts when `missonoverride = -1`, then applies `forceair` / `forceguard`, `keepstate`, and custom-state suppression after slot selection. Claim allowed: bounded helper-parented Projectile explicit `missonoverride = 0` custom-state forceair/forceguard/keepstate route with owner/helper target memory, helper/projectile lifecycle payloads, aerial/guarding frame evidence, keepstate no-override-state behavior, projectile custom-state suppression, and helper `ProjHit` suppression. Claim blocked: custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full helper Projectile HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `15bc955b` gates bounded player-owned Projectile explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` behavior. P1 fires Projectile id `77` with `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile, selects slot `3`, emits override telemetry, keeps P2 in state/action `0` instead of entering state `780`, suppresses projectile custom-state `889`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `889`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. That `pnpm qa:trace` pass was 358/358 artifacts, 328 required and 30 optional. Claim allowed: bounded player-owned Projectile explicit `missonoverride = 0` custom-state forceair/forceguard/keepstate route with target memory, projectile removal evidence, aerial/guarding frame evidence, keepstate no-override-state behavior, and custom-state suppression. Claim blocked: custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full Projectile HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero-forceair-forceguard-keepstate.json` checksum `4d9043a5` gates bounded direct-HitDef explicit `missonoverride = 0` custom-state `HitOverride forceair` / `forceguard` / `keepstate` behavior. P1 declares `p2stateno = 888`, `p2getp1state = 1`, and `missonoverride = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, selects slot `3`, emits override telemetry, keeps P2 in state/action `0` instead of entering state `780`, suppresses owner-backed custom-state `888`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `888`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. That `pnpm qa:trace` pass was 357/357 artifacts, 327 required and 30 optional. Claim allowed: bounded explicit `missonoverride = 0` direct custom-state forceair/forceguard/keepstate route with target memory, aerial/guarding frame evidence, keepstate no-override-state behavior, and custom-state suppression. Claim blocked: helper/projectile custom-state force flags, custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-forceair-forceguard-keepstate.json` checksum `84dc3969` gates bounded helper-parented Projectile `HitOverride forceair` / `forceguard` / `keepstate` behavior. A visual Helper spawns owner-side Projectile id `8875` with `parentId = p1-helper-0`, `p2stateno = 889`, and `p2getp1state = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records owner target link `p1 -> p2 / 8875` plus helper target link `p1-helper-0 -> p2 / 8875`, selects slot `3`, emits override telemetry, records projectile payload evidence with `hasHit = true` / `hitsRemaining = 0`, keeps P2 in state/action `0` instead of entering state `780`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `889`, helper `ProjHit` branch `1282`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. That `pnpm qa:trace` pass was 356/356 artifacts, 326 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes all HitDef parameters, Elecbyte HitOverride docs define `forceair`, and local Ikemen-GO source parses/stores/applies `forceguard` and `keepstate` while applying the same HitOverride scan when `isProjectile` is true. Claim allowed: bounded helper-parented Projectile forceair/forceguard/keepstate route with owner/helper target memory, projectile lifecycle evidence, aerial/guarding frame evidence, and keepstate no-override-state behavior. Claim blocked: final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN helper Projectile HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-forceair-forceguard-keepstate.json` checksum `3806a769` gates bounded player-owned Projectile `HitOverride forceair` / `forceguard` / `keepstate` behavior. P1 fires Projectile id `77` with `p2stateno = 889` / `p2getp1state = 0`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, selects slot `3`, emits override telemetry, records projectile hit/removal lifecycle evidence, keeps P2 in state/action `0` instead of entering state `780`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `889`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. That `pnpm qa:trace` pass was 355/355 artifacts, 325 required and 30 optional. Claim allowed: bounded player-owned Projectile forceair/forceguard/keepstate route with target memory, projectile lifecycle evidence, aerial/guarding frame evidence, and keepstate no-override-state behavior. Claim blocked: final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-forceair-forceguard-keepstate.json` checksum `19787fb2` gates bounded direct-HitDef `HitOverride forceair` / `forceguard` / `keepstate` behavior. P1 hits with `attr = S,NA`; P2 installs active `HitOverride` slot `3 -> 780` with `time = 30`, `forceair = 1`, `forceguard = 1`, and `keepstate = 1`; contact records target link `p1 -> p2 / 77`, selects slot `3`, emits override telemetry, keeps P2 in state/action `0` instead of entering state `780`, and actor-frame evidence observes P2 as aerial/guarding with `stateType = A`, `physics = A`, and `guardingFrames >= 1`. States `780`, `5000`, `150`, and `151` are forbidden; final P2 returns to state/action `0`, life `1000`, moveType `I`. That `pnpm qa:trace` pass was 354/354 artifacts, 324 required and 30 optional. Claim allowed: bounded direct-HitDef forceair/forceguard/keepstate route with target memory, aerial/guarding frame evidence, and keepstate no-override-state behavior. Claim blocked: final-frame forced aerial persistence, exact guarded get-hit variable/chip semantics, custom-state forceair/forceguard/keepstate breadth, custom-state guardflag inheritance/timing, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-guardflag-filter.json` checksum `41a87267` gates bounded helper-parented Projectile `HitOverride guardflag` / `guardflag.not` filtering before slot priority. A visual Helper spawns owner-side Projectile id `8874` with `parentId = p1-helper-0`, `guardflag = H`, `p2stateno = 889`, and `p2getp1state = 0`; P2 installs attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`; contact records owner target link `p1 -> p2 / 8874` plus helper target link `p1-helper-0 -> p2 / 8874`, marks projectile payload `hitsRemaining = 0` / `hasHit = true`, skips slots `1` and `2`, selects slot `5`, redirects P2 through state `779`, keeps life `1000`, and forbids states `776`, `778`, `889`, helper `ProjHit` branch `1280`, `5000`, `150`, and `151`. The gate requires executed `ChangeState`, `Helper`, `Projectile`, and at least three `HitOverride` controllers, typed `helper`, `projectile`, plus at least three `hitoverride` operations, active `x`, helper/projectile lifecycle/payload evidence, owner/helper target-link evidence, override event/combat telemetry containing `HitOverride slot 5`, final P2 state `779` / life `1000`, and forbidden states `776`, `778`, `889`, `1280`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 353/353 artifacts, 323 required and 30 optional. Official source checked: Elecbyte Projectile docs define HitDef-param inheritance and helper-spawned projectile root ownership; Elecbyte tutorial docs define `guardflag`; local Ikemen-GO source stores `guardflag` / `guardflag_not`, checks them while scanning slots low-to-high, and applies them when `isProjectile` is true. Claim allowed: bounded helper-parented Projectile guardflag filters can skip lower attr-matching slots before a later compatible slot redirects while owner/helper target memory is retained. Claim blocked: custom-state guardflag inheritance/timing, custom-state forceair/forceguard/keepstate breadth, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN helper Projectile/HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-guardflag-filter.json` checksum `a51e82ec` gates bounded player-owned Projectile `HitOverride guardflag` / `guardflag.not` filtering before slot priority. P1 fires Projectile id `77` with `guardflag = H` and `p2stateno = 889`; P2 installs attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile as a hit, skips slots `1` and `2`, selects slot `5`, redirects P2 through state `779`, keeps life `1000`, and forbids states `776`, `778`, `889`, `5000`, `150`, and `151`. That gate required executed `ChangeState`, `Projectile`, and at least three `HitOverride` controllers, typed `projectile` plus at least three `hitoverride` operations, active `x`, projectile lifecycle/payload evidence, target-link evidence, override event/combat telemetry containing `HitOverride slot 5`, final P2 state `779` / life `1000`, and forbidden states `776`, `778`, `889`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 352/352 artifacts, 322 required and 30 optional. Claim allowed: bounded player-owned Projectile guardflag filters can skip lower attr-matching slots before a later compatible slot redirects. Claim blocked: custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-guardflag-filter.json` checksum `b88a2da3` gates bounded direct-HitDef `HitOverride guardflag` / `guardflag.not` filtering before slot priority. P1 hits with `attr = S,NA` and `guardflag = H`; P2 installs attr-matching `HitOverride` slot `1 -> 776` with `guardflag.not = HA`, slot `2 -> 778` with `guardflag = A`, and slot `5 -> 779` with `guardflag = H`; contact records target link `p1 -> p2 / 77`, skips slots `1` and `2`, selects slot `5`, redirects P2 through state `779`, keeps life `1000`, and forbids states `776`, `778`, `5000`, `150`, and `151`. The gate requires executed `ChangeState`, `HitDef`, and at least three `HitOverride` controllers, typed `hitdef` plus at least three `hitoverride` operations, active `x`, target-link evidence, override event/combat telemetry containing `HitOverride slot 5`, final P2 state `779` / life `1000`, and forbidden states `776`, `778`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 351/351 artifacts, 321 required and 30 optional. Official source checked: Elecbyte HitOverride docs define slot setup and redirect semantics; local Ikemen-GO source stores `guardflag` / `guardflag_not` and checks them while scanning slots low-to-high. Claim allowed: bounded direct-HitDef guardflag filters can skip lower attr-matching slots before a later compatible slot redirects. Claim blocked: custom-state guardflag inheritance/timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero-slot-priority.json` checksum `92fefd6a` gates bounded direct-HitDef `missonoverride = 0` custom-state slot priority. P1 declares `p2stateno = 888`, `p2getp1state = 1`, and explicit `missonoverride = 0`; P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; contact records target link `p1 -> p2 / 77`, selects slot `2`, redirects P2 through state `778`, keeps P2 life `1000`, and forbids state `779`, owner-backed custom state `888`, and default Common1 states `5000`, `150`, and `151`. The gate requires executed `ChangeState`, `HitDef`, and at least two `HitOverride` controllers, typed `hitdef` plus at least two `hitoverride` operations, active `x`, target-link evidence, override event/combat telemetry containing `HitOverride slot 2`, final P2 state `778` / life `1000`, and forbidden states `779`, `888`, `5000`, `150`, and `151`. `pnpm qa:trace` passed 350/350 artifacts, 320 required and 30 optional. Official source checked: Elecbyte HitDef docs define `p2stateno` / `p2getp1state` custom-state routing; Elecbyte HitOverride docs define slot setup and redirect semantics; local Ikemen-GO source stores `hover[slot]`, scans slots low-to-high, and treats explicit `missonoverride = 0` as the opt-out from the default direct custom-state HitOverride miss. Claim allowed: bounded direct-HitDef `missonoverride = 0` lowest matching HitOverride slot wins before owner-backed custom-state entry. Claim blocked: helper/projectile custom-state slot-priority breadth, helper/projectile `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-slot-priority.json` checksum `1d058518` gates bounded helper-parented Projectile HitOverride slot priority. A visual Helper spawns Projectile id `8873` with `parentId = p1-helper-0`, `p2stateno = 889`, and `p2getp1state = 0`; P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; contact records owner target link `p1 -> p2 / 8873` plus helper target link `p1-helper-0 -> p2 / 8873`, marks projectile payload `hitsRemaining = 0` / `hasHit = true`, selects slot `2`, redirects P2 through state `778`, keeps P2 life `1000`, and forbids state `779`, projectile custom state `889`, helper `ProjHit` branch `1278`, and default Common1 states `5000`, `150`, and `151`. The gate requires executed `ChangeState`, `Helper`, `Projectile`, and at least two `HitOverride` controllers, typed `helper`, `projectile`, plus at least two `hitoverride` operations, active `x`, helper/projectile lifecycle/payload evidence, owner/helper target-link evidence, override event/combat telemetry containing `HitOverride slot 2`, final P2 state `778` / life `1000`, and forbidden states `779`, `889`, `1278`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 349/349 artifacts, 319 required and 30 optional. Official source checked: Elecbyte HitOverride docs define slot setup and redirect semantics; Elecbyte Projectile docs define HitDef-param inheritance and root ownership for helper-spawned projectiles; local Ikemen-GO source stores `hover[slot]`, scans slots low-to-high, selects the first matching slot, and does not apply the default custom-state HitOverride miss to projectile contacts unless `missonoverride = 1`. Claim allowed: bounded helper-parented Projectile lowest matching HitOverride slot wins before helper `ProjHit`, projectile custom-state, or default get-hit routes while owner/helper target memory is retained. Claim blocked: custom-state slot-priority breadth, `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-slot-priority.json` checksum `378d9ce8` gates bounded player-owned Projectile HitOverride slot priority. P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; P1 fires Projectile id `77` with `p2stateno = 889`; contact records target link `p1 -> p2 / 77`, consumes/removes the projectile as a hit, selects slot `2`, redirects P2 through state `778`, keeps P2 life `1000`, and forbids state `779`, projectile custom state `889`, and default Common1 states `5000`, `150`, and `151`. The gate requires executed `ChangeState`, `Projectile`, and at least two `HitOverride` controllers, typed `projectile` plus at least two `hitoverride` operations, active `x`, projectile lifecycle/payload evidence, target-link evidence, override event/combat telemetry containing `HitOverride slot 2`, final P2 state `778` / life `1000`, and forbidden states `779`, `889`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 348/348 artifacts, 318 required and 30 optional. Claim allowed: bounded player-owned Projectile lowest matching HitOverride slot wins before projectile custom-state/default get-hit routes. Claim blocked: custom-state slot-priority breadth, `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-slot-priority.json` checksum `8de62354` gates bounded direct-HitDef HitOverride slot priority. P2 installs matching `HitOverride` slot `5 -> 779` and slot `2 -> 778` in high-to-low controller order; P1 hits with `attr = S,NA`; combat selects slot `2`, redirects P2 through state `778`, keeps P2 life `1000`, and forbids state `779` plus default Common1 states `5000`, `150`, and `151`. The gate requires executed `ChangeState`, `HitDef`, and at least two `HitOverride` controllers, typed `hitdef` plus at least two `hitoverride` operations, active `x`, override event/combat telemetry containing `HitOverride slot 2`, final P2 state `778` / life `1000`, and forbidden states `779`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 347/347 artifacts, 317 required and 30 optional. Claim allowed: bounded direct-HitDef lowest matching HitOverride slot wins. Claim blocked: helper-parented Projectile/custom-state slot-priority breadth, `guardflag` / `guardflag.not` edge timing, forceair/forceguard/keepstate combinations, exact guard KO/no-KO round flow, throws, teams/simul, score movement, and full MUGEN/IKEMEN HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-zero.json` checksum `62d7d6b8` gates bounded helper-parented Projectile `missonoverride = 0` HitOverride redirect behavior. A visual Helper spawns Projectile id `8872` with `parentId = p1-helper-0`, `p2stateno = 889`, `p2getp1state = 1`, and explicit `missonoverride = 0`; defender P2 has active matching `HitOverride` slot `777`; contact records owner target link `p1 -> p2 / 8872` plus helper target link `p1-helper-0 -> p2 / 8872`; projectile payload evidence reaches `hitsRemaining = 0` / `hasHit = true`; and P2 redirects through state `777` instead of missing, entering projectile custom state `889`, taking helper `ProjHit` branch `1276`, or default Common1 states `5000`, `150`, and `151`. The gate requires executed `Helper`, `Projectile`, and `HitOverride`, typed `helper`, `projectile`, and `hitoverride`, active `x`, override event/combat telemetry, helper/projectile lifecycle and payload evidence, both target links, final P2 state `777` / life `1000`, and forbidden states `889`, `1276`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 346/346 artifacts, 316 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes all HitDef parameters and helper-spawned projectiles are root-owned; local Ikemen-GO source only forces projectile HitOverride miss when `missonoverride == 1`, so explicit `missonoverride = 0` keeps redirect behavior. Claim allowed: bounded helper-parented Projectile `missonoverride = 0` HitOverride redirect before projectile custom-state entry. Claim blocked: broader `missonoverride` custom-state breadth, helper/projectile/custom-state slot-priority breadth, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-zero.json` checksum `5c12f3cc` gates bounded player-owned Projectile `missonoverride = 0` HitOverride redirect behavior. A Projectile declares `p2stateno = 889`, `p2getp1state = 1`, and `missonoverride = 0`, defender P2 has active matching `HitOverride` slot `777`, and contact records target id `77`, removes the projectile as a hit, and redirects P2 through state `777` instead of missing or entering projectile custom state `889`; the gate requires executed `Projectile` and `HitOverride`, typed `projectile` and `hitoverride`, active `x`, override event/combat telemetry, projectile lifecycle and payload evidence, target-link evidence, final P2 state `777` / life `1000`, and forbidden states `889`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 345/345 artifacts, 315 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes all HitDef parameters; local Ikemen-GO source only forces projectile HitOverride miss when `missonoverride == 1`, so explicit `missonoverride = 0` keeps redirect behavior. Claim allowed: bounded player-owned Projectile `missonoverride = 0` HitOverride redirect before projectile custom-state entry. Claim blocked: helper-parented Projectile `missonoverride = 0` is covered by the latest checkpoint, while broader `missonoverride` custom-state breadth, exact slot priority, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-missonoverride-one.json` checksum `a99979bb` gates bounded helper-parented Projectile `missonoverride = 1` HitOverride miss behavior. A visual Helper spawns Projectile id `8871` with `parentId = p1-helper-0` and `missonoverride = 1`, defender P2 has active matching `HitOverride` slot `777`, and contact rejects before owner/helper target memory, projectile contact consumption, damage, guard, normal HitOverride redirect, helper `ProjHit` branch `1275`, or projectile custom-state entry; the gate requires executed `Helper`, `Projectile`, and `HitOverride`, typed `helper`, `projectile`, and `hitoverride`, active `x`, reject event/combat telemetry, helper/projectile lifecycle and payload evidence, no target links, active projectile payload evidence with `hitsRemaining = 1` / `hasHit = false`, final P2 state `0` / life `1000` / control, and forbidden states `777`, `889`, `1275`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 344/344 artifacts, 314 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes all HitDef parameters plus projectile parameters and helper-spawned projectiles are root-owned; local Ikemen-GO source returns miss when `missonoverride = 1` and active HitOverride matches. Claim allowed: bounded helper-parented Projectile `missonoverride = 1` HitOverride miss before owner/helper target memory, damage, guard, redirect, helper `ProjHit`, or contact consumption. Claim blocked: helper-parented Projectile `missonoverride = 0`, exact slot priority, exact helper/projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-missonoverride-one.json` checksum `2dc86467` gates bounded player-owned Projectile `missonoverride = 1` HitOverride miss behavior. A Projectile declares `missonoverride = 1`, defender P2 has active matching `HitOverride` slot `777`, and contact rejects before target memory, projectile contact consumption, damage, guard, normal HitOverride redirect, or projectile custom-state entry; the gate requires executed `Projectile` and `HitOverride`, typed `projectile` and `hitoverride`, active `x`, reject event/combat telemetry, no target links, active projectile payload evidence with `hitsRemaining = 1` / `hasHit = false`, final P2 state `0` / life `1000` / control, and forbidden states `777`, `889`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 343/343 artifacts, 313 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes all HitDef parameters plus projectile parameters; local Ikemen-GO source returns miss when `missonoverride = 1` and active HitOverride matches. Claim allowed: bounded player-owned Projectile `missonoverride = 1` HitOverride miss before target/damage/guard/redirect/contact consumption. Claim blocked: helper-parented Projectile `missonoverride = 1` is covered by the latest checkpoint; `missonoverride = 0` projectile/custom-state breadth, exact slot priority, exact projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-helper-projectile-hitoverride-p2stateno.json` checksum `ce4c1d9a` gates bounded helper-parented owner-side Projectile `p2stateno` HitOverride behavior. A visual Helper spawns Projectile id `8870` with `parentId = p1-helper-0`, `p2stateno = 889`, and `p2getp1state = 0`; defender P2 has active matching `HitOverride` slot `777`; contact records owner target link `p1 -> p2 / 8870` and helper target link `p1-helper-0 -> p2 / 8870`; P2 redirects through state `777` / life `1000` instead of entering projectile custom state `889`, helper `ProjHit` branch `1273`, or default Common1 get-hit/guard states `5000`, `150`, and `151`. The gate requires executed `Helper`, `Projectile`, and `HitOverride`, typed `helper`, `projectile`, and `hitoverride`, active `x`, override event/combat telemetry, helper/projectile lifecycle and payload evidence, both target links, final P2 state `777` / life `1000`, and forbidden states `889`, `1273`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 342/342 artifacts, 312 required and 30 optional. Official source checked: Elecbyte Projectile docs state Projectile takes all HitDef parameters plus projectile parameters; local Ikemen-GO source excludes projectile `p2stateno` from the direct-HitDef default HitOverride custom-state miss condition. Claim allowed: bounded helper-parented Projectile `p2stateno` HitOverride redirect before projectile custom-state entry, with owner/helper target memory and helper-parented projectile evidence. Claim blocked: player-owned Projectile `missonoverride = 1` is covered by the latest checkpoint; helper Projectile `missonoverride` gate, `missonoverride = 0` projectile/custom-state breadth, exact slot priority, exact helper-projectile target lifetime/tick order, helper-owned custom-state tables, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity remain blocked.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-projectile-hitoverride-p2stateno.json` checksum `2ec0725a` gates bounded IKEMEN-style player-owned Projectile `p2stateno` HitOverride behavior. A Projectile declares `p2stateno = 889` / `p2getp1state = 0`, defender P2 has active matching `HitOverride` slot redirecting to state `777`, and contact records target id `77` plus redirects P2 through state `777` instead of taking the direct-HitDef custom-state miss path or entering projectile custom state `889`; the gate requires executed `Projectile` and `HitOverride`, typed `projectile` and `hitoverride`, active `x`, override event/combat telemetry, projectile lifecycle/payload evidence, target link `p1 -> p2 / 77`, final P2 state `777`/life `1000`, and forbids states `889`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 341/341 artifacts, 311 required and 30 optional. Claim allowed: bounded player-owned Projectile `p2stateno` HitOverride redirect before projectile custom-state entry, with target memory and projectile lifecycle evidence. Claim blocked: projectile `missonoverride` breadth, exact slot priority, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full MUGEN/IKEMEN Projectile/HitOverride parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-p2getp1state-zero-miss.json` checksum `656730c8` gates bounded IKEMEN direct `HitDef p2stateno` / `p2getp1state = 0` HitOverride miss behavior. A direct `HitDef` declares `p2stateno = 889` / `p2getp1state = 0`, defender P2 has an active matching `HitOverride` slot redirecting to state `777`, target-owned state `889` data exists, and contact rejects before target memory, damage, guard, normal HitOverride redirect, or target-owned custom-state entry; the gate requires executed `HitDef` and `HitOverride`, typed `hitdef` and `hitoverride`, active `x`, reject event/combat telemetry, no target links, final P2 state `0`/life `1000`/control, and forbids states `777`, `889`, `888`, `5000`, `150`, and `151`. `pnpm qa:trace` passes 340/340 artifacts, 310 required and 30 optional. Official source checked: Elecbyte docs define `p2getp1state = 0` as target-owned state/animation routing; local Ikemen-GO source still treats direct non-projectile `p2stateno` as a default miss when active HitOverride matches. Claim allowed: bounded direct-HitDef target-owned `p2getp1state = 0` HitOverride miss before target/damage/guard/redirect/state entry. Claim blocked: helper/projectile `p2stateno`, exact slot priority, helper/projectile `missonoverride` breadth, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-one.json` checksum `78cfedf4` gates bounded IKEMEN `HitDef missonoverride = 1` behavior. A direct `HitDef` declares `missonoverride = 1` without `p1stateno` / `p2stateno`, defender P2 has an active matching `HitOverride` slot redirecting to state `777`, and contact rejects before target memory, damage, guard, normal HitOverride redirect, or custom-state entry; the gate requires executed `HitDef` and `HitOverride`, typed `hitdef` and `hitoverride`, active `x`, reject event/combat telemetry, no target links, final P2 state `0`/life `1000`/control, and forbids states `777`, `888`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 339/339 artifacts, 309 required and 30 optional. Primary source checked: local Ikemen-GO source defines `missonoverride = 1` as an unconditional miss when HitOverride matches. Claim allowed: bounded direct-HitDef owner-backed `missonoverride = 1` HitOverride miss before target/damage/guard/redirect. Claim blocked: helper/projectile `p2stateno`, exact slot priority, helper/projectile `missonoverride` breadth, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-missonoverride-zero.json` checksum `8ffd5678` gates bounded IKEMEN `HitDef missonoverride = 0` behavior. A direct `HitDef` declares `p2stateno = 888` / `p2getp1state = 1` / `missonoverride = 0`, defender P2 has an active matching `HitOverride` slot redirecting to state `777`, and contact records target id `77` plus redirects into state `777` instead of taking the default custom-state miss path; the gate requires executed `HitDef` and `HitOverride`, typed `hitdef` and `hitoverride`, active `x`, override event/combat telemetry, target-link evidence, final P2 state `777`/life `1000`, and forbids states `888`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 338/338 artifacts, 308 required and 30 optional. Official source checked: Elecbyte HitOverride docs define the default custom-state miss conditions; local Ikemen-GO source defines `missonoverride = 0` as the explicit opt-out from default miss when HitOverride matches. Claim allowed: bounded direct-HitDef owner-backed `missonoverride = 0` HitOverride redirect before custom-state entry. Claim blocked: helper/projectile `p2stateno`, exact slot priority, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-hitoverride-p2stateno-miss.json` checksum `6f41eeb1` gates bounded direct-HitDef HitOverride/custom-state miss behavior. A direct `HitDef` declares `p2stateno = 888` / `p2getp1state = 1`, defender P2 has an active matching `HitOverride` slot redirecting to state `777`, and contact is rejected before target memory, damage, guard, normal HitOverride redirect, or custom-state entry; the gate requires executed `HitDef` and `HitOverride`, typed `hitdef` and `hitoverride`, active `x`, reject event/combat telemetry, no target links, final P2 state `0`/life `1000`/control, and forbids states `777`, `888`, `5000`, `150`, and `151`. That `pnpm qa:trace` pass was 337/337 artifacts, 307 required and 30 optional. Official source checked: Elecbyte HitOverride docs exclude active HitOverride from matching custom-state HitDefs using `p1stateno` or `p2getp1state = 1`; local Ikemen-GO misses direct HitDefs with `p1stateno`/`p2stateno` while an active HitOverride matches. Claim allowed: bounded direct-HitDef owner-backed custom-state HitOverride invalidation/miss before target/damage/guard/redirect. Claim blocked: helper/projectile `p2stateno`, exact slot priority, `missonoverride = 1` breadth, exact guard KO/no-KO round flow, helper/projectile custom-state guard metadata, throws, teams/simul, score movement, and full HitOverride/custom-state parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-p2stateno-guard-ignored.json` checksum `76d1becd` gates bounded direct-HitDef `p2stateno` ignored-on-guard behavior. A guarded direct `HitDef` declares `p2stateno = 888` / `p2getp1state = 1`, records target memory for id `77`, then P2 stays in defender-owned Common1-style guard flow `150 -> 151 -> 130 -> 20`; the gate requires slide-stop/control controller and typed operation evidence, target-link evidence, final walk/control, and forbids state `888`. That `pnpm qa:trace` pass was 336/336 artifacts, 306 required and 30 optional. Official source checked: Elecbyte HitDef docs define `p2stateno` as successful-hit custom-state routing; local Ikemen-GO applies `p2stateno` only when `Abs(hitResult) == 1`, not successful guards. Claim allowed: bounded successful guard against a direct HitDef ignores `p2stateno` and uses defender-owned guard-hit CNS. Claim blocked: exact guard KO/no-KO round flow, helper/projectile `p2stateno` behavior, exact guard timing/proximity/effects, throws, teams/simul, score movement, and full guard parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-guard-kill.json` checksum `c889a534` gates bounded owner-backed custom-state guard-hit `GetHitVar(kill)` routing. A guarded direct `HitDef` with `guard.kill = 0` records target memory, owner-local `TargetState` sends P2 into P1-owned state data, then `GetHitVar(kill) = 0 && GetHitVar(guarded) = 1 && GetHitVar(hitshaketime) > 0` branches P2 `888 -> 904` before `SelfState` returns to state `0`/control. That `pnpm qa:trace` pass was 335/335 artifacts, 305 required and 30 optional. Official source checked: Elecbyte HitDef docs define `guard.kill`; Elecbyte `TargetState` / `SelfState` docs define owner-state routing and return semantics; Elecbyte `GetHitVar(param_name)` documents guarded hit metadata reads; local Ikemen-GO exposes `GetHitVar("kill")` from `c.ghv.kill` and switches guarded hit vars to `hd.guard_kill`. Claim allowed: bounded direct-HitDef guard kill metadata can be read while P2 executes attacker-owned custom state data after owner-local `TargetState`. Claim blocked: exact guard KO/no-KO round flow, exact guard timing/proximity/effects, helper/projectile custom-state guard kill inheritance, throws, teams/simul, score movement, and full custom-state/guard parity.
- Previous R1 required helper-parented Projectile guard kill cut: `synthetic-imported-helper-projectile-gethitvar-guard-kill.json` checksum `7f9aa699` gates bounded helper-parented Projectile guard-hit `GetHitVar(kill)` routing through `150 -> 151 -> 334`. Claim allowed: bounded helper-parented Projectile guard kill metadata can be read from defender-owned guard-hit CNS. Claim blocked: exact helper Projectile guard KO/no-KO round flow, exact helper/projectile target lifetime/tick order, exact guard timing/proximity/effects, score movement, and full guard/projectile parity.
- Previous R1 required player-owned Projectile guard kill cut: `synthetic-imported-projectile-gethitvar-guard-kill.json` checksum `3feae5a7` gates bounded player-owned Projectile guard-hit `GetHitVar(kill)` routing through `150 -> 151 -> 333`. Claim allowed: bounded player-owned Projectile guard kill metadata can be read from defender-owned guard-hit CNS. Claim blocked: helper/projectile custom-state guard kill inheritance, exact guard KO/no-KO round flow, exact projectile target lifetime/tick order, exact guard timing/proximity/effects, score movement, and full guard/projectile parity.
- Previous R1 required air guard kill cut: `synthetic-imported-gethitvar-air-guard-kill.json` checksum `4382207e` gates bounded defender-owned air guard-hit `GetHitVar(kill)` routing through `154 -> 155 -> 332`. Claim allowed: bounded direct-HitDef air guard kill metadata can be read from defender-owned air guard-hit CNS. Claim blocked: exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact air guard timing/landing/proximity/effects, score movement, and full guard parity.
- Previous R1 required crouch guard kill cut: `synthetic-imported-gethitvar-crouch-guard-kill.json` checksum `2976fb8c` gates bounded defender-owned crouch guard-hit `GetHitVar(kill)` routing through `152 -> 153 -> 331`. Claim allowed: bounded direct-HitDef crouch guard kill metadata can be read from defender-owned crouch guard-hit CNS. Claim blocked: exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact guard timing/proximity/effects, score movement, and full guard parity.
- Previous R1 required stand guard kill cut: `synthetic-imported-gethitvar-guard-kill.json` checksum `abb4e468` gates bounded defender-owned stand guard-hit `GetHitVar(kill)` routing through `150 -> 151 -> 330`. Claim allowed: bounded direct-HitDef stand guard kill metadata can be read from defender-owned guard-hit CNS. Claim blocked: exact guard KO/no-KO round flow, helper/projectile/custom-state inheritance, exact guard timing/proximity/effects, score movement, and full guard parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-kill.json` checksum `ef5ffabf` gates bounded defender-owned normal-hit `GetHitVar(kill)` routing. A direct normal `HitDef kill = 0` records target memory and stored kill metadata, sends P2 into defender-owned Common1-style state `5000`, then `GetHitVar(kill) = 0 && !GetHitVar(guarded)` branches P2 `5000 -> 329`. That `pnpm qa:trace` pass was 329/329 artifacts, 299 required and 30 optional. Official source checked: local Ikemen-GO source exposes `GetHitVar("kill")` from `c.ghv.kill`, and Ikemen-GO release notes record the `GetHitVar(kill)` trigger. Claim allowed: bounded direct-HitDef normal kill metadata can be read from defender-owned get-hit CNS. Claim blocked: KO/round-flow behavior, helper/projectile/custom-state inheritance, exact target lifetime/tick order, score movement, and full get-hit parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-fall-metadata.json` checksum `4a3a1c6b` gates bounded owner-backed custom-state `GetHitVar(fall.damage/fall.kill/fall.xvel/fall.yvel)` routing. A direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` records target memory and stored fall metadata, sends P2 into P1-owned state data, then `GetHitVar(fall) && GetHitVar(fall.damage) = 70 && GetHitVar(fall.kill) = 0 && GetHitVar(fall.xvel) = 3 && GetHitVar(fall.yvel) = -6` branches P2 `888 -> 903` before `SelfState` returns to state `0`/control. That `pnpm qa:trace` pass was 328/328 artifacts, 298 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `fall.damage`, `fall.kill`, `fall.xvel`, and `fall.yvel`; Elecbyte HitDef docs define `p2stateno` / `p2getp1state` owner-state behavior and `SelfState` return semantics. Claim allowed: bounded direct-HitDef fall damage/kill/velocity metadata can be read while executing attacker-owned custom state data. Claim blocked: exact metadata lifetime/stacking, helper/projectile inheritance, throws, teams/simul, score movement, and full custom-state fall parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-fall-envshake.json` checksum `5c9d1653` gates bounded owner-backed custom-state `GetHitVar(fall.envshake.time/freq/ampl/phase)` routing. A direct fall `HitDef p2stateno = 888` / `p2getp1state = 1` records target memory and fall envshake metadata, sends P2 into P1-owned state data, then `GetHitVar(fall) && GetHitVar(fall.envshake.time) = 15 && GetHitVar(fall.envshake.freq) = 178 && GetHitVar(fall.envshake.ampl) = 6 && GetHitVar(fall.envshake.phase) = 0` branches P2 `888 -> 902` before `SelfState` returns to state `0`/control. That `pnpm qa:trace` pass was 327/327 artifacts, 297 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `fall.envshake.time`, `fall.envshake.freq`, `fall.envshake.ampl`, and `fall.envshake.phase` as values set by attacker HitDef fall envshake params; Elecbyte docs define `TargetState` target routing and `FallEnvShake` metadata consumption. Claim allowed: bounded direct-HitDef fall envshake metadata can be read while executing attacker-owned custom state data. Claim blocked: exact camera waveform, pause/stage/layer interaction, metadata lifetime/stacking, helper/projectile inheritance, throws, teams/simul, score movement, and full custom-state fall presentation parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-guard-timing.json` checksum `ba77beec` gates bounded owner-backed custom-state `GetHitVar(slidetime/ctrltime)` guard timing. A guarded direct `HitDef` target-memory route executes owner-local `TargetState`, sends P2 into P1-owned state data, then `GetHitVar(guarded) = 1 && GetHitVar(slidetime) = 5 && GetHitVar(ctrltime) = 7 && GetHitVar(hitshaketime) > 0` branches P2 `888 -> 901` before `SelfState` returns to state `0`/control. That `pnpm qa:trace` pass was 326/326 artifacts, 296 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `guarded`, `hitshaketime`, `slidetime`, and `ctrltime`; Elecbyte HitDef docs define guarded timing values, target ids, and owner-state behavior. Claim allowed: bounded guarded direct-HitDef timing metadata can be read while executing attacker-owned custom state data after `TargetState`. Claim blocked: `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, score movement, and full custom-state/get-hit parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid.json` checksum `250f77c2` gates bounded owner-backed custom-state `GetHitVar(hitcount/hitid/chainid)` routing. A direct `HitDef id = 77, chainID = 43, numhits = 3` routes P2 through `p2stateno = 888` / `p2getp1state = 1` into P1-owned state data, then `GetHitVar(hitcount) = 3 && GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)` branches P2 `888 -> 900` before `SelfState` returns to state `0`/control. That `pnpm qa:trace` pass was 325/325 artifacts, 295 required and 30 optional. Claim allowed: bounded direct-HitDef numhits/id/chainID metadata can be read while executing attacker-owned custom state data. Claim blocked: exact combo accumulation, chain-hit eligibility arbitration, helper/projectile inheritance, exact target lifetime, teams/simul, score movement, and full custom-state/get-hit parity.
- Previous focused R1 runtime checkpoint: required `synthetic-imported-gethitvar-fallcount.json` checksum `c391d938` gates bounded `GetHitVar(fallcount)` routing. A fall `HitDef` routes P2 into owner-backed state `5100`, `HitFallDamage` records one Common1-style ground-impact count and consumes stored `fall.damage`, then `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0` branches P2 `5100 -> 328`. That `pnpm qa:trace` pass was 324/324 artifacts, 294 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `fallcount` and describes it as the number of times the player has hit the ground in the current combo. Claim allowed: bounded `5100` ground-impact count can be read through imported CNS `GetHitVar(fallcount)`. Claim blocked: multi-ground-hit combo accumulation, non-Common1 impact detection, exact lifetime/reset parity, helper/projectile/custom-state inheritance, score movement, and full fall/get-hit parity.
- Latest focused R2 ownership cut: `RuntimeEffectActorAdvanceWorld` owns bounded effect-actor advance ordering inside `RuntimeEffectActorWorld`. Active effects advance Helpers before Projectiles, paused presentation advances Helpers before Explods, and normal presentation advances Explods without ticking Helpers. Focused `EffectActorSystem` coverage proves both order contracts. Claim allowed: current effect actor active/presentation order has a named, testable boundary. Claim blocked: exact helper pause/combat ordering, projectile lifetime parity, remove-trigger timing, teams/simul roster ownership, visual/audio parity, score movement, and full Helper/Projectile VM parity.
- Previous focused R2 ownership cut: `RuntimeMatchEnvShakeBridgeWorld` owns bounded match-level `EnvShake` / `FallEnvShake` controller emission outside `PlayableMatchRuntime`. Active controller routes now forward controller source, optional typed operation data, runtime tick, telemetry hooks, and `RuntimeEnvShakeWorld` through one named boundary. Focused `RuntimeMatchEnvShakeBridgeSystem` coverage proves typed `EnvShake` forwarding, `FallEnvShake` hit-fall metadata emission/clearing, telemetry forwarding, and no-pending-fall no-event behavior. Claim allowed: current active EnvShake/FallEnvShake emission has a named, testable match boundary. Claim blocked: exact camera waveform, pause/stage/layer timing, helper/redirect ownership breadth, renderer parity, score movement, and full presentation parity.
- Previous focused R1 owner-backed custom-state cut: required `synthetic-imported-custom-state-gethitvar-snap.json` checksum `ce4680b9` gates bounded `GetHitVar(xoff/yoff/zoff)` snap metadata inheritance. A direct `HitDef snap = 16,-24` plus `p2stateno = 888` records defender hit vars while P2 executes P1-owned state data, branches `888 -> 899` through `GetHitVar(xoff) = 16 && GetHitVar(yoff) = -24 && GetHitVar(zoff) = 0`, preserves actor-frame `customOwnerId = p1`, and returns through `SelfState`. That `pnpm qa:trace` pass was 323/323 artifacts, 293 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `xoff`, `yoff`, and `zoff` as snap offsets when hit, Elecbyte HitDef docs define `snap = x_pos, y_pos`, `p2stateno` / `p2getp1state` owner-state behavior, and `SelfState` return semantics. Claim allowed: bounded direct-hit `snap` metadata can be read while P2 executes attacker-owned custom state data and returns through `SelfState`. Claim blocked: exact throw positioning, z-axis support, guard snap behavior, helper/projectile inheritance, broader custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN throw/custom-state parity.
- Previous focused R1 owner-backed get-hit cut: required `synthetic-imported-gethitvar-snap.json` checksum `312a53fc` gates bounded `GetHitVar(xoff/yoff/zoff)` metadata. A direct `HitDef snap = 16,-24` records defender hit vars, applies simple attacker-relative contact positioning, P2 enters owner-backed state `5100`, then `GetHitVar(xoff) = 16 && GetHitVar(yoff) = -24 && GetHitVar(zoff) = 0` branches P2 `5100 -> 288`. `pnpm qa:trace` passes 322/322 artifacts, 292 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `xoff`, `yoff`, and `zoff` as snap offsets when hit, and Elecbyte HitDef docs define `snap = x_pos, y_pos` as moving P2 to a specified position relative to P1 on hit. Claim allowed: bounded direct-hit `snap` metadata can be read from owner-backed get-hit CNS and direct combat applies a simple snap. Claim blocked: exact throw positioning, z-axis support, guard snap behavior, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN throw/get-hit parity.
- Previous focused R1 normal get-hit cut: required `synthetic-imported-gethitvar-hitid-chainid.json` checksum `18df99ed` gates bounded defender-owned Common1-style `GetHitVar(hitid)` / `GetHitVar(chainid)` metadata. A direct `HitDef id = 77, chainID = 43` records target memory, P2 enters defender-owned state `5000`, then `GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && !GetHitVar(guarded)` branches P2 `5000 -> 326`. `pnpm qa:trace` passes 320/320 artifacts, 290 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `hitid` and `chainid`, Elecbyte documents `chainid` as the player-assigned chainID for the last hit taken, and Elecbyte HitDef docs define `id` as targetID default `0` plus `chainID` default `-1`. Claim allowed: bounded direct-hit `id` / `chainID` metadata can be read from defender-owned get-hit CNS. Claim blocked: exact chain-hit eligibility arbitration, helper/projectile/custom-state inheritance breadth, teams/simul, exact target lifetime/tick order, score movement, and full MUGEN/IKEMEN get-hit parity.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-yaccel.json` checksum `549fe48d` gates bounded owner-backed custom-state `GetHitVar(yaccel)` metadata inheritance. A direct `HitDef p2stateno = 888` stores `yaccel = 0.62`, P2 enters P1-owned state data, then `GetHitVar(yaccel) = .62 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)` branches P2 `888 -> 898` before `SelfState` returns to P2 state `0`/control. `pnpm qa:trace` passes 319/319 artifacts, 289 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `yaccel` as the y acceleration set by the hit, and Elecbyte HitDef docs define the `yaccel` source parameter. Claim allowed: bounded direct-hit yaccel metadata can be read while executing attacker-owned custom state data and return through `SelfState`. Claim blocked: exact physics integration, fall acceleration arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-type.json` checksum `38542874` gates bounded owner-backed custom-state `GetHitVar(type)` metadata inheritance. A direct `HitDef p2stateno = 888` stores `ground.type = Low` and `air.type = Trip`, P2 enters P1-owned state data, then `GetHitVar(type) = 2 && GetHitVar(groundtype) = 2 && GetHitVar(airtype) = 3 && !GetHitVar(guarded)` branches P2 `888 -> 897` before `SelfState` returns to P2 state `0`/control. That `pnpm qa:trace` aggregate was 318/318 artifacts, 288 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` lists `type`, `groundtype`, and `airtype`, and Elecbyte HitDef docs define the hit-type metadata source. Claim allowed: bounded direct-hit type metadata can be read while executing attacker-owned custom state data and return through `SelfState`. Claim blocked: exact get-hit animation selection, air-hit arbitration, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-isbound.json` checksum `d25307e9` gates bounded owner-backed custom-state `GetHitVar(isbound)` metadata inheritance. A direct `HitDef` records target memory, P1 executes `TargetBind` before typed owner-local `TargetState`, P2 enters P1-owned state data, then `GetHitVar(isbound) = 1 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)` branches P2 `888 -> 896` before `SelfState` returns to P2 state `0`/control. That `pnpm qa:trace` aggregate was 317/317 artifacts, 287 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` defines `isbound` as true when the player is subject to an attacker's `TargetBind`, and Elecbyte state-controller docs define `TargetBind` and `TargetState`. Claim allowed: bounded direct-HitDef target memory can feed `TargetBind`, preserve bound-subject metadata inside attacker-owned custom state data, and return through `SelfState`. Claim blocked: throws, exact bind tick-order/lifetime, visual bind parity, helper/root/parent redirects, teams/simul, `p2stateno`-on-guard behavior, score movement, and full MUGEN/IKEMEN custom-state parity.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-guarded.json` checksum `54f62821` gates bounded owner-backed custom-state guarded metadata inheritance. A guarded direct `HitDef` records target memory, P1 executes typed `TargetState`, P2 enters P1-owned state data, then `GetHitVar(guarded) = 1 && GetHitVar(hitshaketime) > 0 && GetHitVar(hittime) > 0` branches P2 `888 -> 895` before `SelfState` returns to P2 state `0`/control. That `pnpm qa:trace` aggregate was 316/316 artifacts, 286 required and 30 optional. Official source checked: Elecbyte `GetHitVar(param_name)` includes `guarded`, `hitshaketime`, and `hittime`; Elecbyte state-controller docs define `TargetState`, `p2stateno` owner-state behavior, and `SelfState` return semantics. Claim allowed: bounded guarded direct-HitDef target memory can feed owner-local `TargetState`, preserve guarded hit metadata inside attacker-owned custom state data, and return through `SelfState`. Claim blocked: `p2stateno`-on-guard behavior, exact guard timing, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-velocity.json` checksum `e9d8da9e` gates bounded owner-backed custom-state velocity metadata inheritance. Direct `HitDef p2stateno = 888` with `p2getp1state = 1` and `ground.velocity = 4,-2` routes P2 into P1-owned state data, then `GetHitVar(xvel) = 4 && GetHitVar(yvel) = -2 && !GetHitVar(fall) && !GetHitVar(guarded)` branches P2 into state `894` before `SelfState` returns to P2 state `0`/control. That `pnpm qa:trace` aggregate was 315/315 artifacts, 285 required and 30 optional. Claim allowed: bounded direct-hit velocity metadata inheritance while executing attacker-owned custom state data. Claim blocked: exact velocity lifetime after later physics/controllers, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, score movement, and full MUGEN/IKEMEN custom-state parity.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-down-recover.json` checksum `5bd94568` gates bounded owner-backed custom-state down-recovery metadata inheritance. Direct fall `HitDef p2stateno = 888` with `p2getp1state = 1` routes P2 into P1-owned state data, then `GetHitVar(down.recover) = 1 && GetHitVar(down.recovertime) = 45 && GetHitVar(recovertime) = 45` branches P2 into state `893` before `SelfState` returns to P2 state `0`/control. That `pnpm qa:trace` aggregate was 314/314 artifacts, 284 required and 30 optional. Claim allowed: bounded direct-hit down-recovery metadata inheritance while executing attacker-owned custom state data. Claim blocked: exact lie-down tables, guard metadata inheritance, throws, helper/root/parent redirects, teams/simul, exact bind/tick order, exact recovery threshold behavior, score movement, and full MUGEN/IKEMEN custom-state parity.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-animtype.json` checksum `bbe8777c` gates bounded owner-backed custom-state HitDef type metadata inheritance through state `888 -> 892 -> SelfState`. It remains required coverage but no longer latest.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar-fall.json` checksum `2ccfeb43` gates bounded owner-backed custom-state fall metadata inheritance through state `888 -> 891 -> SelfState`. It remains required coverage but no longer latest.
- Previous focused R1 custom-state cut: required `synthetic-imported-custom-state-gethitvar.json` checksum `40705e74` gates bounded owner-backed custom-state direct-hit `GetHitVar(damage/hittime/guarded)` inheritance through state `888 -> 890 -> SelfState`. It remains required coverage but no longer latest.
- Latest focused R1 `AssertSpecial` cut: required `synthetic-imported-assertspecial-round-flow-telemetry.json` checksum `10f95bdb` gates bounded official `Intro` / `NoKOSlow` telemetry. Static flags lower into typed `assertspecial` operation evidence, runtime execution exposes `intro` and `noKoSlow`, and final imported actor evidence requires normalized `assertSpecialGlobalFlags` for `intro` and `nokoslow`. `pnpm qa:trace` passes 310/310 artifacts, 280 required and 30 optional. Official source checked: Elecbyte `AssertSpecial` flags include `intro` and `nokoslow`. Claim allowed: bounded parser/typed-op/runtime/final-actor telemetry for `Intro` and `NoKOSlow`. Claim blocked: exact intro ownership, KO slow-motion suppression, winpose/round transitions, helper/team/global ownership, pause layering, score movement, and full MUGEN/IKEMEN round-flow parity.
- Previous focused R1 helper controller-param cut: required `synthetic-imported-helper-controller-param-parentroot.json` checksum `9ad71f4e` gates bounded helper-local dynamic controller-parameter `Parent` / `Root` redirect evaluation. A first-generation visual Helper executes `VelSet x = Parent,Life - 995` / `y = Root,StateNo - 203`, reaches actor-frame velocity `5,-3`, and routes to state `1401` / anim `941`. `pnpm qa:trace` passed 309/309 artifacts, 279 required and 30 optional. Claim allowed: bounded helper-local `VelSet` params can read current parent/root state in this first-generation visual Helper route. Claim blocked: nested helper ancestry where root differs from parent, helper-spawned helpers, player `Parent` controller-param redirects, dynamic-parameter typed lowering, recursive redirection, debug warning text, teams/simul, helper-owned controller breadth, score movement, and full helper/controller expression parity.
- Previous focused R1 `AssertSpecial` cut: required `synthetic-imported-assertspecial-helper-explod-shadow.json` trace checksum `83f61b48` extends the bounded shadow presentation evidence to spawned helper/explod actor frames under `GlobalNoShadow`. Previous required `synthetic-imported-assertspecial-shadow-telemetry.json` trace checksum `2b9c8fac` remains the local/global player shadow telemetry oracle for normalized `noshadow` and `globalnoshadow`. The latest helper/explod fixture asserts `GlobalNoShadow` in imported state `200`, spawns a helper-parented Explod, and requires `shadowVisible=false` for P1, P2, the helper, and the explod. `RuntimeSnapshotWorld.match()` projects local/global suppression for player/helper/explod snapshots, and `CharacterRenderer` draws/removes a bounded ellipse shadow from that snapshot contract. At that checkpoint `pnpm qa:trace` passed 308/308 artifacts, 278 required and 30 optional. Claim allowed: bounded current snapshot/render suppression for player shadows plus required trace evidence for helper/explod snapshot suppression in the spawned helper-parented Explod route. Claim blocked: exact MUGEN/IKEMEN shadow skew, stage shadow parameters, projectile shadow semantics, pause/layer behavior, exact global/team/helper ownership beyond this spawned helper/explod route, score movement, and full renderer/global ownership parity.
- Previous focused R1 `AssertSpecial` cut: required `synthetic-imported-assertspecial-global-telemetry.json` checksum `fc793d29` gates bounded global-style flag telemetry. The fixture executes official-style grouped `AssertSpecial` controllers and requires final-actor `assertSpecialGlobalFlags` evidence for `nobardisplay`, `nobg`, `nofg`, `nokosnd`, and `nomusic`. Claim allowed: bounded parsing/normalization/final-actor telemetry for current global-style flags. Claim blocked: lifebar hiding, stage BG/FG suppression, KO sound suppression, music pause, exact global/team/helper ownership, pause/layer behavior, score movement, and full MUGEN/IKEMEN global flag parity.
- Previous focused R1 recovery cut: required `synthetic-imported-default-liedown-fast-recovery.json` checksum `74bdac97` gates the positive bounded fast lie-down recovery shortcut. The fixture omits the authored `5110` `Get Up` `ChangeState`, keeps fresh `recovery` input active while `down.recovertime` is positive in state `5110`, and still routes `5110 -> 5120 -> 0` through the runtime shortcut. Previous `synthetic-imported-assertspecial-nofastrecoverfromliedown.json` checksum `74bf5d85` remains required and gates bounded IKEMEN `AssertSpecial NoFastRecoverFromLieDown` suppression for that shortcut. Claim allowed: bounded synthetic fast lie-down shortcut and bounded `NoFastRecoverFromLieDown` suppression for current `RuntimeRecoverySystem.advanceCommon1LieDownRecovery`. Claim blocked: public KFM proof, exact input-mashing thresholds, exact controller-loop tick order, global/team/helper ownership, pause/hitpause layering, score movement, and full recovery parity.
- Previous focused R1 controller-param Root redirect cut: required `synthetic-imported-controller-param-root-redirect.json` checksum `1d4a73f7` gates bounded controller-parameter `Root` redirect evaluation in current player controller execution. A static `VelSet` seeds P1 velocity `4,-2`, then dynamic `Root, Life - 995` / `Root, StateNo - 203` params resolve through current player root context and set velocity to `5,-3`; actor-frame evidence, named controller order, and static typed `kinematic:velset` evidence guard the route. Claim allowed: bounded dynamic controller-param Root redirect for current player `VelSet` execution. Claim blocked: player Parent controller-param redirects, nested helper ancestry where root differs from parent, helper-spawned helpers, dynamic-parameter typed lowering, recursive redirect evaluation, debug warning text, broad bottom/redirect parity for every controller/parameter family, teams/simul, helper ownership, score movement, and full controller/expression parity.
- Previous focused R1 controller-param Target redirect cut: required `synthetic-imported-controller-param-target-redirect.json` checksum `55bb7b1f` gates bounded controller-parameter `Target(77)` redirect evaluation in current controller execution. A static `VelSet` seeds P1 velocity `4,-2`, direct `HitDef id = 77` records target memory, then dynamic `Target(77), Life - 960` / `Target(77), Life - 966` params set velocity to `3,-3`; actor-frame evidence, target-link evidence, named controller order, `HitDef`, and static typed `kinematic:velset` evidence guard the route. Claim allowed: bounded dynamic controller-param Target redirect for current `VelSet` execution. Claim blocked: player Parent controller-param redirects, nested helper ancestry where root differs from parent, helper-spawned helpers, dynamic-parameter typed lowering, recursive redirect evaluation, debug warning text, broad bottom/redirect parity for every controller/parameter family, teams/simul, helper ownership, score movement, and full controller/expression parity.
- Previous focused R1 controller-param bottom cut: required `synthetic-imported-controller-param-bottom.json` checksum `28ef21ad` gates bounded controller-parameter bottom fallback in current controller execution. A static `VelSet` seeds P1 velocity `4,-2`, then dynamic `Parent,Vel X` / `Parent,Vel Y` params evaluate through a missing parent redirect and fall back to `0,0`; actor-frame evidence observes velocity `4 -> 0` and `-2 -> 0`, with named controller order plus static typed `kinematic:velset` evidence. `pnpm qa:trace` passes 301/301 artifacts, 271 required and 30 optional. Claim allowed: bounded dynamic controller-param bottom fallback to `0` for current `VelSet` execution. Claim blocked: player Parent controller-param redirect support, nested helper ancestry where root differs from parent, helper-spawned helpers, dynamic-parameter typed lowering, recursive redirect evaluation, debug warning text, broad bottom fallback for every controller/parameter family, teams/simul, helper ownership, score movement, and full controller/expression parity.
- Previous focused R1 IfElse-bottom cut: required `synthetic-imported-target-ifelse-bottom.json` checksum `be7554d4` gates bounded `IfElse(...)` invalid-redirect branch-result isolation in current expression/trigger evaluation. A direct `HitDef` target-memory route forbids state `400` when `IfElse(0, MoveHit >= 1, (Target(999), Life = 0))` selects the missing target branch, then routes through state/action `401` when `IfElse(1, MoveHit >= 1, (Target(999), Life = 0))` selects valid `MoveHit` while the unused missing branch remains isolated from the return value. Focused CNS coverage proves `IfElse(...)` still eagerly evaluates unused branches and reports warning-style unsupported diagnostics, unlike `Cond(...)`. Claim allowed: bounded `IfElse(...)` returned-branch isolation for current parser expressions. Claim blocked: Cond-style lazy evaluation, recursive redirection, debug warning text, broader invalid-destination bottom parity, teams/simul, target mutation through redirects, score movement, and full redirect/bottom parity.
- Previous focused R1 Cond-bottom cut: required `synthetic-imported-target-cond-bottom.json` checksum `e882a2bb` gates bounded `Cond(...)` invalid-redirect branch isolation in current expression/trigger evaluation. A direct `HitDef` target-memory route forbids state `398` when `Cond(1, (Target(999), Life = 0), MoveHit >= 1)` selects the missing target branch, then routes through state/action `399` when `Cond(0, (Target(999), Life = 0), MoveHit >= 1)` skips that missing target branch and selects the valid fallback. Focused CNS coverage proves skipped `Cond(...)` branches do not call the unsupported-redirect reporter while selected missing redirects still fail closed. Claim allowed: bounded `Cond(...)` selected-branch evaluation and unused-branch bottom isolation for current parser expressions. Claim blocked: recursive redirection, debug warning text, broader invalid-destination bottom parity, teams/simul, target mutation through redirects, score movement, and full redirect/bottom parity.
- Previous focused R1 redirect-bottom cut: required `synthetic-imported-target-redirect-bottom.json` checksum `5e50a90a` gates bounded invalid-redirect bottom propagation in current parser-expression trigger evaluation. A direct `HitDef` target-memory route forbids state `396` when `(Target(999), Life = 0) || 1` references a missing target destination, then falls through to state/action `397`; focused CNS coverage also proves missing `Parent` redirects propagate through composite expressions while unused `IfElse(...)` branches stay isolated. Claim allowed: bounded failed-redirect bottom propagation for current parser expressions. Claim blocked: recursive redirection, debug warning text, broader invalid-destination bottom parity, teams/simul, target mutation through redirects, score movement, and full redirect/bottom parity.
- Previous focused R1 helper redirect cut: required `synthetic-imported-helper-parentroot.json` checksum `5154220c` gates bounded helper-local `Parent` / `Root` redirect routing. A first-generation visual Helper reads cloned owner/root runtime state through `Parent,StateNo`, `Parent,Vel X`, `Root,Anim`, and a redirected-value `ChangeState` expression before routing `1200 -> 1400` / anim `940`. Claim allowed: bounded read-only helper-local Parent/Root redirect routing for current visual Helper micro-VM. Claim blocked: nested helper ancestry where root differs from parent, player-state parent/root ownership, team/helper-owned redirects, redirect mutation, keyctrl, score movement, and full helper redirect parity.
- Previous focused R1 recovery cut: required `synthetic-imported-assertspecial-nogetupfromliedown.json` checksum `4c3b6281` gates bounded IKEMEN `AssertSpecial NoGetUpFromLieDown`. Static flags lower through typed `assertspecial` operation data, map to runtime `noGetUpFromLieDown`, and block the current Common1 hardcoded `5110 -> 5120` get-up handoff while state `5110` counts `down.recovertime` to `0`; the trace forbids `5120` and leaves P2 in `5110`. Claim allowed: bounded synthetic `NoGetUpFromLieDown` suppression for current `RuntimeRecoverySystem.advanceCommon1LieDownRecovery`. Claim blocked: public KFM proof, exact controller-loop tick order, global/team/helper ownership, pause/hitpause layering, score movement, and full recovery parity.
- Latest R1 QA-hardening cut: `synthetic-imported-default-fall-official-air-recovery` is now part of the `pnpm qa:trace` required artifact coverage-summary contract. The artifact was already required with checksum `b0363be9`; this only prevents coverage-summary drift. Claim allowed: required coverage summaries must retain the existing official-style synthetic air-recovery oracle. Claim blocked: new recovery behavior, checksum movement, public KFM proof, exact threshold/velocity parity, score movement, and full recovery parity.
- Previous completed R2 fighter-advance hook-set ownership cut: `RuntimeFighterAdvanceHookSetWorld` now owns bounded per-fighter advance hook-set construction outside `PlayableMatchRuntime`. Sprite effects, hit eligibility, HitOverride ticking, contact timers, state clock, frame constraints, recovery windows, stun, move lifecycle, kinematics, animation, active controllers, recovery landing, lie-down recovery, and frozen-position preservation share one named seam before `RuntimeFighterAdvanceWorld` executes. Focused coverage proves every current hook route is forwarded without callback replacement. Claim allowed: current per-fighter advance hook-set construction has a named, testable ownership boundary. Claim blocked: new player-advance semantics, exact MUGEN/IKEMEN tick order, persistent-controller timing, helper/team/redirect actor advance semantics, recovery/stun/physics arbitration, visual/audio parity, score movement, and full player VM parity.
- Previous completed R2 active expression-context ownership cut: `RuntimeActiveExpressionContextWorld` now owns bounded active CNS expression-context factory construction outside `PlayableMatchRuntime`. Dynamic controller-param fallback and trigger evaluation share stage bounds/time, owner const routing, runtime RNG, animation timing callbacks, and `InGuardDist` through one named seam before `RuntimeDispatchEvaluationWorld` / `RuntimeTriggerEvaluationWorld` evaluate expressions. Focused `RuntimeActiveExpressionContextSystem`, dispatch, and trigger coverage proves the handoff. Claim allowed: current active CNS dynamic dispatch and trigger-expression context creation has a named, testable boundary. Claim blocked: new expression semantics, exact CNS VM timing, helper/team/redirect expansion, exact `InGuardDist` parity, deterministic MUGEN/IKEMEN RNG stream parity, visual/audio parity, score movement, and full expression/trigger VM parity.
- Previous completed R1 required synthetic air guard landing walk-control cut: required `synthetic-imported-air-guard-landing.json` checksum `d6986d7f` proves bounded synthetic Common1-style air guard-hit route `154 -> 155 -> 52 -> 20`, active `holdback`/`x`, preserved `154`/`155` air guard controller/typed-operation order, landing `VelSet`/`PosSet` typed operations, state-`52` y = 0 evidence, and final imported held-back walk state/action `20` with control. Claim allowed: portable synthetic air guard landing returns to walk control through `20`. Claim blocked: exact air guard physics, exact landing timing, state-`52` internal parity, guard-start/end, proximity guard, guard effects, sparks, sounds, visual/audio parity, public KFM support, score movement, and full MUGEN/IKEMEN guard parity.
- Previous completed R1 optional official KFM air guard landing walk-control cut: optional `kfm-official-default-air-guard-state.json` checksum `f4378971` proves real KFM/Common1 air guard-hit route `154 -> 155 -> 52 -> 20`, active `holdback`/`x`, preserved air guard controller/typed-operation and landing order, and final KFM held-back walk state/action `20` with control when `.scratch/fixtures/kfm-official.zip` exists. Previous optional KFM crouch guard-hold crouch-control cut remains `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0`; previous required portable cut `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699`; previous required stand guard walk-control cut `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c`; optional stand mirror `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da`. Claim allowed: private-fixture KFM air guard-hit lands through `52` and then resumes held-back walking state/action `20` with control. Claim blocked: public KFM support, required portable KFM-specific fixture coverage when the private fixture is absent, exact air guard physics, exact landing timing, state-`52` internal parity, guard-start/end, proximity guard, guard effects, sparks, sounds, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity.
- Previous completed R1 optional official KFM crouch guard-hold crouch-control cut: optional `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` proves real KFM/Common1 crouch guard-hit route `152 -> 153 -> 131 -> 11`, active `holdback`/`holddown`/`x`, preserved crouch slide-stop controller/typed-operation order, and final KFM crouch state/action `11` with control when `.scratch/fixtures/kfm-official.zip` exists. Previous required portable cut `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699` proves bounded synthetic Common1-style crouch guard-hit route `152 -> 153 -> 130 -> 10`; previous required `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c` proves the matching portable stand guard-hit route `150 -> 151 -> 130 -> 20`; optional `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da` mirrors the real KFM/Common1 stand route against the private fixture. `pnpm qa:trace` passes 295/295 artifacts, 265 required and 30 optional. Claim allowed: private-fixture KFM crouch guard-hit returns through observable crouch guard-hold `131` and then resumes held-down crouch state/action `11` with control. Claim blocked: public KFM support, required portable KFM-specific fixture coverage when the private fixture is absent, exact guard-hold duration, exact guard timing/proximity/effects, air guard-hold parity, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity.
- Previous completed R1 optional official KFM ground-recovery priority cut: optional `kfm-official-default-fall-ground-recovery-priority.json` checksum `6d361534` mirrors the required near-ground priority route against the private real KFM fixture. The gate reuses the real `5050 -> 5200 -> 5201 -> 52 -> 0` ground-recovery branch, requires bounded actor-frame plus typed hit/landing kinematic operation evidence, and forbids generic air-recovery state `5210` plus lie-down chain states `5100`/`5101`/`5110`/`5120`. Claim allowed: private-fixture KFM mirrors the bounded ground-over-air recovery priority branch. Claim blocked: public KFM support, exact ground/air recovery arbitration thresholds, recovery tables, velocity math, exact Common1 controller-loop timing, visual/audio parity, score movement, and full MUGEN/IKEMEN fall/recovery parity.
- Previous required R1 Common1 ground-recovery priority cut: required `synthetic-imported-default-fall-ground-recovery-priority.json` checksum `e83b2db7` gates bounded near-ground `command = "recovery"` arbitration in default get-hit fall state `5050`. The gate confirms a defender hit by a fall `HitDef` with `fall.recover = 1` and without `p2stateno` routes `5000 -> 5030 -> 5050 -> 5200 -> 5201 -> 52 -> 0`, requires active `x` and `recovery` command evidence, observes positive-to-zero `fall.recovertime` before `5200`, executes ordered ground-recovery controller/typed operations, and forbids generic air-recovery state `5210` plus lie-down chain states `5100`/`5101`/`5110`/`5120`. Current `pnpm qa:trace` aggregate is 295/295 artifacts, 265 required and 30 optional after the optional KFM crouch guard-hold crouch-control gate. Claim allowed: bounded Common1-style near-ground recovery selects `5200/5201/52` over generic `5210` air recovery when both recovery input and near-ground predicates are true. Claim blocked: exact ground/air recovery arbitration thresholds, recovery tables, velocity math, exact Common1 controller-loop timing, visual/audio parity, score movement, and full MUGEN/IKEMEN fall/recovery parity.
- Previous completed R1 Common1 recovery-input priority cut: required `synthetic-imported-hitfall-recovery-input-priority.json` checksum `bae07bde` gates bounded `command = "recovery"` priority over a competing `HitFall && CanRecover` probe in default get-hit fall state `5050`. The gate confirms a defender hit by a fall `HitDef` with `fall.recover = 1` and without `p2stateno` routes `5000 -> 5030 -> 5050 -> 5210 -> 0`, requires active `x` and `recovery` command evidence, executes ordered `HitVelSet`, typed `kinematic:hitvelset`, `VelAdd`, recovery `VelSet`, typed `kinematic:velset`, `HitFallSet`, and typed `hitfall:hitfallset`, and forbids probe/landing states `5250`/`5200`. `pnpm qa:trace` passes 288/288 artifacts, 262 required and 26 optional. Claim allowed: bounded Common1-style fall recovery input wins over a same-state CanRecover-ready probe when `fall.recovertime` reaches zero. Claim blocked: exact recovery threshold tables, ground/air recovery-input arbitration, velocity math, exact Common1 controller-loop timing, visual/audio parity, score movement, and full MUGEN/IKEMEN fall/recovery parity.
- Previous completed R1 Common1 CanRecover-ready cut: required `synthetic-imported-hitfall-canrecover-ready.json` checksum `c0097d7f` gates a bounded synthetic `HitFall && CanRecover` branch. The gate confirms a defender hit by a fall `HitDef` with `fall.recover = 1` and without `p2stateno` routes `5000 -> 5030 -> 5050 -> 5250 -> 0`, observes `fall.recovertime` drop from positive to zero in `5050`, executes ordered `HitVelSet`, typed `kinematic:hitvelset`, `VelAdd`, and named `HitFall CanRecover Ready Probe` `ChangeState`, excludes active `recovery` command evidence, and forbids recovery states `5210`/`5200`. `pnpm qa:trace` passes 287/287 artifacts, 261 required and 26 optional. Claim allowed: bounded Common1-style CanRecover-ready fall trigger routing is trace-gated. Claim blocked: exact recovery threshold tables, exact Common1 controller-loop timing, recovery-input arbitration, velocity math, visual/audio parity, score movement, and full MUGEN/IKEMEN fall/recovery parity.
- Previous completed R2 active-controller ownership cut: `RuntimeActiveControllerHookSetWorld` now owns bounded active-controller hook-set construction outside `PlayableMatchRuntime`. The match runtime still owns concrete worlds, tick/stage context, telemetry, and callback closures, but state mutation hooks, side-effect controller hooks, and fallback runtime-controller hooks now group behind one named boundary before `RuntimeActiveControllerRunWorld` executes. Focused coverage proves every current hook route is preserved and optional unsupported-hook omission stays exact. Claim allowed: current active-controller hook-set construction has a named, testable ownership boundary. Claim blocked: new controller semantics, exact hook ordering parity, helper/team/redirect scopes, unsupported-controller breadth, visual/audio parity, score movement, and full CNS VM parity.
- Previous completed R2 MatchWorld ownership cut: `RuntimeMatchPausedBridgeWorld` now owns the bounded match-level bridge into `RuntimePausedMatchWorld.advanceRuntime(...)` outside `PlayableMatchRuntime`. The match runtime still owns concrete input, fighter, pause, stage, and lifecycle worlds, but pause snapshot lookup, source-movetime eligibility, pause tick mutation, hitpause-style command buffering, stage/time metadata, actor-constraint/effect-lifecycle forwarding, and paused player/AI/fighter callbacks now pass through one named boundary before paused-match ordering runs. Focused coverage proves the bridge payload and callback hooks. Claim allowed: current paused-match runtime bridge has a named, testable ownership boundary. Claim blocked: new Pause/SuperPause semantics, exact pause layering, helper/team/redirect pause ownership, pause/hitpause command parity, visual/audio parity, score movement, and full paused-match VM parity.
- Previous completed R2 MatchWorld ownership cut: `RuntimeMatchPreFacingAssertSpecialWorld` owns the bounded pre-facing imported `AssertSpecial` trigger/dispatch context bridge outside `PlayableMatchRuntime`. The match runtime still owns concrete worlds, tick order, and stage state, but trigger gating now receives stage bounds through the named bridge and controller execution context creation forwards owner constants, actor hitpause, actor random, stage bounds, and stage time before auto-facing. Focused coverage proves the bridge payload and dispatch context. Claim allowed: current pre-facing imported `AssertSpecial` bridge has a named, testable ownership boundary. Claim blocked: new `AssertSpecial` flags, exact lifetime/global/team/helper ownership, pause/hitpause parity, visual/audio parity, score movement, and full match-frame VM parity.
- Latest completed R1 optional official KFM movement cut: `kfm-official-basic-movement.json` checksum `ef30066c` proves the private official KFM fixture can enter walk state/action `20`, crouch prep `11`, airborne jump `41`, and final idle/control `0` from scripted `F`, `D`, and `U` direction input. The gate requires ordered actor-frame evidence `20 -> 11 -> 41` and final `0`/control evidence. `pnpm qa:trace` passes 286/286 artifacts, 260 required and 26 optional when the private fixture exists. Claim allowed: private-fixture KFM can follow the current bounded movement input route. Claim blocked: public bundled KFM support, exact Common1 movement tables, CMD priority and raw direction-buffer parity, landing/collision/platform parity, AI behavior, visual parity, score movement, and full movement parity.
- Previous completed R1 imported movement cut: required `synthetic-imported-basic-movement.json` checksum `917ff3e5` remains the portable movement oracle proving the current synthetic imported player input route can enter bounded walk state/action `20`, crouch `10`, airborne jump `40`, and final idle/control `0` from scripted `F`, `D`, and `U` direction input. The gate requires imported actor-frame state/physics/velocity/airborne evidence plus ordered movement evidence `20 -> 10 -> 40`. Claim allowed: current imported player input-control route can enter bounded walk/crouch/jump/idle states. Claim blocked: exact MUGEN/IKEMEN Common1 movement state semantics, CMD command-priority quirks, command-buffer raw direction evidence, landing timing parity, collision/platform physics, AI behavior, visual parity, score movement, and full movement parity.
- Previous completed R1 AssertSpecial/round-flow cut: required `synthetic-imported-assertspecial-roundnotover.json` checksum `342d49f0` proves bounded actor-local `AssertSpecial RoundNotOver` can hold the current active round in `state = fight` / `message = Fight` after a 61-frame timer reaches displayed timer `0`, through typed `assertspecial` operation evidence. `RuntimeMatchRoundWorld.finishIfNeeded(...)` skips KO/time-over finish side effects when the current P1/P2 roster reports `roundNotOver` / `roundnotover`. `pnpm qa:trace` passes 284/284 artifacts, 259 required and 25 optional. Claim allowed: current P1/P2 active match round finish can be held by bounded imported `AssertSpecial RoundNotOver`. Claim blocked: exact winpose ownership, KO slow/sound behavior, lifebar behavior, helper/team/global ownership, pause-layer interaction, intro/round transition semantics, score movement, and full MUGEN/IKEMEN round-flow parity.
- Previous completed R1 AssertSpecial/timer cut: required `synthetic-imported-assertspecial-timerfreeze.json` checksum `408528f1` proves bounded actor-local `AssertSpecial TimerFreeze` can stop the current active fight timer through typed `assertspecial` operation evidence. The route holds a 61-frame fight timer at displayed timer `2` for 70 active frames while the match remains in `state = fight` / `message = Fight`, and `RuntimeMatchRoundWorld` skips timer ticks when the current P1/P2 roster reports `timerFreeze` / `timerfreeze`. `pnpm qa:trace` passes 283/283 artifacts, 258 required and 25 optional. Claim allowed: current P1/P2 active match timer can be frozen by bounded imported `AssertSpecial TimerFreeze`. Claim blocked: exact global/team/helper ownership, pause-layer interaction, lifebar behavior, intro/round transition semantics, timer speed/display parity, score movement, and full MUGEN/IKEMEN timer parity.
- Latest completed R1 stage/presentation cut: bounded stage BG `window`/`maskwindow` clipping handoff is implemented with parser/report/renderer/Studio evidence. Stage DEF `[BG ...]` layers preserve rectangular clip metadata, `StageCompatibilityReport` reports clipped layer coverage, Studio Stage layer diagnostics display the clip, and Three.js clips fallback/asset/SFF sprite placements with UV adjustment instead of source squeezing. Focused tests cover parser/report/projection behavior. Claim allowed: imported/native stage BG layers now have bounded rectangular clipping handoff for `window` and `maskwindow`. Claim blocked: exact `windowdelta`, zoom, endpoint, render-mode, and color-zero `mask` semantics, BGCtrl timing/side effects, motif/screenpack ownership, score movement, and full stage parity.
- Previous completed R1 stage/presentation cut: bounded stage BG transparency handoff is implemented with parser/report/renderer/Studio evidence. Stage DEF `[BG ...]` layers preserve supported `trans = none/add/add1/addalpha/sub` plus optional `alpha = src,dst`, `StageCompatibilityReport` reports that supported subset without flagging it unsupported, Studio Stage layer diagnostics display the bounded trans summary, and Three.js stage materials map the subset into normal/additive/subtractive blending with bounded `addalpha` opacity. Focused tests cover parser/report/material projection. `pnpm qa:smoke` passed and screenshots `runtime-desktop.png`, `studio-stage.png`, and `studio-stage-bgctrl.png` were manually inspected. Claim allowed: imported/native stage BG layers now have bounded transparency metadata and material handoff. Claim blocked: exact MUGEN blend math, palette interaction, BGCtrl timing/side effects, motif/screenpack ownership, score movement, and full stage parity.
- Latest completed R1 palette/presentation cut: first-pass ACT + indexed SFF `RemapPal` texture handoff is implemented with focused parser/provider/renderer/loader/report evidence. DEF `pal1..pal12` ACT refs load into character palettes, indexed SFF sprites preserve palette-index pixels plus palette bytes, `SffSpriteProvider` rebuilds decoded indexed canvases from loaded ACT destination palettes, `CharacterRenderer` forwards runtime `paletteRemap`, and `CompatibilityReport.palettes` reports palette coverage. Required `synthetic-imported-palfx-remappal.json` checksum `ba5fc1e6` still proves one imported actor can execute static `PalFX` and `RemapPal` together with typed `sprite-effect:palfx` / `sprite-effect:remappal` operation evidence plus combined actor-frame palette telemetry. That checkpoint passed 282/282 artifacts, 257 required and 25 optional. Claim allowed: bounded indexed ACT/SFF destination-palette handoff plus same-actor palette telemetry for `PalFX` and `RemapPal`. Claim blocked: exact source-bank semantics, truecolor/PNG remap, exact MUGEN/IKEMEN palette math, `sinadd`, blend order, renderer visual parity, helper/redirect palette ownership, score movement, and full palette/presentation parity.
- Latest completed R1 QA hardening cut: required `synthetic-imported-assertspecial-unguardable.json` checksum `e84aa12d` remains the bounded attacker-side `AssertSpecial Unguardable` oracle, and `scripts/qa_traces.cjs` now also lists `synthetic-imported-assertspecial-unguardable` in the required artifact coverage-summary contract. `pnpm qa:trace` passes 281/281 artifacts, 256 required and 25 optional. Claim allowed: the existing attacker-side unguardable oracle is protected by required artifact execution plus required coverage-summary presence. Claim blocked: exact `AssertSpecial` priority, lifetime/persistence layering, pause interaction, helper/team/global ownership, KFM/Common1 confirmation, guard effects, visual parity, score movement, and full MUGEN/IKEMEN guard parity.
- Latest completed R1 audio abspan telemetry cut: required `synthetic-imported-sound.json` checksum `cc9c8c49` remains required and now gates static `PlaySnd abspan = -64` -> `absPan: -64` in required sound-event rules, trace evidence, and final actor sound-event telemetry beside existing `legacyVolume: -8`, `lowPriority`, `volumeScale`, `freqMul`, `loop`, `pan`, `SndPan`, and `StopSnd` coverage. Focused tests cover compiler/runtime event projection, and `pnpm qa:trace` passes 281/281 artifacts, 256 required and 25 optional. Claim allowed: static absolute `PlaySnd abspan` is preserved as bounded diagnostic/playback telemetry through compiler/runtime/trace. Claim blocked: dynamic pan params, exact IKEMEN/MUGEN panning semantics, pre-RC8 legacy volume gain semantics, exact audio priority classes, global channel fallback, pause/superpause audio, timing/mixing, visual/audio parity, score movement, and full MUGEN/IKEMEN audio parity.
- Previous completed R1 audio legacy-volume telemetry cut: required `synthetic-imported-sound.json` checksum `91574367` gated static `PlaySnd volume = -8` -> `legacyVolume: -8` in required sound-event rules, trace evidence, and final actor sound-event telemetry. Focused tests cover compiler/runtime event projection, and `pnpm qa:trace` passed 281/281 artifacts, 256 required and 25 optional. Claim allowed: static legacy `PlaySnd volume` is preserved as bounded diagnostic telemetry through compiler/runtime/trace. Claim blocked: pre-RC8 legacy volume gain semantics, exact audio priority classes, dynamic sound params, global channel fallback, pause/superpause audio, timing/mixing, visual/audio parity, score movement, and full MUGEN/IKEMEN audio parity.
- Latest completed R2 presentation snapshot cut: `RuntimeMatchPresentationSnapshotWorld` now owns bounded match presentation snapshot input construction outside `PlayableMatchRuntime`. Camera shake, stage flash, and P1/P2 effect snapshot groups route through one seam before `RuntimeSnapshotWorld.match()` builds the renderer-independent snapshot. Focused `RuntimeMatchPresentationSnapshotSystem` coverage proves shake/flash/effect-group forwarding and P1/P2 ordering. Claim allowed: current match presentation snapshot input construction has a named, testable ownership boundary. Claim blocked: exact stage/motif camera logic, effect lifecycle semantics, renderer/audio parity, visual/debug UI parity, score movement, and full match snapshot parity.
- Previous completed R2 active-controller telemetry cut: `RuntimeActiveControllerTelemetryWorld` owns bounded active-controller telemetry hook construction outside `PlayableMatchRuntime`. Active state hooks, side-effect dispatchers, and fallback runtime-controller dispatch share one controller/operation hook set before forwarding into `RuntimeCompatibilityTelemetryWorld`. Focused `RuntimeActiveControllerTelemetrySystem` coverage proves controller and operation forwarding through the seam. Claim allowed: current active-controller telemetry hook construction has a named, testable ownership boundary. Claim blocked: exact telemetry event semantics, imported-only filtering, event retention limits, helper/team/redirect telemetry breadth, visual/debug UI parity, score movement, and full CNS VM parity.
- Previous completed R2 combat/helper state-hook cut: `RuntimeMatchCombatStateHooksWorld` owns bounded combat state-hook adapter construction outside `PlayableMatchRuntime`. Direct/projectile combat hooks preserve state-owner availability and entry options, while helper combat hooks keep self-owned availability checks and still forward entry options. Focused `RuntimeMatchCombatStateHooksSystem` coverage proves both contracts; the match runtime now builds both hook sets through the seam before combat bridge handoff. Claim allowed: current combat/helper state-hook adapter construction has a named, testable ownership boundary. Claim blocked: helper-owned custom-state table breadth, throws, teams/simul actor registries, multi-target helper ownership, exact combat/helper tick order, visual/audio parity, score movement, and full combat/helper VM parity.
- Latest completed R2 match actor roster cut: `RuntimeMatchActorRosterWorld` now owns bounded current P1/P2 actor roster projection for `PlayableMatchRuntime`. Helper-owned `TargetState` actor lookup, imported compatibility session projection, and effect-store owner summaries consume that roster boundary instead of rebuilding raw actor arrays or id maps locally. Focused `RuntimeMatchActorRosterSystem` coverage proves stable P1/P2 order, live reference preservation, id lookup, mirrored one-on-one opponent projection, and fail-closed unknown actors. Claim allowed: current match actor roster projection has a named, testable ownership boundary. Claim blocked: real teams/simul roster ownership, helper-owned actor discovery, dynamic roster mutation, richer identity metadata, exact VM scheduling, visual/audio parity, score movement, and full actor registry parity.
- Previous completed R2 match opponent context cut: `RuntimeMatchOpponentContextWorld` owns bounded current 1v1 match-opponent context construction for active/pause/hitpause lifecycle bridges outside `RuntimeMatchInteractionWorld`, `RuntimePausedMatchWorld`, and `RuntimeHitPauseWorld`. Mirrored P1/P2 opponent selection, singleton lifecycle `opponents` list projection, and unknown-actor fail-closed behavior now route through one named seam. Focused `RuntimeMatchOpponentContextSystem` coverage proves mirrored contexts and fail-closed behavior; existing match/pause/hitpause coverage proves active, pause, and hitpause callers still forward the direct opponent plus explicit one-opponent lifecycle list. Claim allowed: current concrete 1v1 lifecycle opponent context has a named, testable ownership boundary. Claim blocked: real teams/simul roster ownership, automatic multi-opponent match discovery, helper-owned opponent roster discovery, richer identity beyond actor refs, exact helper lifecycle/pause/combat ordering, visual/audio parity, score movement, and full match/helper VM parity.
- Previous completed R2 helper/effect lifecycle context cut: `RuntimeEffectHelperContextWorld` owns bounded visual Helper lifecycle context construction outside `RuntimeEffectLifecycleWorld`. Complete-owner validation, parent/root runtime-state projection, current opponent id/state fallback, explicit lifecycle opponent-list to nearest-order helper `opponentRoster` projection, explicit roster override, target-candidate forwarding, and helper `TargetState` / telemetry hook forwarding now route through one named seam. Focused `RuntimeEffectHelperContextSystem` coverage proves nearest roster ordering, explicit roster preservation, target/hook forwarding, and incomplete-owner fail-closed behavior; existing `EffectLifecycleSystem` coverage proves active/paused lifecycle callers still forward context. Claim allowed: current helper/effect lifecycle context construction has a named, testable ownership boundary. Claim blocked: real teams/simul lifecycle roster ownership, automatic multi-opponent match discovery, helper-owned opponent roster discovery, richer identity beyond ids/runtime state, exact helper lifecycle/pause/combat ordering, visual/audio parity, score movement, and full Helper VM parity.
- Previous completed R2 helper Projectile target bridge cut: `RuntimeMatchHelperProjectileTargetWorld` owns bounded match-level helper-parented Projectile target-memory callback wiring outside `PlayableMatchRuntime`. The normal post-fighter combat route forwards owner, defender, projectile, and `RuntimeTargetWorld` through one named seam before lower `RuntimeHelperProjectileTargetWorld` target-memory logic runs. Focused `RuntimeMatchHelperProjectileTargetSystem` coverage proves forwarding and owner-projectile fail-closed behavior. Claim allowed: current helper-parented Projectile target-memory bridge has a named, testable match boundary. Claim blocked: helper-owned Projectile contact timing, helper-owned custom-state tables, teams/simul actor registries, multi-target helper ownership, exact target lifetime, visual/audio parity, score movement, and full Helper/Projectile VM parity.
- Latest completed R1 EnvColor under-layer cut: required `synthetic-imported-envcolor-under.json` checksum `0a7b5c96` gates bounded imported `EnvColor` under-layer evidence. The gate confirms static `value = 16,96,255`, `time = 12`, and `under = 1` lower into typed `envcolor` operation evidence and reach stage-frame flash telemetry with `envColorUnder = true`; previous `synthetic-imported-envcolor.json` checksum `956b0f4b` remains the `under = 0` route. `pnpm qa:trace` now passes 281/281 artifacts, 256 required and 25 optional. Claim allowed: bounded EnvColor stage-flash layer-flag handoff is required for both upper and under routes. Claim blocked: exact blend math, layer/window ordering, pause timing, renderer parity, visual parity, score movement, and full MUGEN/IKEMEN presentation parity.
- Previous completed R1 Common1 HitFall recover-true cut: required `synthetic-imported-hitfall-recover-true.json` checksum `f1e3424a` gates a bounded synthetic `HitFall && GetHitVar(fall.recover) && !CanRecover` branch. The gate confirms a defender hit by a fall `HitDef` with `fall.recover = 1` and without `p2stateno` routes `5000 -> 5030 -> 5050 -> 5240 -> 0`, executes ordered `HitVelSet`, typed `kinematic:hitvelset`, `VelAdd`, and named `HitFall Recover Enabled Probe` `ChangeState`, requires positive `fall.recovertime` metadata in `5050` and `5240`, and forbids recovery states `5210`/`5200`. That checkpoint passed 280/280 artifacts, 255 required and 25 optional. Claim allowed: bounded Common1-style recover-enabled-but-not-yet-recoverable fall trigger routing is trace-gated. Claim blocked: exact recovery threshold tables, exact Common1 controller-loop timing, recovery arbitration, fall/CanRecover precedence, visual/audio parity, score movement, and full MUGEN/IKEMEN fall/recovery parity.
- Previous completed R1 Common1 HitFall recover-disabled cut: required `synthetic-imported-hitfall-recover-false.json` checksum `236df0a8` gates a bounded synthetic `HitFall && !GetHitVar(fall.recover) && !CanRecover` branch. The gate confirms a defender hit by a fall `HitDef` with `fall.recover = 0` and without `p2stateno` routes `5000 -> 5030 -> 5050 -> 5230 -> 0`, executes ordered `HitVelSet`, typed `kinematic:hitvelset`, `VelAdd`, and named `HitFall Recover Disabled Probe` `ChangeState`, requires positive `fall.recovertime` metadata in `5050` and `5230`, and forbids recovery states `5210`/`5200`. Claim allowed: bounded Common1-style recover-disabled fall trigger routing is trace-gated. Claim blocked: exact recovery threshold tables, exact Common1 controller-loop timing, recovery arbitration, fall/CanRecover precedence, visual/audio parity, score movement, and full MUGEN/IKEMEN fall/recovery parity.
- Previous completed R1 Common1 HitFall false cut: required `synthetic-imported-hitfall-false.json` checksum `1d538e43` gates a bounded synthetic normal get-hit `!HitFall && !GetHitVar(fall)` branch. The gate confirms a defender hit by a direct non-fall `HitDef` without `p2stateno` routes `5000 -> 325`, executes named `Normal HitFall False Probe` `ChangeState`, requires actor-frame order `5000 -> 325`, ends P2 in state `325` / moveType `H`, and forbids fall/recovery states `5001`, `5030`, `5050`, `5210`, and `5200`. Claim allowed: bounded Common1-style `!HitFall` / `!GetHitVar(fall)` trigger routing is trace-gated. Claim blocked: exact normal get-hit timing, fall arbitration, custom-state/helper/team inheritance, visual/audio parity, score movement, and full MUGEN/IKEMEN get-hit parity.
- Previous completed R1 Common1 HitFall/CanRecover cut: required `synthetic-imported-hitfall-canrecover.json` checksum `7cf7ab46` gates a bounded synthetic `HitFall && !CanRecover` branch. The gate confirms a defender hit by a fall `HitDef` without `p2stateno` routes `5000 -> 5030 -> 5050 -> 5220 -> 0`, executes ordered `HitVelSet`, typed `kinematic:hitvelset`, `VelAdd`, and named `HitFall CanRecover Probe` `ChangeState`, requires positive `fall.recovertime` evidence in `5050` and `5220`, and forbids recovery states `5210`/`5200`. That checkpoint passed 277/277 artifacts, 252 required and 25 optional. Claim allowed: bounded Common1-style `HitFall` true / `CanRecover` false trigger routing is trace-gated. Claim blocked: exact recovery threshold tables, exact Common1 controller-loop timing, recovery arbitration, visual/audio parity, score movement, and full MUGEN/IKEMEN fall/recovery parity.
- Previous completed R1 Common1 air guard landing cut is now tightened above: required `synthetic-imported-air-guard-landing.json` checksum `d6986d7f` still gates the bounded landing handoff, but now also requires ordered `154 -> 155 -> 52 -> 20` actor-frame evidence and final walk/control `20`.
- Previous completed optional R1 KFM crouch guard slide-stop cut: `kfm-official-default-crouch-guard-slide-stop.json` checksum `d11153d0` now passes when private `.scratch/fixtures/kfm-official.zip` exists. The gate confirms real KFM/Common1 crouch guard-hit state `153` executes ordered `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, and final `ChangeState` after direct guarded contact while holding down-back. Current `pnpm qa:trace` now passes 281/281 artifacts, 256 required and 25 optional. Claim allowed: private official KFM fixture confidence for bounded crouch guard slide-stop/control order. Claim blocked: public KFM support, exact guard timing, proximity guard, guard effects, public `130` guard-hold return, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity.
- Previous completed R1 Common1 crouch guard slide-stop cut: required `synthetic-imported-crouch-guard-slide-stop.json` checksum `2bea7311` gates bounded defender-owned crouch guard-hit routing through `152 -> 153 -> 130` after direct guarded contact. The gate requires active `holddown` / `x`, ordered `153` `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, and final `ChangeState`, plus actor-frame velocity-before-stop/control telemetry. Claim allowed: bounded Common1-style crouch guard slide-stop/control order is trace-gated. Claim blocked: exact guard timing, proximity guard, guard effects, air slide-stop parity, controller-loop tick parity, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity.
- Previous completed R1 audio precision cut: static `PlaySnd lowpriority = 1`, `volume = -8`, `volumescale = 50`, `freqmul = 0.5`, `loop = 1`, `pan = 32`, and `SndPan channel = 2, pan = -48` now lower into typed `audio:playsnd` / `audio:sndpan` metadata, survive runtime event projection as `RuntimeSoundEvent.lowPriority` / `legacyVolume` / `volumeScale` / `freqMul` / `loop` / `pan`, and are required by `synthetic-imported-sound.json` checksum `91574367` beside `PlaySnd channel = 2` and `StopSnd channel = 2`. Focused tests also prove static `abspan` lowering/event projection and bounded pan normalization. `MugenAudioSystem` now resolves explicit-channel actions through a named pure boundary, routes `volumeScale` through a pure gain boundary, routes `freqMul` through a pure playback-rate boundary, maps `loop` to Web Audio source looping, routes `pan` / `abspan` through a pure stereo-pan boundary, tracks unchannelled sources for stop-all cleanup, and applies `SndPan` to active explicit channel panners: normal `PlaySnd` replaces, low-priority `PlaySnd` skips an already active explicit channel, `SndPan` updates active channel panning, and `StopSnd channel = -1` / omitted channel stops tracked channelled and unchannelled sources. Legacy `volume` is preserved as diagnostics but ignored by playback. Claim allowed: bounded static low-priority sound metadata, legacy-volume diagnostic telemetry, explicit-channel skip/replace/pan/stop-all arbitration, bounded `volumescale` gain handoff, bounded `freqmul` playback-rate handoff, bounded loop handoff, and bounded actor/camera-aware `pan` plus absolute `abspan` handoff. Claim blocked: exact priority classes, dynamic sound params, pre-RC8 legacy volume gain semantics, global channel fallback, pause/superpause audio, timing/mixing, visual/audio parity, score movement, and full MUGEN/IKEMEN audio parity.
- Latest completed R1 FightFX prefix package cut: imported DEF `[Info] fightfx.prefix` now flows into runtime fighter `fightFxPrefix`, F-prefixed FightFX asset frames, `HitSpark` events, F-prefixed sound refs, `soundPrefix` audio/trace telemetry, and `RuntimeTrace.requiredHitEffectEvents`; character `[Files] fx = ...` entries now load IKEMEN-style FightFX DEF `[Info] prefix` packages with AIR/SFF/SND assets; and matching prefixes are preferred for runtime `F` spark frames and `F` sound refs before the global `data/fightfx.*` fallback. Required `synthetic-imported-hitdef-fightfx-spark.json` remains checksum `11537b56` and gates `fightFxPrefix = kfm` evidence beside selected AIR frame/multi-frame metadata; required `synthetic-imported-hitdef-hit-effect-package.json` remains checksum `46aa5ce1` and now gates `hitsound = F5,0` with `soundPrefix = kfm` beside FightFX spark metadata. Claim allowed: character FightFX prefix package selection and prefixed-SND lookup survive through the bounded loader/runtime/provider handoff. Claim blocked: exact `sys.ffx` lifetime/refcount/cache semantics, channel fallback, motif/screenpack ownership, sprite lookup parity, visual/audio timing/layering/scale/palette/mixing parity, helper/team/custom-state presentation ownership, and full hit-effect parity.
- Previous completed R2 helper binding cut: `RuntimeMatchHelperBindingWorld` now owns bounded match-level helper callback wiring outside `PlayableMatchRuntime`: helper-owned `TargetState` owner handlers and helper-local Projectile telemetry handlers attach through one named seam, while target entry still delegates to `RuntimeMatchHelperTargetStateWorld` and telemetry filtering still delegates to `RuntimeHelperTelemetryWorld`. Focused `RuntimeMatchHelperBindingSystem` coverage proves owner-specific target-state route forwarding, stale handler replacement, helper-state/owner-state telemetry attribution, and non-Projectile telemetry ignore behavior. Claim allowed: current match-level helper callback wiring has a named, testable ownership boundary. Claim blocked: helper-owned custom-state tables, throws, teams/simul actor registries, multi-target helper ownership, exact helper TargetState/projectile timing, broad helper telemetry semantics, visual/audio parity, score movement, and full Helper VM parity.
- Previous completed R2 helper TargetState actor-resolution cut: `RuntimeMatchHelperTargetStateWorld` now owns bounded match-roster target resolution for helper-owned `TargetState` entry outside `PlayableMatchRuntime`: helper owner validation still routes through `RuntimeHelperTargetStateWorld`, but actor roster lookup, state availability hook, and state entry hook route through one named match-level seam. Focused `RuntimeMatchHelperTargetStateSystem` coverage proves roster-backed target resolution, stale target payload isolation, missing-target no-op behavior, and owner-mismatch fail-closed behavior. Claim allowed: current helper-owned TargetState match actor resolution has a named, testable ownership boundary. Claim blocked: helper-owned custom-state tables, throws, teams/simul actor registries, multi-target helper ownership, exact helper TargetState timing, visual/audio parity, score movement, and full Helper VM parity.
- Previous completed R2 snapshot envelope ownership cut: `RuntimeSnapshotWorld.match()` now owns bounded full match snapshot envelope assembly outside `PlayableMatchRuntime`: selected P1 action, playback/speed/toggles, match pause handoff, stage/camera snapshot, round snapshot, player actor snapshots, effect snapshots, compatibility-session handoff, and 80-line log trimming route through one named boundary. Focused `RuntimeSnapshotSystem` coverage proves envelope fields, actor/effect clone isolation, and log cap behavior. Claim allowed: current snapshot envelope assembly has a named, testable ownership boundary. Claim blocked: snapshot schema changes, compatibility telemetry semantics, exact camera/effect/renderer parity, visual/audio parity, score movement, and full match VM parity.
- Latest completed R2 tick-branch ownership cut: `RuntimeMatchTickBranchWorld` now owns bounded per-tick hitpause / pause / active branch arbitration outside `PlayableMatchRuntime`: hitpause consumes the tick before pause or active, match pause consumes the tick before active when hitpause is clear, and active match runs only when neither pause layer owns the tick. Focused `RuntimeMatchTickBranchSystem` coverage proves branch order. Claim allowed: current per-tick pause-layer branch order has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN pause layering/arbitration, hitpause/pause tick semantics, helper/team scheduling, visual/audio parity, score movement, and full match VM parity.
- Latest completed R2 match-step ownership cut: `RuntimeMatchStepWorld` now owns bounded public match-step cadence outside `PlayableMatchRuntime`: stopped playback snapshots without ticking frame clock, forced stepping advances exactly one tick, sub-1x speed samples on frame-clock divisors, multi-step speed loops advance in order, and round-over checks stop remaining iterations. Focused `RuntimeMatchStepSystem` coverage proves the cadence boundary. Claim allowed: current public match-step cadence has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN pause/round arbitration, frame pacing, replay/rollback timing, helper/team scheduling, visual/audio parity, score movement, and full match VM parity.
- Previous completed R1 trace restoration cut: `synthetic-imported-envshake.json` checksum `061f17d5` is again required by `pnpm qa:trace`. It gates bounded imported `EnvShake` through required `ChangeState`, `EnvShake`, `HitDef`, typed `envshake` operation evidence, and `RuntimeEnvShakeEvent` telemetry for P1 state `200` with `time = 16`, `freq = 30`, `ampl = -7`, and `phase = 0.5`. Current aggregate after later presentation/Common1 gates is 281/281 artifacts, 256 required and 25 optional. Claim allowed: existing imported `EnvShake` telemetry remains wired into the required trace pipeline. Claim blocked: exact camera waveform, pause/stage/layer interaction, helper/redirect ownership, visual/audio parity, score movement, and full MUGEN/IKEMEN presentation parity.
- Latest R2 match active ownership cut: `RuntimeMatchActiveWorld` now owns bounded normal active-match orchestration outside `PlayableMatchRuntime` after hitpause/pause gates: round timer tick, normal command-buffer writes, input control, fighter advance, post-fighter interaction/combat, and round-finish handling route through one named boundary. Focused `RuntimeMatchActiveSystem` coverage proves order and sub-world result preservation. Claim allowed: current normal active-match orchestration order has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN active tick order, pause-start arbitration, helper/team/redirect actor ownership, combat priority parity, visual/audio parity, score movement, and full match VM parity.
- Previous R2 match hitpause ownership cut: `RuntimeMatchHitPauseWorld` now owns bounded normal active-match hitpause handoff outside `PlayableMatchRuntime`: `RuntimeHitPauseWorld.advanceRuntime` delegation, P1/P2 inputs, stage/runtime ticks, paused presentation lifecycle, and ignorehitpause controller execution route through one named boundary before the normal pause branch. Focused `RuntimeMatchHitPauseSystem` coverage proves delegate payload forwarding and paused/not-paused result preservation. Claim allowed: current normal active-match hitpause handoff has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN hitpause tick order, pause arbitration/layering, helper/team/redirect hitpause ownership, visual/audio parity, score movement, and full match VM parity.
- Previous R2 match frame-start ownership cut: `RuntimeMatchFrameStartWorld` now owns bounded normal active-match frame-start ordering outside `PlayableMatchRuntime`: per-frame hit/assert flag reset, pre-facing `AssertSpecial`, and auto-facing route through one named boundary before hitpause/pause handling. Focused `RuntimeMatchFrameStartSystem` coverage proves reset-before-assert and assert-before-facing order for both players. Claim allowed: current normal active-match frame-start ordering has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN frame-start tick order, pause/hitpause arbitration, helper/team/redirect frame-start ownership, visual/audio parity, score movement, and full match VM parity.
- Previous R2 match post-fighter ownership cut: `RuntimeMatchPostFighterWorld` owns bounded normal active-match post-fighter bridge wiring outside `PlayableMatchRuntime`: combat resolver construction and `RuntimeMatchInteractionWorld.advanceRuntime` handoff route through one named boundary. Focused `RuntimeMatchPostFighterSystem` coverage proves target memory, effect lifecycle, projectile clash, actor constraints, target bindings, direct/projectile/helper combat, clamps, and presentation effect forwarding after resolver construction. Claim allowed: current normal active-match post-fighter combat resolver construction plus match-interaction runtime handoff has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN post-fighter tick order, combat priority parity, projectile/helper contact timing, helper/team/redirect ownership, target lifetime parity, visual/audio parity, score movement, and full match VM parity.
- Previous R2 match input-control ownership cut: `RuntimeMatchInputControlWorld` now owns bounded normal active-match P1/P2-controlled/simple-AI input dispatch outside `PlayableMatchRuntime`: P1 player input, controlled P2 player input, and uncontrolled P2 simple-AI fallback route through one named boundary after normal command-buffer writes. Focused `RuntimeMatchInputControlSystem` coverage proves P1-before-controlled-P2 ordering, mirrored opponent arguments, and P1-before-AI fallback ordering. Claim allowed: current normal active-match P1/P2-controlled/simple-AI input dispatch order has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN input priority, command timing, input-conflict resolution, pause/hitpause command parity, helper/team/redirect command ownership, AI parity, visual/audio parity, score movement, and full input VM parity.
- Previous R2 match round ownership cut: `RuntimeMatchRoundWorld` now owns bounded active-match round timer delegation plus finish side effects outside `PlayableMatchRuntime`: timer tick routes through the boundary, and KO/time-over finish routes match stop plus log emission through one named world. Focused `RuntimeMatchRoundSystem` coverage proves timer delegation, finish stop/log mutation, and no-finish no-op behavior. Claim allowed: current active-match round timer/finish side effects have a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN round-flow timing, intros/winposes, KO slowdown, continue flow, teams/simul/turns, lifebar/screenpack behavior, visual/audio parity, score movement, and full round VM parity.
- Previous R2 match fighter-advance ownership cut: `RuntimeMatchFighterAdvanceWorld` now owns bounded active 1v1 fighter-advance orchestration outside `PlayableMatchRuntime`: P1 advance, P2 auto-guard start, pause-gated P2 advance, and P1 auto-guard start route through one named world. Focused `RuntimeMatchFighterAdvanceSystem` coverage proves normal P1/P2 ordering and pause-after-P1 skip behavior. Claim allowed: current active 1v1 fighter-advance ordering has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN player tick order, pause-start arbitration, teams/simul roster advance, helper/team/redirect actor advance semantics, guard-start parity, visual/audio parity, score movement, and full match VM parity.
- Previous R2 pause-controller ownership cut: `RuntimeMatchPauseControllerWorld` owns bounded Pause/SuperPause controller result side effects outside `PlayableMatchRuntime`: pause application routes through `RuntimePauseWorld`, SuperPause power delta is applied through an injected resource hook, and the existing match log line is emitted through one named world. Focused `PauseSystem` coverage proves tick/controller/op forwarding, power-delta handoff, log emission, and zero-length no-side-effect behavior. Claim allowed: current Pause/SuperPause result side effects have a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN pause layering, SuperPause background/effects/sound timing, helper/team/redirect pause ownership, pause/hitpause command parity, exact paused side-effect tick order, visual/audio parity, score movement, and full pause VM parity.
- Latest R2 combat-bridge ownership cut: `RuntimeMatchCombatBridgeWorld` now owns bounded combat resolver construction outside `PlayableMatchRuntime`: priority clash, direct combat, projectile combat, and helper combat callbacks route through one named world before `RuntimeMatchInteractionWorld.advanceRuntime`. Focused `RuntimeMatchCombatBridgeSystem` coverage proves priority/direct/projectile/helper route wiring, hurtbox callback forwarding, projectile target-memory callback forwarding, and log forwarding. Claim allowed: current match interaction combat callback construction has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN direct-combat priority, helper-owned combat/contact timing, projectile hit/cancel timing, teams/simul/multi-target combat breadth, visual/audio parity, score movement, and full combat VM parity.
- Latest R2 move-start ownership cut: `RuntimeMoveStartWorld` now owns bounded native/imported state-move startup outside `PlayableMatchRuntime`: selected `currentMove`, `currentMoveLabel`, `moveTick`, `hasHit`, reversal cleanup, attack `moveType`, control handoff, and authored action state-entry handoff route through a named world. Focused `RuntimeMoveStartSystem` coverage proves selected move metadata/reset behavior and control-before-state-entry hook order. Claim allowed: current move startup has a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN command timing, cancel windows, combo/input priority, helper/team/redirect move startup, persistent-controller timing, visual parity, score movement, and full move VM parity.
- Latest R2 match tick input ownership cut: `RuntimeMatchTickInputWorld` now owns bounded normal-match input/tick stamping outside `PlayableMatchRuntime`: actor `compatibilityTick`, cloned `currentInput`, and normal non-hitpause command-buffer writes route through a named world, while pause and hitpause buffering remain in `RuntimePausedMatchWorld` / `RuntimeHitPauseWorld`. Focused `RuntimeMatchTickInputSystem` coverage proves clone isolation, actor tick stamping, normal command-buffer writes without `hitPause`, and separation from pause/hitpause buffering. Claim allowed: current normal tick input stamping and non-hitpause command buffering have a named, testable ownership boundary. Claim blocked: exact MUGEN/IKEMEN command timing, input conflict priority, pause/hitpause command parity, helper/team/redirect command ownership, visual parity, score movement, and full input VM parity.
- Latest R2 helper telemetry ownership cut: `RuntimeHelperTelemetryWorld` now owns bounded helper-local Projectile controller/op telemetry binding outside `PlayableMatchRuntime`: owner callbacks are installed through a named world, only `Projectile` helper controller/operation events are forwarded, and helper-state attribution falls back to the owner state when the helper has no current state. Focused `RuntimeHelperTelemetrySystem` coverage proves projectile controller/op recording, owner-state fallback, non-projectile ignore behavior, and stale handler replacement. Claim allowed: helper-local Projectile telemetry binding has a named, testable ownership boundary. Claim blocked: exact helper Projectile tick timing, helper-owned Projectile combat/contact presentation, teams/simul helper telemetry, broader helper telemetry semantics, visual/audio parity, score movement, and full Helper VM parity.
- Previous completed required R1 cut: `synthetic-imported-default-guard-slide-stop.json` checksum `a9663641` is required and proves bounded defender-owned stand guard-hit routing through `150 -> 151 -> 130` after direct guarded contact. The gate requires `150` guard-shake handoff, then ordered `151` `HitVelSet` / `kinematic:hitvelset`, `VelSet` / `kinematic:velset`, `CtrlSet` / `resource:ctrlset`, and final `ChangeState`, plus actor-frame velocity evidence before stop/control. That required checkpoint passed `pnpm qa:trace` at 271/271 artifacts, 248 required and 23 optional. Claim allowed: bounded stand Common1-style guard slide-stop/control order is trace-gated. Claim blocked: exact guard timing, proximity guard, guard effects, crouch/air parity, controller-loop tick parity, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity.
- Previous optional R1 KFM stand fixture cut: `kfm-official-default-guard-slide-stop.json` checksum `885bb1da` passes when private `.scratch/fixtures/kfm-official.zip` exists. The newer `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da` adds actor-frame sequence evidence that the same real KFM/Common1 stand guard-hit route `150 -> 151` returns through observable guard-hold `130` and then resumes held-back walking `20` with control. Claim allowed: private official KFM fixture confidence for bounded stand guard slide-stop/control order plus observable guard-hold-to-walk return. Claim blocked: public KFM support, exact guard-hold duration, exact guard timing, proximity guard, guard effects, air parity, visual/audio parity, score movement, and full MUGEN/IKEMEN guard parity.
- Previous completed required R1 cut: `synthetic-imported-gethitvar-snap.json` checksum `312a53fc` is required and proves bounded owner-backed get-hit CNS can branch from `5100` into state/action `288` through `GetHitVar(xoff) = 16 && GetHitVar(yoff) = -24 && GetHitVar(zoff) = 0` after direct `HitDef snap = 16,-24` contact. The cut adds `RuntimeGetHitVars.hitOffset`, typed/imported `HitDef snap` preservation, direct-combat hit-var copies, and simple attacker-relative direct-contact positioning when snap metadata is present. That checkpoint passed 322/322 artifacts, 292 required and 30 optional. Claim allowed: direct-contact HitDef snap metadata is available through `GetHitVar(xoff/yoff/zoff)` in an owner-backed get-hit CNS route, and direct combat applies a bounded attacker-relative snap. Claim blocked: exact throw positioning, z-axis support, guard snap behavior, helper/projectile/custom-state inheritance breadth, teams/simul, exact tick order, visual/audio parity, score movement, and full MUGEN/IKEMEN throw/get-hit parity.
- Previous completed required R1 cut: `synthetic-imported-gethitvar-velocity.json` checksum `878a03f7` is required and proves bounded defender-owned normal get-hit CNS can branch from `5000` into state/action `324` through `GetHitVar(xvel) = 4 && GetHitVar(yvel) = -2 && !GetHitVar(fall) && !GetHitVar(guarded)` after direct `HitDef` contact. That checkpoint passed 270/270 artifacts, 247 required and 23 optional. Claim allowed: direct-contact normal-hit velocity metadata is available through `GetHitVar(xvel/yvel)` in defender-owned get-hit CNS. Claim blocked: exact velocity lifetime after later physics/controllers, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN get-hit parity.
- Previous completed required R1 cut: `synthetic-imported-gethitvar-damage.json` checksum `2c726114` is required and proves bounded defender-owned normal get-hit CNS can branch from `5000` into state/action `323` through `GetHitVar(damage) = 37 && !GetHitVar(guarded)` after direct `HitDef` contact. Claim allowed: direct-contact normal-hit applied damage metadata is available through `GetHitVar(damage)` in defender-owned get-hit CNS, and direct/projectile contact paths store bounded applied damage in shared runtime hit vars. Claim blocked: exact damage lifetime/rounding, guard-chip semantics, helper/projectile/custom-state inheritance breadth, teams/simul, visual/audio parity, score movement, and full MUGEN/IKEMEN get-hit parity.
- Previous completed required R1 cut: `synthetic-imported-default-crouch-gethit-progression.json` checksum `fd986a9e` is required and proves held-crouch imported defenders can route a direct `HitDef` without `p2stateno` into defender-owned `5010`, advance through `HitShakeOver` into `5011`, and return through `HitOver` to idle/control. That required checkpoint passed 267/267 artifacts, 245 required and 22 optional before later addenda. Claim allowed: bounded crouch Common1-style `5010 -> 5011 -> 0` progression has controller-event, actor-frame, crouch physics, collision, and final-control evidence. Claim blocked: exact MUGEN/IKEMEN tick timing, exact crouch get-hit animation/slide tables, fall routing, custom-state/helper/team breadth, visual/audio parity, score movement, and full Common1 get-hit parity.
- Latest optional R1 KFM fixture cut: `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae` passes when private `.scratch/fixtures/kfm-official.zip` exists. The gate confirms real KFM held-crouch prep state `11`, Common1 `5010 -> 5011 -> 0` through `HitShakeOver` / `HitOver`, final checksum `f469a942`, KFM-specific frame evidence for `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width `39/39`, bounded slide velocity, and ordered KFM crouch slide controller/typed-operation evidence through `5010` `ChangeAnim` / `ChangeState`, `5011` `HitVelSet`, `VelMul`, `VelSet`, `DefenceMulSet`, and final `ChangeState`. Claim allowed: private official KFM fixture confidence for bounded crouch get-hit progression plus current bounded KFM slide controller order. Claim blocked: public KFM support, exact tick timing, exact crouch get-hit animation/slide tables beyond this frame/controller profile, fall routing, custom-state/helper/team breadth, visual/audio parity, score movement, and full Common1 get-hit parity.
- Latest optional R1 KFM fixture cut: `kfm-official-default-air-fall-recovery-input.json` checksum `3bce8aba` and `kfm-official-default-air-fall-recovery-too-early.json` checksum `b199382a` now pass when private `.scratch/fixtures/kfm-official.zip` exists. The gates confirm real KFM air-entry recovery routes through `5020 -> 5030 -> 5035 -> 5050`, valid recovery through `5210 -> 52 -> 0`, and early recovery rejection in `5050` with recovery/landing/ground-impact states forbidden. `pnpm qa:trace` now passes 266/266 artifacts, 244 required and 22 optional. Claim allowed: private official KFM fixture confidence for the bounded air-entry recovery-input and too-early routes. Claim blocked: public KFM support, exact recovery thresholds, velocity math, VM tick-order, visual/audio parity, score movement, and full Common1 recovery parity.
- Latest completed R1 cut: `synthetic-imported-default-air-fall-recovery-too-early.json` checksum `48a2e708` is now required and proves airborne imported defenders can route `5020 -> 5030 -> 5050`, keep `command = "recovery"` active too early, and remain in `5050` while `fall.recovertime` stays positive after a fall `HitDef` without `p2stateno`. `pnpm qa:trace` now passes 264/264 artifacts, 244 required and 20 optional. Claim allowed: bounded air-entry early recovery-input rejection with forbidden `5210`/`5200`/`5201`/`52`/`5100`/`5101`/`5110`/`5120` evidence and `5050` recoverTime `11 -> 6`. Claim blocked: exact MUGEN/IKEMEN recovery threshold tables, exact air get-hit animation, exact velocity math, exact controller-loop timing, recovery arbitration between air/ground branches, visual/audio parity, score movement, and full Common1 recovery parity.
- Previous completed R1 cut: `synthetic-imported-default-air-fall-recovery-input.json` checksum `334a419e` remains required and proves airborne imported defenders can route `5020 -> 5030 -> 5050`, accept `command = "recovery"` after positive-to-zero `fall.recovertime`, enter `5210`, land through `52`, and return to idle/control after a fall `HitDef` without `p2stateno`. That checkpoint passed 263/263 artifacts, 243 required and 20 optional. Claim allowed: bounded air-entry recovery-input route with `5210` air-recovery velocity, `52` landing, and final idle/control evidence. Claim blocked: exact MUGEN/IKEMEN recovery threshold tables, exact air get-hit animation, exact velocity math, exact controller-loop timing, recovery arbitration between air/ground branches, visual/audio parity, score movement, and full Common1 recovery parity.
- Previous completed R1 cut: `synthetic-imported-default-air-liedown-recovery.json` checksum `56a8f236` remains required and proves airborne imported defenders can route through defender-owned Common1-style states `5020 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120 -> 0` after a fall `HitDef` without `p2stateno`, with `5101` `HitFallVel`, `5110` `HitFallDamage`, typed `hitfall:hitfallvel` / `hitfall:hitfalldamage` evidence, and bounded `5110` `downRecoverTime` countdown. That checkpoint passed 262/262 artifacts, 242 required and 20 optional. Claim allowed: bounded air fall get-hit state order plus bounce, lie-down countdown, get-up, and idle-return evidence. Claim blocked: exact air get-hit animation, exact `HitShakeOver` / `HitOver` timing, exact ground-impact timing/position, exact bounce physics, exact lie-down duration tables, recovery input, landing nuance, controller-loop timing, visual/audio parity, score movement, and full Common1 fall/get-hit parity.
- Previous completed R1 cut: `synthetic-imported-default-air-ground-impact.json` checksum `0ba3c80f` remains required and proves airborne imported defenders can route through defender-owned Common1-style states `5020 -> 5030 -> 5050 -> 5100` after a fall `HitDef` without `p2stateno`, with `5100` `HitFallDamage` / `hitfall:hitfalldamage` evidence. That checkpoint passed 261/261 artifacts, 241 required and 20 optional. Claim allowed: bounded air fall get-hit state order plus ground-impact controller evidence. Claim blocked: exact air get-hit animation, exact `HitShakeOver` / `HitOver` timing, exact ground-impact timing/position, bounce physics, lie-down timing, landing, recovery input, controller-loop timing, visual/audio parity, score movement, and full Common1 fall/get-hit parity.
- Previous completed R1 cut: `synthetic-imported-default-air-fall-gethit.json` checksum `1230a2f3` remains required and proves airborne imported defenders can route through defender-owned Common1-style states `5020 -> 5030 -> 5050` after a fall `HitDef` without `p2stateno`. That checkpoint passed 260/260 artifacts, 240 required and 20 optional. Claim allowed: bounded air fall get-hit state order. Claim blocked: exact air get-hit animation, exact `HitShakeOver` / `HitOver` timing, ground impact, bounce, lie-down, landing, recovery input, controller-loop timing, visual/audio parity, score movement, and full Common1 fall/get-hit parity.
- Previous completed R1 cut: `synthetic-imported-default-air-gethit.json` checksum `dc4fb7c9` remains required and proves airborne imported defenders can route into defender-owned Common1-style state `5020` after a direct `HitDef` without `p2stateno`. That checkpoint passed 259/259 artifacts, 239 required and 20 optional. Claim allowed: bounded air default get-hit state selection. Claim blocked: exact air get-hit animation, fall route, landing route, air recovery, `HitShakeOver`/`HitOver` progression, controller-loop timing, visual/audio parity, score movement, and full Common1 get-hit parity.
- Previous completed R1 cut: `synthetic-imported-default-crouch-gethit.json` checksum `7ec18c61` remains required and proves held-crouch imported defenders can route into defender-owned Common1-style state `5010` after a direct `HitDef` without `p2stateno`. That checkpoint passed 258/258 artifacts, 238 required and 20 optional. Claim allowed: bounded crouch default get-hit state selection. Claim blocked: exact crouch get-hit animation, slide timing, fall routing, `HitShakeOver`/`HitOver` progression, controller-loop timing, visual/audio parity, score movement, and full Common1 get-hit parity.
- Latest completed R2 cut: `RuntimeActiveSideEffectDispatchWorld` now owns bounded active-state side-effect dispatch routing from `PlayableMatchRuntime`: singleton routes for `HitDef`, `ReversalDef`, `Width`, `FallEnvShake`, `Pause` / `SuperPause`, sound, `EnvColor`, `EnvShake`, and contact controllers plus grouped routes for sprite effects, effect spawns, and `Target*` / `BindToTarget`. Focused `RuntimeActiveSideEffectDispatchSystem` coverage proves all current side-effect groups route to the expected handler, missing hooks fail soft, and non-side-effect dispatches pass through. Claim allowed: current active side-effect controller routing has a named, testable world boundary. Claim blocked: exact CNS VM tick order, persistent-controller semantics, helper/team/redirect controller scopes, side-effect ordering parity, target/combat/presentation semantic parity, visual parity, score movement, and full MUGEN/IKEMEN runtime parity.
- Previous completed R2 cut: `RuntimeMatchInteractionWorld`, `RuntimePausedMatchWorld`, and `RuntimeHitPauseWorld` now pass an explicit one-opponent lifecycle `opponents` list into `RuntimeEffectLifecycleWorld` from their concrete 1v1 match/pause/hitpause bridges while preserving the legacy direct opponent argument. Focused `MatchInteractionSystem`, `PauseSystem`, and `RuntimeHitPauseSystem` coverage proves active and paused lifecycle calls carry `[current opponent]` plus stage/runtime ticks. Claim allowed: current concrete 1v1 lifecycle callers now exercise the explicit roster source path used by helper-local `opponentRoster` construction. Claim blocked: real teams/simul roster registry, automatic multi-opponent match roster discovery, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `RuntimeEffectLifecycleWorld` now accepts an explicit lifecycle `opponents` list, builds an id-bearing nearest-order `opponentRoster` through `RuntimeOpponentSelectionWorld`, and keeps the legacy current `opponentId` / `opponentState` separate for one-opponent fallback routes. Focused `EffectLifecycleSystem` coverage proves an unsorted `[far, near, tied]` list becomes a nearest-order helper roster and the `opponents` control field does not leak into helper options. Claim allowed: first-generation visual Helper lifecycle callers can now supply multi-opponent roster sources explicitly. Claim blocked: real teams/simul roster registry, automatic match-level multi-opponent lifecycle wiring, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `RuntimeOpponentSelectionWorld` now builds id-bearing opponent roster entries in nearest order without cloning runtime states, and `RuntimeEffectLifecycleWorld` delegates current-opponent lifecycle roster construction through that boundary. Focused `RuntimeOpponentSelectionSystem` coverage proves ids and state refs survive nearest ordering; `EffectLifecycleSystem` coverage keeps active/paused lifecycle forwarding green. Claim allowed: current first-generation visual Helper lifecycle execution gets its current-opponent roster from the named opponent-selection boundary. Claim blocked: real teams/simul roster registry, multi-opponent lifecycle roster sources, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `RuntimeEffectLifecycleWorld` now builds and forwards an id-bearing helper-local `opponentRoster` for the current opponent into active and paused Helper lifecycle contexts while preserving legacy `opponentId` / `opponentState` fallback. Focused `EffectLifecycleSystem` coverage proves active and paused lifecycle options carry the same current-opponent id plus runtime-state metadata. Claim allowed: current first-generation visual Helper lifecycle execution receives the current opponent through the same roster metadata path that `HelperSystem` and `RuntimeOpponentSelectionWorld` use. Claim blocked: real teams/simul roster registry, multi-opponent lifecycle roster construction, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `HelperSystem` now accepts optional helper-local `opponentRoster` entries carrying `{ id, state }` metadata, routes those entries through `RuntimeOpponentSelectionWorld`, and preserves ids after nearest ordering so `EnemyNear(index), TeamSide` can resolve non-current roster entries. Focused `EffectActorSystem` coverage proves a visual Helper resolves an unsorted roster with ids by nearest/stable-tie order and reads `EnemyNear(1), TeamSide = 2` after sorting. Claim allowed: bounded visual Helper micro-VM contexts can resolve caller-supplied opponent roster state plus id metadata through the named opponent-selection boundary. Claim blocked: real teams/simul roster registry, helper-owned opponent roster discovery, richer identity metadata beyond ids/team side, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `HelperSystem` now routes caller-supplied helper-local `opponentStates` through `RuntimeOpponentSelectionWorld`, so helper-local `EnemyNear(index)` and `NumEnemy` share the same nearest-roster ordering boundary used by active runtime expression contexts. Focused `RuntimeOpponentSelectionSystem` coverage proves raw runtime-state roster ordering, and focused `EffectActorSystem` coverage proves a visual Helper resolves an unsorted `[far, near, tied]` opponent list as nearest/stable-tie order before indexed `EnemyNear` reads. Claim allowed: bounded visual Helper micro-VM contexts resolve caller-supplied opponent-state lists through the named opponent-selection boundary. Claim blocked: real teams/simul roster registry, helper-owned opponent roster discovery, opponent ids for non-current helper-roster entries, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `RuntimeOpponentSelectionWorld` now owns bounded horizontal body-distance scoring and stable nearest-roster ordering for runtime opponent lists, and `RuntimeExpressionContextWorld` delegates explicit `EnemyNear(index)` roster ordering through that boundary. Focused `RuntimeOpponentSelectionSystem` coverage proves nearest ordering, stable ties, finite-before-nonfinite sorting, and distance values, while focused `RuntimeExpressionContextSystem` coverage keeps the `EnemyNear` integration green. Claim allowed: active runtime expression contexts resolve caller-supplied opponent rosters through a named opponent-selection boundary. Claim blocked: real teams/simul roster ownership, helper-owned opponent rosters, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `RuntimeExpressionContextWorld` now sorts explicit opponent rosters by bounded nearest body-distance before resolving `EnemyNear(index)`, preserving caller order for stable ties and keeping `NumEnemy` tied to supplied roster length. Focused `RuntimeExpressionContextSystem` coverage proves `EnemyNear(0..3)` resolves nearest/stable-tie/far order from an unsorted roster. Claim allowed: active runtime expression contexts can resolve caller-supplied opponent rosters in bounded n-th-nearest order through the shared read model. Claim blocked: real teams/simul roster ownership, helper-owned opponent rosters, y-axis/priority selection parity, broader indexed redirect ownership beyond supplied lists, redirect mutation, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `RuntimeDispatchEvaluationWorld` and `RuntimeTriggerEvaluationWorld` now forward optional explicit opponent rosters into their context factories, and `PlayableMatchRuntime` routes the current one-opponent list through that seam. Focused dispatch/trigger coverage proves roster forwarding plus `NumEnemy` evaluation through dynamic controller-param and trigger paths. Claim allowed: active trigger and dynamic-param adapters preserve caller-supplied opponent rosters all the way into `RuntimeExpressionContextWorld`. Claim blocked: real teams/simul roster ownership, helper-owned opponent rosters, broader indexed redirect ownership beyond supplied lists, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `RuntimeExpressionContextWorld` now owns an optional explicit opponent roster for active runtime contexts, wiring `EnemyNear(index)` and `NumEnemy` through the same bounded list before falling back to the existing single-opponent context. Focused `RuntimeExpressionContextSystem` coverage proves roster-backed `EnemyNear(1)` / `EnemyNear(var(n))` reads, `NumEnemy = 2`, default one-opponent fallback, and missing-index fail-closed behavior. Claim allowed: current runtime expression contexts can evaluate caller-supplied opponent rosters through the shared read model. Claim blocked: real teams/simul roster ownership, helper-owned opponent rosters, broader indexed redirect ownership beyond supplied lists, visual parity, score movement, and full helper/team VM parity.
- Previous completed R2 cut: `ExpressionEvaluator` now resolves `EnemyNear(index)` through an explicit enemy-near redirect callback and accepts an explicit `NumEnemy` provider, while `HelperSystem` passes an `opponentStates` list into helper-local trigger/value evaluation and derives `NumEnemy` from that list. Focused `RuntimeCnsSubset` and `EffectActorSystem` coverage proves direct evaluator support, dynamic index expressions through `var(n)`, helper-local indexed routing, explicit opponent-count reads, and missing-index fail-closed behavior. Claim allowed: current bounded helper-local redirect/count execution can read `EnemyNear(0)`, `EnemyNear(var(n))`, and `NumEnemy` from caller-supplied opponent runtime states. Claim blocked: teams/simul opponent ordering, helper-owned opponent rosters, broader indexed redirect ownership beyond supplied lists, visual parity, score movement, and full helper VM parity.
- Previous completed R2 cut: `RuntimeEffectLifecycleWorld` now forwards `stageBounds`, `stageTime`, and `runtimeTick` into helper-local active and paused lifecycle passes. `RuntimeMatchInteractionWorld`, `RuntimePausedMatchWorld`, `RuntimeHitPauseWorld`, and `PlayableMatchRuntime` pass current tick context through that boundary. Focused `EffectLifecycleSystem` coverage proves helper-local `GameTime` rejection/pass behavior, `FrontEdgeDist` param evaluation from stage bounds, `PlaySnd` `runtimeTick` telemetry, and `ChangeState` handoff. Claim allowed: current helper-local lifecycle execution receives bounded match tick/stage context through a named effect-lifecycle boundary. Claim blocked: exact helper clock parity, pause/combat ordering parity, broader indexed/team redirects, teams/simul, visual parity, score movement, and full helper VM parity.
- Previous R1 required Projectile fixed-id contact/guard-time cut: `synthetic-imported-projectile-contacttime-id.json` checksum `e9ebf36a` and `synthetic-imported-projectile-guardedtime-id.json` checksum `dfd08f28` gate bounded owner-state `ProjContactTime(77)` and `ProjGuardedTime(77)` after a player-owned Projectile contact/guard marker, routing P1 through state/actions `321` and `322` with hit/guard event/reason, Projectile lifecycle, effect-store, and target-link evidence. That checkpoint passed `pnpm qa:trace` at 257/257 artifacts, 237 required and 20 optional. Claim allowed: current imported owner-state CNS can read fixed-id player-owned Projectile contact/guard time. Claim blocked: exact contact/guard tick-order/lifetime, multi-projectile same-id selection, helper-owned projectile routing, redirects, teams/simul, score movement, visual/audio parity, and full Projectile timing parity.
- Previous R1 required Projectile cancel-time owner any-id cut: `synthetic-imported-projectile-canceltime-any.json` checksum `5bff1961` gates bounded owner-state `ProjCancelTime(0)` after that owner's player-owned Projectile id `77` is canceled by an opposing Projectile clash, routing P2 from state/action `200` into `320` with Projectile clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `918`, projectile lifecycle, effect-store, and payload evidence. Claim allowed: current imported owner-state CNS can read cancel time through a bounded any-projectile id `0` route. Claim blocked: exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration beyond this route, exact priority classes, helper-owned projectile routing, redirects, teams/simul, score movement, visual/audio parity, and full Projectile cancel parity.
- Previous R1 required Projectile cancel-time owner var-id cut: `synthetic-imported-projectile-canceltime-var.json` checksum `e057e102` gates bounded owner-state `ProjCancelTime(var(0))` after owner-local `VarSet` seeds canceled player-owned Projectile id `77`, routing P2 from state/action `200` into `319` with Projectile clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `917`, projectile lifecycle, effect-store, and payload evidence. Claim allowed: current imported owner-state CNS can read cancel time through a bounded var-backed player-owned Projectile id. Claim blocked: exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration, exact priority classes, helper-owned projectile routing, redirects, teams/simul, score movement, visual/audio parity, and full Projectile cancel parity.
- Previous R1 required Projectile cancel-time owner dynamic-id cut: `synthetic-imported-projectile-canceltime-dynamic.json` checksum `0da26c87` gates bounded owner-state expression-derived `ProjCancelTime(77 + var(0))` after that owner's player-owned Projectile is canceled by an opposing Projectile clash, routing P2 from state/action `200` into `318` with Projectile clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `916`, projectile lifecycle, effect-store, and payload evidence. Claim allowed: current imported owner-state CNS can read cancel time through a bounded expression-derived player-owned Projectile id. Claim blocked: exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile any-id arbitration, exact priority classes, helper-owned projectile routing, redirects, teams/simul, score movement, visual/audio parity, and full Projectile cancel parity.
- Previous R1 required Helper Projectile cancel-time dynamic-id cut: `synthetic-imported-helper-projcanceltime-dynamic.json` checksum `cc78dde2` gates bounded helper-local expression-derived `ProjCancelTime(8869 + var(0))` after that helper-parented owner-side Projectile is canceled by an opposing Projectile clash, routing the Helper from state/action `1270` into `1271` with Projectile clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `1018`, helper/projectile lifecycle, effect-store, and payload evidence. Focused helper-local runtime coverage also proves nonzero `ProjCancelTime(var(n))` id selection against a helper-parented canceled Projectile. That checkpoint passed 252/252 artifacts, 232 required and 20 optional before later owner-state gates were added. Claim allowed: current imported helper-local CNS can read cancel time through a bounded expression-derived Projectile id and focused runtime can resolve nonzero var-backed dynamic ids. Claim blocked: exact cancel tick-order/lifetime, broader dynamic expression parity, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, score movement, visual/audio parity, and full Helper/Projectile cancel parity.
- Previous R1 required Helper Projectile cancel-time fixed-id cut: `synthetic-imported-helper-projcanceltime-id.json` checksum `fc412176` gates bounded helper-local `ProjCancelTime(8868)` after that helper-parented owner-side Projectile is canceled by an opposing Projectile clash, routing the Helper from state/action `1268` into `1269` with Projectile clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `1008`, helper/projectile lifecycle, effect-store, and payload evidence. Claim allowed: current imported helper-local CNS can read the cancel time for a matching fixed helper-parented Projectile id. Claim blocked: exact cancel tick-order/lifetime, multi-projectile same-id selection, exact priority classes, helper-owned custom states, redirects, teams/simul, score movement, visual/audio parity, and full Helper/Projectile cancel parity.
- Previous R1 required Helper Projectile cancel-time any cut: `synthetic-imported-helper-projcanceltime-any.json` checksum `f7e7fa01` gates bounded helper-local `ProjCancelTime(0)` after a helper-parented owner-side Projectile is canceled by an opposing Projectile clash, routing the Helper from state/action `1266` into `1267` with Projectile clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback anim `998`, helper/projectile lifecycle, effect-store, and payload evidence. Claim allowed: current imported helper-local CNS can read the cancel time for a helper-parented canceled Projectile through the bounded any-projectile id `0` path. Claim blocked: exact cancel tick-order/lifetime, exact priority classes, multi-projectile any-id selection semantics beyond this route, helper-owned custom states, redirects, teams/simul, score movement, visual/audio parity, and full Helper/Projectile cancel parity.
- Previous R1 required Projectile cancel-time cut: `synthetic-imported-projectile-canceltime.json` checksum `64e8dec4` gates bounded owner-state `ProjCancelTime(77)` after a player-owned Projectile is canceled by an opposing Projectile clash, routing P2 through state/action `283` with Projectile clash/cancel runtime-event evidence, loser `projcancelanim` terminal playback, projectile lifecycle, effect-store, and payload evidence. Claim allowed: current imported owner-state CNS can read the cancel time for that owner's canceled player-owned Projectile id `77`. Claim blocked: exact cancel tick-order/lifetime, exact priority classes, multi-projectile any-id arbitration, redirects, teams/simul, score movement, visual/audio parity, and full Projectile cancel parity.
- Previous R1 required Helper Projectile guard/contact-time any cut: `synthetic-imported-helper-projguardedtime-any.json` checksum `1f1a38e4` and `synthetic-imported-helper-projcontacttime-any.json` checksum `0d9f7829` gate bounded helper-local `ProjGuardedTime(0)` and `ProjContactTime(0)` after helper-parented Projectile guard/contact markers, routing the Helper through state/actions `1263` and `1265` with guard event/reason, helper/projectile lifecycle, effect-store, target-link, and sound/FightFX package evidence. Claim allowed: current imported helper-local CNS can read any-projectile guard/contact time for helper-parented Projectile guard/contact. Claim blocked: exact contact/guard tick-order/lifetime, multi-projectile any-id selection, helper-owned custom-state targets, redirects, teams/simul, score movement, visual/audio parity, and full Helper/Projectile parity.
- Previous R1 required Helper Projectile hit-time any cut: `synthetic-imported-helper-projhittime-any.json` checksum `bca9f47b` gates bounded helper-local `ProjHitTime(0)` after a helper-parented Projectile hits P2, routing the Helper through state/action `1261` with hit event/reason, helper/projectile lifecycle, effect-store, target-link, and sound/FightFX package evidence. Claim allowed: current imported helper-local CNS can read any-projectile hit time for helper-parented Projectile hits. Claim blocked: exact hit tick-order/lifetime, multi-projectile any-id selection, helper-owned custom-state targets, redirects, teams/simul, score movement, visual/audio parity, and full Helper/Projectile parity.
- Previous R1 required Projectile hit-time any cut: `synthetic-imported-projectile-hittime-any.json` checksum `47c1cf7f` gates bounded owner-state `ProjHitTime(0)` after a player-owned Projectile hits P2, routing P1 through state/action `282` with hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. Claim allowed: current imported owner-state CNS can read any-projectile hit time for player-owned Projectile hit. Claim blocked: exact hit tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, score movement, visual/audio parity, and full Projectile hit parity.
- Previous R1 required Projectile contact-time any cut: `synthetic-imported-projectile-contacttime-any.json` checksum `f1751155` gates bounded owner-state `ProjContactTime(0)` after a player-owned Projectile hits P2, routing P1 through state/action `281` with hit event/reason, Projectile lifecycle, effect-store, and target-link evidence. Claim allowed: current imported owner-state CNS can read any-projectile contact time for player-owned Projectile contact. Claim blocked: exact contact tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, score movement, visual/audio parity, and full Projectile contact parity.
- Previous R1 required Projectile guarded-time any cut: `synthetic-imported-projectile-guardedtime-any.json` checksum `c8473340` gates bounded owner-state `ProjGuardedTime(0)` after P2 guards a player-owned Projectile, routing P1 through state/action `279` with guard event/reason, Projectile lifecycle, effect-store, and target-link evidence. Claim allowed: current imported owner-state CNS can read any-projectile guarded contact time for player-owned Projectile guard. Claim blocked: exact guard tick-order/lifetime, multi-projectile id=0 selection, exact helper-local timing beyond bounded helper gates, redirects, teams/simul, score movement, visual/audio parity, and full Projectile guard parity.
- Previous R1 required HitDef plus Projectile target-memory mix cut: `synthetic-imported-hitdef-projectile-target-mix.json` checksum `e98d4857` gates a bounded active-state route where direct `HitDef` id `77` and player-owned `Projectile` id `78` both hit P2, then P1 branches through `NumTarget(77)`, `Target(77), Life`, `NumTarget(78)`, and `Target(78), Life` into state/action `278`, with direct hit, Projectile lifecycle/payload, and both owner target-link evidence. `pnpm qa:trace` passed 242/242 artifacts, 222 required and 20 optional at that checkpoint. Claim allowed: current owner-local target memory can retain and read separate direct `HitDef` and player-owned `Projectile` target ids in one bounded imported CNS state. Claim blocked: Target* mutation mixing, helper-owned projectile targets, helper-owned custom state tables, throws, teams/simul, multi-target/helper-owned opponent selection, exact target lifetime/tick order, score movement, and full target/combat parity.
- Previous R1 required Helper Projectile Air Guard GetHitVar hitshaketime cut: `synthetic-imported-helper-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3c3f2e25` gates a bounded defender-owned air guard-hit route where P2 blocks a helper-parented Projectile while airborne and holding back, enters `154 -> 155`, then branches through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `317`, with helper-local Projectile spawn telemetry, helper/projectile lifecycle, and owner/helper target-link evidence. Claim allowed: current imported CNS can read helper-parented Projectile air guard hitshake timing metadata during defender-owned Common1-style air guard-hit CNS. Claim blocked: helper-owned custom states, exact helper Projectile air guard timing/landing/effects, projectile presentation, custom-state inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required Projectile Air Guard GetHitVar hitshaketime cut: `synthetic-imported-projectile-gethitvar-air-guard-hitshaketime.json` checksum `3fcf1421` gates a bounded defender-owned air guard-hit route where P2 blocks a player-owned Projectile while airborne and holding back, enters `154 -> 155`, then branches through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `316`. Claim allowed: current imported CNS can read player-owned Projectile air guard hitshake timing metadata during defender-owned Common1-style air guard-hit CNS. Claim blocked: exact air guard timing/landing/proximity, projectile presentation, custom-state inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required Air Guard GetHitVar hitshaketime cut: `synthetic-imported-gethitvar-air-guard-hitshaketime.json` checksum `703e9328` gates a bounded defender-owned air guard-hit route where P2 blocks a direct `HitDef` while airborne and holding back, enters `154 -> 155`, then branches through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `315`. Claim allowed: current imported CNS can read direct-`HitDef` air guard hitshake timing metadata during defender-owned Common1-style air guard-hit CNS. Claim blocked: exact air guard timing/landing/proximity, guard end/effects, helper/projectile air-guard variants, custom-state inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required Crouch Guard GetHitVar hitshaketime cut: `synthetic-imported-gethitvar-crouch-guard-hitshaketime.json` checksum `b31d1dac` gates a bounded defender-owned crouch guard-hit route where P2 blocks a direct `HitDef` while holding down-back, enters `152 -> 153`, then branches through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `314`. Claim allowed: current imported CNS can read direct-`HitDef` crouch guard hitshake timing metadata during defender-owned Common1-style crouch guard-hit CNS. Claim blocked: exact crouch guard timing/proximity, guard end/effects, helper/projectile crouch-guard variants, custom-state inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required Helper Projectile GetHitVar guard hitshaketime cut: `synthetic-imported-helper-projectile-gethitvar-guard-hitshaketime.json` checksum `64a1a8bd` gates a bounded defender-owned guard-hit route where P2 blocks a helper-parented Projectile, enters `150 -> 151`, then branches through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `313`. Claim allowed: current imported CNS can read helper-parented Projectile guard hitshake timing metadata during defender-owned Common1-style guard-hit CNS, with helper/projectile lifecycle, payload, target-link, and helper-local `Projectile` telemetry evidence. Claim blocked: helper-owned custom states, exact helper Projectile guard timing/effects, projectile visual/audio parity, custom-state inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required Projectile GetHitVar guard hitshaketime cut: `synthetic-imported-projectile-gethitvar-guard-hitshaketime.json` checksum `724f66d6` gates a bounded defender-owned guard-hit route where P2 blocks a player-owned Projectile, enters `150 -> 151`, then branches through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `312`. Claim allowed: current imported CNS can read player-owned Projectile guard hitshake timing metadata during defender-owned Common1-style guard-hit CNS. Claim blocked: exact hitpause lifetime, guard timing/effects, projectile visual/audio parity, custom-state inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required GetHitVar guard hitshaketime cut: `synthetic-imported-gethitvar-guard-hitshaketime.json` checksum `31d76de9` gates a bounded defender-owned guard-hit route where P2 blocks a direct `HitDef`, enters `150 -> 151`, then branches through `GetHitVar(hitshaketime) > 0 && GetHitVar(guarded) = 1` into state/action `311`. Claim allowed: current imported CNS can read direct-`HitDef` guard hitshake timing metadata during defender-owned Common1-style guard-hit CNS. Claim blocked: exact hitpause lifetime, guard timing/proximity, guard end, guard effects, custom-state/helper/projectile inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required GetHitVar hitshaketime cut: `synthetic-imported-gethitvar-hitshaketime.json` checksum `655107b9` gates a bounded defender-owned normal get-hit route where P2 takes a direct `HitDef`, enters state `5000`, then branches through `GetHitVar(hitshaketime) > 0 && !GetHitVar(guarded)` into state/action `310`. Claim allowed: current imported CNS can read direct-contact normal hitshake timing metadata during defender-owned Common1-style get-hit CNS. Claim blocked: exact hitpause tick lifetime, hitshake lifetime, custom-state/helper/projectile inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN get-hit parity.
- Previous R1 required GetHitVar hittime cut: `synthetic-imported-gethitvar-hittime.json` checksum `a11beef0` gates a bounded defender-owned normal get-hit route where P2 takes a direct `HitDef`, enters state `5000`, then branches through `GetHitVar(hittime) > 0 && !GetHitVar(guarded)` into state/action `309`. Claim allowed: current imported CNS can read direct-contact normal hit timing metadata during defender-owned Common1-style get-hit CNS. Claim blocked: exact hitstun tick order, custom-state/helper/projectile inheritance breadth, exact VM tick order, score movement, and full MUGEN/IKEMEN get-hit parity.
- Previous R1 required GetHitVar guard timing cut: `synthetic-imported-gethitvar-guard-timing.json` checksum `cf92c669` gates a bounded defender-owned guard-hit route where P2 blocks a direct `HitDef`, runs `150 -> 151`, executes `HitVelSet`, then branches from state `151` through `GetHitVar(hittime) > 0`, `GetHitVar(slidetime) = 5`, and `GetHitVar(ctrltime) = 7` into state/action `308`. Claim allowed: current imported CNS can read direct-`HitDef` guard timing metadata during defender-owned Common1-style guard-hit CNS. Claim blocked: exact guard timing/proximity, guard end, guard effects, projectile/helper/custom-state inheritance, exact VM tick order, score movement, and full MUGEN/IKEMEN guard parity.
- Previous R1 required GetHitVar down-recover cut: `synthetic-imported-gethitvar-down-recover.json` checksum `b8a7aef0` gates a bounded owner-backed get-hit route where P2 enters state `5100` after a fall `HitDef`, then branches through `GetHitVar(down.recover) = 1`, `GetHitVar(down.recovertime) = 45`, and alias `GetHitVar(recovertime) = 45` into state/action `307` before lie-down recovery consumes the timer. Claim allowed: current imported CNS can read stored direct-`HitDef` down-recovery flag/timer aliases during an owner-backed get-hit state. Claim blocked: exact liedown tables, 5110/5120 tick order, metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, exact VM tick order, score movement, and full MUGEN/IKEMEN Common1 recovery parity.
- Previous R1 required GetHitVar fall env-shake cut: `synthetic-imported-gethitvar-fall-envshake.json` checksum `6364632a` gates a bounded owner-backed get-hit route where P2 enters state `5100` after a fall `HitDef`, then branches through `GetHitVar(fall.envshake.time) = 15`, `GetHitVar(fall.envshake.freq) = 178`, `GetHitVar(fall.envshake.ampl) = 6`, and `GetHitVar(fall.envshake.phase) = 0` into state/action `306` before `FallEnvShake` presentation executes. Claim allowed: current imported CNS can read stored direct-`HitDef` fall env-shake metadata during an owner-backed get-hit state before presentation consumes it. Claim blocked: exact camera waveform, pause/stage/layer interaction, metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, exact VM tick order, score movement, and full MUGEN/IKEMEN fall presentation parity.
- Latest R1 required custom-state GetHitVar guard timing cut: `synthetic-imported-custom-state-gethitvar-guard-timing.json` checksum `ba77beec` gates a bounded owner-backed custom-state route where guarded direct-HitDef target memory feeds owner-local `TargetState`, P2 enters P1-owned state data, then branches through `GetHitVar(guarded) = 1 && GetHitVar(slidetime) = 5 && GetHitVar(ctrltime) = 7 && GetHitVar(hitshaketime) > 0` into state/action `901` before `SelfState`. Claim allowed: current imported CNS can read bounded guarded timing metadata during an attacker-owned custom-state route after `TargetState`. Claim blocked: `p2stateno`-on-guard behavior, exact guard timing, throws, redirects, helper/projectile inheritance, exact VM tick order, score movement, and full custom-state/get-hit parity.
- Previous R1 required custom-state GetHitVar hitcount/id cut: `synthetic-imported-custom-state-gethitvar-hitcount-hitid-chainid.json` checksum `250f77c2` gates a bounded owner-backed custom-state route where P2 enters P1-owned state data after a direct `HitDef id = 77, chainID = 43, numhits = 3`, then branches through `GetHitVar(hitcount) = 3 && GetHitVar(hitid) = 77 && GetHitVar(chainid) = 43 && GetHitVar(hittime) > 0 && !GetHitVar(guarded)` into state/action `900` before `SelfState`. Claim allowed: current imported CNS can read bounded direct-HitDef numhits/id/chainID metadata during an attacker-owned custom-state route. Claim blocked: exact combo accumulation, chain-hit eligibility arbitration, redirects, helper/projectile inheritance, exact target lifetime, exact VM tick order, score movement, and full custom-state/get-hit parity.
- Previous R1 required GetHitVar fallcount cut: `synthetic-imported-gethitvar-fallcount.json` checksum `c391d938` gates a bounded owner-backed get-hit route where P2 enters state `5100` after a fall `HitDef`, `HitFallDamage` records one ground impact and consumes stored `fall.damage`, then branches through `GetHitVar(fallcount) = 1 && GetHitVar(fall.damage) = 0` into state/action `328`. Claim allowed: current imported CNS can read one bounded Common1-style ground-impact count during an owner-backed get-hit state after `HitFallDamage`. Claim blocked: multi-ground-hit combo accumulation, non-Common1 impact detection, lifetime/reset parity, redirects, helper/projectile/custom-state inheritance, exact VM tick order, score movement, and full fall/get-hit parity.
- Previous R1 required GetHitVar fall metadata cut: `synthetic-imported-gethitvar-fall-metadata.json` checksum `474fa734` gates a bounded owner-backed get-hit route where P2 enters state `5100` after a fall `HitDef`, then branches through `GetHitVar(fall.damage) = 70`, `GetHitVar(fall.kill) = 0`, `GetHitVar(fall.xvel) = 3`, and `GetHitVar(fall.yvel) = -6` into state/action `305` before `HitFallDamage` resolves. Claim allowed: current imported CNS can read stored direct-`HitDef` fall damage, kill, and velocity metadata during an owner-backed get-hit state. Claim blocked: exact fall metadata lifetime/stacking, redirects, helper/projectile/custom-state inheritance, exact VM tick order, score movement, and full MUGEN/IKEMEN fall/get-hit parity.
- Previous R1 required TeamSide cut: `synthetic-imported-teamside.json` checksum `f55695b7` gates a bounded imported State -1 route where P1 presses `x`, then `TeamSide = 1` and `EnemyNear, TeamSide = 2` route into state/action `299` without `HitDef` / combat side effects. That checkpoint passed `pnpm qa:trace` at 228/228 artifacts, 208 required and 20 optional before later GetHitVar timing gates were added. Claim allowed: current one-on-one imported CNS can branch on bounded side context for owner and current enemy redirect. Claim blocked: teams/simul/turns, indexed opponent selection, helper-owned opponent lists, dynamic side ownership, score movement, and full MUGEN/IKEMEN `TeamSide` semantics.
- Previous R1 required Helper Projectile GetHitVar guarded cut: `synthetic-imported-helper-projectile-gethitvar-guarded.json` checksum `2b413bd7` gates a bounded defender-owned guard-hit route where P2 blocks a helper-parented Projectile (`parentId = p1-helper-0`), runs `150 -> 151`, executes `HitVelSet` / `CtrlSet` typed ops, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `304` before returning to idle/control. Claim allowed: current imported CNS can read helper-parented Projectile guarded metadata after guard-hit velocity/control controllers, with helper/projectile lifecycle, payload, owner target-link, helper target-link, and helper-local `Projectile` controller/op telemetry evidence. Claim blocked: custom-state guarded metadata, exact guard timing/effects, score movement, and full Common1/guard parity.
- Previous R1 required Projectile GetHitVar guarded cut: `synthetic-imported-projectile-gethitvar-guarded.json` checksum `a0104472` gates a bounded defender-owned guard-hit route where P2 blocks a player-owned Projectile-only attack, runs `150 -> 151`, executes `HitVelSet` / `CtrlSet` typed ops, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `303` before returning to idle/control. Claim allowed: current imported CNS can read player-owned Projectile guarded metadata after guard-hit velocity/control controllers. Claim blocked: custom-state guarded metadata, exact guard timing/effects, score movement, and full Common1/guard parity.
- Previous R1 required GetHitVar guarded cut: `synthetic-imported-gethitvar-guarded.json` checksum `7c36defb` gates a bounded defender-owned guard-hit route where P2 blocks a direct `HitDef`, runs `150 -> 151`, executes `HitVelSet` / `CtrlSet` typed ops, then branches from state `151` through `GetHitVar(guarded) = 1` into state/action `302` before returning to idle/control. Claim allowed: current imported CNS can read direct `HitDef` guarded metadata after guard-hit velocity/control controllers. Claim blocked: custom-state guarded metadata, exact guard timing/effects, score movement, and full Common1/guard parity.
- Previous R1 required GetHitVar fall-recover cut: `synthetic-imported-gethitvar-fall-recover.json` checksum `259b300f` gates a bounded owner-backed get-hit route where P2 enters state `5100` after a recoverable fall `HitDef`, then branches through `GetHitVar(fall.recover) = 1 && GetHitVar(fall.recovertime) > 0 && !CanRecover` into state/action `301`. Claim allowed: current imported CNS can read the HitDef recovery-allowed flag separately from the countdown-gated `CanRecover` trigger. Claim blocked: exact recovery threshold tables, custom-state lifetime, helper/projectile inheritance, controller-loop timing, score movement, and full Common1/get-hit parity.
- Previous R1 required AnimElem offset cut: `synthetic-imported-animelem-offset.json` checksum `4484031d` gates a bounded imported active-state route where P1 enters state `200`, advances through authored AIR frame durations `[2,6,4]`, then exits through legacy `AnimElem = 2, = 4` into state/action `300` without `HitDef` / combat side effects. That checkpoint passed 222/222 artifacts, 202 required and 20 optional. Previous `synthetic-imported-animelem.json` checksum `683d9a10` remains required for `AnimElem = 2, = 0`. Claim allowed: current imported active-state CNS can branch on the current actor AIR element start and bounded elapsed time using `AnimElem = n` / `AnimElem = n, op t` numeric forms, including simple composite expressions. Claim blocked: AIR loop semantics, negative-duration semantics, helper/state-owner namespaces, persistent-controller timing, score movement, and full animation VM parity.
- Previous R1 required OwnerMetrics cut: `synthetic-imported-owner-metrics.json` checksum `1a61aaeb` gates a bounded imported State -1 route where P1 presses `x`, then `StateNo = 0`, `Anim = 0`, `Time >= 0`, `Life = 1000`, `Power = 0`, `Pos X < 0`, `Pos Y = 0`, `Vel X = 0`, and `Vel Y = 0` route into state/action `298` without `HitDef` / combat side effects. Claim allowed: current imported State -1 CNS can branch on bounded current-owner state, animation, time, resource, position, and velocity trigger context. Claim blocked: exact VM tick ordering, helper/team/redirect state namespaces, localcoord scaling, score movement, and full MUGEN/IKEMEN trigger parity.
- Previous R1 required P2Distance cut: `synthetic-imported-p2-distance.json` checksum `2c584be0` gates a bounded imported State -1 route where P1 presses `x`, then `P2Dist X = 55`, `P2Dist Y = 0`, `P2BodyDist X = 7`, and `P2BodyDist Y = 0` route into state/action `297` without `HitDef` / combat side effects. Claim allowed: current imported State -1 CNS can branch on bounded current-opponent spacing trigger context. Claim blocked: teams/simul, helpers, exact opponent selection, localcoord scaling, push/corner adjustment, score movement, and full MUGEN/IKEMEN spacing parity.
- Previous R1 required P2StateContext cut: `synthetic-imported-p2-state-context.json` checksum `caf32557` gates a bounded imported State -1 route where P1 presses `x`, then `P2StateType = S` and `P2MoveType = I` route into state/action `296` without `HitDef` / combat side effects. Claim allowed: current imported State -1 CNS can branch on bounded current-opponent state/move metadata trigger context. Claim blocked: teams/simul, helpers, custom-state opponent ownership, exact opponent selection, score movement, and full MUGEN/IKEMEN trigger parity.
- Previous R1 required StateContext cut: `synthetic-imported-state-context.json` checksum `cb9c3d1e` gates a bounded imported State -1 route where P1 presses `x`, then `ctrl`, `StateType = S`, `MoveType = I`, and `Physics = S` route into state/action `295` without `HitDef` / combat side effects. Claim allowed: current imported State -1 CNS can branch on bounded owner control and state metadata trigger context. Claim blocked: helper/team/redirect metadata ownership, exact controller-loop ordering, score movement, and full MUGEN/IKEMEN trigger parity.
- Previous R1 required GameTime cut: `synthetic-imported-gametime.json` checksum `bab573f3` gates a bounded imported State -1 route where P1 delays the `x` command until `GameTime >= 4`, then enters state/action `294` without `HitDef` / combat side effects. Claim allowed: current imported State -1 CNS can branch on bounded global match tick through `GameTime`. Claim blocked: exact pause accounting, replay/rollback timing, multi-round timer ownership, IKEMEN round-system behavior, score movement, and full MUGEN/IKEMEN global timing parity.
- Previous R1 required edge-distance cut: `synthetic-imported-edge-distance.json` checksum `785de452` gates a bounded imported State -1 route where P1 presses `x`, evaluates `FrontEdgeDist = 340`, `BackEdgeDist = 300`, `FrontEdgeBodyDist = 301`, and `BackEdgeBodyDist = 261` against supplied stage bounds, then enters state/action `293` without `HitDef` / combat side effects. That checkpoint passed 215/215 artifacts, 195 required and 20 optional. Claim allowed: current imported State -1 CNS can branch on bounded stage edge distance with body-width subtraction. Claim blocked: exact camera/screen edge parity, teams/simul/helper namespace breadth, localcoord scaling, push/corner semantics, score movement, and full MUGEN/IKEMEN edge-distance parity.
- Previous R2 bounded edge-distance expression cut: `FrontEdgeDist`, `BackEdgeDist`, `FrontEdgeBodyDist`, and `BackEdgeBodyDist` now read supplied stage bounds through active trigger contexts, dynamic controller-param fallback, extracted passive controller systems, and first-generation visual Helper micro-VM contexts. Focused evaluator/context/helper tests prove bounded values and the no-stage fallback. Claim allowed: current runtime and helper-local CNS expressions can branch on bounded stage edge distance when stage bounds are available. Claim blocked: exact camera/screen edge parity, teams/simul/helper namespace breadth, localcoord scaling, push/corner semantics, score movement, and full MUGEN/IKEMEN edge-distance parity.
- Previous R1 required AnimElemTime cut: `synthetic-imported-animelemtime.json` checksum `2036557d` gates a bounded imported active-state animation-element route where P1 enters state `200`, advances through authored AIR frame durations `[2,4,4]`, and exits through `AnimElemTime(2) = 2` into state `292` without `HitDef` / combat side effects. Claim allowed: current imported active-state CNS can branch on current actor AIR element elapsed time through `AnimElemTime(2) = 2`. Claim blocked: exact AIR loop semantics, invalid-element bottom values, negative-duration semantics, state-owner/helper namespaces, persistent-controller timing, exact controller-loop tick order, score movement, and full animation VM parity.
- Previous R1 required AnimTime cut: `synthetic-imported-animtime.json` checksum `9e42b546` gates a bounded imported active-state animation-end route where P1 enters state `200`, remains there for the authored AIR action duration, and exits through `AnimTime = 0` into state `291` without `HitDef` / combat side effects. Claim allowed: current imported active-state CNS can branch on current actor action completion through `AnimTime = 0`. Claim blocked: exact AIR negative-duration semantics, looped-action semantics, state-owner/helper namespaces, persistent-controller timing, exact controller-loop tick order, score movement, and full animation VM parity.
- Previous R1 required SelfAnimExist cut: `synthetic-imported-selfanimexist.json` checksum `99930032` gates a bounded State -1 own-animation lookup route where existing AIR action `200` passes `SelfAnimExist(200)`, missing action `9999` fails `!SelfAnimExist(9999)`, and P1 enters state `290`. Claim allowed: current imported State -1 routing can branch on bounded own AIR action existence. Claim blocked: redirected animation owners, helper/parent/root lookup, common/FightFX namespaces, exact animation lookup parity, score movement, and full MUGEN/IKEMEN trigger parity.
- Latest R2 EnvColor bridge ownership cut: `RuntimeMatchEnvColorBridgeWorld` now owns bounded match-level EnvColor event handoff outside `PlayableMatchRuntime`: active, pause, and hitpause ignored-controller routes pass controller source, optional typed `envcolor` operation data, runtime tick, and `RuntimeEnvColorWorld` emission through one named boundary. Claim allowed: current match-loop EnvColor callback routing has a named, testable bridge that preserves typed-operation and runtime-tick handoff. Claim blocked: exact EnvColor blend math, layer/window ordering, pause layering/timing, renderer parity, score movement, and full presentation parity.
- Previous R2 fighter state ownership cut: `RuntimeFighterStateWorld` now owns bounded fighter runtime-state construction outside `PlayableMatchRuntime`: resource maxima, damage multipliers, initial runtime/action/control state, command buffers, contact memory, telemetry buckets, injected world references, deterministic RNG seed, and lazy runtime-program compilation. Claim allowed: current P1/P2 fighter construction has a named, testable boundary that preserves native/imported initial state and injected world ownership. Claim blocked: exact player lifecycle parity, helper/custom-state clone breadth, team/simul roster ownership, intro/round lifecycle, visual parity, score movement, and full actor registry parity.
- Previous R2 match reset ownership cut: `RuntimeMatchResetWorld` now owns bounded match reset orchestration outside `PlayableMatchRuntime`: round timer reset, pause reset, EnvColor reset, effect actor store reset, in-place P1/P2 recreation, helper TargetState handler reattachment, and reset logging. Claim allowed: current sandbox reset lifecycle has a named, testable boundary that preserves actor/store identity and callback reattachment. Claim blocked: exact round-flow parity, continue/round intro semantics, helper/custom-state reset breadth, screenpack/lifebar reset behavior, visual parity, score movement, and full MUGEN/IKEMEN match lifecycle parity.
- Previous R2 helper TargetState handler binding cut: `RuntimeHelperTargetStateWorld` now owns bounded helper TargetState handler attach/re-attach wiring outside `PlayableMatchRuntime` constructor/reset. Claim allowed: current helper-local `TargetState` callbacks are bound through a named world boundary that preserves owner/helper/target/state id forwarding. Claim blocked: helper-owned custom-state table parity, throws, teams/simul, multi-target/helper-owned opponent selection, exact helper TargetState timing, visual parity, score movement, and full Helper VM parity.
- Previous R2 frame/collision ownership cut: `RuntimeFrameWorld` now owns bounded current AIR frame lookup plus cloned `Clsn1` / `Clsn2` projection outside `PlayableMatchRuntime` / `RuntimeSnapshotWorld`. Claim allowed: current match runtime and snapshot projection share a named, testable boundary for current-frame lookup, active-hitbox projection, cloned hurtbox projection, and missing-frame default hurtbox fallback. Claim blocked: exact MUGEN/IKEMEN collision priority, frame timing, guard-distance thresholds, rotated/scaled box semantics, helper/team/redirect collision ownership, renderer parity, score movement, and full frame/collision VM parity.
- Previous R2 afterimage sample ownership cut: `RuntimeAfterImageSampleWorld` now owns bounded `AfterImage` sample projection outside `PlayableMatchRuntime`: cloned actor position, facing, self/state-owner sprite owner metadata, and current AIR frame group/index/offset are projected into the ghost-trail sample before `RuntimeSpriteEffectWorld` captures it. Claim allowed: current `AfterImage` / `AfterImageTime` sample construction has a named, testable runtime boundary. Claim blocked: exact ghost-trail sampling cadence, material/blend parity, helper/team/redirect presentation ownership, renderer parity, score movement, and full MUGEN/IKEMEN presentation parity.
- Latest completed R2 controller expression-context ownership cut: `RuntimeControllerExpressionContextSystem` owns shared raw numeric controller expression-context construction for passive/runtime controller worlds. Dynamic fallback params now reuse one redirect-aware helper for `Target(...)`, `Parent`, `Root`, `Const`, `HitPauseTime`, `StageTime`, and `GetHitVar` reads, with focused coverage for redirects plus helper/team metadata forwarding. Claim allowed: current passive/runtime controller numeric params have a named, testable expression-context boundary. Claim blocked: new expression language support, `ID`/player unique-id semantics, recursive redirection, broader helper/team/redirect scopes, exact CNS controller-loop timing, visual parity, score movement, and full MUGEN/IKEMEN controller VM parity.
- Previous R2 controller evaluation context ownership cut: `RuntimeControllerEvaluationContextWorld` now owns bounded `StateControllerExecutor` context creation outside `PlayableMatchRuntime`: owner const reads, actor hitpause reads, actor random callbacks, and stage-time forwarding route through a named context factory. Claim allowed: current active runtime-controller executor context has a named, testable world boundary. Claim blocked: full passive-controller parity, exact CNS controller-loop timing, helper/team/redirect context scopes, exact random stream parity, visual parity, score movement, and full MUGEN/IKEMEN controller VM parity.
- Latest R2 dispatch evaluation ownership cut: `RuntimeDispatchEvaluationWorld` now owns bounded dynamic active-controller dispatch-param fallback outside `PlayableMatchRuntime`: compiled numeric/Boolean values short-circuit, dynamic fallback expressions request a context from `RuntimeExpressionContextWorld`, numeric results are finite/truncated, Boolean params use numeric truthiness, and actor/opponent/owner/tick forwarding is test-covered. Claim allowed: current active-controller dynamic dispatch params have a named, testable world boundary. Claim blocked: full dynamic-param parity, persistent-controller timing, exact CNS controller tick order, helper/team/redirect parameter scopes, visual parity, score movement, and full MUGEN/IKEMEN controller VM parity.
- Latest R2 trigger evaluation ownership cut: `RuntimeTriggerEvaluationWorld` now owns bounded normalized `TriggerIr` expression evaluation outside `PlayableMatchRuntime`: actor/opponent/owner/tick forwarding, context-factory handoff, raw expression result capture, and Boolean pass/fail projection. Focused `RuntimeTriggerEvaluationSystem` coverage proves context forwarding, normalized expression use, and false zero-result behavior. Claim allowed: current active/state-entry single-trigger evaluation has a named, testable world boundary. Claim blocked: full expression language parity, persistent-controller timing, exact CNS trigger tick order, helper/team/redirect trigger scopes, visual parity, score movement, and full MUGEN/IKEMEN trigger VM parity.
- Latest R2 trigger gate ownership cut: `RuntimeTriggerGateWorld` now owns bounded CNS trigger grouping/order outside `PlayableMatchRuntime`: `triggerall` AND preconditions, numbered `triggerN` OR groups, no-numbered-trigger pass-through, and short-circuit ordering. Focused `RuntimeTriggerGateSystem` coverage proves `triggerall` failure skips numbered groups, first passing numbered group wins, no-numbered groups pass after `triggerall`, all-failing numbered groups fail, and callback actor/opponent/owner/tick context is forwarded. Claim allowed: current active/state-entry controller trigger filtering has a named, testable world boundary. Claim blocked: full expression language parity, persistent-controller timing, exact CNS trigger tick order, helper/team/redirect trigger scopes, visual parity, score movement, and full MUGEN/IKEMEN trigger VM parity.
- Latest R2 auto guard-start ownership cut: `RuntimeAutoGuardStartWorld` now owns bounded imported auto guard-start orchestration outside `PlayableMatchRuntime`: imported-defender filtering, current input/current move/hitpause/hitstun eligibility handoff to `RuntimeGuardWorld`, `InGuardDist` gate, guard-start state selection/availability, clear-state-owner entry, and guard-start runtime mutation. Focused `RuntimeAutoGuardStartSystem` coverage proves successful imported guard start plus non-imported, ineligible, out-of-distance, and unavailable-state fail-closed paths. Claim allowed: current bounded imported auto guard-start orchestration has a named, testable world boundary. Claim blocked: exact proximity-guard timing, guard-end/effects/audio, helper/team/redirect guard ownership, visual parity, score movement, and full MUGEN/IKEMEN guard VM parity.
- Latest R2 active controller scan ownership cut: `RuntimeActiveControllerScanWorld` now owns bounded active-state controller scanning outside `PlayableMatchRuntime`: owner/state-owner selection, imported/owner-backed guard, active state lookup, `ignorehitpause` filtering, trigger gating, controller iteration, and stop/continue flow after state-changing controllers. Focused `RuntimeActiveControllerScanSystem` coverage proves owner-backed scanning, hitpause-only filtering, stop-after-ChangeState flow, missing-state skip, non-imported skip, and failed-trigger no-op. Claim allowed: current bounded active controller scan has a named, testable world boundary. Claim blocked: exact CNS VM tick order, persistent controller semantics, helper/team/redirect controller scopes, full controller-loop parity, visual parity, score movement, and full MUGEN/IKEMEN active-controller parity.
- Latest R2 TargetState entry ownership cut: `RuntimeTargetStateEntryWorld` now owns bounded active-state `TargetState` state-entry routing outside `PlayableMatchRuntime`: the controller actor's existing `stateOwner` is preserved, the controller actor becomes the owner otherwise, unavailable target states fail closed, and successful entries route the target into owner-backed state data through explicit hooks. Focused `RuntimeTargetStateEntrySystem` coverage proves controller-owned entry, owner-backed custom-state preservation, and unavailable-state no-op. Claim allowed: current bounded active-state TargetState adapter has a named, testable world boundary. Claim blocked: exact TargetState tick order, throws, helper/team/multi-target target state selection, full custom-state parity, visual parity, score movement, and full MUGEN/IKEMEN TargetState parity.
- Latest R2 helper TargetState ownership cut: `RuntimeHelperTargetStateWorld` now owns bounded helper-owned `TargetState` state-entry routing outside `PlayableMatchRuntime`: helper/owner identity is checked before target lookup, missing targets fail closed, unavailable target states fail closed, and successful entries route the target into owner-backed state data through explicit state-entry hooks. Focused `RuntimeHelperTargetStateSystem` coverage proves owner-backed entry, owner-mismatch skip, missing-target no-op, and unavailable-state no-op. Claim allowed: current bounded helper-owned TargetState adapter has a named, testable world boundary. Claim blocked: helper-owned custom state table parity, throws, teams/simul, multi-target/helper-owned opponent selection, exact helper TargetState timing, visual parity, score movement, and full helper TargetState parity.
- Latest R2 helper Projectile target mirror ownership cut: `RuntimeHelperProjectileTargetWorld` now owns bounded helper-parented Projectile target-memory mirroring outside `PlayableMatchRuntime`: owner-parented Projectile contacts skip, helper-parented Projectile contacts resolve the parent Helper by runtime serial, missing helpers fail closed, and successful contacts write the Projectile target id into helper-local target memory through `TargetSystem`. Focused `RuntimeHelperProjectileTargetSystem` coverage proves mirror, owner-projectile skip, and missing-helper no-op behavior. Claim allowed: current bounded helper-parented Projectile contact target mirror has a named, testable world boundary. Claim blocked: exact helper Projectile target lifetime, helper-owned custom-state tables, teams/simul, multi-target/helper-owned opponent selection, exact combat/effect tick order, visual parity, score movement, and full helper Projectile parity.
- Latest R2 State -1 routing ownership cut: `RuntimeStateEntryRouteWorld` now owns bounded State -1 `ChangeState` route selection outside `PlayableMatchRuntime`: state-entry iteration, non-`ChangeState` filtering, trigger gating, dynamic state-id resolver handoff, route telemetry, authored state-move selection, and raw state-entry fallback. Focused `RuntimeStateEntryRouteSystem` coverage proves route-to-move, route-to-state, failed trigger / unresolved state-id no-op scans, dynamic expression handoff, and empty-list skip behavior. Claim allowed: current bounded command/CNS State -1 `ChangeState` route scan has a named, testable world boundary. Claim blocked: exact State -1 VM ordering, persistent controller semantics, helper/team/redirect command ownership, full CNS VM parity, score movement, and full MUGEN/IKEMEN command routing parity.
- Latest R2 helper direct-combat ownership cut: `RuntimeHelperCombatWorld` now owns bounded helper-owned direct `HitDef` contact resolution outside `PlayableMatchRuntime`: helper iteration, active-window/contact checks, `HitBy`/`NotHitBy` reject logging, direct hit/guard handoff, imported default get-hit/guard-state hooks, helper target-memory remembering, contact presentation, and helper runtime-state sync. Focused `RuntimeHelperCombatSystem` coverage proves hit, guard, target-memory, presentation telemetry, default state hooks, and reject no-op behavior. Claim allowed: current bounded helper-owned direct-HitDef path has a named, testable world boundary. Claim blocked: exact helper hitpause/tick order, helper-owned custom-state tables, multi-target/team helper combat, helper-owned Projectile combat/contact presentation, visual parity, score movement, and full Helper VM parity.
- Latest R1 required EnemyNear indexed 1v1 cut: required `synthetic-imported-enemynear-index.json` checksum `b97e2eda` gates bounded State -1 `EnemyNear(0), StateNo` routing into state `284` while `EnemyNear(1), StateNo` fails closed and forbidden trap state `285` stays absent. `pnpm qa:trace` now passes 211/211 artifacts, 191 required and 20 optional. Claim allowed: portable synthetic imported route proves current one-on-one indexed EnemyNear redirect behavior without private assets. Claim blocked: team/simul nth-nearest enemy selection, helper-owned opponent lists, redirect mutation, dynamic-index parity, exact opponent ordering/lifetime, score movement, and full MUGEN/IKEMEN EnemyNear parity.
- Previous R1 required HitDef data-spark fallback cut: required `synthetic-imported-hitdef-data-spark.json` checksum `d950796c` and `synthetic-imported-hitdef-data-guard-spark.json` checksum `7bd9d162` gate omitted direct `HitDef` `sparkno` / `guard.sparkno` inheriting parsed CNS `[Data] sparkno = 2` / `guard.sparkno = 40` through typed `HitDef` `ControllerOp` data and emitting bounded hit/guard `HitSpark` telemetry with authored `sparkxy` offsets. `pnpm qa:trace` now passes 210/210 artifacts, 190 required and 20 optional. Claim allowed: portable synthetic imported routes prove current default HitDef spark fallback telemetry without private assets. Claim blocked: exact FightFX/common lookup, renderer/audio playback parity, broad HitDef effects, public KFM assets, score movement, and full MUGEN/IKEMEN effect parity.
- Previous R1 optional KFM QCF_x contact cut: optional `kfm-official-qcf-x.json` passes with checksum `349cd995` when the private fixture exists. The gate now requires real KFM `QCF_x` command input, routed/executed state `1000`, ordered actor-frame evidence for crouch/walk-in into `11 -> 20 -> 1000`, ordered `ChangeState` / `PosAdd` / `HitDef` controller evidence, typed `kinematic:posadd` / `hitdef` evidence, attacker-side `PlaySnd` telemetry for authored move sound `S0,3` and contact hitsound `S5,4`, inherited `[Data] sparkno = 2` hit-spark telemetry with authored `sparkxy = -10,-60`, shared sound+spark contact package metadata, target link `p1 -> p2 / 0`, final P1 `power = 35` / `targetCount = 1`, and final P2 `life = 910` / hitfall telemetry. Claim allowed: the private official KFM fixture proves the current bounded Kung Fu Palm input-to-contact route including default HitDef spark fallback. Claim blocked: public bundled KFM assets, exact special timing, exact pushback, renderer/audio playback parity, broad special-move compatibility, score movement, and full MUGEN/IKEMEN special-route parity.
- Previous R1 optional KFM air-recovery precision cut: optional `kfm-official-default-fall-recovery-input.json` still passes with checksum `151486e1`, but its gate now requires ordered private official KFM actor-frame evidence for `5050 -> 5210 -> 52` and bounded controller/typed-operation evidence through `5050` `VelAdd` / `ChangeState`, `5210` `PalFX`, `sprite-effect:palfx`, `PosFreeze`, `bounds:posfreeze`, `Turn`, `orientation:turn`, `NotHitBy`, `eligibility:nothitby`, `VelMul`, `VelAdd`, `ChangeState`, `52` `VarSet`, and state `0` `VelSet` / `kinematic:velset`. That checkpoint passed 208/208 artifacts, 188 required and 20 optional. Claim allowed: the private official KFM fixture confirms the current bounded air recovery-input route with stricter controller/operation and actor-frame evidence. Claim blocked: public bundled KFM assets, exact recovery threshold tables, exact velocity math, exact controller-loop tick order, guard-state parity, score movement, and full MUGEN/IKEMEN recovery parity.
- Latest R2 player Projectile default Target-controller cut: required `synthetic-imported-projectile-default-target-controllers.json` checksum `1c1a3e77` gates a player-owned Projectile-only route where state `200` spawns a Projectile with omitted `projid` / `id`, P2 is hit for 31, owner target link `p1 -> p2 / 0` is recorded, and delayed owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` execute against P2 without direct HitDef controller evidence in the isolated fixture. Evidence includes Projectile anim `915`, lifecycle spawn/remove, effect payload `ownerId = p1` / `effectId = 0` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, TargetBind offset `36,-12`, final P1 `targetCount = 0`, and final P2 `life = 949` / `power = 40`. `pnpm qa:trace` now passes 208/208 artifacts, 188 required and 20 optional. Previous player Projectile default TargetState proof remains required as `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa`; previous explicit-id player Projectile TargetState proof remains required as `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962`; previous player Projectile Target-controller proof remains required as `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2`; previous player Projectile target redirect proof remains required as `synthetic-imported-projectile-target-redirect.json` checksum `cd099094`; previous helper Projectile bare Target proof remains required as `synthetic-imported-helper-projectile-bare-target.json` checksum `8c9129c1`; previous helper Projectile default TargetState proof remains required as `synthetic-imported-helper-projectile-default-targetstate.json` checksum `918c42a1`; previous helper Projectile explicit TargetState proof remains required as `synthetic-imported-helper-projectile-targetstate.json` checksum `b12e1cb3`; previous helper Projectile default Target-controller proof remains required as `synthetic-imported-helper-projectile-default-target-controllers.json` checksum `0c4c69ae`; previous helper Projectile explicit Target-controller proof remains required as `synthetic-imported-helper-projectile-target-controllers.json` checksum `58688be8`; previous helper TargetState proof remains required as `synthetic-imported-helper-targetstate.json` checksum `011633b8`; previous helper Target-controller proof remains required as `synthetic-imported-helper-target-controllers.json` checksum `61f4c61e`; previous helper direct bare `Target, Life` proof remains required as `synthetic-imported-helper-bare-target.json` checksum `15f3c1db`; previous player bare `Target, Life` proof remains required as `synthetic-imported-bare-target-redirect.json` checksum `f9c90aa8`; previous default `Target(0), Life` proof remains required as `synthetic-imported-default-target-redirect.json` checksum `d43caabf`; previous default `NumTarget(0)` proof remains required as `synthetic-imported-default-numtarget.json` checksum `5869ebbd`; previous helper direct default-target proof remains required as `synthetic-imported-helper-default-target.json` checksum `e1bcced0`. Claim allowed: bounded direct player target memory, player-owned Projectile explicit/default target memory reads, direct HitDef plus player-owned Projectile target-memory mix, Target side effects, and TargetState into owner-backed state data, helper-owned explicit/default/bare HitDef target memory, helper-parented Projectile explicit/default/bare target memory, target-link evidence, owner/helper-local `NumTarget(id)` / `NumTarget(0)`, bare `Target, Life`, static `Target(id), Life`, dynamic `Target(expr), Life` trigger reads, bounded helper-owned direct-HitDef and explicit/default helper-parented Projectile Target side effects, and helper-local direct-HitDef plus explicit/default helper-parented Projectile TargetState into owner-backed state data are trace-gated. Claim blocked: Target* mutation mixing, helper-owned custom state tables, throws, teams/simul, multi-target/helper-owned opponent selection, exact target lifetime/tick order, exact final-animation parity, exact helper hitpause/tick order, exact helper HitDef/Projectile lifetime parity, visual parity, score movement, and full helper target/combat/projectile/custom-state parity.
- Previous R2 player Projectile default TargetState cut: required `synthetic-imported-projectile-default-targetstate.json` checksum `8f35f1fa` gates a player-owned Projectile-only route where omitted `projid` / `id` records default target link `p1 -> p2 / 0` and delayed owner-local `TargetState value = 888` routes P2 through attacker-owned TargetState data `888 -> 889 -> 0`.
- Previous R2 player Projectile explicit TargetState cut: required `synthetic-imported-projectile-targetstate.json` checksum `dd1c7962` gates a player-owned Projectile-only route where state `200` spawns Projectile id `77`, records owner target link `p1 -> p2 / 77`, and routes P2 through attacker-owned TargetState data `888 -> 889 -> 0`.
- Previous R2 player Projectile Target-controller cut: required `synthetic-imported-projectile-target-controllers.json` checksum `8c7bd6c2` gates a player-owned Projectile-only route where state `200` spawns Projectile id `77`, P2 is hit for 31, owner target link `p1 -> p2 / 77` is recorded, and delayed owner-local `TargetLifeAdd`, `TargetPowerAdd`, `TargetVelSet`, `TargetVelAdd`, `TargetFacing`, `TargetBind`, and `TargetDrop` execute against P2 without direct HitDef controller evidence in the isolated fixture. Evidence includes Projectile anim `912`, lifecycle spawn/remove, effect payload `ownerId = p1` / `effectId = 77` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, TargetBind offset `36,-12`, final P1 `targetCount = 0`, and final P2 `life = 949` / `power = 40`.
- Previous R2 player Projectile target redirect cut: required `synthetic-imported-projectile-target-redirect.json` checksum `cd099094` gates a player-owned Projectile-only route where state `200` spawns Projectile id `77`, P2 is hit for 31, owner target link `p1 -> p2 / 77` is recorded, and P1 branches to state `277` through `NumTarget(77)` plus `Target(77), Life <= 969`. Evidence includes Projectile anim `911`, lifecycle spawn/remove, effect payload `ownerId = p1` / `effectId = 77` / `hasHit = true` / `removalReason = hit` / `terminalReason = hit`, final P2 `life = 969`, and no direct HitDef controller requirement in the isolated fixture.
- Previous R2 helper direct default-target cut: required `synthetic-imported-helper-default-target.json` checksum `e1bcced0` gates a helper-owned direct `HitDef` with no `id` defaulting to target id `0`, hitting P2 for 33, recording helper target link `p1-helper-0 -> p2 / 0`, and branching helper state `1229/961` through `NumTarget(0)` plus `Target(0), Life`. Previous helper Projectile default-target proof remains required as `synthetic-imported-helper-projectile-default-target.json` checksum `b0daddf6`; previous helper Projectile explicit-id proof remains required as `synthetic-imported-helper-projectile-target.json` checksum `49261b53`; previous helper direct target proof remains required as `synthetic-imported-helper-target.json` checksum `68f95b67`; previous helper direct-combat proof remains required as `synthetic-imported-helper-hitdef.json` checksum `89f9e876`.
- Latest R1 Common1 air-recovery cut: required `synthetic-imported-default-fall-official-air-recovery.json` checksum `b0363be9` gates an official-style synthetic `5050 -> 5210 -> 52 -> 0` route after `command = "recovery"` while airborne. Actor-frame sequence evidence requires positive-to-zero `fall.recovertime` in `5050`, `5210` recovery velocity telemetry, `52` landing y = 0 evidence, and final idle/control. Controller-order evidence requires `VelAdd`, `ChangeState`, `VelSet`, `HitFallSet`, `CtrlSet`, and typed `kinematic:*`, `hitfall:hitfallset`, and `resource:ctrlset` operations. That checkpoint passed 187/187 artifacts, 167 required and 20 optional. Claim allowed: bounded Common1-style official air recovery sequence has required deterministic trace evidence. Claim blocked: exact `fall.recovertime` tables, velocity math, controller-loop timing, public bundled KFM support, visual parity, score movement, and full MUGEN/IKEMEN recovery parity.
- Latest R1 presentation precision cut: `RuntimeTraceGate.requiredHitEffectEvents` now supports `assetFrameOffsetX`, `assetFrameOffsetY`, and `assetFrameDuration` in addition to `offsetX` / `offsetY`, and direct common/FightFX, Projectile, and helper-parented Projectile contact-package gates require selected AIR-frame offset/duration plus authored `sparkxy` offsets in addition to existing sound, contact, source-frame, and multi-frame AIR metadata. Claim allowed: spark package trace requirements now prove selected frame metadata before renderer/audio handoff. Claim blocked: exact visual binding, layer, timing, scale, palette, SND playback, helper-owned presentation ownership, and full MUGEN/IKEMEN presentation parity.
- Previous R1 Common1 ground-recovery cut: required `synthetic-imported-default-fall-official-ground-recovery.json` checksum `74b72495` gates an official-style synthetic `5050 -> 5200 -> 5201 -> 52 -> 0` route after `command = "recovery"` near ground with positive-to-zero `fall.recovertime`, bounded recovery/landing telemetry, `NotHitBy` safety, and final idle/control.
- Previous completed R2 cut: `RuntimeCombatResolutionWorld` owns bounded active direct/projectile contact orchestration from `PlayableMatchRuntime`: direct move eligibility, reversal checks, HitBy/NotHitBy reject logging, HitOverride redirect hooks, target-memory remembering, direct hit/guard result handoff, projectile-combat callbacks, received-damage/contact memory routing, and contact presentation emission. `PlayableMatchRuntime` still supplies runtime tick, frame hurtboxes, concrete state-entry hooks, trigger/controller order, active effect stores, and actor roster. Focused `RuntimeCombatResolutionSystem` coverage proves direct target/contact/presentation ordering and projectile callback routing through target/contact/presentation/damage hooks. Claim allowed: current player-owned direct/projectile contact orchestration has a named, testable world boundary. Claim blocked: projectile target ownership, exact direct/projectile tick order, multi-target/team behavior, exact ReversalDef/HitOverride priority, visual parity, score movement, and full MUGEN/IKEMEN combat VM parity.
- Previous R1 Common1 recovery timer evidence cut: official-style synthetic recovery threshold / too-early rejection routes are now required trace gates. Required `synthetic-imported-default-fall-official-recovery-threshold.json` checksum `86804271` proves an official-style `5050 -> 5200 -> 5201 -> 52 -> 0` ground-recovery threshold route where P2 `5050` actor-frame evidence drops `hitFall.recoverTime` from positive to `0` before state `5200`; required `synthetic-imported-default-fall-official-recovery-too-early.json` checksum `ef945ff5` proves the same style of early `command = "recovery"` rejection keeps P2 in `5050` while minimum observed `recoverTime >= 1` and recovery states remain forbidden. Previous required `synthetic-imported-default-fall-recovery.json` checksum `d83797d9`, `synthetic-imported-default-fall-recovery-threshold.json` checksum `7bb15a5f`, `synthetic-imported-default-fall-recovery-tick-order.json` checksum `e2691aab`, `synthetic-imported-default-fall-recovery-too-early.json` checksum `050e7e3c`, and optional `kfm-official-default-fall-recovery-too-early.json` checksum `d2edbde4` remain active. That R1 checkpoint passed 180/180 artifacts, 160 required and 20 optional. Claim allowed: bounded Common1-style lie-down/get-up, recovery-input countdown, official-style ground-recovery threshold, and early-recovery reject windows now prove countdown across observed frames. Claim blocked: exact `down.recovertime` / `fall.recovertime` tables, exact controller-loop tick order, animation timing, velocity math, public bundled KFM support, and full MUGEN/IKEMEN fall recovery parity.
- Previous R2 helper projectile-contact trigger evidence cut: required `synthetic-imported-helper-projcontact.json` checksum `4dcbdd25` gates bounded helper-local `ProjContact(8855)` plus `ProjContactTime(8855) >= 1` after a visual Helper spawns owner-side Projectile anim `951` with `parentId = p1-helper-0` and routes through `1200 -> 1220 -> 1221` / anims `949` and `950` once P2 guards. Focused `EffectActorSystem` coverage proves same-id player-owned Projectile contact does not satisfy helper-local `ProjContact`, while helper-parented Projectile contact does after the bounded contact age advances. That checkpoint passed 185/185 artifacts, 165 required and 20 optional. Claim allowed: helper-local `ProjContact(id)` / `ProjContactTime(id)` can branch on helper-parented owner-side Projectile generic contact markers in the bounded micro-VM. Claim blocked: helper-owned Projectile combat/contact presentation, helper-owned target memory, exact projectile namespaces/scopes, exact `ProjContact` / `ProjHit` / `ProjGuarded` tick order and lifetime, dynamic ids/params, teams, helper-owned effect namespaces, exact tick order, and full Helper/Projectile parity.
- Previous R2 helper projectile-guard trigger evidence cut: required `synthetic-imported-helper-projguard.json` checksum `d2e2f20d` gates bounded helper-local `ProjGuarded(8854)` plus `ProjGuardedTime(8854) >= 1` after a visual Helper spawns owner-side Projectile anim `948` with `parentId = p1-helper-0` and routes through `1200 -> 1218 -> 1219` / anims `938` and `947` once P2 guards. Focused `EffectActorSystem` coverage proves same-id player-owned Projectile guard contact does not satisfy helper-local `ProjGuarded`, while helper-parented Projectile guard contact does after the bounded contact age advances. Claim allowed: helper-local `ProjGuarded(id)` / `ProjGuardedTime(id)` can branch on helper-parented owner-side Projectile guard markers in the bounded micro-VM. Claim blocked: helper-owned Projectile combat/contact presentation, helper-owned target memory, exact projectile namespaces/scopes, exact `ProjContact` / `ProjHit` / `ProjGuarded` tick order and lifetime, dynamic ids/params, teams, helper-owned effect namespaces, exact tick order, and full Helper/Projectile parity.
- Latest R1 resource actor-frame evidence cut: `RuntimeTraceGate.requiredActorFrames` can now require observed life/power ranges, and required `synthetic-imported-resource.json` checksum `7bbcb2e4` requires imported P1 state/action `289` actor-frame evidence with life `750` and power `900` after typed `resource:lifeadd`, `resource:lifeset`, `resource:poweradd`, and `resource:powerset` execute and route through `Life` / `Power` triggers. Claim allowed: bounded owner-local life/power writes are now visible in actor-frame evidence before final actor checks. Claim blocked: exact scaling, redirects, helper/team ownership, round/KO flow, dynamic lowering, and full controller VM parity.
- Previous helper-effect cut: required `synthetic-imported-helper-projectile.json` checksum `b6269136` gates bounded helper-local `Projectile` execution in `HelperSystem`; a spawned visual Helper routes from `1200` to `1212` / anim `932` while spawning owner-side projectile anim `943` with `parentId = p1-helper-0`. Previous `synthetic-imported-helper-numhelper.json` checksum `4e32e951` gates bounded helper-local `NumHelper(42) > 0`; previous `synthetic-imported-helper-numexplod.json` checksum `4328278a` gates bounded helper-local `NumExplod(8830) > 0` after helper-local static `Explod`; previous `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` gates bounded helper-local static `ModifyExplod`; previous `synthetic-imported-helper-removeexplod.json` checksum `ff8658a2` gates bounded helper-local static `RemoveExplod`; previous `synthetic-imported-helper-explod.json` checksum `87ae363f` gates bounded helper-local static `Explod`; previous `synthetic-imported-helper-bindtoroot.json` checksum `bf72306c` gates bounded helper-local static `BindToRoot`; previous `synthetic-imported-helper-bindtoparent.json` checksum `f9922c0e` gates bounded helper-local static `BindToParent`; previous `synthetic-imported-helper-enemynear.json` checksum `35498955` still gates bounded helper-local `EnemyNear, StateNo` plus `EnemyNear, Life`; previous `synthetic-imported-helper-ishelper.json` checksum `37877602` still gates `IsHelper(42)`. That helper-Projectile checkpoint passed 178/178 artifacts, 158 required and 20 optional. Claim allowed: current visual Helper actors can branch on helper identity, read current owner/opponent state through bounded helper-local redirects, bind position to supplied owner/root runtime state with static bind params, spawn a bounded owner-side visual Explod from helper-local static params when the referenced AIR action exists, remove that owner-side Explod by static id, mutate a helper-parented owner-side Explod by static id, count helper-parented owner-side Explods by static id, count owner-side visual Helpers by static helper id, and spawn bounded owner-side Projectile actors with helper parent metadata. The latest indexed-context cut adds caller-provided `EnemyNear(index)` list reads on top of this older proof. Claim blocked: player-state `BindToParent` / `BindToRoot`, exact helper effect-count/ownership parity, helper-owned projectile combat/contact presentation, helper-owned target memory, helper-owned custom state tables, dynamic bind/effect params, position rebinding, broader indexed/team/helper-owned redirect ownership, teams/simul/turns, `keyctrl`, nested helper ancestry where root differs from parent, helper-owned opponents, helper-owned effect namespaces, exact helper `HitDef` lifetime/multi-hit parity, full helper combat/contact parity, helper-bound Explod timing beyond bounded static spawn/remove/modify/count-id/count-projectile routes, exact helper/opponent/parent/root selection, exact helper resource scopes, exact helper tick order, and full MUGEN/IKEMEN helper parity.
- Previous completed R2 cut: `RuntimeTargetWorld.resolveCandidates` now owns bounded target-candidate filtering from live target memory before current Target* / BindToTarget controller application and active TargetBind / BindToTarget position application. The match runtime still supplies the currently materialized actor roster and owns trigger order, state validation, helper/projectile actor materialization, and combat context. Focused `TargetSystem` coverage proves actor-id and target-id candidate filtering plus mutation only against remembered targets. Claim allowed: current two-actor target side effects and bindings have a named candidate-resolution boundary. Claim blocked: helper/projectile target ownership, exact team/multi-target selection, exact target lifetime, throw binding, exact bind tick order, visual parity, score movement, and full MUGEN/IKEMEN target VM parity.
- Previous completed R2 cut: `RuntimeExpressionContextWorld` now owns bounded active runtime expression/trigger context creation for imported state triggers and dynamic controller-param fallback. `PlayableMatchRuntime` delegates target redirects, contact/projectile/effect count reads, command/const/state/anim/hitvar reads, `HitDefAttr`, `HitPauseTime`/`HitOver`/`HitShakeOver`, `InGuardDist`, random/stage/time wiring through that world while still owning active-state dispatch, next-random source, animation timing callbacks, and exact VM timing. Focused `RuntimeExpressionContextSystem` coverage proves numeric reads, `Target` redirect, compiled trigger evaluation, const/state/HitVar helpers, and shared context creation. Claim allowed: current active runtime expression/trigger read context has a named, testable world boundary. Claim blocked: full expression language parity, composite `HitDefAttr` parity, helper/team/redirect mutation, exact VM timing, visual parity, score movement, and full MUGEN/IKEMEN runtime parity.
- Previous completed R2 cut: `RuntimeStateTransitionControllerWorld` now owns bounded passive `ChangeState` / `SelfState` setup in the basic `StateControllerExecutor` path. `StateControllerExecutor` delegates raw-param `value` / `stateno` expression fallback, previous-state metadata writes, frame/time reset, optional `ctrl`, and missing-value reporting to the world while still owning controller routing, expression context creation, and broader runtime-controller execution; `RuntimeStateEntryWorld` and `PlayableMatchRuntime` still own active-state entry, concrete state/action lookup, custom-state owner selection, and controller tick order. Focused `StateTransitionControllerSystem` coverage proves expression fallback, metadata writes, reset, ctrl, missing-value no-op/reporting, unchanged-state timing reset, and executor routing. Claim allowed: current passive state-transition controller setup has a named, testable world boundary. Claim blocked: exact ChangeState/SelfState tick-order parity, persistent controller semantics, redirects/helper/team ownership, full custom-state breadth, state-entry VM parity, and score movement.
- Previous completed R1 cut: required `synthetic-imported-changeanim2-elem.json` checksum `b0b46d33` now gates one bounded active-state `ChangeAnim2` owner-AIR route: imported P1 enters state `200`, executes `ChangeAnim2 value = 207`, `elem = 3`, `elemtime = 1`, and final evidence ends at `frameIndex = 2` / `animTime = 20` against authored frame durations `[4,5,6]`. Claim allowed: known owner-backed active `ChangeAnim2` element/time positioning has portable trace evidence. Claim blocked: missing-action fallback, redirects/helper/team ownership, multi-import SFF namespaces, exact controller tick order, full animation-source parity, visual parity, and score movement.
- Previous completed R2 cut: `RuntimeAnimationControllerWorld` now owns bounded passive `ChangeAnim` / `ChangeAnim2` setup in the basic `StateControllerExecutor` path. `StateControllerExecutor` delegates raw-param animation retargeting, self/state-owner source marking, reset, and bounded `elem` / `elemtime` seeding to the world while still owning controller routing, expression context creation, and broader runtime-controller execution; `PlayableMatchRuntime` still owns active-state action lookup, state-owner selection, and controller tick order. Focused `AnimationControllerSystem` coverage proves expression fallback, known-AIR `elem` / `elemtime` seeding, clamped/fallback element behavior, missing-value no-op, and executor routing. Claim allowed: current passive animation-controller setup has a named, testable world boundary. Claim blocked: missing-action fallback, broader active-state edge cases, redirects/helper/team ownership, full state-owner namespace behavior, exact animation-source parity, and score movement.
- Previous completed R2 cut: `RuntimeKinematicControllerWorld` now owns bounded passive `VelSet`, `VelAdd`, `VelMul`, `HitVelSet`, `PosSet`, `PosAdd`, and `Gravity` setup from typed `kinematic:*` operations or raw controller params. `StateControllerExecutor` delegates those mutations to the world while still owning controller routing, expression context creation, and broader runtime-controller execution; `RuntimeKinematicsWorld` still owns per-frame actor integration, sandbox gravity, ground snap, and landing hooks. Focused `KinematicControllerSystem` coverage proves typed setup, raw expression fallback, default-axis behavior, hit-velocity flags, gravity defaults, and executor routing. Claim allowed: current passive kinematic controller setup has a named, testable world boundary. Claim blocked: exact MUGEN/IKEMEN physics, velocity tick order, `yaccel` constants, helper/team/redirect ownership, full kinematic VM parity, and score movement.
- Previous completed R2 cut: `RuntimeBoundsControllerWorld` now owns bounded passive `PlayerPush`, `PosFreeze`, and `ScreenBound` setup from typed `collision:playerpush` / `bounds:*` operations or raw controller params. `StateControllerExecutor` delegates those mutations to the world while still owning controller routing, expression context creation, and broader runtime-controller execution; `RuntimeActorConstraintWorld` still owns per-frame reset/projection, stage clamp, and body-push separation. Focused `BoundsControllerSystem` coverage proves typed setup, raw defaults, raw expression fallback, and executor routing. Claim allowed: current passive actor-bounds controller setup has a named, testable world boundary. Claim blocked: exact player/edge collision, team/helper push behavior, screen-edge/camera parity, PosFreeze tick order, full constraint VM parity, and score movement.
- Previous completed R2 cut: `RuntimeHitFallControllerWorld` now owns bounded passive `HitFallVel`, `HitFallDamage`, and `HitFallSet` mutations from typed `hitfall:*` operations or raw controller params. `StateControllerExecutor` delegates those mutations to the world while still owning controller routing, expression context creation, and broader runtime-controller execution. Focused `HitFallControllerSystem` coverage proves typed `HitFallSet`, raw expression fallback, stored fall velocity application, bounded `fall.defence_up` scaling, and nonlethal deferred fall damage. Claim allowed: current HitFall controller mutation has a named, testable world boundary. Claim blocked: exact Common1 controller-loop order, helper/team/redirect ownership, exact recovery thresholds/velocity math, full fall/get-hit parity, and score movement.
- Previous completed R2 cut: `RuntimeStateTypeWorld` now owns bounded passive `StateTypeSet` `stateType` / `moveType` / `physics` setup from typed `metadata:statetypeset` operations or raw controller params. `StateControllerExecutor` delegates those mutations to the world while still owning controller routing and broader runtime-controller execution. Focused `StateTypeSystem` coverage proves typed setup, raw case-normalized fallback, and invalid raw no-op behavior. Claim allowed: current StateTypeSet metadata setup has a named, testable world boundary. Claim blocked: dynamic metadata expressions, helper/team/redirect ownership, exact physics/tick-order interactions, full StateTypeSet parity, and score movement.
- Previous completed R2 cut: `RuntimeDamageScaleWorld` now owns bounded passive `AttackMulSet` and `DefenceMulSet` multiplier setup from typed `damage-scale:*` operations or raw controller params. `StateControllerExecutor` delegates those mutations to the world while still owning controller routing, expression context creation, and broader runtime-controller execution. Focused `DamageScaleSystem` coverage proves typed setup, raw expression fallback, clamp behavior, and no-value no-op behavior. Claim allowed: current AttackMulSet/DefenceMulSet multiplier setup has a named, testable world boundary. Claim blocked: exact MUGEN/IKEMEN scaling stack/order, helper/projectile/custom-state/guard edge cases, redirect ownership, controller-loop timing, full damage-scale parity, and score movement.
- Previous completed R2 cut: `RuntimeHitDefenseWorld` now owns bounded passive defensive slot setup for `HitBy`, `NotHitBy`, and `HitOverride` from typed `eligibility:*` / `hitoverride` operations or raw controller params. `StateControllerExecutor` delegates those mutations to the world while still owning controller routing, expression context creation, and broader runtime-controller execution. Focused `HitDefenseSystem` coverage proves typed and raw setup/removal semantics. Claim allowed: current passive hit-eligibility and HitOverride slot installation has a named, testable world boundary. Claim blocked: exact attr grammar, slot priority, helper/custom-state redirect breadth, forceair/forceguard edge order, controller-loop timing, full HitBy/NotHitBy/HitOverride parity, and score movement.
- Previous completed R2 cut: `RuntimeHitDefControllerDispatchWorld` now owns bounded active-state HitDef activation dispatch from compiled CNS classification into the current attack payload: controller telemetry, typed `hitdef` operation selection, raw fallback attack data, fired-HitDef dedupe, current-frame hitbox handoff, currentMove mutation, attack movetype/control writes, and operation telemetry. Focused `HitDefSystem` coverage proves HitDef activation plus duplicate suppression through the boundary. Claim allowed: current active-state HitDef activation has a named, testable world boundary. Claim blocked: exact HitDef trigger lifetime, contact ordering, multi-hit windows, helper/projectile/custom-state ownership, priority classes beyond existing bounded direct route, broad attr grammar, exact hitpause/tick order, full HitDef VM parity, and score movement.
- Previous completed R2 cut: `RuntimeReversalControllerDispatchWorld` now owns bounded active-state ReversalDef side-effect dispatch from compiled CNS classification into `RuntimeReversalWorld`: controller telemetry, typed `reversaldef` operation selection, raw fallback activation payload, activation handoff, and operation telemetry. Focused `ReversalSystem` coverage proves controller/op telemetry plus ReversalDef activation through the boundary. Claim allowed: current active-state ReversalDef controller dispatch has a named, testable world boundary. Claim blocked: exact ReversalDef priority, guard/projectile/helper/custom-state counter breadth, attr grammar, trigger lifetime, hitpause/tick order, full ReversalDef VM parity, and score movement.
- Previous completed R2 cut: `RuntimeEffectSpawnControllerDispatchWorld` now owns bounded active-state Explod / RemoveExplod / ModifyExplod / Helper / Projectile / ModifyProjectile side-effect dispatch from compiled CNS classification into `RuntimeEffectSpawnWorld`: controller telemetry, typed effect operation selection, spawn/count mutation handoff, and success-gated operation telemetry. Focused `EffectSpawnSystem` coverage proves successful Explod telemetry/mutation and failed ModifyExplod no-operation gating through the boundary. Claim allowed: current active-state effect-spawn controller dispatch has a named, testable world boundary. Claim blocked: exact effect spawn tick order, helper-owned effect namespaces, dynamic effect params, helper-owned projectile combat/contact/target memory, full effect/helper/projectile VM parity, and score movement.
- Previous completed R2 cut: `RuntimeFallEnvShakeControllerDispatchWorld` now owns bounded active-state FallEnvShake side-effect dispatch from compiled CNS classification into `RuntimeEnvShakeWorld`: controller telemetry, typed `fallenvshake` operation selection, fall-shake event handoff, `hitFall.envShake` cleanup, and operation telemetry after a real event. Focused `EnvShakeSystem` coverage proves `FallEnvShake` telemetry/mutation through the boundary. Claim allowed: current active-state FallEnvShake controller dispatch has a named, testable world boundary. Claim blocked: exact waveform, pause/stage/layer interaction, helper/redirect ownership, full CNS/presentation VM parity, and score movement.
- Previous completed R2 cut: `RuntimeActorConstraintControllerDispatchWorld` now owns bounded active-state `Width` side-effect dispatch from compiled CNS classification into `RuntimeActorConstraintWorld`: controller telemetry, typed `collision:width` operation selection, operation telemetry, and body-width mutation handoff. Focused `ActorConstraintSystem` coverage proves `Width` telemetry/mutation through the boundary. Claim allowed: current active-state Width controller dispatch has a named, testable world boundary. Claim blocked: exact player/edge collision, team/helper push behavior, screen-edge/camera parity, PosFreeze tick order, Width edge semantics, full CNS/constraint VM parity, and score movement.
- Previous completed R2 cut: `RuntimePauseControllerDispatchWorld` now owns bounded active-state Pause/SuperPause side-effect dispatch from compiled CNS classification into the match pause handler: controller telemetry, typed `pause` operation selection, apply-controller callback handoff, and operation telemetry only after a real pause result. Focused `PauseSystem` coverage proves `SuperPause` telemetry/application through the boundary. Claim allowed: current active-state Pause/SuperPause controller dispatch has a named, testable world boundary. Claim blocked: exact pause layering, super background/sound/spark timing, helper/redirect ownership, full CNS/pause VM parity, and score movement.
- Previous completed R2 cut: `RuntimeEnvShakeControllerDispatchWorld` now owns bounded active-state EnvShake side-effect dispatch from compiled CNS classification into `RuntimeEnvShakeWorld`: controller telemetry, typed `envshake` operation selection, operation telemetry, and camera-shake event handoff. Focused `EnvShakeSystem` coverage proves `EnvShake` telemetry/mutation through the boundary. Claim allowed: current active-state EnvShake controller dispatch has a named, testable world boundary. Claim blocked: exact waveform, pause/stage/layer interaction, helper/redirect ownership, full CNS/presentation VM parity, and score movement.
- Previous completed R2 cut: `RuntimeEnvColorControllerDispatchWorld` now owns bounded active-state EnvColor side-effect dispatch from compiled CNS classification into `RuntimeEnvColorWorld`: controller telemetry, typed `envcolor` operation selection, operation telemetry, and stage-flash event handoff. Focused `EnvColorSystem` coverage proves `EnvColor` telemetry/mutation through the boundary. Claim allowed: current active-state EnvColor controller dispatch has a named, testable world boundary. Claim blocked: exact blend math, layer/window ordering, pause timing, renderer parity, helper/redirect ownership, full CNS/presentation VM parity, and score movement.
- Previous completed R2 cut: `RuntimeAudioControllerDispatchWorld` now owns bounded active-state audio side-effect dispatch from compiled CNS classification into `RuntimeAudioWorld`: controller telemetry, typed `audio:*` operation selection, operation telemetry, and `PlaySnd` / `StopSnd` event handoff. Focused `AudioEventSystem` coverage proves `PlaySnd` telemetry/mutation through the boundary. Claim allowed: current active-state audio controller dispatch has a named, testable world boundary. Claim blocked: exact SND playback, channel priority, mixing, FightFX/common fallback, helper/redirect ownership, full CNS/audio VM parity, and score movement.
- Latest completed R2 cut: `RuntimeAnimationWorld` now owns bounded active action lookup/reset plus `elem` / `elemtime` seeding for known AIR actions in addition to actor animation advancement and timing helpers. `PlayableMatchRuntime` delegates its local action retarget helper through the animation world while still owning active controller dispatch, state-owner selection, state entry, and controller ordering. Focused `RuntimeAnimationSystem` tests prove authored-action retargeting, same-action element retargeting, and missing-action no-mutation behavior; `pnpm qa:trace` remains stable at 251/251 artifacts, 231 required and 20 optional. Claim allowed: current active action retarget mutation has a named animation-world boundary. Claim blocked: exact AIR negative-duration semantics, missing-action fallback parity, full `elem` / `elemtime` parity, helper/team redirect namespaces, controller tick order, visual parity, score movement, and full MUGEN/IKEMEN animation VM parity.
- Latest completed R2 cut: `RuntimeControllerDispatchWorld` now owns bounded runtime-controller execution dispatch for active imported state controllers, State -1 setup controllers, and pre-facing `AssertSpecial` application. `PlayableMatchRuntime` delegates runtime replacement, evaluation context handoff, controller telemetry, typed-operation telemetry, and unsupported-controller reporting through that world while keeping trigger filtering, `ChangeState` / `ChangeAnim`, side-effect dispatch, and tick order explicit. Focused `RuntimeControllerDispatchSystem` coverage proves runtime mutation, telemetry hooks, dynamic `HitPauseTime` context, and unsupported reporting. Claim allowed: current runtime-controller dispatch has a named, testable world boundary. Claim blocked: exact CNS controller-loop order, persistent controller semantics, side-effect VM parity, helper/team/redirect execution, full controller VM parity, and score movement.
- Previous completed R2 cut: `RuntimeContactControllerDispatchWorld` now owns bounded active-state contact-memory side-effect dispatch from compiled CNS classification into `RuntimeContactMemoryWorld`: controller telemetry, typed `contact:*` operation selection, operation telemetry, `HitAdd` mutation, and `MoveHitReset` direct contact reset. Focused `ContactMemorySystem` coverage proves `HitAdd` telemetry/mutation and `MoveHitReset` reset telemetry through the boundary. Claim allowed: current active-state contact-memory controller dispatch has a named, testable world boundary. Claim blocked: exact combo lifetime, helper/projectile contact ownership, guard-count parity, full CNS VM parity, and score movement.
- Previous completed R2 cut: `RuntimeTargetControllerDispatchWorld` owns bounded active-state Target / BindToTarget side-effect dispatch from compiled CNS classification into `RuntimeTargetWorld`: controller telemetry, typed `target:*` / `bindtotarget` operation selection, operation telemetry, and mutation handoff with match-owned callbacks for damage scaling, TargetState entry, and target constants. Focused `TargetSystem` coverage proves `TargetLifeAdd` telemetry/mutation and `BindToTarget` anchor/position telemetry through the boundary. Claim allowed: current active-state Target / BindToTarget controller dispatch has a named, testable world boundary. Claim blocked: helper/projectile target ownership, exact multi-target semantics, throw binding, full CNS VM parity, and score movement.
- Previous completed R2 cut: `RuntimeSpriteEffectControllerWorld` owns bounded active-state sprite-effect side-effect dispatch from compiled CNS classification into `RuntimeSpriteEffectWorld`: controller telemetry, typed `sprite-effect:*` operation selection, operation telemetry, and mutation handoff for `SprPriority`, `PalFX`, `RemapPal`, `AfterImage`, `AfterImageTime`, `Trans`, and `AngleSet` / `AngleAdd` / `AngleDraw`. Focused `SpriteEffectSystem` coverage proves PalFX telemetry/mutation, RemapPal palette telemetry, AfterImage sampling, and `Trans` render-opacity mutation through the boundary. Claim allowed: current active-state sprite-effect controller dispatch has a named, testable world boundary. Claim blocked: exact visual tick order, helper/redirect ownership, renderer parity, full CNS VM parity, and score movement.
- Previous completed R2 cut: `RuntimeStateEntrySetupWorld` owns bounded imported State -1 setup-controller selection before command routing. `PlayableMatchRuntime` delegates imported-only guard, `ChangeState` bypass, trigger gating, setup-controller classification, and execution handoff through that world while concrete mutation still routes through `RuntimeControllerDispatchWorld`. Focused `RuntimeStateEntrySetupSystem` coverage proves imported setup execution, non-imported skip, trigger failure, and non-setup filtering. Claim allowed: current imported State -1 setup-controller selection has a named, testable world boundary. Claim blocked: exact State -1 ordering, persistent controller semantics, redirect/helper/team scopes, full CNS VM parity, and score movement.
- Previous target-redirect cut: required `synthetic-imported-bare-target-redirect.json` checksum `f9c90aa8` gates bounded bare `Target, Life` trigger reads after a direct `HitDef` contact seeds target id `77`, routing P1 from state `200` to `270` with `HitDef` and target-link evidence. Required `synthetic-imported-target-dynamic-redirect.json` checksum `9985b62a` gates bounded `Target(var(0)), Life` trigger reads after a direct `HitDef` contact seeds target id `77` and owner-local `var(0) = 77`, routing P1 from state `200` to `287` with `VarSet`, `HitDef`, and target-link evidence. Previous `synthetic-imported-target-redirect.json` checksum `89580963` still gates static `Target(77), Life` routing into state `286`. Claim allowed: current imported owner states can read a recent player target through bounded `Target, ...`, static `Target(id), ...`, and dynamic executable target-id expressions such as `Target(var(0)), ...`. Claim blocked: helper/projectile targets, unsupported or negative target-id expressions, mutation through redirects, multi-target selection, teams, exact target lifetime/tick order, and full MUGEN/IKEMEN target redirect parity.
- Previous trigger cut: required `synthetic-imported-identity.json` checksum `c9be5cf1` gates bounded `Name` / `P1Name` / `P2Name` / `AuthorName` and redirected `EnemyNear, AuthorName` State -1 routing into state `276` using current runtime fighter display name and author metadata. Claim allowed: imported State -1 routing can inspect current two-actor identity metadata. Claim blocked: team/simul/turns identity selection, multiple opponents, helper-owned identities, parent/root/target identity redirects, player-indexed semantics, exact string/localization edge behavior, and full MUGEN/IKEMEN identity parity.
- Latest controller cut: accepted player-state no-op controllers now compile into typed `noop:*` operation evidence before `StateControllerExecutor` preserves their no-mutation behavior. Required `synthetic-imported-noop.json` checksum `2877b222` now gates imported `Null`, browser no-op `ForceFeedback`, debug clipboard no-ops, deprecated `MakeDust`, helper-lifecycle player-state `DestroySelf`, and `HitDef` continuation. Claim allowed: those player-state no-op families are accepted, counted, and proven as typed no-op operations in compatibility telemetry. Claim blocked: device force feedback, debug text rendering, clipboard output, dust rendering, helper removal side effects from player states, helper/redirect ownership, persistent timing, exact CNS VM order, and full MUGEN/IKEMEN parity.
- Previous controller cut: `AssertSpecial` now compiles static flags into typed `assertspecial` operation evidence before `StateControllerExecutor` applies the existing bounded runtime flag writes. Required `synthetic-imported-assertspecial-control.json` checksum `144ac7bc` now gates both typed operation evidence and NoAutoTurn/NoWalk/Invisible runtime telemetry. Claim allowed: bounded imported AssertSpecial control flags can be proven as typed operation plus existing control/render telemetry. Claim blocked: dynamic/unparseable flags, global/team/helper ownership, pause-layer lifetimes, flag priority, broad persistence semantics, KFM/Common1 confirmation, and full MUGEN/IKEMEN AssertSpecial parity.
- Previous controller cut: `EnvShake` now compiles static `time`/`freq`/`ampl`/`phase` params into typed `envshake` operation evidence before `RuntimeEnvShakeWorld` emits bounded shake-event telemetry. Required `synthetic-imported-envshake.json` checksum `061f17d5` now gates both typed operation and event evidence. Claim allowed: bounded imported EnvShake controllers can be proven as typed camera-shake operation plus event telemetry. Claim blocked: dynamic params, `mul`, exact pause/stage/layer behavior, helper/redirect ownership, waveform parity, and full MUGEN/IKEMEN presentation parity.
- Latest completed R2 cut: `RuntimeStateEntryWorld` now owns bounded state-entry mutation that was still inline in `PlayableMatchRuntime`: availability lookup, state-number transition metadata, changed-state elapsed reset, owner-backed custom-state assignment/clearing, stale current-move cancellation, `firedHitDefs` clearing, contact reset callback, StateDef `type` / `movetype` / `physics` / `ctrl` / `velset` application, and self/state-owner animation handoff. Focused `RuntimeStateEntrySystem` tests prove normal state entry, owner-backed custom states, owner-derived previous-state metadata, and metadata normalization. Claim allowed: current bounded state-entry mutation has a named runtime-system boundary. Claim blocked: exact CNS `ChangeState` / `SelfState` tick-order parity, persistent-controller timing, helper/team/root redirects, full state-entry parity, and score movement.
- Latest completed R2 cut: `RuntimeStateClockWorld` now owns bounded `Time` / state elapsed clock mutation for per-frame advance and changed-state elapsed reset. `PlayableMatchRuntime` delegates both the active-frame increment and reset-on-transition behavior to the new boundary while preserving the existing `-1 -> 0` first-frame convention. Focused `RuntimeStateClockSystem` tests prove advance, reset, and no-op transition behavior. Claim allowed: current bounded state elapsed clock mutation has a named runtime-system boundary. Claim blocked: exact CNS `Time` tick-order parity, persistent-controller timing parity, pause/hitpause timing changes, helper/team/redirect state clocks, and score movement.
- Previous completed R2 cut: `RuntimeStateMetadataWorld` now owns bounded previous-state transition metadata writes for `prevStateNo`, `prevAnimNo`, `prevStateType`, and `prevMoveType`. `PlayableMatchRuntime` delegates state-number changes through this boundary, and the basic `StateControllerExecutor` `ChangeState` / `SelfState` path uses the same helper for runtime-only executor output. Focused `RuntimeStateMetadataSystem`, `RuntimeCnsSubset`, and `PlayableMatchRuntime` Prev-trigger tests prove changed-state capture and unchanged-state no-op behavior. Claim allowed: current bounded `PrevStateNo`, `PrevAnim`, `PrevStateType`, and `PrevMoveType` metadata writes have a named runtime-system boundary. Claim blocked: exact state-entry tick order, redirects/helper/team previous-state ownership, persistent controller semantics, full `ChangeState` / `SelfState` parity, and score movement.
- Previous completed R2 cut: `TargetSystem` now prunes `TargetBind` binding records during target-memory advance when the bound actor id / target id no longer survives expiry, while active `TargetBind` and `BindToTarget` position application still requires matching live target memory. Focused `TargetSystem` tests prove target-memory expiry pruning, active binding success, and stale-binding no-mutation behavior. Claim allowed: current bounded target-binding lifetime is gated by live target memory. Claim blocked: helper/root/parent target ownership, teams, multi-target selection parity, exact bind/drop tick order, throws/custom-state binding parity, and score movement.
- Previous completed R2 cut: `TargetSystem` now gates active `TargetBind` and `BindToTarget` position application against live target memory, so stale binding records cannot move actors after target memory has been dropped or expired. Focused `TargetSystem` tests prove active binding success and stale-binding no-mutation behavior. Claim allowed: current bounded active target binding application requires matching live target memory. Claim blocked: helper/root/parent target ownership, teams, multi-target selection parity, exact bind/drop tick order, throws/custom-state binding parity, and score movement.
- Previous completed R2 cut: `TargetSystem` now owns bounded `BindToTarget` `postype` anchor resolution previously inline in `PlayableMatchRuntime`. The match runtime now passes only a character-constant resolver, while `TargetSystem.resolveRuntimeTargetAnchor` resolves `Foot` to `0,0`, `Head` through `size.head.pos.x/y`, and `Mid` through `size.mid.pos.x/y` with the legacy scalar x fallback. Focused `TargetSystem` tests prove constant-backed anchor math and existing `BindToTarget` placement. Claim allowed: current bounded `BindToTarget` anchor math has a named target-system boundary. Claim blocked: exact bind tick order, multi-target/team/helper ownership, custom-state throw binding, target lifetime/drop parity, full MUGEN/IKEMEN target parity, and score movement.
- Previous completed R1 Common1 fall-entry cut: required `synthetic-imported-default-fall-gethit.json` checksum `6af73a91` now gates ordered imported P2 actor-frame `5000 -> 5030 -> 5050` evidence plus bounded controller/typed-operation order through `5000` `ChangeState`, `5030` `VelAdd` / `HitVelSet` / `kinematic:hitvelset` / `ChangeState`, and `5050` `VelAdd` / `ChangeState`; optional `kfm-official-default-fall-gethit.json` checksum `a19f43db` now requires bounded official KFM `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110` controller/typed-operation and actor-frame order when the private fixture exists. Claim allowed: bounded airborne fall branch and optional KFM ground-impact/bounce/lie-down entry order are required trace evidence. Claim blocked: exact Common1 controller-loop tick order, velocity math, recovery branching, guard parity, public bundled KFM, and full MUGEN/IKEMEN fall get-hit parity.
- Previous completed R1 Common1 full-chain recovery-order cut: required `synthetic-imported-default-fall-recovery.json` checksum `d83797d9` now gates actor-frame and bounded controller/typed-operation order for imported P2 `5000 -> 5030 -> 5050 -> 5100 -> 5101 -> 5110 -> 5120`, including bounded countdown-range and first-to-last-drop `hitFall.downRecoverTime` evidence during `5110`; optional `kfm-official-default-fall-recovery.json` checksum `26a2aae9` still requires matching `5110 -> 5120` actor-frame order plus bounded official KFM `5110`/`5120` controller/typed-operation order when the private fixture exists. Claim allowed: bounded airborne fall get-hit, ground-impact, bounce, lie-down/get-up order, and down-counting lie-down recovery timer evidence are now required on the synthetic route. Claim blocked: exact Common1 controller-loop tick order, exact animation timing, threshold tables, exact `down.recovertime` / `fall.recovertime` tables, velocity/bounce physics, recovery-input breadth, public bundled KFM support, and full MUGEN/IKEMEN fall recovery parity.
- Latest completed R1 optional fixture cut: `kfm-official-x-hit-sound.json` and `kfm-official-x-hit-spark.json` both pass with checksum `bd153db9` when the private official KFM fixture exists, proving the real KFM `x -> 200` route emits bounded `HitDef` hit sound telemetry `S5,0` and bounded `HitSpark` telemetry from authored `sparkno 0` after contact. This does not prove public bundled KFM support, SND decode/playback, channel priority, exact FightFX/common lookup, layering, scale, palette, render timing, or full presentation parity.
- Latest completed R1 HitDef presentation cut: required `synthetic-imported-hitdef-common-guard-spark.json` checksum `7650a09c` gates unprefixed `guard.sparkno = 7003` into bounded common/default guard-spark AIR metadata, required `synthetic-imported-hitdef-fightfx-guard-spark.json` checksum `32f3e92d` gates `guard.sparkno = F7004` into bounded FightFX guard-spark AIR metadata, required `synthetic-imported-hitdef-guard-effect-package.json` checksum `1c3167b7` gates one guarded direct `HitDef` contact that emits bounded `guardsound = S6,0` `PlaySnd` telemetry plus FightFX `guard.sparkno = F7004` selected-frame/multi-frame AIR metadata with authored `sparkxy = 15,-63` and shared contact package metadata, and required `synthetic-imported-hitdef-hit-effect-package.json` checksum `46aa5ce1` gates one direct hit contact that emits bounded `hitsound = F5,0` `PlaySnd` telemetry with `soundPrefix = kfm` plus FightFX `sparkno = F7002` selected-frame/multi-frame AIR metadata with authored `sparkxy = 18,-68` and shared contact package metadata. These routes require direct/guarded `HitDef` contact, attacker-side `PlaySnd`/`HitSpark` telemetry, shared non-empty `contactId` / `contactTick` / `contactKind`, authored spark offsets, selected first-frame offset `3,-4`, selected first-frame duration `5`, at least 2 asset frames, frame indices `[0, 1]`, total authored duration `11`, and first-frame sprite metadata before renderer/audio handoff. Claim allowed: imported hit/guard-effect routes now have required combined sound plus common/FightFX source-frame, spark offset, selected-frame/multi-frame AIR metadata, and prefixed sound metadata evidence tied to the same contact package. Claim blocked: exact intra-tick sound/spark ordering, channel priority/mixing/timing, common/FightFX lookup, visual timing, binding, layering, scale, palette, motif/screenpack ownership, hit/guard-effect parity, and full MUGEN/IKEMEN presentation parity.
- Latest completed R1 Projectile presentation cut: required `synthetic-imported-projectile-contact.json` checksum `ba22ed74` gates one bounded imported `Projectile` hit contact with owner-state `ProjContact(77)` evidence, target-link evidence, attacker-side `hitsound = S5,0` `PlaySnd` telemetry, and FightFX `sparkno = F7002` selected-frame/multi-frame AIR metadata with authored `sparkxy = 18,-68` sharing the same projectile contact package metadata; required `synthetic-imported-projectile-guard.json` checksum `4bcc5650` gates the held-back guard route with `ProjGuarded(77)`, `guardsound = S6,0`, FightFX `guard.sparkno = F7004`, selected-frame/multi-frame AIR metadata, authored `sparkxy = 15,-63`, and `contactKind = guard`. Claim allowed: bounded Projectile hit/guard contacts can emit shared sound + FightFX spark package telemetry with authored offsets before audio/renderer handoff. Claim blocked: exact projectile effect timing, SND playback/mixing, exact FightFX/common render lookup, binding, layering, scale, palette, helper-owned projectile effects, multi-target presentation, and full Projectile parity.
- Latest completed R1 cut: required `synthetic-imported-target-noko.json` checksum `321a1eba` gates a bounded imported `TargetLifeAdd` lethal damage route where defender-side `AssertSpecial NoKO` executes before P1 `HitDef` and later `TargetLifeAdd`, target-link evidence proves P1 still owns target id `77`, and final P2 life clamps to `1`. This strengthens target-controller no-KO evidence without claiming exact NoKO lifetime, helper/root/parent redirects, teams, multi-target behavior, round flow, or full MUGEN/IKEMEN target parity.
- Previous completed R1 cut: required `synthetic-imported-target.json` checksum `f5a16dc9` gates TargetLifeAdd/TargetPowerAdd/TargetVel*/TargetFacing/TargetBind/BindToTarget/TargetDrop through typed operation evidence, world-visible target-link/binding evidence, actor-frame P2 facing/velocity telemetry, final P1 `targetCount = 0`, and final P2 `life = 943` / `power = 40`. This strengthens bounded two-actor Target* side-effect evidence without claiming redirects, helpers/root/parent targets, teams, multi-target lifetime, exact bind/drop tick-order, or full MUGEN/IKEMEN target parity.
- Latest completed R2 cut: `RuntimeContactPresentationWorld` now owns bounded direct `HitDef` and `Projectile` contact presentation package emission previously inline in `PlayableMatchRuntime`: shared contact package metadata (`contactId`, `contactTick`, `contactKind`), attacker-side `PlaySnd` telemetry, HitSpark telemetry, and hit-spark asset-frame handoff for the current direct/projectile contact routes. Focused `RuntimeContactPresentationSystem` coverage proves direct hit and projectile guard packages share metadata across sound and spark events. Claim allowed: current direct HitDef and Projectile contact presentation package emission has a named system boundary. Claim blocked: exact intra-tick audio/spark ordering, SND playback/mixing/channel priority, exact FightFX/common lookup/binding/layering/timing/scale/palette, helper-owned contact presentation, multi-target presentation, full presentation parity, and score movement.
- Latest completed R2 cut: `RuntimeActiveStateDispatchWorld` now owns bounded active-state `ChangeState` / `SelfState` and `ChangeAnim` / `ChangeAnim2` dispatch mutation previously inline in `PlayableMatchRuntime`: dynamic numeric/boolean param resolution, controller telemetry, state entry handoff, optional ctrl mutation, state-owner animation source selection, and `elem` / `elemtime` handoff. Focused `RuntimeActiveStateDispatchSystem` coverage proves dynamic SelfState with anim/ctrl, ChangeAnim2 owner/elem routing, unresolved state/animation fail-closed behavior, and non-state dispatch pass-through. Claim allowed: current active-state state/animation dispatch has a named, testable world boundary. Claim blocked: exact CNS VM tick order, persistent-controller semantics, helper/team/redirect controller scopes, missing-action fallback parity, full ChangeState/ChangeAnim VM parity, visual parity, score movement, and full MUGEN/IKEMEN runtime parity.
- Previous completed R2 cut: `RuntimeGuardDistanceWorld` now owns bounded `InGuardDist`/auto-guard proximity checks previously inline in `PlayableMatchRuntime`: current move presence, spent-hit rejection, pre-active guard-distance window, guardflag/AssertSpecial/unguardable filtering, caller-supplied hurtboxes, and authored/default `guard.dist` thresholds. Focused `RuntimeGuardDistanceSystem` coverage proves the pre-active window, missing/spent/out-of-window rejects, guardflag and AssertSpecial rejects, unguardable attacks, and authored `guard.dist` thresholds. Claim allowed: current `InGuardDist`/auto-guard distance has a named system boundary. Claim blocked: exact proximity guard parity, guard-end timing, guard effects, air-guard landing, broad Common1 controller-loop parity, full MUGEN/IKEMEN guard VM parity, and score movement.
- Previous completed R2 cut: `RuntimeAnimationWorld` now owns bounded actor animation advancement and timing helpers previously inline in `PlayableMatchRuntime`: `animTime`, `frameIndex`, `frameElapsed`, completion, final-frame hold, `loopStart`, invalid-duration clamping, `AnimTime` / `AnimElemTime` helper math, and current `HitDef` active-window duration math. Focused `RuntimeAnimationSystem` coverage proves empty actions, authored durations, frame changes, loop completion, final-frame hold, invalid-duration clamping, and timing helpers. Claim allowed: current actor animation advancement has a named system boundary. Claim blocked: exact AIR negative-duration semantics, `elem` / `elemtime` parity, state-owner namespace behavior, controller tick-order parity, full MUGEN/IKEMEN animation VM parity, and score movement.
- Previous completed R2 cut: `RuntimeKinematicsWorld` now owns bounded actor position integration previously inline in `PlayableMatchRuntime`: horizontal/vertical position advance, sandbox airborne gravity, ground snap, imported hit-state ground-snap preservation, and landing idle-action requests. Focused `RuntimeKinematicsSystem` coverage proves grounded movement, airborne gravity, landing snap/idle, active-move landing, and imported hit-state ground-snap preservation. Claim allowed: current actor kinematic advance has a named system boundary. Claim blocked: exact MUGEN physics, `yaccel` constants, landing timing, air recovery parity, helper physics ownership, full MUGEN/IKEMEN physics parity, and score movement.
- Previous completed R2 cut: `RuntimeInputControlWorld` now owns bounded local player and simple AI control intent previously inline in `PlayableMatchRuntime`: blocked input gates, State -1 setup/entry precedence, local punch/kick intent, crouch/jump/walk/idle mutation, airborne drift, `NoWalk`, AI chase, and AI attack cooldown. Focused `RuntimeInputControlSystem` coverage proves blocked input, state-entry precedence, movement branches, NoWalk/air drift, and AI chase/attack routes. Claim allowed: current local player/simple AI control intent has a named system boundary. Claim blocked: new input semantics, exact command timing, exact AI behavior parity, full MUGEN/IKEMEN control routing, and score movement.
- Previous completed R2 cut: `RuntimeMoveLifecycleWorld` now owns bounded active-move lifecycle mutation previously inline in `PlayableMatchRuntime`: move tick, non-reversal attack moveType/velocity lock, completed move cleanup, reversal cleanup, and idle/control restoration callbacks. Focused `RuntimeMoveLifecycleSystem` coverage proves no-op, active non-reversal, completed non-reversal, and completed reversal routes. Claim allowed: current active-move lifecycle mutation has a named system boundary. Claim blocked: new move semantics, exact input/cancel timing, exact MUGEN/IKEMEN active-move lifecycle parity, and score movement.
- Latest completed R2 cut: `RuntimeHitPauseWorld.advanceRuntime(...)` now owns the concrete hitpause runtime-system bridge for current command buffering and paused-presentation orchestration. Focused `RuntimeHitPauseSystem` coverage proves runtime command buffers receive current tick/input with `hitPause: true`, and paused presentation routes through `RuntimeEffectLifecycleWorld` with pause kind `hitpause`. Claim allowed: `RuntimeHitPauseWorld` now owns both bounded global hitpause ordering and the concrete runtime bridge for current command buffering plus paused presentation during hitpause. Claim blocked: new hitpause semantics, helper-owned hitpause execution, broad side-effect ordering during hitpause, exact first-frame decrement order, exact MUGEN/IKEMEN hitpause parity, and score movement.
- Previous completed R2 cut: `RuntimePausedMatchWorld.advanceRuntime(...)` now owns the concrete paused-match runtime-system bridge for current source-movetime target-memory aging, active-effect advance, presentation-effect advance, active target binding, stage clamp, and frozen-actor paused presentation. Focused `PauseSystem` coverage proves actor-local `targetWorld`, `effectLifecycleWorld`, and `RuntimeActorConstraintWorld` wiring while preserving existing bounded paused-match order. `pnpm qa:trace` stays stable at the current 163/163 artifacts. Claim allowed: `RuntimePausedMatchWorld` now owns both bounded regular Pause/SuperPause source-movetime ordering and the concrete runtime bridge for current target/effect/constraint side effects during source movetime. Claim blocked: new pause semantics, helper VM execution during pause, exact MUGEN/IKEMEN pause layering, exact paused effect tick order, parent/root/team redirects, and score movement.
- Previous completed R2 cut: `RuntimeMatchInteractionWorld.advanceRuntime(...)` now owns the concrete runtime-system bridge for current post-fighter target-memory aging, active-effect advance, projectile clash, body separation, target binding, stage clamp, and presentation-effect advance in the normal match loop; focused `MatchInteractionSystem` coverage proves the runtime bridge wires actor-local `targetWorld`, `effectLifecycleWorld`, `effectActorWorld.resolveProjectileClashes(...)`, and `RuntimeActorConstraintWorld` while preserving existing post-fighter order. `pnpm qa:trace` stays stable at the current 163/163 artifacts. Claim allowed: `RuntimeMatchInteractionWorld` now owns both bounded ordering and the concrete runtime bridge for current normal-loop interaction side effects. Claim blocked: helper VM execution, new target/projectile/effect semantics, parent/root/team redirects, exact post-fighter tick-order parity, and full MUGEN/IKEMEN interaction parity.
- Latest completed R2 cut: `RuntimeResourceWorld` now owns authored life/power max resolution, runtime power-delta clamping, bounded life deltas, control-flag writes, resource-controller writes, and variable/range assignments behind a named world boundary inside `RuntimeResourceSystem`; the existing helper functions delegate to that world for compatibility. Focused `RuntimeResourceSystem` coverage proves direct world mutation for life, power, ctrl, and vars while existing call sites preserve bounded semantics. Claim allowed: current match-runtime direct/projectile/target/pause/reversal/controller resource mutation paths have a named, testable resource-world boundary. Claim blocked: new controller semantics, exact CNS resource tick-order, helper/team/redirect resource ownership, exact target/projectile parity, round/KO flow, and full MUGEN/IKEMEN resource parity.
- Previous completed R2 cut: `RuntimeEffectActorWorld.countActors(...)` now owns the unified bounded effect-count query consumed by `PlayableMatchRuntime` for `NumExplod`, `NumHelper`, and `NumProj`/`NumProjID` trigger callbacks; focused `EffectActorSystem` coverage proves Explod/Helper/Projectile counts, id filters, and removed-projectile exclusion through one world query. Claim allowed: current owner-local effect-count triggers read through a single effect-actor world boundary. Claim blocked: helper VM execution, exact projectile lifetime, parent/root/team scopes, redirects, helper-owned projectiles, and full MUGEN/IKEMEN effect-trigger parity.
- Latest completed R2 cut: `HelperSystem` now owns bounded helper-local `Parent, ...` / `Root, ...` / `EnemyNear, ...` read-only redirects, caller-provided `EnemyNear(index)` list reads, and static `BindToParent` / `BindToRoot` owner binding for current visual Helper actors when `RuntimeEffectLifecycleWorld` provides owner/root/opponent runtime state and callers supply an explicit `opponentStates` list for indexed reads. Focused `EffectActorSystem`, `RuntimeCnsSubset`, `RuntimeCompiler`, and trace-preset tests prove helper-local branches on owner/root `Var`, `StateNo`, and `Vel X`, a `Root,Var(...)` `ChangeState` value, parent/root operands inside composite arithmetic/boolean/`IfElse(...)` expressions, opponent `StateNo`, `Life`, `Pos X`, and `Var(...)` reads, explicit indexed opponent reads when supplied, owner/opponent clone isolation, static helper-local owner/root binding, and fail-closed behavior when no parent/root/opponent state exists. Required `synthetic-imported-helper-bindtoroot.json` checksum `bf72306c` proves the root-binding route in trace evidence with root `ownerBind` target/offset payload metadata; required `synthetic-imported-helper-bindtoparent.json` checksum `f9922c0e` remains the parent-binding proof with parent `ownerBind` metadata; required `synthetic-imported-helper-enemynear.json` checksum `35498955` remains the opponent redirect proof. Claim allowed: current visual Helper actors can read bounded owner/root/opponent runtime contexts, read caller-supplied indexed opponent contexts, and bind against supplied owner/root runtime state with static params plus inspectable payload metadata. Claim blocked: broader indexed/team/helper-owned redirects, player-state `BindToParent` / `BindToRoot`, dynamic bind params, team/keyctrl ownership, redirect/bind mutation beyond the bounded helper-local subset, nested helper ancestry where root differs from parent, helper-owned opponents/combat/effects/projectiles, exact parent/root/opponent scopes, exact tick order, full helper VM, and score movement.
- Latest completed R2 helper-trigger cut: `ExpressionEvaluator` and `ExpressionCompiler` now support bounded `IsHelper` / `IsHelper(id)` expressions, and `HelperSystem` supplies helper identity into helper-local trigger contexts. Focused evaluator/compiler/helper VM tests prove player-false, helper-true, id match/mismatch, and helper-local state routing, while required `synthetic-imported-helper-ishelper.json` checksum `37877602` proves the route in trace evidence. Claim allowed: current visual Helper actors can inspect their helper identity in the bounded helper-local micro-VM. Claim blocked: indexed/nested helper ancestry, team/keyctrl ownership, helper-owned combat/effects/projectiles, exact helper scopes, exact tick order, full helper VM, and score movement.
- Previous completed R2 cut: `HelperSystem` now owns a bounded helper-local micro-VM for current visual Helper actors spawned with owner runtime-program data. `RuntimeEffectSpawnWorld` hands the owner runtime program and animation map to Helpers; focused `EffectActorSystem` coverage proves helper-local `Time` trigger evaluation, `VelSet`, `ChangeAnim`, `ChangeState`, `DestroySelf` removal, `CtrlSet`, `StateTypeSet`, `LifeAdd`/`LifeSet`/`PowerAdd`/`PowerSet`, `VarSet`/`VarAdd`/`VarRandom`/`VarRangeSet` trigger branches, and helper-local `PlaySnd`/`StopSnd` sound-event telemetry, while `EffectSpawnSystem` coverage proves the runtime-program/animation handoff. `MugenAudioSystem` now scans effect snapshots in addition to player snapshots so bounded helper-local sound events can reach the browser audio adapter. Claim allowed: current visual Helper actors can execute a tiny local state/action/kinematic/control/metadata/resource/variable/sound/destruction subset without mutating `PlayableMatchRuntime` directly. Claim blocked: team ownership, `keyctrl`, helper fvar/sysvar `VarRandom`, exact helper-local sound timing/channel/redirect ownership, helper-owned Projectile combat/contact presentation, exact helper `HitDef` lifetime/multi-hit parity, full helper combat/contact parity, helper-bound Explod timing, exact helper resource semantics, exact random stream parity, exact tick-order/pause parity, custom-state helper lifecycle, and full MUGEN/IKEMEN helper parity.
- Previous completed R2 cut: `HelperSystem` / `RuntimeEffectActorWorld` now own explicit visual-helper removal by helper id or runtime serial, including all-owner helper clear for bounded lifecycle cleanup and p1/p2 store isolation; `RuntimeEffectSpawnWorld` exposes the same handoff for future controller dispatch. Focused `EffectActorSystem` and `EffectSpawnSystem` tests prove the store mutation boundary. Claim allowed: current visual helper actors can be removed through one named effect-actor world boundary. Claim blocked: redirects, parent/root/team ownership, helper-owned target memory, helper-owned custom state tables, helper-owned Projectile combat/contact, exact helper HitDef lifetime/multi-hit parity, custom-state helper lifecycle, and full MUGEN/IKEMEN helper parity.
- Latest completed R2 cut: `RuntimeSnapshotWorld.match()` now owns bounded full match snapshot envelope assembly in addition to stage/camera, player actor, and final effect snapshot projection consumed by `PlayableMatchRuntime`; focused `RuntimeSnapshotSystem` tests prove envelope fields, actor/effect clone isolation, compatibility-session handoff, and 80-line log cap behavior while preserving trace/render consumers.
- Previous completed R2 cut: `RuntimeSnapshotWorld` owns bounded final effect snapshot aggregation in addition to stage/camera and player actor snapshot projection consumed by `PlayableMatchRuntime`; focused `RuntimeSnapshotSystem` tests prove stable p1/p2 explod/helper/projectile ordering plus clone isolation for effect snapshots while preserving trace/render consumers.
- Previous completed R2 cut: `RuntimeSnapshotWorld` owns bounded player actor snapshot projection in addition to stage/camera projection consumed by `PlayableMatchRuntime`; focused `RuntimeSnapshotSystem` tests prove actor identity/source/sprite-owner metadata, cloned runtime state, target refs/bindings, active move or AIR-frame collision boxes, missing-frame fallback hurtbox, and cloned sound/hit-effect/env-shake event histories while `pnpm qa:trace` stays stable.
- Latest completed R2 cut: `RuntimeCompatibilityTelemetryWorld` now owns imported compatibility telemetry and session projection consumed by `PlayableMatchRuntime`; focused `RuntimeCompatibilityTelemetrySystem` tests prove imported/owner-backed filtering, routed state-entry projection, controller/operation count keys, bounded controller-event history, active command dedupe, and command-history handoff while `pnpm qa:trace` stays stable.
- Latest completed R2 cut: `RuntimeSnapshotWorld` now owns bounded stage/camera snapshot projection consumed by `PlayableMatchRuntime`; focused `RuntimeSnapshotSystem` tests prove `ScreenBound moveCameraX = 0` camera exclusion, all-disabled fallback, and EnvShake/EnvColor stage snapshot handoff while `pnpm qa:trace` stays stable at 156/156 artifacts.
- Latest completed R2 cut: `RuntimeAssertSpecialWorld` now owns bounded imported pre-facing `AssertSpecial` ordering consumed by `PlayableMatchRuntime`; focused `RuntimeAssertSpecialSystem` tests prove imported current-state application, owner-backed custom-state application, trigger filtering, and non-imported skip behavior while broader trace behavior is expected to stay stable.
- Latest completed R2 cut: `RuntimeHitPauseWorld` now owns bounded global hitpause ordering consumed by `PlayableMatchRuntime`; focused `RuntimeHitPauseSystem` tests prove command buffering, `ignorehitpause` controller dispatch, paused presentation, actor countdown, and no-op behavior outside hitpause while broader trace behavior is expected to stay stable.
- Latest completed R2 cut: `RuntimePausedMatchWorld` now owns bounded regular `Pause` / `SuperPause` paused-match ordering consumed by `PlayableMatchRuntime`; focused `PauseSystem` tests prove source `movetime` ordering, frozen actor presentation, pause replacement interruption, and pause countdown ticking while broader trace behavior is expected to stay stable.
- Latest completed R2 cut: `RuntimeStunWorld` now owns bounded hitstun/guardstun timer advance, hitstun presentation-action requests, imported hit-state moveType preservation, current-move guardrails, and non-imported idle moveType restoration consumed by `PlayableMatchRuntime`; focused `RuntimeStunSystem` tests prove the boundary while trace behavior stays stable.
- Latest completed R2 cut: `RuntimeStateAvailabilityWorld` now owns bounded state/action availability lookup consumed by `PlayableMatchRuntime`; focused `StateAvailabilitySystem` tests prove compiled runtime-program state precedence, parsed state lookup, animation-only fallback, owner-backed custom-state lookup, and missing-state rejection while `pnpm qa:trace` stays stable at 156/156 artifacts.
- Latest completed R2 cut: `RuntimeHitStateTransitionWorld` now owns bounded direct-hit and ReversalDef `p1stateno` / `p2stateno` / `p2getp1state` transition routing consumed by `PlayableMatchRuntime`; focused `HitStateTransitionSystem` tests prove attacker-owned `p1stateno`, attacker-owned `p2stateno`, target-owned `p2getp1state = 0`, and unavailable-state no-op behavior while `pnpm qa:trace` stays stable at 156/156 artifacts.
- Latest completed R2 cut: `RuntimeGetHitStateWorld` now owns bounded default get-hit state selection for imported direct and projectile hit routes; focused `GetHitStateSystem` tests prove stand `5000`, crouch `5010 -> 5000`, air `5020 -> 5000`, and missing-state no-op selection while `pnpm qa:trace` stays stable at 156/156 artifacts.
- Latest completed R2 cut: `RuntimeGuardWorld` now owns bounded guard-hit state selection and auto guard-start eligibility/mutation consumed by direct combat, projectile combat, and `PlayableMatchRuntime`; focused `GuardSystem` tests prove Common1-style guard-hit/start fallback, held-back/current-move/pause/stun/guard-state rejection, and control/velocity start mutation while trace behavior stays stable.
- Latest completed R2 cut: `RuntimeOrientationWorld` now owns bounded auto-facing and `Turn` facing flips consumed by `PlayableMatchRuntime` and `StateControllerExecutor`; focused `RuntimeOrientationSystem` tests prove opponent-facing, `AssertSpecial NoAutoTurn`, and `Turn` behavior.
- Latest completed R2 cut: `RuntimeHitEligibilityWorld` now owns bounded `HitBy`/`NotHitBy` slot ticking and per-frame `AssertSpecial`/render-opacity reset consumed by `PlayableMatchRuntime`; focused `RuntimeHitEligibilitySystem` tests prove finite slot decrement/removal, infinite slot preservation, empty-container cleanup, and flag reset behavior.
- Latest completed R2 cut: `RuntimeTargetWorld.applyTargetBindings` / `applyBindToTarget` now own active bounded `TargetBind` and `BindToTarget` per-frame position application consumed by `PlayableMatchRuntime`; focused `TargetSystem` tests prove owner-to-target binding, target-to-owner binding, and missing-target no-op behavior.
- Latest completed R2 cut: `RuntimeTargetWorld.applyBindToTargetController` now owns bounded `BindToTarget` target lookup, raw/typed postype binding, duration clamping, facing-aware position application, and operation reporting consumed by `PlayableMatchRuntime`; focused `TargetSystem` tests prove raw Head anchors, typed op application, and miss/no-mutation behavior.
- Latest completed R2 cut: `RuntimeRecoverySystem` now owns bounded `fall.recovertime` countdown, Common1 liedown `data.liedown.time` default/decrement, and imported `5201 -> 52` ground-recovery landing hooks consumed by `PlayableMatchRuntime`; focused `RuntimeRecoverySystem` tests prove the boundary while state validation/entry remains in the match runtime.
- Latest completed R2 cut: `RuntimeRandomSystem` now owns deterministic sandbox-side random seed creation, LCG advance, controller-safe unit clamping, and fallback `VarRandom` unit derivation consumed by `PlayableMatchRuntime` and `StateControllerExecutor`; focused `RuntimeRandomSystem` tests prove the boundary while required `synthetic-imported-variable.json` remains checksum `3b33f7a8`.
- Latest completed R2 presentation cut: `HitSparkAssetSystem` now owns bounded player/common/FightFX HitDef spark asset-frame resolution consumed by `PlayableMatchRuntime` before `RuntimeHitEffectWorld` event insertion; focused tests prove prefix routing, state-owner player lookup, package-library lookup, and missing refs without changing renderer behavior.
- Latest completed R2 cut: `RuntimeContactMemoryWorld` now owns bounded direct/projectile contact-memory creation, reset, mutation, and readback consumed by `PlayableMatchRuntime`, `RuntimeDirectCombatWorld`, and `RuntimeReversalWorld`; focused `ContactMemorySystem`, `DirectCombatSystem`, and `ReversalSystem` tests prove the boundary while existing trace behavior remains intended to stay stable.
- Latest completed R2 cut: `RuntimeMatchInteractionWorld` now owns the bounded post-fighter interaction order for target-memory aging, active-effect advance, projectile clash, body separation, target binding, direct priority/direct combat, projectile combat, stage clamp, and presentation-effect advance, preserving current `PlayableMatchRuntime` behavior through focused `MatchInteractionSystem` order coverage.
- Previous completed R2 cut: `RuntimeEffectLifecycleWorld` now owns bounded active-effect tick, presentation tick, paused presentation tick, effect snapshot grouping, and shared get-hit cleanup orchestration consumed by `PlayableMatchRuntime` plus direct combat/HitOverride/Reversal get-hit routes, preserving existing effect actor telemetry through focused `EffectLifecycleSystem` tests.
- Previous completed R2 cut: `RuntimeEffectSpawnWorld` now owns bounded Explod/Helper/Projectile spawn resolution, RemoveExplod dispatch, and ModifyProjectile dispatch consumed by `PlayableMatchRuntime`, preserving existing effect actor telemetry through focused `EffectSpawnSystem` tests.
- Previous completed R2 cut: `RuntimeProjectileCombatWorld` now owns bounded projectile contact/reject/HitOverride/hit-or-guard/cleanup mutation and projectile clash win/trade/cancel mutation consumed by `RuntimeEffectActorWorld`, preserving existing projectile telemetry through focused `ProjectileCombatSystem` tests.
- Previous completed R2 cut: `RuntimeDirectCombatWorld` now also owns bounded same-tick direct `HitDef` priority clash mutation for win/trade outcomes consumed by `PlayableMatchRuntime`, preserving existing direct priority telemetry through focused `DirectCombatSystem` tests.
- Previous completed R2 cut: `RuntimeReversalWorld` now owns bounded ReversalDef activation, active counter detection, and counter-result mutation for direct HitDef contact consumed by `PlayableMatchRuntime`, preserving existing ReversalDef telemetry through focused `ReversalSystem` tests.
- Previous completed R2 cut: `RuntimeHitOverrideWorld` now owns bounded HitOverride slot ticking and redirect mutation for direct and projectile combat paths consumed by `PlayableMatchRuntime`, preserving existing HitOverride telemetry through focused `HitOverrideSystem` tests.
- Previous completed R2 cut: `RuntimeDirectCombatWorld` now owns bounded direct hit/guard result mutation for life, pause, stun, velocity, hit vars, hit fall metadata, power gain, contact memory, received-damage memory, and get-hit cleanup consumed by `PlayableMatchRuntime`, preserving existing direct HitDef telemetry through focused `DirectCombatSystem` tests.
- Previous completed R2 cut: `RuntimeActorConstraintWorld` now owns bounded `Width`, per-frame `PlayerPush`/`PosFreeze`/`ScreenBound` constraint reset/projection, stage clamping, and body-push separation consumed by `PlayableMatchRuntime`, preserving existing actor bounds/body telemetry through focused `ActorConstraintSystem` tests.
- Previous completed R2 cut: `RuntimeSpriteEffectWorld` now owns current match-runtime `SprPriority`, `PalFX`, `AfterImage`, `AfterImageTime`, `Trans`, and `Angle*` mutation/ticking consumed by `PlayableMatchRuntime`, preserving existing actor presentation telemetry through focused `SpriteEffectSystem` tests.
- Previous completed R2 cut: `RuntimeEnvColorWorld` now owns bounded `EnvColor` event history, stage-flash projection, and reset consumed by `PlayableMatchRuntime`, preserving existing stage-frame color/opacity trace behavior through focused `EnvColorSystem` tests.
- Latest completed R1 cut: optional `kfm-official-default-fall-recovery-threshold.json` checksum `bf7b058a` confirms the private official KFM fixture reaches state `5050` while `hitFall.recoverTime` is still positive, proves a first-to-last `recoverTime` drop inside the summarized `5050` bucket, then routes through ground recovery `5200 -> 5201 -> 52 -> 0` after `command = "recovery"` is accepted near the ground; the gate now also requires ordered actor-frame evidence where that dropping `5050` observation precedes `5200` with `recoverTime = 0`. This relies on local fixture presence and does not claim public broad compatibility.
- Previous completed R2 cut: `RuntimeAudioWorld` now owns bounded `PlaySnd`/`StopSnd` event insertion consumed by `PlayableMatchRuntime`, preserving the same actor sound-event history for Web Audio/debug/trace consumers through focused `AudioEventSystem` tests.
- Previous completed R2 cut: `RuntimeEnvShakeWorld` now owns bounded EnvShake/FallEnvShake event insertion plus deterministic multi-actor camera-shake projection consumed by `PlayableMatchRuntime`, preserving the same actor event history for renderer/debug/trace consumers through focused `EnvShakeSystem` tests.
- Previous completed R2 cut: `RuntimePauseWorld` now owns current match pause state, snapshot projection, source-movetime checks, countdown ticks, controller application, and reset while preserving existing `Pause`/`SuperPause` trace behavior through focused `PauseSystem` / `PlayableMatchRuntime` tests.
- Previous completed R2 cut: `RuntimeTargetWorld.snapshotRuntimeState` now owns cloned target-memory snapshots consumed by `MatchWorld` actor records, with focused `TargetSystem` / `MatchWorld` / `DebugPanel` tests proving target refs, TargetBind bindings, and `BindToTarget` snapshots still render through the registry.
- Previous completed R2 cut: required `synthetic-imported-round-timeover.json` checksum `7d9f7907` uses a short `roundTimerFrames` fixture to gate bounded `RoundSnapshot` `timeover` draw/winner/message/timer evidence through `RuntimeTraceGate.requiredRoundFrames`.
- Previous completed R2 cut: required `synthetic-imported-round-ko.json` checksum `bfd5f073` uses `RuntimeTraceGate.requiredRoundFrames` to gate bounded `RoundSnapshot` KO state, winner, message, timer evidence, and final P2 life `0`.
- Previous completed R2 cut: `RuntimeRoundSystem` owns bounded round timer, KO/time-over finish decision, winner/message snapshot projection, and reset behavior with focused unit coverage. This is ownership cleanup only; no MUGEN/IKEMEN round parity claim.
- Latest completed R1 cut: required `synthetic-imported-default-fall-recovery-tick-order.json` checksum `e2691aab` now gates ordered actor-frame evidence for `5050` with positive `hitFall.recoverTime`, first-to-last drop evidence, and ordering before `5210` with `recoverTime = 0`, plus named controller-event and typed-operation evidence for the bounded `5050` `VelAdd` `Gravity` -> `ChangeState` `Recovery Input` -> `5210` `VelSet` `Air Recovery Velocity` -> `kinematic:velset` -> `HitFallSet` `Fall Recovery Settled` -> `hitfall:hitfallset` -> `ChangeState` `Stand` route.
- Previous completed cut: required `synthetic-imported-default-fall-air-recovery-velocity.json` checksum `560f6308` gates bounded air-recovery velocity telemetry in `5210` after `CanRecover` plus `command = "recovery"`.
- Latest completed R1 cut: required `synthetic-imported-default-fall-ground-recovery.json` checksum `7945fd93` now gates bounded near-ground recovery selection plus ordered controller/typed-operation evidence: imported defender `5050` routes through `5200 -> 5201 -> 52 -> 0` with `SelfState`, `VelSet`, `PosSet`, actor-frame velocity evidence for synthetic `velocity.air.gethit.groundrecover.*` constants, `NotHitBy` / `eligibility:nothitby` safety telemetry, land `kinematic:*` operations, and final `resource:ctrlset` control restore. This is still bounded synthetic evidence, not exact threshold, velocity, or full VM tick-order parity.
- Latest optional KFM ground-recovery precision cut: `kfm-official-default-fall-ground-recovery-priority.json` checksum `6d361534` mirrors the real KFM ground-recovery route while forbidding generic air recovery `5210` and lie-down states. `kfm-official-default-fall-ground-recovery.json` checksum `6d361534` still requires bounded real KFM actor-frame order for observable `5050 -> 5200 -> 52` buckets plus controller/typed-operation order through `5050` `VelAdd`/`ChangeState`, `5200` `VelAdd`/`SelfState`, and `52` landing `VelSet`/`PosSet`/`kinematic:*`/`ChangeState` evidence when the private fixture exists. `5201` remains required as an executed state, but the gate does not claim a summarized `5201` actor-frame bucket. This confirms part of the synthetic ground-recovery order against KFM while keeping exact thresholds, velocity math, full VM tick-order parity, public bundled KFM support, score movement, and guard-state parity blocked.
- Previous completed cut: required `synthetic-imported-default-fall-recovery-threshold.json` checksum `7bb15a5f` gates actor-frame recovery countdown handoff: imported defender `5050` is observed with positive `hitFall.recoverTime`, first-to-last drop evidence, and then `5210` with `recoverTime = 0` after `CanRecover` plus `command = "recovery"` routes.
- Latest completed cut: optional `kfm-official-default-fall-recovery-too-early.json` checksum `d2edbde4` and required `synthetic-imported-default-fall-recovery-too-early.json` checksum `050e7e3c` now gate bounded early recovery-input rejection with actor-frame proof that state `5050` still has positive `hitFall.recoverTime` for the full summarized window (`min >= 1`) while the countdown drops. The private fixture route still forbids states `5210`, `5200`, and `5201`; the required synthetic route still forbids `5210`; both require the defender to remain in `5050`.
- Latest completed guard cut: required `synthetic-imported-default-guard-state.json` checksum `016938a1`, `synthetic-imported-crouch-guard-state.json` checksum `6c4321af`, `synthetic-imported-diagonal-crouch-guard-state.json` checksum `1dd33fb5`, and `synthetic-imported-air-guard-state.json` checksum `ce9cc9ba` now gate ordered named controller/typed-operation evidence plus actor-frame state/physics/body/push telemetry for bounded stand, crouch, atomic-`DB`, and air guard-hit routes: stand `150 -> 151`, crouch `152 -> 153`, and air `154 -> 155`. Stand and air routes also include bounded velocity evidence where the synthetic trace exposes it. Current trace aggregate after this strengthening remains 156/156 artifacts passed, 138 required and 18 optional. Claim allowed: synthetic Common1-style guard-hit routes now have both controller-order and frame-level physics/body evidence. Claim blocked: exact proximity guard, guard effects, air landing parity, exact guard-end timing, controller-loop parity, and full MUGEN/IKEMEN guard VM parity.
- Latest auto guard controller-order cut: required `synthetic-imported-auto-guard-start.json` checksum `0c734290` now gates P2 state `120` `ChangeState` `Guard Start Done` plus ordered actor-frame evidence that defender state `120` appears before `130`; required `synthetic-imported-auto-guard-end.json` checksum `d1dc0aa3` now gates P2 `120` `Guard Start Done` before state `130` `ChangeState` `Stop Guarding`, actor-frame `120 -> 130 -> 140`, plus final `0`/control evidence. Claim allowed: the bounded synthetic auto guard-start/end route has minimum controller/state-order evidence around `InGuardDist`. Claim blocked: exact proximity guard, exact guard-end timing, full Common1 controller-loop parity, guard effects, air guard, and full MUGEN/IKEMEN guard VM parity.
- Latest hit-eligibility cut: required `synthetic-imported-hitby-reject.json` checksum `65185fd1` now gates defender-side `HitBy value = S,NT` rejecting mismatched `HitDef attr = S,NA` through typed `eligibility:hitby`, reject event/combat-reason telemetry, no accepted hit in focused preset coverage, and final P2 life `1000`; required `synthetic-imported-hitby-allow.json` checksum `c75d5c7d` still gates matching `HitBy value = S,NA` acceptance, and `synthetic-imported-reject.json` checksum `5aca7dc0` still gates `NotHitBy` rejection through typed `eligibility:nothitby`. Claim allowed: the bounded static HitBy allow-list route now has both accept and mismatch-reject evidence. Claim blocked: exact attr grammar, slot priority, helper/custom-state ownership, edge timing, and full MUGEN/IKEMEN hit-eligibility parity.
- Latest HitDef effect cuts: required `synthetic-imported-hitdef-hit-sound.json` checksum `6fc00d8a` gates bounded `HitDef hitsound = S5,0` into attacker-side `PlaySnd` telemetry, required `synthetic-imported-hitdef-guard-sound.json` checksum `fdf1f7f6` gates bounded `HitDef guardsound = S6,0` into attacker-side `PlaySnd` telemetry, required `synthetic-imported-hitdef-hit-spark.json` checksum `b6554124` gates bounded `sparkno = S7001` plus `sparkxy = 10,-72` into attacker-side hit `HitSpark` telemetry, required `synthetic-imported-hitdef-guard-spark.json` checksum `72c8fa3a` gates bounded `guard.sparkno = S7000` plus `sparkxy = 12,-64` into attacker-side guard `HitSpark` telemetry, required `synthetic-imported-hitdef-data-spark.json` checksum `d950796c` gates omitted `sparkno` inheriting parsed CNS `[Data] sparkno = 2` plus `sparkxy = -8,-54`, and required `synthetic-imported-hitdef-data-guard-spark.json` checksum `7bd9d162` gates omitted `guard.sparkno` inheriting parsed CNS `[Data] guard.sparkno = 40` plus `sparkxy = 13,-57` after imported direct-hit routes. Optional private-fixture `kfm-official-x-hit-sound.json` and `kfm-official-x-hit-spark.json` both pass with checksum `bd153db9`, confirming the real KFM `x` route emits bounded `S5,0` hit sound and `sparkno 0` hit spark telemetry after contact. `HitSparkRenderer` now consumes snapshot `HitSpark` events, resolves `S` refs as player AIR action ids, classifies unprefixed refs as common/default and `F` refs as FightFX, assigns bounded hit/guard spark layers, resolves player AIR frames with owner sprite context, preserves runtime-provided package-backed common/FightFX frames when `hitSparkLibraries` supplies a matching action, synthesizes bounded common/FightFX system lookup frames through the global sprite namespace when no package frame exists, and draws a 180-frame additive fallback at facing-adjusted `sparkxy` when lookup fails; native demo moves include player AIR spark actions 7000-7002 and `pnpm qa:smoke` warms the renderer, resets the round, drives a native contact route, and gates active desktop/mobile spark plus player-source resolved-sprite diagnostics. `MugenCharacterLoader` now discovers optional `data/fight.def`, resolves `fightfx.air` / `fightfx.sff` / `fightfx.snd`, parses those AIR actions as both `common` and `fightfx` hit-spark libraries, decodes the system SFF and SND when possible, `createImportedFighterDefinition` carries those package-backed libraries into the runtime fighter definition, `App` registers decoded system SFF sprites through a global hit-spark provider route, focused renderer coverage proves package-backed `common`/`fightfx` frames resolve through that global provider route without player-owner context, package-backed frame lists advance by AIR frame duration, resolved spark sprite meshes bind around the `sparkxy` anchor using AIR frame offsets plus SFF axes with facing applied, and QA diagnostics expose selected frame/sprite-axis/local-position metadata for inspection.
- Latest required common/FightFX multi-frame trace cut: required `synthetic-imported-hitdef-common-spark.json` checksum `5ea054d7` gates unprefixed `sparkno = 7001` into bounded common/default hit-spark asset metadata, required `synthetic-imported-hitdef-fightfx-spark.json` checksum `11537b56` gates `sparkno = F7002` into bounded FightFX hit-spark asset metadata, required `synthetic-imported-hitdef-common-guard-spark.json` checksum `7650a09c` gates unprefixed `guard.sparkno = 7003` into bounded common/default guard-spark asset metadata, required `synthetic-imported-hitdef-fightfx-guard-spark.json` checksum `32f3e92d` gates `guard.sparkno = F7004` into bounded FightFX guard-spark asset metadata, required `synthetic-imported-hitdef-guard-effect-package.json` checksum `1c3167b7` gates combined `guardsound = S6,0` telemetry plus FightFX guard-spark metadata with shared contact package metadata on the same guarded contact, and required `synthetic-imported-hitdef-hit-effect-package.json` checksum `46aa5ce1` gates combined `hitsound = F5,0` telemetry plus `soundPrefix = kfm` and FightFX hit-spark metadata with shared contact package metadata on the same direct hit contact. The spark routes require source/action/first-frame/sprite metadata, authored `sparkxy` offsets, selected first-frame offset `3,-4`, selected first-frame duration `5`, at least two asset frames, frame indices `[0, 1]`, and total authored duration `11` before renderer/audio handoff. Claim allowed: trace gates can now require source-frame plus selected-frame/multi-frame AIR metadata, authored offsets, and prefixed hit-sound metadata, including combined hit/guard sound + FightFX spark package routes tied to a shared contact id/tick/kind, before handoff. Claim blocked: exact intra-tick sound/spark ordering, channel priority/mixing/timing, common/FightFX lookup, visual frame timing, render timing, binding, layering, scale, palette, motif/screenpack ownership, and full HitSpark/audio parity.
- Latest Common1 stand/crouch get-hit progression physics-frame cut: required `synthetic-imported-default-gethit-progression.json` checksum `ef2a67f8` gates ordered P2 controller-event evidence for defender-owned state `5000` `ChangeState` before state `5001` `ChangeState`, ordered actor-frame evidence for `5000 -> 5001`, required frame-level `stateType`/`moveType`/`physics`/`Clsn`/velocity telemetry for the bounded stand shake and slide states, and final idle/control evidence. Optional private `kfm-official-default-gethit-progression.json` uses a KFM-specific stand physics-frame profile for `Clsn2 = 2`, body width, state `5001` animation split, and bounded slide velocity when the fixture exists; optional private `kfm-official-default-crouch-gethit-progression.json` checksum `3d197fae` now adds KFM-specific crouch `5010 -> 5011 -> 0` frame evidence for `5010` anim `5010`, `5011` anim `5020`, `Clsn2 = 2/3`, body width, bounded crouch slide velocity, and ordered `HitVelSet` / `VelMul` / `VelSet` / `DefenceMulSet` controller/typed-operation evidence. Claim allowed: bounded `HitShakeOver` / `HitOver` stand and crouch get-hit progression has controller, frame-order, physics-frame, and private KFM crouch slide operation trace evidence. Claim blocked: exact controller tick timing, exact `HitShakeOver`/`HitOver` timing, fall/bounce/liedown/recovery parity, helper/custom-state breadth, public bundled KFM support, and full Common1 VM parity.
- Previous custom-state ownership cut: required `synthetic-imported-target-owned-custom-state.json` checksum `410fb8c0` gates bounded `HitDef p2stateno = 888` with `p2getp1state = 0`, P2-owned state/action `888` actor-frame evidence without attacker `customOwnerId`, P2 `SelfState`, and final P2 state `0`/control. That checkpoint passed after the helper-local Projectile gate at 178/178 artifacts, 158 required and 20 optional. Claim allowed: direct HitDef can route to target-owned state data when `p2getp1state = 0`. Claim blocked: throws, helpers/root/parent redirects, teams, exact bind/tick order, and full MUGEN/IKEMEN custom-state parity.
- Closed R2 match active ownership cut: `RuntimeMatchActiveWorld` owns bounded normal active-match orchestration outside `PlayableMatchRuntime` after hitpause/pause gates: round timer tick, normal command-buffer writes, input control, fighter advance, post-fighter interaction/combat, and round-finish handling route through one named boundary. Focused coverage proves order and sub-world result preservation. Claim allowed: current normal active-match orchestration order has a named/testable boundary without changing runtime semantics. Claim blocked: exact MUGEN/IKEMEN active tick order, pause-start arbitration, helper/team/redirect actor ownership, combat priority parity, visual/audio parity, score movement, and full match VM parity.
- Closed R2 match hitpause ownership cut: `RuntimeMatchHitPauseWorld` owns bounded normal active-match hitpause handoff outside `PlayableMatchRuntime`: `RuntimeHitPauseWorld.advanceRuntime` delegation, P1/P2 inputs, stage/runtime ticks, paused presentation lifecycle, and ignorehitpause controller execution route through one named boundary before the normal pause branch. Focused coverage proves delegate payload forwarding and paused/not-paused result preservation. Claim allowed: current normal active-match hitpause handoff has a named/testable boundary without changing runtime semantics. Claim blocked: exact MUGEN/IKEMEN hitpause tick order, pause arbitration/layering, helper/team/redirect hitpause ownership, visual/audio parity, score movement, and full match VM parity.
- Previous R2 ownership cut: `RuntimeFighterAdvanceWorld` owns bounded per-fighter advance order from `PlayableMatchRuntime`: sprite-effect tick, hit eligibility slots, HitOverride slots, contact timers, render-angle reset, state clock, frame constraints, recovery-window tick, preserve-moveType read, stun, move lifecycle, kinematics, animation, active controllers, ground-recovery landing, lie-down recovery, and frozen-position preservation. Focused coverage proves the order and tick-start position handoff. Claim allowed: current per-fighter match advance plumbing is named/testable without changing runtime semantics. Claim blocked: exact MUGEN/IKEMEN player tick order, persistent-controller timing, helper/team/redirect actor advance semantics, recovery/stun/physics arbitration, visual parity, score movement, and full VM parity.
- Previous R2 ownership cut: `RuntimeActiveControllerDispatchWorld` owns bounded active-controller route orchestration after scan/trigger pass: active state/animation mutation dispatch, shared runtime-controller execution handoff, active side-effect dispatch, and unsupported fail-soft dispatch are selected through one named boundary. Claim allowed: current active-controller dispatch plumbing is named/testable and preserves the existing route order. Claim blocked: exact CNS VM timing, persistent-controller semantics, helper/team/redirect scopes, side-effect ordering parity, missing-action fallback parity, target/combat/presentation semantic parity, visual parity, score movement, and full active-controller parity.
- Previous controller cut: required `synthetic-imported-modifyexplod.json` checksum `bca75991` gates bounded player-state `ModifyExplod` execution through typed `modifyexplod` operation evidence, live owner-side Explod mutation, effect-payload scale/priority/pause/remove telemetry, and actor-frame velocity/scale evidence. Required `synthetic-imported-helper-modifyexplod.json` checksum `0749041c` adds the bounded helper-local static route for helper-parented owner-side Explods. This promotes visual-effect controller coverage without claiming dynamic params, position rebinding, helper-owned effect namespaces, FightFX/common routing, remove-trigger parity, exact tick order, or full Explod lifecycle parity.
- Latest trigger cut: required `synthetic-imported-hitpausetime-ignorehitpause.json` checksum `a3a78bb8` now promotes the bounded `HitPauseTime` + active-state `ignorehitpause = 1` route from focused tests into `pnpm qa:trace`. The artifact proves imported P1 can execute `HitDef`, enter hitpause, evaluate `HitPauseTime > 0` from an active-state `ChangeState` marked `ignorehitpause = 1`, route to state `220`, and record P1 `HitPause:player` advance while P2 remains frozen. Claim allowed: the current hitpause branch can run a bounded imported active-state controller-only route that reads the actor hitpause counter. Claim blocked: persistent controller semantics, full controller execution during pause, helper-owned hitpause, broad side-effect ordering, and exact MUGEN/IKEMEN hitpause tick-order parity.
- Latest no-op controller cut: required `synthetic-imported-noop.json` checksum `2877b222` gates imported `Null`, browser no-op `ForceFeedback`, debug clipboard no-ops `DisplayToClipboard` / `AppendToClipboard` / `ClearClipboard`, deprecated dust presentation no-op `MakeDust`, and helper-lifecycle player-state no-op `DestroySelf` typed `noop:*` operation evidence before a simple `HitDef` route. Claim allowed: those controller families are accepted, counted, and proven as no-op operations in compatibility telemetry. Claim blocked: device force feedback, debug text rendering, clipboard output, dust rendering, helper removal/lifecycle side effects from player states, helper/redirect ownership, persistent/no-op timing edge cases, exact CNS VM order, and full MUGEN/IKEMEN parity.
- Previous variable controller cut: required `synthetic-imported-variable.json` checksum `3b33f7a8` gates imported `VarRandom` execution as typed `variable:varrandom` next to `VarSet`, `VarAdd`, and `VarRangeSet`, with final branch evidence through owner-local `var(...)` triggers. That checkpoint passed 156/156 artifacts, 138 required and 18 optional, with 77 controller families; the current aggregate after the player Projectile target redirect gate is 204/204 artifacts, 184 required and 20 optional. Claim allowed: bounded owner-local int `VarRandom` assignment executes in imported CNS. Claim blocked: exact MUGEN random stream parity, helper/parent/root variable scopes, fvar/sysvar `VarRandom`, persistent/tick-order edge cases, IKEMEN map vars, and full CNS VM parity.
- Latest required/optional guard cut pair: required `synthetic-imported-default-guard-hold-walk-return.json` checksum `75d4db9c` proves portable `150 -> 151 -> 130 -> 20` stand guard-hold walk-control, while optional `kfm-official-default-guard-hold-walk-return.json` checksum `885bb1da` mirrors the real KFM/Common1 stand route on top of `kfm-official-default-guard-slide-stop.json` checksum `885bb1da`. Required `synthetic-imported-crouch-guard-hold-crouch-return.json` checksum `83ecb699` proves portable synthetic `152 -> 153 -> 130 -> 10`, while optional `kfm-official-default-crouch-guard-hold-crouch-return.json` checksum `d11153d0` mirrors real KFM/Common1 `152 -> 153 -> 131 -> 11`. `kfm-official-default-guard-hold-return.json` checksum `885bb1da` remains the stand hold-only subset. `kfm-official-default-crouch-guard-slide-stop.json` checksum `d11153d0` keeps stricter crouch fixture slide-stop evidence.
- Previous completed cut: `synthetic-imported-assertspecial-guarddeny.json` checksum `fd2eb239`, `synthetic-imported-assertspecial-crouch-guarddeny.json` checksum `757cb87a`, `synthetic-imported-assertspecial-air-guarddeny.json` checksum `810a1679`, and `synthetic-imported-assertspecial-lifetime.json` checksum `7d359a76` gate bounded defender-side `NoStandGuard` / `NoCrouchGuard` / `NoAirGuard` hit-over-guard evidence plus one-frame `NoStandGuard` expiry into later guard.
- Latest common-get-hit trace-strengthening cut: required `synthetic-imported-common-gethit.json` checksum `713e49b7` now gates bounded P2 FallEnvShake runtime event telemetry in state `5100` plus ordered controller/typed-operation evidence through `HitFallVel -> HitFallDamage -> HitFallSet -> FallEnvShake`. Claim allowed: the current attacker-owned get-hit route can execute this bounded custom get-hit controller order and emit inspectable `RuntimeEnvShakeWorld` fall-shake telemetry from stored `HitDef fall.envshake.*` metadata. Claim blocked: exact Common1 animation/threshold/velocity math, exact camera waveform, pause/stage/layer interaction, helper/redirect ownership, and full MUGEN/IKEMEN fall presentation parity.
- Latest TargetLifeAdd NoKO cut: required `synthetic-imported-target-noko.json` checksum `321a1eba` gates ordered controller-event evidence where P2 state `0` `Passive AssertSpecial` executes before P1 state `200` `HitDef` and lethal `TargetLifeAdd`, plus target-link evidence and final P2 life `1`. Claim allowed: the current two-player imported target-controller route can apply defender-side NoKO to a lethal `TargetLifeAdd` damage clamp. Claim blocked: exact NoKO lifetime, shadow/round/KO semantics, helper/team/global ownership, pause layering, multi-target redirects, and full MUGEN/IKEMEN no-KO/target parity.
- Previous AssertSpecial NoKO order cut: required `synthetic-imported-assertspecial-noko.json` checksum `f2f60521` gates ordered controller-event evidence where P2 state `0` `Passive AssertSpecial` executes before P1 state `200` lethal `HitDef`, plus final P2 life `1`. Claim allowed: the current two-player imported route can apply defender-side NoKO before lethal direct-hit damage resolution. Claim blocked: exact NoKO lifetime, shadow/round/KO semantics, helper/team/global ownership, pause layering, and full MUGEN/IKEMEN no-KO parity.
- Latest R2 active-controller ownership cut: `RuntimeActiveControllerRunWorld` now owns bounded imported active-controller scan-to-dispatch orchestration outside `PlayableMatchRuntime`: owner-backed state lookup, trigger pass handoff, `ignorehitpause` filtering, compiled controller dispatch creation, route execution, and stop-on-state-change pass through one bridge before concrete state/runtime/side-effect hooks run. Focused coverage proves owner-backed routing and hitpause-only filtering. Claim allowed: current active-controller run orchestration has a named, focused-tested boundary without trace drift. Claim blocked: new controller semantics, exact CNS VM timing, persistent-controller semantics, helper/team/redirect scopes, side-effect ordering parity, unsupported-feature breadth, visual/audio parity, score movement, and full active-controller parity.
- Previous AssertSpecial guard-deny order cut: required `synthetic-imported-assertspecial-guarddeny.json` checksum `fd2eb239`, `synthetic-imported-assertspecial-crouch-guarddeny.json` checksum `757cb87a`, and `synthetic-imported-assertspecial-air-guarddeny.json` checksum `810a1679` now gate ordered controller-event evidence where defender-side stand/crouch/air `AssertSpecial` executes before the attacker-side guardable `HitDef`. Claim allowed: the current two-player imported route applies bounded guard-deny flags before direct-hit guard resolution. Claim blocked: exact guard priority, lifetime/persistence layering, pause interaction, helper/team/global ownership, KFM/Common1 confirmation, and full MUGEN/IKEMEN guard parity.
- Next recommended gate: broader recovery parity, deeper controller/VM tick-order coverage beyond the current bounded named-controller/typed-operation sequences, exact FightFX/common render lookup/layering/timing/scale/palette/audio mixing beyond first-pass package `fight.def`/AIR/SFF/SND loading, prefixed SND lookup, character `[Files] fx` prefix package selection, decoded provider registration/lookup, bounded AIR/SFF axis binding, source-frame plus selected-frame/multi-frame trace metadata, and frame/axis QA diagnostics, or a broader guard/AssertSpecial/custom-state ownership confirmation. Threshold handoff, optional KFM ordered-threshold confirmation, bounded actor-frame tick order, bounded named controller/operation order, required synthetic stand get-hit progression controller/frame order, required synthetic auto guard-start/end controller-order evidence, required synthetic guard-hit actor-frame telemetry, bounded optional KFM guard-hit and auto guard-start/end controller order, bounded official KFM guard-hit physics frame telemetry, required hit/guard sound plus hit/guard spark telemetry, required common/FightFX hit and guard source-frame plus selected-frame/multi-frame metadata trace gates, player AIR HitSpark rendering/source diagnostics with smoke evidence, bounded common/FightFX system lookup-frame tests, package-frame handoff tests, first-pass real fight.def/FightFX/common AIR/SFF/SND loading/provider/lookup tests, bounded character FightFX prefix package loading/selection tests, bounded HitBy accept/reject evidence, bounded target-owned custom-state evidence, bounded synthetic air/ground velocity, required bounded SelfAnimExist own-action lookup, and required bounded AnimTime active-state animation-end routing now have evidence; exact parity remains blocked.
- Alternate recommended gate: broader `AssertSpecial` priority/KFM confirmation/pause layering, or a narrower raw-controller family promotion to required trace evidence.
- Add required traces for controller families currently covered only by unit/runtime tests.
- Previous completed cut: `synthetic-imported-control.json` gates partial static `CtrlSet` typed control evidence and final owner-control telemetry with checksum `80c4c446`; previous `synthetic-imported-kinematic.json` gates partial static `VelSet` / `VelAdd` / `VelMul` / `PosSet` / `PosAdd` typed kinematic evidence and bounded actor position/velocity telemetry with checksum `92804390`.
- Continue shrinking raw controller fallback paths into typed `ControllerOp` execution.
- Move mutable behavior behind named systems before adding broader parity claims.
- Prefer KFM/Common1 precision when a synthetic gate already exists for the same family.

## Acceptance

- Code has focused tests or a required `pnpm qa:trace` artifact.
- Trace checksum drift is intentional and documented.
- `docs/CONTROLLER_SUPPORT_REGISTRY.md`, `docs/SUPPORTED_FEATURES.md`, `docs/WORKPLAN.md`, and `docs/BUILD_EXECUTION_BACKLOG.md` are updated.
- `docs/ROADMAP_EXECUTION_BOARD.md` is updated if R1/R2 queue status changes.
- Claim allowed / claim blocked wording is explicit.

## Blocked Claims

- Full CNS VM parity.
- Full helper/custom-state/redirect/team ownership.
- Full IKEMEN runtime behavior.
- Exact MUGEN/IKEMEN player tick order, persistent-controller timing, helper/team/redirect actor advance semantics, recovery/stun/physics arbitration, visual parity, or full runtime VM parity from the `RuntimeFighterAdvanceWorld` ownership cut.
- Exact tick order beyond the bounded actor-frame and named controller/operation sequences without fixture-backed evidence.
- Exact CNS VM timing, persistent-controller semantics, helper/team/redirect scopes, side-effect ordering parity, missing-action fallback parity, target/combat/presentation semantic parity, or full active-controller parity from the `RuntimeActiveControllerDispatchWorld` ownership cut.
- Full round/lifebar/team/screenpack parity from the bounded `RuntimeRoundSystem` ownership cut.
- Exact KO slowdown, time-over duration, intro/winpose, round transition, lifebar, team, simul/tag/turns, and screenpack parity from the bounded `synthetic-imported-round-ko` / `synthetic-imported-round-timeover` trace gates.
- Full target redirect/team/helper ownership or exact MUGEN/IKEMEN target semantics from the `RuntimeTargetWorld.snapshotRuntimeState` ownership cut.
- Helper/projectile target ownership, exact multi-target semantics, throw binding, target lifetime parity, or full CNS VM parity from the `RuntimeTargetControllerDispatchWorld` ownership cut.
- Exact combo lifetime, helper/projectile contact ownership, guard-count parity, projectile multi-target counters, or full CNS VM parity from the `RuntimeContactControllerDispatchWorld` ownership cut.
- Exact proximity guard, guard-end timing, guard effects, air-guard landing, helper/team/redirect guard ownership, Common1 controller-loop parity, or full MUGEN/IKEMEN guard VM parity from the `RuntimeGuardWorld` ownership cut.
- Exact proximity guard, guard-end timing, guard effects, air-guard landing, helper/team/redirect guard ownership, Common1 controller-loop parity, or full MUGEN/IKEMEN guard VM parity from the `RuntimeGuardDistanceWorld` ownership cut.
- Exact intra-tick audio/spark ordering, SND playback/mixing/channel priority, exact FightFX/common lookup/binding/layering/timing/scale/palette, helper-owned contact presentation, multi-target presentation, or full presentation parity from the `RuntimeContactPresentationWorld` ownership cut.
- Exact Common1 get-hit animation selection, custom-state ownership breadth, helper/team/redirect get-hit routing, projectile-specific get-hit parity, full hit/fall/recovery VM tick order, or full MUGEN/IKEMEN get-hit parity from the `RuntimeGetHitStateWorld` ownership cut.
- Exact throws, helper/root/parent redirects, team ownership, bind/tick-order parity, projectile-specific custom-state breadth, exact custom-state VM semantics, or full MUGEN/IKEMEN custom-state parity from the `RuntimeHitStateTransitionWorld` ownership cut.
- Exact StateDef lookup edge cases, redirects beyond the current owner hook, helper/root/parent/team lookup, state-entry mutation, IKEMEN state lookup extensions, or full CNS VM state ownership from the `RuntimeStateAvailabilityWorld` ownership cut.
- Exact MUGEN/IKEMEN hitstun, guard recovery thresholds, Common1 hit/guard stun tick order, helper/custom-state stun ownership, pause interaction, or full guard/hitstun semantics from the `RuntimeStunWorld` ownership cut.
- New move semantics, exact active-move input/cancel timing, exact MUGEN/IKEMEN active-move lifecycle parity, or score movement from the `RuntimeMoveLifecycleWorld` ownership cut.
- New input semantics, exact command timing, exact AI behavior parity, full MUGEN/IKEMEN control routing, AILevel support, or score movement from the `RuntimeInputControlWorld` ownership cut.
- Exact MUGEN physics, `yaccel` constants, landing timing, air recovery parity, helper physics ownership, full MUGEN/IKEMEN physics parity, or score movement from the `RuntimeKinematicsWorld` ownership cut.
- Exact AIR negative-duration semantics, `elem` / `elemtime` parity, state-owner namespace behavior, controller tick-order parity, full MUGEN/IKEMEN animation VM parity, or score movement from the `RuntimeAnimationWorld` ownership cut.
- Exact CNS `Time` tick-order parity, persistent-controller timing parity, pause/hitpause timing changes, helper/team/redirect state clocks, or score movement from the `RuntimeStateClockWorld` ownership cut.
- Exact state-entry tick order, redirects/helper/team previous-state ownership, persistent controller semantics, full `ChangeState` / `SelfState` parity, or score movement from the `RuntimeStateMetadataWorld` ownership cut.
- Exact `AssertSpecial` persistence layering, global/team/helper ownership, pause interaction, flag priority, and full MUGEN/IKEMEN `AssertSpecial` parity from the `RuntimeAssertSpecialWorld` ownership cut.
- Exact stage/motif camera logic, effect actor VM semantics, effect tick-order parity, compatibility session projection, target semantics, renderer parity, screenpack ownership, and full MUGEN/IKEMEN snapshot/camera/effect parity from the `RuntimeSnapshotWorld` ownership cut.
- New controller semantics, exact CNS VM timing, broad imported runtime parity, or full MUGEN/IKEMEN compatibility from the `RuntimeCompatibilityTelemetryWorld` ownership cut.
- Persistent controller semantics, helper-owned hitpause counters, broad side-effect ordering during hitpause, exact first-frame decrement order, or full MUGEN/IKEMEN hitpause tick parity from the `RuntimeHitPauseWorld` ownership cut.
- Exact MUGEN/IKEMEN pause layering, super backgrounds, helper VM pause behavior, pause-specific helper/explod/projectile VM ownership, rollback timing, or full pause parity from the `RuntimePausedMatchWorld` ownership cut.
- Exact MUGEN/IKEMEN pause layering, super backgrounds, sound/spark timing, helper VM pause behavior, or rollback/netplay timing from the `RuntimePauseWorld` ownership cut.
- Exact pause layering, super background/sound/spark timing, helper/redirect ownership, full CNS/pause VM parity, or score movement from the `RuntimePauseControllerDispatchWorld` ownership cut.
- Ordinary controllers without `ignorehitpause`, persistent controller semantics, helper-owned hitpause counters, broad side-effect ordering during hitpause beyond the required player advance/freeze trace, and exact hitpause tick-order parity from the bounded `HitPauseTime` / `ignorehitpause` active-state controller cut.
- Exact EnvShake pause/stage/layer interaction, helper/redirect ownership, waveform parity, or full presentation parity from the `RuntimeEnvShakeWorld` ownership cut.
- Exact ReversalDef priority, guard/projectile/helper/custom-state counter breadth, attr grammar, trigger lifetime, hitpause/tick order, full ReversalDef VM parity, or score movement from the `RuntimeReversalControllerDispatchWorld` ownership cut.
- Exact effect spawn tick order, helper-owned effect namespaces, dynamic effect params, helper-owned projectile combat/contact/target memory, full effect/helper/projectile VM parity, or score movement from the `RuntimeEffectSpawnControllerDispatchWorld` ownership cut.
- Exact waveform, pause/stage/layer interaction, helper/redirect ownership, full CNS/presentation VM parity, or score movement from the `RuntimeFallEnvShakeControllerDispatchWorld` ownership cut.
- Exact waveform, pause/stage/layer interaction, helper/redirect ownership, full CNS/presentation VM parity, or score movement from the `RuntimeEnvShakeControllerDispatchWorld` ownership cut.
- Exact audio timing, mixing, channel priority/arbitration, loops, pan, volume, helper/redirect ownership, or full MUGEN/IKEMEN sound parity from the `RuntimeAudioWorld` ownership cut.
- Exact SND playback, channel priority, mixing, FightFX/common fallback, helper/redirect audio ownership, full CNS/audio VM parity, or score movement from the `RuntimeAudioControllerDispatchWorld` ownership cut.
- Exact HitDef sound timing, channel priority, FightFX/system sound fallback, exact FightFX/common hit/guard spark lookup/binding/layering/timing/scale/palette, motif/screenpack ownership, or full HitDef effect parity from the bounded `synthetic-imported-hitdef-hit-sound` / `synthetic-imported-hitdef-guard-sound` / `synthetic-imported-hitdef-hit-spark` / `synthetic-imported-hitdef-guard-spark` / `synthetic-imported-hitdef-data-spark` / `synthetic-imported-hitdef-data-guard-spark` / `synthetic-imported-hitdef-common-spark` / `synthetic-imported-hitdef-fightfx-spark` / `synthetic-imported-hitdef-common-guard-spark` / `synthetic-imported-hitdef-fightfx-guard-spark` / `synthetic-imported-hitdef-guard-effect-package` / `synthetic-imported-hitdef-hit-effect-package` telemetry cuts, fallback `HitSparkRenderer`, and first-pass package `fight.def`/AIR/SFF/SND loading/provider route.
- Exact EnvColor blend math, layer/window ordering, pause timing, renderer parity, or full MUGEN/IKEMEN presentation parity from the `RuntimeEnvColorWorld` ownership cut.
- Exact EnvColor blend math, layer/window ordering, pause timing, renderer parity, helper/redirect presentation ownership, full CNS/presentation VM parity, or score movement from the `RuntimeEnvColorControllerDispatchWorld` ownership cut.
- Exact sprite material math, draw-order parity, trail sampling cadence, helper/redirect ownership, palette/remap interaction, renderer parity, or full MUGEN/IKEMEN presentation parity from the `RuntimeSpriteEffectWorld` ownership cut.
- Exact player/edge collision, team/helper push behavior, screen-edge/camera parity, PosFreeze tick order, Width edge semantics, or full MUGEN/IKEMEN actor-constraint parity from the `RuntimeActorConstraintWorld` ownership cut.
- Exact player/edge collision, team/helper push behavior, screen-edge/camera parity, PosFreeze tick order, Width edge semantics, full CNS/constraint VM parity, or score movement from the `RuntimeActorConstraintControllerDispatchWorld` ownership cut.
- Exact player/edge collision, team/helper push behavior, screen-edge/camera parity, PosFreeze tick order, full constraint VM parity, or score movement from the `RuntimeBoundsControllerWorld` passive bounds-controller ownership cut.
- Exact direct-combat priority classes beyond bounded win/trade, hit/guard controller order, throws, custom-state breadth, multi-hit/combo lifetime, helper/team/redirect combat ownership, or full MUGEN/IKEMEN direct-combat parity from the `RuntimeDirectCombatWorld` ownership cuts.
- Exact Common1 controller-loop order, helper/team/redirect ownership, exact recovery thresholds/velocity math, or full fall/get-hit parity from the `RuntimeHitFallControllerWorld` passive HitFall controller ownership cut.
- Dynamic metadata expressions, helper/team/redirect ownership, exact physics/tick-order interactions, or full StateTypeSet parity from the `RuntimeStateTypeWorld` passive metadata setup ownership cut.
- Exact MUGEN/IKEMEN damage scaling stack/order, helper/projectile/custom-state/guard edge cases, redirect ownership, controller-loop timing, or full AttackMulSet/DefenceMulSet parity from the `RuntimeDamageScaleWorld` passive multiplier setup ownership cut.
- Exact attr grammar, slot priority, helper/custom-state redirect breadth, forceair/forceguard edge order, controller-loop timing, or full HitBy/NotHitBy/HitOverride parity from the `RuntimeHitDefenseWorld` passive slot setup ownership cut.
- Helper-parented Projectile/custom-state HitOverride slot priority, attr grammar, helper/custom-state redirect breadth, projectile/custom-state edge timing, forceair/forceguard edge order, or full MUGEN/IKEMEN HitOverride parity from the `RuntimeHitOverrideWorld` ownership cut.
- Exact ReversalDef priority, guard/projectile/helper/custom-state counter breadth, attr grammar, trigger lifetime, hitpause/tick order, or full MUGEN/IKEMEN ReversalDef parity from the `RuntimeReversalWorld` ownership cut.
- Exact projectile priority classes, multi-target projectile behavior, helper-owned projectile combat, exact projectile contact-trigger lifetime, terminal-animation timing, guard effects, pause layering, or full MUGEN/IKEMEN projectile parity from the `RuntimeProjectileCombatWorld` ownership cut.
- Exact Explod/Helper/Projectile spawn timing, helper VM, helper-owned projectiles, exact parent/root/redirect semantics, FightFX/common animation routing, exact bind/removal triggers, or full MUGEN/IKEMEN effect lifecycle parity from the `RuntimeEffectSpawnWorld` ownership cut.
- Exact `ModifyExplod` dynamic params, position rebinding, helper-owned effect namespaces, FightFX/common routing, remove-trigger parity, exact tick order, or full Explod lifecycle parity from the bounded `synthetic-imported-modifyexplod` and `synthetic-imported-helper-modifyexplod` cuts.
- Device force feedback, debug text rendering, clipboard output, dust rendering, helper removal/lifecycle side effects, no-op side effects, helper/redirect ownership, persistent/no-op timing edge cases, exact CNS VM order, or full MUGEN/IKEMEN parity from the bounded `synthetic-imported-noop` cut.
- Exact camera waveform, pause/stage/layer interaction, helper/redirect ownership, and full MUGEN/IKEMEN fall presentation parity from the bounded FallEnvShake event requirement on `synthetic-imported-common-gethit`.
- Exact helper VM lifecycle, exact post-fighter tick order, exact effect pause/combat ordering, exact `removeongethit` trigger timing across every custom-state/helper/projectile path, exact parent/root/redirect ownership, or full MUGEN/IKEMEN interaction/effect lifecycle parity from the `RuntimeMatchInteractionWorld` and `RuntimeEffectLifecycleWorld` ownership cuts.
